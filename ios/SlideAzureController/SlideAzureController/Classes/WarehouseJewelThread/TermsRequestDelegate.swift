
//: Declare String Begin

/*: "APPLE_IAP_PAY_SUCCEED_NOTIFICATION" :*/
fileprivate let noti_recoverPath:[UInt8] = [0xc3,0xd2,0xd2,0xce,0xc7,0xdd,0xcb,0xc3,0xd2,0xdd,0xd2,0xc3,0xdb,0xdd,0xd1,0xd7,0xc1,0xc1,0xc7,0xc7,0xc6,0xdd,0xcc,0xcd,0xd6,0xcb,0xc4,0xcb,0xc1,0xc3,0xd6,0xcb,0xcd,0xcc]

private func streamExist(coat num: UInt8) -> UInt8 {
    return num ^ 130
}

/*: "APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION" :*/
fileprivate let appPublishContent:[UInt8] = [0x6f,0x7e,0x7e,0x62,0x6b,0x71,0x67,0x6f,0x7e,0x71,0x7d,0x7b,0x6c,0x7d,0x6d,0x7c,0x67,0x6c,0x6b,0x71,0x7d,0x7b,0x6d,0x6d,0x6b,0x6b,0x6a,0x71,0x60,0x61,0x7a,0x67,0x68,0x67,0x6d,0x6f,0x7a,0x67,0x61,0x60]

private func currentChannel(photo num: UInt8) -> UInt8 {
    return num ^ 46
}

/*: "mf/recharge/createApplePay" :*/
fileprivate let showBitTitle:[Character] = ["m","f"]
fileprivate let app_exitMessage:String = "/rechmonth strike professional rem"
fileprivate let user_distinctiveContent:String = "cwarma"
fileprivate let const_centerRelateBetweenFormat:String = "salt swing our pull normalePay"

/*: "productId" :*/
fileprivate let dataPanTitle:String = "right guidance merge recording doproductId"

/*: "source" :*/
fileprivate let const_calledData:String = "ssubjectrce"

/*: "IAP充值创建订单失败: :*/
fileprivate let userVitalAgainMistData:String = "IAP充值创\u{5efa}"
fileprivate let kNorSName:[Character] = ["订","\u{5355}","失","败",":"]

/*: "orderNum" :*/
fileprivate let k_sortMessage:[Character] = ["o","r","d","e","r","N","u","m"]

/*: "mf/recharge/applePayNotify" :*/
fileprivate let main_buildMessage:[Character] = ["m","f","/","r","e","c","h","a","r","g","e","/","a","p","p","l","e","P","a","y","N","o"]
fileprivate let main_argumentName:[Character] = ["t","i","f","y"]

/*: "IAP充值校验失败: :*/
fileprivate let kContainerUrl:String = "IAP充值校difference message over text baseball"

/*: "transactionId" :*/
fileprivate let app_tradeData:String = "TRAN"
fileprivate let mainLifeBeastUrl:[Character] = ["s","a","c","t","i","o","n","I","d"]

/*: "reportMoney" :*/
fileprivate let k_minValue:[Character] = ["r","e","p","o","r","t","M","o","n","e","y"]

/*: "mf/AutoSub/AppleCreateOrder" :*/
fileprivate let noti_hairName:String = "mf/Autdrown bride only properly person"
fileprivate let kBugData:String = "AppleCmeeting sign week"
fileprivate let app_splitMsg:String = "big"

/*: "IAP订阅创建订单失败: :*/
fileprivate let mainStateName:[Character] = ["I","A","P","订","阅","创","建","订","\u{5355}","失","败",":"]

/*: "orderId" :*/
fileprivate let constBeginId:[UInt8] = [0x92,0x8f,0x99,0x98,0x8f,0xb4,0x99]

private func motivePersonality(hung num: UInt8) -> UInt8 {
    return num ^ 253
}

/*: "mf/AutoSub/ApplePaySuccess" :*/
fileprivate let main_barrierRichFormat:String = "mf/Autfollowing primary sight"
fileprivate let notiDistrictName:String = "/Applepic used threshold"
fileprivate let showProviderValue:String = "uevaluationevaluatione"
fileprivate let data_nutValue:String = "impressionimpression"

/*: "IAP订阅校验失败: :*/
fileprivate let main_judgeUrl:[Character] = ["I","A","P","\u{8ba2}","阅"]
fileprivate let k_locationTitle:[Character] = ["校","验","失","败",":"]

/*: "App" :*/
fileprivate let user_dateData:String = "fore assist peer normalApp"

/*: "OrderTransactionInfo_ :*/
fileprivate let k_accuracyFormat:String = "slip ski shape electronic honeyOrder"
fileprivate let k_awakeMsg:String = "smomenttio"

/*: "OrderTransactionInfo_Subscribe_ :*/
fileprivate let const_shineUrl:[UInt8] = [0x20,0x1d,0xb,0xa,0x1d,0x3b,0x1d,0xe,0x1,0x1c,0xe,0xc,0x1b,0x6,0x0,0x1,0x26,0x1,0x9,0x0,0x30,0x3c,0x1a,0xd,0x1c,0xc,0x1d,0x6,0xd,0xa,0x30]

private func documentWheel(used num: UInt8) -> UInt8 {
    return num ^ 111
}

/*: "verifyData" :*/
fileprivate let userSaltTitle:[UInt8] = [0xcf,0xbe,0xcb,0xc2,0xbf,0xd2,0x9d,0xba,0xcd,0xba]

fileprivate func coalTar(input num: UInt8) -> UInt8 {
    let value = Int(num) - 89
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "IAP查询内购商品失败, productId: :*/
fileprivate let mainErrRouteRegularMsg:String = "IAP\u{67e5}询"
fileprivate let show_welcomeMessage:String = ", prodscreen act demand"
fileprivate let app_weddingFormat:String = ":"

/*: "IAP查询内购商品失败, products.count为0." :*/
fileprivate let noti_focusUrl:String = "IAP查face engineering baseball depth"
fileprivate let show_closePromptName:String = "品\u{5931}败, pro"
fileprivate let k_rangeUrl:String = "DUCT"
fileprivate let k_carePath:String = "nt为0.continue second portrait"

/*: "IAP查询内购商品失败, Error: :*/
fileprivate let kMakerPersonData:String = "IAP\u{67e5}"
fileprivate let noti_giveStr:String = "\u{54c1}失败,"

/*: "IAP-purchased: :*/
fileprivate let kAssignStr:String = "i"
fileprivate let data_bearId:String = "AP-puoriginal rich female"

/*: , 订单号: :*/
fileprivate let appUpRegionText:String = ", \u{8ba2}单号:"

/*: , 商品Id: :*/
fileprivate let noti_readyUrl:String = ", \u{5546}品Id"
fileprivate let appMarginUrl:String = "filter"

/*: , 苹果transId: :*/
fileprivate let user_southwestData:String = ",all"
fileprivate let const_shapeSideText:String = "苹果tratransaction even capacity"

/*: "IAP-failed: :*/
fileprivate let mainFlagJointId:[Character] = ["I","A","P","-","f","a"]
fileprivate let noti_factContent:[Character] = ["i","l","e","d",":"]

/*: "IAP-deferred: :*/
fileprivate let noti_automaticallyFormat:String = "Inut"
fileprivate let mainMasterMessage:[Character] = ["P","-","d","e","f","e","r","r","e","d",":"]

/*: "IAP未知交易类型: :*/
fileprivate let show_yearMessage:[Character] = ["I","A","P","未","知","交","易"]
fileprivate let main_speakerUrl:[Character] = ["\u{7c7b}","型",":"]

/*: " 未知的交易类型" :*/
fileprivate let show_movieAlterMistUrl:String = " 未知的交易类shade"

/*: "IAP获取本地收据数据失败, transactionId: :*/
fileprivate let kDismissMessage:String = "IAP获取sum without she board admin"
fileprivate let data_localPath:[Character] = ["\u{5931}","\u{8d25}",","," ","t","r","a","n","s","a","c","t","i","o","n","I","d",":"]

/*: , orderId: :*/
fileprivate let user_nothingStr:[Character] = [","," ","o","r","d","e","r","I","d",":"]

/*: , payType: :*/
fileprivate let const_mainPathMessage:[Character] = [","," ","p"]
fileprivate let show_remoteTitle:[Character] = ["a","y","T","y","p","e",":"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  TermsRequestDelegate.swift
//  AbroadTalking
//
//  Created by young on 2022/9/2.
//

//: import StoreKit
import StoreKit
//: import SwiftyJSON
import SwiftyJSON
//: import UIKit
import UIKit

// 苹果支付成功通知
//: public let APPLE_IAP_PAY_SUCCEED_NOTIFICATION = NSNotification.Name(rawValue: "APPLE_IAP_PAY_SUCCEED_NOTIFICATION")
public let kFilterId = NSNotification.Name(rawValue: String(bytes: noti_recoverPath.map{streamExist(coat: $0)}, encoding: .utf8)!)
// 苹果订阅成功通知
//: public let APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION = NSNotification.Name(rawValue: "APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION")
public let notiThirdPointMsg = NSNotification.Name(rawValue: String(bytes: appPublishContent.map{currentChannel(photo: $0)}, encoding: .utf8)!)
// 最大失败重试次数
//: let APPLE_IAP_MAX_RETRY_COUNT = 9
let kFirstFormat = 9

/// 支付类型
//: enum ApplePayType {
enum DismissType {
    //: case Pay
    case Pay // 支付
    //: case Subscribe
    case Subscribe // 订阅
}

/// 支付状态
//: enum AppleIAPStatus: String {
enum ArmDefaultsSerializable: String {
    //: case unknow            = "未知类型"
    case unknow = "未知类型"
    //: case createOrderFail   = "创建订单失败"
    case createOrderFail = "创建订单失败"
    //: case notArrow          = "设备不允许"
    case notArrow = "设备不允许"
    //: case noProductId       = "缺少产品Id"
    case noProductId = "缺少产品Id"
    //: case failed            = "交易失败/取消"
    case failed = "交易失败/取消"
    //: case restored          = "已购买过该商品"
    case restored = "已购买过该商品"
    //: case deferred          = "交易延期"
    case deferred = "交易延期"
    //: case verityFail        = "服务器验证失败"
    case verityFail = "服务器验证失败"
    //: case veritySucceed     = "服务器验证成功"
    case veritySucceed = "服务器验证成功"
    //: case renewSucceed      = "自动续订成功"
    case renewSucceed = "自动续订成功"
}

//: typealias IAPcompletionHandle = (AppleIAPStatus, Double, ApplePayType) -> Void
typealias IAPcompletionHandle = (ArmDefaultsSerializable, Double, DismissType) -> Void

//: class AppleIAPManager: NSObject {
class TermsRequestDelegate: NSObject {
    //: var completionHandle: IAPcompletionHandle?
    var completionHandle: IAPcompletionHandle?
    //: private var productInfoReq: SKProductsRequest?
    private var productInfoReq: SKProductsRequest?
    //: private var reqRetryCountDict = [String: Int]()
    private var reqRetryCountDict = [String: Int]() // 记录每个交易请求重试次数
    //: private var payCacheList = [[String: String]]()
    private var payCacheList = [[String: String]]() // 【购买】缓存数据
    //: private var subscribeCacheList = [[String: String]]()
    private var subscribeCacheList = [[String: String]]() // 【订阅】缓存数据
    //: private var createOrderId: String?
    private var createOrderId: String? // 当前支付服务端创建的订单id
    //: private var currentPayType: ApplePayType = .Pay
    private var currentPayType: DismissType = .Pay // 当前支付类型

    // singleton
    //: static let shared = AppleIAPManager()
    static let shared = TermsRequestDelegate()
    //: override func copy() -> Any { return self }
    override func copy() -> Any { return self }
    //: override func mutableCopy() -> Any { return self }
    override func mutableCopy() -> Any { return self }
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        //: SKPaymentQueue.default().add(self as SKPaymentTransactionObserver)
        SKPaymentQueue.default().add(self as SKPaymentTransactionObserver)
        // 监听应用将要销毁
        //: NotificationCenter.default.addObserver(self, selector: #selector(appWillTerminate),
        NotificationCenter.default.addObserver(self, selector: #selector(positiveRound),
                                               //: name: UIApplication.willTerminateNotification,
                                               name: UIApplication.willTerminateNotification,
                                               //: object: nil)
                                               object: nil)
    }

    // MARK: - NotificationCenter

    //: @objc func appWillTerminate() {
    @objc func positiveRound() {
        //: SKPaymentQueue.default().remove(self as SKPaymentTransactionObserver)
        SKPaymentQueue.default().remove(self as SKPaymentTransactionObserver)
    }
}

// MARK: - 【苹果购买】业务接口

//: extension AppleIAPManager {
private extension TermsRequestDelegate {
    /// 【购买】创建业务订单
    /// - Parameters:
    ///   - productId: 产品Id
    ///   - block: 回调
    //: fileprivate func req_pay_createAppleOrder(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
    func quest(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/recharge/createApplePay"
        reqModel.requestPath = (String(showBitTitle) + String(app_exitMessage.prefix(5)) + "arge/" + user_distinctiveContent.replacingOccurrences(of: "warm", with: "re") + "teAppl" + String(const_centerRelateBetweenFormat.suffix(4)))
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["productId"] = productId
        dict[(String(dataPanTitle.suffix(9)))] = productId
        //: dict["source"] = source
        dict[(const_calledData.replacingOccurrences(of: "subject", with: "ou"))] = source
        //: reqModel.params = dict
        reqModel.params = dict
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: UploadLogTool.writeLog(msg: "IAP充值创建订单失败:\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                MateThen.trackBy(msg: (userVitalAgainMistData + String(kNorSName)) + "\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                //: handle(nil, succeed)
                handle(nil, succeed)
                //: return
                return
            }

            //: let json = JSON(result!)
            let json = JSON(result!)
            //: let orderId = json["orderNum"].string
            let orderId = json[(String(k_sortMessage))].string
            //: handle(orderId, succeed)
            handle(orderId, succeed)
        }
    }

    /// 【购买】上传支付信息到服务器验证
    /// - Parameters:
    ///   - transaction: 交易信息
    ///   - params: 接口参数
    //: fileprivate func req_pay_uploadAppletransaction(_ transactionId: String, params: [String: String]) {
    func perpendicular(_ transactionId: String, params: [String: String]) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/recharge/applePayNotify"
        reqModel.requestPath = (String(main_buildMessage) + String(main_argumentName))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true || errorModel?.errorCode == 405 else {
            guard succeed == true || errorModel?.errorCode == 405 else { // 验证接口失败，重试接口
                //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2) {
                DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2) {
                    //: UploadLogTool.writeLog(msg: "IAP充值校验失败:\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    MateThen.trackBy(msg: (String(kContainerUrl.prefix(6)) + "\u{9a8c}失败:") + "\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    //: self.transcationPurchasedToCheck(transactionId, .Pay)
                    self.purchasedSelect(transactionId, .Pay)
                }
                //: return
                return
            }

            //: let result = JSON(result ?? [:])
            let result = JSON(result ?? [:])

            // 过滤已验证成功的订单数据
            //: let newPayCacheList = self.payCacheList.filter({$0["transactionId"] != transactionId})
            let newPayCacheList = self.payCacheList.filter { $0[(app_tradeData.lowercased() + String(mainLifeBeastUrl))] != transactionId }
            //: let diskPath = self.getPayCachePath()
            let diskPath = self.greetFor()
            //: NSKeyedArchiver.archiveRootObject(newPayCacheList, toFile: diskPath)
            NSKeyedArchiver.archiveRootObject(newPayCacheList, toFile: diskPath)

            // 成功通知
            //: NotificationCenter.default.post(name: APPLE_IAP_PAY_SUCCEED_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: kFilterId, object: nil)
            // 成功回调
            //: self.completionHandle?(.veritySucceed, result["reportMoney"].doubleValue, .Pay)
            self.completionHandle?(.veritySucceed, result[(String(k_minValue))].doubleValue, .Pay)
        }
    }
}

// MARK: - 【苹果订阅】业务接口

//: extension AppleIAPManager {
private extension TermsRequestDelegate {
    /// 【订阅】创建业务订单
    /// - Parameters:
    ///   - productId: 产品Id
    ///   - block: 回调
    //: fileprivate func req_subscribe_createAppleOrder(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
    func linguisticNameProcess(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/AutoSub/AppleCreateOrder"
        reqModel.requestPath = (String(noti_hairName.prefix(6)) + "oSub/" + String(kBugData.prefix(6)) + "reateOrd" + app_splitMsg.replacingOccurrences(of: "big", with: "er"))
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["productId"] = productId
        dict[(String(dataPanTitle.suffix(9)))] = productId
        //: dict["source"] = source
        dict[(const_calledData.replacingOccurrences(of: "subject", with: "ou"))] = source
        //: reqModel.params = dict
        reqModel.params = dict
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: UploadLogTool.writeLog(msg: "IAP订阅创建订单失败:\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                MateThen.trackBy(msg: (String(mainStateName)) + "\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                //: handle(nil, succeed)
                handle(nil, succeed)
                //: return
                return
            }

            //: let json = JSON(result!)
            let json = JSON(result!)
            //: let orderId = json["orderId"].string
            let orderId = json[String(bytes: constBeginId.map{motivePersonality(hung: $0)}, encoding: .utf8)!].string
            //: handle(orderId, succeed)
            handle(orderId, succeed)
        }
    }

    /// 【订阅】上传支付信息到服务器验证
    /// - Parameters:
    ///   - transaction: 交易信息
    ///   - params: 接口参数
    //: fileprivate func req_subscribe_uploadAppletransaction(_ transactionId: String, params: [String: String]) {
    func shadowParams(_ transactionId: String, params: [String: String]) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/AutoSub/ApplePaySuccess"
        reqModel.requestPath = (String(main_barrierRichFormat.prefix(6)) + "oSub" + String(notiDistrictName.prefix(6)) + "PayS" + showProviderValue.replacingOccurrences(of: "evaluation", with: "c") + data_nutValue.replacingOccurrences(of: "impression", with: "s"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true || errorModel?.errorCode == 405 else {
            guard succeed == true || errorModel?.errorCode == 405 else { // 验证接口失败，重试接口
                //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 3) {
                DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 3) {
                    //: UploadLogTool.writeLog(msg: "IAP订阅校验失败:\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    MateThen.trackBy(msg: (String(main_judgeUrl) + String(k_locationTitle)) + "\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    //: self.transcationPurchasedToCheck(transactionId, .Subscribe)
                    self.purchasedSelect(transactionId, .Subscribe)
                }
                //: return
                return
            }

            //: let result = JSON(result ?? [:])
            let result = JSON(result ?? [:])

            // 过滤已验证成功的订单数据
            //: let newSubscribeCacheList = self.subscribeCacheList.filter({$0["transactionId"] != transactionId})
            let newSubscribeCacheList = self.subscribeCacheList.filter { $0[(app_tradeData.lowercased() + String(mainLifeBeastUrl))] != transactionId }
            //: let diskPath = self.getSubscribeCachePath()
            let diskPath = self.mFront()
            //: NSKeyedArchiver.archiveRootObject(newSubscribeCacheList, toFile: diskPath)
            NSKeyedArchiver.archiveRootObject(newSubscribeCacheList, toFile: diskPath)

            // 成功通知
            //: NotificationCenter.default.post(name: APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: notiThirdPointMsg, object: nil)
            // 成功回调
            //: self.completionHandle?(.veritySucceed, result["reportMoney"].doubleValue, .Subscribe)
            self.completionHandle?(.veritySucceed, result[(String(k_minValue))].doubleValue, .Subscribe)
        }
    }
}

// MARK: - Event

//: extension AppleIAPManager {
extension TermsRequestDelegate {
    /// 初始化数据
    //: private func iap_initData() {
    private func alwaysTarget() {
        //: self.payCacheList = getLocalPayCacheList(payType: .Pay)
        self.payCacheList = shadowType(payType: .Pay)
        //: self.subscribeCacheList = getLocalPayCacheList(payType: .Subscribe)
        self.subscribeCacheList = shadowType(payType: .Subscribe)
        //: self.createOrderId = nil
        self.createOrderId = nil
    }

    /// 获取缓存列表
    /// - Parameter payType: 支付类型
    /// - Returns: 缓存列表
    //: private func getLocalPayCacheList(payType: ApplePayType) -> [[String: String]] {
    private func shadowType(payType: DismissType) -> [[String: String]] {
        //: var list: [[String: String]]?
        var list: [[String: String]]?
        //: var diskPath = ""
        var diskPath = ""
        //: if payType == .Pay {
        if payType == .Pay {
            //: diskPath = getPayCachePath()
            diskPath = greetFor()
            //: } else {
        } else {
            //: diskPath = getSubscribeCachePath()
            diskPath = mFront()
        }

        //: if FileManager.default.fileExists(atPath: diskPath) {
        if FileManager.default.fileExists(atPath: diskPath) {
            //: list = NSKeyedUnarchiver.unarchiveObject(withFile: diskPath) as? [[String: String]]
            list = NSKeyedUnarchiver.unarchiveObject(withFile: diskPath) as? [[String: String]]
            //: if list == nil {
            if list == nil {
                //: try? FileManager.default.removeItem(atPath: diskPath)
                try? FileManager.default.removeItem(atPath: diskPath)
            }
        }
        //: if list == nil {
        if list == nil {
            //: list = [[String: String]]()
            list = [[String: String]]()
        }
        //: return list!
        return list!
    }

    /// 获取【购买】缓存路径【和uid关联】
    /// - Returns: 缓存路径
    //: private func getPayCachePath() -> String {
    private func greetFor() -> String {
        //: let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent("App")
        let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent((String(user_dateData.suffix(3))))

        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: if fileManager.fileExists(atPath: appDirectoryPath) == false {
        if fileManager.fileExists(atPath: appDirectoryPath) == false {
            //: try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
            try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
        }

        //: let uid = Defaults.string(forKey: TalkingLoginUidCacheKey) ?? ""
        let uid = data_partyText.string(forKey: mainBottomGameContent) ?? ""
        //: let filePath = (appDirectoryPath as NSString).appendingPathComponent("OrderTransactionInfo_\(uid)")
        let filePath = (appDirectoryPath as NSString).appendingPathComponent((String(k_accuracyFormat.suffix(5)) + "Tran" + k_awakeMsg.replacingOccurrences(of: "moment", with: "ac") + "nInfo_") + "\(uid)")
        //: return filePath
        return filePath
    }

    /// 获取【订阅】缓存路径【和uid关联】
    /// - Returns: 缓存路径
    //: private func getSubscribeCachePath() -> String {
    private func mFront() -> String {
        //: let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent("App")
        let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent((String(user_dateData.suffix(3))))

        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: if fileManager.fileExists(atPath: appDirectoryPath) == false {
        if fileManager.fileExists(atPath: appDirectoryPath) == false {
            //: try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
            try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
        }

        //: let uid = Defaults.string(forKey: TalkingLoginUidCacheKey) ?? ""
        let uid = data_partyText.string(forKey: mainBottomGameContent) ?? ""
        //: let filePath = (appDirectoryPath as NSString).appendingPathComponent("OrderTransactionInfo_Subscribe_\(uid)")
        let filePath = (appDirectoryPath as NSString).appendingPathComponent(String(bytes: const_shineUrl.map{documentWheel(used: $0)}, encoding: .utf8)! + "\(uid)")
        //: return filePath
        return filePath
    }

    /// 获取本地收据数据
    /// - Parameters:
    ///   - transactionId: 收据标识符
    ///   - payType: 支付类型
    /// - Returns: 收据数据
    //: fileprivate func getVerifyData(_ transactionId: String, _ payType: ApplePayType) -> String? {
    fileprivate func fromPer(_ transactionId: String, _ payType: DismissType) -> String? {
        // 有未完成的订单，先取缓存
        //: var paramsArr = [[String: String]]()
        var paramsArr = [[String: String]]()
        //: switch(payType) {
        switch payType {
        //: case .Pay:
        case .Pay:
            //: paramsArr = self.payCacheList.filter({$0["transactionId"] == transactionId})
            paramsArr = self.payCacheList.filter { $0[(app_tradeData.lowercased() + String(mainLifeBeastUrl))] == transactionId }
        //: case .Subscribe:
        case .Subscribe:
            //: paramsArr = self.subscribeCacheList.filter({$0["transactionId"] == transactionId})
            paramsArr = self.subscribeCacheList.filter { $0[(app_tradeData.lowercased() + String(mainLifeBeastUrl))] == transactionId }
        }
        //: if paramsArr.count > 0 && paramsArr.first!["verifyData"] != nil {
        if paramsArr.count > 0, paramsArr.first![String(bytes: userSaltTitle.map{coalTar(input: $0)}, encoding: .utf8)!] != nil {
            //: return paramsArr.first!["verifyData"]
            return paramsArr.first![String(bytes: userSaltTitle.map{coalTar(input: $0)}, encoding: .utf8)!]
        }

        // 取本地
        //: guard let receiptUrl = Bundle.main.appStoreReceiptURL else { return nil }
        guard let receiptUrl = Bundle.main.appStoreReceiptURL else { return nil }
        //: let data = NSData(contentsOf: receiptUrl)
        let data = NSData(contentsOf: receiptUrl)
        //: let receiptStr = data?.base64EncodedString(options: NSData.Base64EncodingOptions(rawValue: 0))
        let receiptStr = data?.base64EncodedString(options: NSData.Base64EncodingOptions(rawValue: 0))
        //: return receiptStr
        return receiptStr
    }
}

// MARK: - 失败重试流程

//: extension AppleIAPManager {
extension TermsRequestDelegate {
    /// 检测未完成的苹果支付【只会重试当前登录用户】
    //: func iap_checkUnfinishedTransactions() {
    func evaluateAnalyse() {
        //: iap_initData()
        alwaysTarget()

        // 【购买】失败重试
        //: for dict in self.payCacheList {
        for dict in self.payCacheList {
            //: iap_failedRetry(dict["transactionId"], .Pay)
            unlessEquity(dict[(app_tradeData.lowercased() + String(mainLifeBeastUrl))], .Pay)
        }

        // 【订阅】失败重试
        //: for dict in self.subscribeCacheList {
        for dict in self.subscribeCacheList {
            //: iap_failedRetry(dict["transactionId"], .Subscribe)
            unlessEquity(dict[(app_tradeData.lowercased() + String(mainLifeBeastUrl))], .Subscribe)
        }
    }

    /// 失败重试
    /// - Parameters:
    ///   - transactionId: Id
    ///   - payType: 支付类型
    //: private func iap_failedRetry(_ transactionId: String?, _ payType: ApplePayType) {
    private func unlessEquity(_ transactionId: String?, _ payType: DismissType) {
        //: guard let transactionId = transactionId else { return }
        guard let transactionId = transactionId else { return }
        // 初始化每个交易请求次数
        //: reqRetryCountDict[transactionId] = 0
        reqRetryCountDict[transactionId] = 0
        // 3. 服务端校验流程
        //: transcationPurchasedToCheck(transactionId, payType)
        purchasedSelect(transactionId, payType)
    }
}

// MARK: - 苹果正常支付流程

//: extension AppleIAPManager {
extension TermsRequestDelegate {
    /// 发起苹果支付【1.创建订单； 2.发起苹果支付； 3.服务端校验】
    /// - Parameters:
    ///   - purchID: 产品ID
    ///   - payType: 支付类型
    ///   - handle: 回调
    ///   - source: 0 常规充值 1 观看视频后充值或订阅
    //: func iap_startPurchase(productId: String, payType: ApplePayType, source: Int = 0, handle: @escaping IAPcompletionHandle) {
    func sourceHandle(productId: String, payType: DismissType, source: Int = 0, handle: @escaping IAPcompletionHandle) {
        //: iap_initData()
        alwaysTarget()
        //: self.completionHandle = handle
        self.completionHandle = handle
        //: self.currentPayType = payType
        self.currentPayType = payType

        // 1. 根据类型创建订单
        //: switch(payType) {
        switch payType {
        //: case .Pay:
        case .Pay:
            //: req_pay_createAppleOrder(productId: productId, source: source) { [weak self] orderId, succeed in
            quest(productId: productId, source: source) { [weak self] orderId, succeed in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: guard succeed == true && orderId != nil else {
                guard succeed == true && orderId != nil else { // 订单创建失败
                    //: self.completionHandle?(.createOrderFail, 0, .Pay)
                    self.completionHandle?(.createOrderFail, 0, .Pay)
                    //: return
                    return
                }

                //: self.createOrderId = orderId
                self.createOrderId = orderId
                //: self.requestProductInfo(productId)
                self.resolutionProduct(productId)
            }

        //: case .Subscribe:
        case .Subscribe:
            //: req_subscribe_createAppleOrder(productId: productId, source: source) { [weak self] orderId, succeed in
            linguisticNameProcess(productId: productId, source: source) { [weak self] orderId, succeed in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: guard succeed == true && orderId != nil else {
                guard succeed == true && orderId != nil else { // 订单创建失败
                    //: self.completionHandle?(.createOrderFail, 0, .Subscribe)
                    self.completionHandle?(.createOrderFail, 0, .Subscribe)
                    //: return
                    return
                }

                //: self.createOrderId = orderId
                self.createOrderId = orderId
                //: self.requestProductInfo(productId)
                self.resolutionProduct(productId)
            }
        }
    }

    // 2 发起苹果支付，查询apple内购商品
    //: fileprivate func requestProductInfo(_ productId: String) {
    fileprivate func resolutionProduct(_ productId: String) {
        //: guard SKPaymentQueue.canMakePayments() else {
        guard SKPaymentQueue.canMakePayments() else {
            //: UploadLogTool.writeLog(msg: "IAP查询内购商品失败, productId:\(productId).")
            MateThen.trackBy(msg: (mainErrRouteRegularMsg + "内购\u{5546}品失败" + String(show_welcomeMessage.prefix(6)) + "uctId" + app_weddingFormat.capitalized) + "\(productId).")
            //: self.completionHandle?(.notArrow, 0, currentPayType)
            self.completionHandle?(.notArrow, 0, currentPayType)
            //: return
            return
        }

        // 销毁当前请求
        //: self.clearProductInfoRequest()
        self.eventWithout()
        // 查询apple内购商品
        //: let identifiers: Set<String> = [productId]
        let identifiers: Set<String> = [productId]
        //: productInfoReq = SKProductsRequest(productIdentifiers: identifiers)
        productInfoReq = SKProductsRequest(productIdentifiers: identifiers)
        //: productInfoReq?.delegate = self
        productInfoReq?.delegate = self
        //: productInfoReq?.start()
        productInfoReq?.start()
    }

    // 销毁当前请求
    //: fileprivate func clearProductInfoRequest() {
    fileprivate func eventWithout() {
        //: guard productInfoReq != nil else { return }
        guard productInfoReq != nil else { return }
        //: productInfoReq?.delegate = nil
        productInfoReq?.delegate = nil
        //: productInfoReq?.cancel()
        productInfoReq?.cancel()
        //: productInfoReq = nil
        productInfoReq = nil
    }
}

// MARK: - SKProductsRequestDelegate【商品查询】

//: extension AppleIAPManager: SKProductsRequestDelegate {
extension TermsRequestDelegate: SKProductsRequestDelegate {
    // 查询apple内购商品成功回调
    //: func productsRequest(_ request: SKProductsRequest, didReceive response: SKProductsResponse) {
    func productsRequest(_: SKProductsRequest, didReceive response: SKProductsResponse) {
        //: guard response.products.count > 0 else {
        guard response.products.count > 0 else {
            //: UploadLogTool.writeLog(msg: "IAP查询内购商品失败, products.count为0.")
            MateThen.trackBy(msg: (String(noti_focusUrl.prefix(4)) + "询内购商" + show_closePromptName + k_rangeUrl.lowercased() + "s.cou" + String(k_carePath.prefix(5))))
            //: self.completionHandle?( .noProductId, 0, currentPayType)
            self.completionHandle?(.noProductId, 0, currentPayType)
            //: return
            return
        }

        //: let payment = SKPayment(product: response.products.first!)
        let payment = SKPayment(product: response.products.first!)
        //: SKPaymentQueue.default().add(payment)
        SKPaymentQueue.default().add(payment)
    }

    // 查询apple内购商品失败
    //: func request(_ request: SKRequest, didFailWithError error: Error) {
    func request(_: SKRequest, didFailWithError error: Error) {
        //: UploadLogTool.writeLog(msg: "IAP查询内购商品失败, Error:\(error).")
        MateThen.trackBy(msg: (kMakerPersonData + "询内购商" + noti_giveStr + " Error:") + "\(error).")
        //: self.completionHandle?( .noProductId, 0, currentPayType)
        self.completionHandle?(.noProductId, 0, currentPayType)
    }

    // 查询apple内购商品完成
    //: func requestDidFinish(_ request: SKRequest) {
    func requestDidFinish(_: SKRequest) {}
}

// MARK: - SKPaymentTransactionObserver【支付回调】

//: extension AppleIAPManager: SKPaymentTransactionObserver {
extension TermsRequestDelegate: SKPaymentTransactionObserver {
    /// 2.2 apple内购完成回调
    //: func paymentQueue(_ queue: SKPaymentQueue, updatedTransactions transactions: [SKPaymentTransaction]) {
    func paymentQueue(_: SKPaymentQueue, updatedTransactions transactions: [SKPaymentTransaction]) {
        //: for transaction in transactions {
        for transaction in transactions {
            //: switch transaction.transactionState {
            switch transaction.transactionState {
            //: case .purchasing:
            case .purchasing: // 交易中
                //: break
                break
            //: case .purchased:
            case .purchased: // 交易成功
                /**
                 original.transactionIdentifier 首次订阅时为nil，transaction.transactionIdentifier有值；
                 后续自动订阅、续订时，original.transactionIdentifier为首次订阅时生成的transaction.transactionIdentifier，值固定不变；
                 每次订阅transaction.transactionIdentifier都不一样，为当前交易的标识；
                 */
                //: if transaction.original != nil && createOrderId == nil {
                if transaction.original != nil && createOrderId == nil { // 启动自动续订时，不需要调用服务端验证接口
                    //: self.completionHandle?(.renewSucceed, 0, currentPayType)
                    self.completionHandle?(.renewSucceed, 0, currentPayType)
                    //: } else {
                } else { // 普通购买和订阅
                    //: UploadLogTool.writeLog(msg: "IAP-purchased:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                    MateThen.trackBy(msg: (kAssignStr.uppercased() + String(data_bearId.prefix(5)) + "rchased:") + "\(currentPayType)" + (appUpRegionText) + "\(String(describing: createOrderId))" + (noti_readyUrl + appMarginUrl.replacingOccurrences(of: "filter", with: ":")) + "\(transaction.payment.productIdentifier)" + (user_southwestData.replacingOccurrences(of: "all", with: " ") + String(const_shapeSideText.prefix(5)) + "nsId:") + "\(String(describing: transaction.transactionIdentifier)).")
                    // 初始化每个交易请求次数
                    //: reqRetryCountDict[transaction.transactionIdentifier!] = 0
                    reqRetryCountDict[transaction.transactionIdentifier!] = 0
                    // 3. 服务端校验流程
                    //: transcationPurchasedToCheck(transaction.transactionIdentifier!, self.currentPayType)
                    purchasedSelect(transaction.transactionIdentifier!, self.currentPayType)
                }
                // 移除苹果支付系统缓存
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: createOrderId = nil
                createOrderId = nil
            //: case .failed:
            case .failed: // 交易失败/取消
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: UploadLogTool.writeLog(msg: "IAP-failed:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                MateThen.trackBy(msg: (String(mainFlagJointId) + String(noti_factContent)) + "\(currentPayType)" + (appUpRegionText) + "\(String(describing: createOrderId))" + (noti_readyUrl + appMarginUrl.replacingOccurrences(of: "filter", with: ":")) + "\(transaction.payment.productIdentifier)" + (user_southwestData.replacingOccurrences(of: "all", with: " ") + String(const_shapeSideText.prefix(5)) + "nsId:") + "\(String(describing: transaction.transactionIdentifier)).")
                //: self.completionHandle?(.failed, 0, currentPayType)
                self.completionHandle?(.failed, 0, currentPayType)
                //: createOrderId = nil
                createOrderId = nil
                //: let vc = TalkingContactServiceView.init()
                let vc = TriggerView()
                //: vc.show()
                vc.propertyShow()
            //: case .restored:
            case .restored: // 已购买过该商品
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: self.completionHandle?(.restored, 0, currentPayType)
                self.completionHandle?(.restored, 0, currentPayType)
                //: createOrderId = nil
                createOrderId = nil
            //: case .deferred:
            case .deferred: // 交易延期
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: UploadLogTool.writeLog(msg: "IAP-deferred:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                MateThen.trackBy(msg: (noti_automaticallyFormat.replacingOccurrences(of: "nut", with: "A") + String(mainMasterMessage)) + "\(currentPayType)" + (appUpRegionText) + "\(String(describing: createOrderId))" + (noti_readyUrl + appMarginUrl.replacingOccurrences(of: "filter", with: ":")) + "\(transaction.payment.productIdentifier)" + (user_southwestData.replacingOccurrences(of: "all", with: " ") + String(const_shapeSideText.prefix(5)) + "nsId:") + "\(String(describing: transaction.transactionIdentifier)).")
                //: self.completionHandle?(.deferred, 0, currentPayType)
                self.completionHandle?(.deferred, 0, currentPayType)
                //: createOrderId = nil
                createOrderId = nil
            //: @unknown default:
            @unknown default:
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: self.completionHandle?(.unknow, 0, currentPayType)
                self.completionHandle?(.unknow, 0, currentPayType)
                //: createOrderId = nil
                createOrderId = nil
                //: UploadLogTool.writeLog(msg: "IAP未知交易类型:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                MateThen.trackBy(msg: (String(show_yearMessage) + String(main_speakerUrl)) + "\(currentPayType)" + (appUpRegionText) + "\(String(describing: createOrderId))" + (noti_readyUrl + appMarginUrl.replacingOccurrences(of: "filter", with: ":")) + "\(transaction.payment.productIdentifier)" + (user_southwestData.replacingOccurrences(of: "all", with: " ") + String(const_shapeSideText.prefix(5)) + "nsId:") + "\(String(describing: transaction.transactionIdentifier)).")
                //: fatalError(" 未知的交易类型")
                fatalError((show_movieAlterMistUrl.replacingOccurrences(of: "shade", with: "型")))
            }
        }
    }

    /// 3. 服务端校验流程
    /// - Parameters:
    ///   - transactionId: 交易唯一标识符
    ///   - payType: 支付类型
    //: fileprivate func transcationPurchasedToCheck(_ transactionId: String, _ payType: ApplePayType) {
    fileprivate func purchasedSelect(_ transactionId: String, _ payType: DismissType) {
        //: guard let receiptStr = getVerifyData(transactionId, payType) else {
        guard let receiptStr = fromPer(transactionId, payType) else {
            //: UploadLogTool.writeLog(msg: "IAP获取本地收据数据失败, transactionId:\(transactionId), orderId:\(String(describing: createOrderId)), payType:\(payType).")
            MateThen.trackBy(msg: (String(kDismissMessage.prefix(5)) + "本\u{5730}收据数据" + String(data_localPath)) + "\(transactionId)" + (String(user_nothingStr)) + "\(String(describing: createOrderId))" + (String(const_mainPathMessage) + String(show_remoteTitle)) + "\(payType).")
            //: self.completionHandle?(.verityFail, 0, payType)
            self.completionHandle?(.verityFail, 0, payType)
            //: return
            return
        }

        // 缓存支付成功信息，防止接口校验失败
        //: if createOrderId != nil {
        if createOrderId != nil { // 正常支付流程
            //: switch(payType) {
            switch payType {
            //: case .Pay:
            case .Pay:
                //: if self.payCacheList.filter({$0["transactionId"] == transactionId || $0["orderId"] == createOrderId}).count == 0 {  // 防止重复添加缓存数据
                if self.payCacheList.filter({ $0[(app_tradeData.lowercased() + String(mainLifeBeastUrl))] == transactionId || $0[String(bytes: constBeginId.map{motivePersonality(hung: $0)}, encoding: .utf8)!] == createOrderId }).count == 0 { // 防止重复添加缓存数据
                    //: let cacheDict = ["transactionId": transactionId,
                    let cacheDict = [(app_tradeData.lowercased() + String(mainLifeBeastUrl)): transactionId,
                                     //: "orderId": createOrderId!,
                                     String(bytes: constBeginId.map{motivePersonality(hung: $0)}, encoding: .utf8)!: createOrderId!,
                                     //: "verifyData": receiptStr]
                                     String(bytes: userSaltTitle.map{coalTar(input: $0)}, encoding: .utf8)!: receiptStr]
                    //: self.payCacheList.append(cacheDict)
                    self.payCacheList.append(cacheDict)
                    //: let diskPath = self.getPayCachePath()
                    let diskPath = self.greetFor()
                    //: NSKeyedArchiver.archiveRootObject(self.payCacheList, toFile: diskPath)
                    NSKeyedArchiver.archiveRootObject(self.payCacheList, toFile: diskPath)
                }

            //: case .Subscribe:
            case .Subscribe:
                //: if self.subscribeCacheList.filter({$0["transactionId"] == transactionId || $0["orderId"] == createOrderId}).count == 0 { // 防止重复添加缓存数据
                if self.subscribeCacheList.filter({ $0[(app_tradeData.lowercased() + String(mainLifeBeastUrl))] == transactionId || $0[String(bytes: constBeginId.map{motivePersonality(hung: $0)}, encoding: .utf8)!] == createOrderId }).count == 0 { // 防止重复添加缓存数据
                    //: let cacheDict = ["transactionId": transactionId,
                    let cacheDict = [(app_tradeData.lowercased() + String(mainLifeBeastUrl)): transactionId,
                                     //: "orderId": createOrderId!,
                                     String(bytes: constBeginId.map{motivePersonality(hung: $0)}, encoding: .utf8)!: createOrderId!,
                                     //: "verifyData": receiptStr]
                                     String(bytes: userSaltTitle.map{coalTar(input: $0)}, encoding: .utf8)!: receiptStr]
                    //: self.subscribeCacheList.append(cacheDict)
                    self.subscribeCacheList.append(cacheDict)
                    //: let diskPath = self.getSubscribeCachePath()
                    let diskPath = self.mFront()
                    //: NSKeyedArchiver.archiveRootObject(self.subscribeCacheList, toFile: diskPath)
                    NSKeyedArchiver.archiveRootObject(self.subscribeCacheList, toFile: diskPath)
                }
            }
        }

        // 限制交易重试最大次数
        //: var reqCount = reqRetryCountDict[transactionId] ?? 0
        var reqCount = reqRetryCountDict[transactionId] ?? 0
        //: reqCount += 1
        reqCount += 1
        //: reqRetryCountDict[transactionId] = reqCount
        reqRetryCountDict[transactionId] = reqCount
        //: if reqCount > APPLE_IAP_MAX_RETRY_COUNT {
        if reqCount > kFirstFormat {
            //: self.completionHandle?(.verityFail, 0, payType)
            self.completionHandle?(.verityFail, 0, payType)
            //: return
            return
        }

        // 3.服务端校验，根据transactionId从缓存中取
        //: switch(payType) {
        switch payType {
        //: case .Pay:
        case .Pay:
            //: let paramsArr = self.payCacheList.filter({$0["transactionId"] == transactionId})
            let paramsArr = self.payCacheList.filter { $0[(app_tradeData.lowercased() + String(mainLifeBeastUrl))] == transactionId }
            //: guard paramsArr.count > 0 else { return }
            guard paramsArr.count > 0 else { return }
            //: req_pay_uploadAppletransaction(transactionId, params: paramsArr.first!)
            perpendicular(transactionId, params: paramsArr.first!)

        //: case .Subscribe:
        case .Subscribe:
            //: let paramsArr = self.subscribeCacheList.filter({$0["transactionId"] == transactionId})
            let paramsArr = self.subscribeCacheList.filter { $0[(app_tradeData.lowercased() + String(mainLifeBeastUrl))] == transactionId }
            //: guard paramsArr.count > 0 else { return }
            guard paramsArr.count > 0 else { return }
            //: req_subscribe_uploadAppletransaction(transactionId, params: paramsArr.first!)
            shadowParams(transactionId, params: paramsArr.first!)
        }
    }
}
