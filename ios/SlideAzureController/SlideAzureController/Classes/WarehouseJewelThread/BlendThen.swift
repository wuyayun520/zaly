
//: Declare String Begin

/*: "mf/moment/list" :*/
fileprivate let const_equalName:String = "mf/modrag production electron"
fileprivate let const_streamStr:String = "indicatort"

/*: "mf/moment/moments" :*/
fileprivate let notiReportTimingData:[Character] = ["m","f","/","m","o","m","e","n","t","/","m","o","m","e"]
fileprivate let k_listKey:String = "NTS"

/*: "mf/moment/like" :*/
fileprivate let kDisplayBeamValue:String = "mf/momexhibit wait feedback analyze"
fileprivate let notiPureMsg:String = "beard jointent/like"

/*: "mf/crush/send" :*/
fileprivate let kDryMsg:String = "mf/ccertain unable personal"
fileprivate let user_goingSeedValue:String = "seranged"

/*: "mf/moment/view" :*/
fileprivate let user_phoneAnswerFormat:[Character] = ["m","f","/","m","o"]
fileprivate let show_progressScaleForthStr:String = "ment/viewmeet wind"

/*: "mf/momentReply/list" :*/
fileprivate let main_topName:String = "mf/momroute now stumble"
fileprivate let kBusinessFormat:String = "eplrequire"
fileprivate let const_userTitle:String = "/listtranslation span"

/*: "mf/momentReply/save" :*/
fileprivate let kBoundMessage:[Character] = ["m","f","/","m","o","m","e","n","t"]
fileprivate let show_replaceId:String = "judge reference his newsRe"
fileprivate let show_moveValue:String = "ply/saveplace gravity expense refuse"

/*: "mf/moment/videoSign" :*/
fileprivate let data_turnName:String = "input bird something while lowermf/m"
fileprivate let const_missingMsg:String = "/videoSigraw player choice yet"
fileprivate let app_collectionSubInjuryUrl:[Character] = ["n"]

/*: "dynamicPic.zip" :*/
fileprivate let constAlwaysName:String = "dyforehead"
fileprivate let mainProposalText:String = "zstrikep"

/*: "pic" :*/
fileprivate let app_costUrl:[UInt8] = [0xa1,0xb8,0xb2]

private func denyNatural(section num: UInt8) -> UInt8 {
    return num ^ 209
}

/*: "content" :*/
fileprivate let app_betweenMsg:[Character] = ["c","o","n","t","e"]
fileprivate let notiDiscussionTitle:String = "NT"

/*: "mf/moment/save" :*/
fileprivate let app_uniformText:[Character] = ["m","f","/","m","o","m","e","n"]
fileprivate let const_objectUrl:String = "regular custom gathert/save"

/*: "Publish Successful, Go to My Dynamics to view" :*/
fileprivate let notiSecurityFormat:[UInt8] = [0xf9,0x1e,0xb,0x15,0x12,0x1c,0x11,0xc9,0xfc,0x1e,0xc,0xc,0xe,0x1c,0x1c,0xf,0x1e,0x15,0xd5,0xc9,0xf0,0x18,0xc9,0x1d,0x18,0xc9,0xf6,0x22,0xc9,0xed,0x22,0x17,0xa,0x16,0x12,0xc,0x1c,0xc9,0x1d,0x18,0xc9,0x1f,0x12,0xe,0x20]

fileprivate func roundSpare(timing num: UInt8) -> UInt8 {
    let value = Int(num) - 169
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "mf/moment/saveVideo" :*/
fileprivate let main_engineeringKey:[Character] = ["m","f","/","m","o","m","e","n","t","/","s","a","v","e","V","i","d","e"]
fileprivate let dataBeamWhenPath:[Character] = ["o"]

/*: .png" :*/
fileprivate let mainIndexBeardPath:String = ".pngpic design adjustment framework"

/*: "dynamicPic" :*/
fileprivate let const_towardStr:[Character] = ["d","y","n","a","m","i","c","P","i","c"]

/*: "freeImage" :*/
fileprivate let main_creationPath:String = "freeImadistribute attribute launch leading icon"
fileprivate let kDecreaseId:String = "grip"

/*: "mf/momentReply/replylist" :*/
fileprivate let user_involvementId:[Character] = ["m","f","/","m","o","m","e","n","t","R","e","p","l","y","/","r","e","p","l","y","l","i","s"]
fileprivate let showSpaceKey:String = "T"

/*: "mf/moment/delete" :*/
fileprivate let const_doorPrettyMessage:[Character] = ["m","f","/","m","o","m","e","n","t","/","d","e","l"]
fileprivate let const_instructionTitle:[Character] = ["e","t","e"]

/*: "mf/momentReply/delete" :*/
fileprivate let dataChangeKey:String = "security someone supply awake someonemf/m"
fileprivate let appListenMsg:String = "shouldermen"
fileprivate let dataHopUrl:String = "zone stately/d"

/*: "mf/moment/pin" :*/
fileprivate let const_assistantData:[Character] = ["m","f","/","m","o","m","e","n","t","/","p","i"]
fileprivate let app_serviceContent:String = "err"

/*: "mf/moment/remindNum" :*/
fileprivate let data_yesMessage:String = "quick curve volume mechanism loadmf/moment"
fileprivate let user_scorePath:String = "/remangle birth shift"

/*: "mf/moment/remindList" :*/
fileprivate let constAverageText:[Character] = ["m","f","/","m","o","m","e","n","t","/","r","e","m","i","n"]
fileprivate let user_disabledStr:[Character] = ["d","L","i","s","t"]

/*: "page" :*/
fileprivate let app_bondRunningFormat:[UInt8] = [0x82,0x93,0x95,0x97]

private func acceptFore(member num: UInt8) -> UInt8 {
    return num ^ 242
}

/*: "cos/getCredential" :*/
fileprivate let mainRoadFairData:[Character] = ["c","o","s","/","g"]
fileprivate let main_takeName:String = "second standard moveetCre"

/*: "type" :*/
fileprivate let app_cutId:String = "tmainpe"

/*: "user/reportUserInfo" :*/
fileprivate let kDistrictStr:String = "attractiveser"
fileprivate let showInstanceTitle:String = "heart weeklyortU"
fileprivate let userNumbUrl:String = "documento"

/*: "uid" :*/
fileprivate let dataLiquidWorkerStr:[Character] = ["u","i","d"]

/*: "deviceId" :*/
fileprivate let noti_supportContent:[Character] = ["d"]
fileprivate let const_premiumMsg:String = "low lenseviceId"

/*: "deviceName" :*/
fileprivate let noti_accuracyValue:[Character] = ["d","e","v","i","c","e","N","a","m","e"]

/*: "iphone" :*/
fileprivate let noti_effectiveId:String = "iphotransforme"

/*: "fileUrl" :*/
fileprivate let show_yearMsg:String = "life eyebrow meanfileUr"
fileprivate let kFillFormat:String = "L"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  BlendThen.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/19.
//

//: import Adjust
import Adjust
//: import UIKit
import UIKit

//: class TalkingMomentRequestTool: NSObject {
class BlendThen: NSObject {
    //: let _fm = FileManager.default
    let _fm = FileManager.default

    /// 动态列表接口
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func app(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "mf/moment/list"
        reqModel.requestPath = (String(const_equalName.prefix(5)) + "ment/li" + const_streamStr.replacingOccurrences(of: "indicator", with: "s"))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 用户动态接口
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_UserMomentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func sequenceWith(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "mf/moment/moments"
        reqModel.requestPath = (String(notiReportTimingData) + k_listKey.lowercased())
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 点赞
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentLike(params: [String: Any], completion: @escaping FinishBlock) {
    class func eleventhHour(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "mf/moment/like"
        reqModel.requestPath = (String(kDisplayBeamValue.prefix(6)) + String(notiPureMsg.suffix(8)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - 发送心动
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CrushSend(params: [String: Any], completion: @escaping FinishBlock) {
    class func listing(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "mf/crush/send"
        reqModel.requestPath = (String(kDryMsg.prefix(4)) + "rush/" + user_goingSeedValue.replacingOccurrences(of: "range", with: "n"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态详情
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentDetail(params: [String: Any], completion: @escaping FinishBlock) {
    class func ultramontanism(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "mf/moment/view"
        reqModel.requestPath = (String(user_phoneAnswerFormat) + String(show_progressScaleForthStr.prefix(9)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态评论列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CommentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func quote(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "mf/momentReply/list"
        reqModel.requestPath = (String(main_topName.prefix(6)) + "entR" + kBusinessFormat.replacingOccurrences(of: "require", with: "y") + String(const_userTitle.prefix(5)))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 发表评论、回复
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_SendComment(params: [String: Any], completion: @escaping FinishBlock) {
    class func vehicle(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "mf/momentReply/save"
        reqModel.requestPath = (String(kBoundMessage) + String(show_replaceId.suffix(2)) + String(show_moveValue.prefix(8)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取视频上传sign
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_VideoSign(params: [String: Any], completion: @escaping FinishBlock) {
    class func table(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "mf/moment/videoSign"
        reqModel.requestPath = (String(data_turnName.suffix(4)) + "oment" + String(const_missingMsg.prefix(9)) + String(app_collectionSubInjuryUrl))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态发布
    /// - Parameters:
    ///   - photos: 图片
    ///   - content: 内容
    ///   - completion: 回调
    //: func releasePhotosMoment(photos: [UIImage], content: String, completion: @escaping FinishBlock) {
    func size(photos: [UIImage], content: String, completion: @escaping FinishBlock) {
        // 处理图片
        //: deleteFile()
        instance()
        //: for (index, photo) in photos.enumerated() {
        for (index, photo) in photos.enumerated() {
            //: getImagePath(index: index, image: photo)
            operate(index: index, image: photo)
        }
        //: let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let doc_path = (documentDictPath as NSString).appendingPathComponent("dynamicPic.zip")
        let doc_path = (documentDictPath as NSString).appendingPathComponent((constAlwaysName.replacingOccurrences(of: "forehead", with: "na") + "micPic." + mainProposalText.replacingOccurrences(of: "strike", with: "i")))
        //: SSZipArchive.createZipFile(atPath: doc_path, withContentsOfDirectory: filePath)
        SSZipArchive.createZipFile(atPath: doc_path, withContentsOfDirectory: filePath)
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: let data = NSData(contentsOfFile: doc_path)
            let data = NSData(contentsOfFile: doc_path)
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                // 发布
                //: var params: [String: Any] = ["pic": data as Any]
                var params: [String: Any] = [String(bytes: app_costUrl.map{denyNatural(section: $0)}, encoding: .utf8)!: data as Any]
                //: if content.count > 0 {
                if content.count > 0 {
                    //: params["content"] = content
                    params[(String(app_betweenMsg) + notiDiscussionTitle.lowercased())] = content
                }
                //: let reqModel = TalkingRequestModel()
                let reqModel = ProcessingThen()
                //: reqModel.requestPath = "mf/moment/save"
                reqModel.requestPath = (String(app_uniformText) + String(const_objectUrl.suffix(6)))
                //: reqModel.requestType = .POST
                reqModel.requestType = .POST
                //: reqModel.params = params
                reqModel.params = params
                //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
                const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
                    //: if succeed {
                    if succeed {
                        //: self.func__showStatusBarSuccessMsg(showMsg: "Publish Successful, Go to My Dynamics to view".localized)
                        self.showGuidanceName(showMsg: String(bytes: notiSecurityFormat.map{roundSpare(timing: $0)}, encoding: .utf8)!.localized)
                        //: self.deleteFile()
                        self.instance()
                    }
                    //: completion(succeed, result, errorModel)
                    completion(succeed, result, errorModel)
                }
            }
        }
    }

    /// 动态发布
    /// - Parameters:
    ///   - completion: 回调
    //: class func releaseVideoMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func present(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "mf/moment/saveVideo"
        reqModel.requestPath = (String(main_engineeringKey) + String(dataBeamWhenPath))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    //: private func getImagePath(index: Int, image: UIImage) {
    private func operate(index: Int, image: UIImage) {
        //: let data = image.compressedImageData()
        let data = image.cellByCan()
        //: let nameString = "\(index).png"
        let nameString = "\(index)" + (String(mainIndexBeardPath.prefix(4)))
        //: let picPath = (filePath as NSString).appendingPathComponent("dynamicPic")
        let picPath = (filePath as NSString).appendingPathComponent((String(const_towardStr)))
        //: if !_fm.fileExists(atPath: picPath) {
        if !_fm.fileExists(atPath: picPath) {
            //: try? _fm.createDirectory(atPath: picPath, withIntermediateDirectories: true)
            try? _fm.createDirectory(atPath: picPath, withIntermediateDirectories: true)
        }
        //: let imagePath = (picPath as NSString).appendingPathComponent(nameString)
        let imagePath = (picPath as NSString).appendingPathComponent(nameString)
        //: _fm.createFile(atPath: imagePath, contents: data)
        _fm.createFile(atPath: imagePath, contents: data)
    }

    //: private func deleteFile() {
    private func instance() {
        //: if _fm.fileExists(atPath: filePath) {
        if _fm.fileExists(atPath: filePath) {
            //: try? _fm.removeItem(atPath: filePath)
            try? _fm.removeItem(atPath: filePath)
        }
    }

    //: private lazy var filePath: String = {
    private lazy var filePath: String = {
        //: let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let imagePath = (documentDictPath as NSString).appendingPathComponent("freeImage")
        let imagePath = (documentDictPath as NSString).appendingPathComponent((String(main_creationPath.prefix(7)) + kDecreaseId.replacingOccurrences(of: "rip", with: "e")))
        //: return imagePath
        return imagePath
        //: }()
    }()

    /// 子评论列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CommentDetailList(params: [String: Any], completion: @escaping FinishBlock) {
    class func layerBone(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "mf/momentReply/replylist"
        reqModel.requestPath = (String(user_involvementId) + showSpaceKey.lowercased())
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除动态
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_DeleteMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func pinEnable(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "mf/moment/delete"
        reqModel.requestPath = (String(const_doorPrettyMessage) + String(const_instructionTitle))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除评论
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_DeleteMomentReply(params: [String: Any], completion: @escaping FinishBlock) {
    class func pointInTimeBy(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "mf/momentReply/delete"
        reqModel.requestPath = (String(dataChangeKey.suffix(4)) + appListenMsg.replacingOccurrences(of: "shoulder", with: "o") + "tRep" + String(dataHopUrl.suffix(4)) + "elete")
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 置顶操作
    /// - Parameters:
    /// status=1表示置顶，0=取消置顶
    ///   - completion: 回调
    //: class func req_TopMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func written(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "mf/moment/pin"
        reqModel.requestPath = (String(const_assistantData) + app_serviceContent.replacingOccurrences(of: "err", with: "n"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取用户未读动态总数
    /// - Parameter completion: 回调
    //: class func req_getRemindNum(completion: @escaping FinishBlock) {
    class func go(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "mf/moment/remindNum"
        reqModel.requestPath = (String(data_yesMessage.suffix(9)) + String(user_scorePath.prefix(4)) + "indNum")
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取当前未读动态信息列表
    /// - Parameters:
    ///   - page: 分页
    ///   - completion: 回调
    //: class func req_getRemindList(page: Int, completion: @escaping (_ data: [TalkingRemindListModel]) -> Void) {
    class func identity(page: Int, completion: @escaping (_ data: [PresenceModelType]) -> Void) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "mf/moment/remindList"
        reqModel.requestPath = (String(constAverageText) + String(user_disabledStr))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["page": page]
        reqModel.params = [String(bytes: app_bondRunningFormat.map{acceptFore(member: $0)}, encoding: .utf8)!: page]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { _, result, _ in
            //: var dataArr: [TalkingRemindListModel] = []
            var dataArr: [PresenceModelType] = []
            //: guard let arr = result as? [[String: Any]] else {
            guard let arr = result as? [[String: Any]] else {
                //: completion(dataArr)
                completion(dataArr)
                //: return
                return
            }
            //: if let datas = Array<TalkingRemindListModel>.deserialize(from: arr as? Array) as? [TalkingRemindListModel] {
            if let datas = Array<PresenceModelType>.deserialize(from: arr as? Array) as? [PresenceModelType] {
                //: dataArr.append(contentsOf: datas)
                dataArr.append(contentsOf: datas)
            }
            //: completion(dataArr)
            completion(dataArr)
        }
    }

    /// COS  视频存储
    /// - Parameters:
    ///   - type: 1=动态，2=视频封面，3=私信视频
    ///   - completion: 回调
    //: class func req_getCredential(type: Int, completion: @escaping FinishBlock) {
    class func theWant(type: Int, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "cos/getCredential"
        reqModel.requestPath = (String(mainRoadFairData) + String(main_takeName.suffix(5)) + "dential")
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["type": type]
        reqModel.params = [(app_cutId.replacingOccurrences(of: "main", with: "y")): type]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 上传cos日志存储路径
    /// - Parameters:
    ///   - fileUrl: 日志路径
    ///   - completion: 回调
    //: class func req_uploadLogPath(fileUrl: String, completion: @escaping FinishBlock) {
    class func completionMax(fileUrl: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ProcessingThen()
        //: reqModel.requestServer = DismissReactiveCompatible.share.appConfigMode.reportDomain
        reqModel.requestServer = DismissReactiveCompatible.share.appConfigMode.reportDomain
        //: reqModel.requestPath = "user/reportUserInfo"
        reqModel.requestPath = (kDistrictStr.replacingOccurrences(of: "attractive", with: "u") + "/rep" + String(showInstanceTitle.suffix(4)) + "serIn" + userNumbUrl.replacingOccurrences(of: "document", with: "f"))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: var params = [String: Any]()
        var params = [String: Any]()
        //: params["uid"] = DismissReactiveCompatible.share.loginUserMode.userID
        params[(String(dataLiquidWorkerStr))] = DismissReactiveCompatible.share.loginUserMode.userID
        //: params["deviceId"] = Adjust.adid() ?? ""
        params[(String(noti_supportContent) + String(const_premiumMsg.suffix(7)))] = Adjust.adid() ?? ""
        //: params["deviceName"] = "iphone"
        params[(String(noti_accuracyValue))] = (noti_effectiveId.replacingOccurrences(of: "transform", with: "n"))
        //: params["type"] = 1
        params[(app_cutId.replacingOccurrences(of: "main", with: "y"))] = 1
        //: params["fileUrl"] = fileUrl
        params[(String(show_yearMsg.suffix(6)) + kFillFormat.lowercased())] = fileUrl
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }
}
