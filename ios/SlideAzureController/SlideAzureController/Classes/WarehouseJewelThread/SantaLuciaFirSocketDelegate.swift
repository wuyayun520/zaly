
//: Declare String Begin

/*: "SOCKET_ADDR_KEY" :*/
fileprivate let constTheaterTitle:[Character] = ["S","O","C","K","E","T","_","A","D","D","R","_","K"]
fileprivate let kAccuracyName:String = "ey"

/*: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION" :*/
fileprivate let user_platformTitle:[UInt8] = [0x21,0x3d,0x31,0x39,0x37,0x26,0x2d,0x24,0x3b,0x36,0x37,0x3d,0x31,0x3a,0x33,0x26,0x2d,0x20,0x37,0x23,0x27,0x37,0x21,0x26,0x31,0x33,0x3e,0x3e,0x2d,0x3c,0x3d,0x26,0x3b,0x34,0x3b,0x31,0x33,0x26,0x3b,0x3d,0x3c]

/*: "RECIVE_FLOATSCREEN_NOTIFICATION" :*/
fileprivate let userSurgeryData:[UInt8] = [0x4e,0x4f,0x49,0x54,0x41,0x43,0x49,0x46,0x49,0x54,0x4f,0x4e,0x5f,0x4e,0x45,0x45,0x52,0x43,0x53,0x54,0x41,0x4f,0x4c,0x46,0x5f,0x45,0x56,0x49,0x43,0x45,0x52]

/*: "isCalling" :*/
fileprivate let notiMidMsg:[Character] = ["i","s","C","a","l","l","i","n"]
fileprivate let mainScanName:String = "relation"
 
/*: "SOCKET 连接成功." :*/
fileprivate let k_somethingKey:String = "SOpoKET"
fileprivate let notiSuspendValue:[Character] = ["功","."]

/*: "SOCKET 连接失败, error: :*/
fileprivate let k_bigFormat:String = "SagainK"
fileprivate let noti_parkData:String = "\u{63a5}失败, e"

/*: "SOCKET 失败 :*/
fileprivate let const_fillTitle:String = "SnoticeCKE"
fileprivate let appPolicyValue:String = "T 失\u{8d25}"

/*: 次, 切换过Socket域名重新尝试，地址： :*/
fileprivate let data_activityKingdomMsg:[Character] = ["次",","," ","切","换","\u{8fc7}","S","o","c","k","e"]
fileprivate let dataLeagueReaderId:String = "t域名重新dit char not blind"
fileprivate let data_overwhelmKey:[Character] = ["尝","试","，","地","址","："]

/*: "SOCKET 已关闭, code: :*/
fileprivate let data_tagMemoryImportMsg:String = "SOCselectionE"
fileprivate let user_trendName:String = "T 已\u{5173}闭"

/*: , reason: :*/
fileprivate let dataDiscourseAnimaContent:String = ", rewhich tab"

/*: /?sid= :*/
fileprivate let constReadyNaturalValue:String = "/?sid=grade crystal clothes"

/*: &uid= :*/
fileprivate let showPartyData:String = "&uid=called fee meeting liquid"

/*: &platform=iphone&version= :*/
fileprivate let show_askSumStr:String = "&platfat object"
fileprivate let const_reduceNatureName:String = "iphone&vwant movement accuracy"
fileprivate let userStaffPath:String = "ersion=m dynamics try"

/*: &packageId= :*/
fileprivate let showBasicSeparateName:[Character] = ["&","p","a","c","k","a","g","e","I","d","="]

/*: "1v1server. :*/
fileprivate let main_skiPath:[Character] = ["1","v","1","s","e","r","v"]
fileprivate let app_bagKey:String = "er.decision reach"

/*: .com:7766" :*/
fileprivate let main_permissionFormat:String = "regular required level.com:"

/*: "43.129.185.194:7767" :*/
fileprivate let kSmartStr:String = "43.129.1"
fileprivate let userCommitText:[Character] = ["8","5",".","1","9","4"]
fileprivate let k_blueFormat:String = ":7767"

/*: "SOCKET 网络reachabilityChanged监听，断网关闭." :*/
fileprivate let const_continuingFormat:[UInt8] = [0x2e,0xad,0x97,0xe9,0xb3,0x85,0xe5,0x91,0xbd,0xe7,0xad,0x96,0xe6,0x8c,0xbc,0xef,0xac,0x90,0xe5,0x91,0x9b,0xe7,0x64,0x65,0x67,0x6e,0x61,0x68,0x43,0x79,0x74,0x69,0x6c,0x69,0x62,0x61,0x68,0x63,0x61,0x65,0x72,0x9c,0xbb,0xe7,0x91,0xbd,0xe7,0x20,0x54,0x45,0x4b,0x43,0x4f,0x53]

/*: "cmd" :*/
fileprivate let constOrientationId:[Character] = ["c","m","d"]

/*: "ping" :*/
fileprivate let constReachName:String = "PING"

/*: "callHeartbeat" :*/
fileprivate let mainCropId:[Character] = ["c","a","l","l","H","e","a","r","t","b","e","a","t"]

/*: "recievedDate" :*/
fileprivate let app_hiddenMessage:[Character] = ["r","e","c","i","e","v","e","d","D","a"]
fileprivate let noti_enableText:String = "electronic"

/*: "liveHeartBeat" :*/
fileprivate let showTranslateName:String = "litransition"
fileprivate let k_assignMsg:[Character] = ["e","H","e","a","r","t","B","e","a","t"]

/*: "🔗 关闭 socket" :*/
fileprivate let constOppositeMessage:[Character] = ["🔗"," ","关","闭"," ","s","o","c"]
fileprivate let userBassStr:String = "anothert"

/*: "The user logs out" :*/
fileprivate let mainDeadlineKey:String = "The utopic gender press reader"
fileprivate let const_enhanceValue:String = "audience decisionlogs "
fileprivate let kLivePath:[Character] = ["o","u","t"]

/*: "SOCKET  :*/
fileprivate let data_usVarietyWrittenId:[Character] = ["S","O","C","K","E","T"," "]

/*: 重连. 重试次数： :*/
fileprivate let show_thinMsg:String = "重\u{8fde}. 重"

/*: "SOCKET 连接正常 不需要重新连接." :*/
fileprivate let noti_bringData:[Character] = ["S","O","C","K","E","T"," ","连","\u{63a5}","正","常"," ","不"]
fileprivate let userSuccessId:[Character] = ["需","\u{8981}","\u{91cd}","\u{65b0}","连","接","."]

/*: "SOCKET 正在连接 不需要重新连接." :*/
fileprivate let notiLoadKey:[Character] = ["S","O","C","K","E","T"," ","正","\u{5728}","\u{8fde}","接"," ","不"]
fileprivate let kRelationId:String = "需要重新\u{8fde}接."

/*: "&resume=1" :*/
fileprivate let constPastFormat:String = "commit deny earn send&resu"

/*: "&lang= :*/
fileprivate let show_inputMessage:String = "nor everyone glass do&lang="

/*: "SOCKET 开始连接, 地址： :*/
fileprivate let main_flexibleFormat:String = "SOCKdonT"
fileprivate let main_wordAgreeMsg:String = "pad, 地址："

/*: "onNetDisabled" :*/
fileprivate let noti_gatherMessage:[Character] = ["o"]
fileprivate let notiSpanTitle:String = "start nail mentionnNet"
fileprivate let constObjectUrl:String = "bleangle"

/*: "errno" :*/
fileprivate let user_accelerateKey:String = "ERRNO"

/*: "data" :*/
fileprivate let user_unknownDateMessage:[Character] = ["d","a","t","a"]

/*: "SOCKET 连接保活cmd: ping." :*/
fileprivate let userMouthMessage:[Character] = ["S","O","C","K","E","T"," ","\u{8fde}","\u{63a5}","保","\u{6d3b}","c","m","d"]
fileprivate let kClipViaUrl:[Character] = [":"," "]
fileprivate let showDetailedData:String = "ping.doc hundred"

/*: "SOCKET trtc保活cmd: callHeartbeat." :*/
fileprivate let constHistoryValue:[UInt8] = [0x2e,0x74,0x61,0x65,0x62,0x74,0x72,0x61,0x65,0x48,0x6c,0x6c,0x61,0x63,0x20,0x3a,0x64,0x6d,0x63,0xbb,0xb4,0xe6,0x9d,0xbf,0xe4,0x63,0x74,0x72,0x74,0x20,0x54,0x45,0x4b,0x43,0x4f,0x53]

/*: "SOCKET live保活cmd: liveHeartBeat." :*/
fileprivate let kVarietyUrl:[UInt8] = [0xbd,0xa1,0xad,0xa5,0xab,0xba,0xce,0x82,0x87,0x98,0x8b,0xa,0x51,0x73,0x8,0x5a,0x55,0x8d,0x83,0x8a,0xd4,0xce,0x82,0x87,0x98,0x8b,0xa6,0x8b,0x8f,0x9c,0x9a,0xac,0x8b,0x8f,0x9a,0xc0]

/*: "onP" :*/
fileprivate let constFloorUrl:String = "name fiscal due term whileonP"

/*: "msg" :*/
fileprivate let kBigValue:String = "mfragmentg"

/*: "🔗 处理消息：cmdName: :*/
fileprivate let showDoingerPolicyKey:String = "🔗 处flat"
fileprivate let app_tooValue:String = "merely social its feed idName:"

/*: , errNo: :*/
fileprivate let k_visitorId:String = ", ermap then feature exit"

/*: , msg: :*/
fileprivate let appRemainingMsg:String = "year district, msg:"

/*: , data: :*/
fileprivate let user_wedMessage:String = ", dataoperate support"
fileprivate let userMinimumUponId:String = ":"

/*: "SOCKET 接收异常消息：cmdName: :*/
fileprivate let mainLifetimeMsg:String = "SOCKET \u{63a5}"
fileprivate let showApplyFormat:String = "收异常\u{6d88}息"

/*: "onReceivedGift" :*/
fileprivate let showOkName:[Character] = ["o","n","R","e","c"]
fileprivate let kVisibleData:String = "me"
fileprivate let app_mirrorData:String = "isubmitd"

/*: "onVideoMatch" :*/
fileprivate let notiLetterMessage:String = "onVideopublic shade para"
fileprivate let mainHundredMsg:[Character] = ["M","a","t","c","h"]

/*: "onRequestCall" :*/
fileprivate let appCapacityValue:[Character] = ["o","n","R","e","q","u","e","s","t","C","a","l","l"]

/*: "onCancelCall" :*/
fileprivate let app_midPath:[Character] = ["o","n","C","a","n","c","e","l","C","a","l","l"]

/*: "onRefuseCall" :*/
fileprivate let userJudgeName:String = "wind hello numb heartonRefus"
fileprivate let showPendingText:[Character] = ["e","C","a","l","l"]

/*: "onAcceptCall" :*/
fileprivate let noti_beginKey:String = "O"
fileprivate let const_spareData:String = "nAcceweek recommendation accounting pitch"

/*: "onStopCall" :*/
fileprivate let main_systemAddFormat:[Character] = ["o","n","S","t","o","p","C","a","l","l"]

/*: "freeCallTimes" :*/
fileprivate let user_hearFormat:[Character] = ["f"]
fileprivate let notiGameFormat:String = "reeCsocial editor"

/*: "mfCoin" :*/
fileprivate let const_hideId:String = "mfCoindata detect uniform"

/*: "onMissCoin" :*/
fileprivate let user_openingMsg:String = "onMispitch spread"
fileprivate let k_enhanceKey:String = "sCoinbond quit plain"

/*: "acceptCall" :*/
fileprivate let appUniformUserKey:[Character] = ["a","c","c","e","p","t","C","a","l","l"]

/*: "stopCall" :*/
fileprivate let notiSuspendRecordPath:String = "stothers"

/*: "requestCall" :*/
fileprivate let showServiceFocusFormat:String = "reqwrittenst"
fileprivate let userPhysicsData:[Character] = ["C","a","l","l"]

/*: "[Invite you to a video call...]" :*/
fileprivate let user_behaviorBurnValue:[UInt8] = [0x8,0x1a,0x3d,0x25,0x3a,0x27,0x36,0x73,0x2a,0x3c,0x26,0x73,0x27,0x3c,0x73,0x32,0x73,0x25,0x3a,0x37,0x36,0x3c,0x73,0x30,0x32,0x3f,0x3f,0x7d,0x7d,0x7d,0xe]

private func transitPublicly(bit num: UInt8) -> UInt8 {
    return num ^ 83
}

/*: "type" :*/
fileprivate let dataUnlessStr:[Character] = ["t","y","p","e"]

/*: "[Invite you to a voice call...]" :*/
fileprivate let showMaleMessage:[UInt8] = [0x10,0x2,0x25,0x3d,0x22,0x3f,0x2e,0x6b,0x32,0x24,0x3e,0x6b,0x3f,0x24,0x6b,0x2a,0x6b,0x3d,0x24,0x22,0x28,0x2e,0x6b,0x28,0x2a,0x27,0x27,0x65,0x65,0x65,0x16]

private func sceneRelated(harassment num: UInt8) -> UInt8 {
    return num ^ 75
}

/*: "uid" :*/
fileprivate let noti_nextMsg:String = "uicorner"

/*: "nickname" :*/
fileprivate let kShadowMsg:[Character] = ["n","i","c","k","n","a","m","e"]

/*: "headPic" :*/
fileprivate let user_noteKey:String = "headPiccorrect meeting kind border"

/*: "onCallResume" :*/
fileprivate let userDraftCutContent:[Character] = ["o","n","C","a","l","l","R"]
fileprivate let data_sequenceName:String = "evolumeume"

/*: "cancelCall" :*/
fileprivate let main_galleryFunMessage:String = "copportunitync"
fileprivate let k_calculationTitle:[Character] = ["l","l"]

/*: "refuseCall" :*/
fileprivate let noti_gestureData:String = "reflabse"

/*: "onCallMotivation" :*/
fileprivate let app_stoneValue:[Character] = ["o","n","C","a","l","l","M"]
fileprivate let appCommunicationUrl:String = "reachiv"

/*: "onLiveWarning" :*/
fileprivate let notiPerId:String = "close general circle thingonLi"
fileprivate let showVidMsg:String = "field"

/*: "reason" :*/
fileprivate let userStrikeTitle:String = "rshadowson"

/*: "time" :*/
fileprivate let notiThinkUrl:String = "timpole"

/*: "onStopLive" :*/
fileprivate let show_readHeatValue:String = "storage fair expected play naponSto"

/*: "onSendProgramGift" :*/
fileprivate let userFiniteStr:[Character] = ["o","n","S","e","n","d","P","r","o","g","r","a","m","G","i"]
fileprivate let main_depthSimultaneouslyText:String = "par"

/*: "onGameFs" :*/
fileprivate let app_anonymousUrl:String = "onGameFstransport brace"

/*: "broadStatus" :*/
fileprivate let main_unitHungStr:String = "request dit always leasebroadS"
fileprivate let main_pingKey:String = "tatdevices"

/*: "onCallChatMsg" :*/
fileprivate let dataBoldReloadBoneStr:[Character] = ["o","n","C","a","l"]
fileprivate let data_licenseHungName:[Character] = ["l","C","h","a","t","M","s","g"]

/*: "onNotificationPopupWin" :*/
fileprivate let const_reportName:[Character] = ["o","n","N","o","t","i","f","i","c","a","t","i","o","n","P","o","p"]
fileprivate let show_memberData:String = "upWinclass cross"

/*: "onUserBlock" :*/
fileprivate let mainDuringId:[Character] = ["o","n","U","s","e","r","B","l","o","c","k"]

/*: "isRecharge" :*/
fileprivate let app_brideStr:[Character] = ["i","s","R","e"]
fileprivate let k_keepUrl:String = "chatakee"

/*: "source" :*/
fileprivate let show_translationId:String = "souapplye"

/*: "invitePop" :*/
fileprivate let const_severalPath:[Character] = ["i","n","v","i","t","e","P","o","p"]

/*: "videoPop" :*/
fileprivate let data_interactionValue:String = "vimic"
fileprivate let kSuitMessage:[Character] = ["e","o","P","o","p"]

/*: "coin" :*/
fileprivate let user_primaryMessage:String = "COIN"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SantaLuciaFirSocketDelegate.swift
//  AbroadTalking
//
//  Created by young on 2022/9/13.
//

//: import SocketRocket
import SocketRocket
//: import UIKit
import UIKit

// 保活心跳包ping的时间间隔
//: let SOCKET_PING_INTERVAL = 60.0
let k_appPath = 60.0
// 视频通话心跳包时间间隔
//: let SOCKET_CALL_INTERVAL = 20.0
let dataInformationMeetingMessage = 20.0
// 直播心跳包时间间隔
//: let SOCKET_LIVE_INTERVAL = 20.0
let showReplaceData = 20.0
// 失败重试次数
//: let SOCKET_ADDR_RETRY_COUNT = 3
let data_titleStr = 3
// socket连接超时时间
//: let SOCKET_MANAGER_CONNECT_TIMEOUT = 30.0
let appMaleId = 30.0
// socket连接成功缓存key
//: let SOCKET_ADDR_KEY = "SOCKET_ADDR_KEY"
let const_qualityText = (String(constTheaterTitle) + kAccuracyName.uppercased())

// 错误码
//: let SOCKET_MANAGER_UNCONNECTED_CODE = 5001
let kWarnData = 5001
//: let SOCKET_MANAGER_SELF_BUSY_CODE   = 5002
let k_adjustContent = 5002
//: let SOCKET_WITHOUT_NET_CODE         = 5003
let noti_fileStr = 5003
//: let SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE = 1002
let constMessageRejectTitle = 1002

// 接收方收到音视频通话通知
//: public let SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION = NSNotification.Name(rawValue: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION")
public let show_logData = NSNotification.Name(rawValue: String(bytes: user_platformTitle.map{$0^114}, encoding: .utf8)!)
// 游戏飘屏通知
//: public let RECIVE_FLOATSCREEN_NOTIFICATION = NSNotification.Name(rawValue: "RECIVE_FLOATSCREEN_NOTIFICATION")
public let notiTopMsg = NSNotification.Name(rawValue: String(bytes: userSurgeryData.reversed(), encoding: .utf8)!)

// MARK: - protocol

// MARK: - socket失败代理

/// socket收到失败消息
//: protocol TalkingSocketManagerErrorDelegate: NSObjectProtocol {
protocol EnableceErrorDelegate: NSObjectProtocol {
    /// socket收到失败消息
    /// - Parameters:
    ///   - errorNo: 失败编码
    ///   - cmd: 命令
    ///   - msg: 失败信息
    ///  - data: 数据
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any])
    func assemblage(errorNo: Int, cmd: String, msg: String, data: [String: Any])
}

// MARK: - 视频速配代理

/// 视频速配代理（男）
//: protocol SocketManagerVideoMatchDelegate: NSObjectProtocol {
protocol ManagingDirectorObjectProtocol: NSObjectProtocol {
    /// 视频速配成功
    /// - Parameter data: 速配数据
    //: func socket_match_onVideoMatch(data: [String: Any])
    func attributeSuspend(data: [String: Any])

    /// 匹配成功，可以拨打通话
    /// - Parameter data: 速配数据
    //: func socket_match_onRequestCall(data: [String: Any])
    func aggregation(data: [String: Any])
}

// MARK: - 音视频通话代理

/// 视频通话【拨打方】代理
//: protocol SocketManagerVideoChatCallerDelegate: NSObjectProtocol {
protocol AssetsObjectProtocol: NSObjectProtocol {
    /// 接收方接受了来电
    /// - Parameter data: 数据
    //: func socket_videoChat_acceptCall(data: [String: Any])
    func layer(data: [String: Any])

    /// 接收方挂断了通话（通话中）
    /// - Parameter data: 数据
    //: func socket_videoChat_stopCall(data: [String: Any])
    func earRate(data: [String: Any])

    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_cancelCall(data: [String: Any])
    func milage(data: [String: Any])

    /// 通话余额不足提醒
    /// - Parameters:
    ///   - msg: 提醒信息
    ///   - data: 数据
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any])
    func maleData(msg: String, data: [String: Any])
}

/// 视频通话【接收方】代理
//: @objc protocol SocketManagerVideoChatCalledPartyDelegate: NSObjectProtocol {
@objc protocol MedalSolarObjectProtocol: NSObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any])
    func sumRelationData(data: [String: Any])

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    /// - Parameter data: 数据
    //: func socket_videoChat_onAcceptCall(data: [String: Any])
    func information(data: [String: Any])

    /// 女性通话积分激励动效展示
    /// - Parameters:
    ///   - data: 数据
    //: @objc optional func socket_videoChat_onCallMotivation(data: [String: Any])
    @objc optional func valueData(data: [String: Any])
}

//: class TalkingSocketManager: NSObject {
class SantaLuciaFirSocketDelegate: NSObject {
    //: enum SocketRetryType: String {
    enum RegularMovement: String {
        //: case AppLogin = "用户登录"
        case AppLogin = "用户登录"
        //: case NetWork = "网络重连"
        case NetWork = "网络重连"
        //: case EnterForeground = "回到APP"
        case EnterForeground = "回到APP"
        //: case SocketFail = "连接失败"
        case SocketFail = "连接失败"
        //: case SocketClose = "连接关闭"
        case SocketClose = "连接关闭"
        //: case SocketSendMsg = "发送消息失败"
        case SocketSendMsg = "发送消息失败"
    }

    /// 音视频最小化
    //: var currInfoVC: AnyObject?
    var currInfoVC: AnyObject? // 当前正在通话的音视频视图
    //: var videoMiniPosition = CGPoint(
    var videoMiniPosition = CGPoint( // 音视频小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: RatingReactiveCompatible.shared.direction == .rightToLeft ? 0 : main_logStr,
        //: y: StatusBarHeight+190
        y: dataPointTitle + 190
    )
    //: var liveMiniPosition = CGPoint(
    var liveMiniPosition = CGPoint( // 直播小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: RatingReactiveCompatible.shared.direction == .rightToLeft ? 0 : main_logStr,
        //: y: StatusBarHeight+190
        y: dataPointTitle + 190
    )
    //: var containerView: UIView?
    var containerView: UIView? // 小窗口容器

    /// delegate
    //: weak var errorDelegate: TalkingSocketManagerErrorDelegate?
    weak var errorDelegate: EnableceErrorDelegate? // error !=0 的异常处理
    //: weak var videoMatchDelegate: SocketManagerVideoMatchDelegate?
    weak var videoMatchDelegate: ManagingDirectorObjectProtocol? // 视频通话速配
    //: weak var callerDelegate: SocketManagerVideoChatCallerDelegate?
    weak var callerDelegate: AssetsObjectProtocol? // 视频通话【拨打方】
    //: weak var calledPartyDelegate: SocketManagerVideoChatCalledPartyDelegate?
    weak var calledPartyDelegate: MedalSolarObjectProtocol? // 视频通话【接收方】

    //: private var webSocket: SRWebSocket?
    private var webSocket: SRWebSocket?
    //: private var keepAliveTimer: Timer?
    private var keepAliveTimer: Timer? // socket保活心跳包
    //: private var connectUrlStr: String?
    private var connectUrlStr: String? // socket连接地址
    //: private var linkIndex = 0
    private var linkIndex = 0 // 当前连接地址索引
    //: private var linkAddrArr: [String]?
    private var linkAddrArr: [String]? // 连接地址数组
    //: private var failureCount = 0
    private var failureCount = 0 // 连接失败次数
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()

    /// talking
    //: var needReconnect = false
    var needReconnect = false // 断网重连socket
    //: @objc dynamic var isInfo = false
    @objc dynamic var isInfo = false // 是否视频通话中
    //: @objc dynamic var isCalling = false
    @objc dynamic var isCalling = false // 是否呼叫中
    //: private var talkTimer: Timer?
    private var talkTimer: Timer? // 视频通话心跳包
    //: private var liveTimer: Timer?
    private var liveTimer: Timer? // 直播心跳包
    //: private var unHandleCallData: [String: Any]?
    private var unHandleCallData: [String: Any]? // 在后台收到的接听消息
    //: private var cancelCall = false
    private var cancelCall = false // 拨打方是否停止呼叫（还未打通）
    //: var isFreeCall = false
    var isFreeCall = false // 是否发起免费通话

    // singleton
    //: static let shared = TalkingSocketManager()
    static let shared = SantaLuciaFirSocketDelegate()
    //: override func copy() -> Any { return self }
    override func copy() -> Any { return self }
    //: override func mutableCopy() -> Any { return self }
    override func mutableCopy() -> Any { return self }
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        // 网络状态监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(yearTo(note:)),
                                               //: name: .reachabilityChanged,
                                               name: .reachabilityChanged,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(checkWebsocket),
                                               selector: #selector(willSelect),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 音视频呼叫时
        //: self.rx
        self.rx
            //: .observeWeakly(Bool.self, "isCalling")
            .observeWeakly(Bool.self, (String(notiMidMsg) + mainScanName.replacingOccurrences(of: "relation", with: "g")))
            //: .subscribe(onNext: {(value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (AdvertisementReactiveCompatible.shared as! AdvertisementReactiveCompatible).accelerateTimer()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)

        // 音视频通话时
        //: self.rx
        self.rx
            .observeWeakly(Bool.self, "isInfo")
            //: .subscribe(onNext: { (value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (AdvertisementReactiveCompatible.shared as! AdvertisementReactiveCompatible).accelerateTimer()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

// MARK: - SRWebSocketDelegate

//: extension TalkingSocketManager: SRWebSocketDelegate {
extension SantaLuciaFirSocketDelegate: SRWebSocketDelegate {
    //: func webSocketDidOpen(_ webSocket: SRWebSocket!) {
    func webSocketDidOpen(_ webSocket: SRWebSocket!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: UploadLogTool.writeLog(msg: "SOCKET 连接成功.")
        MateThen.trackBy(msg: (k_somethingKey.replacingOccurrences(of: "po", with: "C") + " 连接成" + String(notiSuspendValue)))
        //: sendPing()
        kickSwitcheSHeelsPing()
        //: NotificationCenter.default.post(name: SOCKET_IS_OPEN_NOTIFICATION,
        NotificationCenter.default.post(name: k_skipMessage,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: nil)
                                        userInfo: nil)
        // 还原断网重连状态
        //: self.needReconnect = false
        self.needReconnect = false

        //: self.failureCount = 0
        self.failureCount = 0

        // 保存成功连接的记录
        //: if self.linkIndex < self.linkAddrArr?.count ?? 0 {
        if self.linkIndex < self.linkAddrArr?.count ?? 0 {
            //: UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: SOCKET_ADDR_KEY)
            UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: const_qualityText)
        }
        // 创建音视频最小化窗口容器
        //: mini_createContainerView()
        placeOfOrigin()
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
    func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
        //: UploadLogTool.writeLog(msg: "SOCKET 连接失败, error:\(error.localizedDescription).")
        MateThen.trackBy(msg: (k_bigFormat.replacingOccurrences(of: "again", with: "OC") + "ET \u{8fde}" + noti_parkData + "rror:") + "\(error.localizedDescription).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: guard let status = DismissReactiveCompatible.share.reachability?.connection, status != .unavailable else {
        guard let status = DismissReactiveCompatible.share.reachability?.connection, status != .unavailable else {
            //: return
            return
        }
        //: if self.failureCount == SOCKET_ADDR_RETRY_COUNT {
        if self.failureCount == data_titleStr {
            //: self.failureCount = 0
            self.failureCount = 0
            //: self.linkIndex = (self.linkIndex == 0) ? 1:0
            self.linkIndex = (self.linkIndex == 0) ? 1 : 0
            //: self.connectUrlStr = getWebSocketPath(isRetry: true)
            self.connectUrlStr = authority(isRetry: true)
            //: UploadLogTool.writeLog(msg: "SOCKET 失败\(SOCKET_ADDR_RETRY_COUNT)次, 切换过Socket域名重新尝试，地址：\(connectUrlStr ?? "").")
            MateThen.trackBy(msg: (const_fillTitle.replacingOccurrences(of: "notice", with: "O") + appPolicyValue) + "\(data_titleStr)" + (String(data_activityKingdomMsg) + String(dataLeagueReaderId.prefix(5)) + String(data_overwhelmKey)) + "\(connectUrlStr ?? "").")
            //: } else {
        } else {
            //: self.failureCount += 1
            self.failureCount += 1
        }
        //: self.reconnectWebSocket(timeInterval: 2, type: .SocketFail)
        self.replyPlayer(timeInterval: 2, type: .SocketFail)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
    func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: self.handleReceiveMessage(message: message as? String)
        self.miss(message: message as? String)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean: Bool) {
    func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean _: Bool) {
        //: UploadLogTool.writeLog(msg: "SOCKET 已关闭, code:\(code), reason:\(String(describing: reason)).")
        MateThen.trackBy(msg: (data_tagMemoryImportMsg.replacingOccurrences(of: "selection", with: "K") + user_trendName + ", code:") + "\(code)" + (String(dataDiscourseAnimaContent.prefix(4)) + "ason:") + "\(String(describing: reason)).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: self.closeWebSocket(userLoginOut: false)
        self.featureOut(userLoginOut: false)
        //: if code == SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE {
        if code == constMessageRejectTitle {
            //: return
            return
        }
        //: if let status = DismissReactiveCompatible.share.reachability?.connection, status != .unavailable {
        if let status = DismissReactiveCompatible.share.reachability?.connection, status != .unavailable {
            //: self.reconnectWebSocket(timeInterval: 5, type: .SocketClose)
            self.replyPlayer(timeInterval: 5, type: .SocketClose)
        }
    }

    /// 获取socket地址
    //: func getWebSocketPath(isRetry: Bool) -> String? {
    func authority(isRetry: Bool) -> String? {
        //: var linkAddr = ""
        var linkAddr = ""
        //: self.linkAddrArr = DismissReactiveCompatible.share.appConfigMode.ws
        self.linkAddrArr = DismissReactiveCompatible.share.appConfigMode.ws
        //: if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
        if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
            //: self.linkAddrArr = defaultWSArr()
            self.linkAddrArr = starArr()
        }
        //: let lastAddr = UserDefaults.standard.string(forKey: SOCKET_ADDR_KEY)
        let lastAddr = UserDefaults.standard.string(forKey: const_qualityText)
        //: if lastAddr == nil || isRetry {
        if lastAddr == nil || isRetry {
            //: if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
            if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
                //: linkAddr = temLinkAddr
                linkAddr = temLinkAddr
                //: } else {
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
            //: } else {
        } else {
            //: if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
            if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
                //: self.linkIndex = index
                self.linkIndex = index
                //: linkAddr = lastAddr!
                linkAddr = lastAddr!
                //: }else{
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
        }

        //: let uid = DismissReactiveCompatible.share.loginUid
        let uid = DismissReactiveCompatible.share.loginUid
        //: let sid = DismissReactiveCompatible.share.loginSessionId
        let sid = DismissReactiveCompatible.share.loginSessionId
        //: let address = "ws://\(linkAddr)/?sid=\(sid)&uid=\(uid)&platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)"
        let address = "ws://\(linkAddr)" + (String(constReadyNaturalValue.prefix(6))) + "\(sid)" + (String(showPartyData.prefix(5))) + "\(uid)" + (String(show_askSumStr.prefix(6)) + "orm=" + String(const_reduceNatureName.prefix(8)) + String(userStaffPath.prefix(7))) + "\(userCountData)" + (String(showBasicSeparateName)) + "\(userLengthValue)"
        //: return address
        return address
    }

    /// 默认地址
    //: func defaultWSArr() -> [String] {
    func starArr() -> [String] {
        //: return Environment_Formal ? ["1v1server.\(ReplaceUrlDomain).com:7766"] : ["43.129.185.194:7767"]
        return kAddId ? [(String(main_skiPath) + String(app_bagKey.prefix(3))) + "\(showReasonContent)" + (String(main_permissionFormat.suffix(5)) + "7766")] : [(kSmartStr.capitalized + String(userCommitText) + k_blueFormat.capitalized)]
    }
}

// MARK: - NotificationCenter

//: extension TalkingSocketManager {
extension SantaLuciaFirSocketDelegate {
    /// 网络监听通知
    //: @objc func reachabilityChanged(note: Notification) {
    @objc func yearTo(note: Notification) {
        //: let reachability = note.object as? Reachability
        let reachability = note.object as? Reachability
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            // 断网, 关闭socket
            //: UploadLogTool.writeLog(msg: "SOCKET 网络reachabilityChanged监听，断网关闭.")
            MateThen.trackBy(msg: String(bytes: const_continuingFormat.reversed(), encoding: .utf8)!)
            //: self.closeWebSocket(userLoginOut: false)
            self.featureOut(userLoginOut: false)
            //: } else {
        } else {
            // 网络原因重连
            //: self.reconnectWebSocket(timeInterval: 2.0, type: .NetWork)
            self.replyPlayer(timeInterval: 2.0, type: .NetWork)
        }
    }

    /// 检测socket链接状态
    //: @objc func checkWebsocket() {
    @objc func willSelect() {
        //: self.reconnectWebSocket(timeInterval: 2.0, type: .EnterForeground)
        self.replyPlayer(timeInterval: 2.0, type: .EnterForeground)
        // 处理通话逻辑
        //: self.handleCallData()
        self.informationPause()
    }
}

// MARK: - Event

//: extension TalkingSocketManager {
extension SantaLuciaFirSocketDelegate {
    /// socket保活心跳包
    //: private func sendPing() {
    private func kickSwitcheSHeelsPing() {
        //: self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_PING_INTERVAL, block: { timer in
        self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: k_appPath, block: { _ in
            //: self.sendMessage(info: ["cmd": "ping"])
            self.amOrigin(info: [(String(constOrientationId)): (constReachName.lowercased())])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 开启视频通话心跳包
    //: private func startTalkPing() {
    private func systemCoquette() {
        //: endTalkingPing()
        phoneDay()

        //: self.talkTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_CALL_INTERVAL, block: {  timer in
        self.talkTimer = Timer.scheduledTimer(withTimeInterval: dataInformationMeetingMessage, block: { _ in
            /// 不在通话中，不在音视频通话页，没有开启最小化时
            //: if !self.isInfo || (self.currentViewController()?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currentViewController()?.isKind(of: TalkingVoiceChatViewController.self) == false ) && (self.currInfoVC?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currInfoVC?.isKind(of: TalkingVoiceChatViewController.self) == false ) {
            if !self.isInfo || (self.exitSchedule()?.isKind(of: VisualCommunicationViewDelegate.self) == false && self.exitSchedule()?.isKind(of: TrailerRecognizerDelegate.self) == false) && (self.currInfoVC?.isKind(of: VisualCommunicationViewDelegate.self) == false && self.currInfoVC?.isKind(of: TrailerRecognizerDelegate.self) == false) {
                //: return
                return
            }
            //: self.sendMessage(info: ["cmd": "callHeartbeat"])
            self.amOrigin(info: [(String(constOrientationId)): (String(mainCropId))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束视频通话心跳包
    //: func endTalkingPing() {
    func phoneDay() {
        //: if self.talkTimer != nil {
        if self.talkTimer != nil {
            //: self.talkTimer?.invalidate()
            self.talkTimer?.invalidate()
            //: self.talkTimer = nil
            self.talkTimer = nil
        }
    }

    /// 处理通话逻辑
    //: func handleCallData() {
    func informationPause() {
        //: if self.cancelCall == true {
        if self.cancelCall == true { // 拨打方已停止呼叫
            //: self.cancelCall = false
            self.cancelCall = false
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
            //: return
            return
        }

        //: if self.unHandleCallData != nil {
        if self.unHandleCallData != nil {
            //: let recievedTime = self.unHandleCallData!["recievedDate"] as! Date
            let recievedTime = self.unHandleCallData![(String(app_hiddenMessage) + noti_enableText.replacingOccurrences(of: "electronic", with: "te"))] as! Date
            //: let currentTime = Date()
            let currentTime = Date()
            //: if currentTime.timeIntervalSince(recievedTime) < VIDEO_1v1_RECIEVED_CALL_TIMEOUT {
            if currentTime.timeIntervalSince(recievedTime) < data_replacePath {
                //: DismissReactiveCompatible.share.recieve1v1TalkRequest(info: self.unHandleCallData!)
                DismissReactiveCompatible.share.shine(info: self.unHandleCallData!)
            }
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
        }
    }
}

// MARK: - 直播心跳包

//: extension TalkingSocketManager {
extension SantaLuciaFirSocketDelegate {
    /// 开启直播心跳包
    //: func startLivePing() {
    func workMutual() {
        //: endLivePing()
        dotLimited()
        //: self.liveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_LIVE_INTERVAL, block: { timer in
        self.liveTimer = Timer.scheduledTimer(withTimeInterval: showReplaceData, block: { _ in
            //: self.sendMessage(info: ["cmd": "liveHeartBeat"])
            self.amOrigin(info: [(String(constOrientationId)): (showTranslateName.replacingOccurrences(of: "transition", with: "v") + String(k_assignMsg))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束直播心跳包
    //: func endLivePing() {
    func dotLimited() {
        //: if self.liveTimer != nil {
        if self.liveTimer != nil {
            //: self.liveTimer?.invalidate()
            self.liveTimer?.invalidate()
            //: self.liveTimer = nil
            self.liveTimer = nil
        }
    }
}

// MARK: - 音视频窗口最小化

//: extension TalkingSocketManager {
extension SantaLuciaFirSocketDelegate {
    /// 创建窗口最小化容器
    //: private func mini_createContainerView() {
    private func placeOfOrigin() {
        //: guard self.containerView == nil else { return }
        guard self.containerView == nil else { return }

        //: let windowView = TalkingMiniContainerView()
        let windowView = PrecociousView()
        //: let keyWindow = DepictionThen.getWindow()
        let keyWindow = DepictionThen.fossilization()
        //: windowView.frame = keyWindow.bounds
        windowView.frame = keyWindow.bounds
        //: windowView.backgroundColor = .clear
        windowView.backgroundColor = .clear
        //: keyWindow.addSubview(windowView)
        keyWindow.addSubview(windowView)
        //: self.containerView = windowView
        self.containerView = windowView
        //: self.containerView?.isHidden = !self.isInfo
        self.containerView?.isHidden = !self.isInfo
    }

    /// 移除窗口最小化容器
    //: private func mini_removeContainerView() {
    private func pressCharacter() {
        //: guard self.containerView != nil else { return }
        guard self.containerView != nil else { return }
        // 防止抢登录时，用户还在直播中
        //: guard TalkingLiveManager.shared().isLive == false else { return }
        guard ManagerPushListener.nowPanel().isLive == false else { return }

        //: self.containerView?.removeAllSubviews()
        self.containerView?.removeAllSubviews()
        //: self.containerView?.removeFromSuperview()
        self.containerView?.removeFromSuperview()
        //: self.containerView = nil
        self.containerView = nil
    }
}

// MARK: - Socket

//: extension TalkingSocketManager {
extension SantaLuciaFirSocketDelegate {
    /// 关闭socket
    /// - Parameter userLoginOut: 是否是用户退出
    //: func closeWebSocket(userLoginOut: Bool) {
    func featureOut(userLoginOut: Bool) {
        //: printLog(message: "🔗 关闭 socket")
        printLog(message: (String(constOppositeMessage) + userBassStr.replacingOccurrences(of: "another", with: "ke")))
        //: if self.webSocket != nil {
        if self.webSocket != nil {
            //: self.webSocket?.delegate = nil
            self.webSocket?.delegate = nil
            //: if userLoginOut == true {
            if userLoginOut == true {
                //: self.webSocket?.close(withCode: SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE, reason: "The user logs out")
                self.webSocket?.close(withCode: constMessageRejectTitle, reason: (String(mainDeadlineKey.prefix(5)) + "ser " + String(const_enhanceValue.suffix(5)) + String(kLivePath)))
                //: } else {
            } else {
                //: self.webSocket?.close()
                self.webSocket?.close()
            }
            //: self.webSocket = nil
            self.webSocket = nil
        }

        //: if self.keepAliveTimer != nil {
        if self.keepAliveTimer != nil {
            //: self.keepAliveTimer?.invalidate()
            self.keepAliveTimer?.invalidate()
            //: self.keepAliveTimer = nil
            self.keepAliveTimer = nil
        }

        //: endTalkingPing()
        phoneDay()

        //: if userLoginOut == true {
        if userLoginOut == true {
            //: self.connectUrlStr = nil
            self.connectUrlStr = nil
            // 销毁最小化窗口
            //: mini_removeContainerView()
            pressCharacter()
        }
    }

    /// 更新socket连接
    //: func updateWebSocket() {
    func makeupPremium() {
        //: self.connectUrlStr = TalkingSocketManager.shared.getWebSocketPath(isRetry: false)
        self.connectUrlStr = SantaLuciaFirSocketDelegate.shared.authority(isRetry: false)
        //: self.reconnectWebSocket(timeInterval: 0, type: .AppLogin)
        self.replyPlayer(timeInterval: 0, type: .AppLogin)
    }

    /// 重连socket
    //: private func reconnectWebSocket(timeInterval: TimeInterval, type: SocketRetryType) {
    private func replyPlayer(timeInterval: TimeInterval, type: RegularMovement) {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
            //: UploadLogTool.writeLog(msg: "SOCKET \(type.rawValue)重连. 重试次数：\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            MateThen.trackBy(msg: (String(data_usVarietyWrittenId)) + "\(type.rawValue)" + (show_thinMsg + "试次\u{6570}：") + "\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            //: self.connectWebSocket(urlStr: self.connectUrlStr)
            self.dealStr(urlStr: self.connectUrlStr)
        }
    }

    /// 连接socket
    //: private func connectWebSocket(urlStr: String?) {
    private func dealStr(urlStr: String?) {
        //: guard urlStr?.isEmpty == false else { return }
        guard urlStr?.isEmpty == false else { return }

        //: if urlStr == self.connectUrlStr && self.webSocket != nil && (self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING) {
        if urlStr == self.connectUrlStr, self.webSocket != nil, self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING {
            //: if (self.webSocket?.readyState == .OPEN) {
            if self.webSocket?.readyState == .OPEN {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接正常 不需要重新连接.")
                MateThen.trackBy(msg: (String(noti_bringData) + String(userSuccessId)))
                //: } else {
            } else {
                //: UploadLogTool.writeLog(msg: "SOCKET 正在连接 不需要重新连接.")
                MateThen.trackBy(msg: (String(notiLoadKey) + kRelationId))
            }
            //: return
            return
        }

        //: self.connectUrlStr = urlStr
        self.connectUrlStr = urlStr

        //: var newStr = urlStr!
        var newStr = urlStr!
        //: if self.isInfo {
        if self.isInfo {
            //: newStr = newStr.appending("&resume=1")
            newStr = newStr.appending((String(constPastFormat.suffix(5)) + "me=1"))
        }
        //: newStr = newStr.appending("&lang=\(TalkingRequestAddrTool.share.interfaceLang)")
        newStr = newStr.appending((String(show_inputMessage.suffix(6))) + "\(ManufacturerThen.share.interfaceLang)")
        //: closeWebSocket(userLoginOut: false)
        featureOut(userLoginOut: false)

        //: let url = URL(string: newStr)
        let url = URL(string: newStr)
        //: var request = URLRequest(url: url!)
        var request = URLRequest(url: url!)
        //: request.timeoutInterval = SOCKET_MANAGER_CONNECT_TIMEOUT
        request.timeoutInterval = appMaleId
        //: self.webSocket = SRWebSocket(urlRequest: request)
        self.webSocket = SRWebSocket(urlRequest: request)
        //: self.webSocket?.delegate = self
        self.webSocket?.delegate = self
        //: self.webSocket?.open()
        self.webSocket?.open()
        //: UploadLogTool.writeLog(msg: "SOCKET 开始连接, 地址：\(newStr).")
        MateThen.trackBy(msg: (main_flexibleFormat.replacingOccurrences(of: "don", with: "E") + " 开始\u{8fde}" + main_wordAgreeMsg.replacingOccurrences(of: "pad", with: "接")) + "\(newStr).")
    }

    /// 发送消息
    /// - Parameter info: 消息体
    /// - Returns: 发送结果
    //: @discardableResult
    @discardableResult
    //: func sendMessage(info: [String: Any]?) -> Bool {
    func amOrigin(info: [String: Any]?) -> Bool {
        //: guard info != nil else { return false }
        guard info != nil else { return false }

        // 1. 判断是否有网络连接
        //: if DismissReactiveCompatible.share.reachability?.connection == nil || DismissReactiveCompatible.share.reachability?.connection == .unavailable {
        if DismissReactiveCompatible.share.reachability?.connection == nil || DismissReactiveCompatible.share.reachability?.connection == .unavailable {
            //: let info: [String: Any] = ["cmd": "onNetDisabled",
            let info: [String: Any] = [(String(constOrientationId)): (String(noti_gatherMessage) + String(notiSpanTitle.suffix(4)) + "Disa" + constObjectUrl.replacingOccurrences(of: "angle", with: "d")),
                                       //: "errno": "\(SOCKET_WITHOUT_NET_CODE)",
                                       (user_accelerateKey.lowercased()): "\(noti_fileStr)",
                                       //: "data": [:]]
                                       (String(user_unknownDateMessage)): [:]]
            //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: handleReceiveMessage(message: message)
            miss(message: message)
            //: return false
            return false
        }

        // 2. 尝试发送消息
        //: if self.webSocket?.readyState == SRReadyState.OPEN {
        if self.webSocket?.readyState == SRReadyState.OPEN {
            //: let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: if info!["cmd"] as? String == "ping" {
            if info![(String(constOrientationId))] as? String == (constReachName.lowercased()) {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接保活cmd: ping.")
                MateThen.trackBy(msg: (String(userMouthMessage) + String(kClipViaUrl) + String(showDetailedData.prefix(5))))
                //: } else if info!["cmd"] as? String == "callHeartbeat" {
            } else if info![(String(constOrientationId))] as? String == (String(mainCropId)) {
                //: UploadLogTool.writeLog(msg: "SOCKET trtc保活cmd: callHeartbeat.")
                MateThen.trackBy(msg: String(bytes: constHistoryValue.reversed(), encoding: .utf8)!)
                //: } else if info!["cmd"] as? String == "liveHeartBeat" {
            } else if info![(String(constOrientationId))] as? String == (showTranslateName.replacingOccurrences(of: "transition", with: "v") + String(k_assignMsg)) {
                //: UploadLogTool.writeLog(msg: "SOCKET live保活cmd: liveHeartBeat.")
                MateThen.trackBy(msg: String(bytes: kVarietyUrl.map{$0^238}, encoding: .utf8)!)
            }
            //: self.webSocket?.send(message)
            self.webSocket?.send(message)
            //: return true
            return true

            //: } else {
        } else {
            //: if info!["cmd"] as? String == "ping" {
            if info![(String(constOrientationId))] as? String == (constReachName.lowercased()) {
                //: let info: [String: Any] = ["cmd": "onP",
                let info: [String: Any] = [(String(constOrientationId)): (String(constFloorUrl.suffix(3))),
                                           //: "errno": "\(SOCKET_MANAGER_UNCONNECTED_CODE)",
                                           (user_accelerateKey.lowercased()): "\(kWarnData)",
                                           //: "data": [:]]
                                           (String(user_unknownDateMessage)): [:]]
                //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                //: let message = String(data: data!, encoding: String.Encoding.utf8)
                let message = String(data: data!, encoding: String.Encoding.utf8)
                //: handleReceiveMessage(message: message)
                miss(message: message)
            }
            //: self.reconnectWebSocket(timeInterval: 5.0, type: .SocketSendMsg)
            self.replyPlayer(timeInterval: 5.0, type: .SocketSendMsg)
            //: return false
            return false
        }
    }

    /// 处理接收到的消息
    /// - Parameter message: 消息体
    //: private func handleReceiveMessage(message: String?) {
    private func miss(message: String?) {
        //: guard message != nil else { return }
        guard message != nil else { return }

        //: let json = JSON(parseJSON: message!)
        let json = JSON(parseJSON: message!)
        //: let cmdName = json["cmd"].stringValue
        let cmdName = json[(String(constOrientationId))].stringValue
        //: let errorNo = json["errno"].intValue
        let errorNo = json[(user_accelerateKey.lowercased())].intValue
        //: let msg = json["msg"].stringValue
        let msg = json[(kBigValue.replacingOccurrences(of: "fragment", with: "s"))].stringValue
        //: let data = json["data"].dictionaryObject ?? [:]
        let data = json[(String(user_unknownDateMessage))].dictionaryObject ?? [:]

        //: printLog(message: "🔗 处理消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data)")
        printLog(message: (showDoingerPolicyKey.replacingOccurrences(of: "flat", with: "理") + "消息：cm" + String(app_tooValue.suffix(6))) + "\(cmdName)" + (String(k_visitorId.prefix(4)) + "rNo:") + "\(errorNo)" + (String(appRemainingMsg.suffix(6))) + "\(msg)" + (String(user_wedMessage.prefix(6)) + userMinimumUponId.capitalized) + "\(data)")

        // 检测是否需要恢复直播
        //: check_stopOrRestoreLive(cmdName)
        destroyLive(cmdName)

        // 处理异常
        //: guard errorNo == 0 else {
        guard errorNo == 0 else {
            //: UploadLogTool.writeLog(msg: "SOCKET 接收异常消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data).")
            MateThen.trackBy(msg: (mainLifetimeMsg + showApplyFormat + "：cmdName:") + "\(cmdName)" + (String(k_visitorId.prefix(4)) + "rNo:") + "\(errorNo)" + (String(appRemainingMsg.suffix(6))) + "\(msg)" + (String(user_wedMessage.prefix(6)) + userMinimumUponId.capitalized) + "\(data).")
            //: if errorNo == 444 {
            if errorNo == 444 {
                //: DismissReactiveCompatible.share.loginUserMode.freeCallTimes = 0
                DismissReactiveCompatible.share.loginUserMode.freeCallTimes = 0
                //: } else if errorNo == 60003, !DismissReactiveCompatible.share.loginUserMode.isNaUser {
            } else if errorNo == 60003, !DismissReactiveCompatible.share.loginUserMode.isNaUser {
                /// 没真人认证时，需要弹窗提示
                //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
                OutletWindowManager.shared.totaleraction()
                //: return
                return
            }
            //: self.errorDelegate?.socket_didRecieveError(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            self.errorDelegate?.assemblage(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            //: return
            return
        }

        // 处理正常接收的消息
        //: if cmdName == "onReceivedGift" { // 礼物
        if cmdName == (String(showOkName) + kVisibleData.replacingOccurrences(of: "me", with: "e") + app_mirrorData.replacingOccurrences(of: "submit", with: "ve") + "Gift") { // 礼物
            //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
            NotificationCenter.default.post(name: userThirdFormat, object: data)

            //: } else if cmdName == "onVideoMatch" { // 视频速配
        } else if cmdName == (String(notiLetterMessage.prefix(7)) + String(mainHundredMsg)) { // 视频速配
            //: self.videoMatchDelegate?.socket_match_onVideoMatch(data: data)
            self.videoMatchDelegate?.attributeSuspend(data: data)

            //: } else if cmdName == "onRequestCall" { // 拨打方发出requestCall回执
        } else if cmdName == (String(appCapacityValue)) { // 拨打方发出requestCall回执
            //: self.isCalling = true
            self.isCalling = true
            // 视频速配 || 音视频通话
            //: self.videoMatchDelegate?.socket_match_onRequestCall(data: data)
            self.videoMatchDelegate?.aggregation(data: data)

            //: } else if cmdName == "onCancelCall" { // 拨打方发出cancelCall回执，目前无需处理
        } else if cmdName == (String(app_midPath)) { // 拨打方发出cancelCall回执，目前无需处理
            //: } else if cmdName == "onRefuseCall" { // 接收方发送refuseCall回执，目前无需处理
        } else if cmdName == (String(userJudgeName.suffix(7)) + String(showPendingText)) { // 接收方发送refuseCall回执，目前无需处理
            //: } else if cmdName == "onAcceptCall" { // 接收方发送acceptCall回执
        } else if cmdName == (noti_beginKey.lowercased() + String(const_spareData.prefix(5)) + "ptCall") { // 接收方发送acceptCall回执
            //: self.isInfo = true
            self.isInfo = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            systemCoquette()
            //: self.calledPartyDelegate?.socket_videoChat_onAcceptCall(data: data)
            self.calledPartyDelegate?.information(data: data)

            //: } else if cmdName == "onStopCall" { // 接收方发送stopCall回执
        } else if cmdName == (String(main_systemAddFormat)) { // 接收方发送stopCall回执
            //: self.isInfo = false
            self.isInfo = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            phoneDay()
            //: if let freeCallTimes = data["freeCallTimes"] as? Int {
            if let freeCallTimes = data[(String(user_hearFormat) + String(notiGameFormat.prefix(4)) + "allTimes")] as? Int {
                //: DismissReactiveCompatible.share.loginUserMode.freeCallTimes = freeCallTimes
                DismissReactiveCompatible.share.loginUserMode.freeCallTimes = freeCallTimes
            }
            // 更新余额
            //: DismissReactiveCompatible.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            DismissReactiveCompatible.share.loginUserMode.mf_coin = json[(String(user_unknownDateMessage))][(String(const_hideId.prefix(6)))].stringValue

            //: inviteCallPopViewEndShow(data: data)
            baptise(data: data)
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: app_toKey, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (AdvertisementReactiveCompatible.shared as! AdvertisementReactiveCompatible).rewardFront()
            //: } else if cmdName == "onMissCoin" { // 余额不足提醒
        } else if cmdName == (String(user_openingMsg.prefix(5)) + String(k_enhanceKey.prefix(5))) { // 余额不足提醒
            //: self.callerDelegate?.socket_videoChat_onMissCoin(msg: msg, data: data)
            self.callerDelegate?.maleData(msg: msg, data: data)

            //: } else if cmdName == "acceptCall" { // 接收方接受了来电，改变状态为通话中,开始推拉流
        } else if cmdName == (String(appUniformUserKey)) { // 接收方接受了来电，改变状态为通话中,开始推拉流
            //: self.isInfo = true
            self.isInfo = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            systemCoquette()
            //: self.callerDelegate?.socket_videoChat_acceptCall(data: data)
            self.callerDelegate?.layer(data: data)

            //: } else if cmdName == "stopCall" { // 接收方/拨打方 挂断了通话
        } else if cmdName == (notiSuspendRecordPath.replacingOccurrences(of: "others", with: "op") + "Call") { // 接收方/拨打方 挂断了通话
            //: self.isInfo = false
            self.isInfo = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            phoneDay()
            //: self.callerDelegate?.socket_videoChat_stopCall(data: data)
            self.callerDelegate?.earRate(data: data)
            // 更新余额
            //: DismissReactiveCompatible.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            DismissReactiveCompatible.share.loginUserMode.mf_coin = json[(String(user_unknownDateMessage))][(String(const_hideId.prefix(6)))].stringValue
            //: if let freeCallTimes = data["freeCallTimes"] as? Int {
            if let freeCallTimes = data[(String(user_hearFormat) + String(notiGameFormat.prefix(4)) + "allTimes")] as? Int {
                //: DismissReactiveCompatible.share.loginUserMode.freeCallTimes = freeCallTimes
                DismissReactiveCompatible.share.loginUserMode.freeCallTimes = freeCallTimes
            }
            //: inviteCallPopViewEndShow(data: data)
            baptise(data: data)
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: app_toKey, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (AdvertisementReactiveCompatible.shared as! AdvertisementReactiveCompatible).rewardFront()
            //: } else if cmdName == "requestCall" { // 接收方收到了拨打方的申请通话
        } else if cmdName == (showServiceFocusFormat.replacingOccurrences(of: "written", with: "ue") + String(userPhysicsData)) { // 接收方收到了拨打方的申请通话
            //: self.isCalling = true
            self.isCalling = true
            // 发送通知，告知外界处理逻辑
            //: NotificationCenter.default.post(name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION, object: self)
            NotificationCenter.default.post(name: show_logData, object: self)

            //: if UIApplication.shared.applicationState != .background {
            if UIApplication.shared.applicationState != .background { // 应用在前台
                //: DismissReactiveCompatible.share.recieve1v1TalkRequest(info: data)
                DismissReactiveCompatible.share.shine(info: data)
                //: } else {
            } else {
                //: self.cancelCall = false
                self.cancelCall = false
                //: self.unHandleCallData = data
                self.unHandleCallData = data
                //: self.unHandleCallData?["recievedDate"] = Date()
                self.unHandleCallData?[(String(app_hiddenMessage) + noti_enableText.replacingOccurrences(of: "electronic", with: "te"))] = Date()

                // 应用在后台，发送本地推送告知用户
                //: var body = "[Invite you to a video call...]".localized
                var body = String(bytes: user_behaviorBurnValue.map{transitPublicly(bit: $0)}, encoding: .utf8)!.localized
                //: if json["data"]["type"].intValue == 1 { // 音频
                if json[(String(user_unknownDateMessage))][(String(dataUnlessStr))].intValue == 1 { // 音频
                    //: body = "[Invite you to a voice call...]".localized
                    body = String(bytes: showMaleMessage.map{sceneRelated(harassment: $0)}, encoding: .utf8)!.localized
                }
                //: AppDelegateHelper.pushLocalNotification(uid: String(json["data"]["uid"].intValue),
                PortraitReactiveCompatible.join(uid: String(json[(String(user_unknownDateMessage))][(noti_nextMsg.replacingOccurrences(of: "corner", with: "d"))].intValue),
                                       //: title: data["nickname"] as? String,
                                       title: data[(String(kShadowMsg))] as? String,
                                       //: body: body,
                                       body: body,
                                       //: imageUrl: data["headPic"] as? String)
                                       imageUrl: data[(String(user_noteKey.prefix(7)))] as? String)
                // 播放来电音乐
                //: AppDelegateHelper.shared.startPlayBGM()
                PortraitReactiveCompatible.shared.simultaneously()
            }

            //: } else if cmdName == "onCallResume" { // 接收方/发送方 恢复通话中的页面，并开始推拉流
        } else if cmdName == (String(userDraftCutContent) + data_sequenceName.replacingOccurrences(of: "volume", with: "s")) { // 接收方/发送方 恢复通话中的页面，并开始推拉流
            //: self.isInfo = true
            self.isInfo = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            systemCoquette()

            //: var resumeData = data
            var resumeData = data
            //: resumeData["onCallResume"] = "1"
            resumeData[(String(userDraftCutContent) + data_sequenceName.replacingOccurrences(of: "volume", with: "s"))] = "1"
            //: DismissReactiveCompatible.share.recieve1v1TalkResumeRequest(info: resumeData)
            DismissReactiveCompatible.share.athleticGame(info: resumeData)
            //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
            (AdvertisementReactiveCompatible.shared as! AdvertisementReactiveCompatible).accelerateTimer()

            //: } else if cmdName == "cancelCall" { // 拨打方拒绝了通话，接收方收到该指令
        } else if cmdName == (main_galleryFunMessage.replacingOccurrences(of: "opportunity", with: "a") + "elCa" + String(k_calculationTitle)) { // 拨打方拒绝了通话，接收方收到该指令
            //: self.isInfo = false
            self.isInfo = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.cancelCall = true
            self.cancelCall = true
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            phoneDay()
            //: self.callerDelegate?.socket_videoChat_cancelCall(data: data)
            self.callerDelegate?.milage(data: data)
            // 应用在后台
            //: if UIApplication.shared.applicationState == .background {
            if UIApplication.shared.applicationState == .background {
                // 移除来电本地推送
                //: AppDelegateHelper.local_removeDeliveredNotification(identifier: String(json["data"]["uid"].intValue))
                PortraitReactiveCompatible.animal(identifier: String(json[(String(user_unknownDateMessage))][(noti_nextMsg.replacingOccurrences(of: "corner", with: "d"))].intValue))
                // 停止播放来电音乐
                //: AppDelegateHelper.shared.stopPlayBGM()
                PortraitReactiveCompatible.shared.action()
            }

            //: } else if cmdName == "refuseCall" { // 接收方拒绝了通话，拨打方收到该指令
        } else if cmdName == (noti_gestureData.replacingOccurrences(of: "lab", with: "u") + "Call") { // 接收方拒绝了通话，拨打方收到该指令
            //: self.isInfo = false
            self.isInfo = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            phoneDay()
            //: self.calledPartyDelegate?.socket_videoChat_refuseCall(data: data)
            self.calledPartyDelegate?.sumRelationData(data: data)

            //: } else if cmdName == "onCallMotivation" { // 女性通话收到的积分（用于展示赚钱激励动效）
        } else if cmdName == (String(app_stoneValue) + appCommunicationUrl.replacingOccurrences(of: "reach", with: "ot") + "ation") { // 女性通话收到的积分（用于展示赚钱激励动效）
            //: if DismissReactiveCompatible.share.loginUserMode.sex == Gender.female.rawValue {
            if DismissReactiveCompatible.share.loginUserMode.sex == FenceSequence.female.rawValue {
                //: self.calledPartyDelegate?.socket_videoChat_onCallMotivation?(data: data)
                self.calledPartyDelegate?.valueData?(data: data)
            }

            //: } else if cmdName == "onLiveWarning" { // 直播违规警告
        } else if cmdName == (String(notiPerId.suffix(4)) + "veWarnin" + showVidMsg.replacingOccurrences(of: "field", with: "g")) { // 直播违规警告
            //: TalkingLiveManager.live_showWarningView(content: msg, reason: json["data"]["reason"].stringValue, time: json["data"]["time"].stringValue)
            ManagerPushListener.product(content: msg, reason: json[(String(user_unknownDateMessage))][(userStrikeTitle.replacingOccurrences(of: "shadow", with: "ea"))].stringValue, time: json[(String(user_unknownDateMessage))][(notiThinkUrl.replacingOccurrences(of: "pole", with: "e"))].stringValue)

            //: } else if cmdName == "onStopLive" { // 停止直播
        } else if cmdName == (String(show_readHeatValue.suffix(5)) + "pLive") { // 停止直播
            //: TalkingLiveManager.shared().live_releaseAllResource()
            ManagerPushListener.nowPanel().translation()

            //: } else if cmdName == "onSendProgramGift" { // 视频通话节目送礼成功tost
        } else if cmdName == (String(userFiniteStr) + main_depthSimultaneouslyText.replacingOccurrences(of: "par", with: "ft")) { // 视频通话节目送礼成功tost
            //: self.func__showStatusBarSuccessMsg(showMsg: msg)
            self.showGuidanceName(showMsg: msg)

            //: } else if cmdName == "onGameFs" { // 游戏中奖飘屏
        } else if cmdName == (String(app_anonymousUrl.prefix(8))) { // 游戏中奖飘屏
            //: let broadStatus = data["broadStatus"] as? Int ?? -1
            let broadStatus = data[(String(main_unitHungStr.suffix(6)) + main_pingKey.replacingOccurrences(of: "device", with: "u"))] as? Int ?? -1
            //: if DismissReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue {
            if DismissReactiveCompatible.share.appStatus == AfterArtSumLiteral.normal.rawValue {
                //: if broadStatus == 1 {
                if broadStatus == 1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: notiTopMsg, object: self, userInfo: data)
                    //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
                    NotificationCenter.default.post(name: userThirdFormat, object: data)
                    //: } else if broadStatus == -1 {
                } else if broadStatus == -1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: notiTopMsg, object: self, userInfo: data)
                }
            }

            //: } else if cmdName == "onCallChatMsg" { // 收到视频通话中的聊天消息
        } else if cmdName == (String(dataBoldReloadBoneStr) + String(data_licenseHungName)) { // 收到视频通话中的聊天消息
            //: TalkingVideoDanmuManager.shared().onRecvDanmuNewMsg(msg: data)
            SparkDanmuManager.mouthSplitDown().danmuStrokeMsg(msg: data)

            //: } else if cmdName == "onNotificationPopupWin" { // 审核拒绝提示弹窗
        } else if cmdName == (String(const_reportName) + String(show_memberData.prefix(5))) { // 审核拒绝提示弹窗
            //: TalkingPopupWindowManager.shared.attestationRefusePopUpWindow(dic: data)
            OutletWindowManager.shared.sash(dic: data)

            //: } else if cmdName == "onUserBlock" { // 用户被封号
        } else if cmdName == (String(mainDuringId)) { // 用户被封号
            //: TalkingLiveManager.shared().live_releaseAllResource()
            ManagerPushListener.nowPanel().translation()
            //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
            NotificationCenter.default.post(name: kOnValue, object: nil, userInfo: nil)
        }
    }
}

// MARK: - 处理业务逻辑

//: extension TalkingSocketManager {
extension SantaLuciaFirSocketDelegate {
    /// 检测是否需要停止或恢复直播
    /// - Parameter cmdName: socket命令
    //: private func check_stopOrRestoreLive(_ cmdName: String) {
    private func destroyLive(_ cmdName: String) {
        // 【关闭直播】收到来电或主动通话时，关闭直播
        //: if cmdName == "requestCall" || cmdName == "onRequestCall" {
        if cmdName == (showServiceFocusFormat.replacingOccurrences(of: "written", with: "ue") + String(userPhysicsData)) || cmdName == (String(appCapacityValue)) {
            //: if TalkingLiveManager.shared().isLive == true {
            if ManagerPushListener.nowPanel().isLive == true {
                //: NEED_RESTORE_LIVE = true
                app_playCurrencyMsg = true
                //: TalkingLiveManager.req_liveStop(type: 3)
                ManagerPushListener.tabCollection(type: 3)
                //: TalkingLiveManager.shared().live_releaseAllResource()
                ManagerPushListener.nowPanel().translation()
                //: return
                return
            }
        }

        // 【恢复直播】结束通话后，判断是否需要恢复直播
        //: guard NEED_RESTORE_LIVE == true else { return }
        guard app_playCurrencyMsg == true else { return }
        //: if cmdName == "cancelCall" || cmdName == "onCancelCall" ||
        if cmdName == (main_galleryFunMessage.replacingOccurrences(of: "opportunity", with: "a") + "elCa" + String(k_calculationTitle)) || cmdName == (String(app_midPath)) ||
            //: cmdName == "refuseCall" || cmdName == "onRefuseCall" ||
            cmdName == (noti_gestureData.replacingOccurrences(of: "lab", with: "u") + "Call") || cmdName == (String(userJudgeName.suffix(7)) + String(showPendingText)) ||
            //: cmdName == "stopCall" || cmdName == "onStopCall" {
            cmdName == (notiSuspendRecordPath.replacingOccurrences(of: "others", with: "op") + "Call") || cmdName == (String(main_systemAddFormat))
        {
            //: NEED_RESTORE_LIVE = false
            app_playCurrencyMsg = false
            //: let alert = TalkingLiveAlertView(frame: .zero, type: .turnOnLive)
            let alert = ProviderAlertView(frame: .zero, type: .turnOnLive)
            //: alert.rightBlcok = {
            alert.rightBlcok = {
                //: TalkingLiveManager.shared().checkLiveAuthAndTurnOn()
                ManagerPushListener.nowPanel().sideSlip()
            }
            //: alert.show()
            alert.stripPhone()
        }
    }

    /// 邀请通话通话结束之后，判断弹窗类型
    //: private func inviteCallPopViewEndShow(data: [String: Any]) {
    private func baptise(data: [String: Any]) {
        // 展示评分弹窗
        //: AppStoreRatingManager.shared.requestQuickRating()
        CoverRatingManager.shared.duringResConversation()
        //: guard DismissReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue else {
        guard DismissReactiveCompatible.share.loginUserMode.sex == FenceSequence.male.rawValue else {
            //: return
            return
        }
        // 弹出优惠充值弹窗
        //: if (data["isRecharge"]) as? Bool == false {
        if data[(String(app_brideStr) + k_keepUrl.replacingOccurrences(of: "take", with: "rg"))] as? Bool == false {
            //: PushThen.share.func_pushToDiscountRechargeView()
            PushThen.share.cherryRed()
        }
        // 半屏充值弹窗场景
        //: let source = data["source"] as? String
        let source = data[(show_translationId.replacingOccurrences(of: "apply", with: "rc"))] as? String
        //: guard source == "invitePop" || source == "videoPop" else {
        guard source == (String(const_severalPath)) || source == (data_interactionValue.replacingOccurrences(of: "mic", with: "d") + String(kSuitMessage)) else {
            //: return
            return
        }
        //: if (data["coin"] as? Int) ?? 0 < 60 {
        if (data[(user_primaryMessage.lowercased())] as? Int) ?? 0 < 60 {
            //: PushThen.share.func__pushToHalfWebVC(webViewType: .RechargeHalfPage)
            PushThen.share.obligatoEvent(webViewType: .RechargeHalfPage)
        }
    }
}
