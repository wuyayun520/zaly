
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let const_verticalData:[UInt8] = [0x3f,0x44,0x3f,0x4a,0xfe,0x39,0x45,0x3a,0x3b,0x48,0x10,0xff,0xf6,0x3e,0x37,0x49,0xf6,0x44,0x45,0x4a,0xf6,0x38,0x3b,0x3b,0x44,0xf6,0x3f,0x43,0x46,0x42,0x3b,0x43,0x3b,0x44,0x4a,0x3b,0x3a]

fileprivate func foundCertain(face num: UInt8) -> UInt8 {
    let value = Int(num) - 214
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "#322870" :*/
fileprivate let notiTriggerMessage:String = "#322870"

/*: "icon_home_position" :*/
fileprivate let userBlindStr:String = "visitor dynamics versionicon_h"
fileprivate let app_whyHangPath:String = "powhat"

/*: "btn_video_minimize" :*/
fileprivate let user_beforeBookStr:[Character] = ["b","t","n","_","v","i","d","e","o","_","m","i","n"]
fileprivate let k_buttonKey:String = "likemlikeze"

/*: "btn_video_drop_nor" :*/
fileprivate let const_mustGoldTitle:String = "successful circle bridgebtn_"
fileprivate let main_exForthSafeFormat:String = "trail deadline air forward can_drop"

/*: "btn_video_call_nor" :*/
fileprivate let appProfessionalKey:String = "btn_vpull trap"
fileprivate let const_floorId:String = "successfuldeo"

/*: "transform.scale" :*/
fileprivate let noti_greenFormat:[Character] = ["t","r","a","n","s","f","o","r","m",".","s","c","a","l"]
fileprivate let notiTooData:String = "attractive"

/*: "animation" :*/
fileprivate let constSitValue:[Character] = ["a","n","i","m","a","t"]
fileprivate let k_qualityStr:String = "iforth"

/*: "icon_videocall_female_answer_bg" :*/
fileprivate let main_scriptTitle:[UInt8] = [0xf0,0xfa,0xf6,0xf7,0xc6,0xef,0xf0,0xfd,0xfc,0xf6,0xfa,0xf8,0xf5,0xf5,0xc6,0xff,0xfc,0xf4,0xf8,0xf5,0xfc,0xc6,0xf8,0xf7,0xea,0xee,0xfc,0xeb,0xc6,0xfb,0xfe]

private func shirtStatus(thanks num: UInt8) -> UInt8 {
    return num ^ 153
}

/*: "icon_videocall_female_answer" :*/
fileprivate let data_trainActionMessage:[Character] = ["i","c","o","n","_","v","i","d","e","o","c","a","l","l","_","f","e","m","a","l","e","_","a","n","s","w","e","r"]

/*: "icon_calltalk_points" :*/
fileprivate let app_throughNagId:[Character] = ["i","c","o","n","_","c","a","l","l","t","a","l","k","_"]
fileprivate let kFavoriteSplitMsg:String = "poinorts"

/*: "get json error" :*/
fileprivate let dataCommentMedalName:String = "get mention mode think"
fileprivate let appItemName:String = "ebindbindobind"

/*: "btn_voice_Micr_nor" :*/
fileprivate let k_clubId:[Character] = ["b","t","n","_","v","o","i","c","e","_","M","i","c"]
fileprivate let noti_judgeUrl:[Character] = ["r","_","n","o","r"]

/*: "btn_voice_Micr_pre" :*/
fileprivate let showFeatureMsg:String = "btn_voquit lifetime deal equal"
fileprivate let dataOppositeDoorKey:String = "recover"
fileprivate let k_arrowKey:String = "ce_Micbeat tip live say"

/*: "btn_voice_hands_nor" :*/
fileprivate let show_withMsg:[Character] = ["b","t","n","_","v","o","i","c","e","_"]
fileprivate let notiRoundUrl:String = "hands_norand play quality"

/*: "btn_voice_hands_pre" :*/
fileprivate let user_tickPath:String = "connect qualitybtn_voic"
fileprivate let showEraseStr:[Character] = ["e","_","h","a","n","d","s","_","p","r","e"]

/*: "icon_coin_nor" :*/
fileprivate let constSignPath:[Character] = ["i","c","o","n","_","c","o","i","n","_","n","o","r"]

/*: "btn_voice_gift_nor" :*/
fileprivate let noti_requireUrl:String = "personal reject load social whichbtn_v"
fileprivate let constFormatConcentrationData:[Character] = ["o","i","c","e","_","g","i","f","t","_","n","o","r"]

/*: "Waiting to answer the phone..." :*/
fileprivate let app_remoteKey:String = "draft resortWaiti"
fileprivate let data_historyId:String = "when list blockng t"
fileprivate let mainSmoothData:String = "maker proposal eyeo answ"
fileprivate let noti_meetingValue:String = "e phpolicy allow"

/*: "yyyy-MM-dd" :*/
fileprivate let data_originConfirmFormat:[Character] = ["y","y","y","y","-","M","M","-","d","d"]

/*: "00:00" :*/
fileprivate let kMateMsg:[Character] = ["0","0",":","0","0"]

/*: "Waiting for %@ to answer the phone..." :*/
fileprivate let dataNumbereractionName:[UInt8] = [0xc1,0xcb,0xd3,0xde,0xd3,0xd8,0xd1,0x8a,0xd0,0xd9,0xdc,0x8a,0x8f,0xaa,0x8a,0xde,0xd9,0x8a,0xcb,0xd8,0xdd,0xe1,0xcf,0xdc,0x8a,0xde,0xd2,0xcf,0x8a,0xda,0xd2,0xd9,0xd8,0xcf,0x98,0x98,0x98]

fileprivate func strokeAnima(everyone num: UInt8) -> UInt8 {
    let value = Int(num) + 150
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "%@ points/min" :*/
fileprivate let constSureName:String = "tone father%@ poi"
fileprivate let k_shadowData:String = "MIN"

/*: "CIGaussianBlur" :*/
fileprivate let user_statValue:String = "CIGamuscle wealthy exception"
fileprivate let mainNobodyEaseContent:[Character] = ["u","r"]

/*: "toUid" :*/
fileprivate let mainDistinctivePath:String = "plan across involvementtoUid"

/*: "giftId" :*/
fileprivate let data_outlineValue:String = "giftIdable shot sun"

/*: "giftNum" :*/
fileprivate let kWrapData:String = "customer which extent which conditiongiftNum"

/*: "from" :*/
fileprivate let userAccompanyStr:[Character] = ["f","r","o","m"]

/*: "audioCall" :*/
fileprivate let const_tipStr:String = "audbond"
fileprivate let show_fairTitle:String = "normal left automatically leveloCall"

/*: "pkgItemsetId" :*/
fileprivate let notiThePath:[Character] = ["p","k","g","I","t","e","m","s"]
fileprivate let noti_mysteryName:String = "define activity modifyetId"

/*: "msgInfo" :*/
fileprivate let notiLoadMsg:[Character] = ["m","s","g","I","n","f","o"]

/*: "msgType" :*/
fileprivate let appAdviceName:[Character] = ["m","s","g","T","y","p","e"]

/*: "gift" :*/
fileprivate let mainApplyValue:[Character] = ["g","i","f","t"]

/*: "totalMfCoin" :*/
fileprivate let constValueFormat:[Character] = ["t","o","t","a","l","M","f","C"]
fileprivate let user_nearPromptFormat:String = "oslown"

/*: "cellData" :*/
fileprivate let notiLightId:[UInt8] = [0xcc,0xce,0xd5,0xd5,0xad,0xca,0xdd,0xca]

fileprivate func addedOccur(ta num: UInt8) -> UInt8 {
    let value = Int(num) + 151
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  DarkView.swift
//  AbroadTalking
//
//  Created by young on 2022/12/15.
//

//: import UIKit
import UIKit

//: protocol Talking1v1VoiceInteractionViewDelegate: NSObjectProtocol {
protocol SpillViewDelegate: NSObjectProtocol {
    /// 发送方结束通话
    //: func interactionView_shouldEndCall()
    func bowdlerizer()

    /// 接听音频通话
    //: func interactionView_shouldAcceptCall()
    func notice()

    /// 是否静音
    /// - Parameter isMute: 是否静音（默认否）
    //: func interactionView_shouldChangeMute(isMute: Bool)
    func readyDown(isMute: Bool)

    /// 是否免提
    /// - Parameter isHandsfree: 是否免提（默认否）
    //: func interactionView_shouldChangeSpeaker(isHandsfree: Bool)
    func querier(isHandsfree: Bool)

    /// 展示语音通话小窗口
    //: func interactionView_shouldDisplayMiniWindow()
    func characterWindow()

    /// 更新小窗口通话时间
    /// - Parameter time: 通话时间
    //: func interactionView_shouldUpdateTime(time: Double)
    func visualAspectTime(time: Double)
}

//: class Talking1v1VoiceInteractionView: UIView {
class DarkView: UIView {
    //: weak var delegate: Talking1v1VoiceInteractionViewDelegate?
    weak var delegate: SpillViewDelegate?
    //: private var chatModel = TalkingVideoChatModel()
    private var chatModel = IceChatModel() // 引用类型，整个音频通话共用同一个model
    //: private var waitTimer: Timer?
    private var waitTimer: Timer? // 等待接听动画定时器
    //: private var startTime: TimeInterval?
    private var startTime: TimeInterval? // 开始的时间戳记 since1970
    //: private var countTimer: Timer?
    private var countTimer: Timer? // 通话时间定时器

    //: init(frame: CGRect, chatModel: TalkingVideoChatModel?) {
    init(frame: CGRect, chatModel: IceChatModel?) {
        //: super.init(frame: frame)
        super.init(frame: frame)
        //: setupSubviews()
        rewardAttention()
        //: setupSubViewsConstraint()
        yesterdaySpend()
        //: if chatModel != nil {
        if chatModel != nil {
            //: self.chatModel = chatModel!
            self.chatModel = chatModel!
            //: self.startTime = chatModel!.startTime
            self.startTime = chatModel!.startTime
        }
        //: showViewsViaStage()
        outsideCityAm()
        //: initCachEffectData()
        motivation()
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: const_verticalData.map{foundCertain(face: $0)}, encoding: .utf8)!)
    }

    //: deinit {
    deinit {
        //: if waitTimer != nil {
        if waitTimer != nil {
            //: waitTimer?.invalidate()
            waitTimer?.invalidate()
            //: waitTimer = nil
            waitTimer = nil
        }
        //: if countTimer != nil {
        if countTimer != nil {
            //: countTimer?.invalidate()
            countTimer?.invalidate()
            //: countTimer = nil
            countTimer = nil
        }
//        self.player.stopPlay()
    }

    // MARK: - Lazy Load

    // 渐变背景视图
    //: lazy var bgView: UIImageView = {
    lazy var bgView: UIImageView = {
        //: let v = UIImageView()
        let v = UIImageView()
        //: v.contentMode = .scaleAspectFill
        v.contentMode = .scaleAspectFill
        //: v.layer.masksToBounds = true
        v.layer.masksToBounds = true
        // 渐变色
        //: let layer = CAGradientLayer()
        let layer = CAGradientLayer()
        // 宽高+1是为了适配在某些设备上出现渐变填充不满的情况
        //: layer.frame = CGRect(x: 0, y: 0, width: ScreenWidth+1, height: ScreenHeight+1)
        layer.frame = CGRect(x: 0, y: 0, width: main_logStr + 1, height: main_nameData + 1)
        //: layer.startPoint = CGPoint(x: 0.5, y: 0)
        layer.startPoint = CGPoint(x: 0.5, y: 0)
        //: layer.endPoint = CGPoint(x: 0.5, y: 1)
        layer.endPoint = CGPoint(x: 0.5, y: 1)
        //: layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: "#322870")!.cgColor]
        layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: (notiTriggerMessage.capitalized))!.cgColor]
        //: layer.locations = [0, 1]
        layer.locations = [0, 1]
        //: v.layer.addSublayer(layer)
        v.layer.addSublayer(layer)

        //: return v
        return v
        //: }()
    }()

    // 点击展示/隐藏交互按钮
    //: private lazy var touchHidenBtn: UIButton = {
    private lazy var touchHidenBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(changeInteractionShowState), for: .touchUpInside)
        btn.addTarget(self, action: #selector(produceMutual), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 接收方头像
    //: private lazy var pairPicV: UIImageView = {
    private lazy var pairPicV: UIImageView = {
        //: let v = UIImageView()
        let v = UIImageView()
        //: v.contentMode = .scaleAspectFill
        v.contentMode = .scaleAspectFill
        //: v.isUserInteractionEnabled = true
        v.isUserInteractionEnabled = true
        //: v.layer.cornerRadius = actualWidth(w: 100)/2
        v.layer.cornerRadius = actualWidth(w: 100) / 2
        //: v.layer.masksToBounds = true
        v.layer.masksToBounds = true
        //: return v
        return v
        //: }()
    }()

    // 接收方昵称
    //: private lazy var pairNameLab: UILabel = {
    private lazy var pairNameLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangMediumFont(fontSize: 20)
        lab.font = UIFont.pingfang(fontSize: 20)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var locationBtn: TalkingButton = {
    private lazy var locationBtn: RangeReactiveCompatible = {
        //: let btn = TalkingButton()
        let btn = RangeReactiveCompatible()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_home_position"), for: .normal)
        btn.setImage(UIImage.locationName(name: (String(userBlindStr.suffix(6)) + "ome_" + app_whyHangPath.replacingOccurrences(of: "what", with: "si") + "tion")), for: .normal)
        //: btn.spaceBetweenTitleAndImage = 2
        btn.spaceBetweenTitleAndImage = 2
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangRugularFont(fontSize: 12)
        btn.titleLabel?.font = UIFont.bonePlace(fontSize: 12)
        //: btn.titleLabel?.lineBreakMode = .byTruncatingTail
        btn.titleLabel?.lineBreakMode = .byTruncatingTail
        //: return btn
        return btn
        //: }()
    }()

    // 呼叫中描述
    //: private lazy var desLab: UILabel = {
    private lazy var desLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = UIColor(white: 1, alpha: 1)
        lab.textColor = UIColor(white: 1, alpha: 1)
        //: lab.font = UIFont.pingfangRugularFont(fontSize: 17)
        lab.font = UIFont.bonePlace(fontSize: 17)
        //: lab.numberOfLines = 0
        lab.numberOfLines = 0
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: return lab
        return lab
        //: }()
    }()

    // 最小化按钮
    //: private lazy var minimizeBtn: UIButton = {
    private lazy var minimizeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_video_minimize"), for: .normal)
        btn.setImage(UIImage.locationName(name: (String(user_beforeBookStr) + k_buttonKey.replacingOccurrences(of: "like", with: "i"))), for: .normal)
        //: btn.addTarget(self, action: #selector(minimizeButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(innerSuite), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 挂断/取消 按钮
    //: private lazy var hangupBtn: UIButton = {
    private lazy var hangupBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_video_drop_nor"), for: .normal)
        btn.setImage(UIImage.locationName(name: (String(const_mustGoldTitle.suffix(4)) + "video" + String(main_exForthSafeFormat.suffix(5)) + "_nor")), for: .normal)
        //: btn.addTarget(self, action: #selector(hangupButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(doActualClick), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 接听按钮
    //: private lazy var acceptBtn: UIButton = {
    private lazy var acceptBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_video_call_nor"), for: .normal)
        btn.setImage(UIImage.locationName(name: (String(appProfessionalKey.prefix(5)) + const_floorId.replacingOccurrences(of: "successful", with: "i") + "_call_nor")), for: .normal)
        //: btn.addTarget(self, action: #selector(acceptButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(beyondClick), for: .touchUpInside)

        // 添加关键帧动画
        //: let keyAnimation = CAKeyframeAnimation()
        let keyAnimation = CAKeyframeAnimation()
        //: keyAnimation.keyPath = "transform.scale"
        keyAnimation.keyPath = (String(noti_greenFormat) + notiTooData.replacingOccurrences(of: "attractive", with: "e"))
        //: keyAnimation.values = [NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1)),
        keyAnimation.values = [NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1)),
                               //: NSValue(cgAffineTransform: CGAffineTransform(scaleX: actualWidth(w: 80/70), y: actualWidth(w: 80/70))),
                               NSValue(cgAffineTransform: CGAffineTransform(scaleX: actualWidth(w: 80 / 70), y: actualWidth(w: 80 / 70))),
                               //: NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1))]
                               NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1))]
        //: keyAnimation.duration = 1.2
        keyAnimation.duration = 1.2
        //: keyAnimation.repeatCount = MAXFLOAT
        keyAnimation.repeatCount = MAXFLOAT
        //: keyAnimation.isRemovedOnCompletion = false
        keyAnimation.isRemovedOnCompletion = false
        //: keyAnimation.fillMode = .both
        keyAnimation.fillMode = .both
        //: keyAnimation.calculationMode = .cubicPaced
        keyAnimation.calculationMode = .cubicPaced
        //: btn.layer.add(keyAnimation, forKey: "animation")
        btn.layer.add(keyAnimation, forKey: (String(constSitValue) + k_qualityStr.replacingOccurrences(of: "forth", with: "on")))

        //: return btn
        return btn
        //: }()
    }()

    // 女性接听按钮
    //: private lazy var femaleAcceptBGBtn: UIButton = {
    private lazy var femaleAcceptBGBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setBackgroundImage(UIImage.BundleImageNamed(name: "icon_videocall_female_answer_bg"), for: .normal)
        btn.setBackgroundImage(UIImage.locationName(name: String(bytes: main_scriptTitle.map{shirtStatus(thanks: $0)}, encoding: .utf8)!), for: .normal)
        // 添加关键帧动画
        //: let keyAnimation = CAKeyframeAnimation()
        let keyAnimation = CAKeyframeAnimation()
        //: keyAnimation.keyPath = "transform.scale"
        keyAnimation.keyPath = (String(noti_greenFormat) + notiTooData.replacingOccurrences(of: "attractive", with: "e"))
        //: keyAnimation.values = [NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1)),
        keyAnimation.values = [NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1)),
                               //: NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1.02, y: 1.02)),
                               NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1.02, y: 1.02)),
                               //: NSValue(cgAffineTransform: CGAffineTransform(scaleX: 0.95, y: 0.95))]
                               NSValue(cgAffineTransform: CGAffineTransform(scaleX: 0.95, y: 0.95))]
        //: keyAnimation.duration = 0.6
        keyAnimation.duration = 0.6
        //: keyAnimation.repeatCount = MAXFLOAT
        keyAnimation.repeatCount = MAXFLOAT
        //: keyAnimation.isRemovedOnCompletion = false
        keyAnimation.isRemovedOnCompletion = false
        //: keyAnimation.fillMode = .both
        keyAnimation.fillMode = .both
        //: keyAnimation.calculationMode = .cubicPaced
        keyAnimation.calculationMode = .cubicPaced
        //: btn.layer.add(keyAnimation, forKey: "animation")
        btn.layer.add(keyAnimation, forKey: (String(constSitValue) + k_qualityStr.replacingOccurrences(of: "forth", with: "on")))
        //: return btn
        return btn
        //: }()
    }()

    // 女性接听按钮
    //: private lazy var femaleAcceptBtn: UIButton = {
    private lazy var femaleAcceptBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_videocall_female_answer"), for: .normal)
        btn.setImage(UIImage.locationName(name: (String(data_trainActionMessage))), for: .normal)
        //: btn.addTarget(self, action: #selector(acceptButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(beyondClick), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 女性接听积分提示
    //: private lazy var femaleAcceptTipsBtn: UIButton = {
    private lazy var femaleAcceptTipsBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_calltalk_points"), for: .normal)
        btn.setImage(UIImage.locationName(name: (String(app_throughNagId) + kFavoriteSplitMsg.replacingOccurrences(of: "nor", with: "n"))), for: .normal)
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangRugularFont(fontSize: 15)
        btn.titleLabel?.font = UIFont.bonePlace(fontSize: 15)
        //: btn.isUserInteractionEnabled = false
        btn.isUserInteractionEnabled = false
        //: return btn
        return btn
        //: }()
    }()

    //: lazy var acceptPlayer: SVGAPlayer = {
    lazy var acceptPlayer: SVGAPlayer = {
        //: let player = SVGAPlayer.init()
        let player = SVGAPlayer()
        //: player.loops = 0
        player.loops = 0
        //: player.clearsAfterStop = false
        player.clearsAfterStop = false
        //: player.isUserInteractionEnabled = false
        player.isUserInteractionEnabled = false
        //: player.contentMode = .scaleAspectFill
        player.contentMode = .scaleAspectFill
        //: let url = SVGAEffectTool.default.getZipEffectPath(type: .CallTalk_Accept)
        let url = BlackEffectTool.default.recordingRevenue(type: .CallTalk_Accept)
        //: do {
        do {
            //: let data = try Data(contentsOf: url)
            let data = try Data(contentsOf: url)
            //: let parser = SVGAParser.init()
            let parser = SVGAParser()
            //: parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
            parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: player.videoItem = videoItem
                player.videoItem = videoItem
                //: player.startAnimation()
                player.startAnimation()
            }
            //: } catch {
        } catch {
            //: printLog(message: "get json error")
            printLog(message: (String(dataCommentMedalName.prefix(4)) + "json " + appItemName.replacingOccurrences(of: "bind", with: "r")))
        }
        //: if LanguageManager.shared.direction == .rightToLeft {
        if RatingReactiveCompatible.shared.direction == .rightToLeft {
            //: player.transform = CGAffineTransform(scaleX: -1, y: 1)
            player.transform = CGAffineTransform(scaleX: -1, y: 1)
        }
        //: return player
        return player
        //: }()
    }()

    // 开启/关闭麦克风按钮
    //: private lazy var micBtn: UIButton = {
    private lazy var micBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_voice_Micr_nor"), for: .normal)
        btn.setImage(UIImage.locationName(name: (String(k_clubId) + String(noti_judgeUrl))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_voice_Micr_pre"), for: .selected)
        btn.setImage(UIImage.locationName(name: (String(showFeatureMsg.prefix(6)) + dataOppositeDoorKey.replacingOccurrences(of: "recover", with: "i") + String(k_arrowKey.prefix(6)) + "r_pre")), for: .selected)
        //: btn.addTarget(self, action: #selector(microphoneButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(shadowOperate), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 开启/关闭免提按钮
    //: lazy var handsfreeBtn: UIButton = {
    lazy var handsfreeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_voice_hands_nor"), for: .normal)
        btn.setImage(UIImage.locationName(name: (String(show_withMsg) + String(notiRoundUrl.prefix(9)))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_voice_hands_pre"), for: .selected)
        btn.setImage(UIImage.locationName(name: (String(user_tickPath.suffix(8)) + String(showEraseStr))), for: .selected)
        //: btn.addTarget(self, action: #selector(handsfreeButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(time), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 充值按钮
    //: private lazy var rechargeBtn: UIButton = {
    private lazy var rechargeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_coin_nor"), for: .normal)
        btn.setImage(UIImage.locationName(name: (String(constSignPath))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_coin_nor"), for: .selected)
        btn.setImage(UIImage.locationName(name: (String(constSignPath))), for: .selected)
        //: btn.addTarget(self, action: #selector(rechargeBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(reduce), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 送礼按钮
    //: private lazy var sendGiftBtn: UIButton = {
    private lazy var sendGiftBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_voice_gift_nor"), for: .normal)
        btn.setImage(UIImage.locationName(name: (String(noti_requireUrl.suffix(5)) + String(constFormatConcentrationData))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_voice_gift_nor"), for: .selected)
        btn.setImage(UIImage.locationName(name: (String(noti_requireUrl.suffix(5)) + String(constFormatConcentrationData))), for: .selected)
        //: btn.addTarget(self, action: #selector(sendGiftButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(sendClick), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var timeLab: UILabel = {
    private lazy var timeLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangMediumFont(fontSize: 18)
        lab.font = UIFont.pingfang(fontSize: 18)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: return lab
        return lab
        //: }()
    }()

    //: lazy var giftView: TalkingChatGiftView = {
    lazy var giftView: YaDataSource = {
        //: let giftV = TalkingChatGiftView.init(style: .call)
        let giftV = YaDataSource(style: .call)
        //: giftV.frame = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight)
        giftV.frame = CGRect(x: 0, y: 0, width: main_logStr, height: main_nameData)
        //: return giftV
        return giftV
        //: }()
    }()

    //: lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    lazy var giftEffectView: TickGenerateView = {
        //: let effectView = TalkingPrivateChatAnimatView()
        let effectView = TickGenerateView()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var giftTrackView: TalkingGiftTrackView = {
    lazy var giftTrackView: RatingTrackView = {
        //: let trackView = TalkingGiftTrackView()
        let trackView = RatingTrackView()
        //: trackView.isUserInteractionEnabled = false
        trackView.isUserInteractionEnabled = false
        //: return trackView
        return trackView
        //: }()
    }()

    //: lazy var pointAnimView: TalkingPointAnimationView = {
    lazy var pointAnimView: MovementWritingAnimationView = {
        //: let pointView = TalkingPointAnimationView()
        let pointView = MovementWritingAnimationView()
        //: pointView.isUserInteractionEnabled = false
        pointView.isUserInteractionEnabled = false
        //: return pointView
        return pointView
        //: }()
    }()
}

// MARK: - Event

//: extension Talking1v1VoiceInteractionView {
extension DarkView {
    /// 展示视图状态
    //: func showViewsViaStage() {
    func outsideCityAm() {
        //: switch chatModel.videoStage {
        switch chatModel.videoStage {
        //: case .Waiting:
        case .Waiting:
            //: desLab.text = "Waiting to answer the phone...".localized
            desLab.text = (String(app_remoteKey.suffix(5)) + String(data_historyId.suffix(4)) + String(mainSmoothData.suffix(6)) + "er th" + String(noti_meetingValue.prefix(4)) + "one...").localized
            //: desLab.isHidden = false
            desLab.isHidden = false
            //: minimizeBtn.isHidden = true
            minimizeBtn.isHidden = true
            //: hangupBtn.isHidden = false
            hangupBtn.isHidden = false
            //: acceptBtn.isHidden = true
            acceptBtn.isHidden = true
            //: femaleAcceptBtn.isHidden = true
            femaleAcceptBtn.isHidden = true
            //: femaleAcceptTipsBtn.isHidden = true
            femaleAcceptTipsBtn.isHidden = true
            //: acceptPlayer.isHidden = true
            acceptPlayer.isHidden = true
            //: femaleAcceptBGBtn.isHidden = true
            femaleAcceptBGBtn.isHidden = true
            //: micBtn.isHidden = true
            micBtn.isHidden = true
            //: handsfreeBtn.isHidden = true
            handsfreeBtn.isHidden = true
            //: sendGiftBtn.isHidden = true
            sendGiftBtn.isHidden = true
            //: rechargeBtn.isHidden = true
            rechargeBtn.isHidden = true
            //: timeLab.isHidden = true
            timeLab.isHidden = true
            //: touchHidenBtn.isEnabled = false
            touchHidenBtn.isEnabled = false

        //: case .Recieved:
        case .Recieved:
            //: desLab.text = nil
            desLab.text = nil
            //: desLab.isHidden = true
            desLab.isHidden = true
            //: minimizeBtn.isHidden = true
            minimizeBtn.isHidden = true
            //: hangupBtn.isHidden = false
            hangupBtn.isHidden = false
            //: if DismissReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue {
            if DismissReactiveCompatible.share.loginUserMode.sex == FenceSequence.male.rawValue {
                //: acceptBtn.isHidden = false
                acceptBtn.isHidden = false
                //: femaleAcceptBtn.isHidden = true
                femaleAcceptBtn.isHidden = true
                //: femaleAcceptTipsBtn.isHidden = true
                femaleAcceptTipsBtn.isHidden = true
                //: acceptPlayer.isHidden = true
                acceptPlayer.isHidden = true
                //: femaleAcceptBGBtn.isHidden = true
                femaleAcceptBGBtn.isHidden = true
                //: } else {
            } else {
                //: acceptBtn.isHidden = true
                acceptBtn.isHidden = true
                //: femaleAcceptBtn.isHidden = false
                femaleAcceptBtn.isHidden = false
                //: femaleAcceptTipsBtn.isHidden = false
                femaleAcceptTipsBtn.isHidden = false
                //: femaleAcceptBGBtn.isHidden = false
                femaleAcceptBGBtn.isHidden = false
                //: let date = Defaults.object(forKey: TalkingCallShowTag)
                let date = data_partyText.object(forKey: constBackText)
                //: let today = NSDate.getTimeString(date: Date(), dateFormat: "yyyy-MM-dd")
                let today = NSDate.switchly(date: Date(), dateFormat: (String(data_originConfirmFormat)))
                //: if date == nil || (date as! String) != today {
                if date == nil || (date as! String) != today {
                    //: Defaults.set(today, forKey: TalkingCallShowTag)
                    data_partyText.set(today, forKey: constBackText)
                    //: acceptPlayer.isHidden = false
                    acceptPlayer.isHidden = false
                    //: } else {
                } else {
                    //: acceptPlayer.isHidden = true
                    acceptPlayer.isHidden = true
                }
            }
            //: micBtn.isHidden = true
            micBtn.isHidden = true
            //: handsfreeBtn.isHidden = true
            handsfreeBtn.isHidden = true
            //: sendGiftBtn.isHidden = true
            sendGiftBtn.isHidden = true
            //: rechargeBtn.isHidden = true
            rechargeBtn.isHidden = true
            //: timeLab.isHidden = true
            timeLab.isHidden = true
            //: touchHidenBtn.isEnabled = false
            touchHidenBtn.isEnabled = false

        //: case .Calling:
        case .Calling:
            //: desLab.text = nil
            desLab.text = nil
            //: desLab.isHidden = true
            desLab.isHidden = true
            //: minimizeBtn.isHidden = false
            minimizeBtn.isHidden = false
            //: hangupBtn.isHidden = false
            hangupBtn.isHidden = false
            //: acceptBtn.isHidden = true
            acceptBtn.isHidden = true
            //: femaleAcceptBtn.isHidden = true
            femaleAcceptBtn.isHidden = true
            //: femaleAcceptTipsBtn.isHidden = true
            femaleAcceptTipsBtn.isHidden = true
            //: acceptPlayer.isHidden = true
            acceptPlayer.isHidden = true
            //: femaleAcceptBGBtn.isHidden = true
            femaleAcceptBGBtn.isHidden = true
            //: micBtn.isHidden = false
            micBtn.isHidden = false
            //: handsfreeBtn.isHidden = false
            handsfreeBtn.isHidden = false
            //: sendGiftBtn.isHidden = false
            sendGiftBtn.isHidden = false
            //: rechargeBtn.isHidden = false
            rechargeBtn.isHidden = false
            //: timeLab.isHidden = false
            timeLab.isHidden = false
            //: touchHidenBtn.isEnabled = true
            touchHidenBtn.isEnabled = true
            //: locationBtn.isHidden = true
            locationBtn.isHidden = true
            //: if startTime != nil {
            if startTime != nil {
                //: changeTalkTime()
                animalFee()
                //: } else {
            } else {
                //: timeLab.text = "00:00"
                timeLab.text = (String(kMateMsg))
            }
            //: startTalkCount()
            dateAgent()
            //: touchHiddenTimer()
            crush()
        }

        //: updateLayout()
        selectedLayout()
        //: afterGetInfo()
        comeUpMakeup()
    }

    /// 刷新通话视图数据
    //: func afterGetInfo() {
    func comeUpMakeup() {
        //: if chatModel.videoStage == .Waiting {
        if chatModel.videoStage == .Waiting {
            //: desLab.text = "Waiting for %@ to answer the phone...".localizedArguments(chatModel.pairNickName)
            desLab.text = String(bytes: dataNumbereractionName.map{strokeAnima(everyone: $0)}, encoding: .utf8)!.unitedStates(chatModel.pairNickName)
        }
        //: pairNameLab.text = chatModel.pairNickName
        pairNameLab.text = chatModel.pairNickName
        //: locationBtn.isHidden = chatModel.location.count <= 0
        locationBtn.isHidden = chatModel.location.count <= 0
        //: locationBtn.setTitle("\(chatModel.location)", for: .normal)
        locationBtn.setTitle("\(chatModel.location)", for: .normal)
        //: let optionsInfo = [KingfisherOptionsInfoItem.processor(RoundCornerImageProcessor(cornerRadius: 20))]
        let optionsInfo = [KingfisherOptionsInfoItem.processor(RoundCornerImageProcessor(cornerRadius: 20))]
        //: pairPicV.setUrlImage(urlStr: chatModel.pairHeadPic, options: optionsInfo)
        pairPicV.loadPoiseFinishParkImageEye(urlStr: chatModel.pairHeadPic, options: optionsInfo)
        //: femaleAcceptTipsBtn.setTitle(" +" + "%@ points/min".localizedArguments(chatModel.points), for: .normal)
        femaleAcceptTipsBtn.setTitle(" +" + (String(constSureName.suffix(6)) + "nts/" + k_shadowData.lowercased()).unitedStates(chatModel.points), for: .normal)
        // 设置背景头像
        //: if !chatModel.pairHeadPic.isEmptyString {
        if !chatModel.pairHeadPic.isEmptyString {
            //: bgView.setUrlImage(urlStr: chatModel.pairHeadPic, options: nil) { [weak self] _, image in
            bgView.loadPoiseFinishParkImageEye(urlStr: chatModel.pairHeadPic, options: nil) { [weak self] _, image in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if image != nil {
                if image != nil {
                    //: self.bgView.image = self.blurImage(image!)
                    self.bgView.image = self.existInstance(image!)
                }
            }
        }
    }

    /// 最小化按钮事件
    //: @objc private func minimizeButtonClick() {
    @objc private func innerSuite() {
        //: delegate?.interactionView_shouldDisplayMiniWindow()
        delegate?.characterWindow()
    }

    /// 挂断按钮事件
    //: @objc private func hangupButtonClick() {
    @objc private func doActualClick() {
        //: delegate?.interactionView_shouldEndCall()
        delegate?.bowdlerizer()
    }

    /// 接听按钮事件
    //: @objc private func acceptButtonClick() {
    @objc private func beyondClick() {
        //: delegate?.interactionView_shouldAcceptCall()
        delegate?.notice()
    }

    /// 麦克风按钮事件
    //: @objc private func microphoneButtonClick() {
    @objc private func shadowOperate() {
        //: micBtn.isSelected = !micBtn.isSelected
        micBtn.isSelected = !micBtn.isSelected
        //: delegate?.interactionView_shouldChangeMute(isMute: micBtn.isSelected)
        delegate?.readyDown(isMute: micBtn.isSelected)
    }

    /// 免提按钮事件
    //: @objc private func handsfreeButtonClick() {
    @objc private func time() {
        //: delegate?.interactionView_shouldChangeSpeaker(isHandsfree: !handsfreeBtn.isSelected)
        delegate?.querier(isHandsfree: !handsfreeBtn.isSelected)
    }

    /// 送礼按钮事件
    //: @objc private func sendGiftButtonClick() {
    @objc private func sendClick() {
        //: func__sendGift()
        weft()
    }

    /// 充值按钮事件
    //: @objc private func rechargeBtnClick() {
    @objc private func reduce() {
        //: PushThen.share.func__pushToHalfWebVC(webViewType: .RechargeHalfPage)
        PushThen.share.obligatoEvent(webViewType: .RechargeHalfPage)
    }

    /// 点击展示/隐藏通话中的按钮
    //: @objc private func changeInteractionShowState() {
    @objc private func produceMutual() {
        //: let isHidden = !hangupBtn.isHidden
        let isHidden = !hangupBtn.isHidden
        //: minimizeBtn.isHidden = isHidden
        minimizeBtn.isHidden = isHidden
        //: hangupBtn.isHidden = isHidden
        hangupBtn.isHidden = isHidden
        //: micBtn.isHidden = isHidden
        micBtn.isHidden = isHidden
        //: handsfreeBtn.isHidden = isHidden
        handsfreeBtn.isHidden = isHidden
        //: sendGiftBtn.isHidden = isHidden
        sendGiftBtn.isHidden = isHidden
        //: rechargeBtn.isHidden = isHidden
        rechargeBtn.isHidden = isHidden
        //: if isHidden {
        if isHidden {
            //: invalidateIdleTimer()
            inmateBold()
            //: } else {
        } else {
            //: touchHiddenTimer()
            crush()
        }
    }

    //: func blurImage(_ image: UIImage) -> UIImage {
    func existInstance(_ image: UIImage) -> UIImage {
        //: let context = CIContext(options: nil)
        let context = CIContext(options: nil)
        //: let inputImage = CIImage(image: image)
        let inputImage = CIImage(image: image)
        // 使用高斯模糊滤镜
        //: let filter = CIFilter(name: "CIGaussianBlur")!
        let filter = CIFilter(name: (String(user_statValue.prefix(4)) + "ussianBl" + String(mainNobodyEaseContent)))!
        //: filter.setValue(inputImage, forKey: kCIInputImageKey)
        filter.setValue(inputImage, forKey: kCIInputImageKey)
        // 设置模糊半径值（越大越模糊）
        //: filter.setValue(5, forKey: kCIInputRadiusKey)
        filter.setValue(5, forKey: kCIInputRadiusKey)
        //: let outputCIImage = filter.outputImage!
        let outputCIImage = filter.outputImage!
        //: let rect = CGRect(origin: CGPoint.zero, size: image.size)
        let rect = CGRect(origin: CGPoint.zero, size: image.size)
        //: let cgImage = context.createCGImage(outputCIImage, from: rect)
        let cgImage = context.createCGImage(outputCIImage, from: rect)
        // 显示生成的模糊图片
        //: let newImage = UIImage(cgImage: cgImage!)
        let newImage = UIImage(cgImage: cgImage!)
        //: return newImage
        return newImage
    }

    //: func showPointAnimaData(point: String) {
    func streamRequire(point: String) {
        //: addSubview(pointAnimView)
        addSubview(pointAnimView)
        //: pointAnimView.snp.remakeConstraints { make in
        pointAnimView.snp.remakeConstraints { make in
            //: make.center.equalToSuperview()
            make.center.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 100), height: actualWidth(w: 100)))
            make.size.equalTo(CGSize(width: actualWidth(w: 100), height: actualWidth(w: 100)))
        }
        //: pointAnimView.setPointData(point: point)
        pointAnimView.flowPrimary(point: point)
    }
}

// MARK: - 送礼

//: extension Talking1v1VoiceInteractionView {
extension DarkView {
    //: func func__sendGift() {
    func weft() {
        //: TalkingChatGiftManager.share.func__sendGiftEvent {
        MotivationThen.share.tillTurn {
            //: self.func__showGiftChoiceView()
            self.dragRecommend()
        }
    }

    //: func func__showGiftChoiceView() {
    func dragRecommend() {
        //: giftView.updateGiftInfo(needReload: true, mf_coin: DismissReactiveCompatible.share.loginUserMode.mf_coin)
        giftView.path(needReload: true, mf_coin: DismissReactiveCompatible.share.loginUserMode.mf_coin)
        //: window?.addSubview(giftView)
        window?.addSubview(giftView)
        //: giftView.showView()
        giftView.score()
        //: giftView.sendActionBlock = { [weak self] (_ giftModel: TalkingRoomGiftModel, _ num: String) in
        giftView.sendActionBlock = { [weak self] (_ giftModel: TillGiftModel, _ num: String) in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.func__sendGiftMsg(giftModel: giftModel, giftNum: num)
            self.runoff(giftModel: giftModel, giftNum: num)
        }
    }

    //: func func_giftViewDismiss() {
    func routeStatus() {
        //: giftView.dismissView()
        giftView.maneuver()
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func runoff(giftModel: TillGiftModel, giftNum: String) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            enablece(showMsg: main_textStr)
            //: return
            return
        }
        //: var params: [String: Any] = [:]
        var params: [String: Any] = [:]
        //: if chatModel.iUid == DismissReactiveCompatible.share.loginUserMode.userID {
        if chatModel.iUid == DismissReactiveCompatible.share.loginUserMode.userID {
            //: params["toUid"] = chatModel.pairUid
            params[(String(mainDistinctivePath.suffix(5)))] = chatModel.pairUid
            //: } else {
        } else {
            //: params["toUid"] = chatModel.iUid
            params[(String(mainDistinctivePath.suffix(5)))] = chatModel.iUid
        }
        //: params["giftId"] = giftModel.gid
        params[(String(data_outlineValue.prefix(6)))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(kWrapData.suffix(7)))] = giftNum
        //: params["from"] = "audioCall"
        params[(String(userAccompanyStr))] = (const_tipStr.replacingOccurrences(of: "bond", with: "i") + String(show_fairTitle.suffix(5)))
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(notiThePath) + String(noti_mysteryName.suffix(4)))] = giftModel.pkgItemsetId
        }

        //: TalkingChatRequestTool.req_SendGiftPrivete(param: params, completion: { succeed, result, errorModel in
        TamperRequestTool.underSendReply(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.reattribute(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.giftView.localRoad(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! [String: Any?])
            self.placeMessage(extral: result as! [String: Any?])
            //: })
        })
    }

    //: func func__insertGiftMessageWithExtral(extral: [String: Any?]) {
    func placeMessage(extral: [String: Any?]) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: [String: Any] = extralInfo["msgInfo"] as! [String: Any]
        var msgInfoDic: [String: Any] = extralInfo[(String(notiLoadMsg))] as! [String: Any]
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[(String(appAdviceName))] = (String(mainApplyValue))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(notiLoadMsg))] = msgInfoDic

        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any])
        juvenilePersonDic(giftDic: extral as [String: Any])
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((String(constValueFormat) + user_nearPromptFormat.replacingOccurrences(of: "slow", with: "i"))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(String(constValueFormat) + user_nearPromptFormat.replacingOccurrences(of: "slow", with: "i"))] as! NSNumber
            //: DismissReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            DismissReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: giftView.updateGiftInfo(needReload: false, mf_coin: DismissReactiveCompatible.share.loginUserMode.mf_coin)
        giftView.path(needReload: false, mf_coin: DismissReactiveCompatible.share.loginUserMode.mf_coin)

        //: var toUid = ""
        var toUid = ""
        //: if chatModel.iUid == DismissReactiveCompatible.share.loginUserMode.userID {
        if chatModel.iUid == DismissReactiveCompatible.share.loginUserMode.userID {
            //: toUid = chatModel.pairUid
            toUid = chatModel.pairUid
            //: } else {
        } else {
            //: toUid = chatModel.iUid
            toUid = chatModel.iUid
        }

        //: let cellData = TUISocialChatManager.insertTXMessage(withExtral: extralInfo as [String: Any], toUid: toUid, isSender: true, isTip: false)
        let cellData = TictacReactiveCompatible.terminalBeginEngraftDigitizerCipherCircleWith(withExtral: extralInfo as [String: Any], toUid: toUid, isSender: true, isTip: false)
        //: NotificationCenter.default.post(name: CHAT_ADD_CALL_RECORD_MSG_NOTIFICATION, object: self, userInfo: ["cellData": cellData])
        NotificationCenter.default.post(name: k_serverKey, object: self, userInfo: [String(bytes: notiLightId.map{addedOccur(ta: $0)}, encoding: .utf8)!: cellData])
    }

    //: func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool) {
    func reattribute(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool) {
        //: if errorCode == 50203 {
        if errorCode == 50203 {
            //: guard DismissReactiveCompatible.share.loginUserMode.status != 1 else {
            guard DismissReactiveCompatible.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    enablece(showMsg: errorStr)
                }
                //: return
                return
            }
            //: PushThen.share.func__jumpToWebRecharge(clickEvent: clickSendingiftsButton, sufficient: false)
            PushThen.share.twoParams(clickEvent: showLimitTitle, sufficient: false)
            //: giftView.dismissView()
            giftView.maneuver()
            //: } else if errorCode == 10888 {
        } else if errorCode == 10888 {
            //: let view = TalkingLiveVipSubscribePopView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = DisenableThen(frame: CGRect(x: 0, y: 0, width: main_logStr, height: main_nameData))
            //: view.show()
            view.animaShow()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                enablece(showMsg: errorStr)
            }
        }
    }

    //: func initCachEffectData() {
    func motivation() {
        //: var targetId = ""
        var targetId = ""
        //: if chatModel.iUid == DismissReactiveCompatible.share.loginUserMode.userID {
        if chatModel.iUid == DismissReactiveCompatible.share.loginUserMode.userID {
            //: targetId = chatModel.pairUid
            targetId = chatModel.pairUid
            //: } else {
        } else {
            //: targetId = chatModel.iUid
            targetId = chatModel.iUid
        }
        //: giftEffectView.initCachAnimatData(tagetID: targetId)
        giftEffectView.thenMakeupId(tagetID: targetId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: [String: Any]) {
    func juvenilePersonDic(giftDic: [String: Any]) {
        //: let giftModel = giftEffectView.changeGiftToModel(dict: giftDic) ?? TalkingGiftMsgAnimatModel()
        let giftModel = giftEffectView.variation(dict: giftDic) ?? RoughnessMeasurable()
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in MotivationThen.share.beyondGet() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [FenceTransformable]
        }
        //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel)
        giftTrackView.modelSource(model: giftModel)
        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.addressReloadDict(dict: giftDic)
    }

    //: func addGiftEffectModelArr(giftArr: [Any]) {
    func rewardArr(giftArr: [Any]) {
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in MotivationThen.share.beyondGet() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [FenceTransformable]
        }
        //: for i in 0 ..< giftArr.count {
        for i in 0 ..< giftArr.count {
            //: let giftModel = giftArr[i]
            let giftModel = giftArr[i]
            //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel as! TalkingGiftMsgAnimatModel)
            giftTrackView.modelSource(model: giftModel as! RoughnessMeasurable)
        }
        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.framework(modelArr: giftArr)
    }
}

// MARK: - 通话计时器

//: extension Talking1v1VoiceInteractionView {
extension DarkView {
    /// 开始通话计时
    //: private func startTalkCount() {
    private func dateAgent() {
        //: guard countTimer == nil else { return }
        guard countTimer == nil else { return }

        //: if startTime == nil {
        if startTime == nil {
            //: let interval = Date().timeIntervalSince1970
            let interval = Date().timeIntervalSince1970
            //: startTime = interval
            startTime = interval
            //: chatModel.startTime = startTime
            chatModel.startTime = startTime
        }
        //: countTimer = Timer.scheduledTimer(withTimeInterval: 1, block: { [weak self] _ in
        countTimer = Timer.scheduledTimer(withTimeInterval: 1, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.changeTalkTime()
            self.animalFee()
            //: }, repeats: true)
        }, repeats: true)
    }

    // 展示通话时间
    //: private func changeTalkTime() {
    private func animalFee() {
        //: let duration = getCurrentTalkingTime()
        let duration = allSmallApply()
        //: let min = floor(duration/60)
        let min = floor(duration / 60)
        //: let sec = floor(duration.truncatingRemainder(dividingBy: 60))
        let sec = floor(duration.truncatingRemainder(dividingBy: 60))
        //: timeLab.text = String(format: "%02i:%02i", Int(min), Int(sec))
        timeLab.text = String(format: "%02i:%02i", Int(min), Int(sec))
        // 更新小窗口通话时间
        //: delegate?.interactionView_shouldUpdateTime(time: duration)
        delegate?.visualAspectTime(time: duration)
    }

    /// 恢复通话，更新通话时间
    /// - Parameter startTime: 通话时间
    //: func updateStartTime(startTime: TimeInterval) {
    func noDraftNext(startTime: TimeInterval) {
        //: self.startTime = startTime
        self.startTime = startTime
        //: changeTalkTime()
        animalFee()
    }

    /// 获取当前通话时间
    /// - Returns: 秒
    //: func getCurrentTalkingTime() -> Double {
    func allSmallApply() -> Double {
        //: guard chatModel.videoStage == .Calling else { return 0 }
        guard chatModel.videoStage == .Calling else { return 0 }
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let duration = floor(interval-(startTime ?? 0))
        let duration = floor(interval - (startTime ?? 0))
        //: return duration
        return duration
    }
}

// MARK: - 【通话中】触屏隐藏交互按钮逻辑

//: extension Talking1v1VoiceInteractionView {
extension DarkView {
    /// 触摸隐藏开启延时
    //: private func touchHiddenTimer() {
    private func crush() {
        //: invalidateIdleTimer()
        inmateBold()
        //: perform(#selector(hideInteractionView), with: nil, afterDelay: 10)
        perform(#selector(resumeIn), with: nil, afterDelay: 10)
    }

    /// 销毁延迟
    //: private func invalidateIdleTimer() {
    private func inmateBold() {
        //: NSObject.cancelPreviousPerformRequests(withTarget: self, selector: #selector(hideInteractionView), object: nil)
        NSObject.cancelPreviousPerformRequests(withTarget: self, selector: #selector(resumeIn), object: nil)
    }

    /// 延时到时隐藏视图
    //: @objc private func hideInteractionView() {
    @objc private func resumeIn() {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.changeInteractionShowState()
            self.produceMutual()
        }
    }
}

// MARK: - Layout

//: extension Talking1v1VoiceInteractionView {
extension DarkView {
    /// 添加视图
    //: private func setupSubviews() {
    private func rewardAttention() {
        //: addSubview(bgView)
        addSubview(bgView)
        //: addSubview(giftEffectView)
        addSubview(giftEffectView)
        //: insertSubview(giftTrackView, belowSubview: giftEffectView)
        insertSubview(giftTrackView, belowSubview: giftEffectView)
        //: addSubview(touchHidenBtn)
        addSubview(touchHidenBtn)
        //: addSubview(pairPicV)
        addSubview(pairPicV)
        //: addSubview(pairNameLab)
        addSubview(pairNameLab)
        //: addSubview(locationBtn)
        addSubview(locationBtn)
        //: addSubview(minimizeBtn)
        addSubview(minimizeBtn)
        //: addSubview(hangupBtn)
        addSubview(hangupBtn)
        //: addSubview(acceptBtn)
        addSubview(acceptBtn)
        //: addSubview(micBtn)
        addSubview(micBtn)
        //: addSubview(handsfreeBtn)
        addSubview(handsfreeBtn)
        //: addSubview(sendGiftBtn)
        addSubview(sendGiftBtn)
        //: addSubview(rechargeBtn)
        addSubview(rechargeBtn)
        //: addSubview(desLab)
        addSubview(desLab)
        //: addSubview(timeLab)
        addSubview(timeLab)
        //: addSubview(femaleAcceptBGBtn)
        addSubview(femaleAcceptBGBtn)
        //: addSubview(femaleAcceptBtn)
        addSubview(femaleAcceptBtn)
        //: femaleAcceptBtn.addSubview(femaleAcceptTipsBtn)
        femaleAcceptBtn.addSubview(femaleAcceptTipsBtn)
        //: addSubview(acceptPlayer)
        addSubview(acceptPlayer)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func yesterdaySpend() {
        //: bgView.snp.makeConstraints { make in
        bgView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: giftEffectView.snp.makeConstraints { make in
        giftEffectView.snp.makeConstraints { make in
            //: make.leading.trailing.top.bottom.equalTo(self)
            make.leading.trailing.top.bottom.equalTo(self)
        }
        //: touchHidenBtn.snp.makeConstraints { make in
        touchHidenBtn.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: pairPicV.snp.makeConstraints { make in
        pairPicV.snp.makeConstraints { make in
            //: make.bottom.equalTo(self.snp.centerY).offset(actualHeight(h: -148))
            make.bottom.equalTo(self.snp.centerY).offset(actualHeight(h: -148))
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 100), height: actualWidth(w: 100)))
            make.size.equalTo(CGSize(width: actualWidth(w: 100), height: actualWidth(w: 100)))
        }
        //: pairNameLab.snp.makeConstraints { make in
        pairNameLab.snp.makeConstraints { make in
            //: make.top.equalTo(pairPicV.snp.bottom).offset(14)
            make.top.equalTo(pairPicV.snp.bottom).offset(14)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.width.equalTo(actualWidth(w: 133))
            make.width.equalTo(actualWidth(w: 133))
        }
        //: locationBtn.snp.makeConstraints { make in
        locationBtn.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.top.equalTo(pairNameLab.snp.bottom).offset(8)
            make.top.equalTo(pairNameLab.snp.bottom).offset(8)
            //: make.height.equalTo(14)
            make.height.equalTo(14)
        }
        //: giftTrackView.snp.makeConstraints { make in
        giftTrackView.snp.makeConstraints { make in
            //: make.leading.equalTo(self)
            make.leading.equalTo(self)
            //: make.height.equalTo(180)
            make.height.equalTo(180)
            //: make.top.equalTo(pairNameLab.snp.bottom).offset(90)
            make.top.equalTo(pairNameLab.snp.bottom).offset(90)
        }
        //: minimizeBtn.snp.makeConstraints { make in
        minimizeBtn.snp.makeConstraints { make in
            //: make.top.equalTo(StatusBarHeight+10)
            make.top.equalTo(dataPointTitle + 10)
            //: make.leading.equalTo(10)
            make.leading.equalTo(10)
            //: make.width.height.equalTo(actualWidth(w: 42))
            make.width.height.equalTo(actualWidth(w: 42))
        }
        //: hangupBtn.snp.makeConstraints { make in
        hangupBtn.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.bottom.equalTo(-kDeviceSafeBottomHeight-161)
            make.bottom.equalTo(-appInviteValue - 161)
            //: make.size.equalTo(actualWidth(w: 70))
            make.size.equalTo(actualWidth(w: 70))
        }
        //: acceptBtn.snp.makeConstraints { make in
        acceptBtn.snp.makeConstraints { make in
            //: make.edges.equalTo(hangupBtn)
            make.edges.equalTo(hangupBtn)
        }
        //: femaleAcceptBtn.snp.makeConstraints { make in
        femaleAcceptBtn.snp.makeConstraints { make in
            //: make.edges.equalTo(hangupBtn)
            make.edges.equalTo(hangupBtn)
        }
        //: femaleAcceptTipsBtn.snp.makeConstraints { make in
        femaleAcceptTipsBtn.snp.makeConstraints { make in
            //: make.bottom.equalTo(-9)
            make.bottom.equalTo(-9)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }
        //: let letftPlace = (ScreenWidth-actualWidth(w: 45)*4-actualWidth(w: 25)*3)/2
        let letftPlace = (main_logStr - actualWidth(w: 45) * 4 - actualWidth(w: 25) * 3) / 2
        //: micBtn.snp.makeConstraints { make in
        micBtn.snp.makeConstraints { make in
            //: make.leading.equalTo(letftPlace)
            make.leading.equalTo(letftPlace)
            //: make.bottom.equalTo(-kDeviceSafeBottomHeight-16)
            make.bottom.equalTo(-appInviteValue - 16)
            //: make.size.equalTo(actualWidth(w: 45))
            make.size.equalTo(actualWidth(w: 45))
        }
        //: handsfreeBtn.snp.makeConstraints { make in
        handsfreeBtn.snp.makeConstraints { make in
            //: make.leading.equalTo(micBtn.snp.trailing).offset(actualWidth(w: 25))
            make.leading.equalTo(micBtn.snp.trailing).offset(actualWidth(w: 25))
            //: make.bottom.size.equalTo(micBtn)
            make.bottom.size.equalTo(micBtn)
        }
        //: rechargeBtn.snp.makeConstraints { make in
        rechargeBtn.snp.makeConstraints { make in
            //: make.leading.equalTo(handsfreeBtn.snp.trailing).offset(actualWidth(w: 25))
            make.leading.equalTo(handsfreeBtn.snp.trailing).offset(actualWidth(w: 25))
            //: make.bottom.size.equalTo(micBtn)
            make.bottom.size.equalTo(micBtn)
        }
        //: sendGiftBtn.snp.makeConstraints { make in
        sendGiftBtn.snp.makeConstraints { make in
            //: make.leading.equalTo(rechargeBtn.snp.trailing).offset(actualWidth(w: 25))
            make.leading.equalTo(rechargeBtn.snp.trailing).offset(actualWidth(w: 25))
            //: make.bottom.size.equalTo(micBtn)
            make.bottom.size.equalTo(micBtn)
        }
        //: desLab.snp.makeConstraints { make in
        desLab.snp.makeConstraints { make in
            //: make.top.equalTo(locationBtn.snp.bottom).offset(10)
            make.top.equalTo(locationBtn.snp.bottom).offset(10)
            //: make.leading.equalTo(54)
            make.leading.equalTo(54)
            //: make.trailing.equalTo(-54)
            make.trailing.equalTo(-54)
        }
        //: timeLab.snp.makeConstraints { make in
        timeLab.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.top.equalTo(StatusBarHeight+16)
            make.top.equalTo(dataPointTitle + 16)
        }
    }

    /// 更新约束
    //: private func updateLayout() {
    private func selectedLayout() {
        //: if chatModel.videoStage == .Recieved {
        if chatModel.videoStage == .Recieved {
            //: if DismissReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue {
            if DismissReactiveCompatible.share.loginUserMode.sex == FenceSequence.male.rawValue {
                //: hangupBtn.snp.remakeConstraints { make in
                hangupBtn.snp.remakeConstraints { make in
                    //: make.leading.equalTo(ScreenWidth/2-42-70)
                    make.leading.equalTo(main_logStr / 2 - 42 - 70)
                    //: make.bottom.equalTo(-kDeviceSafeBottomHeight-161)
                    make.bottom.equalTo(-appInviteValue - 161)
                    //: make.size.equalTo(70)
                    make.size.equalTo(70)
                }
                //: acceptBtn.snp.remakeConstraints { make in
                acceptBtn.snp.remakeConstraints { make in
                    //: make.leading.equalTo(ScreenWidth/2+42)
                    make.leading.equalTo(main_logStr / 2 + 42)
                    //: make.bottom.equalTo(hangupBtn)
                    make.bottom.equalTo(hangupBtn)
                    //: make.size.equalTo(hangupBtn)
                    make.size.equalTo(hangupBtn)
                }
                //: } else {
            } else {
                //: hangupBtn.snp.remakeConstraints { make in
                hangupBtn.snp.remakeConstraints { make in
                    //: make.leading.equalTo(34)
                    make.leading.equalTo(34)
                    //: make.bottom.equalTo(-kDeviceSafeBottomHeight-161)
                    make.bottom.equalTo(-appInviteValue - 161)
                    //: make.size.equalTo(70)
                    make.size.equalTo(70)
                }
                //: femaleAcceptBGBtn.snp.remakeConstraints { make in
                femaleAcceptBGBtn.snp.remakeConstraints { make in
                    //: make.trailing.equalTo(-33)
                    make.trailing.equalTo(-33)
                    //: make.bottom.equalTo(-kDeviceSafeBottomHeight-147)
                    make.bottom.equalTo(-appInviteValue - 147)
                    //: make.size.equalTo(CGSize.init(width: 208, height: 99))
                    make.size.equalTo(CGSize(width: 208, height: 99))
                }
                //: femaleAcceptBtn.snp.remakeConstraints { make in
                femaleAcceptBtn.snp.remakeConstraints { make in
                    //: make.centerX.equalTo(femaleAcceptBGBtn)
                    make.centerX.equalTo(femaleAcceptBGBtn)
                    //: make.bottom.equalTo(femaleAcceptBGBtn.snp.bottom).offset(-15)
                    make.bottom.equalTo(femaleAcceptBGBtn.snp.bottom).offset(-15)
                    //: make.size.equalTo(CGSize.init(width: 180, height: 70))
                    make.size.equalTo(CGSize(width: 180, height: 70))
                }
                //: acceptPlayer.snp.remakeConstraints { make in
                acceptPlayer.snp.remakeConstraints { make in
                    //: make.trailing.equalTo(-8)
                    make.trailing.equalTo(-8)
                    //: make.bottom.equalTo(femaleAcceptBtn.snp.bottom).offset(10)
                    make.bottom.equalTo(femaleAcceptBtn.snp.bottom).offset(10)
                    //: make.size.equalTo(85)
                    make.size.equalTo(85)
                }
            }

            //: } else {
        } else {
            //: hangupBtn.snp.remakeConstraints { make in
            hangupBtn.snp.remakeConstraints { make in
                //: make.centerX.equalToSuperview()
                make.centerX.equalToSuperview()
                //: make.bottom.equalTo(-kDeviceSafeBottomHeight-161)
                make.bottom.equalTo(-appInviteValue - 161)
                //: make.size.equalTo(actualWidth(w: 70))
                make.size.equalTo(actualWidth(w: 70))
            }
        }
    }
}
