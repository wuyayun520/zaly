
//: Declare String Begin

/*: "IM退出登录失败，code: :*/
fileprivate let user_sweetId:[Character] = ["I","M","退","\u{51fa}","\u{767b}","\u{5f55}","失","败","，"]
fileprivate let kSharedUrl:[Character] = ["c","o","d","e",":"]

/*: , desc: :*/
fileprivate let user_alongsideName:[Character] = [","," "]
fileprivate let show_windMsg:[Character] = ["d","e","s","c",":"]

/*: "IM登录失败，code: :*/
fileprivate let noti_normallyFormat:String = "IM登pop catch holder price"
fileprivate let mainScopeValue:String = "录\u{5931}\u{8d25}\u{ff0c}code:"

/*: "IM获取未读消息总数失败，code: :*/
fileprivate let showDecideFormat:[Character] = ["I","M","\u{83b7}","\u{53d6}","未","读","消"]
fileprivate let noti_assistStr:String = "息总数"
fileprivate let main_zoneGivenCurFormat:String = "random base advertising arc失败，code:"

/*: "crushBadge" :*/
fileprivate let notiActiveMessage:String = "crseash"
fileprivate let k_globalMsg:String = "context"

/*: "badNumber" :*/
fileprivate let dataDestroyId:[UInt8] = [0x9a,0x99,0x9c,0xb6,0x8d,0x95,0x9a,0x9d,0x8a]

/*: "plaza" :*/
fileprivate let userToneMsg:String = "redaza"

/*: "roomNum" :*/
fileprivate let showNoteFormat:String = "river due wedding analyze funroomN"
fileprivate let dataChoiceName:String = "us"

/*: "num" :*/
fileprivate let data_reStr:String = "ncontinuing"

/*: "onlyOneRoomId" :*/
fileprivate let noti_capPath:String = "onlyOnfailure topic"
fileprivate let data_sharedUrl:[Character] = ["d"]

/*: "LIVE-CHAT" :*/
fileprivate let show_observerKey:String = "LIvideoE"
fileprivate let notiSupplyTitle:String = "pow send-CHAT"

/*: "extra" :*/
fileprivate let data_lipMsg:String = "exaveragera"

/*: "msgType" :*/
fileprivate let app_fenceNeckTitle:String = "door theater then parmsgType"

/*: "newPkgGift" :*/
fileprivate let const_privacyStrokeData:[Character] = ["n","e"]
fileprivate let user_resistanceId:[Character] = ["w","P","k","g","G","i","f","t"]

/*: "MF:ChatMentionMsg" :*/
fileprivate let user_whichData:String = "us let shot tun handMF:C"
fileprivate let kFollowUrl:[Character] = ["h","a","t","M","e","n","t","i","o","n","M","s","g"]

/*: "uid" :*/
fileprivate let noti_viewUrl:[UInt8] = [0x64,0x69,0x75]

/*: "onlineStatus" :*/
fileprivate let dataDocumentContent:[UInt8] = [0xa,0xb,0x9,0xc,0xb,0x0,0x36,0x11,0x4,0x11,0x10,0x16]

private func invitationSmall(trigger num: UInt8) -> UInt8 {
    return num ^ 101
}

/*: "isNewUser" :*/
fileprivate let userMomentBroadArrayName:[UInt8] = [0xeb,0xf1,0xcc,0xe7,0xf5,0xd7,0xf1,0xe7,0xf0]

/*: "c2c_ :*/
fileprivate let data_attributeUrl:[Character] = ["c","2","c","_"]

/*: "IM获取会话失败，convId: :*/
fileprivate let user_singleValue:[Character] = ["I","M","获","取","\u{4f1a}","话","失","败","，","c","o"]
fileprivate let showFinishKey:[Character] = ["n","v","I","d",":"]

/*: , code: :*/
fileprivate let notiExtendFormat:[Character] = [","," ","c","o","d","e",":"]

/*: "IM插入消息失败，targetId: :*/
fileprivate let kSurgeryNatureValue:String = "IM插入\u{6d88}"
fileprivate let noti_familyUrl:[Character] = ["息","失","败","，","t"]
fileprivate let showDiskHolderBlankMessage:String = "come finite brush orientationargetId:"

/*: "changeDirection" :*/
fileprivate let constCanPortraitPath:String = "chaslowge"
fileprivate let userUntilMsg:[Character] = ["c","t","i","o","n"]

/*: "rYMsgType" :*/
fileprivate let mainPastKey:String = "in label within properlyrYMsgT"
fileprivate let data_alongValue:[Character] = ["y","p","e"]

/*: "GJ:CallCustom" :*/
fileprivate let data_darkMessage:String = "GJ:CallCagent export fee liquid week"
fileprivate let kBounceFormat:[Character] = ["u","s","t","o","m"]

/*: "IM获取会话列表失败，code: :*/
fileprivate let kResolutionData:String = "sweep stumble my pureIM获取会话"
fileprivate let show_cloudStr:String = "ode:close ting"

/*: "msgInfo" :*/
fileprivate let showReceiveFormat:[Character] = ["m","s","g","I","n","f"]
fileprivate let notiBraceStr:String = "pure"

/*: "needFold" :*/
fileprivate let userMovieText:String = "cute broad condition scoreneedFold"

/*: "group_ :*/
fileprivate let constDestroyPath:String = "GROUP"
fileprivate let userLabMessage:String = "automatically"

/*: "IM删除会话失败，convID: :*/
fileprivate let notiBucketHoldTitle:[Character] = ["I","M","删","\u{9664}","会","话","失","败","，","c","o","n","v","I"]
fileprivate let showAcquireName:[Character] = ["D",":"]

/*: "IM置顶会话失败，convId: :*/
fileprivate let showDomainName:String = "explain ting give hostIM置顶会话"
fileprivate let app_sitData:[Character] = ["v","I","d",":"]

/*: "SDK 正在连接到服务器" :*/
fileprivate let dataHoweverDiscoursePath:[Character] = ["S","D","K"," ","正","在","连","接"]
fileprivate let data_cuteId:String = "到服务器"

/*: "SDK 已经成功连接到服务器" :*/
fileprivate let userIllegalPath:String = "seed today side southwestSDK 已"
fileprivate let k_migrationId:String = "show"
fileprivate let dataBoldValue:[Character] = ["\u{6210}","功","连","接","到","服","\u{52a1}","器"]

/*: "IM连接服务器失败，code: :*/
fileprivate let main_languageData:String = "IM连接服务\u{5668}"
fileprivate let const_proName:String = "失"
fileprivate let constOffPath:String = "fast advice aid resistance败，code:"

/*: , error: :*/
fileprivate let appPlaceFormat:[Character] = [","," ","e","r","r","o","r",":"]

/*: "IM账号被挤下线。" :*/
fileprivate let appGalleryFormat:String = "ex new material mouthIM账号"

/*: "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password." :*/
fileprivate let data_trainId:[UInt8] = []

fileprivate func leastPersonalityMin(common num: UInt8) -> UInt8 {
    let value = Int(num) - 68
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Offline Notification" :*/
fileprivate let const_portfolioName:String = "Offlidynamic control"
fileprivate let user_welcomeLibraryValue:[Character] = ["n"]
fileprivate let notiSecureId:[Character] = ["e"," ","N","o","t","i","f","i","c","a","t","i","o","n"]

/*: "Cancel" :*/
fileprivate let data_socialPath:[Character] = ["C","a","n","c","e","l"]

/*: "Re Login" :*/
fileprivate let k_dataCollectFormat:[Character] = ["R","e"," ","L","o","g","i","n"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  AfterThen.swift
//  AbroadTalking
//
//  Created by Hemming on 2023/3/7.
//

//: import UIKit
import UIKit

/// 视频通话【拨打方】代理
//: @objc public protocol IMManagerDelegate: NSObjectProtocol {
@objc public protocol ManufacturerManagerDelegate: NSObjectProtocol {
    /// 会话列表更新
    //: @objc optional func onRefreshConversationList(data: [TalkingConversationModel])
    @objc optional func heat(data: [IceTapThen])

    /// 未读数更新
    //: @objc optional func onUnreadMsgCountChanged(count: Int)
    @objc optional func failure(count: Int)

    /// 收到新消息
    //: @objc optional func onRecvNewMessage(msg: V2TIMMessage)
    @objc optional func control(msg: V2TIMMessage)
}

/// 数据列表枚举
//: enum ChatListDataType {
enum FirDataType {
    //: case normalList
    case normalList // 默认列表
    //: case topList
    case topList // 置顶列表
    //: case moreList
    case moreList // 更多消息（超过5天未回复）
}

//: @objcMembers public class AfterThen: NSObject {
@objcMembers public class AfterThen: NSObject {
    //: public static let shared = AfterThen()
    public static let shared = AfterThen()
    //: public dynamic var isConnection = false
    public dynamic var isConnection = false
    //: var isRefreshSysMsg = false
    var isRefreshSysMsg = false
    //: var lastSysTime: TimeInterval = 0
    var lastSysTime: TimeInterval = 0

    //: var officialList: [TalkingConversationModel] = []
    var officialList: [IceTapThen] = [] // 官方列表（like+系统通知+客服）
    //: var topConvList: [TalkingConversationModel] = []
    var topConvList: [IceTapThen] = [] // 置顶列表
    //: var norConvList: [TalkingConversationModel] = []
    var norConvList: [IceTapThen] = [] // 普通会话列表
    //: var chatRoomList: [TalkingConversationModel] = []
    var chatRoomList: [IceTapThen] = [] // 聊天室
    //: var moreMsgList: [TalkingConversationModel] = []
    var moreMsgList: [IceTapThen] = [] // 更多消息（超过5天未回复）

    /// 消息列表数据分页
    //: private var nextSeq: UInt64 = 0
    private var nextSeq: UInt64 = 0
    /// 多个代理容器
    //: private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    /// 会话列表缓存数据（避免重复转换）
    //: private var convListCache = [String: TalkingConversationModel]()
    private var convListCache = [String: IceTapThen]()

    //: override private init() {
    override private init() {
        //: super.init()
        super.init()
        //: self.initialization()
        self.fail()
    }

    // MARK: - SDK初始化

    //: func func__TXSDKInit() {
    func brazilPrimary() {
        //: let config = V2TIMSDKConfig()
        let config = V2TIMSDKConfig()
        //: config.logLevel = .LOG_DEBUG
        config.logLevel = .LOG_DEBUG
        //: V2TIMManager.sharedInstance().initSDK(TXIMAppID, config: config)
        V2TIMManager.sharedInstance().initSDK(constSuccessStr, config: config)
        //: V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        //: V2TIMManager.sharedInstance().addConversationListener(listener: self)
        V2TIMManager.sharedInstance().addConversationListener(listener: self)
        //: V2TIMManager.sharedInstance().add(self)
        V2TIMManager.sharedInstance().add(self)
    }

    // MARK: - 清除单例状态

    //: func initialization() {
    func fail() {
        //: func__removeAllDelegate()
        signalCamera()
        //: self.isConnection = false
        self.isConnection = false
        //: self.nextSeq = 0
        self.nextSeq = 0
        //: self.lastSysTime = 0
        self.lastSysTime = 0
        //: self.func__adddofficialList()
        self.voiceList()
        //: self.convListCache.removeAll()
        self.convListCache.removeAll()
        //: self.norConvList.removeAll()
        self.norConvList.removeAll()
        //: self.topConvList.removeAll()
        self.topConvList.removeAll()
        //: self.moreMsgList.removeAll()
        self.moreMsgList.removeAll()
    }

    /// 添加官方数据模型
    //: func func__adddofficialList() {
    func voiceList() {
        //: self.officialList.removeAll()
        self.officialList.removeAll()
        // 添加官方占位model
        //: let systemModel = TalkingConversationModel.getPlaceholderModel(chatType: .system)
        let systemModel = IceTapThen.tagFollowing(chatType: .system)
        //: let serviceModel = TalkingConversationModel.getPlaceholderModel(chatType: .service)
        let serviceModel = IceTapThen.tagFollowing(chatType: .service)
        //: self.officialList = [systemModel, serviceModel]
        self.officialList = [systemModel, serviceModel]
    }

    /// 获取官方数据列表（在审核模式下过滤客服消息不展示）
    /// - Returns: 模型
    //: func getOfficialList() -> [TalkingConversationModel] {
    func beyond() -> [IceTapThen] {
        // 审核模式，在消息列表移除官方客服
        //: if DismissReactiveCompatible.share.appStatus == AppSkinStatus.special.rawValue {
        if DismissReactiveCompatible.share.appStatus == AfterArtSumLiteral.special.rawValue {
            //: if let index = self.officialList.firstIndex(where: { $0.userID == DepictionThen.getCustomerServiceID() }) {
            if let index = self.officialList.firstIndex(where: { $0.userID == DepictionThen.noteFamily() }) {
                //: self.officialList.remove(at: index)
                self.officialList.remove(at: index)
            }
        }
        //: return self.officialList
        return self.officialList
    }
}

// MARK: - 管理多个代理

//: extension AfterThen {
extension AfterThen {
    //: func func__removeAllDelegate() {
    func signalCamera() {
        //: self.multiDelegate.removeAllObjects()
        self.multiDelegate.removeAllObjects()
    }

    //: func func__addDelegate(_ delegate: IMManagerDelegate) {
    func funcSocialTranslateDelegate(_ delegate: ManufacturerManagerDelegate) {
        //: self.multiDelegate.add(delegate)
        self.multiDelegate.add(delegate)
    }

    //: func func__removeDelegate(_ delegate: IMManagerDelegate) {
    func waveDelegate(_ delegate: ManufacturerManagerDelegate) {
        //: self.func__invokeDelegate {
        self.skirt {
            //: if $0 === delegate as AnyObject {
            if $0 === delegate as AnyObject {
                //: self.multiDelegate.remove($0)
                self.multiDelegate.remove($0)
            }
        }
    }

    //: private func func__invokeDelegate(_ invocation: (IMManagerDelegate) -> Void) {
    private func skirt(_ invocation: (ManufacturerManagerDelegate) -> Void) {
        //: for delegate in self.multiDelegate.allObjects.reversed() {
        for delegate in self.multiDelegate.allObjects.reversed() {
            //: invocation(delegate as! IMManagerDelegate)
            invocation(delegate as! ManufacturerManagerDelegate)
        }
    }
}

// MARK: - IM登录态管理

//: extension AfterThen {
extension AfterThen {
    /// 退出登录IM
    //: func func__LogingOut() {
    func lifetime() {
        //: V2TIMManager.sharedInstance().logout {} fail: { code, desc in
        V2TIMManager.sharedInstance().logout {} fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM退出登录失败，code:\(code), desc:\(String(describing: desc)).")
            MateThen.trackBy(msg: (String(user_sweetId) + String(kSharedUrl)) + "\(code)" + (String(user_alongsideName) + String(show_windMsg)) + "\(String(describing: desc)).")
        }
        //: self.initialization()
        self.fail()
    }

    /// 登录IM
    //: func func__LogingIn(completion: ((_ succeed: Bool) -> Void)? = nil) {
    func offHauled(completion: ((_ succeed: Bool) -> Void)? = nil) {
        //: guard self.isConnection == false else { return }
        guard self.isConnection == false else { return }
        //: let uSigStr = DismissReactiveCompatible.share.appUserConfigMode.uSig
        let uSigStr = DismissReactiveCompatible.share.appUserConfigMode.uSig
        //: let uidStr = DismissReactiveCompatible.share.loginUserMode.userID
        let uidStr = DismissReactiveCompatible.share.loginUserMode.userID
        //: V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
        V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
            //: self.isConnection = true
            self.isConnection = true
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: self.func__getUnreadMsgCount()
                self.underBeginSufficient()
            }
            //: completion?(true)
            completion?(true)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM登录失败，code:\(code), desc:\(String(describing: desc)).")
            MateThen.trackBy(msg: (String(noti_normallyFormat.prefix(3)) + mainScopeValue) + "\(code)" + (String(user_alongsideName) + String(show_windMsg)) + "\(String(describing: desc)).")
            //: self.isConnection = false
            self.isConnection = false
            //: completion?(false)
            completion?(false)
        }
    }

    /// 检测是否登录
    //: func func__checkCanOperateList() -> Bool {
    func direct() -> Bool {
        //: let status = V2TIMManager.sharedInstance().getLoginStatus()
        let status = V2TIMManager.sharedInstance().getLoginStatus()
        //: if self.isConnection == false || status != .STATUS_LOGINED || DismissReactiveCompatible.share.networkStatus == .Unavailable {
        if self.isConnection == false || status != .STATUS_LOGINED || DismissReactiveCompatible.share.networkStatus == .Unavailable {
            //: self.func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            self.enablece(showMsg: main_textStr)
            //: return false
            return false
        }
        //: return true
        return true
    }
}

// MARK: - 获取未读消息数、wholikeme消息角标、聊天室信息

//: extension AfterThen {
extension AfterThen {
    /// 获取未读消息数
    //: func func__getUnreadMsgCount() {
    func underBeginSufficient() {
        //: V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
        V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
            //: self.onTotalUnreadMessageCountChanged(totalCount)
            self.onTotalUnreadMessageCountChanged(totalCount)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM获取未读消息总数失败，code:\(code), desc:\(String(describing: desc)).")
//            MateThen.trackBy(msg: (String(showDecideFormat) + noti_assistStr.capitalized + String(main_zoneGivenCurFormat.suffix(8))) + "\(code)" + (String(user_alongsideName) + String(show_windMsg)) + "\(String(describing: desc)).")
        }
    }

    /// 刷新wholikeme消息角标
    //: func func__refreshSysMsg() {
    func answer() {
        //: let nowTs = NSDate.timeIntervalSinceReferenceDate
        let nowTs = NSDate.timeIntervalSinceReferenceDate
        //: if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
        if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
            //: return
            return
        }
        //: self.lastSysTime = nowTs
        self.lastSysTime = nowTs
        //: self.isRefreshSysMsg = true
        self.isRefreshSysMsg = true

        //: TalkingChatRequestTool.req_sysMessageList { _, result in
        TamperRequestTool.completion { _, result in
            //: self.isRefreshSysMsg = false
            self.isRefreshSysMsg = false
            //: guard let dict = result as? [String: Any] else {
            guard let dict = result as? [String: Any] else {
                //: return
                return
            }
            //: let number = dict["crushBadge"] as! Int
            let number = dict[(notiActiveMessage.replacingOccurrences(of: "sea", with: "u") + "Badg" + k_globalMsg.replacingOccurrences(of: "context", with: "e"))] as! Int
            //: NotificationCenter.default.post(name: WHOLIKEME_BADGENUMBER_NOTIF, object: self, userInfo: ["badNumber": number])
            NotificationCenter.default.post(name: showToMessage, object: self, userInfo: [String(bytes: dataDestroyId.map{$0^248}, encoding: .utf8)!: number])
        }
    }

    /// 聊天室信息
    /// - Parameter completion: 回调
    //: func getChatRoomData(completion: @escaping (_ succeed: Bool) -> Void) {
    func phaseSpace(completion: @escaping (_ succeed: Bool) -> Void) {
        //: chatRoomList.removeAll()
        chatRoomList.removeAll()
        //: TalkingChatRequestTool.req_getChatRoomInfo { succeed, result, _ in
        TamperRequestTool.atWork { succeed, result, _ in
            //: guard succeed,
            guard succeed,
                  //: let data = result as? [String: Any],
                  let data = result as? [String: Any],
                  //: let plaza = data["plaza"] as? [String: Any] else {
                  let plaza = data[(userToneMsg.replacingOccurrences(of: "red", with: "pl"))] as? [String: Any]
            else {
                //: completion(false)
                completion(false)
                //: return
                return
            }

            //: if plaza["roomNum"] as? Int ?? 0 > 0 {
            if plaza[(String(showNoteFormat.suffix(5)) + dataChoiceName.replacingOccurrences(of: "s", with: "m"))] as? Int ?? 0 > 0 {
                //: let model = TalkingConversationModel.getPlaceholderModel(chatType: .group)
                let model = IceTapThen.tagFollowing(chatType: .group)
                //: model.num = plaza["num"] as? Int ?? 0
                model.num = plaza[(data_reStr.replacingOccurrences(of: "continuing", with: "um"))] as? Int ?? 0
                //: model.onlyOneRoomId = plaza["onlyOneRoomId"] as? String ?? ""
                model.onlyOneRoomId = plaza[(String(noti_capPath.prefix(6)) + "eRoomI" + String(data_sharedUrl))] as? String ?? ""
                //: self.chatRoomList.append(model)
                self.chatRoomList.append(model)
            }
            //: completion(true)
            completion(true)
        }
    }
}

// MARK: - V2TIMAdvancedMsgListener

//: extension AfterThen: V2TIMAdvancedMsgListener {
extension AfterThen: V2TIMAdvancedMsgListener {
    /// 接收到新消息
    /// - Parameter msg: 消息数据
    //: public func onRecvNewMessage(_ msg: V2TIMMessage!) {
    public func onRecvNewMessage(_ msg: V2TIMMessage!) {
        //: if msg.groupID != nil && (msg.groupID.contains("LIVE-CHAT")) { // 开播 弹幕提醒
        if msg.groupID != nil && (msg.groupID.contains((show_observerKey.replacingOccurrences(of: "video", with: "V") + String(notiSupplyTitle.suffix(5))))) { // 开播 弹幕提醒
            //: TalkingDanmuManager.shared().onRecvDanmuNewMsg(msg: msg)
            TotalensitySpillDecisionMakerDanmuManager.reader().notCommon(msg: msg)
            //: return
            return
        }
        //: if msg.sender == DismissReactiveCompatible.share.loginUserMode.userID { return }
        if msg.sender == DismissReactiveCompatible.share.loginUserMode.userID { return }

        //: let dic = AcousticPowerThen.parseTXMessageData(data: msg.customElem.data)
        let dic = AcousticPowerThen.leaveDoingice(data: msg.customElem.data)
        //: let extraDic = dic["extra"] as! [AnyHashable: Any]
        let extraDic = dic[(data_lipMsg.replacingOccurrences(of: "average", with: "t"))] as! [AnyHashable: Any]
        //: let msgModel = try? AbTalkingChatMsgBaseModel(dictionary: extraDic)
        let msgModel = try? ServiceModel(dictionary: extraDic)

        //: NotificationCenter.default.post(name: CHAT_NEW_MSG_NOTIFICATION, object: msg)
        NotificationCenter.default.post(name: userBusyMsg, object: msg)

        //: if let type = extraDic["msgType"] {
        if let type = extraDic[(String(app_fenceNeckTitle.suffix(7)))] {
            //: if type as? String == "newPkgGift" { // 刷新礼物背包
            if type as? String == (String(const_privacyStrokeData) + String(user_resistanceId)) { // 刷新礼物背包
                //: NotificationCenter.default.post(name: GIFT_REFRESHPACKAGE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: data_failId, object: nil)
            }
        }

        //: if msg.userID != nil && msg.userID.count > 0 {
        if msg.userID != nil && msg.userID.count > 0 {
            //: if AcousticPowerThen.checkVoiceMessage(message: msg) {
            if AcousticPowerThen.usedEnable(message: msg) { // 语音消息
                //: let dic = TUISocialChatManager.packageDict(withTXMessage: msg)
                let dic = TictacReactiveCompatible.bankManager(withTXMessage: msg)
                //: WCDBVoiceMsgTable.db_inserVoiceMsgToDB(dic)
                RedMsgTable.soul(dic)
            }
            //: self.judgeisHaveIntimate(msg: msg, dic: dic)
            self.book(msg: msg, dic: dic)
            // 处理礼物消息
            //: AbTalkingPrivateChatAnimatTool.shared.disposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            WeddingReactiveCompatible.shared.execute(giftMessageDic: dic as NSDictionary)
        }
        //: if msgModel?.msgInfo.messageType == .MessageTypeGift {
        if msgModel?.msgInfo.messageType == .MessageTypeGift { // 群聊收礼
            //: AbTalkingPrivateChatAnimatTool.shared.chatRoomDisposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            WeddingReactiveCompatible.shared.performName(giftMessageDic: dic as NSDictionary)
        }
        //: if msg.customElem.extension == "MF:ChatMentionMsg" { // 群聊@ 消息
        if msg.customElem.extension == (String(user_whichData.suffix(4)) + String(kFollowUrl)) { // 群聊@ 消息
            //: TalkingGroupChatManager.share.receivedMentionMsg(msg: msg, extraInfo: extraDic)
            ConfirmGuideReactiveCompatible.share.productMatch(msg: msg, extraInfo: extraDic)
        }

        //: if msgModel?.gift.unlockMsgId.count ?? 0 > 0 {
        if msgModel?.gift.unlockMsgId.count ?? 0 > 0 { // 记录解锁礼物消息
            //: var array: Array = UserDefaults.standard.object(forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            var array: Array = UserDefaults.standard.object(forKey: NameureThen.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            //: array.append(msgModel!.gift.unlockMsgId)
            array.append(msgModel!.gift.unlockMsgId)
            //: UserDefaults.standard.set(array, forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey)
            UserDefaults.standard.set(array, forKey: NameureThen.share.UnlockMsgIdArrayKey)
        }
        //: if msg.sender != DepictionThen.getXiaoMiID() && (msg.userID != nil && msg.userID.count > 0) {
        if msg.sender != DepictionThen.xiao() && (msg.userID != nil && msg.userID.count > 0) { // 过滤系统通知、群聊
            // delegate转发
            //: self.func__invokeDelegate { $0.onRecvNewMessage?(msg: msg) }
            self.skirt { $0.control?(msg: msg) }
            // 处理消息通知
            //: TalkingMessageNotificationManager.shared.onRecvNewMessage(msg: msg, dataDict: dic)
            TriggerPhaseThen.shared.turn(msg: msg, dataDict: dic)
            // 更新消息列表用户为在线状态
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: let userInfo = ["uid": msg.userID ?? "",
                let userInfo = [String(bytes: noti_viewUrl.reversed(), encoding: .utf8)!: msg.userID ?? "",
                                //: "onlineStatus": 1,
                                String(bytes: dataDocumentContent.map{invitationSmall(trigger: $0)}, encoding: .utf8)!: 1,
                                //: "isNewUser": msgModel?.msgInfo.isNewUser ?? false] as [String: Any]
                                String(bytes: userMomentBroadArrayName.map{$0^130}, encoding: .utf8)!: msgModel?.msgInfo.isNewUser ?? false] as [String: Any]
                //: NotificationCenter.default.post(name: MSGLIST_UPDATE_ONLINESTATUS_NOTIFICATION,
                NotificationCenter.default.post(name: show_voiceMsg,
                                                //: object: nil,
                                                object: nil,
                                                //: userInfo: userInfo)
                                                userInfo: userInfo)
            }
        }
    }

    /// C2C 收到对端用户会话已读通知
    //: public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
    public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
        //: for receipt in receiptList {
        for receipt in receiptList {
            // 更新数据库和缓存
            //: DBUserInfoManager.cache_updateReadReceiptTime(with: receipt.userID, time: receipt.timestamp)
            CutInfoManager.ownIn(with: receipt.userID, time: receipt.timestamp)
            //: TalkingPrivateChatManager.share.readReceiptDict[receipt.userID] = receipt.timestamp
            NameureThen.share.readReceiptDict[receipt.userID] = receipt.timestamp
            // 发送已读回执通知
            //: NotificationCenter.default.post(name: CHAT_MSG_READ_RECEIPT_NOTIFICATION, object: receipt)
            NotificationCenter.default.post(name: k_replaceInfoMsg, object: receipt)
        }
    }

    //: @objc public func func__handleMsg(targetId: String, msgCellData: SparkCellData, isPrivete: Bool) {
    @objc public func researchLaboratoryButton(targetId: String, msgCellData: SparkCellData, isPrivete: Bool) {
        //: if let imMsg = msgCellData.innerMessage {
        if let imMsg = msgCellData.innerMessage {
            //: self.func__insertMsgToLocalStorage(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
            self.robot(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
        }
        //: var convId = ""
        var convId = ""
        //: if isPrivete {
        if isPrivete {
            //: convId = "c2c_\(targetId)"
            convId = (String(data_attributeUrl)) + "\(targetId)"
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            //: V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
            V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if conv != nil {
                if conv != nil {
                    //: let list = self.func__handleConversationList(convList: [conv!])
                    let list = self.inviteCheckCorrigenda(convList: [conv!])
                    //: self.func__updateConversationList(conVersationList: list)
                    self.willDoing(conVersationList: list)
                }
                //: } fail: { code, desc in
            } fail: { code, desc in
                //: UploadLogTool.writeLog(msg: "IM获取会话失败，convId:\(convId), code:\(code), desc:\(String(describing: desc)).")
                MateThen.trackBy(msg: (String(user_singleValue) + String(showFinishKey)) + "\(convId)" + (String(notiExtendFormat)) + "\(code)" + (String(user_alongsideName) + String(show_windMsg)) + "\(String(describing: desc)).")
            }
        }
    }

    //: func func__insertMsgToLocalStorage(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
    func robot(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
        //: let senderStr = DismissReactiveCompatible.share.loginUserMode.userID
        let senderStr = DismissReactiveCompatible.share.loginUserMode.userID
        //: if isPrivete {
        if isPrivete {
            //: V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
            V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
                //: UploadLogTool.writeLog(msg: "IM插入消息失败，targetId:\(targetId), code:\(code), desc:\(String(describing: desc)).")
                MateThen.trackBy(msg: (kSurgeryNatureValue + String(noti_familyUrl) + String(showDiskHolderBlankMessage.suffix(8))) + "\(targetId)" + (String(notiExtendFormat)) + "\(code)" + (String(user_alongsideName) + String(show_windMsg)) + "\(String(describing: desc)).")
            }
        }
    }

    //: func judgeisHaveIntimate(msg: V2TIMMessage, dic: [String: Any]) {
    func book(msg: V2TIMMessage, dic: [String: Any]) {
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let isSelf = jsonDic["changeDirection"].boolValue
        let isSelf = jsonDic[(constCanPortraitPath.replacingOccurrences(of: "slow", with: "n") + "Dire" + String(userUntilMsg))].boolValue
        //: if jsonDic["rYMsgType"] == "GJ:CallCustom" && isSelf && PushThen.share.func__getCurrentActivityVC()!.isKind(of: TalkingPrivateChatController.self) == false {
        if jsonDic[(String(mainPastKey.suffix(6)) + String(data_alongValue))].stringValue == (String(data_darkMessage.prefix(8)) + String(kBounceFormat)) && isSelf && PushThen.share.doingGoing()!.isKind(of: SmallControllerDelegate.self) == false {
            //: var intimate: [String: Any] = UserDefaults.standard.object(forKey: isConversationIntimateCacheKey) as? [String: Any] ?? Dictionary()
            var intimate: [String: Any] = UserDefaults.standard.object(forKey: const_serverFormat) as? [String: Any] ?? Dictionary()
            //: intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            //: UserDefaults.standard.set(intimate, forKey: isConversationIntimateCacheKey)
            UserDefaults.standard.set(intimate, forKey: const_serverFormat)
        }
    }
}

// MARK: - V2TIMConversationListener【会话、未读数】

//: extension AfterThen: V2TIMConversationListener {
extension AfterThen: V2TIMConversationListener {
    /// 有新的会话
    //: public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
    public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.inviteCheckCorrigenda(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.willDoing(conVersationList: list)
    }

    /// 某些会话的关键信息发生变化
    //: public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
    public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.inviteCheckCorrigenda(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.willDoing(conVersationList: list)
    }

    /// 会话未读总数变更通知
    //: public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
    public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
        //: DismissReactiveCompatible.share.unreadMessageNum = Int(totalUnreadCount)
        DismissReactiveCompatible.share.unreadMessageNum = Int(totalUnreadCount)
        //: UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        //: self.func__invokeDelegate {
        self.skirt {
            //: $0.onUnreadMsgCountChanged?(count: Int(totalUnreadCount))
            $0.failure?(count: Int(totalUnreadCount))
        }
    }

    /// 批量更新会话列表
    //: func func__updateConversationList(conVersationList: [TalkingConversationModel]) {
    func willDoing(conVersationList: [IceTapThen]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            // 防止刚启动APP，消息列表还未初始化，先缓存数据，找合适时机批量请求
            //: if DBUserInfoManager.shared().needReqUidsSet != nil {
            if CutInfoManager.passage().needReqUidsSet != nil {
                // 提取非空 userId 并去重
                //: let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                //: DBUserInfoManager.shared().needReqUidsSet!.formUnion(set)
                CutInfoManager.passage().needReqUidsSet!.formUnion(set)
            }
            //: self.func__invokeDelegate {
            self.skirt {
                //: $0.onRefreshConversationList?(data: conVersationList)
                $0.heat?(data: conVersationList)
            }
        }
    }

    //: func func__handleTXConversationList(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func popularCompletion(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: var convList: [TalkingConversationModel] = []
        var convList: [IceTapThen] = []

        // V2TIMConversation->IceTapThen
        //: for conversation in listArr {
        for conversation in listArr {
            //: let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            //: var tempConvModel = self.convListCache[targetId!]
            var tempConvModel = self.convListCache[targetId!]
            //: if tempConvModel == nil {
            if tempConvModel == nil {
                //: tempConvModel = TalkingConversationModel(conv: conversation)
                tempConvModel = IceTapThen(conv: conversation)
            }
            //: tempConvModel?.func__updateLastConvModel(conv: conversation)
            tempConvModel?.behindOnly(conv: conversation)
            //: convList.append(tempConvModel!)
            convList.append(tempConvModel!)
        }

        // 增量
        //: var resultList: [TalkingConversationModel] = []
        var resultList: [IceTapThen] = []
        //: let dbModels = WCDBUserInfoTable.db_getUserInfos(with: Array(self.convListCache.keys))
        let dbModels = ConiferousTreeInfoTable.successfullyWith(with: Array(self.convListCache.keys))
        //: let dbIdSet = Set(dbModels.map { $0.uid })
        let dbIdSet = Set(dbModels.map { $0.uid })
        //: for conversation in convList {
        for conversation in convList {
            // 添加db中是否有数据判断，防止刚启动APP，Delegate方法还未实现
            //: if self.func__addConvList(convModel: conversation, insertTop: false) ||
            if self.signatureActivity(convModel: conversation, insertTop: false) ||
                //: (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
                (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
            {
                //: resultList.append(conversation)
                resultList.append(conversation)
            }
        }
        //: let leftMore = (resultList.count > 0)
        let leftMore = (resultList.count > 0)
        //: completion(resultList, leftMore)
        completion(resultList, leftMore)
    }
}

// MARK: - 会话排序、删除、置顶

//: extension AfterThen {
extension AfterThen {
    /// 获取消息列表数据
    /// - Parameter completion: 回调
    //: func func__getConversationList(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func confab(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
        V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
            //: self.nextSeq = nextSeq
            self.nextSeq = nextSeq
            //: self.func__handleTXConversationList(listArr: list!) { arr, leftMore in
            self.popularCompletion(listArr: list!) { arr, leftMore in
                //: self.func__updateConversationList(conVersationList: arr as? [TalkingConversationModel] ?? [])
                self.willDoing(conVersationList: arr as? [IceTapThen] ?? [])
                //: completion(arr, leftMore)
                completion(arr, leftMore)
            }
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM获取会话列表失败，code:\(code), desc:\(String(describing: desc)).")
            MateThen.trackBy(msg: (String(kResolutionData.suffix(6)) + "列\u{8868}失\u{8d25}，c" + String(show_cloudStr.prefix(4))) + "\(code)" + (String(user_alongsideName) + String(show_windMsg)) + "\(String(describing: desc)).")
        }
    }

    /// 从缓存会话中获取模型
    //: func func__getCacheConversationModel(targetId: String) -> TalkingConversationModel? {
    func request(targetId: String) -> IceTapThen? {
        //: return self.convListCache[targetId]
        return self.convListCache[targetId]
    }

    //: func func__handleConversationList(convList: [V2TIMConversation]) -> [TalkingConversationModel] {
    func inviteCheckCorrigenda(convList: [V2TIMConversation]) -> [IceTapThen] {
        //: var tempArr: [TalkingConversationModel] = []
        var tempArr: [IceTapThen] = []
        //: for conv in convList {
        for conv in convList {
            //: let targetId = conv.userID ?? conv.groupID
            let targetId = conv.userID ?? conv.groupID
            //: var convModel = self.convListCache[targetId ?? ""]
            var convModel = self.convListCache[targetId ?? ""]
            //: if convModel == nil {
            if convModel == nil {
                //: convModel = TalkingConversationModel(conv: conv)
                convModel = IceTapThen(conv: conv)
            }
            // 更新会话
            //: convModel?.func__updateLastConvModel(conv: conv)
            convModel?.behindOnly(conv: conv)
            //: if self.func__addConvList(convModel: convModel!, insertTop: true) {
            if self.signatureActivity(convModel: convModel!, insertTop: true) {
                //: tempArr.append(convModel!)
                tempArr.append(convModel!)
            }
        }
        //: return tempArr
        return tempArr
    }

    /// 会话列表新增会话
    /// - Parameters:
    ///   - convModel: 会话模型
    ///   - insertTop: 是否插入顶部
    /// - Returns: 是否添加成功
    //: func func__addConvList(convModel: TalkingConversationModel, insertTop: Bool) -> Bool {
    func signatureActivity(convModel: IceTapThen, insertTop: Bool) -> Bool {
        //: if convModel.chatType == .unDefined {
        if convModel.chatType == .unDefined {
            //: return false
            return false
        }
        //: if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }
        if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }

        //: let targetId = convModel.targetId
        let targetId = convModel.targetId
        // 系统消息，无需重新排序
        //: if convModel.chatType == .system || convModel.chatType == .service {
        if convModel.chatType == .system || convModel.chatType == .service {
            //: self.convListCache[targetId] = convModel
            self.convListCache[targetId] = convModel
            //: if self.officialList.count == 0 {
            if self.officialList.count == 0 {
                //: self.func__adddofficialList()
                self.voiceList()
            }
            //: if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
            if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
                //: let data = self.officialList[index]
                let data = self.officialList[index]
                //: convModel.showName = data.showName
                convModel.showName = data.showName
                //: self.officialList[index] = convModel
                self.officialList[index] = convModel
            }
            //: return true
            return true
        }

        // 普通消息
        //: var dataType: ChatListDataType = .normalList
        var dataType: FirDataType = .normalList
        //: var listArr = self.norConvList
        var listArr = self.norConvList
        //: let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - DismissReactiveCompatible.share.appConfigMode.moreMessageTimestamp
        let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - DismissReactiveCompatible.share.appConfigMode.moreMessageTimestamp
        // 是否建立会话
        //: var haveSeSSion = false
        var haveSeSSion = false
        //: if let userModel = DBUserInfoManager.cache_getCachedUserInfo(targetId: convModel.targetId) {
        if let userModel = CutInfoManager.frontId(targetId: convModel.targetId) {
            //: haveSeSSion = userModel.isHaveSession
            haveSeSSion = userModel.isHaveSession
        }
        // 是否需要折叠
        //: var needFold = false
        var needFold = false
        //: if let elemData = convModel.listShowMessage?.customElem.data {
        if let elemData = convModel.listShowMessage?.customElem.data {
            //: let dict = AcousticPowerThen.parseTXMessageData(data: elemData)
            let dict = AcousticPowerThen.leaveDoingice(data: elemData)
            //: let json = JSON(dict)
            let json = JSON(dict)
            //: needFold = json["extra"]["msgInfo"]["needFold"].boolValue
            needFold = json[(data_lipMsg.replacingOccurrences(of: "average", with: "t"))][(String(showReceiveFormat) + notiBraceStr.replacingOccurrences(of: "pure", with: "o"))][(String(userMovieText.suffix(8)))].boolValue
        }
        // 非置顶，非自己发送，超出时间 || 未建立会话，需要折叠
        //: if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
        if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
            //: (!haveSeSSion && needFold && convModel.isPinned == false)
            (!haveSeSSion && needFold && convModel.isPinned == false)
        {
            //: dataType = .moreList
            dataType = .moreList
            //: listArr = self.moreMsgList
            listArr = self.moreMsgList
            // 移除重复数据
            //: self.topConvList.removeAll { $0.userID == convModel.userID }
            self.topConvList.removeAll { $0.userID == convModel.userID }
            //: self.norConvList.removeAll { $0.userID == convModel.userID }
            self.norConvList.removeAll { $0.userID == convModel.userID }

            //: } else {
        } else {
            // 移除更多消息
            //: self.moreMsgList.removeAll { $0.userID == convModel.userID }
            self.moreMsgList.removeAll { $0.userID == convModel.userID }
            // 置顶消息
            //: if convModel.isPinned {
            if convModel.isPinned {
                //: dataType = .topList
                dataType = .topList
                //: listArr = self.topConvList
                listArr = self.topConvList
            }
        }

        //: let tempConvModel = self.convListCache[targetId]
        let tempConvModel = self.convListCache[targetId]
        //: if tempConvModel != nil {
        if tempConvModel != nil {
            //: if insertTop && listArr.first != tempConvModel {
            if insertTop && listArr.first != tempConvModel {
                //: if let index = listArr.firstIndex(of: tempConvModel!) {
                if let index = listArr.firstIndex(of: tempConvModel!) {
                    //: listArr.remove(at: index)
                    listArr.remove(at: index)
                }
                //: listArr.append(tempConvModel!)
                listArr.append(tempConvModel!)
                //: self.func__sortConVList(convList: listArr, dataType: dataType)
                self.note(convList: listArr, dataType: dataType)
                //: return true
                return true
            }
            //: return false
            return false
        }

        //: self.convListCache[targetId] = convModel
        self.convListCache[targetId] = convModel
        //: listArr.append(convModel)
        listArr.append(convModel)
        //: self.func__sortConVList(convList: listArr, dataType: dataType)
        self.note(convList: listArr, dataType: dataType)
        //: return true
        return true
    }

    /// 数据排序
    /// - Parameters:
    ///   - convList: 会话列表
    ///   - dataType: 数据类型
    //: func func__sortConVList(convList: [TalkingConversationModel], dataType: ChatListDataType) {
    func note(convList: [IceTapThen], dataType: FirDataType) {
        //: switch dataType {
        switch dataType {
        //: case .topList:
        case .topList:
            //: self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .normalList:
        case .normalList:
            //: self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .moreList:
        case .moreList:
            //: self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
            self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
        }
    }

    /// 删除会话
    /// - Parameters:
    ///   - targetId: 会话Id
    ///   - dataType: 会话类型
    //: func func__removeConversation(targetId: String, dataType: ChatListDataType) {
    func halogen(targetId: String, dataType: FirDataType) {
        //: guard !targetId.isEmpty else { return }
        guard !targetId.isEmpty else { return }
        //: guard let tempConvModel = self.convListCache[targetId] else { return }
        guard let tempConvModel = self.convListCache[targetId] else { return }
        //: let convID = (tempConvModel.chatType == .private ? "c2c_\(targetId)" : "group_\(targetId)")
        let convID = (tempConvModel.chatType == .private ? (String(data_attributeUrl)) + "\(targetId)" : (constDestroyPath.lowercased() + userLabMessage.replacingOccurrences(of: "automatically", with: "_")) + "\(targetId)")
        //: V2TIMManager.sharedInstance().deleteConversation(convID) {
        V2TIMManager.sharedInstance().deleteConversation(convID) {
            // 移除db
            //: DBUserInfoManager.db_removeRecord(with: targetId)
            CutInfoManager.year(with: targetId)
            //: WCDBVoiceMsgTable.db_deleteVoiceMsg(DismissReactiveCompatible.share.loginUserMode.userID, toUid: targetId)
            RedMsgTable.doinglyWeekly(DismissReactiveCompatible.share.loginUserMode.userID, toUid: targetId)

            //: self.convListCache.removeValue(forKey: targetId)
            self.convListCache.removeValue(forKey: targetId)
            //: switch dataType {
            switch dataType {
            //: case .topList:
            case .topList:
                //: if let index = self.topConvList.firstIndex(of: tempConvModel) {
                if let index = self.topConvList.firstIndex(of: tempConvModel) {
                    //: self.topConvList.remove(at: index)
                    self.topConvList.remove(at: index)
                }
            //: case .normalList:
            case .normalList:
                //: if let index = self.norConvList.firstIndex(of: tempConvModel) {
                if let index = self.norConvList.firstIndex(of: tempConvModel) {
                    //: self.norConvList.remove(at: index)
                    self.norConvList.remove(at: index)
                }
            //: case .moreList:
            case .moreList:
                //: if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                    //: self.moreMsgList.remove(at: index)
                    self.moreMsgList.remove(at: index)
                }
            }

            //: let unreadCount = tempConvModel.unreadCount
            let unreadCount = tempConvModel.unreadCount
            //: if unreadCount > 0 {
            if unreadCount > 0 {
                //: tempConvModel.unreadCount = 0
                tempConvModel.unreadCount = 0
                //: tempConvModel.isShowMsgUnread = false
                tempConvModel.isShowMsgUnread = false
            }
            //: self.func__getUnreadMsgCount()
            self.underBeginSufficient()

            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM删除会话失败，convID:\(convID), code:\(code), desc:\(String(describing: desc)).")
            MateThen.trackBy(msg: (String(notiBucketHoldTitle) + String(showAcquireName)) + "\(convID)" + (String(notiExtendFormat)) + "\(code)" + (String(user_alongsideName) + String(show_windMsg)) + "\(String(describing: desc)).")
        }
    }

    /// 置顶会话
    //: func func__setConversationTop(convModel: TalkingConversationModel, isTop: Bool) {
    func thenIncrease(convModel: IceTapThen, isTop: Bool) {
        //: V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
        V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM置顶会话失败，convId:\(convModel.conversationID), code:\(code), desc:\(String(describing: desc)).")
            MateThen.trackBy(msg: (String(showDomainName.suffix(6)) + "失\u{8d25}\u{ff0c}con" + String(app_sitData)) + "\(convModel.conversationID)" + (String(notiExtendFormat)) + "\(code)" + (String(user_alongsideName) + String(show_windMsg)) + "\(String(describing: desc)).")
        }
        //: convModel.isPinned = isTop
        convModel.isPinned = isTop
        //: if isTop {
        if isTop {
            //: self.topConvList.insert(convModel, at: 0)
            self.topConvList.insert(convModel, at: 0)
            //: if let index = self.norConvList.firstIndex(of: convModel) {
            if let index = self.norConvList.firstIndex(of: convModel) {
                //: self.norConvList.remove(at: index)
                self.norConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.topConvList, dataType: .topList)
            self.note(convList: self.topConvList, dataType: .topList)
            //: } else {
        } else {
            //: self.norConvList.insert(convModel, at: 0)
            self.norConvList.insert(convModel, at: 0)
            //: if let index = self.topConvList.firstIndex(of: convModel) {
            if let index = self.topConvList.firstIndex(of: convModel) {
                //: self.topConvList.remove(at: index)
                self.topConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.norConvList, dataType: .normalList)
            self.note(convList: self.norConvList, dataType: .normalList)
        }
    }
}

// MARK: - V2TIMSDKListener

//: extension AfterThen: V2TIMSDKListener {
extension AfterThen: V2TIMSDKListener {
    //: public func onConnecting() {
    public func onConnecting() {
        //: printLog(message: "SDK 正在连接到服务器")
        printLog(message: (String(dataHoweverDiscoursePath) + data_cuteId.capitalized))
    }

    //: public func onConnectSuccess() {
    public func onConnectSuccess() {
        //: printLog(message: "SDK 已经成功连接到服务器")
        printLog(message: (String(userIllegalPath.suffix(5)) + k_migrationId.replacingOccurrences(of: "show", with: "经") + String(dataBoldValue)))
    }

    //: public func onConnectFailed(_ code: Int32, err: String!) {
    public func onConnectFailed(_ code: Int32, err: String!) {
        //: UploadLogTool.writeLog(msg: "IM连接服务器失败，code:\(code), error:\(err!).")
        MateThen.trackBy(msg: (main_languageData + const_proName.capitalized + String(constOffPath.suffix(7))) + "\(code)" + (String(appPlaceFormat)) + "\(err!).")
    }

    //: public func onKickedOffline() {
    public func onKickedOffline() {
        //: UploadLogTool.writeLog(msg: "IM账号被挤下线。")
        MateThen.trackBy(msg: (String(appGalleryFormat.suffix(4)) + "被挤下线。"))
        // socket断开连接
        //: TalkingSocketManager.shared.closeWebSocket(userLoginOut: true)
        SantaLuciaFirSocketDelegate.shared.featureOut(userLoginOut: true)
        //: AfterThen.shared.isConnection = false
        AfterThen.shared.isConnection = false
        // 销毁上传busy状态定时器
        //: (TalkingApplication.shared as! TalkingApplication).destroy()
        (AdvertisementReactiveCompatible.shared as! AdvertisementReactiveCompatible).tickSh()

        //: let msg = "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password.".localized
        let msg = String(bytes: data_trainId.map{leastPersonalityMin(common: $0)}, encoding: .utf8)!.localized
        //: let config = ShowAlertConfig()
        let config = PortraitAlertConfig()
        //: if TalkingRequestAddrTool.share.interfaceLang == LangType.ar.rawValue || TalkingRequestAddrTool.share.interfaceLang == LangType.es.rawValue ||
        if ManufacturerThen.share.interfaceLang == ImpressionName.ar.rawValue || ManufacturerThen.share.interfaceLang == ImpressionName.es.rawValue ||
            //: TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue
            ManufacturerThen.share.interfaceLang == ImpressionName.pt.rawValue
        {
            //: config.width = 340
            config.width = 340
            //: config.rightFont = UIFont.pingfangMediumFont(fontSize: 13)
            config.rightFont = UIFont.pingfang(fontSize: 13)
        }

        //: TalkingAlertShow.alert()
        AllowanceReactiveCompatible.maximal()
        //: TalkingAlertShow.alert(title: "Offline Notification".localized, message: msg, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Re Login".localized, leftBlock: {
        AllowanceReactiveCompatible.maximal(title: (String(const_portfolioName.prefix(5)) + String(user_welcomeLibraryValue) + String(notiSecureId)).localized, message: msg, leftBtnTitle: (String(data_socialPath)).localized, rightBtnTitle: (String(k_dataCollectFormat)).localized, leftBlock: {
            // 销毁直播
            //: TalkingLiveManager.shared().live_releaseAllResource()
            ManagerPushListener.nowPanel().translation()
            // 调用退出登录接口
            //: TalkingLoginRequestTool.req_loginOut { t in
            FirRequestTool.allowFor { t in
                //: ProgressHUD.dismiss()
                InsertProgressHUD.detectDismiss()
                //: if t {
                if t {
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: kOnValue, object: nil, userInfo: nil)
                }
            }
            //: }, rightBlock: {
        }, rightBlock: {
            //: TalkingLoginRequestTool.func__requestReloginUserInfo { t in
            FirRequestTool.state { t in
                //: if t {
                if t {
                    //: AfterThen.shared.func__LogingIn { _ in
                    AfterThen.shared.offHauled { _ in
                        /// 直播弹幕重连
                        //: if TalkingLiveManager.shared().liveRoomModel.chatGroupId.count > 0 {
                        if ManagerPushListener.nowPanel().liveRoomModel.chatGroupId.count > 0 {
                            //: V2TIMManager.sharedInstance().joinGroup(TalkingLiveManager.shared().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            V2TIMManager.sharedInstance().joinGroup(ManagerPushListener.nowPanel().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            }
                        }
                    }
                    // socket重连
                    //: TalkingSocketManager.shared.updateWebSocket()
                    SantaLuciaFirSocketDelegate.shared.makeupPremium()
                }
            }
            //: }, config: config)
        }, config: config)
    }
}
