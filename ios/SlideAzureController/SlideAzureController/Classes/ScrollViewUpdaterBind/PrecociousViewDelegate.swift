
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let kAlterKey:[UInt8] = [0x41,0x46,0x41,0x4c,0x0,0x3b,0x47,0x3c,0x3d,0x4a,0x12,0x1,0xf8,0x40,0x39,0x4b,0xf8,0x46,0x47,0x4c,0xf8,0x3a,0x3d,0x3d,0x46,0xf8,0x41,0x45,0x48,0x44,0x3d,0x45,0x3d,0x46,0x4c,0x3d,0x3c]

fileprivate func lovelyTail(hunting num: UInt8) -> UInt8 {
    let value = Int(num) + 40
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "icon_home_position" :*/
fileprivate let showAmAlongMsg:String = "compare disc concentrationicon_"
fileprivate let const_controlName:String = "jumpsit"
fileprivate let noti_electronContent:[Character] = ["i","o","n"]

/*: "btn_video_minimize" :*/
fileprivate let const_formCharmKey:String = "btn_vicheck circle host"
fileprivate let showConversionContent:[Character] = ["m","i","z","e"]

/*: "btn_video_drop_nor" :*/
fileprivate let notiLoadReasonId:String = "hello wide entry alive highlightbtn_"
fileprivate let app_artUrl:String = "rop_norfrom discussion"

/*: "btn_video_call_nor" :*/
fileprivate let const_beamPrimaryBalanceMessage:[Character] = ["b","t","n","_","v","i","d","e","o","_","c","a","l"]
fileprivate let constGiftBottomName:[Character] = ["l","_","n","o","r"]

/*: "transform.scale" :*/
fileprivate let k_onceKeyValue:String = "ttermsn"
fileprivate let constGapFormat:String = ".scalesomething enable minimum"

/*: "animation" :*/
fileprivate let showBagUrl:String = "fashion"
fileprivate let main_includeContent:[Character] = ["n","i","m","a","t","i","o","n"]

/*: "icon_videocall_female_answer_bg" :*/
fileprivate let const_executeUrl:[UInt8] = [0x8f,0x85,0x89,0x88,0xb9,0x90,0x8f,0x82,0x83,0x89,0x85,0x87,0x8a,0x8a,0xb9,0x80,0x83,0x8b,0x87,0x8a,0x83,0xb9,0x87,0x88,0x95,0x91,0x83,0x94,0xb9,0x84,0x81]

/*: "icon_videocall_female_answer" :*/
fileprivate let constActualData:String = "icon_vsecret wide pin selection between"
fileprivate let kStoneMessage:String = "solar"
fileprivate let k_togetherPrettyMsg:String = "deosoon"
fileprivate let userGuidanceKey:String = "male_arevenue ask register proximate"

/*: "icon_calltalk_points" :*/
fileprivate let k_picName:String = "icon_chin cos"
fileprivate let const_pastStr:String = "talk_poinpin far hello para invitation"
fileprivate let constPostKey:String = "be"

/*: "get json error" :*/
fileprivate let constMyUnitKey:[Character] = ["g"]
fileprivate let kAddName:[Character] = ["e","t"," ","j","s","o","n"," ","e","r","r","o","r"]

/*: "btn_video_turn_nor" :*/
fileprivate let dataDatabaseId:[Character] = ["b","t","n","_","v","i","d"]
fileprivate let userBurstPath:String = "eo_tursubject time military successfully tick"

/*: "btn_video_turn_pre" :*/
fileprivate let mainFreshUrl:String = "off our genderbtn_"
fileprivate let app_validFormat:String = "cos table blueturn_pre"

/*: "btn_video_start_nor" :*/
fileprivate let dataMatchCertainLibraryValue:String = "shared duringbtn_"
fileprivate let appMerelyStr:String = "START"

/*: "btn_video_start_pre" :*/
fileprivate let main_happyKey:String = "bench chance subject weight observerbtn_vi"
fileprivate let data_strokeId:String = "contentrt"

/*: "icon_complaint" :*/
fileprivate let main_proposalPath:String = "representative wed quest layicon_c"
fileprivate let user_someoneExOptionData:String = "ompperformint"

/*: "showBullet" :*/
fileprivate let userLengthDeedData:String = "showBubot kingdom"
fileprivate let user_disableFormat:String = "shineet"

/*: "icon_videoCall_coin_nor" :*/
fileprivate let appMigrationPath:String = "nobodyon"
fileprivate let notiProvideData:String = "Calllibrary statistical no a fit"
fileprivate let noti_selectedName:String = "NOR"

/*: "btn_video_gift_nor" :*/
fileprivate let mainParkSecureName:String = "pending nothing restriction giving firebtn_"
fileprivate let mainSitKey:String = "reason beyond communication mmgift_no"
fileprivate let k_masterTitle:String = "harassment"

/*: "Say something...     " :*/
fileprivate let appReKey:String = "Say sline bear"
fileprivate let const_southwestData:String = "rendering"

/*: "waiting for the other party to accept the video invitation..." :*/
fileprivate let mainResultDetailUrl:[UInt8] = [0xac,0xba,0xb2,0xaf,0xb2,0xb5,0xbc,0xfb,0xbd,0xb4,0xa9,0xfb,0xaf,0xb3,0xbe,0xfb,0xb4,0xaf,0xb3,0xbe,0xa9,0xfb,0xab,0xba,0xa9,0xaf,0xa2,0xfb,0xaf,0xb4,0xfb,0xba,0xb8,0xb8,0xbe,0xab,0xaf,0xfb,0xaf,0xb3,0xbe,0xfb,0xad,0xb2,0xbf,0xbe,0xb4,0xfb,0xb2,0xb5,0xad,0xb2,0xaf,0xba,0xaf,0xb2,0xb4,0xb5,0xf5,0xf5,0xf5]

private func everyLast(hand num: UInt8) -> UInt8 {
    return num ^ 219
}

/*: "Invite you to a video call..." :*/
fileprivate let show_invitationContent:String = "chance daily bug broadcast shirtInvit"
fileprivate let noti_shrinkMsg:String = "bean dismissu to a"
fileprivate let userWindowDocId:String = "living height noticeeo ca"
fileprivate let main_cordTrackStr:String = "never"

/*: "yyyy-MM-dd" :*/
fileprivate let const_componentData:[Character] = ["y","y"]
fileprivate let kToText:String = "yy-MM-ddnature inside"

/*: "00:00" :*/
fileprivate let appNeckKey:String = "00:00"

/*: "%@ points/min" :*/
fileprivate let main_theoryStr:String = "never production bad template due%@ point"
fileprivate let show_formPath:[Character] = ["s","/","m","i","n"]

/*: "Camera must be on" :*/
fileprivate let k_numberStr:[Character] = ["C","a","m","e","r","a"," ","m","u"]
fileprivate let constLateName:String = "kind home food botst b"

/*: "msgId" :*/
fileprivate let mainBoundHappyValue:[UInt8] = [0x64,0x49,0x67,0x73,0x6d]

/*: "senderUid" :*/
fileprivate let k_maleFormat:[UInt8] = [0x50,0x42,0x4b,0x41,0x42,0x4f,0x32,0x46,0x41]

fileprivate func bassHand(above num: UInt8) -> UInt8 {
    let value = Int(num) - 221
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "toUid" :*/
fileprivate let mainEventMessage:[Character] = ["t","o","U","i","d"]

/*: "nickname" :*/
fileprivate let k_motivationStr:[UInt8] = [0xc9,0xce,0xc4,0xcc,0xc9,0xc6,0xca,0xc2]

private func movementVersion(process num: UInt8) -> UInt8 {
    return num ^ 167
}

/*: "content" :*/
fileprivate let showBeautyMessage:[UInt8] = [0x55,0x59,0x58,0x42,0x53,0x58,0x42]

private func chanceSucceed(create num: UInt8) -> UInt8 {
    return num ^ 54
}

/*: "isMySender" :*/
fileprivate let show_customData:[UInt8] = [0x1c,0x6,0x38,0xc,0x26,0x10,0x1b,0x11,0x10,0x7]

private func hostContact(additional num: UInt8) -> UInt8 {
    return num ^ 117
}

/*: "hasFace" :*/
fileprivate let data_missingPath:String = "hasFanature weekly"
fileprivate let noti_perceiveKey:String = "CE"

/*: "Automatically answer for you in %@s" :*/
fileprivate let constTeamUrl:[UInt8] = [0xda,0xee,0xef,0xf4,0xf6,0xfa,0xef,0xf2,0xf8,0xfa,0xf7,0xf7,0xe2,0xbb,0xfa,0xf5,0xe8,0xec,0xfe,0xe9,0xbb,0xfd,0xf4,0xe9,0xbb,0xe2,0xf4,0xee,0xbb,0xf2,0xf5,0xbb,0xbe,0xdb,0xe8]

private func foundWealthy(pop num: UInt8) -> UInt8 {
    return num ^ 155
}

/*: "giftId" :*/
fileprivate let const_goThreadId:String = "another detect generategiftId"

/*: "giftNum" :*/
fileprivate let dataStyleName:[Character] = ["g","i","f","t","N","u","m"]

/*: "from" :*/
fileprivate let constAdminName:String = "freachm"

/*: "videoCall" :*/
fileprivate let userAgreeKey:String = "videoCalgender production"
fileprivate let data_arrangementId:String = "research"

/*: "programSort" :*/
fileprivate let app_funTitle:String = "template translationprogramSo"
fileprivate let user_heartData:[Character] = ["r","t"]

/*: "pkgItemsetId" :*/
fileprivate let data_correctStr:String = "upper wealthy managerpkgIt"

/*: "msgInfo" :*/
fileprivate let constMotionLockMessage:String = "relief flow laboratorymsgInfo"

/*: "msgType" :*/
fileprivate let notiInstallFormat:String = "msgTypeseveral click"

/*: "gift" :*/
fileprivate let constHisId:String = "gblockt"

/*: "totalMfCoin" :*/
fileprivate let data_somewhereId:String = "disable crushtotalM"
fileprivate let kStrengthMessage:[Character] = ["f","C","o","i","n"]

/*: "cellData" :*/
fileprivate let notiProductionKey:[UInt8] = [0xf2,0xf4,0xfd,0xfd,0xd5,0xf0,0xe5,0xf0]

/*: "icon_close_nor" :*/
fileprivate let user_anotherUrl:[Character] = ["i","c","o","n","_","c","l","o","s","e","_"]
fileprivate let noti_withoutData:String = "noprosecutor"

/*: "FaceActionEvent_Interval_12_frame" :*/
fileprivate let main_unlessStr:[UInt8] = [0xc7,0xe0,0xe2,0xe4,0xc0,0xe2,0xf5,0xe8,0xee,0xef,0xc4,0xf7,0xe4,0xef,0xf5,0xde,0xc8,0xef,0xf5,0xe4,0xf3,0xf7,0xe0,0xed,0xde,0xb0,0xb3,0xde,0xe7,0xf3,0xe0,0xec,0xe4]

private func ticketLifetime(even num: UInt8) -> UInt8 {
    return num ^ 129
}

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  PrecociousViewDelegate.swift
//  AbroadTalking
//
//  Created by young on 2022/11/8.
//

//: import UIKit
import UIKit

//: protocol Talking1v1VideoInteractionViewDelegate: NSObjectProtocol {
protocol HiddenObjectProtocol: NSObjectProtocol {
    /// 发送方结束通话
    //: func interactionView_shouldEndCall()
    func sumerplay()

    /// 接听视频通话
    //: func interactionView_shouldAcceptCall()
    func browsing()

    /// 旋转摄像头
    /// - Parameter isFront: 是否前置（默认前置）
    //: func interactionView_shouldChangeCamera(isFront: Bool)
    func crunch(isFront: Bool)

    /// 禁用/开启摄像头
    /// - Parameter forbidden: 是否禁用摄像头
    //: func interactionView_shouldForbiddenCamera(forbidden: Bool)
    func camera(forbidden: Bool)

    /// 展示视频通话小窗口
    //: func interactionView_shouldDisplayMiniWindow()
    func recital()

    /// 投诉成功
    //: func interactionView_reportSucceed()
    func declaration()
}

//: class Talking1v1VideoInteractionView: UIView {
class PrecociousViewDelegate: UIView {
    //: let place = (ScreenWidth-actualWidth(w: 45)*5)/6
    let place = (main_logStr - actualWidth(w: 45) * 5) / 6
    //: weak var delegate: Talking1v1VideoInteractionViewDelegate?
    weak var delegate: HiddenObjectProtocol?
    //: private var chatModel = TalkingVideoChatModel()
    private var chatModel = IceChatModel() // 引用类型，整个视频通话共用同一个model
    //: private var waitTimer: Timer?
    private var waitTimer: Timer? // 等待接听动画定时器
    //: private var startTime: TimeInterval?
    private var startTime: TimeInterval? // 开始的时间戳记 since1970
    //: private var countTimer: Timer?
    private var countTimer: Timer? // 通话时间定时器
    //: private var freeCountTimer: Timer?
    private var freeCountTimer: Timer? // 免费通话剩余时间定时器
//    var isShowRecharge  = false                         // 是否展示充值页中
    //: var isShowInput  = false
    var isShowInput = false // 是否在聊天中
    //: private var autoAcceptTimer: DispatchSourceTimer?
    private var autoAcceptTimer: DispatchSourceTimer? // 自动接听定时器

    //: init(frame: CGRect, chatModel: TalkingVideoChatModel?) {
    init(frame: CGRect, chatModel: IceChatModel?) {
        //: super.init(frame: frame)
        super.init(frame: frame)
        //: setupSubviews()
        dimensionSubviews()
        //: setupSubViewsConstraint()
        joinConstraint()
        //: if chatModel != nil {
        if chatModel != nil {
            //: self.chatModel = chatModel!
            self.chatModel = chatModel!
            //: self.startTime = chatModel!.startTime
            self.startTime = chatModel!.startTime
        }
        //: showViewsViaStage()
        validStage()
        //: bindInteraction()
        reportCounteraction()
        //: initNotificationObserver()
        putIn()
        //: initCachEffectData()
        byproduct()
        //: isShowHangupBtn()
        mergeIcon()

        //: TalkingVideoDanmuManager.shared().delegate = self
        SparkDanmuManager.mouthSplitDown().delegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: kAlterKey.map{lovelyTail(hunting: $0)}, encoding: .utf8)!)
    }

    //: deinit {
    deinit {
        //: if waitTimer != nil {
        if waitTimer != nil {
            //: waitTimer?.invalidate()
            waitTimer?.invalidate()
            //: waitTimer = nil
            waitTimer = nil
        }
        //: if countTimer != nil {
        if countTimer != nil {
            //: countTimer?.invalidate()
            countTimer?.invalidate()
            //: countTimer = nil
            countTimer = nil
        }
        //: if freeCountTimer != nil {
        if freeCountTimer != nil {
            //: freeCountTimer?.invalidate()
            freeCountTimer?.invalidate()
            //: freeCountTimer = nil
            freeCountTimer = nil
        }
        //: cancelAutoAcceptTimer()
        recognise()
        //: player.stopPlay()
        player.storageReward()
        //: invalidateIdleTimer()
        activeTimer()
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    // MARK: - Lazy Load

    // 点击展示/隐藏交互按钮
    //: private lazy var touchHidenBtn: UIButton = {
    private lazy var touchHidenBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(touchHidenBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(markBubble), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 接收方头像背景
    //: private lazy var pairInfoBg: UIView = {
    private lazy var pairInfoBg: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = UIColor(white: 0, alpha: 0.4)
        v.backgroundColor = UIColor(white: 0, alpha: 0.4)
        //: v.layer.cornerRadius = actualWidth(w: 50)/2
        v.layer.cornerRadius = actualWidth(w: 50) / 2
        //: v.layer.masksToBounds = true
        v.layer.masksToBounds = true
        //: return v
        return v
        //: }()
    }()

    //: private lazy var locationBtn: TalkingButton = {
    private lazy var locationBtn: RangeReactiveCompatible = {
        //: let btn = TalkingButton()
        let btn = RangeReactiveCompatible()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_home_position"), for: .normal)
        btn.setImage(UIImage.locationName(name: (String(showAmAlongMsg.suffix(5)) + "home_p" + const_controlName.replacingOccurrences(of: "jump", with: "o") + String(noti_electronContent))), for: .normal)
        //: btn.spaceBetweenTitleAndImage = 2
        btn.spaceBetweenTitleAndImage = 2
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangRugularFont(fontSize: 12)
        btn.titleLabel?.font = UIFont.bonePlace(fontSize: 12)
        //: btn.contentHorizontalAlignment = LanguageManager.shared.direction == .rightToLeft ? .right:.left
        btn.contentHorizontalAlignment = RatingReactiveCompatible.shared.direction == .rightToLeft ? .right : .left
        //: return btn
        return btn
        //: }()
    }()

    // 接收方头像
    //: private lazy var pairPicV: UIImageView = {
    private lazy var pairPicV: UIImageView = {
        //: let v = UIImageView()
        let v = UIImageView()
        //: v.contentMode = .scaleAspectFill
        v.contentMode = .scaleAspectFill
        //: v.isUserInteractionEnabled = true
        v.isUserInteractionEnabled = true
        //: v.layer.cornerRadius = actualWidth(w: 40)/2
        v.layer.cornerRadius = actualWidth(w: 40) / 2
        //: v.layer.masksToBounds = true
        v.layer.masksToBounds = true
        //: v.layer.borderWidth = 1
        v.layer.borderWidth = 1
        //: v.layer.borderColor = UIColor.white.cgColor
        v.layer.borderColor = UIColor.white.cgColor
        //: return v
        return v
        //: }()
    }()

    // 接收方昵称
    //: private lazy var pairNameLab: UILabel = {
    private lazy var pairNameLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangMediumFont(fontSize: 17)
        lab.font = UIFont.pingfang(fontSize: 17)
        //: return lab
        return lab
        //: }()
    }()

    // 呼叫中描述
    //: private lazy var desLab: UILabel = {
    private lazy var desLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangRugularFont(fontSize: 17)
        lab.font = UIFont.bonePlace(fontSize: 17)
        //: lab.numberOfLines = 0
        lab.numberOfLines = 0
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: return lab
        return lab
        //: }()
    }()

    // 最小化按钮
    //: private lazy var minimizeBtn: UIButton = {
    private lazy var minimizeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_video_minimize"), for: .normal)
        btn.setImage(UIImage.locationName(name: (String(const_formCharmKey.prefix(6)) + "deo_mini" + String(showConversionContent))), for: .normal)
        //: btn.addTarget(self, action: #selector(minimizeButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(minimizeStepClick), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 挂断/取消 按钮
    //: private lazy var hangupBtn: UIButton = {
    private lazy var hangupBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_video_drop_nor"), for: .normal)
        btn.setImage(UIImage.locationName(name: (String(notiLoadReasonId.suffix(4)) + "video_d" + String(app_artUrl.prefix(7)))), for: .normal)
        //: btn.addTarget(self, action: #selector(hangupButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(hearOut), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 接听按钮
    //: private lazy var acceptBtn: UIButton = {
    private lazy var acceptBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_video_call_nor"), for: .normal)
        btn.setImage(UIImage.locationName(name: (String(const_beamPrimaryBalanceMessage) + String(constGiftBottomName))), for: .normal)
        //: btn.addTarget(self, action: #selector(acceptButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(customIn), for: .touchUpInside)

        // 添加关键帧动画
        //: let keyAnimation = CAKeyframeAnimation()
        let keyAnimation = CAKeyframeAnimation()
        //: keyAnimation.keyPath = "transform.scale"
        keyAnimation.keyPath = (k_onceKeyValue.replacingOccurrences(of: "terms", with: "ra") + "sform" + String(constGapFormat.prefix(6)))
        //: keyAnimation.values = [NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1)),
        keyAnimation.values = [NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1)),
                               //: NSValue(cgAffineTransform: CGAffineTransform(scaleX: actualWidth(w: 80/70), y: actualWidth(w: 80/70))),
                               NSValue(cgAffineTransform: CGAffineTransform(scaleX: actualWidth(w: 80 / 70), y: actualWidth(w: 80 / 70))),
                               //: NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1))]
                               NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1))]
        //: keyAnimation.duration = 1.2
        keyAnimation.duration = 1.2
        //: keyAnimation.repeatCount = MAXFLOAT
        keyAnimation.repeatCount = MAXFLOAT
        //: keyAnimation.isRemovedOnCompletion = false
        keyAnimation.isRemovedOnCompletion = false
        //: keyAnimation.fillMode = .both
        keyAnimation.fillMode = .both
        //: keyAnimation.calculationMode = .cubicPaced
        keyAnimation.calculationMode = .cubicPaced
        //: btn.layer.add(keyAnimation, forKey: "animation")
        btn.layer.add(keyAnimation, forKey: (showBagUrl.replacingOccurrences(of: "fashion", with: "a") + String(main_includeContent)))

        //: return btn
        return btn
        //: }()
    }()

    // 女性接听按钮
    //: private lazy var femaleAcceptBGBtn: UIButton = {
    private lazy var femaleAcceptBGBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setBackgroundImage(UIImage.BundleImageNamed(name: "icon_videocall_female_answer_bg"), for: .normal)
        btn.setBackgroundImage(UIImage.locationName(name: String(bytes: const_executeUrl.map{$0^230}, encoding: .utf8)!), for: .normal)
        // 添加关键帧动画
        //: let keyAnimation = CAKeyframeAnimation()
        let keyAnimation = CAKeyframeAnimation()
        //: keyAnimation.keyPath = "transform.scale"
        keyAnimation.keyPath = (k_onceKeyValue.replacingOccurrences(of: "terms", with: "ra") + "sform" + String(constGapFormat.prefix(6)))
        //: keyAnimation.values = [NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1)),
        keyAnimation.values = [NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1)),
                               //: NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1.02, y: 1.02)),
                               NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1.02, y: 1.02)),
                               //: NSValue(cgAffineTransform: CGAffineTransform(scaleX: 0.95, y: 0.95))]
                               NSValue(cgAffineTransform: CGAffineTransform(scaleX: 0.95, y: 0.95))]
        //: keyAnimation.duration = 0.6
        keyAnimation.duration = 0.6
        //: keyAnimation.repeatCount = MAXFLOAT
        keyAnimation.repeatCount = MAXFLOAT
        //: keyAnimation.isRemovedOnCompletion = false
        keyAnimation.isRemovedOnCompletion = false
        //: keyAnimation.fillMode = .both
        keyAnimation.fillMode = .both
        //: keyAnimation.calculationMode = .cubicPaced
        keyAnimation.calculationMode = .cubicPaced
        //: btn.layer.add(keyAnimation, forKey: "animation")
        btn.layer.add(keyAnimation, forKey: (showBagUrl.replacingOccurrences(of: "fashion", with: "a") + String(main_includeContent)))
        //: return btn
        return btn
        //: }()
    }()

    // 女性接听按钮
    //: private lazy var femaleAcceptBtn: UIButton = {
    private lazy var femaleAcceptBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_videocall_female_answer"), for: .normal)
        btn.setImage(UIImage.locationName(name: (String(constActualData.prefix(6)) + kStoneMessage.replacingOccurrences(of: "solar", with: "i") + k_togetherPrettyMsg.replacingOccurrences(of: "soon", with: "ca") + "ll_fe" + String(userGuidanceKey.prefix(6)) + "nswer")), for: .normal)
        //: btn.addTarget(self, action: #selector(acceptButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(customIn), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 女性接听积分提示
    //: private lazy var femaleAcceptTipsBtn: TalkingButton = {
    private lazy var femaleAcceptTipsBtn: RangeReactiveCompatible = {
        //: let btn = TalkingButton()
        let btn = RangeReactiveCompatible()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_calltalk_points"), for: .normal)
        btn.setImage(UIImage.locationName(name: (String(k_picName.prefix(5)) + "call" + String(const_pastStr.prefix(9)) + constPostKey.replacingOccurrences(of: "be", with: "ts"))), for: .normal)
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangRugularFont(fontSize: 15)
        btn.titleLabel?.font = UIFont.bonePlace(fontSize: 15)
        //: btn.isUserInteractionEnabled = false
        btn.isUserInteractionEnabled = false
        //: return btn
        return btn
        //: }()
    }()

    //: lazy var acceptPlayer: SVGAPlayer = {
    lazy var acceptPlayer: SVGAPlayer = {
        //: let player = SVGAPlayer.init()
        let player = SVGAPlayer()
        //: player.loops = 0
        player.loops = 0
        //: player.clearsAfterStop = false
        player.clearsAfterStop = false
        //: player.isUserInteractionEnabled = false
        player.isUserInteractionEnabled = false
        //: player.contentMode = .scaleAspectFill
        player.contentMode = .scaleAspectFill
        //: let url = SVGAEffectTool.default.getZipEffectPath(type: .CallTalk_Accept)
        let url = BlackEffectTool.default.recordingRevenue(type: .CallTalk_Accept)
        //: do {
        do {
            //: let data = try Data(contentsOf: url)
            let data = try Data(contentsOf: url)
            //: let parser = SVGAParser.init()
            let parser = SVGAParser()
            //: parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
            parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: player.videoItem = videoItem
                player.videoItem = videoItem
                //: player.startAnimation()
                player.startAnimation()
            }
            //: } catch {
        } catch {
            //: printLog(message: "get json error")
            printLog(message: (String(constMyUnitKey) + String(kAddName)))
        }
        //: if LanguageManager.shared.direction == .rightToLeft {
        if RatingReactiveCompatible.shared.direction == .rightToLeft {
            //: player.transform = CGAffineTransform(scaleX: -1, y: 1)
            player.transform = CGAffineTransform(scaleX: -1, y: 1)
        }
        //: return player
        return player
        //: }()
    }()

    // 旋转摄像头按钮
    //: private lazy var rotateCameraBtn: UIButton = {
    private lazy var rotateCameraBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_video_turn_nor"), for: .normal)
        btn.setImage(UIImage.locationName(name: (String(dataDatabaseId) + String(userBurstPath.prefix(6)) + "n_nor")), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_video_turn_pre"), for: .selected)
        btn.setImage(UIImage.locationName(name: (String(mainFreshUrl.suffix(4)) + "video_" + String(app_validFormat.suffix(8)))), for: .selected)
        //: btn.addTarget(self, action: #selector(rotateCameraButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(writtenJust), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 开启/关闭摄像头按钮
    //: private lazy var openCameraBtn: UIButton = {
    private lazy var openCameraBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_video_start_nor"), for: .normal)
        btn.setImage(UIImage.locationName(name: (String(dataMatchCertainLibraryValue.suffix(4)) + "video_" + appMerelyStr.lowercased() + "_nor")), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_video_start_pre"), for: .selected)
        btn.setImage(UIImage.locationName(name: (String(main_happyKey.suffix(6)) + "deo_s" + data_strokeId.replacingOccurrences(of: "content", with: "ta") + "_pre")), for: .selected)
        //: btn.addTarget(self, action: #selector(openCameraButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(moreClick), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 投诉按钮（男性）
    //: private lazy var reportBtn: UIButton = {
    private lazy var reportBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_complaint"), for: .normal)
        btn.setImage(UIImage.locationName(name: (String(main_proposalPath.suffix(6)) + user_someoneExOptionData.replacingOccurrences(of: "perform", with: "la"))), for: .normal)
        //: btn.addTarget(self, action: #selector(reportButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(betweenPosition), for: .touchUpInside)
        //: let data = JSON(DismissReactiveCompatible.share.appConfigMode.videoReport)
        let data = JSON(DismissReactiveCompatible.share.appConfigMode.videoReport)
        //: if data["showBullet"].boolValue == true {
        if data[(String(userLengthDeedData.prefix(6)) + user_disableFormat.replacingOccurrences(of: "shine", with: "ll"))].boolValue == true {
            //: btn.isEnabled = true
            btn.isEnabled = true
            //: } else {
        } else {
            //: btn.isEnabled = false
            btn.isEnabled = false
        }
        //: return btn
        return btn
        //: }()
    }()

    // 充值按钮
    //: private lazy var rechargeBtn: UIButton = {
    private lazy var rechargeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_videoCall_coin_nor"), for: .normal)
        btn.setImage(UIImage.locationName(name: (appMigrationPath.replacingOccurrences(of: "nobody", with: "ic") + "_video" + String(notiProvideData.prefix(4)) + "_coin_" + noti_selectedName.lowercased())), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_videoCall_coin_nor"), for: .selected)
        btn.setImage(UIImage.locationName(name: (appMigrationPath.replacingOccurrences(of: "nobody", with: "ic") + "_video" + String(notiProvideData.prefix(4)) + "_coin_" + noti_selectedName.lowercased())), for: .selected)
        //: btn.addTarget(self, action: #selector(rechargeBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(snarl), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 送礼按钮
    //: private lazy var sendGiftBtn: UIButton = {
    private lazy var sendGiftBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_video_gift_nor"), for: .normal)
        btn.setImage(UIImage.locationName(name: (String(mainParkSecureName.suffix(4)) + "video_" + String(mainSitKey.suffix(7)) + k_masterTitle.replacingOccurrences(of: "harassment", with: "r"))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_video_gift_nor"), for: .selected)
        btn.setImage(UIImage.locationName(name: (String(mainParkSecureName.suffix(4)) + "video_" + String(mainSitKey.suffix(7)) + k_masterTitle.replacingOccurrences(of: "harassment", with: "r"))), for: .selected)
        //: btn.addTarget(self, action: #selector(sendGiftButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(largesse), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var timeLab: UILabel = {
    private lazy var timeLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangMediumFont(fontSize: 18)
        lab.font = UIFont.pingfang(fontSize: 18)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var freeTimeView: TalkingFreeCallRechargeTipsView = {
    private lazy var freeTimeView: OthersWallTipsView = {
        //: let view = TalkingFreeCallRechargeTipsView()
        let view = OthersWallTipsView()
        //: view.backgroundColor = .clear
        view.backgroundColor = .clear
        //: view.touchRechargeBtnBlock = { [weak self] in
        view.touchRechargeBtnBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
//            self.isShowRecharge = true
        }
        //: return view
        return view
        //: }()
    }()

    //: lazy var giftView: TalkingChatGiftView = {
    lazy var giftView: YaDataSource = {
        //: let giftV = TalkingChatGiftView.init(style: .call)
        let giftV = YaDataSource(style: .call)
        //: giftV.frame = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight)
        giftV.frame = CGRect(x: 0, y: 0, width: main_logStr, height: main_nameData)
        //: return giftV
        return giftV
        //: }()
    }()

    //: lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    lazy var giftEffectView: TickGenerateView = {
        //: let effectView = TalkingPrivateChatAnimatView()
        let effectView = TickGenerateView()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var giftTrackView: TalkingGiftTrackView = {
    lazy var giftTrackView: RatingTrackView = {
        //: let trackView = TalkingGiftTrackView()
        let trackView = RatingTrackView()
        //: trackView.isUserInteractionEnabled = false
        trackView.isUserInteractionEnabled = false
        //: return trackView
        return trackView
        //: }()
    }()

    //: lazy var pointAnimView: TalkingPointAnimationView = {
    lazy var pointAnimView: MovementWritingAnimationView = {
        //: let pointView = TalkingPointAnimationView()
        let pointView = MovementWritingAnimationView()
        //: pointView.isUserInteractionEnabled = false
        pointView.isUserInteractionEnabled = false
        //: return pointView
        return pointView
        //: }()
    }()

    // 视频播放器
    //: private lazy var player: TalkingVideoPlayerManager = {
    private lazy var player: PackagingPlayListener = {
        //: let player = TalkingVideoPlayerManager()
        let player = PackagingPlayListener()
        //: player.setMute(bEnable: true)
        player.scheduleColumn(bEnable: true)
        //: return player
        return player
        //: }()
    }()

    // 视频封面
    //: private lazy var coverImg: UIImageView = {
    private lazy var coverImg: UIImageView = {
        //: let v = UIImageView()
        let v = UIImageView()
        //: v.contentMode = .scaleAspectFill
        v.contentMode = .scaleAspectFill
        //: v.isUserInteractionEnabled = true
        v.isUserInteractionEnabled = true
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()

    /// 1.7.2 聊天消息
    //: private lazy var chatBtn: UIButton = {
    private lazy var chatBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.backgroundColor = UIColor.init(white: 0, alpha: 0.25)
        btn.backgroundColor = UIColor(white: 0, alpha: 0.25)
        //: btn.layer.cornerRadius = 17
        btn.layer.cornerRadius = 17
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.setTitle("   "+"Say something...     ".localized, for: .normal)
        btn.setTitle("   " + (String(appReKey.prefix(5)) + "omet" + const_southwestData.replacingOccurrences(of: "render", with: "h") + "...     ").localized, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangRugularFont(fontSize: 14)
        btn.titleLabel?.font = UIFont.bonePlace(fontSize: 14)
        //: btn.setTitleColor(UIColor.init(white: 1, alpha: 0.8), for: .normal)
        btn.setTitleColor(UIColor(white: 1, alpha: 0.8), for: .normal)
        //: btn.contentHorizontalAlignment = LanguageManager.shared.direction == .rightToLeft ? .right:.left
        btn.contentHorizontalAlignment = RatingReactiveCompatible.shared.direction == .rightToLeft ? .right : .left
        //: btn.addTarget(self, action: #selector(commentBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(botSnap), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var danmuView: TalkingVideoDanmuMsgListTableView = {
    private lazy var danmuView: PortraitViewDelegate = {
        //: let view = TalkingVideoDanmuMsgListTableView.init()
        let view = PortraitViewDelegate()
        //: view.touchTableGesblock = { [weak self] in
        view.touchTableGesblock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.touchHidenBtnClick()
            self.markBubble()
        }
        //: return view
        return view
        //: }()
    }()

    //: private lazy var commInputView: TalkingVideoCallInputView = {
    private lazy var commInputView: MovementView = {
        //: let text = TalkingVideoCallInputView.init()
        let text = MovementView()
        //: text.delegate = self
        text.delegate = self
        //: text.isHidden = true
        text.isHidden = true
        //: return text
        return text
        //: }()
    }()

    // 自动接听文案描述（女性）
    //: private lazy var autoAcceptDesLab: UILabel = {
    private lazy var autoAcceptDesLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangRugularFont(fontSize: 17)
        lab.font = UIFont.bonePlace(fontSize: 17)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: return lab
        return lab
        //: }()
    }()
}

// MARK: - Event

//: extension Talking1v1VideoInteractionView {
extension PrecociousViewDelegate {
    /// 展示视图状态
    //: func showViewsViaStage() {
    func validStage() {
        //: switch chatModel.videoStage {
        switch chatModel.videoStage {
        //: case .Waiting:
        case .Waiting:
            //: desLab.text = "waiting for the other party to accept the video invitation...".localized
            desLab.text = String(bytes: mainResultDetailUrl.map{everyLast(hand: $0)}, encoding: .utf8)!.localized
            //: desLab.isHidden = false
            desLab.isHidden = false
            //: pairInfoBg.isHidden = false
            pairInfoBg.isHidden = false
            //: minimizeBtn.isHidden = true
            minimizeBtn.isHidden = true
            //: hangupBtn.isHidden = false
            hangupBtn.isHidden = false
            //: acceptBtn.isHidden = true
            acceptBtn.isHidden = true
            //: femaleAcceptBtn.isHidden = true
            femaleAcceptBtn.isHidden = true
            //: femaleAcceptTipsBtn.isHidden = true
            femaleAcceptTipsBtn.isHidden = true
            //: acceptPlayer.isHidden = true
            acceptPlayer.isHidden = true
            //: femaleAcceptBGBtn.isHidden = true
            femaleAcceptBGBtn.isHidden = true
            //: if DismissReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue {
            if DismissReactiveCompatible.share.loginUserMode.sex == FenceSequence.male.rawValue {
                //: rotateCameraBtn.isHidden = true
                rotateCameraBtn.isHidden = true
                //: openCameraBtn.isHidden = true
                openCameraBtn.isHidden = true
            }
            //: reportBtn.isHidden = true
            reportBtn.isHidden = true
            //: sendGiftBtn.isHidden = true
            sendGiftBtn.isHidden = true
            //: rechargeBtn.isHidden = true
            rechargeBtn.isHidden = true
            //: touchHidenBtn.isEnabled = false
            touchHidenBtn.isEnabled = false
            //: timeLab.isHidden = true
            timeLab.isHidden = true
            //: coverImg.isHidden = false
            coverImg.isHidden = false
            //: chatBtn.isHidden = true
            chatBtn.isHidden = true
            //: danmuView.isHidden = true
            danmuView.isHidden = true
            //: autoAcceptDesLab.isHidden = true
            autoAcceptDesLab.isHidden = true
        //: case .Recieved:
        case .Recieved:
            //: desLab.text = "Invite you to a video call...".localized
            desLab.text = (String(show_invitationContent.suffix(5)) + "e yo" + String(noti_shrinkMsg.suffix(6)) + " vid" + String(userWindowDocId.suffix(5)) + "ll.." + main_cordTrackStr.replacingOccurrences(of: "never", with: ".")).localized
            //: desLab.isHidden = false
            desLab.isHidden = false
            //: pairInfoBg.isHidden = false
            pairInfoBg.isHidden = false
            //: minimizeBtn.isHidden = true
            minimizeBtn.isHidden = true
            //: hangupBtn.isHidden = false
            hangupBtn.isHidden = false
            //: autoAcceptDesLab.isHidden = true
            autoAcceptDesLab.isHidden = true
            //: if DismissReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue {
            if DismissReactiveCompatible.share.loginUserMode.sex == FenceSequence.male.rawValue {
                //: acceptBtn.isHidden = false
                acceptBtn.isHidden = false
                //: femaleAcceptBtn.isHidden = true
                femaleAcceptBtn.isHidden = true
                //: femaleAcceptTipsBtn.isHidden = true
                femaleAcceptTipsBtn.isHidden = true
                //: acceptPlayer.isHidden = true
                acceptPlayer.isHidden = true
                //: femaleAcceptBGBtn.isHidden = true
                femaleAcceptBGBtn.isHidden = true
                //: rotateCameraBtn.isHidden = true
                rotateCameraBtn.isHidden = true
                //: openCameraBtn.isHidden = true
                openCameraBtn.isHidden = true
                //: } else {
            } else {
                //: acceptBtn.isHidden = true
                acceptBtn.isHidden = true
                //: femaleAcceptBtn.isHidden = false
                femaleAcceptBtn.isHidden = false
                //: femaleAcceptTipsBtn.isHidden = false
                femaleAcceptTipsBtn.isHidden = false
                //: femaleAcceptBGBtn.isHidden = false
                femaleAcceptBGBtn.isHidden = false
                //: let date = Defaults.object(forKey: TalkingCallShowTag)
                let date = data_partyText.object(forKey: constBackText)
                //: let today = NSDate.getTimeString(date: Date(), dateFormat: "yyyy-MM-dd")
                let today = NSDate.switchly(date: Date(), dateFormat: (String(const_componentData) + String(kToText.prefix(8))))
                //: if date == nil || (date as! String) != today {
                if date == nil || (date as! String) != today {
                    //: Defaults.set(today, forKey: TalkingCallShowTag)
                    data_partyText.set(today, forKey: constBackText)
                    //: acceptPlayer.isHidden = false
                    acceptPlayer.isHidden = false
                    //: } else {
                } else {
                    //: acceptPlayer.isHidden = true
                    acceptPlayer.isHidden = true
                }
                //: autoAcceptDesLab.isHidden = false
                autoAcceptDesLab.isHidden = false
                // 直播时自动接听
                //: if DismissReactiveCompatible.share.appConfigMode.autoAnswer == 2 &&
                if DismissReactiveCompatible.share.appConfigMode.autoAnswer == 2 &&
                    //: NEED_RESTORE_LIVE == true {
                    app_playCurrencyMsg == true
                {
                    //: showOrHidenAutoRecieved(autoAccept: true)
                    hiden(autoAccept: true)
                    //: } else {
                } else {
                    //: showOrHidenAutoRecieved()
                    hiden()
                }
            }
            //: reportBtn.isHidden = true
            reportBtn.isHidden = true
            //: sendGiftBtn.isHidden = true
            sendGiftBtn.isHidden = true
            //: rechargeBtn.isHidden = true
            rechargeBtn.isHidden = true
            //: touchHidenBtn.isEnabled = false
            touchHidenBtn.isEnabled = false
            //: timeLab.isHidden = true
            timeLab.isHidden = true
            //: coverImg.isHidden = false
            coverImg.isHidden = false
            //: chatBtn.isHidden = true
            chatBtn.isHidden = true
            //: danmuView.isHidden = true
            danmuView.isHidden = true
        //: case .Calling:
        case .Calling:
            //: desLab.text = nil
            desLab.text = nil
            //: desLab.isHidden = true
            desLab.isHidden = true
            //: pairInfoBg.isHidden = true
            pairInfoBg.isHidden = true
            //: minimizeBtn.isHidden = false
            minimizeBtn.isHidden = false
            //: hangupBtn.isHidden = false
            hangupBtn.isHidden = false
            //: acceptBtn.isHidden = true
            acceptBtn.isHidden = true
            //: femaleAcceptBtn.isHidden = true
            femaleAcceptBtn.isHidden = true
            //: femaleAcceptTipsBtn.isHidden = true
            femaleAcceptTipsBtn.isHidden = true
            //: acceptPlayer.isHidden = true
            acceptPlayer.isHidden = true
            //: femaleAcceptBGBtn.isHidden = true
            femaleAcceptBGBtn.isHidden = true
            //: rotateCameraBtn.isHidden = false
            rotateCameraBtn.isHidden = false
            //: openCameraBtn.isHidden = false
            openCameraBtn.isHidden = false
            //: reportBtn.isHidden = !reportBtn.isEnabled || DismissReactiveCompatible.share.loginUserMode.sex == Gender.female.rawValue
            reportBtn.isHidden = !reportBtn.isEnabled || DismissReactiveCompatible.share.loginUserMode.sex == FenceSequence.female.rawValue
            //: sendGiftBtn.isHidden = false
            sendGiftBtn.isHidden = false
            //: rechargeBtn.isHidden = false
            rechargeBtn.isHidden = false
            //: touchHidenBtn.isEnabled = true
            touchHidenBtn.isEnabled = true
            //: timeLab.isHidden = false
            timeLab.isHidden = false
            //: chatBtn.isHidden = false
            chatBtn.isHidden = false
            //: danmuView.isHidden = false
            danmuView.isHidden = false
            //: autoAcceptDesLab.isHidden = true
            autoAcceptDesLab.isHidden = true
            //: if startTime != nil {
            if startTime != nil {
                //: changeTalkTime()
                doingicial()
                //: } else {
            } else {
                //: timeLab.text = "00:00"
                timeLab.text = (appNeckKey.capitalized)
            }
            //: startTalkCount()
            smallSeek()
            //: touchHiddenTimer()
            makeExplain()
            //: if TalkingSocketManager.shared.isFreeCall {
            if SantaLuciaFirSocketDelegate.shared.isFreeCall {
                //: setFreeView()
                dedication()
                //: setFreeCountTimer()
                colorTimer()
            }
            //: if reportBtn.isHidden {
            if reportBtn.isHidden {
                //: sendGiftBtn.snp.remakeConstraints { make in
                sendGiftBtn.snp.remakeConstraints { make in
                    //: make.trailing.equalToSuperview().offset(-10)
                    make.trailing.equalToSuperview().offset(-10)
                    //: make.bottom.equalTo(reportBtn)
                    make.bottom.equalTo(reportBtn)
                    //: make.size.equalTo(reportBtn)
                    make.size.equalTo(reportBtn)
                }
            }
        }

        //: updateLayout()
        betweenLayout()
        //: afterGetInfo()
        agentInfo()
    }

    /// 刷新通话视图数据
    //: func afterGetInfo() {
    func agentInfo() {
        //: pairNameLab.text = chatModel.pairNickName
        pairNameLab.text = chatModel.pairNickName
        //: locationBtn.isHidden = chatModel.location.count <= 0
        locationBtn.isHidden = chatModel.location.count <= 0
        //: locationBtn.setTitle("\(chatModel.location)", for: .normal)
        locationBtn.setTitle("\(chatModel.location)", for: .normal)
        //: femaleAcceptTipsBtn.setTitle(" +" + "%@ points/min".localizedArguments(chatModel.points), for: .normal)
        femaleAcceptTipsBtn.setTitle(" +" + (String(main_theoryStr.suffix(8)) + String(show_formPath)).unitedStates(chatModel.points), for: .normal)
        //: let optionsInfo = [KingfisherOptionsInfoItem.processor(RoundCornerImageProcessor(cornerRadius: actualWidth(w: 20)))]
        let optionsInfo = [KingfisherOptionsInfoItem.processor(RoundCornerImageProcessor(cornerRadius: actualWidth(w: 20)))]
        //: pairPicV.setUrlImage(urlStr: chatModel.pairHeadPic, options: optionsInfo)
        pairPicV.loadPoiseFinishParkImageEye(urlStr: chatModel.pairHeadPic, options: optionsInfo)

        // 男性展示视频封面 【非通话中】
        //: if DismissReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue {
        if DismissReactiveCompatible.share.loginUserMode.sex == FenceSequence.male.rawValue {
            //: coverImg.isHidden = false
            coverImg.isHidden = false
            //: coverImg.setUrlImage(urlStr: chatModel.pairHeadPic)
            coverImg.bolt(urlStr: chatModel.pairHeadPic)
            //: if chatModel.videoStage != .Calling, chatModel.videoUrl.count > 0 {
            if chatModel.videoStage != .Calling, chatModel.videoUrl.count > 0 {
                //: player.delegate = self
                player.delegate = self
                //: player.playerWithUrlAndVideoView(url: chatModel.videoUrl, view: self)
                player.loseTrack(url: chatModel.videoUrl, view: self)
            }
        }
        //: if chatModel.videoStage == .Calling {
        if chatModel.videoStage == .Calling {
            //: coverImg.isHidden = true
            coverImg.isHidden = true
            //: player.stopPlay()
            player.storageReward()
            //: player.removeVideoWidget()
            player.since()
        }
    }

    /// 最小化按钮事件
    //: @objc private func minimizeButtonClick() {
    @objc private func minimizeStepClick() {
        //: delegate?.interactionView_shouldDisplayMiniWindow()
        delegate?.recital()
    }

    /// 添加视频通话画面窗口
    /// - Parameter view: 视频画面窗口
    //: func addWindowView(view: UIView) {
    func bindExecute(view: UIView) {
        //: insertSubview(view, belowSubview: pairPicV)
        insertSubview(view, belowSubview: pairPicV)
    }

    /// 挂断按钮事件
    //: @objc private func hangupButtonClick() {
    @objc private func hearOut() {
        //: delegate?.interactionView_shouldEndCall()
        delegate?.sumerplay()
    }

    /// 接听按钮事件
    //: @objc private func acceptButtonClick() {
    @objc private func customIn() {
        //: self.cancelAutoAcceptTimer()
        self.recognise()
        //: delegate?.interactionView_shouldAcceptCall()
        delegate?.browsing()
    }

    /// 旋转摄像头事件
    //: @objc private func rotateCameraButtonClick() {
    @objc private func writtenJust() {
        //: if openCameraBtn.isSelected == true {
        if openCameraBtn.isSelected == true { // 腾讯美颜 && 摄像头关闭时
            //: func__showStatusBarErrorMsg(showMsg: "Camera must be on".localized)
            enablece(showMsg: (String(k_numberStr) + String(constLateName.suffix(4)) + "e on").localized)
            //: return
            return
        }
        //: rotateCameraBtn.isSelected = !rotateCameraBtn.isSelected
        rotateCameraBtn.isSelected = !rotateCameraBtn.isSelected
        //: delegate?.interactionView_shouldChangeCamera(isFront: !rotateCameraBtn.isSelected)
        delegate?.crunch(isFront: !rotateCameraBtn.isSelected)
    }

    /// 是否禁用摄像头事件
    //: @objc private func openCameraButtonClick() {
    @objc private func moreClick() {
        //: openCameraBtn.isSelected = !openCameraBtn.isSelected
        openCameraBtn.isSelected = !openCameraBtn.isSelected
        //: delegate?.interactionView_shouldForbiddenCamera(forbidden: openCameraBtn.isSelected)
        delegate?.camera(forbidden: openCameraBtn.isSelected)
    }

    /// 举报按钮点击事件（男性）
    //: @objc private func reportButtonClick() {
    @objc private func betweenPosition() {
        //: let reportView = TalkingReportAlertView(frame: UIScreen.main.bounds, type: .reportVideo, rId: String(chatModel.logId))
        let reportView = PositionView(frame: UIScreen.main.bounds, type: .reportVideo, rId: String(chatModel.logId))
        //: reportView.showReportViewIn(view: nil)
        reportView.priceBlack(view: nil)
        //: reportView.reportHideBlock = { [weak self] succeed in
        reportView.reportHideBlock = { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            // 投诉成功
            //: self.reportBtn.isEnabled = false
            self.reportBtn.isEnabled = false
            //: self.reportBtn.isHidden = true
            self.reportBtn.isHidden = true
            //: self.delegate?.interactionView_reportSucceed()
            self.delegate?.declaration()
        }
    }

    /// 送礼按钮事件
    //: @objc private func sendGiftButtonClick() {
    @objc private func largesse() {
        //: func__sendGift()
        resOff()
    }

    /// 充值按钮事件
    //: @objc private func rechargeBtnClick() {
    @objc private func snarl() {
//        self.isShowRecharge = true
        //: PushThen.share.func__pushToHalfWebVC(webViewType: .RechargeHalfPage)
        PushThen.share.obligatoEvent(webViewType: .RechargeHalfPage)
    }

    //: @objc private func touchHidenBtnClick() {
    @objc private func markBubble() {
        //: changeInteractionShowState()
        changeShowAdministrativeDistrictAction()
        //: if isShowInput {
        if isShowInput {
            //: dismissClick()
            socialEventBridge()
        }
    }

    /// 点击展示/隐藏通话中的按钮
    //: @objc private func changeInteractionShowState() {
    @objc private func changeShowAdministrativeDistrictAction() {
        //: let isHidden = !rotateCameraBtn.isHidden
        let isHidden = !rotateCameraBtn.isHidden
        //: minimizeBtn.isHidden = isHidden
        minimizeBtn.isHidden = isHidden
        //: rotateCameraBtn.isHidden = isHidden
        rotateCameraBtn.isHidden = isHidden
        //: openCameraBtn.isHidden = isHidden
        openCameraBtn.isHidden = isHidden
        //: reportBtn.isHidden = (reportBtn.isEnabled ? isHidden:true) ||  DismissReactiveCompatible.share.loginUserMode.sex == Gender.female.rawValue
        reportBtn.isHidden = (reportBtn.isEnabled ? isHidden : true) || DismissReactiveCompatible.share.loginUserMode.sex == FenceSequence.female.rawValue
        //: sendGiftBtn.isHidden = isHidden
        sendGiftBtn.isHidden = isHidden
        //: rechargeBtn.isHidden = isHidden
        rechargeBtn.isHidden = isHidden
        //: hangupBtn.isHidden = isHidden
        hangupBtn.isHidden = isHidden
        //: timeLab.isHidden = isHidden
        timeLab.isHidden = isHidden
        //: if TalkingSocketManager.shared.isFreeCall {
        if SantaLuciaFirSocketDelegate.shared.isFreeCall {
            //: self.freeTimeView.isHidden = isHidden
            self.freeTimeView.isHidden = isHidden
        }
        //: if isHidden {
        if isHidden {
            //: invalidateIdleTimer()
            activeTimer()
            //: } else {
        } else {
            //: touchHiddenTimer()
            makeExplain()
        }
    }

    /// 去电，x秒后才显示挂断按钮
    //: private func isShowHangupBtn() {
    private func mergeIcon() {
        //: if chatModel.isCaller && chatModel.videoStage == .Waiting && DismissReactiveCompatible.share.appUserConfigMode.callCancelBtnDelayTime > 0 && DismissReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue {
        if chatModel.isCaller, chatModel.videoStage == .Waiting, DismissReactiveCompatible.share.appUserConfigMode.callCancelBtnDelayTime > 0, DismissReactiveCompatible.share.loginUserMode.sex == FenceSequence.male.rawValue {
            //: hangupBtn.isHidden = true
            hangupBtn.isHidden = true
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now()+Double(DismissReactiveCompatible.share.appUserConfigMode.callCancelBtnDelayTime)) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + Double(DismissReactiveCompatible.share.appUserConfigMode.callCancelBtnDelayTime)) {
                //: self.hangupBtn.isHidden = false
                self.hangupBtn.isHidden = false
            }
        }
    }

    /// 取消GCD自动接听定时器
    //: private func cancelAutoAcceptTimer() {
    private func recognise() {
        //: if autoAcceptTimer != nil {
        if autoAcceptTimer != nil {
            //: autoAcceptTimer?.cancel()
            autoAcceptTimer?.cancel()
            //: autoAcceptTimer = nil
            autoAcceptTimer = nil
        }
    }
}

// MARK: - SparkDanmuManager

//: extension Talking1v1VideoInteractionView: TalkingVideoDanmuManagerDelegate {
extension PrecociousViewDelegate: HiddenReactiveCompatible {
    //: func func__DanmuRecvNewModel(Msg: TalkingVideoCallDanmuModel) {
    func quantityerestAlongside(Msg: FirAimModelType) {
        //: danmuView.addNewMsg(msgModel: Msg)
        danmuView.cityInput(msgModel: Msg)
    }

    //: @objc private func commentBtnClick() {
    @objc private func botSnap() {
        /// 拉起弹幕评论
        //: self.bringSubviewToFront(commInputView)
        self.bringSubviewToFront(commInputView)
        //: commInputView.updatePlaceholder()
        commInputView.aboveMargin()
    }
}

// MARK: - 发表弹幕代理

//: extension Talking1v1VideoInteractionView: TalkingVideoCallInputViewDelegate {
extension PrecociousViewDelegate: ArmDismissViewDelegate {
    //: @objc func dismissClick() {
    @objc func socialEventBridge() {
        //: self.isShowInput = false
        self.isShowInput = false
//        commInputView.inputTextView.text = ""
        //: commInputView.resignkeyBoard()
        commInputView.resignkey()
    }

    //: func func__sendTextMsg(msgStr: String) {
    func referExample(msgStr: String) {
        //: self.isShowInput = false
        self.isShowInput = false
        //: sendToTextMsg(msgStr: msgStr)
        contrive(msgStr: msgStr)
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func gestureBottom(heightToBottom: CGFloat) {
        //: if heightToBottom > 0 {
        if heightToBottom > 0 {
            //: self.isShowInput = true
            self.isShowInput = true
        }
        //: commInputView.snp.updateConstraints { make in
        commInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self).offset(-heightToBottom)
            make.bottom.equalTo(self).offset(-heightToBottom)
        }
        //: danmuView.snp.updateConstraints { make in
        danmuView.snp.updateConstraints { make in
            //: let height = heightToBottom == 0 ? -10 : -(heightToBottom-chatBtn.height+commInputView.height-kDeviceSafeBottomHeight+10)
            let height = heightToBottom == 0 ? -10 : -(heightToBottom - chatBtn.height + commInputView.height - appInviteValue + 10)
            //: make.bottom.equalTo(chatBtn.snp.top).offset(height)
            make.bottom.equalTo(chatBtn.snp.top).offset(height)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func iterate(height: CGFloat) {
        //: commInputView.snp.updateConstraints { make in
        commInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }

        //: self.needsUpdateConstraints()
        self.needsUpdateConstraints()
        //: self.updateConstraintsIfNeeded()
        self.updateConstraintsIfNeeded()
        //: UIView.animate(withDuration: 0.3, animations: {
        UIView.animate(withDuration: 0.3, animations: {
            //: self.layoutIfNeeded()
            self.layoutIfNeeded()
            //: })
        })
    }

    /// 发送文本消息
    //: func sendToTextMsg(msgStr: String) {
    func contrive(msgStr: String) {
        //: TalkingVideoDanmuManager.uploadToTextMsg(logId: "\(chatModel.logId)", content: msgStr) { succeed, result, errorModel in
        SparkDanmuManager.modusVivendi(logId: "\(chatModel.logId)", content: msgStr) { succeed, result, _ in
            //: if succeed {
            if succeed {
                //: let json = JSON(result!)
                let json = JSON(result!)
                //: let msgId = json["msgId"].string
                let msgId = json[String(bytes: mainBoundHappyValue.reversed(), encoding: .utf8)!].string
                //: var toUid: String?
                var toUid: String?
                //: if self.chatModel.iUid == DismissReactiveCompatible.share.loginUserMode.userID {
                if self.chatModel.iUid == DismissReactiveCompatible.share.loginUserMode.userID {
                    //: toUid = self.chatModel.pairUid
                    toUid = self.chatModel.pairUid
                    //: } else {
                } else {
                    //: toUid = self.chatModel.iUid
                    toUid = self.chatModel.iUid
                }
                //: let data = ["msgId": msgId, "senderUid": DismissReactiveCompatible.share.loginUserMode.userID, "toUid": toUid, "nickname": DismissReactiveCompatible.share.loginUserMode.nickname, "content": msgStr, "isMySender": "1"]
                let data = [String(bytes: mainBoundHappyValue.reversed(), encoding: .utf8)!: msgId, String(bytes: k_maleFormat.map{bassHand(above: $0)}, encoding: .utf8)!: DismissReactiveCompatible.share.loginUserMode.userID, (String(mainEventMessage)): toUid, String(bytes: k_motivationStr.map{movementVersion(process: $0)}, encoding: .utf8)!: DismissReactiveCompatible.share.loginUserMode.nickname, String(bytes: showBeautyMessage.map{chanceSucceed(create: $0)}, encoding: .utf8)!: msgStr, String(bytes: show_customData.map{hostContact(additional: $0)}, encoding: .utf8)!: "1"]
                //: TalkingVideoDanmuManager.shared().onRecvDanmuNewMsg(msg: data as [String: Any])
                SparkDanmuManager.mouthSplitDown().danmuStrokeMsg(msg: data as [String: Any])
                //: self.rotateCameraBtn.isHidden = true
                self.rotateCameraBtn.isHidden = true
                //: self.changeInteractionShowState()
                self.changeShowAdministrativeDistrictAction()
                //: self.commInputView.inputTextView.attributedText = nil
                self.commInputView.inputTextView.attributedText = nil
            }
        }
    }
}

// MARK: - BilliardPlayerThen【视频播放器】

//: extension Talking1v1VideoInteractionView: TalkingVideoPlayerDelegate {
extension PrecociousViewDelegate: BilliardPlayerThen {
    //: func func__playerStatus(player: TalkingVideoPlayerManager, status: VideoPlayerStatus) {
    func yesterday(player _: PackagingPlayListener, status: RatingPlayerStatus) {
        //: if status == .Playing {
        if status == .Playing {
            //: self.player.setRenderMode(renderMode: .FILL_SCREEN)
            self.player.succeed(renderMode: .FILL_SCREEN)
            //: self.coverImg.isHidden = true
            self.coverImg.isHidden = true
        }
    }

    //: func func__playerDuration(player: TalkingVideoPlayerManager, duration: Int, currentTime: Int) {}
    func tillChannelTime(player _: PackagingPlayListener, duration _: Int, currentTime _: Int) {}

    //: func func__playerProgress(player: TalkingVideoPlayerManager, progress: CGFloat) {}
    func funcCompareProgress(player _: PackagingPlayListener, progress _: CGFloat) {}
}

// MARK: - 通知事件

//: extension Talking1v1VideoInteractionView {
extension PrecociousViewDelegate {
    /// 商汤美颜是否捕获到人脸通知（女性）
    //: @objc private func catchFaceResultNotification(notification: NSNotification) {
    @objc private func chooseTing(notification: NSNotification) {
        // 女性 && 等待接听
        //: guard DismissReactiveCompatible.share.loginUserMode.sex == Gender.female.rawValue else { return }
        guard DismissReactiveCompatible.share.loginUserMode.sex == FenceSequence.female.rawValue else { return }
        //: guard self.chatModel.videoStage == .Recieved else { return }
        guard self.chatModel.videoStage == .Recieved else { return }

        //: let userInfo = notification.userInfo as! [String: AnyObject]
        let userInfo = notification.userInfo as! [String: AnyObject]
        //: let hasFace = (userInfo["hasFace"] as! NSNumber).description.boolValue
        let hasFace = (userInfo[(String(data_missingPath.prefix(5)) + noti_perceiveKey.lowercased())] as! NSNumber).description.boolValue
        //: showOrHidenAutoRecieved(autoAccept: hasFace)
        hiden(autoAccept: hasFace)
    }

    /// 是否展示自动接听按钮
    /// - Parameter autoAccept: 是否自动接听（默认：false）
    //: private func showOrHidenAutoRecieved(autoAccept: Bool = false) {
    private func hiden(autoAccept: Bool = false) {
        //: var isActive = false
        var isActive = false

        //: if DismissReactiveCompatible.share.appConfigMode.autoAnswer == 1 {
        if DismissReactiveCompatible.share.appConfigMode.autoAnswer == 1 {
            //: let timeDifference = Date().timeIntervalSince1970 - (TalkingApplication.shared as! TalkingApplication).lastEventTime
            let timeDifference = Date().timeIntervalSince1970 - (AdvertisementReactiveCompatible.shared as! AdvertisementReactiveCompatible).lastEventTime
            //: if timeDifference < 5 {
            if timeDifference < 5 {
                //: isActive = true
                isActive = true
            }
        }

        //: DispatchQueue.main.async { [weak self] in
        DispatchQueue.main.async { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if autoAccept || isActive {
            if autoAccept || isActive { // 自动接听
                //: self.autoAcceptDesLab.isHidden = false
                self.autoAcceptDesLab.isHidden = false

                //: var countdown = DismissReactiveCompatible.share.appConfigMode.autoAnswerCountdownTime
                var countdown = DismissReactiveCompatible.share.appConfigMode.autoAnswerCountdownTime
                // 防止多次初始化
                //: guard self.autoAcceptTimer == nil else { return }
                guard self.autoAcceptTimer == nil else { return }
                //: self.autoAcceptTimer = DispatchSource.makeTimerSource(queue: DispatchQueue.global())
                self.autoAcceptTimer = DispatchSource.makeTimerSource(queue: DispatchQueue.global())
                //: self.autoAcceptTimer?.schedule(deadline: .now(), repeating: .seconds(1))
                self.autoAcceptTimer?.schedule(deadline: .now(), repeating: .seconds(1))
                //: self.autoAcceptTimer?.setEventHandler {
                self.autoAcceptTimer?.setEventHandler {
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: guard countdown > 0 else {
                        guard countdown > 0 else {
                            //: if self.chatModel.videoStage == .Recieved {
                            if self.chatModel.videoStage == .Recieved {
                                //: self.acceptButtonClick()
                                self.customIn()
                            }
                            //: return
                            return
                        }
                        //: self.autoAcceptDesLab.text = "Automatically answer for you in %@s".localizedArguments(countdown)
                        self.autoAcceptDesLab.text = String(bytes: constTeamUrl.map{foundWealthy(pop: $0)}, encoding: .utf8)!.unitedStates(countdown)
                        //: countdown -= 1
                        countdown -= 1
                    }
                }
                //: self.autoAcceptTimer?.resume()
                self.autoAcceptTimer?.resume()
            }
        }
    }

    /// 半屏充值页退下
    //: @objc private func changeRechargeStatus() {
    @objc private func decreaseShapeStatus() {}
}

// MARK: -

//: extension Talking1v1VideoInteractionView {
extension PrecociousViewDelegate {
    //: func showPointAnimaData(point: String) {
    func replacement(point: String) {
        //: addSubview(pointAnimView)
        addSubview(pointAnimView)
        //: pointAnimView.snp.remakeConstraints { make in
        pointAnimView.snp.remakeConstraints { make in
            //: make.center.equalToSuperview()
            make.center.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 100), height: actualWidth(w: 100)))
            make.size.equalTo(CGSize(width: actualWidth(w: 100), height: actualWidth(w: 100)))
        }
        //: pointAnimView.setPointData(point: point)
        pointAnimView.flowPrimary(point: point)
    }
}

// MARK: - 送礼

//: extension Talking1v1VideoInteractionView {
extension PrecociousViewDelegate {
    //: func func__sendGift() {
    func resOff() {
        //: TalkingChatGiftManager.share.func__sendGiftEvent {
        MotivationThen.share.tillTurn {
            //: self.func__showGiftChoiceView()
            self.suite()
        }
    }

    //: func func__showGiftChoiceView() {
    func suite() {
        //: giftView.updateGiftInfo(needReload: true, mf_coin: DismissReactiveCompatible.share.loginUserMode.mf_coin)
        giftView.path(needReload: true, mf_coin: DismissReactiveCompatible.share.loginUserMode.mf_coin)
        //: window?.addSubview(giftView)
        window?.addSubview(giftView)
        //: giftView.showView()
        giftView.score()
        //: giftView.sendActionBlock = { [weak self] (_ giftModel: TalkingRoomGiftModel, _ num: String) in
        giftView.sendActionBlock = { [weak self] (_ giftModel: TillGiftModel, _ num: String) in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.func__sendGiftMsg(giftId: giftModel.gid, giftNum: num, pkgItemsetId: giftModel.pkgItemsetId, programSort: nil)
            self.detailNeed(giftId: giftModel.gid, giftNum: num, pkgItemsetId: giftModel.pkgItemsetId, programSort: nil)
        }
    }

    //: func func_giftViewDismiss() {
    func platformDismiss() {
        //: giftView.dismissView()
        giftView.maneuver()
    }

    //: func func__sendGiftMsg(giftId: String, giftNum: String, pkgItemsetId: String?, programSort: String?) {
    func detailNeed(giftId: String, giftNum: String, pkgItemsetId: String?, programSort: String?) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            enablece(showMsg: main_textStr)
            //: return
            return
        }
        //: var params: [String: Any] = [:]
        var params: [String: Any] = [:]
        //: if chatModel.iUid == DismissReactiveCompatible.share.loginUserMode.userID {
        if chatModel.iUid == DismissReactiveCompatible.share.loginUserMode.userID {
            //: params["toUid"] = chatModel.pairUid
            params[(String(mainEventMessage))] = chatModel.pairUid
            //: } else {
        } else {
            //: params["toUid"] = chatModel.iUid
            params[(String(mainEventMessage))] = chatModel.iUid
        }
        //: params["giftId"] = giftId
        params[(String(const_goThreadId.suffix(6)))] = giftId
        //: params["giftNum"] = giftNum
        params[(String(dataStyleName))] = giftNum
        //: params["from"] = "videoCall"
        params[(constAdminName.replacingOccurrences(of: "each", with: "o"))] = (String(userAgreeKey.prefix(8)) + data_arrangementId.replacingOccurrences(of: "research", with: "l"))
        //: if programSort != nil {
        if programSort != nil {
            //: params["programSort"] = programSort
            params[(String(app_funTitle.suffix(9)) + String(user_heartData))] = programSort
        }
        //: if pkgItemsetId != nil {
        if pkgItemsetId != nil {
            //: params["pkgItemsetId"] = pkgItemsetId
            params[(String(data_correctStr.suffix(5)) + "emsetId")] = pkgItemsetId
        }

        //: TalkingChatRequestTool.req_SendGiftPrivete(param: params, completion: { succeed, result, errorModel in
        TamperRequestTool.underSendReply(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.error(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if pkgItemsetId != nil {
            if pkgItemsetId != nil {
                //: self.giftView.bags_removeGiftPackage(pkgItemsetId: pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.giftView.localRoad(pkgItemsetId: pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! [String: Any?])
            self.fill(extral: result as! [String: Any?])
            //: })
        })
    }

    //: func func__insertGiftMessageWithExtral(extral: [String: Any?]) {
    func fill(extral: [String: Any?]) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: [String: Any] = extralInfo["msgInfo"] as! [String: Any]
        var msgInfoDic: [String: Any] = extralInfo[(String(constMotionLockMessage.suffix(7)))] as! [String: Any]
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[(String(notiInstallFormat.prefix(7)))] = (constHisId.replacingOccurrences(of: "block", with: "if"))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(constMotionLockMessage.suffix(7)))] = msgInfoDic

        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any])
        toEnrichmentSoundSendGiftAugustaPast(giftDic: extral as [String: Any])
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((String(data_somewhereId.suffix(6)) + String(kStrengthMessage))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(String(data_somewhereId.suffix(6)) + String(kStrengthMessage))] as! NSNumber
            //: DismissReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            DismissReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: giftView.updateGiftInfo(needReload: false, mf_coin: DismissReactiveCompatible.share.loginUserMode.mf_coin)
        giftView.path(needReload: false, mf_coin: DismissReactiveCompatible.share.loginUserMode.mf_coin)

        //: var toUid = ""
        var toUid = ""
        //: if chatModel.iUid == DismissReactiveCompatible.share.loginUserMode.userID {
        if chatModel.iUid == DismissReactiveCompatible.share.loginUserMode.userID {
            //: toUid = chatModel.pairUid
            toUid = chatModel.pairUid
            //: } else {
        } else {
            //: toUid = chatModel.iUid
            toUid = chatModel.iUid
        }

        //: let cellData = TUISocialChatManager.insertTXMessage(withExtral: extralInfo, toUid: toUid, isSender: true, isTip: false)
        let cellData = TictacReactiveCompatible.terminalBeginEngraftDigitizerCipherCircleWith(withExtral: extralInfo, toUid: toUid, isSender: true, isTip: false)

        //: NotificationCenter.default.post(name: CHAT_ADD_CALL_RECORD_MSG_NOTIFICATION, object: self, userInfo: ["cellData": cellData])
        NotificationCenter.default.post(name: k_serverKey, object: self, userInfo: [String(bytes: notiProductionKey.map{$0^145}, encoding: .utf8)!: cellData])
    }

    //: func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool) {
    func error(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool) {
        //: if errorCode == 50203 {
        if errorCode == 50203 {
            //: guard DismissReactiveCompatible.share.loginUserMode.status != 1 else {
            guard DismissReactiveCompatible.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    enablece(showMsg: errorStr)
                }
                //: return
                return
            }
            //: PushThen.share.func__jumpToWebRecharge(clickEvent: clickSendingiftsButton, sufficient: false)
            PushThen.share.twoParams(clickEvent: showLimitTitle, sufficient: false)
            //: giftView.dismissView()
            giftView.maneuver()
            //: } else if errorCode == 10888 {
        } else if errorCode == 10888 {
            //: let view = TalkingLiveVipSubscribePopView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = DisenableThen(frame: CGRect(x: 0, y: 0, width: main_logStr, height: main_nameData))
            //: view.show()
            view.animaShow()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                enablece(showMsg: errorStr)
            }
        }
    }

    //: func initCachEffectData() {
    func byproduct() {
        //: var targetId = ""
        var targetId = ""
        //: if chatModel.iUid == DismissReactiveCompatible.share.loginUserMode.userID {
        if chatModel.iUid == DismissReactiveCompatible.share.loginUserMode.userID {
            //: targetId = chatModel.pairUid
            targetId = chatModel.pairUid
            //: } else {
        } else {
            //: targetId = chatModel.iUid
            targetId = chatModel.iUid
        }
        //: giftEffectView.initCachAnimatData(tagetID: targetId)
        giftEffectView.thenMakeupId(tagetID: targetId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: [String: Any]) {
    func toEnrichmentSoundSendGiftAugustaPast(giftDic: [String: Any]) {
        //: let giftModel = giftEffectView.changeGiftToModel(dict: giftDic) ?? TalkingGiftMsgAnimatModel()
        let giftModel = giftEffectView.variation(dict: giftDic) ?? RoughnessMeasurable()
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in MotivationThen.share.beyondGet() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [FenceTransformable]
        }
        //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel)
        giftTrackView.modelSource(model: giftModel)

        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.addressReloadDict(dict: giftDic)
    }

    //: func addGiftEffectModelArr(giftArr: [Any]) {
    func dealSuccess(giftArr: [Any]) {
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in MotivationThen.share.beyondGet() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [FenceTransformable]
        }
        //: for i in 0..<giftArr.count {
        for i in 0 ..< giftArr.count {
            //: let giftModel = giftArr[i]
            let giftModel = giftArr[i]
            //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel as! TalkingGiftMsgAnimatModel)
            giftTrackView.modelSource(model: giftModel as! RoughnessMeasurable)
        }

        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.framework(modelArr: giftArr)
    }
}

// MARK: - 通话计时器

//: extension Talking1v1VideoInteractionView {
extension PrecociousViewDelegate {
    /// 开始通话计时
    //: private func startTalkCount() {
    private func smallSeek() {
        //: guard countTimer == nil else { return }
        guard countTimer == nil else { return }

        //: if startTime == nil {
        if startTime == nil {
            //: let interval = Date().timeIntervalSince1970
            let interval = Date().timeIntervalSince1970
            //: startTime = interval
            startTime = interval
            //: chatModel.startTime = startTime
            chatModel.startTime = startTime
        }
        //: countTimer = Timer.scheduledTimer(withTimeInterval: 1, block: { [weak self] _ in
        countTimer = Timer.scheduledTimer(withTimeInterval: 1, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.changeTalkTime()
            self.doingicial()
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 免费视频剩余时间倒计时
    //: private func setFreeCountTimer() {
    private func colorTimer() {
        //: guard freeCountTimer == nil else { return }
        guard freeCountTimer == nil else { return }
        //: var index = DismissReactiveCompatible.share.loginUserMode.rechargePopCountdown
        var index = DismissReactiveCompatible.share.loginUserMode.rechargePopCountdown
        //: freeCountTimer = Timer.scheduledTimer(withTimeInterval: 1, block: { [weak self] _ in
        freeCountTimer = Timer.scheduledTimer(withTimeInterval: 1, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: index -= 1
            index -= 1
            //: self.freeTimeView.setTipsMessage(count: index)
            self.freeTimeView.invite(count: index)
            //: }, repeats: true)
        }, repeats: true)
    }

    // 展示通话时间
    //: private func changeTalkTime() {
    private func doingicial() {
        //: let duration = getCurrentTalkingTime()
        let duration = equalAlbumRound()
        //: let min = floor(duration/60)
        let min = floor(duration / 60)
        //: let sec = floor(duration.truncatingRemainder(dividingBy: 60))
        let sec = floor(duration.truncatingRemainder(dividingBy: 60))
        //: timeLab.text = String(format: "%02i:%02i", Int(min), Int(sec))
        timeLab.text = String(format: "%02i:%02i", Int(min), Int(sec))
    }

    /// 恢复通话，更新通话时间
    /// - Parameter startTime: 通话时间
    //: func updateStartTime(startTime: TimeInterval) {
    func bringCreate(startTime: TimeInterval) {
        //: self.startTime = startTime
        self.startTime = startTime
        //: changeTalkTime()
        doingicial()
    }

    /// 获取当前通话时间
    /// - Returns: 秒
    //: func getCurrentTalkingTime() -> Double {
    func equalAlbumRound() -> Double {
        //: guard chatModel.videoStage == .Calling else { return 0 }
        guard chatModel.videoStage == .Calling else { return 0 }
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let duration = floor(interval-(startTime ?? 0))
        let duration = floor(interval - (startTime ?? 0))
        //: return duration
        return duration
    }
}

// MARK: - 【通话中】触屏隐藏交互按钮逻辑

//: extension Talking1v1VideoInteractionView {
extension PrecociousViewDelegate {
    /// 触摸隐藏开启延时
    //: private func touchHiddenTimer() {
    private func makeExplain() {
        //: invalidateIdleTimer()
        activeTimer()
        //: perform(#selector(hideInteractionView), afterDelay: 10)
        perform(#selector(fullCloud), afterDelay: 10)
    }

    /// 销毁延迟
    //: private func invalidateIdleTimer() {
    private func activeTimer() {
        //: NSObject.cancelPreviousPerformRequests(withTarget: self, selector: #selector(hideInteractionView), object: nil)
        NSObject.cancelPreviousPerformRequests(withTarget: self, selector: #selector(fullCloud), object: nil)
    }

    /// 延时到时隐藏视图
    //: @objc private func hideInteractionView() {
    @objc private func fullCloud() {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.changeInteractionShowState()
            self.changeShowAdministrativeDistrictAction()
        }
    }
}

// MARK: - Layout

//: extension Talking1v1VideoInteractionView {
extension PrecociousViewDelegate {
    /// 添加视图
    //: private func setupSubviews() {
    private func dimensionSubviews() {
        //: addSubview(giftEffectView)
        addSubview(giftEffectView)
        //: insertSubview(giftTrackView, belowSubview: giftEffectView)
        insertSubview(giftTrackView, belowSubview: giftEffectView)
        //: addSubview(coverImg)
        addSubview(coverImg)
        //: addSubview(touchHidenBtn)
        addSubview(touchHidenBtn)
        //: addSubview(pairInfoBg)
        addSubview(pairInfoBg)
        //: pairInfoBg.addSubview(pairPicV)
        pairInfoBg.addSubview(pairPicV)
        //: pairInfoBg.addSubview(pairNameLab)
        pairInfoBg.addSubview(pairNameLab)
        //: pairInfoBg.addSubview(locationBtn)
        pairInfoBg.addSubview(locationBtn)
        //: addSubview(desLab)
        addSubview(desLab)
        //: addSubview(minimizeBtn)
        addSubview(minimizeBtn)
        //: addSubview(hangupBtn)
        addSubview(hangupBtn)
        //: addSubview(rotateCameraBtn)
        addSubview(rotateCameraBtn)
        //: addSubview(openCameraBtn)
        addSubview(openCameraBtn)
        //: addSubview(sendGiftBtn)
        addSubview(sendGiftBtn)
        //: addSubview(rechargeBtn)
        addSubview(rechargeBtn)
        //: addSubview(timeLab)
        addSubview(timeLab)
        //: addSubview(reportBtn)
        addSubview(reportBtn)
        //: addSubview(acceptBtn)
        addSubview(acceptBtn)
        //: addSubview(chatBtn)
        addSubview(chatBtn)
        //: addSubview(danmuView)
        addSubview(danmuView)
        //: addSubview(commInputView)
        addSubview(commInputView)
        //: addSubview(femaleAcceptBGBtn)
        addSubview(femaleAcceptBGBtn)
        //: addSubview(femaleAcceptBtn)
        addSubview(femaleAcceptBtn)
        //: femaleAcceptBtn.addSubview(femaleAcceptTipsBtn)
        femaleAcceptBtn.addSubview(femaleAcceptTipsBtn)
        //: addSubview(acceptPlayer)
        addSubview(acceptPlayer)
        //: addSubview(autoAcceptDesLab)
        addSubview(autoAcceptDesLab)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func joinConstraint() {
        //: giftEffectView.snp.makeConstraints { make in
        giftEffectView.snp.makeConstraints { make in
            //: make.leading.trailing.top.bottom.equalTo(self)
            make.leading.trailing.top.bottom.equalTo(self)
        }
        //: giftTrackView.snp.makeConstraints { make in
        giftTrackView.snp.makeConstraints { make in
            //: make.leading.equalTo(self)
            make.leading.equalTo(self)
            //: make.height.equalTo(180)
            make.height.equalTo(180)
            //: make.bottom.equalTo(-actualHeight(h: 220)-kDeviceSafeBottomHeight)
            make.bottom.equalTo(-actualHeight(h: 220) - appInviteValue)
        }
        //: coverImg.snp.makeConstraints { make in
        coverImg.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: touchHidenBtn.snp.makeConstraints { make in
        touchHidenBtn.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: pairInfoBg.snp.makeConstraints { make in
        pairInfoBg.snp.makeConstraints { make in
            //: make.top.equalTo(StatusBarHeight+16)
            make.top.equalTo(dataPointTitle + 16)
            //: make.leading.equalTo(15)
            make.leading.equalTo(15)
            //: make.size.equalTo(CGSize(width: actualWidth(w: 128), height: actualWidth(w: 50)))
            make.size.equalTo(CGSize(width: actualWidth(w: 128), height: actualWidth(w: 50)))
        }
        //: pairPicV.snp.makeConstraints { make in
        pairPicV.snp.makeConstraints { make in
            //: make.leading.equalTo(actualWidth(w: 5))
            make.leading.equalTo(actualWidth(w: 5))
            //: make.centerY.equalToSuperview()
            make.centerY.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 40), height: actualWidth(w: 40)))
            make.size.equalTo(CGSize(width: actualWidth(w: 40), height: actualWidth(w: 40)))
        }
        //: pairNameLab.snp.makeConstraints { make in
        pairNameLab.snp.makeConstraints { make in
            //: make.leading.equalTo(pairPicV.snp.trailing).offset(8)
            make.leading.equalTo(pairPicV.snp.trailing).offset(8)
            //: make.top.equalTo(7)
            make.top.equalTo(7)
            //: make.height.equalTo(17)
            make.height.equalTo(17)
            //: make.trailing.equalToSuperview().offset(-7)
            make.trailing.equalToSuperview().offset(-7)
        }
        //: locationBtn.snp.makeConstraints { make in
        locationBtn.snp.makeConstraints { make in
            //: make.leading.equalTo(pairNameLab)
            make.leading.equalTo(pairNameLab)
            //: make.top.equalTo(pairNameLab.snp.bottom).offset(4)
            make.top.equalTo(pairNameLab.snp.bottom).offset(4)
            //: make.trailing.equalToSuperview().offset(-10)
            make.trailing.equalToSuperview().offset(-10)
        }
        //: desLab.snp.makeConstraints { make in
        desLab.snp.makeConstraints { make in
            //: make.top.equalToSuperview().offset(StatusBarHeight+86)
            make.top.equalToSuperview().offset(dataPointTitle + 86)
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
        }
        //: minimizeBtn.snp.makeConstraints { make in
        minimizeBtn.snp.makeConstraints { make in
            //: make.top.equalTo(StatusBarHeight+10)
            make.top.equalTo(dataPointTitle + 10)
            //: make.leading.equalTo(10)
            make.leading.equalTo(10)
            //: make.width.height.equalTo(actualWidth(w: 42))
            make.width.height.equalTo(actualWidth(w: 42))
        }

        //: acceptBtn.snp.makeConstraints { make in
        acceptBtn.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.bottom.equalTo(hangupBtn)
            make.bottom.equalTo(hangupBtn)
            //: make.size.equalTo(hangupBtn)
            make.size.equalTo(hangupBtn)
        }
        //: femaleAcceptBtn.snp.makeConstraints { make in
        femaleAcceptBtn.snp.makeConstraints { make in
            //: make.edges.equalTo(hangupBtn)
            make.edges.equalTo(hangupBtn)
        }
        //: femaleAcceptTipsBtn.snp.makeConstraints { make in
        femaleAcceptTipsBtn.snp.makeConstraints { make in
            //: make.bottom.equalTo(-9)
            make.bottom.equalTo(-9)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }
        //: timeLab.snp.makeConstraints { make in
        timeLab.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.top.equalTo(StatusBarHeight+20)
            make.top.equalTo(dataPointTitle + 20)
        }
        //: autoAcceptDesLab.snp.makeConstraints { make in
        autoAcceptDesLab.snp.makeConstraints { make in
            //: make.center.width.equalToSuperview()
            make.center.width.equalToSuperview()
        }
        //: chatBtn.snp.makeConstraints { make in
        chatBtn.snp.makeConstraints { make in
            //: make.bottom.equalTo(-kDeviceSafeBottomHeight)
            make.bottom.equalTo(-appInviteValue)
            //: make.leading.equalTo(10)
            make.leading.equalTo(10)
            //: make.width.equalTo(actualWidth(w: 157))
            make.width.equalTo(actualWidth(w: 157))
            //: make.height.equalTo(actualWidth(w: 34))
            make.height.equalTo(actualWidth(w: 34))
        }
        //: danmuView.snp.makeConstraints { make in
        danmuView.snp.makeConstraints { make in
            //: make.bottom.equalTo(chatBtn.snp.top).offset(-10)
            make.bottom.equalTo(chatBtn.snp.top).offset(-10)
            //: make.leading.equalTo(10)
            make.leading.equalTo(10)
            //: make.height.equalTo(VideoMsgTableViewHeight)
            make.height.equalTo(userSumReplaceFormat)
            //: make.width.equalTo(VideoMsgTableViewWidth)
            make.width.equalTo(data_textValue)
        }
        //: reportBtn.snp.makeConstraints { make in
        reportBtn.snp.makeConstraints { make in
            //: make.trailing.equalToSuperview().offset(-10)
            make.trailing.equalToSuperview().offset(-10)
            //: make.centerY.equalTo(chatBtn)
            make.centerY.equalTo(chatBtn)
            //: make.size.equalTo(actualWidth(w: 33))
            make.size.equalTo(actualWidth(w: 33))
        }
        //: sendGiftBtn.snp.makeConstraints { make in
        sendGiftBtn.snp.makeConstraints { make in
            //: make.trailing.equalTo(reportBtn.snp.leading).offset(-10)
            make.trailing.equalTo(reportBtn.snp.leading).offset(-10)
            //: make.bottom.equalTo(reportBtn)
            make.bottom.equalTo(reportBtn)
            //: make.size.equalTo(reportBtn)
            make.size.equalTo(reportBtn)
        }
        //: rechargeBtn.snp.makeConstraints { make in
        rechargeBtn.snp.makeConstraints { make in
            //: make.trailing.equalTo(sendGiftBtn.snp.leading).offset(-10)
            make.trailing.equalTo(sendGiftBtn.snp.leading).offset(-10)
            //: make.bottom.equalTo(reportBtn)
            make.bottom.equalTo(reportBtn)
            //: make.size.equalTo(reportBtn)
            make.size.equalTo(reportBtn)
        }

        //: hangupBtn.snp.makeConstraints { make in
        hangupBtn.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.bottom.equalTo(-kDeviceSafeBottomHeight-161)
            make.bottom.equalTo(-appInviteValue - 161)
            //: make.size.equalTo(actualWidth(w: 70))
            make.size.equalTo(actualWidth(w: 70))
        }
        //: commInputView.snp.makeConstraints { make in
        commInputView.snp.makeConstraints { make in
            //: make.bottom.equalTo(self)
            make.bottom.equalTo(self)
            //: make.leading.trailing.equalTo(0)
            make.leading.trailing.equalTo(0)
            //: make.height.equalTo(actualWidth(w: 55))
            make.height.equalTo(actualWidth(w: 55))
        }
    }

    /// 更新约束
    //: private func updateLayout() {
    private func betweenLayout() {
        //: if chatModel.videoStage == .Recieved {
        if chatModel.videoStage == .Recieved {
            //: if DismissReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue {
            if DismissReactiveCompatible.share.loginUserMode.sex == FenceSequence.male.rawValue {
                //: hangupBtn.snp.remakeConstraints { make in
                hangupBtn.snp.remakeConstraints { make in
                    //: make.leading.equalTo(ScreenWidth/2-42-70)
                    make.leading.equalTo(main_logStr / 2 - 42 - 70)
                    //: make.bottom.equalTo(-kDeviceSafeBottomHeight-161)
                    make.bottom.equalTo(-appInviteValue - 161)
                    //: make.size.equalTo(70)
                    make.size.equalTo(70)
                }
                //: acceptBtn.snp.remakeConstraints { make in
                acceptBtn.snp.remakeConstraints { make in
                    //: make.leading.equalTo(ScreenWidth/2+42)
                    make.leading.equalTo(main_logStr / 2 + 42)
                    //: make.bottom.equalTo(hangupBtn)
                    make.bottom.equalTo(hangupBtn)
                    //: make.size.equalTo(hangupBtn)
                    make.size.equalTo(hangupBtn)
                }
                //: } else {
            } else {
                //: hangupBtn.snp.remakeConstraints { make in
                hangupBtn.snp.remakeConstraints { make in
                    //: make.leading.equalTo(34)
                    make.leading.equalTo(34)
                    //: make.bottom.equalTo(-kDeviceSafeBottomHeight-161)
                    make.bottom.equalTo(-appInviteValue - 161)
                    //: make.size.equalTo(70)
                    make.size.equalTo(70)
                }
                //: femaleAcceptBGBtn.snp.remakeConstraints { make in
                femaleAcceptBGBtn.snp.remakeConstraints { make in
                    //: make.trailing.equalTo(-33)
                    make.trailing.equalTo(-33)
                    //: make.bottom.equalTo(-kDeviceSafeBottomHeight-147)
                    make.bottom.equalTo(-appInviteValue - 147)
                    //: make.size.equalTo(CGSize.init(width: 208, height: 99))
                    make.size.equalTo(CGSize(width: 208, height: 99))
                }
                //: femaleAcceptBtn.snp.remakeConstraints { make in
                femaleAcceptBtn.snp.remakeConstraints { make in
                    //: make.centerX.equalTo(femaleAcceptBGBtn)
                    make.centerX.equalTo(femaleAcceptBGBtn)
                    //: make.bottom.equalTo(femaleAcceptBGBtn.snp.bottom).offset(-15)
                    make.bottom.equalTo(femaleAcceptBGBtn.snp.bottom).offset(-15)
                    //: make.size.equalTo(CGSize.init(width: 180, height: 70))
                    make.size.equalTo(CGSize(width: 180, height: 70))
                }
                //: acceptPlayer.snp.remakeConstraints { make in
                acceptPlayer.snp.remakeConstraints { make in
                    //: make.trailing.equalTo(-8)
                    make.trailing.equalTo(-8)
                    //: make.bottom.equalTo(femaleAcceptBtn.snp.bottom).offset(10)
                    make.bottom.equalTo(femaleAcceptBtn.snp.bottom).offset(10)
                    //: make.size.equalTo(85)
                    make.size.equalTo(85)
                }
                //: openCameraBtn.snp.makeConstraints { make in
                openCameraBtn.snp.makeConstraints { make in
                    //: make.leading.equalTo(ScreenWidth/2+12)
                    make.leading.equalTo(main_logStr / 2 + 12)
                    //: make.bottom.equalTo(-kDeviceSafeBottomHeight-15)
                    make.bottom.equalTo(-appInviteValue - 15)
                    //: make.size.equalTo(actualWidth(w: 45))
                    make.size.equalTo(actualWidth(w: 45))
                }
                //: rotateCameraBtn.snp.makeConstraints { make in
                rotateCameraBtn.snp.makeConstraints { make in
                    //: make.trailing.equalTo(openCameraBtn.snp.leading).offset(-25)
                    make.trailing.equalTo(openCameraBtn.snp.leading).offset(-25)
                    //: make.centerY.equalTo(openCameraBtn)
                    make.centerY.equalTo(openCameraBtn)
                    //: make.size.equalTo(openCameraBtn)
                    make.size.equalTo(openCameraBtn)
                }
            }
            //: } else if chatModel.videoStage == .Calling {
        } else if chatModel.videoStage == .Calling {
            //: hangupBtn.setImage(UIImage.BundleImageNamed(name: "icon_close_nor"), for: .normal)
            hangupBtn.setImage(UIImage.locationName(name: (String(user_anotherUrl) + noti_withoutData.replacingOccurrences(of: "prosecutor", with: "r"))), for: .normal)
            //: hangupBtn.snp.remakeConstraints { make in
            hangupBtn.snp.remakeConstraints { make in
                //: make.trailing.equalTo(rotateCameraBtn.snp.leading).offset(-10)
                make.trailing.equalTo(rotateCameraBtn.snp.leading).offset(-10)
                //: make.centerY.equalTo(openCameraBtn)
                make.centerY.equalTo(openCameraBtn)
                //: make.size.equalTo(openCameraBtn)
                make.size.equalTo(openCameraBtn)
            }
            //: openCameraBtn.snp.remakeConstraints { make in
            openCameraBtn.snp.remakeConstraints { make in
                //: make.top.equalTo(StatusBarHeight+actualHeight(h: 25)+20)
                make.top.equalTo(dataPointTitle + actualHeight(h: 25) + 20)
                //: make.trailing.equalToSuperview().offset(-20)
                make.trailing.equalToSuperview().offset(-20)
                //: make.size.equalTo(actualWidth(w: 33))
                make.size.equalTo(actualWidth(w: 33))
            }
            //: rotateCameraBtn.snp.remakeConstraints { make in
            rotateCameraBtn.snp.remakeConstraints { make in
                //: make.trailing.equalTo(openCameraBtn.snp.leading).offset(-10)
                make.trailing.equalTo(openCameraBtn.snp.leading).offset(-10)
                //: make.centerY.equalTo(openCameraBtn)
                make.centerY.equalTo(openCameraBtn)
                //: make.size.equalTo(openCameraBtn)
                make.size.equalTo(openCameraBtn)
            }
            //: } else {
        } else {
            //: hangupBtn.snp.makeConstraints { make in
            hangupBtn.snp.makeConstraints { make in
                //: make.centerX.equalToSuperview()
                make.centerX.equalToSuperview()
                //: make.bottom.equalTo(-kDeviceSafeBottomHeight-161)
                make.bottom.equalTo(-appInviteValue - 161)
                //: make.size.equalTo(actualWidth(w: 70))
                make.size.equalTo(actualWidth(w: 70))
            }
            //: openCameraBtn.snp.makeConstraints { make in
            openCameraBtn.snp.makeConstraints { make in
                //: make.leading.equalTo(ScreenWidth/2+12)
                make.leading.equalTo(main_logStr / 2 + 12)
                //: make.bottom.equalTo(-kDeviceSafeBottomHeight-15)
                make.bottom.equalTo(-appInviteValue - 15)
                //: make.size.equalTo(actualWidth(w: 45))
                make.size.equalTo(actualWidth(w: 45))
            }
            //: rotateCameraBtn.snp.makeConstraints { make in
            rotateCameraBtn.snp.makeConstraints { make in
                //: make.trailing.equalTo(openCameraBtn.snp.leading).offset(-25)
                make.trailing.equalTo(openCameraBtn.snp.leading).offset(-25)
                //: make.centerY.equalTo(openCameraBtn)
                make.centerY.equalTo(openCameraBtn)
                //: make.size.equalTo(openCameraBtn)
                make.size.equalTo(openCameraBtn)
            }
        }
    }

    /// 添加事件
    //: private func bindInteraction() {
    private func reportCounteraction() {
        // 视频等待...动画
        //: waitTimer = Timer.scheduledTimer(withTimeInterval: 0.5, block: { [weak self] _ in
        waitTimer = Timer.scheduledTimer(withTimeInterval: 0.5, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard var text = self.desLab.text else { return }
            guard var text = self.desLab.text else { return }

            //: if text.hasSuffix("...") {
            if text.hasSuffix("...") {
                //: text = text.replacingOccurrences(of: "...", with: ".")
                text = text.replacingOccurrences(of: "...", with: ".")
                //: } else if text.hasSuffix("..") {
            } else if text.hasSuffix("..") {
                //: text = text.replacingOccurrences(of: "..", with: "...")
                text = text.replacingOccurrences(of: "..", with: "...")
                //: } else {
            } else {
                //: text = text.replacingOccurrences(of: ".", with: "..")
                text = text.replacingOccurrences(of: ".", with: "..")
            }
            //: self.desLab.text = text
            self.desLab.text = text

            //: }, repeats: true)
        }, repeats: true)
        //: waitTimer?.fire()
        waitTimer?.fire()
    }

    //: private func setFreeView() {
    private func dedication() {
        //: addSubview(freeTimeView)
        addSubview(freeTimeView)
        //: freeTimeView.snp.remakeConstraints { make in
        freeTimeView.snp.remakeConstraints { make in
            //: make.trailing.equalToSuperview()
            make.trailing.equalToSuperview()
            //: make.top.equalTo(timeLab.snp.bottom).offset(actualHeight(h: 299))
            make.top.equalTo(timeLab.snp.bottom).offset(actualHeight(h: 299))
            //: make.height.equalTo(111)
            make.height.equalTo(111)
            //: make.width.equalTo(126)
            make.width.equalTo(126)
        }
    }

    /// 添加通知
    //: private func initNotificationObserver() {
    private func putIn() {
        // 女性用户 && 使用商汤 && 人脸检测自动接听
        //: if DismissReactiveCompatible.share.loginUserMode.sex == Gender.female.rawValue &&
        if DismissReactiveCompatible.share.loginUserMode.sex == FenceSequence.female.rawValue,
           //: SenseTime_Use == true &&
           main_dismissMessage == true,
           //: DismissReactiveCompatible.share.appConfigMode.autoAnswer == 1 {
           DismissReactiveCompatible.share.appConfigMode.autoAnswer == 1
        {
            /// 监听 是否捕获到人脸
            //: NotificationCenter.default.addObserver(self,
            NotificationCenter.default.addObserver(self,
                                                   //: selector: #selector(catchFaceResultNotification(notification:)),
                                                   selector: #selector(chooseTing(notification:)),
                                                   //: name: NSNotification.Name(rawValue: "FaceActionEvent_Interval_12_frame"),
                                                   name: NSNotification.Name(rawValue: String(bytes: main_unlessStr.map{ticketLifetime(even: $0)}, encoding: .utf8)!),
                                                   //: object: nil)
                                                   object: nil)
        }

        //: NotificationCenter.default.addObserver(self, selector: #selector(changeRechargeStatus), name: RECHARGEHALFPAGE_END_CLOSE_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(decreaseShapeStatus), name: app_busyTitle, object: nil)
    }
}
