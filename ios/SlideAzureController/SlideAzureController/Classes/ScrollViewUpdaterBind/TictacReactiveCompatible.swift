
//: Declare String Begin

/*: "msgType" :*/
fileprivate let notiManualTradeStr:[Character] = ["m"]
fileprivate let main_closeRetainUrl:String = "sufficient a ad push floorsgType"

/*: "tips" :*/
fileprivate let k_othersId:String = "tipsignature"

/*: "extra" :*/
fileprivate let appDistrictContent:[Character] = ["e","x","t","r","a"]

/*: "msgInfo" :*/
fileprivate let userOralKey:[Character] = ["m","s"]
fileprivate let kCloudEnoughHalfKey:[Character] = ["g","I","n","f","o"]

/*: "user" :*/
fileprivate let user_environmentKey:String = "towarder"

/*: "uid" :*/
fileprivate let main_iconValue:String = "uialive"

/*: "nickname" :*/
fileprivate let userFromReferUrl:[UInt8] = [0x66,0x61,0x6b,0x63,0x66,0x69,0x65,0x6d]

private func cameraAlbum(visible num: UInt8) -> UInt8 {
    return num ^ 8
}

/*: "headPic" :*/
fileprivate let main_pageId:[UInt8] = [0x65,0x68,0x6c,0x69,0x5d,0x64,0x6e]

private func alterBottom(succeed num: UInt8) -> UInt8 {
    return num ^ 13
}

/*: "age" :*/
fileprivate let main_beamData:[UInt8] = [0x65,0x67,0x61]

/*: "gift" :*/
fileprivate let noti_scenarioMsg:String = "gilibraryt"

/*: "isGroup" :*/
fileprivate let const_upMaleMessage:String = "ibirth"
fileprivate let dataSharedSelectionName:[Character] = ["G","r","o","u","p"]

/*: "WantGift" :*/
fileprivate let user_picText:[Character] = ["W","a","n"]
fileprivate let showBanTitle:[Character] = ["t","G","i","f","t"]

/*: "txt" :*/
fileprivate let showPartyBirdText:String = "tlive"

/*: "img" :*/
fileprivate let notiDoingdStr:String = "IMG"

/*: "video" :*/
fileprivate let show_groupFormat:String = "VIDEO"

/*: "audio" :*/
fileprivate let noti_nutWithinMagnitudeerestedFormat:[Character] = ["a","u","d","i","o"]

/*: "messageType" :*/
fileprivate let kCanName:String = "presentationess"
fileprivate let userCustomerStr:[Character] = ["a","g","e","T","y","p","e"]

/*: "contentType" :*/
fileprivate let constGoldReferUrl:String = "crevenuent"

/*: "ImgMsg" :*/
fileprivate let constExpectedUrl:[Character] = ["I","m","g","M","s"]
fileprivate let const_burstStr:[Character] = ["g"]

/*: "imageUri" :*/
fileprivate let notiUnitValue:[Character] = ["i","m","a","g","e","U","r","i"]

/*: "type" :*/
fileprivate let noti_sMsg:String = "typfit"

/*: "pic" :*/
fileprivate let k_foreheadMsg:String = "pishade"

/*: "isBurn" :*/
fileprivate let appHostText:String = "ya each mouth public coldisBurn"

/*: "giftId" :*/
fileprivate let user_meetingLeavePath:String = "option sufficient narrowgiftId"

/*: "params" :*/
fileprivate let appFirstWhoStr:[Character] = ["p","a","r","a","m","s"]

/*: "audioLength" :*/
fileprivate let noti_middleDitBadValue:String = "autimingio"
fileprivate let kAdjustId:[Character] = ["L","e","n","g","t","h"]

/*: "len" :*/
fileprivate let constChannelFilterMessage:[Character] = ["l","e","n"]

/*: "audioSandbox" :*/
fileprivate let app_maximumId:String = "AUDI"
fileprivate let notiQuitMessage:[Character] = ["o","S","a","n","d","b","o","x"]

/*: "senderId" :*/
fileprivate let data_justName:String = "senderIburst environment clip"
fileprivate let k_attachMessage:[Character] = ["d"]

/*: "AudioMsg" :*/
fileprivate let show_betterPath:String = "AudioMsgcost forward action script wealthy"

/*: "content" :*/
fileprivate let dataTrustUrl:String = "conappenapp"

/*: "TxtMsg" :*/
fileprivate let const_mortalUrl:String = "swing var op shift showTxtMsg"

/*: "message" :*/
fileprivate let mainStreetUrl:String = "mnotesage"

/*: "quoteMsgId" :*/
fileprivate let user_shadowMessage:String = "quosuite"
fileprivate let userHoweverFormat:String = "channel"

/*: "quoteMsgInfo" :*/
fileprivate let appStepStr:String = "qone"
fileprivate let show_conductValue:[Character] = ["o","t","e","M","s","g","I","n","f","o"]

/*: "replayId" :*/
fileprivate let showPitchAcceptMsg:String = "communicatepla"
fileprivate let dataReadingName:String = "object restore composition lieyId"

/*: "rYMsgType" :*/
fileprivate let notiRunKey:String = "rYMsgtheme little cut revenue direction"

/*: "GJ:Sess:AutoGreet:TxtCustom" :*/
fileprivate let dataForceValue:String = "beat personal yellow come earnGJ:Se"
fileprivate let dataKingdomUrl:String = "suspend team nobody clock recordtoGre"
fileprivate let user_soccerId:String = "tCcollection appear life serve should"
fileprivate let userForeheadMsg:String = "runstom"

/*: "changeDirection" :*/
fileprivate let showDesignData:String = "clengthang"
fileprivate let data_questUrl:String = "flowion"

/*: "system" :*/
fileprivate let app_outputFemaleData:[Character] = ["s"]
fileprivate let app_aidFormat:String = "ystscale"

/*: "title" :*/
fileprivate let constPressFormat:[Character] = ["t","i","t","l","e"]

/*: "icon" :*/
fileprivate let app_greetWeValue:[Character] = ["i","c","o","n"]

/*: "all" :*/
fileprivate let userBeastMessage:String = "bridgell"

/*: "icon_chats_pcr" :*/
fileprivate let notiExceptionStr:String = "saveo"
fileprivate let mainOccurMsg:String = "middle minute strengthn_chat"

/*: "status" :*/
fileprivate let notiHalfContent:String = "oktatuok"

/*: "msgIncome" :*/
fileprivate let mainPriorityPath:[Character] = ["m","s","g","I","n","c","o","m","e"]

/*: "msgId" :*/
fileprivate let userSaveTitle:[Character] = ["m","s","g","I","d"]

/*: "ArtVarArg" :*/
fileprivate let showThereMsg:String = "deed assistant brace marketingTMsgT"
fileprivate let userProcedurePath:String = "ateStaprotection replacement bring arrow"
fileprivate let mainUpperPath:String = "qualityus"

/*: "TMsgTranslatedContent" :*/
fileprivate let appFreshValue:String = "TMsgTplatform through outline aid"
fileprivate let kOutlineUrl:String = "edConrobot unknown"

/*: "formatType" :*/
fileprivate let constPoleName:String = "fidentityrmat"
fileprivate let appEarnName:String = "would distance transport separate loseType"

/*: "plain" :*/
fileprivate let appCollectionPath:String = "plasuccessn"

/*: "imageData" :*/
fileprivate let app_editorKey:[Character] = ["i","m","a","g","e"]
fileprivate let userAlongResignUrl:[Character] = ["D","a","t","a"]

/*: "VideoMsg" :*/
fileprivate let app_approvalText:String = "Videnail interrupt"

/*: "match" :*/
fileprivate let kStateData:String = "MATCH"

/*: "userIntro" :*/
fileprivate let constAngleUrl:String = "uslessr"

/*: "targetId" :*/
fileprivate let appOrangeData:String = "blow shade content activitytargetId"

/*: "yyyy-MM-dd HH:mm:ss" :*/
fileprivate let app_middleMsg:String = "origin delivery builderyyyy-"
fileprivate let k_zoneRejectName:String = "HH:mreading mode"
fileprivate let userTeamValue:[Character] = ["m",":","s","s"]

/*: "sendTime" :*/
fileprivate let appResourceKey:String = "ting jaw kingdomsendT"
fileprivate let noti_spotValue:String = "iinmatee"

/*: "isRead" :*/
fileprivate let showItemAppearanceUrl:String = "positive prosecutor material identify varietyisRead"

/*: "audioData" :*/
fileprivate let user_togetherMessage:String = "aenhancedio"

/*: "audioUri" :*/
fileprivate let notiBroadcastMsg:[Character] = ["a","u","d","i","o","U"]
fileprivate let user_recognizeData:String = "rmap"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  TictacReactiveCompatible.swift

//: import Foundation
import Foundation

// MARK: - 枚举定义

//: @objc public enum TXMessageContentType: UInt {
@objc public enum ContentTransformable: UInt {
    //: case TXMessageContentType_text = 1
    case TXMessageContentType_text = 1
    //: case TXMessageContentType_picture
    case TXMessageContentType_picture
    //: case TXMessageContentType_gift
    case TXMessageContentType_gift
    //: case TXMessageContentType_askforGift
    case TXMessageContentType_askforGift
    //: case TXMessageContentType_voice
    case TXMessageContentType_voice
    //: case TXMessageContentType_local
    case TXMessageContentType_local
    //: case TXMessageContentType_tips
    case TXMessageContentType_tips
    //: case TXMessageContentType_userCard
    case TXMessageContentType_userCard
    //: case TXMessageContentType_video
    case TXMessageContentType_video
}

// MARK: - TictacReactiveCompatible

//: @objcMembers public class TUISocialChatManager: NSObject {
@objcMembers public class TictacReactiveCompatible: NSObject {
    // MARK: - 公共方法

    /// 获取消息插入的时间戳 毫秒
    //: public class func getMessageInsertTime() -> Int64 {
    public class func magnitude() -> Int64 {
        //: let curDate = Date().timeIntervalSince1970
        let curDate = Date().timeIntervalSince1970
        //: let msInterval = Int64(floor(curDate * 1000))
        let msInterval = Int64(floor(curDate * 1000))
        //: var deltaTime = Int64(V2TIMManager.sharedInstance().getServerTime())
        var deltaTime = Int64(V2TIMManager.sharedInstance().getServerTime()) // 秒为单位
        //: if deltaTime < 1 {
        if deltaTime < 1 {
            //: deltaTime = 0
            deltaTime = 0
        }
        //: let timeStamp = msInterval - deltaTime
        let timeStamp = msInterval - deltaTime
        //: return timeStamp
        return timeStamp
    }

    // MARK: - 腾讯IM消息处理

    /// 腾讯IM消息插入
    //: public class func insertTXMessage(withExtral extral: [String: Any], toUid: String, isSender: Bool, isTip: Bool) -> SparkCellData? {
    public class func terminalBeginEngraftDigitizerCipherCircleWith(withExtral extral: [String: Any], toUid: String, isSender: Bool, isTip: Bool) -> SparkCellData? {
        //: let data = handleTXMessageSend(withExtral: extral, isSender: isSender, isTip: isTip)
        let data = smooth(withExtral: extral, isSender: isSender, isTip: isTip)
        //: if let data = data {
        if let data = data {
            //: AfterThen.shared.func__handleMsg(targetId: toUid, msgCellData: data, isPrivete: true)
            AfterThen.shared.researchLaboratoryButton(targetId: toUid, msgCellData: data, isPrivete: true)
        }
        //: return data
        return data
    }

    /// 腾讯IM发送消息组装
    //: public class func handleTXMessageSend(withExtral extral: [String: Any], isSender: Bool, isTip: Bool) -> SparkCellData? {
    public class func smooth(withExtral extral: [String: Any], isSender: Bool, isTip: Bool) -> SparkCellData? {
        //: if isTip {
        if isTip {
            //: var extralDict = extral
            var extralDict = extral
            //: extralDict["msgType"] = "tips"
            extralDict[(String(notiManualTradeStr) + String(main_closeRetainUrl.suffix(6)))] = (k_othersId.replacingOccurrences(of: "signature", with: "s"))
            //: let extraDict = ["extra": extralDict]
            let extraDict = [(String(appDistrictContent)): extralDict]
            //: let extra = parseTXMessageJsonStr(data: extraDict)
            let extra = oldMatch(data: extraDict)
            //: let data = TalkingChatTipsMsgCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = TransferCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extra.data(using: .utf8)
            let cData = extra.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: data.innerMessage.isExcludedFromLastMessage = true
            data.innerMessage.isExcludedFromLastMessage = true
            //: let model = AbTalkingChatMsgBaseModel()
            let model = ServiceModel()
            //: if let tipModel = try? AbTalkingChatMsgTipsModel(dictionary: extralDict) {
            if let tipModel = try? MatchModel(dictionary: extralDict) {
                //: model.tips = tipModel
                model.tips = tipModel
            }
            //: data.msgModel = model
            data.msgModel = model
            //: return data
            return data
        }

        //: var tempDict = extral
        var tempDict = extral
        //: guard let msgInfo = extral["msgInfo"] as? [String: Any] else {
        guard let msgInfo = extral[(String(userOralKey) + String(kCloudEnoughHalfKey))] as? [String: Any] else {
            //: return nil
            return nil
        }

        //: tempDict["user"] = [
        tempDict[(user_environmentKey.replacingOccurrences(of: "toward", with: "us"))] = [
            //: "uid": DismissReactiveCompatible.share.loginUserMode.userID,
            (main_iconValue.replacingOccurrences(of: "alive", with: "d")): DismissReactiveCompatible.share.loginUserMode.userID,
            //: "nickname": DismissReactiveCompatible.share.loginUserMode.nickname,
            String(bytes: userFromReferUrl.map{cameraAlbum(visible: $0)}, encoding: .utf8)!: DismissReactiveCompatible.share.loginUserMode.nickname,
            //: "headPic": DismissReactiveCompatible.share.loginUserMode.headPic,
            String(bytes: main_pageId.map{alterBottom(succeed: $0)}, encoding: .utf8)!: DismissReactiveCompatible.share.loginUserMode.headPic,
            //: "age": DismissReactiveCompatible.share.loginUserMode.age,
            String(bytes: main_beamData.reversed(), encoding: .utf8)!: DismissReactiveCompatible.share.loginUserMode.age,
        ]

        //: let extraDict = ["extra": tempDict]
        let extraDict = [(String(appDistrictContent)): tempDict]
        //: let extraString = parseTXMessageJsonStr(data: extraDict)
        let extraString = oldMatch(data: extraDict)

        // 礼物
        //: if msgInfo["msgType"] as? String == "gift" {
        if msgInfo[(String(notiManualTradeStr) + String(main_closeRetainUrl.suffix(6)))] as? String == (noti_scenarioMsg.replacingOccurrences(of: "library", with: "f")) {
            //: var data: SparkCellData
            var data: SparkCellData
            //: if msgInfo["isGroup"] as? String == "1" {  // 群聊
            if msgInfo[(const_upMaleMessage.replacingOccurrences(of: "birth", with: "s") + String(dataSharedSelectionName))] as? String == "1" { // 群聊
                //: data = TalkingGroupChatGiftMsgCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = InsertCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else {
                //: data = ResolveCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = ResolveCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }

            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ServiceModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 索要礼物
        //: if msgInfo["msgType"] as? String == "WantGift" {
        if msgInfo[(String(notiManualTradeStr) + String(main_closeRetainUrl.suffix(6)))] as? String == (String(user_picText) + String(showBanTitle)) {
            //: let data = RangeThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = RangeThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ServiceModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 文字
        //: if msgInfo["msgType"] as? String == "txt" {
        if msgInfo[(String(notiManualTradeStr) + String(main_closeRetainUrl.suffix(6)))] as? String == (showPartyBirdText.replacingOccurrences(of: "live", with: "xt")) {
            //: var data: SparkCellData
            var data: SparkCellData
            //: if msgInfo["isGroup"] as? String == "1" {  // 群聊
            if msgInfo[(const_upMaleMessage.replacingOccurrences(of: "birth", with: "s") + String(dataSharedSelectionName))] as? String == "1" { // 群聊
                //: data = GroupTitleChatCellPortraitDataThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = GroupTitleChatCellPortraitDataThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else {
                //: data = CellReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = CellReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ServiceModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 图片
        //: if msgInfo["msgType"] as? String == "img" {
        if msgInfo[(String(notiManualTradeStr) + String(main_closeRetainUrl.suffix(6)))] as? String == (notiDoingdStr.lowercased()) {
            //: let data = EngineeringCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = EngineeringCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ServiceModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 视频
        //: if msgInfo["msgType"] as? String == "video" {
        if msgInfo[(String(notiManualTradeStr) + String(main_closeRetainUrl.suffix(6)))] as? String == (show_groupFormat.lowercased()) {
            //: let data = NumberensityReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = NumberensityReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ServiceModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 语音
        //: if msgInfo["msgType"] as? String == "audio" {
        if msgInfo[(String(notiManualTradeStr) + String(main_closeRetainUrl.suffix(6)))] as? String == (String(noti_nutWithinMagnitudeerestedFormat)) {
            //: let data = RangeCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = RangeCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ServiceModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 打招呼
        //: if extral["gift"] != nil && (msgInfo["messageType"] as? Int) == 4 {
        if extral[(noti_scenarioMsg.replacingOccurrences(of: "library", with: "f"))] != nil && (msgInfo[(kCanName.replacingOccurrences(of: "presentation", with: "m") + String(userCustomerStr))] as? Int) == 4 {
            //: let data = CellReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = CellReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ServiceModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        //: return nil
        return nil
    }

    /// 腾讯IM重发消息组装 （只有私聊在用）
    //: public class func handleTXMessageReSend(withData cellData: SparkCellData) -> SparkCellData? {
    public class func enableInvitation(withData cellData: SparkCellData) -> SparkCellData? {
        // 图片
        //: if let reCellData = cellData as? EngineeringCellData {
        if let reCellData = cellData as? EngineeringCellData {
            //: let paths = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true)
            let paths = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true)
            //: var filePath = paths.first!
            var filePath = paths.first!
            //: filePath = (filePath as NSString).appendingPathComponent(DepictionThen.getImageFileName())
            filePath = (filePath as NSString).appendingPathComponent(DepictionThen.shaftText())
            //: filePath = (filePath as NSString).appendingPathComponent((reCellData.imageUrl! as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((reCellData.imageUrl! as NSString).lastPathComponent)

            //: if !FileManager.default.fileExists(atPath: filePath) {
            if !FileManager.default.fileExists(atPath: filePath) {
                //: return nil
                return nil
            }

            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]
            //: msgInfo["contentType"] = "ImgMsg"
            msgInfo[(constGoldReferUrl.replacingOccurrences(of: "revenue", with: "o") + "entType")] = (String(constExpectedUrl) + String(const_burstStr))
            //: msgInfo["msgType"] = "img"
            msgInfo[(String(notiManualTradeStr) + String(main_closeRetainUrl.suffix(6)))] = (notiDoingdStr.lowercased())
            //: msgInfo["imageUri"] = filePath
            msgInfo[(String(notiUnitValue))] = filePath
            //: dic["msgInfo"] = msgInfo
            dic[(String(userOralKey) + String(kCloudEnoughHalfKey))] = msgInfo

            //: params["type"] = "ImgMsg"
            params[(noti_sMsg.replacingOccurrences(of: "fit", with: "e"))] = (String(constExpectedUrl) + String(const_burstStr))
            //: params["pic"] = filePath
            params[(k_foreheadMsg.replacingOccurrences(of: "shade", with: "c"))] = filePath

            // 重发私密照片
            //: let isBurn = reCellData.msgModel.msgInfo.customSend.isBurn
            let isBurn = reCellData.msgModel.msgInfo.customSend.isBurn
            //: if !isBurn.isEmpty {
            if !isBurn.isEmpty {
                //: params["isBurn"] = isBurn
                params[(String(appHostText.suffix(6)))] = isBurn
                //: } else {
            } else {
                //: params["isBurn"] = "0"
                params[(String(appHostText.suffix(6)))] = "0"
            }

            //: let giftId = reCellData.msgModel.msgInfo.customSend.giftId
            let giftId = reCellData.msgModel.msgInfo.customSend.giftId
            //: if !giftId.isEmpty {
            if !giftId.isEmpty {
                //: params["giftId"] = giftId
                params[(String(user_meetingLeavePath.suffix(6)))] = giftId
                //: } else {
            } else {
                //: params["giftId"] = "0"
                params[(String(user_meetingLeavePath.suffix(6)))] = "0"
            }

            //: dic["params"] = params
            dic[(String(appFirstWhoStr))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = smooth(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        // 视频
        //: if let reCellData = cellData as? NumberensityReactiveCompatible {
        if let reCellData = cellData as? NumberensityReactiveCompatible {
            //: reCellData.status = .Msg_Status_Fail
            reCellData.status = .Msg_Status_Fail
            //: return reCellData
            return reCellData
        }

        // 语音
        //: if let reCellData = cellData as? RangeCellData {
        if let reCellData = cellData as? RangeCellData {
            //: let voiceModel = DBUserInfoManager.cache_getAudioDB(message: reCellData.innerMessage)
            let voiceModel = CutInfoManager.corner(message: reCellData.innerMessage)
            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]

            //: if !voiceModel.db_audioLength.isEmpty {
            if !voiceModel.db_audioLength.isEmpty {
                //: msgInfo["audioLength"] = voiceModel.db_audioLength
                msgInfo[(noti_middleDitBadValue.replacingOccurrences(of: "timing", with: "d") + String(kAdjustId))] = voiceModel.db_audioLength
                //: params["len"] = voiceModel.db_audioLength
                params[(String(constChannelFilterMessage))] = voiceModel.db_audioLength
            }
            //: if !voiceModel.db_filePath.isEmpty {
            if !voiceModel.db_filePath.isEmpty {
                //: msgInfo["audioSandbox"] = voiceModel.db_filePath
                msgInfo[(app_maximumId.lowercased() + String(notiQuitMessage))] = voiceModel.db_filePath
                //: params["audioSandbox"] = voiceModel.db_filePath
                params[(app_maximumId.lowercased() + String(notiQuitMessage))] = voiceModel.db_filePath
            }
            //: msgInfo["msgType"] = "audio"
            msgInfo[(String(notiManualTradeStr) + String(main_closeRetainUrl.suffix(6)))] = (String(noti_nutWithinMagnitudeerestedFormat))
            //: msgInfo["senderId"] = DismissReactiveCompatible.share.loginUserMode.userID
            msgInfo[(String(data_justName.prefix(7)) + String(k_attachMessage))] = DismissReactiveCompatible.share.loginUserMode.userID
            //: dic["msgInfo"] = msgInfo
            dic[(String(userOralKey) + String(kCloudEnoughHalfKey))] = msgInfo

            //: params["type"] = "AudioMsg"
            params[(noti_sMsg.replacingOccurrences(of: "fit", with: "e"))] = (String(show_betterPath.prefix(8)))
            //: dic["params"] = params
            dic[(String(appFirstWhoStr))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = smooth(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        // 文本
        //: if let reCellData = cellData as? CellReactiveCompatible {
        if let reCellData = cellData as? CellReactiveCompatible {
            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]

            //: msgInfo["content"] = reCellData.contentStr ?? ""
            msgInfo[(dataTrustUrl.replacingOccurrences(of: "app", with: "t"))] = reCellData.contentStr ?? ""
            //: msgInfo["msgType"] = "txt"
            msgInfo[(String(notiManualTradeStr) + String(main_closeRetainUrl.suffix(6)))] = (showPartyBirdText.replacingOccurrences(of: "live", with: "xt"))
            //: dic["msgInfo"] = msgInfo
            dic[(String(userOralKey) + String(kCloudEnoughHalfKey))] = msgInfo
            //: params["type"] = "TxtMsg"
            params[(noti_sMsg.replacingOccurrences(of: "fit", with: "e"))] = (String(const_mortalUrl.suffix(6)))
            //: params["message"] = reCellData.contentStr ?? ""
            params[(mainStreetUrl.replacingOccurrences(of: "note", with: "es"))] = reCellData.contentStr ?? ""

            //: if reCellData.msgModel.quoteMsgInfo.isQuoteMsg() == true {
            if reCellData.msgModel.quoteMsgInfo.click() == true { // 引用消息重发
                //: params["quoteMsgId"] = reCellData.msgModel.quoteMsgInfo.msgId
                params[(user_shadowMessage.replacingOccurrences(of: "suite", with: "t") + "eMsgI" + userHoweverFormat.replacingOccurrences(of: "channel", with: "d"))] = reCellData.msgModel.quoteMsgInfo.msgId
                //: dic["quoteMsgInfo"] = reCellData.msgModel.quoteMsgInfo
                dic[(appStepStr.replacingOccurrences(of: "one", with: "u") + String(show_conductValue))] = reCellData.msgModel.quoteMsgInfo
                    //: .toDictionary()
                    .toDictionary()
            }

            //: if let replayId = reCellData.msgModel.params["replayId"] { // 问题回答消息重发
            if let replayId = reCellData.msgModel.params[(showPitchAcceptMsg.replacingOccurrences(of: "communicate", with: "re") + String(dataReadingName.suffix(3)))] { // 问题回答消息重发
                //: params["replayId"] = replayId
                params[(showPitchAcceptMsg.replacingOccurrences(of: "communicate", with: "re") + String(dataReadingName.suffix(3)))] = replayId
            }

            //: dic["params"] = params
            dic[(String(appFirstWhoStr))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = smooth(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        //: return nil
        return nil
    }

    /// 腾讯IM接收消息组装
    //: public class func handleTXReceiveMessage(withExtral dic: [String: Any], cusData: Data, tempModel: V2TIMMessage) -> SparkCellData? {
    public class func group(withExtral dic: [String: Any], cusData: Data, tempModel: V2TIMMessage) -> SparkCellData? {
        //: var cellData: SparkCellData?
        var cellData: SparkCellData?
        //: let extraDict = dic["extra"] as? [String: Any]
        let extraDict = dic[(String(appDistrictContent))] as? [String: Any]
        //: let msgInfoDict = extraDict?["msgInfo"] as? [String: Any]
        let msgInfoDict = extraDict?[(String(userOralKey) + String(kCloudEnoughHalfKey))] as? [String: Any]
        //: let user = dic["user"] as? [String: Any] ?? [:]
        let user = dic[(user_environmentKey.replacingOccurrences(of: "toward", with: "us"))] as? [String: Any] ?? [:]

        //: guard let model = try? AbTalkingChatMsgBaseModel(dictionary: extraDict) else {
        guard let model = try? ServiceModel(dictionary: extraDict) else {
            //: return nil
            return nil
        }
        //: var isSelf = (model.user.uid == DismissReactiveCompatible.share.loginUserMode.userID.intValue)
        var isSelf = (model.user.uid == DismissReactiveCompatible.share.loginUserMode.userID.intValue)

        // 后台代发送的自动打招呼消息
        //: var isAutoSendMsg = false
        var isAutoSendMsg = false
        //: if model.msgInfo.uid.intValue == DismissReactiveCompatible.share.loginUserMode.userID.intValue &&
        if model.msgInfo.uid.intValue == DismissReactiveCompatible.share.loginUserMode.userID.intValue &&
            //: model.msgInfo.messageType.rawValue == 3 {
            model.msgInfo.messageType.rawValue == 3
        {
            //: isSelf = true
            isSelf = true
            //: isAutoSendMsg = true
            isAutoSendMsg = true
        }

        //: if dic["rYMsgType"] as? String == "GJ:Sess:AutoGreet:TxtCustom" {
        if dic[(String(notiRunKey.prefix(5)) + "Type")] as? String == (String(dataForceValue.suffix(5)) + "ss:Au" + String(dataKingdomUrl.suffix(5)) + "et:Tx" + String(user_soccerId.prefix(2)) + userForeheadMsg.replacingOccurrences(of: "run", with: "u")) {
            //: isSelf = msgInfoDict?["changeDirection"] as? Bool ?? false
            isSelf = msgInfoDict?[(showDesignData.replacingOccurrences(of: "length", with: "h") + "eDirec" + data_questUrl.replacingOccurrences(of: "flow", with: "t"))] as? Bool ?? false
        }

        //: let contentType = messageCellType(dic)
        let contentType = panelType(dic)

        // 系统消息
        //: if extraDict?["msgType"] as? String == "system" ||
        if extraDict?[(String(notiManualTradeStr) + String(main_closeRetainUrl.suffix(6)))] as? String == (String(app_outputFemaleData) + app_aidFormat.replacingOccurrences(of: "scale", with: "em")) ||
            //: contentType == .TXMessageContentType_tips {
            contentType == .TXMessageContentType_tips
        {
            //: let model = AbTalkingChatMsgBaseModel()
            let model = ServiceModel()
            //: let tipModel = try? AbTalkingChatMsgTipsModel(dictionary: extraDict)
            let tipModel = try? MatchModel(dictionary: extraDict)
            //: cellData = TalkingChatTipsMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = TransferCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: if let content = dic["content"] as? String {
            if let content = dic[(dataTrustUrl.replacingOccurrences(of: "app", with: "t"))] as? String {
                //: tipModel?.content = content
                tipModel?.content = content
            }
            //: if tipModel != nil {
            if tipModel != nil {
                //: model.tips = tipModel!
                model.tips = tipModel!
            }
            //: cellData?.msgModel = model
            cellData?.msgModel = model
            //: return cellData
            return cellData
        }

        // 其他消息类型的创建...
        //: switch contentType {
        switch contentType {
        //: case .TXMessageContentType_picture:
        case .TXMessageContentType_picture:
            //: cellData = EngineeringCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = EngineeringCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: case .TXMessageContentType_video:
        case .TXMessageContentType_video:
            //: cellData = NumberensityReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = NumberensityReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: case .TXMessageContentType_gift:
        case .TXMessageContentType_gift:
            //: if let groupID = tempModel.groupID, !groupID.isEmpty {
            if let groupID = tempModel.groupID, !groupID.isEmpty { // 群聊gift
                //: cellData = TalkingGroupChatGiftMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = InsertCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else { // 私聊gift
                //: cellData = ResolveCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = ResolveCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }

        //: case .TXMessageContentType_askforGift:
        case .TXMessageContentType_askforGift: // 索要礼物
            //: let tempDic = parseTXMessageData(data: cusData)
            let tempDic = sourcePresentStop(data: cusData)
            //: if let extraData = tempDic["extra"] as? [String: Any],
            if let extraData = tempDic[(String(appDistrictContent))] as? [String: Any],
               //: let msgInfoData = extraData["msgInfo"] as? [String: Any],
               let msgInfoData = extraData[(String(userOralKey) + String(kCloudEnoughHalfKey))] as? [String: Any],
               //: !msgInfoData.isEmpty {
               !msgInfoData.isEmpty
            {
                //: if let info = try? AbTalkingChatMsgInfoModel(dictionary: msgInfoData) {
                if let info = try? ServicePerModel(dictionary: msgInfoData) {
                    //: model.msgInfo = info
                    model.msgInfo = info
                }
            }
            //: cellData = RangeThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = RangeThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: cellData?.msgModel = model
            cellData?.msgModel = model

        //: case .TXMessageContentType_voice:
        case .TXMessageContentType_voice:
            //: cellData = RangeCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = RangeCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: if let audioCellData = cellData as? RangeCellData {
            if let audioCellData = cellData as? RangeCellData {
                //: if audioCellData.voiceModel.db_voiceCacheWrap.msgId.isEmpty {
                if audioCellData.voiceModel.db_voiceCacheWrap.msgId.isEmpty {
                    //: let mainKey = checkPrimaryKey(withMsgModel: tempModel)
                    let mainKey = regardModel(withMsgModel: tempModel)
                    //: var cacheWrap = WCDBVoiceMsgTable.db_getVoiceMsg(with: mainKey)
                    var cacheWrap = RedMsgTable.unitedNationsAgency(with: mainKey)
                    //: if cacheWrap == nil {
                    if cacheWrap == nil {
                        //: cacheWrap = WCDBVoiceMsgTable.db_inserVoiceMsgToDB(packageDict(withTXMessage: tempModel))
                        cacheWrap = RedMsgTable.soul(bankManager(withTXMessage: tempModel))
                        //: audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        //: } else {
                    } else {
                        //: audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                    }
                }
            }

        //: case .TXMessageContentType_local:
        case .TXMessageContentType_local:
            //: cellData = TalkingChatMatchMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = TotalensityLovelyCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: default:
        default:
            //: let title = extraDict?["title"] as? String ?? ""
            let title = extraDict?[(String(constPressFormat))] as? String ?? ""
            //: let content = extraDict?["content"] as? String ?? ""
            let content = extraDict?[(dataTrustUrl.replacingOccurrences(of: "app", with: "t"))] as? String ?? ""
            //: let img = extraDict?["img"] as? String ?? ""
            let img = extraDict?[(notiDoingdStr.lowercased())] as? String ?? ""
            //: if !title.isEmpty || !content.isEmpty || !img.isEmpty {
            if !title.isEmpty || !content.isEmpty || !img.isEmpty { // 系统新消息
                //: cellData = TalkingChatSystemNotifJumpMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = ContainerReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData?.extraDict = extraDict
                cellData?.extraDict = extraDict
                //: } else if let groupID = tempModel.groupID, !groupID.isEmpty {
            } else if let groupID = tempModel.groupID, !groupID.isEmpty { // 群聊text
                //: cellData = GroupTitleChatCellPortraitDataThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = GroupTitleChatCellPortraitDataThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else { // 私聊text
                //: cellData = CellReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = CellReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }
        }

        // 设置用户信息
        //: if model.user.nickname.isEmpty {
        if model.user.nickname.isEmpty {
            //: var userDict = user
            var userDict = user
            //: userDict.merge(msgInfoDict ?? [:]) { (_, new) in new }
            userDict.merge(msgInfoDict ?? [:]) { _, new in new }
            //: if let userModel = try? AbTalkingChatMsgUserModel(dictionary: userDict) {
            if let userModel = try? RootModel(dictionary: userDict) {
                //: if userModel.uid == 0 {
                if userModel.uid == 0 {
                    //: userModel.uid = userDict["id"] as? Int ?? 0
                    userModel.uid = userDict["id"] as? Int ?? 0
                }
                //: if userModel.headPic.isEmpty == true {
                if userModel.headPic.isEmpty == true {
                    //: userModel.headPic = userDict["icon"] as? String ?? ""
                    userModel.headPic = userDict[(String(app_greetWeValue))] as? String ?? ""
                }

                //: if isAutoSendMsg {
                if isAutoSendMsg {
                    //: userModel.uid = model.msgInfo.toUid.intValue
                    userModel.uid = model.msgInfo.toUid.intValue
                    //: userModel.headPic =
                    userModel.headPic =
                        //: DismissReactiveCompatible.share.loginUserMode.headPic ?? ""
                        DismissReactiveCompatible.share.loginUserMode.headPic ?? ""
                }
                //: model.user = userModel
                model.user = userModel
            }
        }

        //: if model.msgInfo == nil {
        if model.msgInfo == nil {
            //: let msgInfoModel = AbTalkingChatMsgInfoModel()
            let msgInfoModel = ServicePerModel()
            //: msgInfoModel.content = dic["content"] as? String ?? ""
            msgInfoModel.content = dic[(dataTrustUrl.replacingOccurrences(of: "app", with: "t"))] as? String ?? ""
            //: msgInfoModel.jumps = extraDict?["extra"] as? [Any] ?? []
            msgInfoModel.jumps = extraDict?[(String(appDistrictContent))] as? [Any] ?? []
            //: model.msgInfo = msgInfoModel
            model.msgInfo = msgInfoModel
        }

        // 聊天室送礼消息
        //: if contentType == .TXMessageContentType_gift && tempModel.groupID != nil && !tempModel.groupID!.isEmpty {
        if contentType == .TXMessageContentType_gift && tempModel.groupID != nil && !tempModel.groupID!.isEmpty {
            //: if model.toUser.uid.isEmpty {
            if model.toUser.uid.isEmpty { // 送礼所有人
                //: let toUserInfo = AbTalkingChatMsgToUserModel()
                let toUserInfo = DestroyModel()
                //: toUserInfo.uid = "all"
                toUserInfo.uid = (userBeastMessage.replacingOccurrences(of: "bridge", with: "a"))
                //: toUserInfo.nickname = model.gift.sendNum
                toUserInfo.nickname = model.gift.sendNum
                //: toUserInfo.headPic = "icon_chats_pcr"
                toUserInfo.headPic = (notiExceptionStr.replacingOccurrences(of: "save", with: "ic") + String(mainOccurMsg.suffix(6)) + "s_pcr")
                //: model.toUser = toUserInfo
                model.toUser = toUserInfo
            }
        }
        //: cellData?.msgModel = model
        cellData?.msgModel = model

        // 本地自定义字段处理
        //: let customStr = String(data: cusData, encoding: .utf8) ?? ""
        let customStr = String(data: cusData, encoding: .utf8) ?? ""
        //: let customDic = NSDictionary.dictionary(withJsonString: customStr) ?? [:]
        let customDic = NSDictionary.dictionaryToTitle(withJsonString: customStr) ?? [:]
        //: let statusStr = customDic["status"] as? String ?? "0"
        let statusStr = customDic[(notiHalfContent.replacingOccurrences(of: "ok", with: "s"))] as? String ?? "0"
        //: let status = TMsgStatus(rawValue: UInt(statusStr) ?? 0) ?? .Msg_Status_Init
        let status = TMsgStatus(rawValue: UInt(statusStr) ?? 0) ?? .Msg_Status_Init
        //: cellData?.status = status
        cellData?.status = status
        //: cellData?.msgIncome = CGFloat(customDic["msgIncome"] as? Float ?? 0)
        cellData?.msgIncome = CGFloat(customDic[(String(mainPriorityPath))] as? Float ?? 0)
        //: cellData?.messageType = msgInfoDict?["messageType"] as? Int ?? 0
        cellData?.messageType = msgInfoDict?[(kCanName.replacingOccurrences(of: "presentation", with: "m") + String(userCustomerStr))] as? Int ?? 0

        //: if let msgId = customDic["msgId"] as? String {
        if let msgId = customDic[(String(userSaveTitle))] as? String {
            //: cellData?.msgModel.msgInfo.msgId = msgId
            cellData?.msgModel.msgInfo.msgId = msgId
        }

        // 文本翻译
        //: if let translateStatus = customDic["TMsgTranslateStatus"] as? Int {
        if let translateStatus = customDic[(String(showThereMsg.suffix(5)) + "ransl" + String(userProcedurePath.prefix(6)) + mainUpperPath.replacingOccurrences(of: "quality", with: "t"))] as? Int {
            //: cellData?.translateStatus = TMsgTranslateStatus(rawValue: UInt(translateStatus)) ?? .MsgTranslate_init
            cellData?.translateStatus = ArtVarArg(rawValue: UInt(translateStatus)) ?? .MsgTranslate_init
        }
        //: if let translatedContent = customDic["TMsgTranslatedContent"] as? String {
        if let translatedContent = customDic[(String(appFreshValue.prefix(5)) + "ranslat" + String(kOutlineUrl.prefix(5)) + "tent")] as? String {
            //: cellData?.translatedContent = translatedContent
            cellData?.translatedContent = translatedContent
        }

        // 私密照片Cache
        //: if contentType == .TXMessageContentType_picture {
        if contentType == .TXMessageContentType_picture {
            //: let dict = AbTalkingChatMsgIntimatePhotoModel.loadLocalIntimatePhotoDict()
            let dict = RootMemberModel.from()
            //: if let photoData = dict[tempModel.msgID as Any] as? String {
            if let photoData = dict[tempModel.msgID as Any] as? String {
                //: cellData?.msgModel.msgInfo.intimatePhoto = AbTalkingChatMsgIntimatePhotoModel(string: photoData, error: nil)
                cellData?.msgModel.msgInfo.intimatePhoto = RootMemberModel(string: photoData, error: nil)
            }
        }

        // 私密视频Cache
        //: if contentType == .TXMessageContentType_video {
        if contentType == .TXMessageContentType_video {
            //: let dict = AbTalkingChatMsgVideoModel.loadLocalIntimateVideoDict()
            let dict = HistoricalModel.graft()
            //: if let videoData = dict[tempModel.msgID as Any] as? String {
            if let videoData = dict[tempModel.msgID as Any] as? String {
                //: cellData?.msgModel.msgInfo.video = AbTalkingChatMsgVideoModel(string: videoData, error: nil)
                cellData?.msgModel.msgInfo.video = HistoricalModel(string: videoData, error: nil)
            }
        }

        // 索要礼物Cache
        //: if contentType == .TXMessageContentType_askforGift {
        if contentType == .TXMessageContentType_askforGift {
            //: let arr = WantGiftModel.loadAskForGiftArr()
            let arr = ExampleWishWellModel.pic()
            //: if arr.contains(tempModel.msgID as Any) {
            if arr.contains(tempModel.msgID as Any) { // 已发送
                //: cellData?.msgModel.msgInfo.wantGift.sendStatus = 1
                cellData?.msgModel.msgInfo.wantGift.sendStatus = 1
            }
        }

        //: return cellData
        return cellData
    }

    // MARK: - 辅助方法

    /// 根据字典获取消息类型
    //: public class func messageCellType(_ extraDic: [String: Any]) -> TXMessageContentType {
    public class func panelType(_ extraDic: [String: Any]) -> ContentTransformable {
        //: let dic = extraDic["extra"] as? [String: Any]
        let dic = extraDic[(String(appDistrictContent))] as? [String: Any]
        //: let msgInfo = dic?["msgInfo"] as? [String: Any]
        let msgInfo = dic?[(String(userOralKey) + String(kCloudEnoughHalfKey))] as? [String: Any]
        //: guard let model = try? AbTalkingChatMsgBaseModel(dictionary: dic) else {
        guard let model = try? ServiceModel(dictionary: dic) else {
            //: return .TXMessageContentType_text
            return .TXMessageContentType_text
        }

        // 系统消息 官方小密
        //: if dic?["formatType"] as? String == "plain" {
        if dic?[(constPoleName.replacingOccurrences(of: "identity", with: "o") + String(appEarnName.suffix(4)))] as? String == (appCollectionPath.replacingOccurrences(of: "success", with: "i")) {
            //: let imageUri = dic?["imageUri"] as? String ?? ""
            let imageUri = dic?[(String(notiUnitValue))] as? String ?? ""
            //: let imageData = dic?["imageData"] as? String ?? ""
            let imageData = dic?[(String(app_editorKey) + String(userAlongResignUrl))] as? String ?? ""
            //: let isImageStyle = !imageData.isEmpty || !imageUri.isEmpty
            let isImageStyle = !imageData.isEmpty || !imageUri.isEmpty
            //: if isImageStyle {
            if isImageStyle {
                //: return .TXMessageContentType_picture
                return .TXMessageContentType_picture
            }
        }

        // 图片||私密照片
        //: if !model.msgInfo.imageUri.isEmpty || !model.msgInfo.imageData.isEmpty || !model.msgInfo.intimatePhoto.originUrl.isEmpty {
        if !model.msgInfo.imageUri.isEmpty || !model.msgInfo.imageData.isEmpty || !model.msgInfo.intimatePhoto.originUrl.isEmpty {
            //: return .TXMessageContentType_picture
            return .TXMessageContentType_picture
        }

        // 接收的视频消息 || 本人发送的视频消息
        //: if !model.msgInfo.video.videoUrl.isEmpty || model.msgInfo.contentType == "VideoMsg" {
        if !model.msgInfo.video.videoUrl.isEmpty || model.msgInfo.contentType == (String(app_approvalText.prefix(4)) + "oMsg") {
            //: return .TXMessageContentType_video
            return .TXMessageContentType_video
        }

        // 礼物，非打招呼消息
        //: if !model.gift.name.isEmpty && model.msgInfo.messageType.rawValue != 4 {
        if !model.gift.name.isEmpty && model.msgInfo.messageType.rawValue != 4 {
            //: return .TXMessageContentType_gift
            return .TXMessageContentType_gift
        }

        //: let isVoiceStyle = checkVoiceMsg(msginfo: msgInfo ?? [:])
        let isVoiceStyle = proto(msginfo: msgInfo ?? [:])
        //: if isVoiceStyle {
        if isVoiceStyle {
            //: return .TXMessageContentType_voice
            return .TXMessageContentType_voice
        }

        //: if (dic?["match"] as? Int) == 1 || (msgInfo?["match"] as? Int) == 1 {
        if (dic?[(kStateData.lowercased())] as? Int) == 1 || (msgInfo?[(kStateData.lowercased())] as? Int) == 1 {
            // 匹配消息
            //: return .TXMessageContentType_local
            return .TXMessageContentType_local
        }

        //: if dic?["msgType"] as? String == "tips" || (dic?["messageType"] as? Int) == 17 {
        if dic?[(String(notiManualTradeStr) + String(main_closeRetainUrl.suffix(6)))] as? String == (k_othersId.replacingOccurrences(of: "signature", with: "s")) || (dic?[(kCanName.replacingOccurrences(of: "presentation", with: "m") + String(userCustomerStr))] as? Int) == 17 {
            // 提示消息,包含超时未回复
            //: return .TXMessageContentType_tips
            return .TXMessageContentType_tips
        }

        //: if (dic?["formatType"] as? String == "mf") && ((dic?["messageType"] as? Int) == 15 || (dic?["messageType"] as? Int) == 16) {
        if (dic?[(constPoleName.replacingOccurrences(of: "identity", with: "o") + String(appEarnName.suffix(4)))] as? String == "mf") && ((dic?[(kCanName.replacingOccurrences(of: "presentation", with: "m") + String(userCustomerStr))] as? Int) == 15 || (dic?[(kCanName.replacingOccurrences(of: "presentation", with: "m") + String(userCustomerStr))] as? Int) == 16) {
            // 提示消息，打招呼提示送出神秘礼物,对方设置价格消息提示
            //: return .TXMessageContentType_tips
            return .TXMessageContentType_tips
        }

        //: if dic?["msgType"] as? String == "userIntro" {
        if dic?[(String(notiManualTradeStr) + String(main_closeRetainUrl.suffix(6)))] as? String == (constAngleUrl.replacingOccurrences(of: "less", with: "e") + "Intro") {
            //: return .TXMessageContentType_userCard
            return .TXMessageContentType_userCard
        }

        //: if msgInfo?["msgType"] as? String == "WantGift" || (msgInfo?["messageType"] as? Int) == 25 {
        if msgInfo?[(String(notiManualTradeStr) + String(main_closeRetainUrl.suffix(6)))] as? String == (String(user_picText) + String(showBanTitle)) || (msgInfo?[(kCanName.replacingOccurrences(of: "presentation", with: "m") + String(userCustomerStr))] as? Int) == 25 {
            //: return .TXMessageContentType_askforGift
            return .TXMessageContentType_askforGift
        }

        //: return .TXMessageContentType_text
        return .TXMessageContentType_text
    }

    // MARK: - 语音消息处理

    /// 从TX的model里提取参数
    //: public class func packageDict(withTXMessage message: V2TIMMessage) -> [String: Any] {
    public class func bankManager(withTXMessage message: V2TIMMessage) -> [String: Any] {
        //: let extra = String(data: message.customElem.data, encoding: .utf8) ?? ""
        let extra = String(data: message.customElem.data, encoding: .utf8) ?? ""
        //: let dic = NSDictionary.dictionary(withJsonString: extra) ?? [:]
        let dic = NSDictionary.dictionaryToTitle(withJsonString: extra) ?? [:]
        //: let extraData = dic["extra"] as? [String: Any]
        let extraData = dic[(String(appDistrictContent))] as? [String: Any]
        //: let msgInfo = extraData?["msgInfo"] as? [String: Any]
        let msgInfo = extraData?[(String(userOralKey) + String(kCloudEnoughHalfKey))] as? [String: Any]
        //: let user = extraData?["user"] as? [String: Any]
        let user = extraData?[(user_environmentKey.replacingOccurrences(of: "toward", with: "us"))] as? [String: Any]

        //: let senderId = msgInfo?["senderId"] as? String ?? ""
        let senderId = msgInfo?[(String(data_justName.prefix(7)) + String(k_attachMessage))] as? String ?? ""
        //: var info = msgInfo
        var info = msgInfo

        // 自己发送的和接收的数据结构不一样
        //: if senderId == DismissReactiveCompatible.share.loginUserMode.userID {
        if senderId == DismissReactiveCompatible.share.loginUserMode.userID {
            //: info?["senderId"] = senderId
            info?[(String(data_justName.prefix(7)) + String(k_attachMessage))] = senderId
            //: info?["targetId"] = message.userID ?? (user?["targetId"] as? String ?? "")
            info?[(String(appOrangeData.suffix(8)))] = message.userID ?? (user?[(String(appOrangeData.suffix(8)))] as? String ?? "")
            //: } else {
        } else {
            //: info?["senderId"] = "\(msgInfo?["uid"] as? Int ?? 0)"
            info?[(String(data_justName.prefix(7)) + String(k_attachMessage))] = "\(msgInfo?[(main_iconValue.replacingOccurrences(of: "alive", with: "d"))] as? Int ?? 0)"
            //: info?["targetId"] = "\(msgInfo?["uid"] as? Int ?? 0)"
            info?[(String(appOrangeData.suffix(8)))] = "\(msgInfo?[(main_iconValue.replacingOccurrences(of: "alive", with: "d"))] as? Int ?? 0)"
        }

        //: if msgInfo?.keys.contains("msgId") ?? false {
        if msgInfo?.keys.contains((String(userSaveTitle))) ?? false {
            //: info?["msgId"] = "\(msgInfo?["msgId"] ?? "")"
            info?[(String(userSaveTitle))] = "\(msgInfo?[(String(userSaveTitle))] ?? "")"
            //: } else {
        } else {
            //: let customStr = String(data: message.localCustomData, encoding: .utf8) ?? ""
            let customStr = String(data: message.localCustomData, encoding: .utf8) ?? ""
            //: let customDic = NSDictionary.dictionary(withJsonString: customStr) ?? [:]
            let customDic = NSDictionary.dictionaryToTitle(withJsonString: customStr) ?? [:]
            //: if customDic["msgId"] != nil {
            if customDic[(String(userSaveTitle))] != nil {
                //: info?["msgId"] = "\(customDic["msgId"] ?? "")"
                info?[(String(userSaveTitle))] = "\(customDic[(String(userSaveTitle))] ?? "")"
                //: } else {
            } else {
                //: let txMsgId = !message.msgID.isEmpty ? message.msgID : "\(Int64(Date().timeIntervalSince1970))"
                let txMsgId = !message.msgID.isEmpty ? message.msgID : "\(Int64(Date().timeIntervalSince1970))"
                //: info?["msgId"] = txMsgId
                info?[(String(userSaveTitle))] = txMsgId
            }
        }

        //: if msgInfo?.keys.contains("audioSandbox") ?? false {
        if msgInfo?.keys.contains((app_maximumId.lowercased() + String(notiQuitMessage))) ?? false {
            //: info?["audioSandbox"] = "\(msgInfo?["audioSandbox"] ?? "")"
            info?[(app_maximumId.lowercased() + String(notiQuitMessage))] = "\(msgInfo?[(app_maximumId.lowercased() + String(notiQuitMessage))] ?? "")"
        }
        //: if msgInfo?.keys.contains("audioLength") ?? false {
        if msgInfo?.keys.contains((noti_middleDitBadValue.replacingOccurrences(of: "timing", with: "d") + String(kAdjustId))) ?? false {
            //: info?["audioLength"] = "\(msgInfo?["audioLength"] ?? "")"
            info?[(noti_middleDitBadValue.replacingOccurrences(of: "timing", with: "d") + String(kAdjustId))] = "\(msgInfo?[(noti_middleDitBadValue.replacingOccurrences(of: "timing", with: "d") + String(kAdjustId))] ?? "")"
        }

        //: let formatter = DateFormatter()
        let formatter = DateFormatter()
        //: formatter.dateFormat = "yyyy-MM-dd HH:mm:ss"
        formatter.dateFormat = (String(app_middleMsg.suffix(5)) + "MM-dd " + String(k_zoneRejectName.prefix(4)) + String(userTeamValue))
        //: info?["sendTime"] = formatter.string(from: message.timestamp ?? Date())
        info?[(String(appResourceKey.suffix(5)) + noti_spotValue.replacingOccurrences(of: "inmate", with: "m"))] = formatter.string(from: message.timestamp ?? Date())
        //: info?["isRead"] = senderId == DismissReactiveCompatible.share.loginUserMode.userID ? "1" : "0"
        info?[(String(showItemAppearanceUrl.suffix(6)))] = senderId == DismissReactiveCompatible.share.loginUserMode.userID ? "1" : "0"

        //: return info ?? [:]
        return info ?? [:]
    }

    //: public class func checkPrimaryKey(withMsgModel tempModel: V2TIMMessage) -> String {
    public class func regardModel(withMsgModel tempModel: V2TIMMessage) -> String {
        //: let v2Info = packageDict(withTXMessage: tempModel)
        let v2Info = bankManager(withTXMessage: tempModel)
        //: let key = "\(v2Info["msgId"] ?? "")"
        let key = "\(v2Info[(String(userSaveTitle))] ?? "")"
        //: print("key = \(key)")
        //: return key
        return key
    }

    // MARK: - JSON处理方法

    //: @objc public class func parseTXMessageData(data: Data) -> [String: Any] {
    @objc public class func sourcePresentStop(data: Data) -> [String: Any] {
        //: do {
        do {
            //: let dict = try JSONSerialization.jsonObject(
            let dict = try JSONSerialization.jsonObject(
                //: with: data, options: .mutableContainers)
                with: data, options: .mutableContainers
            )
            //: return dict as? [String: Any] ?? [:]
            return dict as? [String: Any] ?? [:]
            //: } catch {
        } catch {
            //: return [:]
            return [:]
        }
    }

    //: @objc public class func parseTXMessageJsonStr(data: [String: Any]) -> String {
    @objc public class func oldMatch(data: [String: Any]) -> String {
        //: do {
        do {
            //: let jsonData = try JSONSerialization.data(
            let jsonData = try JSONSerialization.data(
                //: withJSONObject: data, options: [])
                withJSONObject: data, options: []
            )
            //: return String(data: jsonData, encoding: .utf8) ?? ""
            return String(data: jsonData, encoding: .utf8) ?? ""
            //: } catch {
        } catch {
            //: return ""
            return ""
        }
    }

    // MARK: - 语音消息检测

    //: @objc public class func checkVoiceMsg(msginfo: [String: Any]) -> Bool {
    @objc public class func proto(msginfo: [String: Any]) -> Bool {
        //: if let msgType = msginfo["msgType"] as? String, msgType == "audio" {
        if let msgType = msginfo[(String(notiManualTradeStr) + String(main_closeRetainUrl.suffix(6)))] as? String, msgType == (String(noti_nutWithinMagnitudeerestedFormat)) {
            //: return true
            return true
        }
        //: if let contentType = msginfo["contentType"] as? String,
        if let contentType = msginfo[(constGoldReferUrl.replacingOccurrences(of: "revenue", with: "o") + "entType")] as? String,
           //: contentType == "AudioMsg"
           contentType == (String(show_betterPath.prefix(8)))
        {
            //: return true
            return true
        }
        //: if let audioData = msginfo["audioData"] as? String, !audioData.isEmpty {
        if let audioData = msginfo[(user_togetherMessage.replacingOccurrences(of: "enhance", with: "u") + "Data")] as? String, !audioData.isEmpty {
            //: return true
            return true
        }
        //: if let audioUri = msginfo["audioUri"] as? String, !audioUri.isEmpty {
        if let audioUri = msginfo[(String(notiBroadcastMsg) + user_recognizeData.replacingOccurrences(of: "map", with: "i"))] as? String, !audioUri.isEmpty {
            //: return true
            return true
        }
        //: return false
        return false
    }
}
