
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let dataMasterMessage:[UInt8] = [0xdf,0xe4,0xdf,0xea,0x9e,0xd9,0xe5,0xda,0xdb,0xe8,0xb0,0x9f,0x96,0xde,0xd7,0xe9,0x96,0xe4,0xe5,0xea,0x96,0xd8,0xdb,0xdb,0xe4,0x96,0xdf,0xe3,0xe6,0xe2,0xdb,0xe3,0xdb,0xe4,0xea,0xdb,0xda]

fileprivate func galleryEnroll(gap num: UInt8) -> UInt8 {
    let value = Int(num) + 138
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "#128CFF" :*/
fileprivate let user_parName:String = "#128CFFblue condition"

/*: "Feedback" :*/
fileprivate let noti_blowUrl:String = "rug reject mystery richFeedb"
fileprivate let userTranslationStr:String = "acrequest"

/*: "#8370FF" :*/
fileprivate let notiPlayFormat:[Character] = ["#","8","3","7","0","F"]
fileprivate let mainSampleMsg:String = "f"

/*: "Quick Greeting" :*/
fileprivate let kBlowStoryPath:String = "Quicguide component moment indicator reserve"
fileprivate let data_runnerPath:String = "ebindin"
fileprivate let dataQuantityervalExtraUrl:[Character] = ["g"]

/*: "Beauty Photos" :*/
fileprivate let main_suiteUrl:String = "via sweep go multi weeklyBeauty"
fileprivate let main_transmissionName:String = "success evaluation Photos"

/*: "url" :*/
fileprivate let show_sitData:String = "urgo"

/*: "unlockGift" :*/
fileprivate let noti_twoData:String = "UNLOC"
fileprivate let show_trendData:String = "instance leading flexiblekGift"

/*: "isBurn" :*/
fileprivate let appAttachData:[UInt8] = [0xaf,0xb5,0x84,0xb3,0xb4,0xa8]

private func insertPole(gold num: UInt8) -> UInt8 {
    return num ^ 198
}

/*: "giftId" :*/
fileprivate let noti_mutualPath:[Character] = ["g","i","f","t","I","d"]

/*: "rYMsgType" :*/
fileprivate let user_selectedIsolatePickMessage:[Character] = ["r","Y","M","s","g","T","y","p","e"]

/*: "GJ:CallCustom" :*/
fileprivate let main_stillMsg:String = "hi strength huge waitGJ:C"
fileprivate let noti_accuracyGraySweetContent:String = "ospan"

/*: "changeDirection" :*/
fileprivate let mainEarnUniversalMsg:[Character] = ["c","h","a","n","g","e","D","i"]
fileprivate let notiServeMsg:String = "rectioextend"

/*: "extra" :*/
fileprivate let data_explainAllowSharedStr:[Character] = ["e","x","t","r","a"]

/*: "msgInfo" :*/
fileprivate let main_comeToolStr:String = "msgInforush strike"

/*: "toUid" :*/
fileprivate let notiSteepFormat:[Character] = ["t","o","U","i","d"]

/*: "wantGift" :*/
fileprivate let mainFutureBulletData:String = "wappearnt"

/*: "jumpKey" :*/
fileprivate let data_guideFormat:String = "jumpKeyhair cloud strength animal page"

/*: "truePersonAuth" :*/
fileprivate let main_elementWhiteLieData:[Character] = ["t","r","u","e","P","e"]
fileprivate let notiSouthwestText:String = "rsignore"

/*: "logType" :*/
fileprivate let main_marginFormat:[Character] = ["l","o","g","T","y","p","e"]

/*: "content" :*/
fileprivate let const_protectionValue:[UInt8] = [0x74,0x6e,0x65,0x74,0x6e,0x6f,0x63]

/*: "msgType" :*/
fileprivate let user_justiceValue:[UInt8] = [0x65,0x70,0x79,0x54,0x67,0x73,0x6d]

/*: "txt" :*/
fileprivate let showFeedOutlineStr:[Character] = ["t","x","t"]

/*: "message" :*/
fileprivate let app_generalPath:[UInt8] = [0x40,0x38,0x46,0x46,0x34,0x3a,0x38]

fileprivate func sweepEnjoy(fun num: UInt8) -> UInt8 {
    let value = Int(num) - 211
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "replayId" :*/
fileprivate let main_smartFormat:[UInt8] = [0x7f,0x68,0x7d,0x61,0x6c,0x74,0x44,0x69]

private func indicatorScene(stream num: UInt8) -> UInt8 {
    return num ^ 13
}

/*: "type" :*/
fileprivate let dataShStr:String = "gapype"

/*: "TxtMsg" :*/
fileprivate let show_segmentCookieKey:[Character] = ["T","x","t","M","s","g"]

/*: "params" :*/
fileprivate let showFileName:[UInt8] = [0x30,0x21,0x32,0x21,0x2d,0x33]

fileprivate func totaloPure(magnitude num: UInt8) -> UInt8 {
    let value = Int(num) + 64
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "MultiQuickMsg" :*/
fileprivate let show_startBassMessage:String = "league"
fileprivate let show_produceMsg:String = "sick income marketingultiQu"

/*: "QuickMsg" :*/
fileprivate let data_normalPath:[Character] = ["Q","u","i","c","k","M","s","g"]

/*: "txtId" :*/
fileprivate let notiMethodFormat:[Character] = ["t","x","t","I","d"]

/*: "Voice hello download failed. Procedure" :*/
fileprivate let appHolderUrl:[UInt8] = [0xf2,0xb,0x5,0xff,0x1,0xbc,0x4,0x1,0x8,0x8,0xb,0xbc,0x0,0xb,0x13,0xa,0x8,0xb,0xfd,0x0,0xbc,0x2,0xfd,0x5,0x8,0x1,0x0,0xca,0xbc,0xec,0xe,0xb,0xff,0x1,0x0,0x11,0xe,0x1]

fileprivate func fatherPause(re num: UInt8) -> UInt8 {
    let value = Int(num) - 156
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "length" :*/
fileprivate let const_turnContent:[Character] = ["l","e","n","g","t","h"]

/*: "audio" :*/
fileprivate let constCanId:String = "audsomebodyo"

/*: "audioId" :*/
fileprivate let show_indexAgentMsg:String = "aglassio"
fileprivate let kMeetingText:String = "Idfuture month variety wealthy"

/*: "audioLength" :*/
fileprivate let constLookData:[UInt8] = [0x65,0x79,0x68,0x6d,0x73,0x50,0x69,0x72,0x6b,0x78,0x6c]

fileprivate func jumpingOnPlace(coordinate num: UInt8) -> UInt8 {
    let value = Int(num) - 4
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "audioSandbox" :*/
fileprivate let data_belowValue:[Character] = ["a"]
fileprivate let show_soulData:[Character] = ["u","d","i","o","S","a","n","d","b","o","x"]

/*: "senderId" :*/
fileprivate let mainFinishValue:[UInt8] = [0xdc,0xca,0xc1,0xcb,0xca,0xdd,0xe6,0xcb]

private func regionKey(situation num: UInt8) -> UInt8 {
    return num ^ 175
}

/*: "AudioMsg" :*/
fileprivate let dataBeardTitle:String = "variation stage slim another appearAudioMsg"

/*: "Can't send blank message" :*/
fileprivate let mainResolutionMsg:String = "Can\'"
fileprivate let constSeparateSignPath:[Character] = ["d"," ","b","l","a","n","k"," ","m","e","s","s","a","g","e"]

/*: "quoteMsgId" :*/
fileprivate let constDeedMsg:[Character] = ["q","u","o","t","e","M","s","g","I","d"]

/*: "quoteMsgInfo" :*/
fileprivate let showProfileId:String = "quotnet"

/*: "contentType" :*/
fileprivate let data_planBalanceKey:[UInt8] = [0x65,0x70,0x79,0x54,0x74,0x6e,0x65,0x74,0x6e,0x6f,0x63]

/*: "ImgMsg" :*/
fileprivate let dataCharmKey:String = "package"
fileprivate let dataCurRejectPath:String = "mgMsgban player clearly normally lens"

/*: "img" :*/
fileprivate let app_greatestPath:String = "ichin"

/*: "imageUri" :*/
fileprivate let show_mapPath:[UInt8] = [0x69,0x72,0x55,0x65,0x67,0x61,0x6d,0x69]

/*: "customSend" :*/
fileprivate let app_thinName:[UInt8] = [0x16,0x0,0x6,0x1,0x1a,0x18,0x26,0x10,0x1b,0x11]

/*: "pic" :*/
fileprivate let kChannelFormat:String = "pisnap"

/*: "VideoMsg" :*/
fileprivate let appHisMapPath:String = "VideoMsgestimated observer anyone agent"

/*: "video" :*/
fileprivate let mainMarginValue:String = "vitailo"

/*: "coverImg" :*/
fileprivate let appExceptionStr:[UInt8] = [0x48,0x54,0x5b,0x4a,0x57,0x2e,0x52,0x4c]

fileprivate func capDoc(course num: UInt8) -> UInt8 {
    let value = Int(num) - 229
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "videoUrl" :*/
fileprivate let kSomebodyUrl:[UInt8] = [0x7d,0x62,0x6f,0x6e,0x64,0x5e,0x79,0x67]

private func representativeFairFashion(doc num: UInt8) -> UInt8 {
    return num ^ 11
}

/*: "duration" :*/
fileprivate let appAlbumPath:[UInt8] = [0x2a,0x3b,0x3c,0x2f,0x3a,0x27,0x21,0x20]

private func shakeWe(plant num: UInt8) -> UInt8 {
    return num ^ 78
}

/*: "len" :*/
fileprivate let noti_allName:[UInt8] = [0xb1,0xaa,0xb3]

fileprivate func reportEvery(clearly num: UInt8) -> UInt8 {
    let value = Int(num) - 69
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Documents/User/voice/" :*/
fileprivate let main_acrossName:[Character] = ["D","o","c","u","m","e","n","t","s","/","U","s","e","r","/","v"]
fileprivate let show_whoStr:[Character] = ["o","i","c","e","/"]

/*: "Documents/User/record/" :*/
fileprivate let showProcessingMsg:[Character] = ["D","o","c","u","m","e"]
fileprivate let const_outlineData:String = "nts/Uselection hundred native lip"
fileprivate let mainNapValue:String = "accept text clean unablecord/"

/*: "msgIncome" :*/
fileprivate let user_betweenUrl:String = "week positive conduct coldmsgInc"
fileprivate let noti_changeTitle:[Character] = ["o","m","e"]

/*: "mfBean" :*/
fileprivate let userSupportPath:[UInt8] = [0x4,0xfd,0xd9,0xfc,0xf8,0x5]

fileprivate func indoorsBefore(vital num: UInt8) -> UInt8 {
    let value = Int(num) - 151
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "messageType" :*/
fileprivate let noti_multiNextStr:String = "MESSA"

/*: "WantGift" :*/
fileprivate let show_plyFormat:String = "WantGifttemporary cap fresh"

/*: _PrivateChatTipsTitle_ :*/
fileprivate let data_bearMsg:String = "_Privhappy mirror earn pattern"
fileprivate let mainWithinTitle:[Character] = ["t","T","i","p"]
fileprivate let app_waitKey:String = "sTitle_distance random place price view"

/*: "msgId" :*/
fileprivate let dataStrokeFormat:[Character] = ["m","s","g","I","d"]

/*: "totalMfCoin" :*/
fileprivate let notiFrontMessage:String = "totalMftransport wire"
fileprivate let userFieldText:String = "resolution own copy observe labCoin"

/*: "attachSysMsg" :*/
fileprivate let kStageMessage:String = "southwest again ethical impulseattachS"
fileprivate let app_containKey:String = "primary mediaysMsg"

/*: "intimatePhoto" :*/
fileprivate let data_capacityMessage:String = "inliteralmat"
fileprivate let kBeatId:String = "military blue research false wealthyePhoto"

/*: "totalIntimate" :*/
fileprivate let constMatchSlideName:[Character] = ["t","o","t","a","l","I","n","t","i"]
fileprivate let noti_borderName:String = "stageate"

/*: "uid" :*/
fileprivate let appSeveralMsg:[UInt8] = [0x16,0xa,0x7]

private func succeedAdjust(procedure num: UInt8) -> UInt8 {
    return num ^ 99
}

/*: "intimate" :*/
fileprivate let main_burstKey:[UInt8] = [0xbe,0xc3,0xc9,0xbe,0xc2,0xb6,0xc9,0xba]

fileprivate func holdTiming(dit num: UInt8) -> UInt8 {
    let value = Int(num) + 171
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Can't use the function due to your lack of intimacy level." :*/
fileprivate let showResMsg:[UInt8] = [0x93,0xb1,0xbe,0xf7,0xa4,0xf0,0xa5,0xa3,0xb5,0xf0,0xa4,0xb8,0xb5,0xf0,0xb6,0xa5,0xbe,0xb3,0xa4,0xb9,0xbf,0xbe,0xf0,0xb4,0xa5,0xb5,0xf0,0xa4,0xbf,0xf0,0xa9,0xbf,0xa5,0xa2,0xf0,0xbc,0xb1,0xb3,0xbb,0xf0,0xbf,0xb6,0xf0,0xb9,0xbe,0xa4,0xb9,0xbd,0xb1,0xb3,0xa9,0xf0,0xbc,0xb5,0xa6,0xb5,0xbc,0xfe]

private func decisionResort(main num: UInt8) -> UInt8 {
    return num ^ 208
}

/*: "giftNum" :*/
fileprivate let app_manageData:String = "giftNuharvest without visitor"
fileprivate let kYetId:[Character] = ["m"]

/*: "pkgItemsetId" :*/
fileprivate let k_masterKey:[Character] = ["p","k","g","I","t","e","m","s","e","t","I"]
fileprivate let data_distributePath:[Character] = ["d"]

/*: "from" :*/
fileprivate let user_natureStr:[Character] = ["f","r","o","m"]

/*: "wantGiftId" :*/
fileprivate let notiRevenueId:String = "wcreationt"

/*: "WantGiftMsg" :*/
fileprivate let const_tooFitJustText:[Character] = ["W","a","n","t","G","i","f"]
fileprivate let data_pauseFormat:String = "tMsghundred under"

/*: "gift" :*/
fileprivate let constBrideFormat:String = "GIFT"

/*: "View info" :*/
fileprivate let userOffBirthKey:[Character] = ["V","i","e","w"," ","i","n","f","o"]

/*: "Notes" :*/
fileprivate let mainCleanTitle:String = "maker allow m reader powNotes"

/*: "Report" :*/
fileprivate let userTransactionData:[Character] = ["R","e","p","o","r"]
fileprivate let const_ehEnhanceValue:[Character] = ["t"]

/*: "View read receipts?" :*/
fileprivate let k_remainMsg:[Character] = ["V","i","e","w"," ","r","e","a","d"," ","r","e","c","e","i","p","t","s","?"]

/*: "She" :*/
fileprivate let mainThanSureMsg:[Character] = ["S","h","e"]

/*: "%@ is using too low a version" :*/
fileprivate let userCompoundData:String = "happy enroll%@ is "
fileprivate let appComePath:String = "accept spot too lo"
fileprivate let app_genderMessage:String = "w a vetarget interrupt"

/*: "Shoot video at least 5 seconds" :*/
fileprivate let mainRemarkMasterName:[Character] = ["S","h","o","o","t"," ","v","i","d","e","o"," ","a","t"," ","l","e"]
fileprivate let k_confirmName:[Character] = ["a","s","t"," ","5"," ","s","e","c","o","n","d","s"]

/*: "Upload failed" :*/
fileprivate let notiLineMethodMsg:[Character] = ["U","p","l","o"]
fileprivate let noti_stallAlterMsg:[Character] = ["a","d"," ","f","a","i","l","e","d"]

/*: "播放状态: :*/
fileprivate let appWriteName:String = "播放状态:"

/*: "c2c_" :*/
fileprivate let mainSpendMsg:String = "image workerc2c_"

/*: "msg" :*/
fileprivate let mainRenderValue:[Character] = ["m","s","g"]

/*: "tips" :*/
fileprivate let noti_amPath:[Character] = ["t","i","p","s"]

/*: "You can use the video chat feature to give %@ a call" :*/
fileprivate let noti_whySpanContent:[UInt8] = [0x3b,0xd,0x17,0x42,0x1,0x3,0xc,0x42,0x17,0x11,0x7,0x42,0x16,0xa,0x7,0x42,0x14,0xb,0x6,0x7,0xd,0x42,0x1,0xa,0x3,0x16,0x42,0x4,0x7,0x3,0x16,0x17,0x10,0x7,0x42,0x16,0xd,0x42,0x5,0xb,0x14,0x7,0x42,0x47,0x22,0x42,0x3,0x42,0x1,0x3,0xe,0xe]

private func missBone(select num: UInt8) -> UInt8 {
    return num ^ 98
}

/*: "cellData" :*/
fileprivate let user_everyUrl:[Character] = ["c","e","l","l","D","a","t"]
fileprivate let const_collectPath:[Character] = ["a"]

/*: "F5F5F5" :*/
fileprivate let k_capitalStr:String = "f"
fileprivate let userMeId:String = "5F5F5panel continue lease"

/*: "System notification" :*/
fileprivate let dataBrushId:[Character] = ["S","y","s","t","e","m"]
fileprivate let k_continuingTitle:String = " notifprocedure auto disk bag extend"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SmallControllerDelegate.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/9.
//

//: import AVFAudio
import AVFAudio
//: import Photos
import Photos
//: import UIKit
import UIKit

/// 私聊入口枚举
//: enum PrivateChatIsFromEnum {
enum EnablestageOthersParaFromEnum {
    //: case Normal
    case Normal // 默认入口
    //: case LiveRoom
    case LiveRoom // 来自直播间（半屏展示）
}

//: class TalkingPrivateChatController: TalkingBaseViewController {
class SmallControllerDelegate: WeddingLovelyRecognizerDelegate {
    //: static var replyLatestTimes: Date = Date(timeIntervalSince1970: 0)
    static var replyLatestTimes: Date = .init(timeIntervalSince1970: 0) // 最新回复时间
    //: var needShowRating = false
    var needShowRating = false // 是否展示app store评分
    //: var quickGreetingTextDict: [String: Any]?
    var quickGreetingTextDict: [String: Any]? // 一键打招呼文本消息
    //: var quickGreetingAudioDict: [String: Any]?
    var quickGreetingAudioDict: [String: Any]? // 一键打招呼语音消息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var conversationType: TalkingIMChatType = .private
    private var conversationType: TalkingIMChatType = .private
    //: private var userWrap = ExecuteBetterTransformable()
    private var userWrap = ExecuteBetterTransformable() // chatInfo模型
    //: private var isFrom = PrivateChatIsFromEnum.Normal
    private var isFrom = EnablestageOthersParaFromEnum.Normal // 私聊入口

    //: private var isShowIntimateView = false
    private var isShowIntimateView = false // 顶部是否已经展示了亲密度头像
    //: var QuickEndBlock: ((_ isSuccess: Bool, _ errCode: Int) -> Void)?
    var QuickEndBlock: ((_ isSuccess: Bool, _ errCode: Int) -> Void)? /// 1.7.8快速打招呼完成后

    //: init(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, userWrap: ExecuteBetterTransformable = ExecuteBetterTransformable()) {
    init(chatID: String, isFrom: EnablestageOthersParaFromEnum = .Normal, userWrap: ExecuteBetterTransformable = ExecuteBetterTransformable()) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: self.msgView.targetID = chatID
        self.msgView.targetID = chatID
        //: self.userWrap = userWrap
        self.userWrap = userWrap
        //: self.msgView.isRecharge = userWrap.isRecharge
        self.msgView.isRecharge = userWrap.isRecharge

        //: TalkingPrivateChatController.replyLatestTimes = Date(timeIntervalSince1970: 0)
        SmallControllerDelegate.replyLatestTimes = Date(timeIntervalSince1970: 0)
        //: TalkingPrivateChatManager.share.readReceiptDict[msgView.targetID] = self.userWrap.readReceiptTime
        NameureThen.share.readReceiptDict[msgView.targetID] = self.userWrap.readReceiptTime
        //: self.isFrom = isFrom
        self.isFrom = isFrom
        //: if msgView.targetID == DepictionThen.getXiaoMiID() {
        if msgView.targetID == DepictionThen.xiao() { // 系统消息
            //: conversationType = .system
            conversationType = .system
            //: hideNavi = false
            hideNavi = false
            //: } else if msgView.targetID == DepictionThen.getCustomerServiceID() {
        } else if msgView.targetID == DepictionThen.noteFamily() { // 官方客服
            //: conversationType = .service
            conversationType = .service
            //: hideNavi = true
            hideNavi = true

            //: } else {
        } else {
            //: hideNavi = true
            hideNavi = true
        }
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: dataMasterMessage.map{galleryEnroll(gap: $0)}, encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if DismissReactiveCompatible.share.appConfigMode.disableShootScreen {
            if DismissReactiveCompatible.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = AeronauticalEngineeringReactiveCompatible(frame: CGRect(x: 0, y: 0, width: main_logStr, height: main_nameData))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: half_showViewAnimation()
        topM()
        //: createUI()
        medium()
        //: createUIConstraints()
        extra()
        //: bindInteraction()
        itemBold()
        //: addTapGestureRecognizer()
        workIn()
        //: func__requestChatInfo()
        anima()
        //: func__getFillDraftText()
        ad()
        //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
        RevenuePlayerDelegate.shared.said()
        //: func__addVoiceNotification()
        makeupInside()
        //: addReadPeceiptNotification()
        quantityoPara()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            strength()
            //: func__TipsNotification()
            sumo()
            //: if DismissReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue {
            if DismissReactiveCompatible.share.loginUserMode.sex == FenceSequence.male.rawValue {
                // 处理男性侧快捷回复逻辑
                //: func__setReplyTipsView()
                publishView()
                // 判断问题回答消息遮罩是否展示
                //: refreshRechargeRequiredView()
                week()
            }
        }
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        compare(isAdd: true)
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        compare(isAdd: false)
        //: if conversationType == .private {
        if conversationType == .private {
            //: func__saveFillDraftText()
            several()
        }
    }

    //: override func viewDidDisappear(_ animated: Bool) {
    override func viewDidDisappear(_ animated: Bool) {
        //: super.viewDidDisappear(animated)
        super.viewDidDisappear(animated)
        // 展示App Store评分
        //: if needShowRating {
        if needShowRating {
            //: AppStoreRatingManager.shared.requestQuickRating()
            CoverRatingManager.shared.duringResConversation()
        }
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    // MARK: - Lazy Load

    //: private lazy var contentView: UIView = {
    private lazy var contentView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .white
        v.backgroundColor = .white
        //: return v
        return v
        //: }()
    }()

    //: lazy var msgView: TUIMessageController = {
    lazy var msgView: HeavyViewController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = HeavyViewController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: msgVC.tableView.register(TalkingChatTextMsgCell.self, forCellReuseIdentifier: TalkingChatTextMsgCell.className())
        msgVC.tableView.register(OutletReactiveCompatible.self, forCellReuseIdentifier: OutletReactiveCompatible.className())
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: TalkingChatImageMsgCell.className())
        msgVC.tableView.register(ActualMessageCell.self, forCellReuseIdentifier: ActualMessageCell.className())
        //: msgVC.tableView.register(TalkingChatVideoMsgCell.self, forCellReuseIdentifier: TalkingChatVideoMsgCell.className())
        msgVC.tableView.register(MateView.self, forCellReuseIdentifier: MateView.className())
        //: msgVC.tableView.register(TalkingChatGiftMsgCell.self, forCellReuseIdentifier: TalkingChatGiftMsgCell.className())
        msgVC.tableView.register(NightView.self, forCellReuseIdentifier: NightView.className())
        //: msgVC.tableView.register(TalkingChatAskForGiftMsgCell.self, forCellReuseIdentifier: TalkingChatAskForGiftMsgCell.className())
        msgVC.tableView.register(SolarViewCell.self, forCellReuseIdentifier: SolarViewCell.className())
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: TalkingChatTipsMsgCell.className())
        msgVC.tableView.register(FirstViewCell.self, forCellReuseIdentifier: FirstViewCell.className())
        //: msgVC.tableView.register(TalkingChatMatchMsgCell.self, forCellReuseIdentifier: TalkingChatMatchMsgCell.className())
        msgVC.tableView.register(RatingSpecialMsgCell.self, forCellReuseIdentifier: RatingSpecialMsgCell.className())
        //: msgVC.tableView.register(TalkingChatAudioMsgCell.self, forCellReuseIdentifier: TalkingChatAudioMsgCell.className())
        msgVC.tableView.register(CompartmentMsgCell.self, forCellReuseIdentifier: CompartmentMsgCell.className())
        //: msgVC.tableView.register(TalkingChatSystemNotifJumpMsgCell.self, forCellReuseIdentifier: TalkingChatSystemNotifJumpMsgCell.className())
        msgVC.tableView.register(SymbolViewCell.self, forCellReuseIdentifier: SymbolViewCell.className())
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: WireView.TSystemMessageCell_ReuseId)
        msgVC.tableView.register(RangeViewCell.self, forCellReuseIdentifier: WireView.TSystemMessageCell_ReuseId)
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType

        //: return msgVC
        return msgVC
        //: }()
    }()

    //: lazy var bgImgeView: UIImageView = {
    lazy var bgImgeView: UIImageView = {
        //: let imgView = UIImageView.init()
        let imgView = UIImageView()
        //: imgView.backgroundColor = .clear
        imgView.backgroundColor = .clear
        //: imgView.contentMode = .scaleAspectFill
        imgView.contentMode = .scaleAspectFill
        //: imgView.clipsToBounds = true
        imgView.clipsToBounds = true
        //: return imgView
        return imgView
        //: }()
    }()

    //: lazy var coverView: UIVisualEffectView = {
    lazy var coverView: UIVisualEffectView = {
        //: let effect = UIBlurEffect.init(style: .dark)
        let effect = UIBlurEffect(style: .dark)
        //: let effectView = UIVisualEffectView.init(effect: effect)
        let effectView = UIVisualEffectView(effect: effect)
        //: effectView.alpha = 0.45
        effectView.alpha = 0.45
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var bgView: UIView = {
    lazy var bgView: UIView = {
        //: let view = UIImageView.init()
        let view = UIImageView()
        //: view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        //: return view
        return view
        //: }()
    }()

    //: private lazy var videoCallManager: TalkingCallMenuManager = {
    private lazy var videoCallManager: RedObjectProtocol = {
        //: let m = TalkingCallMenuManager()
        let m = RedObjectProtocol()
        //: m.uid = msgView.targetID
        m.uid = msgView.targetID
        //: m.onReqestCallErrorBlock = { [weak self] errNo in
        m.onReqestCallErrorBlock = { [weak self] errNo in
            //: guard let self = self,errNo == 101 else { return }
            guard let self = self, errNo == 101 else { return }
            //: self.msgInputView.setHidenFreeMsgTimesView()
            self.msgInputView.followOutCookie()
        }
        //: return m
        return m
        //: }()
    }()

    //: lazy var msgInputView: TalkingChatInputView = {
    lazy var msgInputView: PowderHornInputView = {
        //: var type: InputViewType = .PrivateChat
        var type: EngineeringViewType = .PrivateChat
        //: if conversationType == .service {
        if conversationType == .service {
            //: type = .CustomerChat
            type = .CustomerChat
            //: } else if self.isFrom == .LiveRoom {
        } else if self.isFrom == .LiveRoom {
            //: type = .LiveRoom
            type = .LiveRoom
        }

        //: let view = TalkingChatInputView(type: type)
        let view = PowderHornInputView(type: type)
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedView: UIView = {
    lazy var feedView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedbackBtn: UIButton = {
    lazy var feedbackBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: let attributes = [.foregroundColor: UIColor(hex: "#128CFF")!, .font: UIFont.pingfangFont(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor(hex: (String(user_parName.prefix(7))))!, .font: UIFont.bigGallery(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Feedback".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(noti_blowUrl.suffix(5)) + userTranslationStr.replacingOccurrences(of: "request", with: "k")).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: btn.addTarget(self, action: #selector(feedbackBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(indicatorOf), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    lazy var giftEffectView: TickGenerateView = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = TickGenerateView()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var navView: TalkingChatNavTitleView = {
    lazy var navView: OfNameView = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = OfNameView(type: conversationType)
        //: return navView
        return navView
        //: }()
    }()

    //: lazy var sendGreetBtn: UIButton = {
    lazy var sendGreetBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.bigGallery(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(notiPlayFormat) + mainSampleMsg.uppercased())), for: .normal)
        //: btn.setTitle("Quick Greeting".localized, for: .normal)
        btn.setTitle((String(kBlowStoryPath.prefix(4)) + "k Gre" + data_runnerPath.replacingOccurrences(of: "bind", with: "t") + String(dataQuantityervalExtraUrl)).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.attractiveIn().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(sendGreetBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(layPurchase), for: .touchUpInside)
        //: btn.isHidden = !DismissReactiveCompatible.share.appUserConfigMode.showDirectGreet || DismissReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue || DismissReactiveCompatible.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = !DismissReactiveCompatible.share.appUserConfigMode.showDirectGreet || DismissReactiveCompatible.share.loginUserMode.sex == FenceSequence.male.rawValue || DismissReactiveCompatible.share.appStatus == AfterArtSumLiteral.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var replyTipsView: TalkingPrivateChatReplyTipsView? = {
    private lazy var replyTipsView: CommixTipsView? = {
        //: let view = TalkingPrivateChatReplyTipsView.init()
        let view = CommixTipsView()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    // 文本输入翻译检测
    //: private lazy var selectTranView: TalkingSelectTranslationView = {
    private lazy var selectTranView: LeaseTranslationView = {
        //: let v = TalkingSelectTranslationView()
        let v = LeaseTranslationView()
        //: v.isHidden = true
        v.isHidden = true
        //: v.transBlock = { [weak self] succeed, text in
        v.transBlock = { [weak self] succeed, text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            // 展示翻译结果视图
            //: self.view.endEditing(true)
            self.view.endEditing(true)
            //: self.transResultView.showTranslationResultView(text)
            self.transResultView.detect(text)
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var transResultView: TalkingTranslationResultView = {
    private lazy var transResultView: MedalThen = {
        //: let v = TalkingTranslationResultView()
        let v = MedalThen()
        //: v.isHidden = true
        v.isHidden = true
        //: v.sendBlock = { [weak self] text in
        v.sendBlock = { [weak self] text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            // 发送翻译内容, 清空输入框
            //: self.func__sendTextMsg(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            self.blow(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            //: self.msgInputView.inputTextView.text = ""
            self.msgInputView.inputTextView.text = ""
            //: self.msgInputView.quoteModel = nil
            self.msgInputView.quoteModel = nil
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var beautyPhotosBtn: UIButton = {
    private lazy var beautyPhotosBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.bigGallery(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(notiPlayFormat) + mainSampleMsg.uppercased())), for: .normal)
        //: btn.setTitle("Beauty Photos".localized, for: .normal)
        btn.setTitle((String(main_suiteUrl.suffix(6)) + String(main_transmissionName.suffix(7))).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.attractiveIn().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(beautyPhotosBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(motivationEnable), for: .touchUpInside)
        //: btn.isHidden = DismissReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue || DismissReactiveCompatible.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = DismissReactiveCompatible.share.loginUserMode.sex == FenceSequence.male.rawValue || DismissReactiveCompatible.share.appStatus == AfterArtSumLiteral.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var gamesView: TalkingLiveRoomGamesView = {
    private lazy var gamesView: GrantViewFlowLayout = {
        //: let v = TalkingLiveRoomGamesView()
        let v = GrantViewFlowLayout()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var bannerView: TalkingPrivateBannerView = {
    private lazy var bannerView: TickDataSource = {
        //: let view = TalkingPrivateBannerView.init()
        let view = TickDataSource()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var floatScreenView: TalkingGameFloatingScreenView = {
    private lazy var floatScreenView: PeculiarRecognizerDelegate = { // 飘屏
        //: let v = TalkingGameFloatingScreenView()
        let v = PeculiarRecognizerDelegate()
        //: v.isHidden = (DismissReactiveCompatible.share.appStatus == AppSkinStatus.special.rawValue)
        v.isHidden = (DismissReactiveCompatible.share.appStatus == AfterArtSumLiteral.special.rawValue)
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: v.endPlayBlock = { [weak self] in
        v.endPlayBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: v.isUserInteractionEnabled = false
            v.isUserInteractionEnabled = false
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var notesTopView: PrivateChatNotesTopView = {
    private lazy var notesTopView: RatingVanguardView = { // 备注置顶
        //: let v = PrivateChatNotesTopView()
        let v = RatingVanguardView()
        //: v.tapBlock = { [weak self] in
        v.tapBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.jumpToNotesView()
            self.transfer()
        }
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()

    //: private lazy var rechargeView: RechargeRequiredView = {
    private lazy var rechargeView: RechargeThen = { // 充值限制视图
        //: let v = RechargeRequiredView()
        let v = RechargeThen()
        //: v.tapBlock = { [weak self] in
        v.tapBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if self.userWrap.isRecharge == -1 {
            if self.userWrap.isRecharge == -1 { // 首充优惠
                //: PushThen.share.func_pushToDiscountRechargeView()
                PushThen.share.cherryRed()
                //: } else if self.userWrap.isRecharge == 0 {
            } else if self.userWrap.isRecharge == 0 { // 半屏充值
                //: PushThen.share.func__pushToHalfWebVC(webViewType: .RechargeHalfPage)
                PushThen.share.obligatoEvent(webViewType: .RechargeHalfPage)
            }
        }
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()

    //: private lazy var quickReplayView: QuestionQuickReplayView = {
    private lazy var quickReplayView: EquityThen = { // 问题快捷回复视图
        //: let v = QuestionQuickReplayView()
        let v = EquityThen()
        //: v.tapBlock = { [weak self] model in
        v.tapBlock = { [weak self] model in
            //: guard let self = self else { return }
            guard let self = self else { return }
            // 调用发送消息接口
            //: self.func_sendQuestionQuickReplayMsg(model)
            self.shareWithWindow(model)
        }
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()
}

// MARK: - banner

//: extension TalkingPrivateChatController: PrivateBannerDelegate {
extension SmallControllerDelegate: StateOfMatterThen {
    //: func changeBannerViewFream(isMiniView: Bool) {
    func birdSEyeView(isMiniView: Bool) {
        //: guard self.isFrom == .Normal else { return }
        guard self.isFrom == .Normal else { return }
        //: UIView.animate(withDuration: 0.3) {
        UIView.animate(withDuration: 0.3) {
            //: if isMiniView {
            if isMiniView {
                //: if DismissReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue {
                if DismissReactiveCompatible.share.loginUserMode.sex == FenceSequence.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                    }

                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                    }
                }
                //: } else {
            } else {
                //: if DismissReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue {
                if DismissReactiveCompatible.share.loginUserMode.sex == FenceSequence.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                }
            }
            //: self.bannerView.layoutIfNeeded()
            self.bannerView.layoutIfNeeded()
            //: self.bannerView.resetView(isMiniView: isMiniView)
            self.bannerView.sumoView(isMiniView: isMiniView)
        }
    }
}

// MARK: - 聊天相关

//: extension TalkingPrivateChatController: TMessageControllerDelegate {
extension SmallControllerDelegate: PronatorDisabled {
    /// 一键打招呼
    //: @objc func sendGreetBtnClick() {
    @objc func layPurchase() {
        //: TalkingPrivateChatManager.chat_sendQuickGreeting(toUserId: self.msgView.targetID) { errorCode, errorMsg, textDict, audioDict in
        NameureThen.mortalFrame(toUserId: self.msgView.targetID) { _, errorMsg, textDict, audioDict in
            //: guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
            guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
                //: if errorMsg?.count ?? 0 > 0 {
                if errorMsg?.count ?? 0 > 0 {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorMsg!)
                    self.enablece(showMsg: errorMsg!)
                }
                //: return
                return
            }

            // 发送一键打招呼消息
            //: self.func_sendQuickGreetingMsg(textDict: textDict, audioDict: audioDict)
            self.less(textDict: textDict, audioDict: audioDict)
        }
    }

    /// 图片打招呼
    //: @objc func beautyPhotosBtnClick() {
    @objc func motivationEnable() {
        //: TalkingPrivateChatManager.chat_sendBeautyPhotos(toUserId: self.msgView.targetID) { isPushSet, dict in
        NameureThen.glamorTo(toUserId: self.msgView.targetID) { isPushSet, dict in
            //: if isPushSet {
            if isPushSet {
                //: let vc = TalkingGreetPhotoVC.init()
                let vc = CountenanceRecognizerDelegate()
                //: self.navigationController?.pushViewController(vc, animated: true)
                self.navigationController?.pushViewController(vc, animated: true)
                //: return
                return
            }
            //: if dict != nil {
            if dict != nil {
                //: UIImageView().setUrlImage(urlStr: dict?["url"] as? String ?? "") {[weak self] _, image in
                UIImageView().loadPoiseFinishParkImageEye(urlStr: dict?[(show_sitData.replacingOccurrences(of: "go", with: "l"))] as? String ?? "") { [weak self] _, image in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: if image != nil {
                    if image != nil {
                        //: let unlockGift = dict?["unlockGift"] as? Dictionary<String, Any>
                        let unlockGift = dict?[(noti_twoData.lowercased() + String(show_trendData.suffix(5)))] as? [String: Any]
                        //: let sendModel = AbTalkingChatMsgCustomSendModel()
                        let sendModel = SendModel()
                        //: sendModel.isBurn = String.init(format: "%d", dict?["isBurn"] as? Int ?? 0)
                        sendModel.isBurn = String(format: "%d", dict?[String(bytes: appAttachData.map{insertPole(gold: $0)}, encoding: .utf8)!] as? Int ?? 0)
                        //: sendModel.giftId = unlockGift?["giftId"] as? String ?? "0"
                        sendModel.giftId = unlockGift?[(String(noti_mutualPath))] as? String ?? "0"
                        //: self.func__sendPictureMsg(img: image!, sendModel: sendModel)
                        self.alongProperty(img: image!, sendModel: sendModel)
                        //: return
                        return
                    }
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> SparkCellData! {
    func receive(_: HeavyViewController!, action data: V2TIMMessage!) -> SparkCellData! {
        //: let dic = AcousticPowerThen.parseTXMessageData(data: data.customElem.data)
        let dic = AcousticPowerThen.leaveDoingice(data: data.customElem.data)
        //: var cellData = TUISocialChatManager.handleTXReceiveMessage(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        var cellData = TictacReactiveCompatible.group(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        //: if data.isKind(of: TalkingChatMatchMsgCellData.self), self.userWrap.uid.count > 0 {
        if data.isKind(of: TotalensityLovelyCellData.self), self.userWrap.uid.count > 0 {
            //: cellData?.msgModel.userInfoModel = self.userWrap
            cellData?.msgModel.userInfoModel = self.userWrap
        }

        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: if jsonDic["rYMsgType"].stringValue == "GJ:CallCustom" { // 音视频通话消息
        if jsonDic[(String(user_selectedIsolatePickMessage))].stringValue == (String(main_stillMsg.suffix(4)) + "allCust" + noti_accuracyGraySweetContent.replacingOccurrences(of: "span", with: "m")) { // 音视频通话消息
            //: let isSelf = jsonDic["changeDirection"].boolValue
            let isSelf = jsonDic[(String(mainEarnUniversalMsg) + notiServeMsg.replacingOccurrences(of: "extend", with: "n"))].boolValue
            //: if isSelf == true {
            if isSelf == true { // 消息需要翻转方向
                // 更正当前用户uid
                //: cellData?.msgModel.user.uid = jsonDic["extra"]["msgInfo"]["toUid"].intValue
                cellData?.msgModel.user.uid = jsonDic[(String(data_explainAllowSharedStr))][(String(main_comeToolStr.prefix(7)))][(String(notiSteepFormat))].intValue
                // 更正消息展示方向
                //: let msgModel = cellData!.msgModel
                let msgModel = cellData!.msgModel
                //: cellData = CellReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing:.MsgDirectionIncoming)
                cellData = CellReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData?.msgModel = msgModel
                cellData?.msgModel = msgModel
            }
        }
        //: if cellData?.isKind(of: RangeCellData.self) == true ||
        if cellData?.isKind(of: RangeCellData.self) == true ||
            //: cellData?.isKind(of: EngineeringCellData.self) == true ||
            cellData?.isKind(of: EngineeringCellData.self) == true ||
            //: cellData?.isKind(of: CellReactiveCompatible.self) == true ||
            cellData?.isKind(of: CellReactiveCompatible.self) == true ||
            //: cellData?.isKind(of: NumberensityReactiveCompatible.self) == true {
            cellData?.isKind(of: NumberensityReactiveCompatible.self) == true
        {
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
        }

        //: if let cellData = cellData {
        if let cellData = cellData {
            // 接收解锁 私密消息（照片、视频）礼物，刷新本地消息状态
            //: TalkingPrivateChatManager.chat_onNewMessage_refreshLocalIntimateMsgStatus(cellData: cellData, msgView: self.msgView)
            NameureThen.pointView(cellData: cellData, msgView: self.msgView)
            // 女性记录最新回复时间, 刷新表格
            //: TalkingPrivateChatManager.updateReplyLatestTimeAndReload(cellData.msgIncome, data.timestamp, msgView)
            NameureThen.rewardReload(cellData.msgIncome, data.timestamp, msgView)
            // 男性是否展示快捷回复消息
            //: checkQuestionQuickReplayView()
            region()
        }

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: SparkCellData!) -> WireView! {
    func burnManager(_: HeavyViewController!, layerSuite data: SparkCellData!) -> WireView! {
        // 更新聊天头像，不会更新腾讯数据库
        //: let data = changeChatHeadPic(data: data)
        let data = weekly(data: data)

        //: if data.isKind(of: CellReactiveCompatible.self) {
        if data.isKind(of: CellReactiveCompatible.self) {
            //: let cell = TalkingChatTextMsgCell.init(style: .default, reuseIdentifier: TalkingChatTextMsgCell.className())
            let cell = OutletReactiveCompatible(style: .default, reuseIdentifier: OutletReactiveCompatible.className())
            //: let cellData = data as! CellReactiveCompatible
            let cellData = data as! CellReactiveCompatible

            //: if cellData.msgModel.quoteMsgInfo.isQuoteMsg() {
            if cellData.msgModel.quoteMsgInfo.click() {
                //: if cellData.msgModel.quoteMsgInfo.uid == DismissReactiveCompatible.share.loginUserMode.userID {
                if cellData.msgModel.quoteMsgInfo.uid == DismissReactiveCompatible.share.loginUserMode.userID {
                    //: cellData.msgModel.quoteMsgInfo.sendName = DismissReactiveCompatible.share.loginUserMode.nickname ?? ""
                    cellData.msgModel.quoteMsgInfo.sendName = DismissReactiveCompatible.share.loginUserMode.nickname ?? ""
                    //: }else{
                } else {
                    //: cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                    cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                }
            }
            //: cell.fill(with: cellData)
            cell.namePost(with: cellData)
//            cell.transBlock = { [weak self] in
//                guard let self = self else { return }
//                self.msgView.translateMsg(with: cellData)
//            }
            //: return cell
            return cell
        }

        //: if data.isKind(of: EngineeringCellData.self) {
        if data.isKind(of: EngineeringCellData.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: TalkingChatImageMsgCell.className())
            let cell = ActualMessageCell(style: .default, reuseIdentifier: ActualMessageCell.className())
            //: let cellData = data as! EngineeringCellData
            let cellData = data as! EngineeringCellData
            //: cell.fill(with: cellData)
            cell.namePost(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: NumberensityReactiveCompatible.self) {
        if data.isKind(of: NumberensityReactiveCompatible.self) {
            //: let cell = TalkingChatVideoMsgCell.init(style: .default, reuseIdentifier: TalkingChatVideoMsgCell.className())
            let cell = MateView(style: .default, reuseIdentifier: MateView.className())
            //: let cellData = data as! NumberensityReactiveCompatible
            let cellData = data as! NumberensityReactiveCompatible
            //: cell.fill(with: cellData)
            cell.namePost(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: ResolveCellData.self) {
        if data.isKind(of: ResolveCellData.self) {
            //: let cell = TalkingChatGiftMsgCell.init(style: .default, reuseIdentifier: TalkingChatGiftMsgCell.className())
            let cell = NightView(style: .default, reuseIdentifier: NightView.className())
            //: let cellData = data as! ResolveCellData
            let cellData = data as! ResolveCellData
            //: cell.fill(with: cellData)
            cell.namePost(with: cellData)
            //: return cell
            return cell
        }
        //: if data.isKind(of: RangeThen.self) {
        if data.isKind(of: RangeThen.self) {
            //: let cell = TalkingChatAskForGiftMsgCell.init(style: .default, reuseIdentifier: TalkingChatAskForGiftMsgCell.className())
            let cell = SolarViewCell(style: .default, reuseIdentifier: SolarViewCell.className())
            //: let cellData = data as! RangeThen
            let cellData = data as! RangeThen
            //: cell.fill(with: cellData)
            cell.namePost(with: cellData)
            //: cell.sendBlock = { [weak self] in
            cell.sendBlock = { [weak self] in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: var model = TalkingRoomGiftModel.init()
                var model = TillGiftModel()
                //: model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                //: self.func__sendGiftMsg(giftModel: model, giftNum: "1", cellData: cellData, fromStr: "wantGift")
                self.searchedStr(giftModel: model, giftNum: "1", cellData: cellData, fromStr: (mainFutureBulletData.replacingOccurrences(of: "appear", with: "a") + "Gift"))
            }
            //: return cell
            return cell
        }
        //: if data.isKind(of: RangeCellData.self) {
        if data.isKind(of: RangeCellData.self) {
            //: let cell = TalkingChatAudioMsgCell.init(style: .default, reuseIdentifier: TalkingChatAudioMsgCell.className())
            let cell = CompartmentMsgCell(style: .default, reuseIdentifier: CompartmentMsgCell.className())
            //: let cellData = data as! RangeCellData
            let cellData = data as! RangeCellData
            //: cell.fill(with: cellData)
            cell.namePost(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatMatchMsgCellData.self) {
        if data.isKind(of: TotalensityLovelyCellData.self) {
            //: let cell = TalkingChatMatchMsgCell.init(style: .default, reuseIdentifier: TalkingChatMatchMsgCell.className())
            let cell = RatingSpecialMsgCell(style: .default, reuseIdentifier: RatingSpecialMsgCell.className())
            //: let cellData = data as! TalkingChatMatchMsgCellData
            let cellData = data as! TotalensityLovelyCellData
            //: if self.userWrap.uid.count > 0 {
            if self.userWrap.uid.count > 0 {
                //: cellData.msgModel.userInfoModel = self.userWrap
                cellData.msgModel.userInfoModel = self.userWrap
            }
            //: cell.fill(with: cellData)
            cell.namePost(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatTipsMsgCellData.self) {
        if data.isKind(of: TransferCellData.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: TalkingChatTipsMsgCell.className())
            let cell = FirstViewCell(style: .default, reuseIdentifier: FirstViewCell.className())
            //: let cellData = data as! TalkingChatTipsMsgCellData
            let cellData = data as! TransferCellData
            //: cell.fill(with: cellData)
            cell.namePost(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatSystemNotifJumpMsgCellData.self) {
        if data.isKind(of: ContainerReactiveCompatible.self) {
            //: let cell = TalkingChatSystemNotifJumpMsgCell.init(style: .default, reuseIdentifier: TalkingChatSystemNotifJumpMsgCell.className())
            let cell = SymbolViewCell(style: .default, reuseIdentifier: SymbolViewCell.className())
            //: let cellData = data as! TalkingChatSystemNotifJumpMsgCellData
            let cellData = data as! ContainerReactiveCompatible
            //: cell.fill(with: cellData)
            cell.namePost(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: WireView!) {
    func white(_: HeavyViewController!, phoneMessageAvatar cell: WireView!) {
        //: guard self.isFrom != .LiveRoom,
        guard self.isFrom != .LiveRoom,
              //: conversationType != .system,
              conversationType != .system,
              //: String(cell.messageData?.msgModel.user.uid ?? 0) != DepictionThen.getCustomerServiceID() else { return }
              String(cell.messageData?.msgModel.user.uid ?? 0) != DepictionThen.noteFamily() else { return }

        //: if let uid = cell.messageData?.msgModel.user.uid {
        if let uid = cell.messageData?.msgModel.user.uid {
            //: PushThen.share.func__pushToUserDetailVC(uid: String(uid))
            PushThen.share.sessionSomebody(uid: String(uid))
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: WireView!) {
    func dimension(_: HeavyViewController!, waveAngle cell: WireView!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: ActualMessageCell.self) { // 图片
            //: let imageCellData = cell.messageData as! EngineeringCellData
            let imageCellData = cell.messageData as! EngineeringCellData
            //: if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 {
            if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 { // 是私密照片
                // 已销毁的图片不能点击
                //: guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                // 展示私密照片视图
                //: let vc = TalkingIntimatePhotoPreviewVC(imageCellData: imageCellData, toUid: self.msgView.targetID)
                let vc = ImpressionPreviewVc(imageCellData: imageCellData, toUid: self.msgView.targetID)
                // 更改本地私密照片状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimatePhotoMsg(imageCellData, photoModel: model)
                    self.msgView.background(imageCellData, color: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.boardMember(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: dataBeautyKey)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.subView(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.snake(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                self.controller()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 普通图片
                //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
                let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!

                //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
                var filePath = (libraryPath as NSString).appendingPathComponent(userStyleFormat)
                //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
                filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

                //: var model = TalkingMomentPhotoModel.init()
                var model = DemonstrateModelType()
                //: if FileManager.default.fileExists(atPath: filePath) {
                if FileManager.default.fileExists(atPath: filePath) {
                    //: let data = NSData.init(contentsOfFile: filePath)
                    let data = NSData(contentsOfFile: filePath)
                    //: model.imagePic = UIImage(data: data! as Data)
                    model.imagePic = UIImage(data: data! as Data)
                    //: } else {
                } else {
                    //: if imageCellData.imageData != nil {
                    if imageCellData.imageData != nil {
                        //: model.imagePic = imageCellData.imageData
                        model.imagePic = imageCellData.imageData
                        //: } else {
                    } else {
                        //: model.imgUrl =  imageCellData.imageUrl
                        model.imgUrl = imageCellData.imageUrl
                    }
                }
                //: var currenModel = TalkingMomentModel.init()
                var currenModel = IceModelType()
                //: var tempArr: [TalkingMomentPhotoModel] = []
                var tempArr: [DemonstrateModelType] = []
                //: tempArr.append(model)
                tempArr.append(model)
                //: currenModel.pic = tempArr
                currenModel.pic = tempArr
                //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
                let vc = RegisterThen(momentModel: currenModel, index: 0, type: .normal)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                self.controller()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: MateView.self) { // 视频
            //: let videoCellData = cell.messageData as! NumberensityReactiveCompatible
            let videoCellData = cell.messageData as! NumberensityReactiveCompatible
            //: let videoModel = videoCellData.msgModel.msgInfo.video
            let videoModel = videoCellData.msgModel.msgInfo.video
            //: if (videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0") { // 普通视频
            if videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0" { // 普通视频
                // 优先使用本地视频，没有再使用网络
                //: var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                //: if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                    //: videoPath = videoModel.videoUrl
                    videoPath = videoModel.videoUrl
                }

                //: let vc = TalkingMomentVideoVC.init(videoPath: videoPath)
                let vc = NightRecognizerDelegate(videoPath: videoPath)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                self.controller()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 私密视频
                // 已销毁的视频不能点击
                //: guard videoModel.burnStatus != 1 else { return }
                guard videoModel.burnStatus != 1 else { return }
                // 预览私密视频
                //: let vc = TalkingIntimateVideoPreviewVC(videoCellData: videoCellData, toUid: self.msgView.targetID)
                let vc = EndThen(videoCellData: videoCellData, toUid: self.msgView.targetID)
                // 更改本地私密视频状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimateVideoMsg(videoCellData, videoModel: model)
                    self.msgView.method(videoCellData, gravitationalForce: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.boardMember(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: dataBeautyKey)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.subView(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.snake(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                self.controller()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: CompartmentMsgCell.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! CompartmentMsgCell
            //: func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: audioCell.cellData as! RangeCellData, cellView: audioCell)
            commentEnableicial(cellData: audioCell.cellData as! RangeCellData, cellView: audioCell)

            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: OutletReactiveCompatible.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! OutletReactiveCompatible
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = AcousticPowerThen.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = AcousticPowerThen.leaveDoingice(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(String(data_explainAllowSharedStr))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(data_guideFormat.prefix(7)))) {
                //: let truePersonAuth = extraDic["jumpKey"] as? String ?? ""
                let truePersonAuth = extraDic[(String(data_guideFormat.prefix(7)))] as? String ?? ""
                //: if truePersonAuth == "truePersonAuth" {
                if truePersonAuth == (String(main_elementWhiteLieData) + notiSouthwestText.replacingOccurrences(of: "ignore", with: "on") + "Auth") {
                    //: PushThen.share.func__pushUserVerifyController(toast: nil)
                    PushThen.share.undercoverOperation(toast: nil)
                    //: return
                    return
                }
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<ArmModelType>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: PushThen.share.func__actionPushForMessage(jumpModel: jumpModel)
                    PushThen.share.orientationAction(jumpModel: jumpModel)
                }
            }

            //: let extraJson = JSON(extraDic)
            let extraJson = JSON(extraDic)
            //: if  dic["rYMsgType"] as? String == "GJ:CallCustom" {  // 音视频通话消息
            if dic[(String(user_selectedIsolatePickMessage))] as? String == (String(main_stillMsg.suffix(4)) + "allCust" + noti_accuracyGraySweetContent.replacingOccurrences(of: "span", with: "m")) { // 音视频通话消息
                //: if extraJson["msgInfo"]["logType"].intValue == 1 {
                if extraJson[(String(main_comeToolStr.prefix(7)))][(String(main_marginFormat))].intValue == 1 {
                    //: checkAndCallVoice()
                    unused()
                    //: } else {
                } else {
                    //: checkAndCallVideo()
                    blockVideo()
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: FirstViewCell.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! FirstViewCell
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = AcousticPowerThen.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = AcousticPowerThen.leaveDoingice(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(String(data_explainAllowSharedStr))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(data_guideFormat.prefix(7)))) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<ArmModelType>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: PushThen.share.func__actionPushForMessage(jumpModel: jumpModel)
                    PushThen.share.orientationAction(jumpModel: jumpModel)
                }
            }
            //: } else if cell.isKind(of: TalkingChatMatchMsgCell.self) {
        } else if cell.isKind(of: RatingSpecialMsgCell.self) {
            //: PushThen.share.func__pushToUserDetailVC(uid: msgView.targetID)
            PushThen.share.sessionSomebody(uid: msgView.targetID)
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: WireView!) {
    func first(_: HeavyViewController!, plus cell: WireView!) {
        //: var cellData: TalkingChatMsgBaseCellData?
        var cellData: AfterwardsReactiveCompatible?
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: ActualMessageCell.self) {
            //: let imageCell = cell as! TalkingChatImageMsgCell
            let imageCell = cell as! ActualMessageCell
            //: cellData = imageCell.cellData as! EngineeringCellData
            cellData = imageCell.cellData as! EngineeringCellData
            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: MateView.self) {
            //: let videoCell = cell as! TalkingChatVideoMsgCell
            let videoCell = cell as! MateView
            //: cellData = videoCell.cellData as! NumberensityReactiveCompatible
            cellData = videoCell.cellData as! NumberensityReactiveCompatible
            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: CompartmentMsgCell.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! CompartmentMsgCell
            //: cellData = audioCell.cellData as! RangeCellData
            cellData = audioCell.cellData as! RangeCellData
            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: OutletReactiveCompatible.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! OutletReactiveCompatible
            //: cellData = textCell.cellData as! CellReactiveCompatible
            cellData = textCell.cellData as! CellReactiveCompatible
        }
        //: if cellData != nil {
        if cellData != nil {
            //: let reCellData: SparkCellData? = TUISocialChatManager.handleTXMessageReSend(withData: cellData!)
            let reCellData: SparkCellData? = TictacReactiveCompatible.enableInvitation(withData: cellData!)
            //: if reCellData != nil {
            if reCellData != nil {
                //: cell.messageData?.msgModel = reCellData!.msgModel
                cell.messageData?.msgModel = reCellData!.msgModel
                //: cell.messageData?.status = .Msg_Status_Fail
                cell.messageData?.status = .Msg_Status_Fail
                //: if reCellData!.isKind(of: NumberensityReactiveCompatible.self) {
                if reCellData!.isKind(of: NumberensityReactiveCompatible.self) { // 视频重发
                    //: func__sendVideoMsg(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    periodOfTime(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    //: } else {
                } else {
                    //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
                    male(msgData: cell.messageData!, reSend: true)
                }
            }
        }
    }

    /// 回复消息
    //: func messageController(_ controller: TUIMessageController!, onReplyMessage cellData: SparkCellData!) {
    func send(_: HeavyViewController!, pullUp cellData: SparkCellData!) {
        //: msgInputView.quoteModel = TalkingPrivateChatManager.handleReplyMsg(cellData: cellData, targetId: userWrap.uid)
        msgInputView.quoteModel = NameureThen.barrier(cellData: cellData, targetId: userWrap.uid)
    }

    /// 点击“引用”消息
    //: func messageController(_ controller: TUIMessageController!, onClickReplyMessage cellData: SparkCellData!) {
    func fragment(_: HeavyViewController!, pressDoing cellData: SparkCellData!) {
        //: TalkingPrivateChatManager.handleClickReplyMsg(cellData: cellData, targetId: userWrap.uid)
        NameureThen.personality(cellData: cellData, targetId: userWrap.uid)
    }

    /// 实时更新聊天双方头像
    /// - Parameter data: 腾讯数据
    /// - Returns: 腾讯数据
    //: private func changeChatHeadPic(data: SparkCellData) -> SparkCellData {
    private func weekly(data: SparkCellData) -> SparkCellData {
        //: let isSelf = data.msgModel.user.uid == Int(DismissReactiveCompatible.share.loginUserMode.userID)
        let isSelf = data.msgModel.user.uid == Int(DismissReactiveCompatible.share.loginUserMode.userID)
        //: if isSelf {
        if isSelf {
            //: if DismissReactiveCompatible.share.loginUserMode.headPic?.count ?? 0 > 0 {
            if DismissReactiveCompatible.share.loginUserMode.headPic?.count ?? 0 > 0 {
                //: data.msgModel.user.headPic = DismissReactiveCompatible.share.loginUserMode.headPic!
                data.msgModel.user.headPic = DismissReactiveCompatible.share.loginUserMode.headPic!
                //: data.msgModel.user.headPicFrame = DismissReactiveCompatible.share.loginUserMode.headPicFrame
                data.msgModel.user.headPicFrame = DismissReactiveCompatible.share.loginUserMode.headPicFrame
            }
            //: } else {
        } else {
            //: if self.userWrap.headPic.count > 0 {
            if self.userWrap.headPic.count > 0 {
                //: data.msgModel.user.headPic = self.userWrap.headPic
                data.msgModel.user.headPic = self.userWrap.headPic
                //: data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
                data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
            }
        }
        //: return data
        return data
    }
}

// MARK: - 发送消息

//: extension TalkingPrivateChatController {
extension SmallControllerDelegate {
    /// 发送问题回复消息 -- 回答
    /// - Parameter model: 回答问题模型
    //: func func_sendQuestionQuickReplayMsg(_ model: QuestionQuickReplayModel) {
    func shareWithWindow(_ model: MasterClassModel) {
        //: let msgInfo = ["content": model.content, "msgType": "txt"]
        let msgInfo = [String(bytes: const_protectionValue.reversed(), encoding: .utf8)!: model.content, String(bytes: user_justiceValue.reversed(), encoding: .utf8)!: (String(showFeedOutlineStr))]
        //: let paramsDic = ["message": model.content, "replayId": model.replayId, "type": "TxtMsg"] as [String : Any]
        let paramsDic = [String(bytes: app_generalPath.map{sweepEnjoy(fun: $0)}, encoding: .utf8)!: model.content, String(bytes: main_smartFormat.map{indicatorScene(stream: $0)}, encoding: .utf8)!: model.replayId, (dataShStr.replacingOccurrences(of: "gap", with: "t")): (String(show_segmentCookieKey))] as [String: Any]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(main_comeToolStr.prefix(7))): msgInfo, String(bytes: showFileName.map{totaloPure(magnitude: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = TictacReactiveCompatible.smooth(withExtral: dic, isSender: true, isTip: false) {
            //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
            self.male(msgData: cellData, reSend: false)
        }
    }

    /// 发送一键打招呼消息
    //: func func_sendSayHiMsg() {
    func gravity() {
        //: let msgInfo = ["msgType": "txt"]
        let msgInfo = [String(bytes: user_justiceValue.reversed(), encoding: .utf8)!: (String(showFeedOutlineStr))]
        //: let paramsDic = ["type": "MultiQuickMsg"]
        let paramsDic = [(dataShStr.replacingOccurrences(of: "gap", with: "t")): (show_startBassMessage.replacingOccurrences(of: "league", with: "M") + String(show_produceMsg.suffix(6)) + "ickMsg")]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(main_comeToolStr.prefix(7))): msgInfo, String(bytes: showFileName.map{totaloPure(magnitude: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = TictacReactiveCompatible.smooth(withExtral: dic, isSender: true, isTip: false) {
            //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
            self.male(msgData: cellData, reSend: false)
        }
    }

    /// 发送快速打招呼消息
    //: func func_sendQuickGreetingMsg() {
    func thumbGreeting() {
        //: let msgInfo = ["msgType": "txt"]
        let msgInfo = [String(bytes: user_justiceValue.reversed(), encoding: .utf8)!: (String(showFeedOutlineStr))]
        //: let paramsDic = ["type": "QuickMsg"]
        let paramsDic = [(dataShStr.replacingOccurrences(of: "gap", with: "t")): (String(data_normalPath))]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(main_comeToolStr.prefix(7))): msgInfo, String(bytes: showFileName.map{totaloPure(magnitude: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = TictacReactiveCompatible.smooth(withExtral: dic, isSender: true, isTip: false) {
            //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
            self.male(msgData: cellData, reSend: false)
        }
    }

    /// 发送一键打招呼消息
    /// - Parameters:
    ///   - textDict: 文本消息
    ///   - audioDict: 语音消息
    //: private func func_sendQuickGreetingMsg(textDict: [String: Any]?, audioDict: [String: Any]?) {
    private func less(textDict: [String: Any]?, audioDict: [String: Any]?) {
        //: if textDict?.count ?? 0 > 0 {
        if textDict?.count ?? 0 > 0 { // 发送文本消息
            //: let content = textDict?["content"]
            let content = textDict?[String(bytes: const_protectionValue.reversed(), encoding: .utf8)!]
            //: let greetId = textDict?["id"]
            let greetId = textDict?["id"]
            //: let msgInfo = ["content": content, "txtId": greetId, "msgType": "txt"]
            let msgInfo = [String(bytes: const_protectionValue.reversed(), encoding: .utf8)!: content, (String(notiMethodFormat)): greetId, String(bytes: user_justiceValue.reversed(), encoding: .utf8)!: (String(showFeedOutlineStr))]
            //: let paramsDic = ["message": content, "txtId": greetId, "type": "TxtMsg"]
            let paramsDic = [String(bytes: app_generalPath.map{sweepEnjoy(fun: $0)}, encoding: .utf8)!: content, (String(notiMethodFormat)): greetId, (dataShStr.replacingOccurrences(of: "gap", with: "t")): (String(show_segmentCookieKey))]
            //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
            let dic = [(String(main_comeToolStr.prefix(7))): msgInfo, String(bytes: showFileName.map{totaloPure(magnitude: $0)}, encoding: .utf8)!: paramsDic]
            //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
            if let cellData = TictacReactiveCompatible.smooth(withExtral: dic, isSender: true, isTip: false) {
                //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                self.male(msgData: cellData, reSend: false)
            }
        }

        //: if audioDict?.count ?? 0 > 0 {
        if audioDict?.count ?? 0 > 0 { // 发送语音消息
            //: VoiceDownloadTaskManager.shared.addAudioGreetDownLoadTask(audioDict!) { model in
            SampleTaskDelegate.shared.anDirect(audioDict!) { model in
                //: if model.taskTypeId != .oneKeyGreet {
                if model.taskTypeId != .oneKeyGreet {
                    //: return
                    return
                }
                //: if model.taskStatus != .finish {
                if model.taskStatus != .finish {
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.func__showStatusBarErrorMsg(showMsg: "Voice hello download failed. Procedure")
                        self.enablece(showMsg: String(bytes: appHolderUrl.map{fatherPause(re: $0)}, encoding: .utf8)!)
                        //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.msgView.targetID, isHave: false)
                        NameureThen.operationBy(toUserId: self.msgView.targetID, isHave: false)
                    }
                    //: return
                    return
                }
                //: let greetId = audioDict?["id"]
                let greetId = audioDict?["id"]
                //: let length = audioDict?["length"]
                let length = audioDict?[(String(const_turnContent))]
                //: let msgInfo = ["msgType": "audio",
                let msgInfo = [String(bytes: user_justiceValue.reversed(), encoding: .utf8)!: (constCanId.replacingOccurrences(of: "somebody", with: "i")),
                               //: "audioId": greetId,
                               (show_indexAgentMsg.replacingOccurrences(of: "glass", with: "ud") + String(kMeetingText.prefix(2))): greetId,
                               //: "audioLength": length,
                               String(bytes: constLookData.map{jumpingOnPlace(coordinate: $0)}, encoding: .utf8)!: length,
                               //: "audioSandbox": model.unZipFilePath,
                               (String(data_belowValue) + String(show_soulData)): model.unZipFilePath,
                               //: "senderId": DismissReactiveCompatible.share.loginUserMode.userID]
                               String(bytes: mainFinishValue.map{regionKey(situation: $0)}, encoding: .utf8)!: DismissReactiveCompatible.share.loginUserMode.userID]

                //: let paramsDic = ["audioId": greetId,
                let paramsDic = [(show_indexAgentMsg.replacingOccurrences(of: "glass", with: "ud") + String(kMeetingText.prefix(2))): greetId,
                                 //: "audioLength": length,
                                 String(bytes: constLookData.map{jumpingOnPlace(coordinate: $0)}, encoding: .utf8)!: length,
                                 //: "audioSandbox": model.unZipFilePath,
                                 (String(data_belowValue) + String(show_soulData)): model.unZipFilePath,
                                 //: "type": "AudioMsg"]
                                 (dataShStr.replacingOccurrences(of: "gap", with: "t")): (String(dataBeardTitle.suffix(8)))]
                //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
                let dic = [(String(main_comeToolStr.prefix(7))): msgInfo, String(bytes: showFileName.map{totaloPure(magnitude: $0)}, encoding: .utf8)!: paramsDic]
                //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
                if let cellData = TictacReactiveCompatible.smooth(withExtral: dic, isSender: true, isTip: false) {
                    //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                    self.male(msgData: cellData, reSend: false)
                }
            }
        }
    }

    /// 发送文本消息
    /// - Parameter msgStr: 文本
    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func blow(msgStr: String, quoteModel: LightPrimeModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            enablece(showMsg: (mainResolutionMsg + "t sen" + String(constSeparateSignPath)).localized)
            //: return
            return
        }

        //: let msgInfo = ["content": temStr, "msgType": "txt"]
        let msgInfo = [String(bytes: const_protectionValue.reversed(), encoding: .utf8)!: temStr, String(bytes: user_justiceValue.reversed(), encoding: .utf8)!: (String(showFeedOutlineStr))]
        //: var paramsDic = ["message": temStr, "type": "TxtMsg"]
        var paramsDic = [String(bytes: app_generalPath.map{sweepEnjoy(fun: $0)}, encoding: .utf8)!: temStr, (dataShStr.replacingOccurrences(of: "gap", with: "t")): (String(show_segmentCookieKey))]
        //: var dic:[String: Any] = ["msgInfo": msgInfo]
        var dic: [String: Any] = [(String(main_comeToolStr.prefix(7))): msgInfo]

        //: if let tempQuoteModel = quoteModel{
        if let tempQuoteModel = quoteModel {
            //: paramsDic["quoteMsgId"] = tempQuoteModel.msgId
            paramsDic[(String(constDeedMsg))] = tempQuoteModel.msgId
            //: dic["quoteMsgInfo"] = tempQuoteModel.toDictionary()
            dic[(showProfileId.replacingOccurrences(of: "net", with: "e") + "MsgInfo")] = tempQuoteModel.toDictionary()
        }
        //: dic["params"] = paramsDic
        dic[String(bytes: showFileName.map{totaloPure(magnitude: $0)}, encoding: .utf8)!] = paramsDic

        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = TictacReactiveCompatible.smooth(withExtral: dic, isSender: true, isTip: false) {
            //: cellData.msgModel.quoteMsgInfo = quoteModel ?? AbTalkingChatMsgQuoteModel()
            cellData.msgModel.quoteMsgInfo = quoteModel ?? LightPrimeModel()
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            male(msgData: cellData, reSend: false)
        }
    }

    /// 发送图片消息
    /// - Parameters:
    ///   - img: 图片
    ///   - sendModel: 本地数据模型
    //: private func func__sendPictureMsg(img: UIImage, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func alongProperty(img: UIImage, sendModel: SendModel) {
        //: let imagePath = EngineeringCellData.getChatImagePath(image: img)
        let imagePath = EngineeringCellData.getImpressionImageChatPath(image: img)
        // 缓存本地的消息
        //: let sendDict = sendModel.toDictionary() ?? [:]
        let sendDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath, "customSend": sendDict]
        let msgInfo: [String: Any] = [String(bytes: data_planBalanceKey.reversed(), encoding: .utf8)!: (dataCharmKey.replacingOccurrences(of: "package", with: "I") + String(dataCurRejectPath.prefix(5))), String(bytes: user_justiceValue.reversed(), encoding: .utf8)!: (app_greatestPath.replacingOccurrences(of: "chin", with: "mg")), String(bytes: show_mapPath.reversed(), encoding: .utf8)!: imagePath, String(bytes: app_thinName.map{$0^117}, encoding: .utf8)!: sendDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(kChannelFormat.replacingOccurrences(of: "snap", with: "c")): imagePath,
                                        //: "type": "ImgMsg",
                                        (dataShStr.replacingOccurrences(of: "gap", with: "t")): (dataCharmKey.replacingOccurrences(of: "package", with: "I") + String(dataCurRejectPath.prefix(5))),
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: appAttachData.map{insertPole(gold: $0)}, encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(noti_mutualPath)): sendModel.giftId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(main_comeToolStr.prefix(7))): msgInfo, String(bytes: showFileName.map{totaloPure(magnitude: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = TictacReactiveCompatible.smooth(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            male(msgData: cellData, reSend: false)
        }
    }

    /// 发送视频消息
    /// - Parameters:
    ///   - msgData: 发送视频cellData；nil：上传流程，有值 ：发送流程
    ///   - sendModel: 视频消息自定义本地模型
    //: private func func__sendVideoMsg(msgData: SparkCellData?, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func periodOfTime(msgData: SparkCellData?, sendModel: SendModel) {
        // 缓存本地的消息
        //: let videoDict = sendModel.toDictionary() ?? [:]
        let videoDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "VideoMsg", "msgType": "video", "customSend": videoDict]
        let msgInfo: [String: Any] = [String(bytes: data_planBalanceKey.reversed(), encoding: .utf8)!: (String(appHisMapPath.prefix(8))), String(bytes: user_justiceValue.reversed(), encoding: .utf8)!: (mainMarginValue.replacingOccurrences(of: "tail", with: "de")), String(bytes: app_thinName.map{$0^117}, encoding: .utf8)!: videoDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["type": "VideoMsg",
        let paramsDic: [String: Any] = [(dataShStr.replacingOccurrences(of: "gap", with: "t")): (String(appHisMapPath.prefix(8))),
                                        //: "coverImg": sendModel.coverImg,
                                        String(bytes: appExceptionStr.map{capDoc(course: $0)}, encoding: .utf8)!: sendModel.coverImg,
                                        //: "videoUrl": sendModel.videoUrl,
                                        String(bytes: kSomebodyUrl.map{representativeFairFashion(doc: $0)}, encoding: .utf8)!: sendModel.videoUrl,
                                        //: "duration": sendModel.duration,
                                        String(bytes: appAlbumPath.map{shakeWe(plant: $0)}, encoding: .utf8)!: sendModel.duration,
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: appAttachData.map{insertPole(gold: $0)}, encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(noti_mutualPath)): sendModel.giftId]
        //: let dict = ["msgInfo": msgInfo, "params": paramsDic]
        let dict = [(String(main_comeToolStr.prefix(7))): msgInfo, String(bytes: showFileName.map{totaloPure(magnitude: $0)}, encoding: .utf8)!: paramsDic]

        // 压缩成功 ---> 创建cellData ---> 上传视频
        //: guard let cellData = msgData else {
        guard let cellData = msgData else {
            //: if let uploadCellData = TUISocialChatManager.handleTXMessageSend(withExtral: dict, isSender: true, isTip: false) {
            if let uploadCellData = TictacReactiveCompatible.smooth(withExtral: dict, isSender: true, isTip: false) {
                //: self.msgView.sendCustomMessage(uploadCellData)
                self.msgView.contrast(uploadCellData)
                //: self.cos_UploadVideo(msgData: uploadCellData, sendModel: sendModel)
                self.split(msgData: uploadCellData, sendModel: sendModel)
            }
            //: return
            return
        }

        // 失败重试，展示加载动画
        //: if cellData.status != .Msg_Status_Sending_2 {
        if cellData.status != .Msg_Status_Sending_2 {
            //: self.msgView.changeCustomMsg(cellData, status: .Msg_Status_Sending_2)
            self.msgView.face(cellData, appBy: .Msg_Status_Sending_2)
        }

        // 失败重试上传视频流程
        //: guard sendModel.videoUrl.count > 0 else {
        guard sendModel.videoUrl.count > 0 else {
            //: self.cos_UploadVideo(msgData: cellData, sendModel: sendModel)
            self.split(msgData: cellData, sendModel: sendModel)
            //: return
            return
        }

        // 修改本地消息数据，调用发送视频接口
        //: self.msgView.modifyCustomMessageData(withExtral: dict, cellData: cellData)
        self.msgView.turbinateBoneData(dict, filter: cellData)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        male(msgData: cellData, reSend: false)
    }

    /// 发送语音
    /// - Parameters:
    ///   - voicePath: 语音路径
    ///   - voiceTime: 时间
    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {
    func exceptFile(voicePath: String, voiceTime: Int) {
        //: let msgInfo = ["msgType": "audio",
        let msgInfo = [String(bytes: user_justiceValue.reversed(), encoding: .utf8)!: (constCanId.replacingOccurrences(of: "somebody", with: "i")),
                       //: "audioLength": "\(voiceTime)",
                       String(bytes: constLookData.map{jumpingOnPlace(coordinate: $0)}, encoding: .utf8)!: "\(voiceTime)",
                       //: "audioSandbox": voicePath,
                       (String(data_belowValue) + String(show_soulData)): voicePath,
                       //: "senderId": DismissReactiveCompatible.share.loginUserMode.userID]
                       String(bytes: mainFinishValue.map{regionKey(situation: $0)}, encoding: .utf8)!: DismissReactiveCompatible.share.loginUserMode.userID]

        //: let paramsDic = ["audioSandbox": voicePath,
        let paramsDic = [(String(data_belowValue) + String(show_soulData)): voicePath,
                         //: "type": "AudioMsg",
                         (dataShStr.replacingOccurrences(of: "gap", with: "t")): (String(dataBeardTitle.suffix(8))),
                         //: "len": "\(voiceTime)"]
                         String(bytes: noti_allName.map{reportEvery(clearly: $0)}, encoding: .utf8)!: "\(voiceTime)"]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(main_comeToolStr.prefix(7))): msgInfo, String(bytes: showFileName.map{totaloPure(magnitude: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = TictacReactiveCompatible.smooth(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            male(msgData: cellData, reSend: false)
        }
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: SparkCellData, reSend: Bool) {
    private func male(msgData: SparkCellData, reSend _: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            enablece(showMsg: main_textStr)
            //: return
            return
        }

        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
        //: params["toUid"] = self.msgView.targetID
        params[(String(notiSteepFormat))] = self.msgView.targetID

        // 发送视频消息，索要礼物消息，快速打招呼消息，不在此处插入
        //: if msgData.isKind(of: NumberensityReactiveCompatible.self) == false && msgData.isKind(of: RangeThen.self) == false && params["type"] as? String != "QuickMsg" && params["type"] as? String != "MultiQuickMsg" {
        if msgData.isKind(of: NumberensityReactiveCompatible.self) == false, msgData.isKind(of: RangeThen.self) == false, params[(dataShStr.replacingOccurrences(of: "gap", with: "t"))] as? String != (String(data_normalPath)), params[(dataShStr.replacingOccurrences(of: "gap", with: "t"))] as? String != (show_startBassMessage.replacingOccurrences(of: "league", with: "M") + String(show_produceMsg.suffix(6)) + "ickMsg") {
            //: self.msgView.sendCustomMessage(msgData)
            self.msgView.contrast(msgData)
        }
//        let recordTime = AcousticPowerThen.getMessageInsertTime()

        //: if params.keys .contains("pic") { // 图片
        if params.keys.contains((kChannelFormat.replacingOccurrences(of: "snap", with: "c"))) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(kChannelFormat.replacingOccurrences(of: "snap", with: "c"))] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(kChannelFormat.replacingOccurrences(of: "snap", with: "c"))] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }
        //: let type  = params["type"] as! String
        let type = params[(dataShStr.replacingOccurrences(of: "gap", with: "t"))] as! String
        //: if type == "AudioMsg" { // 语音
        if type == (String(dataBeardTitle.suffix(8))) { // 语音
            //: if params.keys.contains("audioSandbox") {
            if params.keys.contains((String(data_belowValue) + String(show_soulData))) {
                //: var path =  params["audioSandbox"] as! String
                var path = params[(String(data_belowValue) + String(show_soulData))] as! String
                //: params.removeValue(forKey: "audioSandbox")
                params.removeValue(forKey: (String(data_belowValue) + String(show_soulData)))

                //: let theFileName = (path as NSString).lastPathComponent
                let theFileName = (path as NSString).lastPathComponent

                //: if path.contains("Documents/User/voice/") {
                if path.contains((String(main_acrossName) + String(show_whoStr))) {
                    //: path = Talking_Chat_voice_SandboxPath + theFileName
                    path = const_voiceMsg + theFileName
                    //: } else if(path.contains("Documents/User/record/")) {
                } else if path.contains((String(showProcessingMsg) + String(const_outlineData.prefix(5)) + "ser/re" + String(mainNapValue.suffix(5)))) {
                    //: path = Talking_Chat_record_SandboxPath + theFileName
                    path = app_searchContent + theFileName
                }

                //: let audioId = params["audioId"] ?? "0"
                let audioId = params[(show_indexAgentMsg.replacingOccurrences(of: "glass", with: "ud") + String(kMeetingText.prefix(2)))] ?? "0"
                //: if  (audioId as! NSString).integerValue == 0 {
                if (audioId as! NSString).integerValue == 0 {
                    //: do {
                    do {
                        //: let voiceData = try Data.init(contentsOf: URL.init(fileURLWithPath: path))
                        let voiceData = try Data(contentsOf: URL(fileURLWithPath: path))
                        //: params["audio"] = voiceData
                        params[(constCanId.replacingOccurrences(of: "somebody", with: "i"))] = voiceData
                        //: } catch {
                    } catch {}
                }
            }
        }

        // 发送消息接口
        //: TalkingChatRequestTool.req_SendPrivete(param: params, completion: { succeed, result, errorModel in
        TamperRequestTool.destinationIt(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: if type == "QuickMsg" || type == "MultiQuickMsg" {
                if type == (String(data_normalPath)) || type == (show_startBassMessage.replacingOccurrences(of: "league", with: "M") + String(show_produceMsg.suffix(6)) + "ickMsg") {
                    //: self.QuickEndBlock?(false, errorModel?.errorCode ?? 0)
                    self.QuickEndBlock?(false, errorModel?.errorCode ?? 0)
                }
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.usanceLeftHandednessNetIncreaseThin(msgData, present: nil, push: NSNumber(value: 0), direction: .Msg_Status_Fail)
                
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickMessagesendButton)
                self.boardMember(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: mainOnMsg)
                //: if params.keys.contains("txtId") {
                if params.keys.contains((String(notiMethodFormat))) {
                    //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.msgView.targetID, isHave: false)
                    NameureThen.operationBy(toUserId: self.msgView.targetID, isHave: false)
                }
                /// 打招呼达到设定次数失败时
                //: if errorModel!.errorCode == 10015 {
                if errorModel!.errorCode == 10015 {
                    //: self.msgView.deleteCustomMessage(msgData)
                    self.msgView.filterAmericanism(msgData)
                }
                //: return
                return
            }
            //: var updateMsgData = msgData
            var updateMsgData = msgData
            //: let dataDict = result as! Dictionary<String, Any>
            let dataDict = result as! [String: Any]
            //: var msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
            var msgInfo = dataDict[(String(main_comeToolStr.prefix(7)))] as! [String: Any]

            //: var msgIncome: NSNumber = NSNumber.init(value: 0)
            var msgIncome: NSNumber = .init(value: 0)
            //: if msgInfo.keys.contains("msgIncome") {
            if msgInfo.keys.contains((String(user_betweenUrl.suffix(6)) + String(noti_changeTitle))) {
                //: msgIncome = msgInfo["msgIncome"] as! NSNumber
                msgIncome = msgInfo[(String(user_betweenUrl.suffix(6)) + String(noti_changeTitle))] as! NSNumber
            }

            // 直播时回复消息，积分更改
            //: if TalkingLiveManager.shared().isLive && msgIncome.floatValue > 0 {
            if ManagerPushListener.nowPanel().isLive && msgIncome.floatValue > 0 {
                //: let mfBean = Double(truncating: msgIncome)
                let mfBean = Double(truncating: msgIncome)
                //: NotificationCenter.default.post(name: LIVE_USER_POINT_CHANGE, object: nil, userInfo: ["mfBean": mfBean])
                NotificationCenter.default.post(name: k_succeedUrl, object: nil, userInfo: [String(bytes: userSupportPath.map{indoorsBefore(vital: $0)}, encoding: .utf8)!: mfBean])
            }
            /// 亲密度
            //: self.sendMsgIntimateTool(msgInfo: msgInfo)
            self.flatBridge(msgInfo: msgInfo)

            /// 索要礼物，快速打招呼
            //: let messageType = msgInfo["messageType"] as? Int
            let messageType = msgInfo[(noti_multiNextStr.lowercased() + "geType")] as? Int
            //: if messageType == 25 || type == "QuickMsg" || type == "MultiQuickMsg" {
            if messageType == 25 || type == (String(data_normalPath)) || type == (show_startBassMessage.replacingOccurrences(of: "league", with: "M") + String(show_produceMsg.suffix(6)) + "ickMsg") {
                //: if messageType == 25 {
                if messageType == 25 {
                    //: msgInfo["msgType"] = "WantGift"
                    msgInfo[String(bytes: user_justiceValue.reversed(), encoding: .utf8)!] = (String(show_plyFormat.prefix(8)))
                    //: uploadRecord.uploadRecordEvent(eventID: clickMessageDetailGiftPopAskBtnSusType)
                    const_pathMsg.tabKeep(eventID: main_sinceMsg)
                    //: } else if type == "QuickMsg" || type == "MultiQuickMsg" {
                } else if type == (String(data_normalPath)) || type == (show_startBassMessage.replacingOccurrences(of: "league", with: "M") + String(show_produceMsg.suffix(6)) + "ickMsg") {
                    //: msgInfo["msgType"] = "txt"
                    msgInfo[String(bytes: user_justiceValue.reversed(), encoding: .utf8)!] = (String(showFeedOutlineStr))
                    //: self.QuickEndBlock?(true, errorModel?.errorCode ?? 0)
                    self.QuickEndBlock?(true, errorModel?.errorCode ?? 0)
                }
                //: let dic = ["msgInfo": msgInfo]
                let dic = [(String(main_comeToolStr.prefix(7))): msgInfo]
                //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
                if let cellData = TictacReactiveCompatible.smooth(withExtral: dic, isSender: true, isTip: false) {
                    //: updateMsgData = cellData
                    updateMsgData = cellData
                    //: self.msgView.sendCustomMessage(updateMsgData)
                    self.msgView.contrast(updateMsgData)
                }
            }

            // 更新提示文案
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
            //: if self.rechargeView.isHidden {
            if self.rechargeView.isHidden {
                //: if DismissReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue &&
                if DismissReactiveCompatible.share.loginUserMode.sex == FenceSequence.male.rawValue,
                   //: type != "QuickMsg" &&
                   type != (String(data_normalPath)),
                   //: type != "MultiQuickMsg" {
                   type != (show_startBassMessage.replacingOccurrences(of: "league", with: "M") + String(show_produceMsg.suffix(6)) + "ickMsg")
                {
                    //: let key =  "\(DismissReactiveCompatible.share.loginUserMode.userID)_PrivateChatTipsTitle_\(String(self.msgView.targetID))"
                    let key = "\(DismissReactiveCompatible.share.loginUserMode.userID)" + (String(data_bearMsg.prefix(5)) + "ateCha" + String(mainWithinTitle) + String(app_waitKey.prefix(7))) + "\(String(self.msgView.targetID))"
                    //: UserDefaults.standard.set(true, forKey: key)
                    UserDefaults.standard.set(true, forKey: key)
                    //: self.removeReplyTipsView()
                    self.resSearch()
                }
            }
            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(updateMsgData, msgId: msgInfo["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
            self.msgView.usanceLeftHandednessNetIncreaseThin(updateMsgData, present: msgInfo[(String(dataStrokeFormat))] as? String, push: msgIncome, direction: .Msg_Status_Succ)
            
            // 女性记录最新回复时间, 刷新表格
            //: TalkingPrivateChatManager.updateReplyLatestTimeAndReload(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)
            NameureThen.rewardReload(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)

            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((String(notiFrontMessage.prefix(7)) + String(userFieldText.suffix(4)))) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(String(notiFrontMessage.prefix(7)) + String(userFieldText.suffix(4)))] as! NSNumber
                //: DismissReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                DismissReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }

            /// 免费消息提示
            //: if dataDict.keys.contains("attachSysMsg") {
            if dataDict.keys.contains((String(kStageMessage.suffix(7)) + String(app_containKey.suffix(5)))) {
                //: let attachSysMsg: String = dataDict["attachSysMsg"] as? String ?? ""
                let attachSysMsg: String = dataDict[(String(kStageMessage.suffix(7)) + String(app_containKey.suffix(5)))] as? String ?? ""
                //: if attachSysMsg.count>0 {
                if attachSysMsg.count > 0 {
                    //: self.func__privateAttachSys_insertTipMsg(str: attachSysMsg)
                    self.opendDoor(str: attachSysMsg)
                }
            }

            //: if msgInfo.keys.contains("intimatePhoto") { // 发送成功私密照片 => model，更新本地消息
            if msgInfo.keys.contains((data_capacityMessage.replacingOccurrences(of: "literal", with: "ti") + String(kBeatId.suffix(6)))) { // 发送成功私密照片 => model，更新本地消息
                //: let intimatePhotoDict = msgInfo["intimatePhoto"] as! [String: Any]
                let intimatePhotoDict = msgInfo[(data_capacityMessage.replacingOccurrences(of: "literal", with: "ti") + String(kBeatId.suffix(6)))] as! [String: Any]
                //: if let model = try? AbTalkingChatMsgIntimatePhotoModel(dictionary: intimatePhotoDict) {
                if let model = try? RootMemberModel(dictionary: intimatePhotoDict) {
                    //: updateMsgData.msgModel.msgInfo.intimatePhoto = model
                    updateMsgData.msgModel.msgInfo.intimatePhoto = model
                    //: self.msgView.changeIntimatePhotoMsg(updateMsgData, photoModel: model)
                    self.msgView.background(updateMsgData, color: model)
                }

                //: } else if msgInfo.keys.contains("video") { // 发送成功私密视频 => model，更新本地消息
            } else if msgInfo.keys.contains((mainMarginValue.replacingOccurrences(of: "tail", with: "de"))) { // 发送成功私密视频 => model，更新本地消息
                //: let intimateVideoDict = msgInfo["video"] as! [String: Any]
                let intimateVideoDict = msgInfo[(mainMarginValue.replacingOccurrences(of: "tail", with: "de"))] as! [String: Any]
                //: if let model = try? AbTalkingChatMsgVideoModel(dictionary: intimateVideoDict) {
                if let model = try? HistoricalModel(dictionary: intimateVideoDict) {
                    //: updateMsgData.msgModel.msgInfo.video = model
                    updateMsgData.msgModel.msgInfo.video = model
                    //: self.msgView.changeIntimateVideoMsg(updateMsgData, videoModel: model)
                    self.msgView.method(updateMsgData, gravitationalForce: model)
                }
            }
            //: })
        })

        // 判断是否需要插入音视频提示消息
        //: func__privateChatVideo_insertTipMsg()
        capture()

        //: updateSession()
        plySession()
    }

    /// 建立会话标识，本地更新数据和会话列表
    //: func updateSession() {
    func plySession() {
        //: DBUserInfoManager.cache_updateSessionAndPostNotif(with: msgView.targetID)
        CutInfoManager.fromEachDoinge(with: msgView.targetID)
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool, clickEvent: String) {
    private func boardMember(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool, clickEvent: String) {
        //: if errorCode == 50203 {
        if errorCode == 50203 { // 余额不足
            //: guard DismissReactiveCompatible.share.loginUserMode.status != 1 else {
            guard DismissReactiveCompatible.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    enablece(showMsg: errorStr)
                }
                //: return
                return
            }

            //: PushThen.share.func__jumpToWebRecharge(clickEvent: clickEvent, sufficient: false)
            PushThen.share.twoParams(clickEvent: clickEvent, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.maneuver()
            //: DismissReactiveCompatible.share.loginUserMode.freeMsgTimes = ""
            DismissReactiveCompatible.share.loginUserMode.freeMsgTimes = ""
            //: msgInputView.setHidenFreeMsgTimesView()
            msgInputView.followOutCookie()
//            insertTipMsg_Nobalance(tipStr: errorStr)
            //: } else if errorCode == 10888 {
        } else if errorCode == 10888 { // vip订阅弹窗
            //: let view = TalkingLiveVipSubscribePopView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = DisenableThen(frame: CGRect(x: 0, y: 0, width: main_logStr, height: main_nameData))
            //: view.show()
            view.animaShow()
            //: } else if errorCode == 60003, !DismissReactiveCompatible.share.loginUserMode.isNaUser {
        } else if errorCode == 60003, !DismissReactiveCompatible.share.loginUserMode.isNaUser {
            /// 真人认证提示弹窗
            //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
            OutletWindowManager.shared.totaleraction()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                enablece(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 亲密度

//: extension TalkingPrivateChatController {
extension SmallControllerDelegate {
    /// 解锁图片视频送礼亲密度
    //: private func unlockPhotoOrVideoTool(dataDict: Dictionary<String, Any>) {
    private func snake(dataDict: [String: Any]) {
        //: let msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
        let msgInfo = dataDict[(String(main_comeToolStr.prefix(7)))] as! [String: Any]
        /// 亲密度
        //: self.sendMsgIntimateTool(msgInfo: msgInfo)
        self.flatBridge(msgInfo: msgInfo)
    }

    /// 初次展示导航栏的亲密度头像
    //: private func showFirstIntimateView() {
    private func angstrom() {
        //: if self.userWrap.intimate >= DismissReactiveCompatible.share.appUserConfigMode.intimateLimit.chatTitleEffect && conversationType == .private  {
        if self.userWrap.intimate >= DismissReactiveCompatible.share.appUserConfigMode.intimateLimit.chatTitleEffect, conversationType == .private {
            //: self.isShowIntimateView = true
            self.isShowIntimateView = true
            //: navView.private_updateIntimateNavView(userModel: userWrap)
            navView.countRoute(userModel: userWrap)
            /// 初次展示动效
            //: let key = "\(DismissReactiveCompatible.share.loginUserMode.userID)_\(String(self.msgView.targetID))"
            let key = "\(DismissReactiveCompatible.share.loginUserMode.userID)_\(String(self.msgView.targetID))"
            //: var intimateCacheData = (Defaults.array(forKey: TalkingFirstShowIntimateGuide)) as? [String] ?? []
            var intimateCacheData = (data_partyText.array(forKey: user_intervalPath)) as? [String] ?? []
            //: if !intimateCacheData.contains(key) {
            if !intimateCacheData.contains(key) {
                //: navView.playAnimation()
                navView.equidistant()
                //: intimateCacheData.append(key)
                intimateCacheData.append(key)
                //: Defaults.set(intimateCacheData, forKey: TalkingFirstShowIntimateGuide)
                data_partyText.set(intimateCacheData, forKey: user_intervalPath)
            }
        }
    }

    /// 收到亲密度消息
    //: @objc func newMsgIntimate(notification: NSNotification) {
    @objc func ex(notification: NSNotification) {
        //: let data = notification.object as! V2TIMMessage
        let data = notification.object as! V2TIMMessage
        //: let dic = AcousticPowerThen.parseTXMessageData(data: data.customElem.data)
        let dic = AcousticPowerThen.leaveDoingice(data: data.customElem.data)
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let extra = jsonDic["extra"].dictionary
        let extra = jsonDic[(String(data_explainAllowSharedStr))].dictionary
        //: let msgInfo = extra?["msgInfo"]?.dictionary
        let msgInfo = extra?[(String(main_comeToolStr.prefix(7)))]?.dictionary
        //: if data.sender == self.msgView.targetID {
        if data.sender == self.msgView.targetID {
            //: if let totalIntimate = msgInfo?["totalIntimate"]?.int {
            if let totalIntimate = msgInfo?[(String(constMatchSlideName) + noti_borderName.replacingOccurrences(of: "stage", with: "m"))]?.int {
                //: if self.conversationType == .private {
                if self.conversationType == .private {
                    //: let time = data.timestamp.timeIntervalSince1970
                    let time = data.timestamp.timeIntervalSince1970
                    //: self.intimateTool(currenIntimate: totalIntimate, time: Int(time))
                    self.listen(currenIntimate: totalIntimate, time: Int(time))
                }
            }
        }
    }

    /// 发送消息时
    //: private func sendMsgIntimateTool(msgInfo: Dictionary<String, Any>) {
    private func flatBridge(msgInfo: [String: Any]) {
        //: if msgInfo.keys.contains("totalIntimate") && self.conversationType == .private {
        if msgInfo.keys.contains((String(constMatchSlideName) + noti_borderName.replacingOccurrences(of: "stage", with: "m"))), self.conversationType == .private {
            //: let totalIntimacy = msgInfo["totalIntimate"] as? Int ?? 0
            let totalIntimacy = msgInfo[(String(constMatchSlideName) + noti_borderName.replacingOccurrences(of: "stage", with: "m"))] as? Int ?? 0
            //: self.intimateTool(currenIntimate: totalIntimacy)
            self.listen(currenIntimate: totalIntimacy)
        }
    }

    /// 亲密度操作
    //: func intimateTool(currenIntimate: Int, time: Int = 0) {
    func listen(currenIntimate: Int, time: Int = 0) {
        //: guard currenIntimate > 0 else { return }
        guard currenIntimate > 0 else { return }
        //: let model = DBUserInfoManager.cache_getCachedUserInfo(targetId: self.msgView.targetID)
        let model = CutInfoManager.frontId(targetId: self.msgView.targetID)
        //: self.userWrap.intimate = currenIntimate
        self.userWrap.intimate = currenIntimate
        /// 满足最低条件，展示页面
        //: if currenIntimate >= DismissReactiveCompatible.share.appUserConfigMode.intimateLimit.chatTitleEffect {
        if currenIntimate >= DismissReactiveCompatible.share.appUserConfigMode.intimateLimit.chatTitleEffect {
            /// 亲密度增加
            //: if !self.isShowIntimateView {
            if !self.isShowIntimateView {
                //: self.isShowIntimateView = true
                self.isShowIntimateView = true
                //: navView.private_updateIntimateNavView(userModel: self.userWrap)
                navView.countRoute(userModel: self.userWrap)
            }
            //: if currenIntimate - (model?.intimate ?? 0) > 0 {
            if currenIntimate - (model?.intimate ?? 0) > 0 {
                //: self.navView.playIntimateAnim(intimate: currenIntimate - (model?.intimate ?? 0))
                self.navView.befog(intimate: currenIntimate - (model?.intimate ?? 0))
                /// 播放完毕
                //: self.navView.animEndBlock = { [weak self] in
                self.navView.animEndBlock = { [weak self] in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: self.navView.setAllData(intimate: currenIntimate)
                    self.navView.originLevel(intimate: currenIntimate)
                }
                //: } else {
            } else {
                //: self.navView.setAllData(intimate: currenIntimate)
                self.navView.originLevel(intimate: currenIntimate)
            }
        }

        //: let currTime = time > 0 ? time: Int(Date().timeIntervalSince1970)
        let currTime = time > 0 ? time : Int(Date().timeIntervalSince1970)
        //: DBUserInfoManager.cache_updateIntimate(with: msgView.targetID, intimate: currenIntimate, currTime: currTime)
        CutInfoManager.exampleYear(with: msgView.targetID, intimate: currenIntimate, currTime: currTime)
        //: let userInfo = ["uid": self.msgView.targetID ?? "",
        let userInfo = [String(bytes: appSeveralMsg.map{succeedAdjust(procedure: $0)}, encoding: .utf8)!: self.msgView.targetID ?? "",
                        //: "intimate": currenIntimate] as [String : Any]
                        String(bytes: main_burstKey.map{holdTiming(dit: $0)}, encoding: .utf8)!: currenIntimate] as [String: Any]
        //: NotificationCenter.default.post(name: MSGLIST_UPDATE_INTIMATE_NOTIFICATION,
        NotificationCenter.default.post(name: app_barLogId,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: userInfo)
                                        userInfo: userInfo)
    }
}

// MARK: - 输入框

//: extension TalkingPrivateChatController: ChatInputViewDelegate {
extension SmallControllerDelegate: FirstReactiveCompatible {
    /// 检测是否需要展示翻译入口
    //: func func_inputContentIntervalOneSecond() {
    func assignSave() {
        //: guard DismissReactiveCompatible.share.loginUserMode.sex == Gender.female.rawValue else { return }
        guard DismissReactiveCompatible.share.loginUserMode.sex == FenceSequence.female.rawValue else { return }
        //: let isEnglish = selectTranView.checkInputLanguage(self.msgInputView.inputTextView.text)
        let isEnglish = selectTranView.expostulation(self.msgInputView.inputTextView.text)
        //: self.selectTranView.isHidden = isEnglish
        self.selectTranView.isHidden = isEnglish
    }

    //: func func__selectMentionUser(range: NSRange) {}
    func dialogDownFlat(range _: NSRange) {}

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func liveBy(actionType: DevoteMagnitudeLiteral) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= DismissReactiveCompatible.share.appUserConfigMode.intimateLimit.sendImg {
                if self.userWrap.intimate >= DismissReactiveCompatible.share.appUserConfigMode.intimateLimit.sendImg {
                    //: func__choicePhotos()
                    form()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    tempStr()
                }
                //: } else {
            } else {
                //: func__choicePhotos()
                form()
            }
        //: case .VideoCall:
        case .VideoCall: // 音/视频通话
            //: var videoStr: String?
            var videoStr: String?
            //: var voiceStr: String?
            var voiceStr: String?
            //: if DismissReactiveCompatible.share.loginUserMode.sex == Gender.female.rawValue {
            if DismissReactiveCompatible.share.loginUserMode.sex == FenceSequence.female.rawValue {
                //: videoStr = self.userWrap.videoBean
                videoStr = self.userWrap.videoBean
                //: voiceStr = self.userWrap.voiceBean
                voiceStr = self.userWrap.voiceBean
                //: } else {
            } else {
                //: if DismissReactiveCompatible.share.loginUserMode.loungePlus {
                if DismissReactiveCompatible.share.loginUserMode.loungePlus { // 男性会员
                    //: videoStr = self.userWrap.videoVIPPrice
                    videoStr = self.userWrap.videoVIPPrice
                    //: voiceStr = self.userWrap.voiceVIPPrice
                    voiceStr = self.userWrap.voiceVIPPrice
                    //: } else {
                } else {
                    //: videoStr = self.userWrap.videoPrice
                    videoStr = self.userWrap.videoPrice
                    //: voiceStr = self.userWrap.voicePrice
                    voiceStr = self.userWrap.voicePrice
                }
            }
            //: if self.userWrap.intimate >= DismissReactiveCompatible.share.appUserConfigMode.intimateLimit.requestCall {
            if self.userWrap.intimate >= DismissReactiveCompatible.share.appUserConfigMode.intimateLimit.requestCall {
                //: videoCallManager.showVideoCallMenu(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                videoCallManager.quickClick(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                //: } else {
            } else {
                //: showIntimateErrorStr()
                tempStr()
            }

        //: case .ShortVideo:
        case .ShortVideo: // 短视频
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= DismissReactiveCompatible.share.appUserConfigMode.intimateLimit.sendVideo {
                if self.userWrap.intimate >= DismissReactiveCompatible.share.appUserConfigMode.intimateLimit.sendVideo {
                    //: func__sendShortVideo()
                    manager()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    tempStr()
                }
                //: } else {
            } else {
                //: func__sendShortVideo()
                manager()
            }

        //: case .Gift: break
        case .Gift: break

        //: case .Game:
        case .Game: // 游戏
            //: gamesView.showView(from: .PrivateChat)
            gamesView.crushRequireInmate(from: .PrivateChat)
        }
    }

    //: private func showIntimateErrorStr() {
    private func tempStr() {
        //: self.func__showStatusBarErrorMsg(showMsg: "Can't use the function due to your lack of intimacy level.".localized)
        self.enablece(showMsg: String(bytes: showResMsg.map{decisionResort(main: $0)}, encoding: .utf8)!.localized)
    }

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {}
    func bestowmentModel(giftModel _: TillGiftModel, giftNum _: String, model _: SparkTransformable) {}

    // 聊天面板送礼
    /// - Parameters:
    ///   - giftModel: 礼物模型
    ///   - giftNum: 礼物数量
    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func hello(giftModel: TillGiftModel, giftNum: String) {
        //: func__sendGiftMsg(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
        searchedStr(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, cellData: RangeThen?, fromStr: String?) {
    func searchedStr(giftModel: TillGiftModel, giftNum: String, cellData: RangeThen?, fromStr: String?) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            enablece(showMsg: main_textStr)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] = self.msgView.targetID
        params[(String(notiSteepFormat))] = self.msgView.targetID
        //: params["giftId"] = giftModel.gid
        params[(String(noti_mutualPath))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(app_manageData.prefix(6)) + String(kYetId))] = giftNum
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(k_masterKey) + String(data_distributePath))] = giftModel.pkgItemsetId
        }
        //: if fromStr != nil {
        if fromStr != nil {
            //: params["from"] = fromStr
            params[(String(user_natureStr))] = fromStr
        }
        // 送礼接口
        //: TalkingChatRequestTool.req_SendGiftPrivete(param: params, completion: { succeed, result, errorModel in
        TamperRequestTool.underSendReply(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickSendingiftsButton)
                self.boardMember(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: showLimitTitle)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.localRoad(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: true)
            self.subView(extral: result as! [String: Any?], isFromInputView: true)
            //: self.updateSession()
            self.plySession()
            /// 亲密度
            //: let extralInfo = result as! Dictionary<String, Any?>
            let extralInfo = result as! [String: Any?]
            //: let msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
            let msgInfoDic: [String: Any] = extralInfo[(String(main_comeToolStr.prefix(7)))] as! [String: Any]
            //: if msgInfoDic.keys.contains("totalIntimate") && self.conversationType == .private {
            if msgInfoDic.keys.contains((String(constMatchSlideName) + noti_borderName.replacingOccurrences(of: "stage", with: "m"))), self.conversationType == .private {
                //: let totalIntimacy = msgInfoDic["totalIntimate"] as? Int ?? 0
                let totalIntimacy = msgInfoDic[(String(constMatchSlideName) + noti_borderName.replacingOccurrences(of: "stage", with: "m"))] as? Int ?? 0
                //: self.intimateTool(currenIntimate: totalIntimacy)
                self.listen(currenIntimate: totalIntimacy)
            }
            /// 索要礼物送礼
            //: if cellData != nil {
            if cellData != nil {
                // 修改状态
                //: self.msgView.changeIntimateGiftMsg(cellData)
                self.msgView.judge(cellData)
                //: uploadRecord.uploadRecordEvent(eventID: ClickmessageDetailaskMsgsendBtn)
                const_pathMsg.tabKeep(eventID: kButtonKey)
            }
            //: })
        })
    }

    /// 索要礼物
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func bubble(giftModel: TillGiftModel, giftNum _: String) {
        //: uploadRecord.uploadRecordEvent(eventID: ClickMessageDetailGiftPopAskBtnAllType)
        const_pathMsg.tabKeep(eventID: noti_domainId)
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            enablece(showMsg: main_textStr)
            //: return
            return
        }
        //: guard self.userWrap.intimate >= DismissReactiveCompatible.share.appUserConfigMode.intimateLimit.wantGift else {
        guard self.userWrap.intimate >= DismissReactiveCompatible.share.appUserConfigMode.intimateLimit.wantGift else {
            //: showIntimateErrorStr()
            tempStr()
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["wantGiftId"] = giftModel.gid
        params[(notiRevenueId.replacingOccurrences(of: "creation", with: "an") + "GiftId")] = giftModel.gid
        //: params["type"] = "WantGiftMsg"
        params[(dataShStr.replacingOccurrences(of: "gap", with: "t"))] = (String(const_tooFitJustText) + String(data_pauseFormat.prefix(4)))

        //: let msgInfo = ["msgType": "WantGift"]
        let msgInfo = [String(bytes: user_justiceValue.reversed(), encoding: .utf8)!: (String(show_plyFormat.prefix(8)))]
        //: let dic = ["msgInfo": msgInfo, "params": params]
        let dic = [(String(main_comeToolStr.prefix(7))): msgInfo, String(bytes: showFileName.map{totaloPure(magnitude: $0)}, encoding: .utf8)!: params]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = TictacReactiveCompatible.smooth(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            male(msgData: cellData, reSend: false)
        }
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func tillNarrow(heightToBottom: CGFloat) {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.snp.updateConstraints { make in
            contentView.snp.updateConstraints { make in
                //: make.top.equalTo(HalfViewTopMargin-heightToBottom)
                make.top.equalTo(showFileTitle - heightToBottom)
            }
        }
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.input(true)
            self.msgView.input(true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func bottom(height: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.input(true)
            self.msgView.input(true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func countRecognizer(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.countRecognizer(tap)
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: let location = tap.location(ofTouch: 0, in: self.contentView)
            let location = tap.location(ofTouch: 0, in: self.contentView)
            //: if !contentView.point(inside: location, with: nil) {
            if !contentView.point(inside: location, with: nil) {
                // 点击列表以外区域，移除视图（包括聊天列表）
                //: half_dismissViewAnimation(true)
                mp(true)
            }
        }
    }

    /// 开始录音操作
    //: func func__starRecordBtnDown() {
    func presentation() {
        //: if TalkingVoiceMsgPlayManager.shared.playStatus == .Playing {
        if RevenuePlayerDelegate.shared.playStatus == .Playing {
            //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
            RevenuePlayerDelegate.shared.visitor()
        }
    }
}

// MARK: - 动效

//: extension TalkingPrivateChatController {
extension SmallControllerDelegate {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, isFromInputView: Bool) {
    func subView(extral: [String: Any?], isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(String(main_comeToolStr.prefix(7)))] as! [String: Any]
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[String(bytes: user_justiceValue.reversed(), encoding: .utf8)!] = (constBrideFormat.lowercased())
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(main_comeToolStr.prefix(7)))] = msgInfoDic

        //: let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = TictacReactiveCompatible.smooth(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.contrast(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((String(user_betweenUrl.suffix(6)) + String(noti_changeTitle))) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(String(user_betweenUrl.suffix(6)) + String(noti_changeTitle))] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.usanceLeftHandednessNetIncreaseThin(cellData, present: msgInfoDic[(String(dataStrokeFormat))] as? String, push: msgIncome, direction: .Msg_Status_Succ)
        
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any])
        shadeDic(giftDic: extral as [String: Any])
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((String(notiFrontMessage.prefix(7)) + String(userFieldText.suffix(4)))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(String(notiFrontMessage.prefix(7)) + String(userFieldText.suffix(4)))] as! NSNumber
            //: DismissReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            DismissReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: DismissReactiveCompatible.share.loginUserMode.mf_coin)
            msgInputView.giftView.path(needReload: false, mf_coin: DismissReactiveCompatible.share.loginUserMode.mf_coin)
        }
    }

    //: func initCachEffectData() {
    func strength() {
        //: giftEffectView.initCachAnimatData(tagetID: msgView.targetID)
        giftEffectView.thenMakeupId(tagetID: msgView.targetID)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>) {
    func shadeDic(giftDic: [String: Any]) {
        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.addressReloadDict(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func diskArr(giftArr: [Any]) {
        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.framework(modelArr: giftArr)
    }

    //: @objc func getChatVCTargetId() ->String {
    @objc func inmateGetTarget() -> String {
        //: return msgView.targetID
        return msgView.targetID
    }
}

// MARK: - 音视频通话Event

//: extension TalkingPrivateChatController {
extension SmallControllerDelegate {
    /// 检查权限后发起音频通话
    //: func checkAndCallVoice() {
    func unused() {
        //: videoCallManager.checkAndCallVoice {[weak self] succeed in
        videoCallManager.shortAccount { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.plySession()
        }
    }

    /// 检测权限后发起视频通话
    //: func checkAndCallVideo() {
    func blockVideo() {
        //: videoCallManager.checkAndCallVideo {[weak self] succeed in
        videoCallManager.charm { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.plySession()
        }
    }
}

// MARK: - Event

//: extension TalkingPrivateChatController {
extension SmallControllerDelegate {
    //: @objc private func feedbackBtnClick() {
    @objc private func indicatorOf() {
        //: let vc = TalkingFeedbackVC.init()
        let vc = ProgViewController()
        //: getNavigationController()?.pushViewController(vc, animated: true)
        controller()?.pushViewController(vc, animated: true)
    }

    /// 更多按钮事件
    //: private func moreBtnClick() {
    private func glass() {
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = DoingcePopView(frame: self.view.frame)
        //: vc.initwithList(cellTitleList: ["View info".localized, "Notes".localized, "Report".localized])
        vc.reasonPostList(cellTitleList: [(String(userOffBirthKey)).localized, (String(mainCleanTitle.suffix(5))).localized, (String(userTransactionData) + String(const_ehEnhanceValue)).localized])
        //: vc.cancelColor = .appTitleColor()
        vc.cancelColor = .dialog()
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: PushThen.share.func__pushToUserDetailVC(uid: self.msgView.targetID)
                PushThen.share.sessionSomebody(uid: self.msgView.targetID)

                //: } else if index == 1 {
            } else if index == 1 { // 备注
                //: jumpToNotesView()
                transfer()

                //: } else if index == 2 {
            } else if index == 2 { // 举报
                //: self.msgInputView.showOtherInputView = true
                self.msgInputView.showOtherInputView = true
                //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.msgView.targetID)
                let reportView = PositionView(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.msgView.targetID)
                //: reportView.showReportViewIn(view: nil)
                reportView.priceBlack(view: nil)
                //: reportView.reportHideBlock = { _ in
                reportView.reportHideBlock = { _ in
                    //: self.msgInputView.showOtherInputView = false
                    self.msgInputView.showOtherInputView = false
                }
            }
        }
    }

    /// 跳转到备注页面
    //: private func jumpToNotesView() {
    private func transfer() {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let view = PrivateChatNotesView()
        let view = SightNotesView()
        //: view.show(self.userWrap.content, isTop: self.userWrap.top, toUid: self.msgView.targetID)
        view.appearanceUid(self.userWrap.content, isTop: self.userWrap.top, toUid: self.msgView.targetID)
        //: view.dismissBlock = { content, isTop in
        view.dismissBlock = { content, isTop in
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: self.userWrap.content = content
            self.userWrap.content = content
            //: self.userWrap.top = isTop
            self.userWrap.top = isTop
            //: self.notesTopView.refresh(self.userWrap)
            self.notesTopView.bottomDown(self.userWrap)
        }
    }

    /// 返回按钮点击事件
    //: private func backButtonClick() {
    private func fasteningToo() {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: half_dismissViewAnimation(false)
            mp(false)
            //: } else {
        } else {
            //: getNavigationController()?.popViewController(animated: true)
            controller()?.popViewController(animated: true)
        }
    }

    /// 展示飘屏内容
    //: @objc private func showFloatScreen(notification: NSNotification) {
    @objc private func constraintAir(notification: NSNotification) {
        //: if conversationType == .private {
        if conversationType == .private {
            //: let dataInfo = notification.userInfo as! [String: AnyObject]
            let dataInfo = notification.userInfo as! [String: AnyObject]
            //: if let model = TalkingGameFloatingScreenModel.deserialize(from: dataInfo as? Dictionary) {
            if let model = NaturalHandyJSON.deserialize(from: dataInfo as? Dictionary) {
                //: self.floatScreenView.isUserInteractionEnabled = true
                self.floatScreenView.isUserInteractionEnabled = true
                //: self.floatScreenView.didReceive(model)
                self.floatScreenView.fort(model)
            }
        }
    }
}

// MARK: - VIP消息已读回执

//: extension TalkingPrivateChatController {
extension SmallControllerDelegate {
    /// 添加通知
    //: private func addReadPeceiptNotification() {
    private func quantityoPara() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__msgReadReceipt(notification:)),
                                               selector: #selector(member(notification:)),
                                               //: name: CHAT_MSG_READ_RECEIPT_NOTIFICATION,
                                               name: k_replaceInfoMsg,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__refreshMsgList),
                                               selector: #selector(tool),
                                               //: name: USER_UPDATE_LOUNGEPLUSINFO_NOTIFICATION,
                                               name: mainWorkFormat,
                                               //: object: nil)
                                               object: nil)
    }

    /// 收到对端已读消息回执通知 -> 刷新列表
    //: @objc func func__msgReadReceipt(notification: NSNotification) {
    @objc func member(notification: NSNotification) {
        //: let obj = notification.object as! V2TIMMessageReceipt
        let obj = notification.object as! V2TIMMessageReceipt
        //: guard obj.userID == msgView.targetID else { return }
        guard obj.userID == msgView.targetID else { return }
        //: guard DismissReactiveCompatible.share.loginUserMode.loungePlus == true else { return }
        guard DismissReactiveCompatible.share.loginUserMode.loungePlus == true else { return }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 订阅成功 -> 更改订阅状态 -> 刷新列表
    //: @objc private func func__refreshMsgList() {
    @objc private func tool() {
        //: DismissReactiveCompatible.share.loginUserMode.loungePlus = true
        DismissReactiveCompatible.share.loginUserMode.loungePlus = true
        //: userWrap.isRecharge = 1
        userWrap.isRecharge = 1
        //: msgView.isRecharge = userWrap.isRecharge
        msgView.isRecharge = userWrap.isRecharge
        //: refreshRechargeRequiredView()
        week()
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 判断是否插入已读回执引导充值VIP提示消息
    //: func func__privateVIPReadReceipt_insertTipsMsg() {
    func exceptMini() {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
            //: guard let lastCellData = self.msgView.uiMsgs.lastObject as? SparkCellData else { return }
            guard let lastCellData = self.msgView.uiMsgs.lastObject as? SparkCellData else { return }
            //: if TalkingPrivateChatManager.needInsertVIPReadReceiptMsg(cellData: lastCellData) {
            if NameureThen.takeApp(cellData: lastCellData) {
                // 只做内存插入展示
                //: self.insertTipMsg_Nobalance(tipStr: "View read receipts?".localized, needInsertLocal: false)
                self.terminal(tipStr: (String(k_remainMsg)).localized, needInsertLocal: false)
            }
        }
    }
}

// MARK: - 选择图片

//: extension TalkingPrivateChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension SmallControllerDelegate: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func form() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        SpecialPermissionTool.burst(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = MedalPickTool.sameMember(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: self.present(vc, animated: true)
                self.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 _: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 _: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.showUsePhoto(img: photos![0])
                            self.shadeFeature(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func pick() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        SpecialPermissionTool.exitSmart(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.showUsePhoto(img: photo)
                self.shadeFeature(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }

    /// 展示阅后即焚+私密照片选项
    /// - Parameter img: 图片资源
    //: private func showUsePhoto(img: UIImage) {
    private func shadeFeature(img: UIImage) {
        //: guard conversationType == .private &&
        guard conversationType == .private &&
            //: DismissReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else {
            DismissReactiveCompatible.share.appStatus == AfterArtSumLiteral.normal.rawValue
        else { // 非私聊 && 非审核模式
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = SendModel()
            //: sendModel.isBurn = "0"
            sendModel.isBurn = "0"
            //: sendModel.giftId = "0"
            sendModel.giftId = "0"
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.alongProperty(img: img, sendModel: sendModel)
            //: return
            return
        }

        //: let vc = TalkingMomentPhotosVC(images: [img], index: 0, type: .sendIntimatePhoto)
        let vc = RegisterThen(images: [img], index: 0, type: .sendIntimatePhoto)
        //: vc.sendIntimatePhotoBlock = {[weak self] isBurn, giftId in
        vc.sendIntimatePhotoBlock = { [weak self] isBurn, giftId in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = SendModel()
            //: sendModel.isBurn = isBurn
            sendModel.isBurn = isBurn
            //: sendModel.giftId = giftId
            sendModel.giftId = giftId
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.alongProperty(img: img, sendModel: sendModel)
        }
        //: getNavigationController()?.pushViewController(vc, animated: true)
        controller()?.pushViewController(vc, animated: true)
    }
}

// MARK: - 选择视频

//: extension TalkingPrivateChatController {
extension SmallControllerDelegate {
    /// 发送短视频
    //: private func func__sendShortVideo() {
    private func manager() {
        // 检测用户短视频版本是否可用
        //: guard TalkingPrivateChatManager.checkShortVideo(userVersion: self.userWrap.version) else {
        guard NameureThen.customerVersion(userVersion: self.userWrap.version) else {
            //: let gender = self.userWrap.sex == Gender.male.rawValue ? "He".localized:"She".localized
            let gender = self.userWrap.sex == FenceSequence.male.rawValue ? "He".localized : (String(mainThanSureMsg)).localized
            //: self.func__showStatusBarErrorMsg(showMsg: "%@ is using too low a version".localizedArguments(gender))
            self.enablece(showMsg: (String(userCompoundData.suffix(6)) + "using" + String(appComePath.suffix(7)) + String(app_genderMessage.prefix(6)) + "rsion").unitedStates(gender))
            //: return
            return
        }

        //: self.openVideoPicker()
        self.satisfyPicker()
    }

    /// 打开视频相册
    //: private func openVideoPicker() {
    private func satisfyPicker() {
        //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: false, allowVideo: true)
        let vc = MedalPickTool.sameMember(maxCount: 1, allowPhoto: false, allowVideo: true)
        //: vc.modalPresentationStyle = .fullScreen
        vc.modalPresentationStyle = .fullScreen
        //: self.present(vc, animated: true)
        self.present(vc, animated: true)
        //: vc.didFinishPickingVideoHandle = { [weak self] ( coverImage: UIImage?, assets: PHAsset?)  in
        vc.didFinishPickingVideoHandle = { [weak self] (_: UIImage?, assets: PHAsset?) in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingImagePickTool.getVideoPath(asset: assets) { filePath in
            MedalPickTool.thanCompletion(asset: assets) { filePath in
                //: guard filePath != nil else { return }
                guard filePath != nil else { return }
                //: let asset = AVURLAsset.init(url: filePath!)
                let asset = AVURLAsset(url: filePath!)
                //: let time = asset.duration
                let time = asset.duration
                //: let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                //: if seconds >= 5 {
                if seconds >= 5 { // 压缩视频
                    //: self.CompressedVideo(url: filePath!)
                    self.mobileRecent(url: filePath!)
                    //: } else {
                } else {
                    //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                    DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) { // 视频压缩取消
                        //: self.func__showStatusBarErrorMsg(showMsg: "Shoot video at least 5 seconds".localized)
                        self.enablece(showMsg: (String(mainRemarkMasterName) + String(k_confirmName)).localized)
                    }
                }
            }
        }
    }

    /// 视频压缩
    /// - Parameter url: 本地路径
    //: private func CompressedVideo(url: URL) {
    private func mobileRecent(url: URL) {
        //: let style: TalkingVideoEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        let style: DismissEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        //: let videoVC = TalkingVideoEditorController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
        let videoVC = ResolveWallReactiveCompatible(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
            //: guard let videoInfo = videoInfo else { return }
            guard let videoInfo = videoInfo else { return }
            // 压缩成功，实例化模型
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = SendModel()
            //: sendModel.giftId = videoInfo.giftId
            sendModel.giftId = videoInfo.giftId
            //: sendModel.isBurn = videoInfo.isBurn
            sendModel.isBurn = videoInfo.isBurn
            //: sendModel.localCoverImg = videoInfo.coverPath
            sendModel.localCoverImg = videoInfo.coverPath
            //: sendModel.localVideoUrl = videoInfo.videoPath
            sendModel.localVideoUrl = videoInfo.videoPath
            //: sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            // 发送视频消息
            //: self.func__sendVideoMsg(msgData: nil, sendModel: sendModel)
            self.periodOfTime(msgData: nil, sendModel: sendModel)
        }

        //: ProgressHUD.dismiss()
        InsertProgressHUD.detectDismiss()
        //: getNavigationController()?.pushViewController(videoVC, animated: true)
        controller()?.pushViewController(videoVC, animated: true)
    }

    /// 上传视频
    /// - Parameters:
    ///   - msgData: cellData，此处只数据传递
    ///   - sendModel: 消息自定义本地模型
    //: private func cos_UploadVideo(msgData: SparkCellData, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func split(msgData: SparkCellData, sendModel: SendModel) {
        //: TalkingMomentVideoManager.shared.cos_uploadVideo(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
        SpillVideoManager.shared.commitButtonCompletion(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
            //: guard succeed else {
            guard succeed else {
                //: self.func__showStatusBarErrorMsg(showMsg: "Upload failed".localized)
                self.enablece(showMsg: (String(notiLineMethodMsg) + String(noti_stallAlterMsg)).localized)
                // 更改消息状态为失败
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.usanceLeftHandednessNetIncreaseThin(msgData, present: nil, push: NSNumber(value: 0), direction: .Msg_Status_Fail)

                //: return
                return
            }

            /// 上传成功，刷新本地视频消息，调用发消息接口
            //: sendModel.coverImg = coverUrl
            sendModel.coverImg = coverUrl
            //: sendModel.videoUrl = videoUrl
            sendModel.videoUrl = videoUrl
            //: self.func__sendVideoMsg(msgData: msgData, sendModel: sendModel)
            self.periodOfTime(msgData: msgData, sendModel: sendModel)
        }
    }
}

// MARK: - 语音消息

//: extension TalkingPrivateChatController {
extension SmallControllerDelegate {
    //: func func__addVoiceNotification() {
    func makeupInside() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidBecomeActive),
        NotificationCenter.default.addObserver(self, selector: #selector(route),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidEnterBack),
        NotificationCenter.default.addObserver(self, selector: #selector(talkOf),
                                               //: name: UIApplication.didEnterBackgroundNotification,
                                               name: UIApplication.didEnterBackgroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(audioSessionInterrupted(notification:)),
                                               selector: #selector(profileNotification(notification:)),
                                               //: name: AVAudioSession.interruptionNotification,
                                               name: AVAudioSession.interruptionNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(insertGiftMsgNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(trap(notification:)),
                                               //: name: CHAT_ADD_CALL_RECORD_MSG_NOTIFICATION,
                                               name: k_serverKey,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(newMsgIntimate(notification:)),
                                               selector: #selector(ex(notification:)),
                                               //: name: CHAT_NEW_MSG_NOTIFICATION,
                                               name: userBusyMsg,
                                               //: object: nil)
                                               object: nil)
    }

    //: func func__addOrRrmoveObserverForMsgPlayer(isAdd: Bool) {
    func compare(isAdd: Bool) {
        //: if isAdd {
        if isAdd {
            //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
            RevenuePlayerDelegate.shared.said()
            //: } else {
        } else {
            //: TalkingVoiceMsgPlayManager.shared.removeDaskManagerDelegate()
            RevenuePlayerDelegate.shared.dryWall()
        }
    }

    //: @objc func appDidBecomeActive() {
    @objc func route() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        compare(isAdd: true)
    }

    //: @objc func appDidEnterBack() {
    @objc func talkOf() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        compare(isAdd: false)
    }

    //: @objc func audioSessionInterrupted(notification: NSNotification) {
    @objc func profileNotification(notification: NSNotification) {
        //: guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        //: switch reasonValue {
        switch reasonValue {
        //: case AVAudioSession.InterruptionType.began.rawValue:
        case AVAudioSession.InterruptionType.began.rawValue: // Began
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
            compare(isAdd: false)
        //: break
        //: case AVAudioSession.InterruptionType.ended.rawValue:
        case AVAudioSession.InterruptionType.ended.rawValue: // End
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
            compare(isAdd: true)
        //: break
        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: RangeCellData, cellView: TalkingChatAudioMsgCell) {
    func commentEnableicial(cellData: RangeCellData, cellView: CompartmentMsgCell) {
        //: guard TalkingSocketManager.shared.isInfo == false else {
        guard SantaLuciaFirSocketDelegate.shared.isInfo == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.enablece(showMsg: kLikeStr)
            //: return
            return
        }

        //: let message = cellData.innerMessage
        let message = cellData.innerMessage

        //: for indexModel in self.msgView.uiMsgs {
        for indexModel in self.msgView.uiMsgs {
            //: if (indexModel as? SparkCellData)!.isKind(of: RangeCellData.self) == false {
            if (indexModel as? SparkCellData)!.isKind(of: RangeCellData.self) == false {
                //: continue
                continue
            }
            //: let audioModel = indexModel as! RangeCellData
            let audioModel = indexModel as! RangeCellData
            //: audioModel.voiceModel.isPlayingStatus = 2
            audioModel.voiceModel.isPlayingStatus = 2
            //: audioModel.voiceModel.activityShowStatus = 2
            audioModel.voiceModel.activityShowStatus = 2
            // 红点提示隐藏
            //: if (audioModel.isEqual(cellData)) {
            if audioModel.isEqual(cellData) {
                //: cellView.voiceTrackPoint.isHidden = true
                cellView.voiceTrackPoint.isHidden = true
            }
        }
        //: if message != nil {
        if message != nil {
            //: cellData.voiceModel.db_voiceCacheWrap = DBUserInfoManager.cache_getAudioDB(message: message!)
            cellData.voiceModel.db_voiceCacheWrap = CutInfoManager.corner(message: message!)
        }
        //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
        RevenuePlayerDelegate.shared.visitor()

        //: if TalkingVoiceMsgPlayManager.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
        if RevenuePlayerDelegate.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
            //: printLog(message: "播放状态:\(TalkingVoiceMsgPlayManager.shared.playStatus)")
            printLog(message: (appWriteName.capitalized) + "\(RevenuePlayerDelegate.shared.playStatus)")
            //: if TalkingVoiceMsgPlayManager.shared.playStatus != .Unknown {
            if RevenuePlayerDelegate.shared.playStatus != .Unknown {
                //: TalkingVoiceMsgPlayManager.shared.playStatus = .Unknown
                RevenuePlayerDelegate.shared.playStatus = .Unknown
                //: return
                return
            }
        }
        // 指定播放的cell
        //: TalkingVoiceMsgPlayManager.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        RevenuePlayerDelegate.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        //: TalkingVoiceMsgPlayManager.shared.playStatus = .Waiting
        RevenuePlayerDelegate.shared.playStatus = .Waiting
        //: var waitArray = [cellData.voiceModel]
        var waitArray = [cellData.voiceModel]
        //: if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
        if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
            //: let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            //: for index in currentIndex+1 ..< self.msgView.uiMsgs.count {
            for index in currentIndex + 1 ..< self.msgView.uiMsgs.count {
                //: let subModel: SparkCellData = self.msgView.uiMsgs.object(at: index) as! SparkCellData
                let subModel: SparkCellData = self.msgView.uiMsgs.object(at: index) as! SparkCellData
                //: let subMessage = subModel.innerMessage
                let subMessage = subModel.innerMessage
                //: if subModel.isKind(of: RangeCellData.self) {
                if subModel.isKind(of: RangeCellData.self) {
                    //: let audioModel = subModel as! RangeCellData
                    let audioModel = subModel as! RangeCellData
                    //: let voiceModel = audioModel.voiceModel
                    let voiceModel = audioModel.voiceModel
                    //: if subMessage != nil {
                    if subMessage != nil {
                        //: voiceModel.db_voiceCacheWrap = DBUserInfoManager.cache_getAudioDB(message: subMessage!)
                        voiceModel.db_voiceCacheWrap = CutInfoManager.corner(message: subMessage!)
                    }
                    //: if (voiceModel.db_voiceCacheWrap.db_isRead == "0") {
                    if voiceModel.db_voiceCacheWrap.db_isRead == "0" {
                        //: waitArray.append(voiceModel)
                        waitArray.append(voiceModel)
                    }
                    //: cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    //: cellView.voiceTrackPoint.isHidden = true
                    cellView.voiceTrackPoint.isHidden = true
                    //: WCDBVoiceMsgTable.db_updateVoiceMsg(cellData.voiceModel.db_voiceCacheWrap)
                    RedMsgTable.afterSecond(cellData.voiceModel.db_voiceCacheWrap)
                }
            }
        }
        //: TalkingVoiceMsgPlayManager.shared.starPlayMsg(msgArr: waitArray)
        RevenuePlayerDelegate.shared.thinStar(msgArr: waitArray)
    }
}

// MARK: - 用户相关

//: extension TalkingPrivateChatController {
extension SmallControllerDelegate {
    /// 请求聊天用户信息 V1.8.0 提前到进入页面之前，先判断是否封号
    //: func func__requestChatInfo() {
    func anima() {
        //: guard conversationType == .private || conversationType == .service else { return }
        guard conversationType == .private || conversationType == .service else { return }

        //: self.func__updateUIWithUserWrap()
        self.pendingDistance()
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        // 判断是否需要插入一键打招呼消息
        //: if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
        if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
            //: self.func_sendQuickGreetingMsg(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
            self.less(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
        }
        //: if self.msgView.uiMsgs.count > 0 && self.userWrap.existSess == false {
        if self.msgView.uiMsgs.count > 0, self.userWrap.existSess == false {
            //: for model in self.msgView.uiMsgs {
            for model in self.msgView.uiMsgs {
                //: let indexModel = model as? SparkCellData
                let indexModel = model as? SparkCellData
                //: if indexModel?.isKind(of: RangeCellData.self) == true || indexModel?.isKind(of: EngineeringCellData.self) == true || indexModel?.isKind(of: CellReactiveCompatible.self) == true || indexModel?.isKind(of: NumberensityReactiveCompatible.self) == true {
                if indexModel?.isKind(of: RangeCellData.self) == true || indexModel?.isKind(of: EngineeringCellData.self) == true || indexModel?.isKind(of: CellReactiveCompatible.self) == true || indexModel?.isKind(of: NumberensityReactiveCompatible.self) == true {
                    //: self.userWrap.existSess = true
                    self.userWrap.existSess = true
                    //: break
                    break
                }
            }
        }
        // 判断是否需要插入音视频提示消息
        //: self.func__privateChatVideo_insertTipMsg()
        self.capture()
        // 判断是否需要插入官方客服提示消息
        //: self.func__customerServiceCenter_insertTipMsg()
        self.shrink()
        /// 判断是否插入已读回执引导充值VIP提示消息
        //: self.func__privateVIPReadReceipt_insertTipsMsg()
        self.exceptMini()
    }

    /// 刷新UI
    //: func func__updateUIWithUserWrap() {
    func pendingDistance() {
        //: guard userWrap.uid.count > 0 else { return }
        guard userWrap.uid.count > 0 else { return }
        //: bgImgeView.setUrlImage(urlStr: userWrap.headPic)
        bgImgeView.bolt(urlStr: userWrap.headPic)
        //: navView.private_updateNavView(userModel: userWrap, isFrom: self.isFrom)
        navView.compute(userModel: userWrap, isFrom: self.isFrom)
        /// 亲密度头像
        //: self.showFirstIntimateView()
        self.angstrom()

        //: if sendGreetBtn.isHidden == false {
        if sendGreetBtn.isHidden == false {
            //: sendGreetBtn.isHidden = userWrap.sex == DismissReactiveCompatible.share.loginUserMode.sex
            sendGreetBtn.isHidden = userWrap.sex == DismissReactiveCompatible.share.loginUserMode.sex
        }
        //: if beautyPhotosBtn.isHidden == false {
        if beautyPhotosBtn.isHidden == false {
            //: beautyPhotosBtn.isHidden = userWrap.sex == DismissReactiveCompatible.share.loginUserMode.sex
            beautyPhotosBtn.isHidden = userWrap.sex == DismissReactiveCompatible.share.loginUserMode.sex
        }
        // 备注置顶
        //: notesTopView.refresh(userWrap)
        notesTopView.bottomDown(userWrap)
    }

    //: func func__getFillDraftText() {
    func ad() {
        //: let userID = "c2c_"+msgView.targetID
        let userID = (String(mainSpendMsg.suffix(4))) + msgView.targetID
        //: V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
        V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                //: let draftText = conv?.draftText ?? ""
                let draftText = conv?.draftText ?? ""
                //: if !draftText.isEmptyString {
                if !draftText.isEmptyString {
                    //: self.msgInputView.inputTextView.text = conv?.draftText
                    self.msgInputView.inputTextView.text = conv?.draftText
                    //: self.msgInputView.inputTextView.becomeFirstResponder()
                    self.msgInputView.inputTextView.becomeFirstResponder()
                    //: self.func_inputContentIntervalOneSecond()
                    self.assignSave()
                }
            }
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }

    //: func func__saveFillDraftText() {
    func several() {
        //: let userID = "c2c_"+msgView.targetID
        let userID = (String(mainSpendMsg.suffix(4))) + msgView.targetID
        //: V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
        V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }
}

// MARK: - 提示消息

//: extension TalkingPrivateChatController {
extension SmallControllerDelegate {
    //: func func__TipsNotification() {
    func sumo() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(insertTipsNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(off(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: user_dismissData,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) -> Void {
    @objc func off(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: TalkingChatTipsMsgCellData  = userinfo["msg"] as! TalkingChatTipsMsgCellData
        let celldata: TransferCellData = userinfo[(String(mainRenderValue))] as! TransferCellData

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.input(true)
        self.msgView.input(true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func terminal(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo: [String: Any] = ["content": tipStr,
        let msgInfo: [String: Any] = [String(bytes: const_protectionValue.reversed(), encoding: .utf8)!: tipStr,
                                      //: "msgType": "tips"]
                                      String(bytes: user_justiceValue.reversed(), encoding: .utf8)!: (String(noti_amPath))]
        //: var message: SparkCellData?
        var message: SparkCellData?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: message = TUISocialChatManager.insertTXMessage(withExtral: msgInfo, toUid: msgView.targetID, isSender: true, isTip: true)
            message = TictacReactiveCompatible.terminalBeginEngraftDigitizerCipherCircleWith(withExtral: msgInfo, toUid: msgView.targetID, isSender: true, isTip: true)
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSend(withExtral: msgInfo, isSender: true, isTip: true)
            message = TictacReactiveCompatible.smooth(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.input(true)
        self.msgView.input(true)
    }

    /// 官方客服插入系统提示消息
    //: func func__customerServiceCenter_insertTipMsg() {
    func shrink() {
        //: guard conversationType == .service else { return }
        guard conversationType == .service else { return }
        //: TalkingPrivateChatManager.chat_customerServiceCenter_insertTipMsg { tipsStr in
        NameureThen.shape { tipsStr in
            //: guard tipsStr != nil else { return }
            guard tipsStr != nil else { return }
            // 客服提示消息，只做内存插入展示
            //: self.insertTipMsg_Nobalance(tipStr: tipsStr!, needInsertLocal: false)
            self.terminal(tipStr: tipsStr!, needInsertLocal: false)
        }
    }

    /// 判断是否需要插入免费提示消息
    //: func func__privateAttachSys_insertTipMsg(str: String) {
    func opendDoor(str: String) {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: self.insertTipMsg_Nobalance(tipStr: str)
        self.terminal(tipStr: str)
    }

    /// 判断是否需要插入音视频通话提示消息
    //: func func__privateChatVideo_insertTipMsg() {
    func capture() {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: TalkingPrivateChatManager.chat_privateChatVideo_insertTipMsg(toUid: self.userWrap.uid, msgView: self.msgView) {
        NameureThen.privateExposureHandler(toUid: self.userWrap.uid, msgView: self.msgView) {
            // 插入音视频提示消息
            //: let str = self.userWrap.nickname
            let str = self.userWrap.nickname
            //: self.insertTipMsg_Nobalance(tipStr: "You can use the video chat feature to give %@ a call".localizedArguments(str))
            self.terminal(tipStr: String(bytes: noti_whySpanContent.map{missBone(select: $0)}, encoding: .utf8)!.unitedStates(str))
        }
    }

    /// 通话礼物消息
    //: @objc func insertGiftMsgNotif(notification: NSNotification) {
    @objc func trap(notification: NSNotification) {
        //: let dict = notification.userInfo
        let dict = notification.userInfo
        //: let cellData: SparkCellData = dict?["cellData"] as! SparkCellData
        let cellData: SparkCellData = dict?[(String(user_everyUrl) + String(const_collectPath))] as! SparkCellData
        //: self.msgView.uiMsgs.add(cellData)
        self.msgView.uiMsgs.add(cellData)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.input(true)
        self.msgView.input(true)
    }
}

// MARK: - 回复文案

//: extension TalkingPrivateChatController: PrivateChatReplyTipsProtocol {
extension SmallControllerDelegate: RubyRedThen {
    //: func func__setReplyTipsView() {
    func publishView() {
        //: let key = "\(DismissReactiveCompatible.share.loginUserMode.userID)_PrivateChatTipsTitle_\(String(self.msgView.targetID))"
        let key = "\(DismissReactiveCompatible.share.loginUserMode.userID)" + (String(data_bearMsg.prefix(5)) + "ateCha" + String(mainWithinTitle) + String(app_waitKey.prefix(7))) + "\(String(self.msgView.targetID))"
        //: let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        //: if !isShow && DismissReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue {
        if !isShow && DismissReactiveCompatible.share.appStatus == AfterArtSumLiteral.normal.rawValue {
            //: contentView.addSubview(replyTipsView!)
            contentView.addSubview(replyTipsView!)
            //: replyTipsView?.snp.makeConstraints { make in
            replyTipsView?.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(50)
                make.height.equalTo(50)
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(replyTipsView!.snp.top)
                make.bottom.equalTo(replyTipsView!.snp.top)
            }
            //: self.replyTipsView?.setTipsStrView()
            self.replyTipsView?.paradigmView()
            // 修改banner位置
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            birdSEyeView(isMiniView: self.bannerView.isMiniView)
            //: } else {
        } else {
            //: removeReplyTipsView()
            resSearch()
        }
    }

    //: func seleteReplyTipsMessage(str: String) {
    func labelReplyAgree(str: String) {
        //: self.userWrap.existSess = true
        self.userWrap.existSess = true
        //: func__sendTextMsg(msgStr: str, quoteModel: nil)
        blow(msgStr: str, quoteModel: nil)
        //: removeReplyTipsView()
        resSearch()
    }

    //: func removeReplyTipsView() {
    func resSearch() {
        //: if conversationType == .private {
        if conversationType == .private {
            //: if self.replyTipsView != nil {
            if self.replyTipsView != nil {
                //: self.replyTipsView?.removeFromSuperview()
                self.replyTipsView?.removeFromSuperview()
                //: self.replyTipsView = nil
                self.replyTipsView = nil
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            birdSEyeView(isMiniView: self.bannerView.isMiniView)
        }
    }
}

// MARK: - 问题回答消息

//: extension TalkingPrivateChatController {
extension SmallControllerDelegate {
    /// 判断问题回答消息遮罩是否展示
    //: private func refreshRechargeRequiredView() {
    private func week() {
        //: guard DismissReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue else { return }
        guard DismissReactiveCompatible.share.loginUserMode.sex == FenceSequence.male.rawValue else { return }
        //: if userWrap.isRecharge == 1 || userWrap.isOfficial {
        if userWrap.isRecharge == 1 || userWrap.isOfficial { // 已充值过
            //: quickReplayView.isHidden = true
            quickReplayView.isHidden = true
            //: rechargeView.isHidden = true
            rechargeView.isHidden = true

            //: } else {
        } else { // 未充值
            //: rechargeView.isHidden = false
            rechargeView.isHidden = false
            //: self.checkQuestionQuickReplayView()
            self.region()
        }
    }

    /// 检查快捷回复视图是否展示
    //: private func checkQuestionQuickReplayView() {
    private func region() {
        //: guard userWrap.isOfficial == false else { return }
        guard userWrap.isOfficial == false else { return }
        // 检查男性侧快捷回复是否展示（延时为了拿到uiMsgs数据）
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            //: TalkingPrivateChatManager.chat_onNewMessage_checkQuestionQuickReplayMsg(isRecharge: self.userWrap.isRecharge == 1, msgView: self.msgView) { msgInfo in
            NameureThen.openChoice(isRecharge: self.userWrap.isRecharge == 1, msgView: self.msgView) { msgInfo in
                //: guard let msgInfo = msgInfo else { return }
                guard let msgInfo = msgInfo else { return }
                //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                    //: self.quickReplayView.isHidden = false
                    self.quickReplayView.isHidden = false
                    //: self.quickReplayView.refreshQuickReplayView(msgInfo: msgInfo)
                    self.quickReplayView.delayEnable(msgInfo: msgInfo)
                }
            }
        }
    }
}

// MARK: - 半屏页面展示/隐藏动画

//: extension TalkingPrivateChatController {
extension SmallControllerDelegate {
    /// 半屏展示动画
    //: private func half_showViewAnimation() {
    private func topM() {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }
        //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_SHOW, object: nil, userInfo: nil)
        NotificationCenter.default.post(name: data_mineSaltStr, object: nil, userInfo: nil)
        //: self.view.frame = CGRect(x: 0, y: ScreenHeight-HalfViewTopMargin, width: ScreenWidth, height: ScreenHeight)
        self.view.frame = CGRect(x: 0, y: main_nameData - showFileTitle, width: main_logStr, height: main_nameData)
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: 0,
                                     y: 0,
                                     //: width: ScreenWidth,
                                     width: main_logStr,
                                     //: height: ScreenHeight)
                                     height: main_nameData)
        }
    }

    /// 半屏页消失动画
    /// - Parameter removeChatList: 是否需要移除聊天列表
    //: private func half_dismissViewAnimation(_ removeChatList: Bool) {
    private func mp(_ removeChatList: Bool) {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }

        //: self.view.endEditing(true)
        self.view.endEditing(true)
        // 移除聊天列表
        //: if removeChatList == true, let currentVC = self.currentViewController() {
        if removeChatList == true, let currentVC = self.exitSchedule() {
            //: for vc in currentVC.children {
            for vc in currentVC.children {
                //: if vc.isKind(of: TalkingChatListViewController.self) {
                if vc.isKind(of: BibliographyViewDelegate.self) {
                    //: vc.removeFromParent()
                    vc.removeFromParent()
                    //: vc.view.removeFromSuperview()
                    vc.view.removeFromSuperview()
                    //: break
                    break
                }
            }
        }

        // 移除私聊页
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: ScreenHeight-HalfViewTopMargin,
                                     y: main_nameData - showFileTitle,
                                     //: width: ScreenWidth,
                                     width: main_logStr,
                                     //: height: ScreenHeight)
                                     height: main_nameData)
            //: } completion: { finish in
        } completion: { _ in
            //: self.removeFromParent()
            self.removeFromParent()
            //: self.view.removeFromSuperview()
            self.view.removeFromSuperview()
        }
    }
}

// MARK: - Layout

//: extension TalkingPrivateChatController {
extension SmallControllerDelegate {
    /// 创建UI
    //: private func createUI() {
    private func medium() {
        //: if conversationType == .private {
        if conversationType == .private {
            //: msgView.view.backgroundColor = UIColor.clear
            msgView.view.backgroundColor = UIColor.clear
            //: msgView.tableView.backgroundColor = UIColor.clear
            msgView.tableView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: msgView.view.backgroundColor = UIColor.clear
            msgView.view.backgroundColor = UIColor.clear
            //: msgView.tableView.backgroundColor = UIColor(hex: "F5F5F5")
            msgView.tableView.backgroundColor = UIColor(hex: (k_capitalStr.uppercased() + String(userMeId.prefix(5))))
        }
        //: view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear:UIColor.white
        view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear : UIColor.white
        //: view.addSubview(contentView)
        view.addSubview(contentView)

        //: if conversationType == .private {
        if conversationType == .private {
            //: contentView.addSubview(bgImgeView)
            contentView.addSubview(bgImgeView)
            //: contentView.addSubview(bgView)
            contentView.addSubview(bgView)
            //: contentView.addSubview(coverView)
            contentView.addSubview(coverView)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(beautyPhotosBtn)
            contentView.addSubview(beautyPhotosBtn)
            //: contentView.addSubview(sendGreetBtn)
            contentView.addSubview(sendGreetBtn)
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: contentView.addSubview(bannerView)
                contentView.addSubview(bannerView)
            }
            //: contentView.addSubview(floatScreenView)
            contentView.addSubview(floatScreenView)
            //: contentView.addSubview(giftEffectView)
            contentView.addSubview(giftEffectView)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: contentView.addSubview(notesTopView)
            contentView.addSubview(notesTopView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)
            //: view.addSubview(rechargeView)
            view.addSubview(rechargeView)
            //: view.addSubview(quickReplayView)
            view.addSubview(quickReplayView)

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else {
        } else {
            //: title = "System notification".localized
            title = (String(dataBrushId) + String(k_continuingTitle.prefix(6)) + "ication").localized
            //: contentView.addSubview(feedView)
            contentView.addSubview(feedView)
            //: feedView.addSubview(feedbackBtn)
            feedView.addSubview(feedbackBtn)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
        }
    }

    /// 约束
    //: private func createUIConstraints() {
    private func extra() {
        //: contentView.snp.makeConstraints { make in
        contentView.snp.makeConstraints { make in
            //: make.bottom.leading.trailing.equalToSuperview()
            make.bottom.leading.trailing.equalToSuperview()
            //: make.top.equalTo((self.isFrom == .LiveRoom) ? HalfViewTopMargin:0)
            make.top.equalTo((self.isFrom == .LiveRoom) ? showFileTitle : 0)
        }

        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.layoutIfNeeded()
            contentView.layoutIfNeeded()
            //: contentView.Corner(width: contentView.width,
            contentView.rogueStateForRadii(width: contentView.width,
                                           //: height: contentView.height,
                                           height: contentView.height,
                                           //: corners: [.topLeft, .topRight],
                                           corners: [.topLeft, .topRight],
                                           //: cornerRadii: .init(width: 8, height: 8))
                                           cornerRadii: .init(width: 8, height: 8))
        }

        //: if conversationType == .private {
        if conversationType == .private {
            //: bgView.snp.makeConstraints { make in
            bgView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: bgImgeView.snp.makeConstraints { make in
            bgImgeView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: coverView.snp.makeConstraints { make in
            coverView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? userCellCurrencyPath : constAgentJumpValue)
            }
            //: notesTopView.snp.makeConstraints { make in
            notesTopView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalTo(36)
                make.height.equalTo(36)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: if beautyPhotosBtn.isHidden {
                if beautyPhotosBtn.isHidden {
                    //: make.bottom.equalTo(msgInputView.snp.top)
                    make.bottom.equalTo(msgInputView.snp.top)
                    //: } else {
                } else {
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                }
            }
            //: giftEffectView.snp.makeConstraints { make in
            giftEffectView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: sendGreetBtn.snp.makeConstraints { make in
            sendGreetBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(beautyPhotosBtn)
                make.bottom.equalTo(beautyPhotosBtn)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.size.equalTo(beautyPhotosBtn)
                make.size.equalTo(beautyPhotosBtn)
            }
            //: beautyPhotosBtn.snp.makeConstraints { make in
            beautyPhotosBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                //: make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                //: make.width.equalTo(134)
                make.width.equalTo(134)
                //: make.height.equalTo(30)
                make.height.equalTo(30)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kDeviceSafeBottomHeight)
                make.height.equalTo(206 + appInviteValue)
            }
            //: floatScreenView.snp.makeConstraints { make in
            floatScreenView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(40)
                make.height.equalTo(40)
            }
            //: rechargeView.snp.makeConstraints { make in
            rechargeView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(rechargeView.ContentView_H)
                make.height.equalTo(rechargeView.ContentView_H)
            }
            //: quickReplayView.snp.makeConstraints { make in
            quickReplayView.snp.makeConstraints { make in
                //: make.leading.equalTo(24)
                make.leading.equalTo(24)
                //: make.width.equalTo(270)
                make.width.equalTo(270)
                //: make.bottom.equalTo(rechargeView.snp.top).offset(-13)
                make.bottom.equalTo(rechargeView.snp.top).offset(-13)
            }
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: bannerView.snp.makeConstraints { make in
                bannerView.snp.makeConstraints { make in
                    //: make.trailing.equalToSuperview()
                    make.trailing.equalToSuperview()
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    //: make.height.equalTo(106)
                    make.height.equalTo(106)
                    //: make.width.equalTo(93)
                    make.width.equalTo(93)
                }
            }

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? userCellCurrencyPath : constAgentJumpValue)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kDeviceSafeBottomHeight)
                make.height.equalTo(206 + appInviteValue)
            }

            //: } else {
        } else {
            //: feedView.snp.makeConstraints { make in
            feedView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo((view))
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(56+kDeviceSafeBottomHeight)
                make.height.equalTo(56 + appInviteValue)
            }
            //: feedbackBtn.snp.makeConstraints { make in
            feedbackBtn.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo(feedView)
                make.leading.trailing.top.equalTo(feedView)
                //: make.height.equalTo(56)
                make.height.equalTo(56)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo((view))
                make.leading.trailing.top.equalTo(view)
                //: make.bottom.equalTo(feedView.snp.top)
                make.bottom.equalTo(feedView.snp.top)
            }
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func itemBold() {
        //: navView.backButton.rx.tap
        navView.backButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.backButtonClick()
                self.fasteningToo()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.moreButton.rx.controlEvent(.touchUpInside)
        navView.moreButton.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.moreBtnClick()
                self.glass()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.videoCallBtn.rx.controlEvent(.touchUpInside)
        navView.videoCallBtn.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.func__selectMoreFunAction(actionType: .VideoCall)
                self.liveBy(actionType: .VideoCall)
                //: uploadRecord.uploadRecordEvent(eventID: ClickChatVideoButton, toUid: self.userWrap.uid )
                const_pathMsg.tabKeep(eventID: kToId, toUid: self.userWrap.uid)
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
    }
}
