
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let noti_threeMsg:[UInt8] = [0xc5,0xc2,0xc5,0xd8,0x84,0xcf,0xc3,0xc8,0xc9,0xde,0x96,0x85,0x8c,0xc4,0xcd,0xdf,0x8c,0xc2,0xc3,0xd8,0x8c,0xce,0xc9,0xc9,0xc2,0x8c,0xc5,0xc1,0xdc,0xc0,0xc9,0xc1,0xc9,0xc2,0xd8,0xc9,0xc8]

/*: "roomId" :*/
fileprivate let const_deliveryUrl:String = "R"
fileprivate let appPresentationMsg:String = "oomIdcarrier spec"

/*: "type" :*/
fileprivate let kStripKey:[UInt8] = [0x65,0x70,0x79,0x74]

/*: "extra" :*/
fileprivate let notiThemePath:String = "EXTRA"

/*: "jumpKey" :*/
fileprivate let userWhichDealFormat:[UInt8] = [0x50,0x4f,0x57,0x4a,0x71,0x5f,0x43]

private func containerFast(line num: UInt8) -> UInt8 {
    return num ^ 58
}

/*: "Send Gift" :*/
fileprivate let main_bouncePath:String = "Send trunk used warn announcement square"

/*: "View info" :*/
fileprivate let notiSecureMessage:[Character] = ["V","i","e","w"," ","i"]
fileprivate let kValidKey:[Character] = ["n","f","o"]

/*: "Report" :*/
fileprivate let appLeaguePath:[Character] = ["R","e","p","o","r","t"]

/*: "uid" :*/
fileprivate let k_floorFormat:[UInt8] = [0x64,0x69,0x75]

/*: "name" :*/
fileprivate let noti_resentMessage:[UInt8] = [0xec,0xe3,0xef,0xe7]

private func pressSuccess(connect num: UInt8) -> UInt8 {
    return num ^ 130
}

/*: "Can't send blank message" :*/
fileprivate let appDisableData:String = "Can\'"
fileprivate let notiMenuFocusKey:String = "instruction word tun tender remlank m"

/*: "user" :*/
fileprivate let main_mateData:String = "USER"

/*: "content" :*/
fileprivate let noti_squareUrl:[UInt8] = [0x74,0x6e,0x65,0x74,0x6e,0x6f,0x63]

/*: "msgType" :*/
fileprivate let user_contactStatisticalValue:String = "science beard statmsgType"

/*: "txt" :*/
fileprivate let app_ehPath:String = "manualxmanual"

/*: "isGroup" :*/
fileprivate let k_addData:[Character] = ["i"]
fileprivate let user_matePath:String = "sGroupagree launch welcome argument"

/*: "jumps" :*/
fileprivate let user_continuingPath:[Character] = ["j","u","m","p","s"]

/*: "message" :*/
fileprivate let show_tapTitle:[UInt8] = [0x29,0x21,0x37,0x37,0x25,0x23,0x21]

private func tingOral(appeal num: UInt8) -> UInt8 {
    return num ^ 68
}

/*: "TxtMsg" :*/
fileprivate let appDarkPartnerValue:String = "circle wireTxtMsg"

/*: "toUid" :*/
fileprivate let constNoticeTitle:String = "toUidspot save black"

/*: "msgInfo" :*/
fileprivate let kBeyondKey:[Character] = ["m","s"]
fileprivate let userNumbStr:String = "gInfoelect please original action replacement"

/*: "params" :*/
fileprivate let mainPoKey:[UInt8] = [0x54,0x45,0x56,0x45,0x51,0x57]

fileprivate func pureWrapBass(sense num: UInt8) -> UInt8 {
    let value = Int(num) - 228
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "contentType" :*/
fileprivate let main_firstName:[UInt8] = [0x86,0x8a,0x8b,0x91,0x80,0x8b,0x91,0xb1,0x9c,0x95,0x80]

/*: "ImgMsg" :*/
fileprivate let userDemandId:String = "ImgMsgpick transport problem supply this"

/*: "img" :*/
fileprivate let mainExampleKey:[Character] = ["i","m","g"]

/*: "imageUri" :*/
fileprivate let kSinceQuickStr:[UInt8] = [0x6e,0x72,0x66,0x6c,0x6a,0x5a,0x77,0x6e]

fileprivate func complaintMotion(stream num: UInt8) -> UInt8 {
    let value = Int(num) - 5
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "pic" :*/
fileprivate let kMakerUrl:[Character] = ["p","i","c"]

/*: "msgId" :*/
fileprivate let show_minimizePath:[Character] = ["m","s","g","I","d"]

/*: "totalMfCoin" :*/
fileprivate let const_lipUrl:String = "totalMfCohere rush year cookie him"
fileprivate let showSubmitMessage:String = "mist"

/*: "giftId" :*/
fileprivate let data_redWithinStr:String = "remark control heartgiftId"

/*: "giftNum" :*/
fileprivate let userDistrictUrl:[Character] = ["g","i","f","t","N"]
fileprivate let show_spareName:[Character] = ["u","m"]

/*: "pkgItemsetId" :*/
fileprivate let constAdministrativeInnerUrl:[Character] = ["p","k","g","I","t","e","m","s","e","t","I","d"]

/*: "msg" :*/
fileprivate let showMediaPath:String = "richsg"

/*: "tips" :*/
fileprivate let user_backgroundProblemMessage:String = "towardips"

/*: "gift" :*/
fileprivate let k_runKey:String = "glopet"

/*: "nickname" :*/
fileprivate let user_anotherTitle:String = "nicknamyear"

/*: "headPic" :*/
fileprivate let data_regionStr:[UInt8] = [0x69,0x64,0x60,0x65,0x51,0x68,0x62]

private func transportationProximate(numb num: UInt8) -> UInt8 {
    return num ^ 1
}

/*: "All Numbers" :*/
fileprivate let showTwoId:String = "finite decrease detect boldAll Nu"
fileprivate let appMouthTitle:String = "mbedistributions"

/*: "sendNum" :*/
fileprivate let k_halfFashionFormat:[Character] = ["s","e","n","d","N","u","m"]

/*: "toUser" :*/
fileprivate let data_viewPremiumName:[Character] = ["t","o","U","s","e","r"]

/*: "msgIncome" :*/
fileprivate let show_varietyName:[Character] = ["m","s","g","I","n","c","o"]
fileprivate let const_satisfyValue:[Character] = ["m","e"]

/*: "isConnection" :*/
fileprivate let dataHolderOwnerFinishStr:String = "I"
fileprivate let appBetweenBoxMessage:[Character] = ["s","C","o","n","n","e","c","t","i","o","n"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SmallChatController.swift
//  AbroadTalking
//
//  Created by young on 2023/4/4.
//

//: import UIKit
import UIKit

//: class TalkingGroupChatController: TalkingBaseViewController {
class SmallChatController: WeddingLovelyRecognizerDelegate {
    //: var infoModel = TalkingChatRoomInfoModel()
    var infoModel = OfThen() // 群聊信息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var tipsTimer: Timer?
    private var tipsTimer: Timer? // 提示消息定时器
    //: private var conversationData = TalkingConversationModel()
    private var conversationData = IceTapThen() // 会话cellData
    //: private var conversationType: TalkingIMChatType = .group
    private var conversationType: TalkingIMChatType = .group // 会话类型
    //: private var atFlag = false
    private var atFlag = false // 记录是否@

    //: init(groupType: String, infoData: [String: Any]) {
    init(groupType: String, infoData: [String: Any]) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: hideNavi = true
        hideNavi = true
        //: if let model = TalkingChatRoomInfoModel.deserialize(from: infoData) {
        if let model = OfThen.deserialize(from: infoData) {
            //: infoModel = model
            infoModel = model
        }
        //: conversationData.groupID = infoModel.roomId
        conversationData.groupID = infoModel.roomId
        //: conversationData.groupType = groupType
        conversationData.groupType = groupType
        //: if groupType == GroupType_Meeting {
        if groupType == GroupType_Meeting {
            //: conversationType = .meeting
            conversationType = .meeting
        }
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: noti_threeMsg.map{$0^172}, encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if DismissReactiveCompatible.share.appConfigMode.disableShootScreen {
            if DismissReactiveCompatible.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = AeronauticalEngineeringReactiveCompatible(frame: CGRect(x: 0, y: 0, width: main_logStr, height: main_nameData))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: createUI()
        arteriaCiliaris()
        //: createUIConstraints()
        eachSmooth()
        //: bindInteraction()
        presentationBirth()
        //: addTapGestureRecognizer()
        workIn()
        //: func__chatRoom_insertTipMsg()
        spend()
        //: chatSettings()
        lineament()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            nearTableIncome()
        }
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: if self.atFlag {
        if self.atFlag {
            //: self.msgInputView.inputTextView.becomeFirstResponder()
            self.msgInputView.inputTextView.becomeFirstResponder()
            //: self.atFlag = false
            self.atFlag = false
        }
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
    }

    //: deinit {
    deinit {
        //: if self.conversationData.groupType == GroupType_Meeting {
        if self.conversationData.groupType == GroupType_Meeting {
            //: V2TIMManager.sharedInstance().quitGroup(self.conversationData.groupID, succ: nil, fail: nil)
            V2TIMManager.sharedInstance().quitGroup(self.conversationData.groupID, succ: nil, fail: nil)
        }
        //: self.func__joinOrQuitRoom(join: false)
        self.drown(join: false)
        //: self.destroyTipsTimer()
        self.opposition()
    }

    // MARK: - Lazy Load

    //: private lazy var msgView: TUIMessageController = {
    private lazy var msgView: HeavyViewController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = HeavyViewController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: msgVC.view.backgroundColor = UIColor.clear
        msgVC.view.backgroundColor = UIColor.clear
        //: msgVC.tableView.backgroundColor = .clear
        msgVC.tableView.backgroundColor = .clear
        //: msgVC.tableView.register(TalkingGroupChatTextMsgCell.self, forCellReuseIdentifier: TalkingGroupChatTextMsgCell.className())
        msgVC.tableView.register(SpecialMessageCell.self, forCellReuseIdentifier: SpecialMessageCell.className())
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: TalkingChatImageMsgCell.className())
        msgVC.tableView.register(ActualMessageCell.self, forCellReuseIdentifier: ActualMessageCell.className())
        //: msgVC.tableView.register(TalkingGroupChatGiftMsgCell.self, forCellReuseIdentifier: TalkingGroupChatGiftMsgCell.className())
        msgVC.tableView.register(GroupGiftCompartmentTrailerManufacturerMessageCell.self, forCellReuseIdentifier: GroupGiftCompartmentTrailerManufacturerMessageCell.className())
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: TalkingChatTipsMsgCell.className())
        msgVC.tableView.register(FirstViewCell.self, forCellReuseIdentifier: FirstViewCell.className())
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: WireView.TSystemMessageCell_ReuseId)
        msgVC.tableView.register(RangeViewCell.self, forCellReuseIdentifier: WireView.TSystemMessageCell_ReuseId)
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType
        //: return msgVC
        return msgVC
        //: }()
    }()

    //: private lazy var msgInputView: TalkingChatInputView = {
    private lazy var msgInputView: PowderHornInputView = {
        //: let view = TalkingChatInputView(type: .GroupChat)
        let view = PowderHornInputView(type: .GroupChat)
        //: view.chatRoomID = infoModel.roomId
        view.chatRoomID = infoModel.roomId
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    private lazy var giftEffectView: TickGenerateView = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = TickGenerateView()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: private lazy var giftTrackView: TalkingGiftTrackView = {
    private lazy var giftTrackView: RatingTrackView = {
        //: let trackView = TalkingGiftTrackView.init()
        let trackView = RatingTrackView()
        //: trackView.isUserInteractionEnabled = false
        trackView.isUserInteractionEnabled = false
        //: return trackView
        return trackView
        //: }()
    }()

    //: private lazy var navView: TalkingChatNavTitleView = {
    private lazy var navView: OfNameView = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = OfNameView(type: conversationType)
        //: navView.titleLabel.text = infoModel.name
        navView.titleLabel.text = infoModel.name
        //: return navView
        return navView
        //: }()
    }()

    //: private lazy var someoneView: TalkingSomeoneAtMeView = {
    private lazy var someoneView: CalendarReactiveCompatible = {
        //: let v = TalkingSomeoneAtMeView()
        let v = CalendarReactiveCompatible()
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()

    //: private lazy var mentionInfoArr: [[String: Any]] = {
    private lazy var mentionInfoArr: [[String: Any]] = //: return [[String: Any]]()
        .init()
    //: }()
}

// MARK: - Load Data

//: extension TalkingGroupChatController {
extension SmallChatController {
    /// 群聊设置
    //: private func chatSettings() {
    private func lineament() {
        //: TalkingGroupChatManager.share.chatRoomVC = self
        ConfirmGuideReactiveCompatible.share.chatRoomVC = self

        //: func__joinOrQuitRoom(join: true)
        drown(join: true)
    }

    /// 加入或退出聊天室
    /// - Parameter join: 是否加入
    //: private func func__joinOrQuitRoom(join: Bool) {
    private func drown(join: Bool) {
        //: let params = ["roomId": self.conversationData.groupID, "type": (join == true ? 1:2)] as [String: Any]
        let params = [(const_deliveryUrl.lowercased() + String(appPresentationMsg.prefix(5))): self.conversationData.groupID, String(bytes: kStripKey.reversed(), encoding: .utf8)!: join == true ? 1 : 2] as [String: Any]
        //: TalkingChatRequestTool.req_chatRoomJoinOrQuit(params: params) { succeed, result, errorModel in
        TamperRequestTool.menuCompute(params: params) { succeed, _, _ in
            //: guard succeed else { return }
            guard succeed else { return }
//            guard let data = result as? [String: Any] else {
//                return
//            }
//
//            if join {
//
//            }
        }
    }
}

// MARK: - PronatorDisabled

//: extension TalkingGroupChatController: TMessageControllerDelegate {
extension SmallChatController: PronatorDisabled {
    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> SparkCellData! {
    func receive(_: HeavyViewController!, action data: V2TIMMessage!) -> SparkCellData! {
        //: let dic = AcousticPowerThen.parseTXMessageData(data: data.customElem.data)
        let dic = AcousticPowerThen.leaveDoingice(data: data.customElem.data)
        //: let cellData = TUISocialChatManager.handleTXReceiveMessage(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        let cellData = TictacReactiveCompatible.group(withExtral: dic, cusData: data.localCustomData, tempModel: data)

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: SparkCellData!) -> WireView! {
    func burnManager(_: HeavyViewController!, layerSuite data: SparkCellData!) -> WireView! {
        //: if data.isKind(of: GroupTitleChatCellPortraitDataThen.self) {
        if data.isKind(of: GroupTitleChatCellPortraitDataThen.self) {
            //: let cell = TalkingGroupChatTextMsgCell.init(style: .default, reuseIdentifier: TalkingGroupChatTextMsgCell.className())
            let cell = SpecialMessageCell(style: .default, reuseIdentifier: SpecialMessageCell.className())
            //: let cellData = data as! GroupTitleChatCellPortraitDataThen
            let cellData = data as! GroupTitleChatCellPortraitDataThen
            //: cell.fill(with: cellData)
            cell.namePost(with: cellData)
            //: cellData.atBlock = { [weak self] uid in
            cellData.atBlock = { [weak self] uid in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.menu_goToUserDetailController(uid: uid)
                self.asFunUid(uid: uid)
            }
            //: cell.transBlock = { [weak self] in
            cell.transBlock = { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.msgView.translateMsg(with: cellData)
                self.msgView.photo(cellData)
            }

            //: return cell
            return cell
        }

        //: if data.isKind(of: EngineeringCellData.self) {
        if data.isKind(of: EngineeringCellData.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: TalkingChatImageMsgCell.className())
            let cell = ActualMessageCell(style: .default, reuseIdentifier: ActualMessageCell.className())
            //: let cellData = data as! EngineeringCellData
            let cellData = data as! EngineeringCellData
            //: cell.fill(with: cellData)
            cell.namePost(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingGroupChatGiftMsgCellData.self) {
        if data.isKind(of: InsertCellData.self) {
            //: let cell = TalkingGroupChatGiftMsgCell.init(style: .default, reuseIdentifier: TalkingGroupChatGiftMsgCell.className())
            let cell = GroupGiftCompartmentTrailerManufacturerMessageCell(style: .default, reuseIdentifier: GroupGiftCompartmentTrailerManufacturerMessageCell.className())
            //: let cellData = data as! TalkingGroupChatGiftMsgCellData
            let cellData = data as! InsertCellData
            //: cell.fill(with: cellData)
            cell.namePost(with: cellData)

            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatTipsMsgCellData.self) {
        if data.isKind(of: TransferCellData.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: TalkingChatTipsMsgCell.className())
            let cell = FirstViewCell(style: .default, reuseIdentifier: FirstViewCell.className())
            //: let cellData = data as! TalkingChatTipsMsgCellData
            let cellData = data as! TransferCellData
            //: cell.fill(with: cellData)
            cell.namePost(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: WireView!) {
    func white(_: HeavyViewController!, phoneMessageAvatar cell: WireView!) {
        //: if let userModel = cell.messageData?.msgModel.user {
        if let userModel = cell.messageData?.msgModel.user {
            //: if userModel.uid == Int(DismissReactiveCompatible.share.loginUid) {
            if userModel.uid == Int(DismissReactiveCompatible.share.loginUid) {
                //: menu_goToUserDetailController(uid: userModel.uid )
                asFunUid(uid: userModel.uid)
                //: }else{
            } else {
                //: menu_showMunuPopView(user: userModel)
                carteDuJourWithMortal(user: userModel)
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: WireView!) {
    func dimension(_: HeavyViewController!, waveAngle cell: WireView!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: ActualMessageCell.self) { // 图片
            //: let imageCellData = cell.messageData as! EngineeringCellData
            let imageCellData = cell.messageData as! EngineeringCellData
            //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
            var filePath = (libraryPath as NSString).appendingPathComponent(userStyleFormat)
            //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

            //: var model = TalkingMomentPhotoModel.init()
            var model = DemonstrateModelType()
            //: if FileManager.default.fileExists(atPath: filePath) {
            if FileManager.default.fileExists(atPath: filePath) {
                //: let data = NSData.init(contentsOfFile: filePath)
                let data = NSData(contentsOfFile: filePath)
                //: model.imagePic = UIImage(data: data! as Data)
                model.imagePic = UIImage(data: data! as Data)
                //: } else {
            } else {
                //: if imageCellData.imageData != nil {
                if imageCellData.imageData != nil {
                    //: model.imagePic = imageCellData.imageData
                    model.imagePic = imageCellData.imageData
                    //: } else {
                } else {
                    //: model.imgUrl =  imageCellData.imageUrl
                    model.imgUrl = imageCellData.imageUrl
                }
            }
            //: var currenModel = TalkingMomentModel.init()
            var currenModel = IceModelType()
            //: var tempArr: [TalkingMomentPhotoModel] = []
            var tempArr: [DemonstrateModelType] = []
            //: tempArr.append(model)
            tempArr.append(model)
            //: currenModel.pic = tempArr
            currenModel.pic = tempArr
            //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
            let vc = RegisterThen(momentModel: currenModel, index: 0, type: .normal)
            //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
            self.exitSchedule()?.navigationController?.pushViewController(vc, animated: true)

            //: } else if cell.isKind(of: TalkingGroupChatTextMsgCell.self) {
        } else if cell.isKind(of: SpecialMessageCell.self) {
            //: let textCell = cell as! TalkingGroupChatTextMsgCell
            let textCell = cell as! SpecialMessageCell
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = AcousticPowerThen.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = AcousticPowerThen.leaveDoingice(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(notiThemePath.lowercased())] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains(String(bytes: userWhichDealFormat.map{containerFast(line: $0)}, encoding: .utf8)!) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<ArmModelType>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: PushThen.share.func__actionPushForMessage(jumpModel: jumpModel)
                    PushThen.share.orientationAction(jumpModel: jumpModel)
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: FirstViewCell.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! FirstViewCell
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = AcousticPowerThen.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = AcousticPowerThen.leaveDoingice(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(notiThemePath.lowercased())] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains(String(bytes: userWhichDealFormat.map{containerFast(line: $0)}, encoding: .utf8)!) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<ArmModelType>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: PushThen.share.func__actionPushForMessage(jumpModel: jumpModel)
                    PushThen.share.orientationAction(jumpModel: jumpModel)
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: WireView!) {
    func first(_: HeavyViewController!, plus cell: WireView!) {
        //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
        warning(msgData: cell.messageData!, reSend: true)
    }
}

// MARK: - Event

//: extension TalkingGroupChatController {
extension SmallChatController {
    /// 销毁倒计时
    //: private func destroyTipsTimer() {
    private func opposition() {
        //: if tipsTimer != nil {
        if tipsTimer != nil {
            //: tipsTimer?.invalidate()
            tipsTimer?.invalidate()
            //: tipsTimer = nil
            tipsTimer = nil
        }
    }

    /// 点击用户头像，展示操作面板
    /// - Parameter user: 用户模型
    //: private func menu_showMunuPopView(user: AbTalkingChatMsgUserModel) {
    private func carteDuJourWithMortal(user: RootModel) {
        //: view.endEditing(true)
        view.endEditing(true)
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = DoingcePopView(frame: self.view.frame)
        //: vc.title = user.nickname
        vc.title = user.nickname
        //: vc.initwithList(cellTitleList: ["Send Gift".localized, "@", "View info".localized, "Report".localized])
        vc.reasonPostList(cellTitleList: [(String(main_bouncePath.prefix(5)) + "Gift").localized, "@", (String(notiSecureMessage) + String(kValidKey)).localized, (String(appLeaguePath)).localized])
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: self.menu_sentGiftAction(user: user)
                self.lightEnableLimit(user: user)
                //: } else if index == 1 {
            } else if index == 1 {
                //: self.menu_atTa(user: user)
                self.at(user: user)
                //: } else if index == 2 {
            } else if index == 2 {
                //: self.menu_goToUserDetailController(uid: user.uid)
                self.asFunUid(uid: user.uid)
                //: } else if index == 3 {
            } else if index == 3 {
                //: self.menu_reportAction(uid: String(user.uid))
                self.confirm(uid: String(user.uid))
            }
        }
    }

    /// 送礼
    /// - Parameter user: 用户模型
    //: private func menu_sentGiftAction(user: AbTalkingChatMsgUserModel) {
    private func lightEnableLimit(user: RootModel) {
        //: self.msgInputView.clickgiftBtn()
        self.msgInputView.fileRouteBtn()
        //: let model = TalkingChatRoomMemberModel()
        let model = SparkTransformable()
        //: model.uid = "\(user.uid)"
        model.uid = "\(user.uid)"
        //: model.nickname = user.nickname
        model.nickname = user.nickname
        //: model.headPic = user.headPic
        model.headPic = user.headPic
        //: model.sex = user.sex
        model.sex = user.sex
        //: model.age = Int(user.age) ?? 0
        model.age = Int(user.age) ?? 0
        //: self.msgInputView.giftView.didClickSelectedRow(model: model)
        self.msgInputView.giftView.searchedContainer(model: model)
    }

    /// @Ta
    /// - Parameter user: 用户模型
    //: private func menu_atTa(user: AbTalkingChatMsgUserModel) {
    private func at(user: RootModel) {
        //: let dict: [String: Any] = ["uid": String(user.uid), "name": user.nickname]
        let dict: [String: Any] = [String(bytes: k_floorFormat.reversed(), encoding: .utf8)!: String(user.uid), String(bytes: noti_resentMessage.map{pressSuccess(connect: $0)}, encoding: .utf8)!: user.nickname]
        //: self.msgInputView.receiveAtInfo(range: nil, dict: dict)
        self.msgInputView.wrap(range: nil, dict: dict)
        //: self.msgInputView.inputTextView.becomeFirstResponder()
        self.msgInputView.inputTextView.becomeFirstResponder()
    }

    /// 访问用户详情
    /// - Parameter uid: 用户Id
    //: private func menu_goToUserDetailController(uid: Int) {
    private func asFunUid(uid: Int) {
        //: PushThen.share.func__pushToUserDetailVC(uid: String(uid))
        PushThen.share.sessionSomebody(uid: String(uid))
    }

    /// 举报
    /// - Parameter uid: 用户Id
    //: private func menu_reportAction(uid: String) {
    private func confirm(uid: String) {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: uid)
        let reportView = PositionView(frame: UIScreen.main.bounds, type: .reportUserType, rId: uid)
        //: reportView.showReportViewIn(view: nil)
        reportView.priceBlack(view: nil)
        //: reportView.reportHideBlock = { [weak self] _ in
        reportView.reportHideBlock = { [weak self] _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: return
        }
    }
}

// MARK: - 发送消息

//: extension TalkingGroupChatController {
extension SmallChatController {
    /// 发送文本消息
    /// - Parameter msgStr: 文本

    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func blow(msgStr: String, quoteModel _: LightPrimeModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            enablece(showMsg: (appDisableData + "t send b" + String(notiMenuFocusKey.suffix(6)) + "essage").localized)
            //: return
            return
        }

        //: let uidStr = NSMutableString()
        let uidStr = NSMutableString()
        //: var jumps = [[String: String]]()
        var jumps = [[String: String]]()
        //: if msgInputView.atArr.count > 0 {
        if msgInputView.atArr.count > 0 { // 有@ 消息
            //: msgInputView.atArr.forEach { uid in
            msgInputView.atArr.forEach { uid in
                //: let dict = ["jumpKey": "user", "uid": uid]
                let dict = [String(bytes: userWhichDealFormat.map{containerFast(line: $0)}, encoding: .utf8)!: (main_mateData.lowercased()), String(bytes: k_floorFormat.reversed(), encoding: .utf8)!: uid]
                //: uidStr.append(uid+",")
                uidStr.append(uid + ",")
                //: jumps.append(dict)
                jumps.append(dict)
            }
            //: uidStr.deleteCharacters(in: NSRange(location: uidStr.length-1, length: 1))
            uidStr.deleteCharacters(in: NSRange(location: uidStr.length - 1, length: 1))
        }

        //: var msgInfo: [String: Any] = ["content": temStr, "msgType": "txt", "isGroup": "1"]
        var msgInfo: [String: Any] = [String(bytes: noti_squareUrl.reversed(), encoding: .utf8)!: temStr, (String(user_contactStatisticalValue.suffix(7))): (app_ehPath.replacingOccurrences(of: "manual", with: "t")), (String(k_addData) + String(user_matePath.prefix(6))): "1"]
        //: if jumps.count > 0 {
        if jumps.count > 0 {
            //: msgInfo["jumps"] = jumps
            msgInfo[(String(user_continuingPath))] = jumps
        }

        // 接口参数
        //: var paramsDic: [String: Any] = ["message": temStr,
        var paramsDic: [String: Any] = [String(bytes: show_tapTitle.map{tingOral(appeal: $0)}, encoding: .utf8)!: temStr,
                                        //: "type": "TxtMsg",
                                        String(bytes: kStripKey.reversed(), encoding: .utf8)!: (String(appDarkPartnerValue.suffix(6))),
                                        //: "roomId": self.infoModel.roomId]
                                        (const_deliveryUrl.lowercased() + String(appPresentationMsg.prefix(5))): self.infoModel.roomId]
        //: if uidStr.length > 0 {
        if uidStr.length > 0 {
            //: paramsDic["toUid"] = uidStr
            paramsDic[(String(constNoticeTitle.prefix(5)))] = uidStr
        }

        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(kBeyondKey) + String(userNumbStr.prefix(5))): msgInfo, String(bytes: mainPoKey.map{pureWrapBass(sense: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = TictacReactiveCompatible.smooth(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            warning(msgData: cellData, reSend: false)
        }
    }

    /// 发送图片消息
    /// - Parameter img: 图片
    //: private func func__sendPictureMsg(img: UIImage) {
    private func moveIn(img: UIImage) {
        //: let imagePath = EngineeringCellData.getChatImagePath(image: img)
        let imagePath = EngineeringCellData.getImpressionImageChatPath(image: img)
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath]
        let msgInfo: [String: Any] = [String(bytes: main_firstName.map{$0^229}, encoding: .utf8)!: (String(userDemandId.prefix(6))), (String(user_contactStatisticalValue.suffix(7))): (String(mainExampleKey)), String(bytes: kSinceQuickStr.map{complaintMotion(stream: $0)}, encoding: .utf8)!: imagePath]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(String(kMakerUrl)): imagePath,
                                        //: "type": "ImgMsg",
                                        String(bytes: kStripKey.reversed(), encoding: .utf8)!: (String(userDemandId.prefix(6))),
                                        //: "roomId": self.infoModel.roomId]
                                        (const_deliveryUrl.lowercased() + String(appPresentationMsg.prefix(5))): self.infoModel.roomId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(kBeyondKey) + String(userNumbStr.prefix(5))): msgInfo, String(bytes: mainPoKey.map{pureWrapBass(sense: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = TictacReactiveCompatible.smooth(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            warning(msgData: cellData, reSend: false)
        }
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: SparkCellData, reSend: Bool) {
    private func warning(msgData: SparkCellData, reSend _: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            enablece(showMsg: main_textStr)
            //: return
            return
        }
        // 插入本地
        //: self.msgView.sendCustomMessage(msgData)
        self.msgView.contrast(msgData)
        // 处理请求参数
        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
//        params["toUid"] = self.targetId
        //: if params.keys.contains("pic") { // 图片
        if params.keys.contains((String(kMakerUrl))) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(String(kMakerUrl))] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(String(kMakerUrl))] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }

        // 发送消息接口
        //: TalkingChatRequestTool.req_SendRoomMsg(param: params, completion: { succeed, result, errorModel in
        TamperRequestTool.livePrompt(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.usanceLeftHandednessNetIncreaseThin(msgData, present: nil, push: NSNumber(value: 0), direction: .Msg_Status_Fail)
                                
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.yesterdayResend(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            //: let dataDict = result as! [String: Any]
            let dataDict = result as! [String: Any]
            //: let msgInfo = dataDict["msgInfo"] as! [String: Any]
            let msgInfo = dataDict[(String(kBeyondKey) + String(userNumbStr.prefix(5)))] as! [String: Any]

            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(msgData, msgId: msgInfo["msgId"] as? String, msgIncome: NSNumber(value: 0), status: .Msg_Status_Succ)
            self.msgView.usanceLeftHandednessNetIncreaseThin(msgData, present: msgInfo[(String(show_minimizePath))] as? String, push: NSNumber(value: 0), direction: .Msg_Status_Succ)
            
            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((String(const_lipUrl.prefix(9)) + showSubmitMessage.replacingOccurrences(of: "mist", with: "in"))) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(String(const_lipUrl.prefix(9)) + showSubmitMessage.replacingOccurrences(of: "mist", with: "in"))] as! NSNumber
                //: DismissReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                DismissReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }
            //: })
        })
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool) {
    private func yesterdayResend(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool) {
        //: if errorCode == 50203 {
        if errorCode == 50203 {
            //: guard DismissReactiveCompatible.share.loginUserMode.status != 1 else {
            guard DismissReactiveCompatible.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    enablece(showMsg: errorStr)
                }
                //: return
                return
            }

            //: PushThen.share.func__jumpToWebRecharge(clickEvent: clickSendingiftsButton, sufficient: false)
            PushThen.share.twoParams(clickEvent: showLimitTitle, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.maneuver()
            //: insertTipMsg_Nobalance(tipStr: errorStr)
            nanometer(tipStr: errorStr)
            //: }else if errorCode == 10888 {
        } else if errorCode == 10888 {
            //: let view = TalkingLiveVipSubscribePopView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = DisenableThen(frame: CGRect(x: 0, y: 0, width: main_logStr, height: main_nameData))
            //: view.show()
            view.animaShow()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                enablece(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 输入框

//: extension TalkingGroupChatController: ChatInputViewDelegate {
extension SmallChatController: FirstReactiveCompatible {
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func bubble(giftModel _: TillGiftModel, giftNum _: String) {}

    /// 输入框输入内容停顿超过1秒
    //: func func_inputContentIntervalOneSecond() {}
    func assignSave() {}

    /// 输入框@ 在线列表用户
    //: func func__selectMentionUser(range: NSRange) {
    func dialogDownFlat(range: NSRange) {
        //: let vc = TalkingChatRoomOnlineListController()
        let vc = SamplePutDataSource()
        //: vc.roomID = self.infoModel.roomId
        vc.roomID = self.infoModel.roomId
        //: vc.titleType = .callNumber
        vc.titleType = .callNumber
        //: vc.selectedBlock = {[weak self] dict in
        vc.selectedBlock = { [weak self] dict in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.atFlag = true
            self.atFlag = true
            //: self.msgInputView.receiveAtInfo(range: range, dict: dict)
            self.msgInputView.wrap(range: range, dict: dict)
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
        }
        //: self.navigationController?.pushViewController(vc, animated: true)
        self.navigationController?.pushViewController(vc, animated: true)
    }

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func liveBy(actionType: DevoteMagnitudeLiteral) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: func__choicePhotos()
            succeedTool()

        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {}
    func hello(giftModel _: TillGiftModel, giftNum _: String) {}

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {
    func bestowmentModel(giftModel: TillGiftModel, giftNum: String, model: SparkTransformable) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            enablece(showMsg: main_textStr)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] =  model.uid
        params[(String(constNoticeTitle.prefix(5)))] = model.uid
        //: params["giftId"] = giftModel.gid
        params[(String(data_redWithinStr.suffix(6)))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(userDistrictUrl) + String(show_spareName))] = giftNum
        //: params["roomId"] = infoModel.roomId
        params[(const_deliveryUrl.lowercased() + String(appPresentationMsg.prefix(5)))] = infoModel.roomId
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(constAdministrativeInnerUrl))] = giftModel.pkgItemsetId
        }

        // 送礼接口
        //: TalkingChatRequestTool.req_SendGiftChatRoom(param: params, completion: { succeed, result, errorModel in
        TamperRequestTool.financialAid(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.yesterdayResend(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.localRoad(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, model: model, isFromInputView: true)
            self.speed(extral: result as! [String: Any?], model: model, isFromInputView: true)
            //: })
        })
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func tillNarrow(heightToBottom: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: someoneView.snp.updateConstraints { make in
        someoneView.snp.updateConstraints { make in
            //: make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            make.bottom.equalTo(msgInputView.snp.top).offset(-103)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.input(true)
            self.msgView.input(true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func bottom(height: CGFloat) {
        //: self.msgInputView.snp.updateConstraints { make in
        self.msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: self.view.needsUpdateConstraints()
        self.view.needsUpdateConstraints()
        //: self.view.updateConstraintsIfNeeded()
        self.view.updateConstraintsIfNeeded()
        //: UIView.animate(withDuration: 0.3, animations: {
        UIView.animate(withDuration: 0.3, animations: {
            //: self.view.layoutIfNeeded()
            self.view.layoutIfNeeded()
            //: })
        })
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.input(true)
            self.msgView.input(true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func countRecognizer(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.countRecognizer(tap)
    }

    //: func func__starRecordBtnDown() {}
    func presentation() {}

    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {}
    func exceptFile(voicePath _: String, voiceTime _: Int) {}
}

// MARK: - 选择图片

//: extension TalkingGroupChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension SmallChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func succeedTool() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        SpecialPermissionTool.burst(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = MedalPickTool.sameMember(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: PushThen.share.func__getCurrentActivityVC()?.present(vc, animated: true)
                PushThen.share.doingGoing()?.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 _: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 _: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.func__sendPictureMsg(img: photos![0])
                            self.moveIn(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func camaraMapPhotos() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        SpecialPermissionTool.exitSmart(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.func__sendPictureMsg(img: photo)
                self.moveIn(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }
}

// MARK: - 处理@ 消息

//: extension TalkingGroupChatController {
extension SmallChatController {
    /// 接收到群聊@ 消息
    /// - Parameter info: @消息数据
    //: func handleMentionMsg(info: [String: Any]) {
    func totalerpretDown(info: [String: Any]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard info["roomId"] as! String == self.conversationData.groupID else { return }
            guard info[(const_deliveryUrl.lowercased() + String(appPresentationMsg.prefix(5)))] as! String == self.conversationData.groupID else { return }
            //: if self.mentionInfoArr.count >= 5 {
            if self.mentionInfoArr.count >= 5 { // 只保留最新5条@ 消息
                //: self.mentionInfoArr.removeFirst()
                self.mentionInfoArr.removeFirst()
            }
            //: self.mentionInfoArr.append(info)
            self.mentionInfoArr.append(info)
            //: self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false:true
            self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false : true
        }
    }
}

// MARK: - 提示消息

//: extension TalkingGroupChatController {
extension SmallChatController {
    //: func func__TipsNotification() {
    func attachPan() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(insertTipsNotif(notification:)),
                                               selector: #selector(banTips(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: user_dismissData,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) {
    @objc func banTips(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: TalkingChatTipsMsgCellData  = userinfo["msg"] as! TalkingChatTipsMsgCellData
        let celldata: TransferCellData = userinfo[(showMediaPath.replacingOccurrences(of: "rich", with: "m"))] as! TransferCellData

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.input(true)
        self.msgView.input(true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func nanometer(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo = ["content": tipStr, "msgType": "tips"]
        let msgInfo = [String(bytes: noti_squareUrl.reversed(), encoding: .utf8)!: tipStr, (String(user_contactStatisticalValue.suffix(7))): (user_backgroundProblemMessage.replacingOccurrences(of: "toward", with: "t"))]
        //: var message: SparkCellData?
        var message: SparkCellData?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSend(withExtral: msgInfo, isSender: true, isTip: true)
            message = TictacReactiveCompatible.smooth(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.input(true)
        self.msgView.input(true)
    }

    /// 公共聊天室插入系统提示消息
    //: func func__chatRoom_insertTipMsg() {
    func spend() {
        // 延时，内存插入
        //: tipsTimer = Timer.scheduledTimer(withTimeInterval: 5, repeats: false, block: {[weak self] time in
        tipsTimer = Timer.scheduledTimer(withTimeInterval: 5, repeats: false, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard DismissReactiveCompatible.share.appUserConfigMode.rTips.count > 0 else { return }
            guard DismissReactiveCompatible.share.appUserConfigMode.rTips.count > 0 else { return }
            //: self.insertTipMsg_Nobalance(tipStr: DismissReactiveCompatible.share.appUserConfigMode.rTips, needInsertLocal: false)
            self.nanometer(tipStr: DismissReactiveCompatible.share.appUserConfigMode.rTips, needInsertLocal: false)
            //: })
        })
    }
}

// MARK: - 动效

//: extension TalkingGroupChatController {
extension SmallChatController {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, model: TalkingChatRoomMemberModel, isFromInputView: Bool) {
    func speed(extral: [String: Any?], model: SparkTransformable, isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(String(kBeyondKey) + String(userNumbStr.prefix(5)))] as! [String: Any]
        /// 用于本地判断celldata类型
        //: msgInfoDic["isGroup"] = "1"
        msgInfoDic[(String(k_addData) + String(user_matePath.prefix(6)))] = "1"
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[(String(user_contactStatisticalValue.suffix(7)))] = (k_runKey.replacingOccurrences(of: "lope", with: "if"))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(kBeyondKey) + String(userNumbStr.prefix(5)))] = msgInfoDic

        //: var toUser = ["uid": model.uid,
        var toUser = [String(bytes: k_floorFormat.reversed(), encoding: .utf8)!: model.uid,
                      //: "nickname": model.nickname,
                      (user_anotherTitle.replacingOccurrences(of: "year", with: "e")): model.nickname,
                      //: "headPic": model.headPic]
                      String(bytes: data_regionStr.map{transportationProximate(numb: $0)}, encoding: .utf8)!: model.headPic]

        //: if model.nickname == "All Numbers".localized {
        if model.nickname == (String(showTwoId.suffix(6)) + appMouthTitle.replacingOccurrences(of: "distribution", with: "r")).localized {
            //: if extralInfo.keys.contains("gift") != false {
            if extralInfo.keys.contains((k_runKey.replacingOccurrences(of: "lope", with: "if"))) != false {
                //: let giftdic = extralInfo["gift"] as! Dictionary<String, Any>
                let giftdic = extralInfo[(k_runKey.replacingOccurrences(of: "lope", with: "if"))] as! [String: Any]
                //: toUser["nickname"] = "\(giftdic["sendNum"] ?? "1")"
                toUser[(user_anotherTitle.replacingOccurrences(of: "year", with: "e"))] = "\(giftdic[(String(k_halfFashionFormat))] ?? "1")"
            }
        }
        //: extralInfo["toUser"] = toUser
        extralInfo[(String(data_viewPremiumName))] = toUser

        //: let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = TictacReactiveCompatible.smooth(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.contrast(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((String(show_varietyName) + String(const_satisfyValue))) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(String(show_varietyName) + String(const_satisfyValue))] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.usanceLeftHandednessNetIncreaseThin(cellData, present: msgInfoDic[(String(show_minimizePath))] as? String, push: msgIncome, direction: .Msg_Status_Succ)
        
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any], model: model)
        replacementTo(giftDic: extral as [String: Any], model: model)
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((String(const_lipUrl.prefix(9)) + showSubmitMessage.replacingOccurrences(of: "mist", with: "in"))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(String(const_lipUrl.prefix(9)) + showSubmitMessage.replacingOccurrences(of: "mist", with: "in"))] as! NSNumber
            //: DismissReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            DismissReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: DismissReactiveCompatible.share.loginUserMode.mf_coin)
            msgInputView.giftView.path(needReload: false, mf_coin: DismissReactiveCompatible.share.loginUserMode.mf_coin)
        }
        //: msgInputView.giftView.saveRecentMembers()
        msgInputView.giftView.everyInmate()
    }

    //: func initCachEffectData() {
    func nearTableIncome() {
        //: giftEffectView.initCachAnimatData(tagetID: infoModel.roomId)
        giftEffectView.thenMakeupId(tagetID: infoModel.roomId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>, model: TalkingChatRoomMemberModel) {
    func replacementTo(giftDic: [String: Any], model: SparkTransformable) {
        //: guard let giftModel = giftEffectView.chatRoomDisposeReceiveGiftMsg(giftMessageDic: (giftDic as NSDictionary) as! Dictionary<String, Any>, model: model) else { return }
        guard let giftModel = giftEffectView.dispose(giftMessageDic: (giftDic as NSDictionary) as! [String: Any], model: model) else { return }
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray.init()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in MotivationThen.share.beyondGet() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [FenceTransformable]
        }
        //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel)
        giftTrackView.modelSource(model: giftModel)

        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.addressReloadDict(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func dowry(giftArr: [Any]) {
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray.init()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in MotivationThen.share.beyondGet() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [FenceTransformable]
        }
        //: for i in 0..<giftArr.count {
        for i in 0 ..< giftArr.count {
            //: let giftModel = giftArr[i]
            let giftModel = giftArr[i]
            //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel as! TalkingGiftMsgAnimatModel)
            giftTrackView.modelSource(model: giftModel as! RoughnessMeasurable)
        }

        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.framework(modelArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingGroupChatController {
extension SmallChatController {
    /// 创建UI
    //: private func createUI() {
    private func arteriaCiliaris() {
        //: self.addChild(msgView)
        self.addChild(msgView)
        //: view.addSubview(msgView.view)
        view.addSubview(msgView.view)
        //: view.addSubview(someoneView)
        view.addSubview(someoneView)
        //: view.addSubview(giftEffectView)
        view.addSubview(giftEffectView)
        //: view.insertSubview(giftTrackView, belowSubview: giftEffectView)
        view.insertSubview(giftTrackView, belowSubview: giftEffectView)
        //: view.addSubview(msgInputView)
        view.addSubview(msgInputView)
        //: view.addSubview(navView)
        view.addSubview(navView)
        //: msgView.targetID = self.conversationData.groupID
        msgView.targetID = self.conversationData.groupID
    }

    /// 约束
    //: private func createUIConstraints() {
    private func eachSmooth() {
        //: navView.snp.makeConstraints { make in
        navView.snp.makeConstraints { make in
            //: make.leading.trailing.top.equalTo(view)
            make.leading.trailing.top.equalTo(view)
            //: make.height.equalTo(StatusBarNavigationBarHeight)
            make.height.equalTo(constAgentJumpValue)
        }
        //: msgInputView.snp.makeConstraints { make in
        msgInputView.snp.makeConstraints { make in
            //: make.leading.trailing.bottom.equalTo(view)
            make.leading.trailing.bottom.equalTo(view)
            //: make.height.equalTo(msgInputView.BackViewHeight)
            make.height.equalTo(msgInputView.BackViewHeight)
        }
        //: msgView.view.snp.makeConstraints { make in
        msgView.view.snp.makeConstraints { make in
            //: make.leading.trailing.equalTo(view)
            make.leading.trailing.equalTo(view)
            //: make.top.equalTo(StatusBarNavigationBarHeight)
            make.top.equalTo(constAgentJumpValue)
            //: make.bottom.equalTo(msgInputView.snp.top)
            make.bottom.equalTo(msgInputView.snp.top)
        }
        //: someoneView.snp.makeConstraints { make in
        someoneView.snp.makeConstraints { make in
            //: make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            //: make.trailing.equalToSuperview()
            make.trailing.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 130, height: 37))
            make.size.equalTo(CGSize(width: 130, height: 37))
        }
        //: giftEffectView.snp.makeConstraints { make in
        giftEffectView.snp.makeConstraints { make in
            //: make.leading.trailing.top.bottom.equalTo(view)
            make.leading.trailing.top.bottom.equalTo(view)
        }
        //: giftTrackView.snp.makeConstraints { make in
        giftTrackView.snp.makeConstraints { make in
            //: make.leading.equalTo(view)
            make.leading.equalTo(view)
            //: make.height.equalTo(180)
            make.height.equalTo(180)
            //: make.bottom.equalTo(-actualHeight(h: 220)-kDeviceSafeBottomHeight)
            make.bottom.equalTo(-actualHeight(h: 220) - appInviteValue)
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func presentationBirth() {
        // 返回
        //: navView.backButton.rx.tap.subscribe(onNext: { [weak self] in
        navView.backButton.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
            //: }).disposed(by: disposeBag)
        }).disposed(by: disposeBag)

        // 在线列表
        //: navView.onlineListBtn.rx.tap.subscribe(onNext: { [weak self] in
        navView.onlineListBtn.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let vc = TalkingChatRoomOnlineListController()
            let vc = SamplePutDataSource()
            //: vc.roomID = self.infoModel.roomId
            vc.roomID = self.infoModel.roomId
            //: vc.titleType = .normal
            vc.titleType = .normal
            //: self.navigationController?.pushViewController(vc, animated: true)
            self.navigationController?.pushViewController(vc, animated: true)
            //: }).disposed(by: disposeBag)
        }).disposed(by: disposeBag)

        // 群聊someone@ me 点击事件
        //: someoneView.mentionBtn.rx.tap
        someoneView.mentionBtn.rx.tap
            //: .throttle(.milliseconds(1000), scheduler: MainScheduler.instance)
            .throttle(.milliseconds(1000), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                // 找到当前消息，滚动到指定位置
                //: guard let dict = self.mentionInfoArr.first else { return }
                guard let dict = self.mentionInfoArr.first else { return }
                //: for (index, cellData) in self.msgView.uiMsgs.enumerated() {
                for (index, cellData) in self.msgView.uiMsgs.enumerated() {
                    //: if (cellData as! SparkCellData).isKind(of: GroupTitleChatCellPortraitDataThen.self) {
                    if (cellData as! SparkCellData).isKind(of: GroupTitleChatCellPortraitDataThen.self) {
                        //: let groupCellData = cellData as! GroupTitleChatCellPortraitDataThen
                        let groupCellData = cellData as! GroupTitleChatCellPortraitDataThen
                        //: if groupCellData.msgID == dict["msgId"] as! String {
                        if groupCellData.msgID == dict[(String(show_minimizePath))] as! String {
                            //: self.msgView.tableView.scroll(toRow: UInt(index), inSection: 0, at: .bottom, animated: true)
                            self.msgView.tableView.scroll(toRow: UInt(index), inSection: 0, at: .bottom, animated: true)
                            //: self.mentionInfoArr.removeFirst()
                            self.mentionInfoArr.removeFirst()
                            //: self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false:true
                            self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false : true
                        }
                    }
                }
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)

        // 被挤下线时，退出群聊
        //: AfterThen.shared.rx
        AfterThen.shared.rx
            //: .observeWeakly(Bool.self, "isConnection")
            .observeWeakly(Bool.self, (dataHolderOwnerFinishStr.lowercased() + String(appBetweenBoxMessage)))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if value == false {
                if value == false {
                    //: self.navigationController?.popViewController(animated: true)
                    self.navigationController?.popViewController(animated: true)
                }
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }
}
