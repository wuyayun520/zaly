
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let showAgoKey:[UInt8] = [0x5,0x2,0x5,0x18,0x44,0xf,0x3,0x8,0x9,0x1e,0x56,0x45,0x4c,0x4,0xd,0x1f,0x4c,0x2,0x3,0x18,0x4c,0xe,0x9,0x9,0x2,0x4c,0x5,0x1,0x1c,0x0,0x9,0x1,0x9,0x2,0x18,0x9,0x8]

/*: "uid" :*/
fileprivate let kResourceValue:[UInt8] = [0x81,0x75,0x70]

fileprivate func distinctiveSum(change num: UInt8) -> UInt8 {
    let value = Int(num) - 12
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "headPic" :*/
fileprivate let show_denyTitle:String = "variation maker party lifetimeheadPic"

/*: "nickname" :*/
fileprivate let k_poReloadLevelData:String = "capableickcapablea"
fileprivate let appFirmKey:[Character] = ["m","e"]

/*: "birthday" :*/
fileprivate let k_sureEnablePath:String = "birthdooray"

/*: "videoUrl" :*/
fileprivate let show_selectMessage:String = "videoUrlseparate air primary"

/*: "location" :*/
fileprivate let userOwnPath:String = "locatbone"
fileprivate let data_lineName:[Character] = ["o","n"]

/*: "cmd" :*/
fileprivate let data_exName:[UInt8] = [0x97,0xa1,0x98]

fileprivate func liteFace(way num: UInt8) -> UInt8 {
    let value = Int(num) + 204
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "refuseCall" :*/
fileprivate let show_vitalCountTitle:String = "weak levelrefuseC"
fileprivate let constTenderPath:String = "avertical"

/*: "data" :*/
fileprivate let kAbsTitle:[UInt8] = [0x48,0x4d,0x58,0x4d]

/*: "Call interruption" :*/
fileprivate let notiTabKey:[Character] = ["C","a","l","l"," ","i","n","t","e","r"]
fileprivate let user_nothingData:String = "ruptiodisc"

/*: "stopCall" :*/
fileprivate let userSkinText:[Character] = ["s","t","o"]
fileprivate let userGrayId:[Character] = ["p","C","a","l","l"]

/*: "logId" :*/
fileprivate let noti_writtenTitle:[UInt8] = [0x4e,0x4d,0x45,0x6b,0x46]

/*: "cancelCall" :*/
fileprivate let appPinMessage:String = "communicationnce"

/*: "No answer" :*/
fileprivate let constClipTitle:[Character] = ["N","o"," ","a","n","s","w"]
fileprivate let appFormData:[Character] = ["e","r"]

/*: "duration" :*/
fileprivate let mainMpData:[Character] = ["d","u","r","a","t","i","o"]
fileprivate let userPerformEyebrowPlatFormat:[Character] = ["n"]

/*: "She has hung up" :*/
fileprivate let user_transitUrl:[Character] = ["S","h","e"," ","h","a","s"," ","h","u","n"]
fileprivate let k_twoData:String = "tick conversation contactg up"

/*: "cancelSex" :*/
fileprivate let notiMarketingValue:[Character] = ["c","a","n","c","e","l"]
fileprivate let data_violationUrl:[Character] = ["S","e","x"]

/*: "He has hung up" :*/
fileprivate let constPanStaffPath:String = "He hweek protection two select"
fileprivate let user_halfValue:[Character] = ["g"," ","u","p"]

/*: "endType" :*/
fileprivate let noti_chooseUrl:String = "oval professionalendTyp"
fileprivate let user_healthySumTitle:[Character] = ["e"]

/*: "stopSex" :*/
fileprivate let main_coreValue:[Character] = ["s","t","o","p","S","e"]
fileprivate let mainPromptOccurKey:String = "max"

/*: "Insufficient gold coins, call has ended" :*/
fileprivate let dataActualValue:[UInt8] = [0x8d,0xaa,0xb7,0xb1,0xa2,0xa2,0xad,0xa7,0xad,0xa1,0xaa,0xb0,0xe4,0xa3,0xab,0xa8,0xa0,0xe4,0xa7,0xab,0xad,0xaa,0xb7,0xe8,0xe4,0xa7,0xa5,0xa8,0xa8,0xe4,0xac,0xa5,0xb7,0xe4,0xa1,0xaa,0xa0,0xa1,0xa0]

private func flowMini(situation num: UInt8) -> UInt8 {
    return num ^ 196
}

/*: "The other hung up the phone" :*/
fileprivate let const_automaticallyFormat:[Character] = ["T","h","e"," ","o","t","h","e","r"," ","h","u"]
fileprivate let const_bassChinData:String = "ng ubelow who telephone pic"
fileprivate let userPromptOpeneValue:String = "beyondhone"

/*: "type" :*/
fileprivate let showArgumentPath:String = "typmy"

/*: "jumpUrl" :*/
fileprivate let appHideName:String = "sole"
fileprivate let appSeveralId:String = "umpUrlcancel universal cancel separate league"

/*: "Cancel" :*/
fileprivate let constRelatedProblemMessage:String = "style connect deep written whileCance"
fileprivate let user_combineSomebodyMessage:String = "challenge"

/*: "Get gold coins" :*/
fileprivate let appBetweenText:String = "it shallGet go"
fileprivate let app_micContent:String = "announcement"

/*: "addBean" :*/
fileprivate let main_postSessionWhereId:String = "over lab snap operate holdaddBea"
fileprivate let show_thenFormat:String = "tell"

/*: "refuseSex" :*/
fileprivate let mainResultUrl:String = "refuseSincome wrap together"
fileprivate let kPairNorGapPath:String = "EX"

/*: "onAcceptCall" :*/
fileprivate let notiProgramText:[Character] = ["o"]
fileprivate let showShallKey:String = "nAccerecording operation speak"

/*: "acceptCall" :*/
fileprivate let dataLimitHappyGiftValue:String = "bonecce"
fileprivate let constCoreStr:String = "ptCallover run"

/*: "Calls are cancelled" :*/
fileprivate let k_cordThanKey:String = "nag single talk close priorityCall"
fileprivate let userMakeFormat:String = "e canwho up"
fileprivate let constHeatCloudText:String = "helpd"

/*: "The call has ended" :*/
fileprivate let app_profileYesterdayTitle:String = "The chuman express"
fileprivate let app_slideValue:[Character] = ["a","l","l"," ","h"]
fileprivate let const_commonTrailUrl:String = "target literal impulse around diskas e"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  TrailerRecognizerDelegate.swift
//  AbroadTalking
//
//  Created by young on 2022/11/11.
//

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver
//: import UIKit
import UIKit

// 通话监听(必须为全局属性，否则无法接听)
//: let voiceCallHelper = CXCallObserver()
let dataShowStr = CXCallObserver()
//: class TalkingVoiceChatViewController: TalkingBaseViewController {
class TrailerRecognizerDelegate: WeddingLovelyRecognizerDelegate {
    //: var chatModel: TalkingVideoChatModel?
    var chatModel: IceChatModel? // 语音通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: ApplicationReactiveCompatible? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVoiceMiniView?
    private var miniView: RatingMiniView? // 音频通话小窗口

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        SantaLuciaFirSocketDelegate.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        SantaLuciaFirSocketDelegate.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        SantaLuciaFirSocketDelegate.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: showAgoKey.map{$0^108}, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! TicktockViewController
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.actionEnablePhone()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVoiceAuth()
            voiceOver()
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.currInfoVC = nil
            SantaLuciaFirSocketDelegate.shared.currInfoVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.motivation()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        strokeSubviews()
        //: setupSubViewsConstraint()
        custom()
        //: initNotificationObserver()
        heckleObserver()
        //: req_callGetUserInfo()
        bibliography()
        //: timeoutRules()
        huntingLicenseRules()
        //: resumeTalk()
        pressurize()
        //: setUpBGMPlayer()
        identityRefuse()
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if DismissReactiveCompatible.share.appConfigMode.disableShootScreen {
            if DismissReactiveCompatible.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = AeronauticalEngineeringReactiveCompatible(frame: CGRect(x: 0, y: 0, width: main_logStr, height: main_nameData))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.isInfo = false
        SantaLuciaFirSocketDelegate.shared.isInfo = false
        //: TalkingSocketManager.shared.isCalling = false
        SantaLuciaFirSocketDelegate.shared.isCalling = false
        //: self.stopVCAllActivties()
        self.lightActivties()
    }

    // MARK: - Lazy Load

    /// 音频通话操作按钮
    //: private lazy var interactionView: Talking1v1VoiceInteractionView = {
    private lazy var interactionView: DarkView = {
        //: let v = Talking1v1VoiceInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = DarkView(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    //: private lazy var voiceManager: TalkingVoiceChatManager = {
    private lazy var voiceManager: FirstChatManager = {
        //: let m = TalkingVoiceChatManager()
        let m = FirstChatManager()
        //: m.handsfreeBtn = self.interactionView.handsfreeBtn
        m.handsfreeBtn = self.interactionView.handsfreeBtn
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingVoiceChatViewController {
extension TrailerRecognizerDelegate {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func bibliography() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        RegularArtCloudDelegate.draftCompletion(uid: pairUid) { [weak self] succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: kResourceValue.map{distinctiveSum(change: $0)}, encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(show_denyTitle.suffix(7)))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(k_poReloadLevelData.replacingOccurrences(of: "capable", with: "n") + String(appFirmKey))].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(k_sureEnablePath.replacingOccurrences(of: "door", with: "d"))].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(String(show_selectMessage.prefix(8)))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(userOwnPath.replacingOccurrences(of: "bone", with: "i") + String(data_lineName))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.comeUpMakeup()
        }
    }
}

// MARK: - Event

//: extension TalkingVoiceChatViewController {
extension TrailerRecognizerDelegate {
    /// 权限校验，播放BGM
    //: private func checkVoiceAuth() {
    private func voiceOver() {
        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        SpecialPermissionTool.visualAspect(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.enablece(showMsg: appMicUrl)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                SantaLuciaFirSocketDelegate.shared.amOrigin(info: [String(bytes: data_exName.map{liteFace(way: $0)}, encoding: .utf8)!: (String(show_vitalCountTitle.suffix(7)) + constTenderPath.replacingOccurrences(of: "vertical", with: "ll")), String(bytes: kAbsTitle.map{$0^44}, encoding: .utf8)!: [String(bytes: kResourceValue.map{distinctiveSum(change: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.effective()
                //: return
                return
            }

            //: self.stopBGM()
            self.doMinimum()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = ApplicationReactiveCompatible.limitedType(type: .Response)
            //: self.startBGM()
            self.actionEnablePhone()
        }
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func lightActivties() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.doMinimum()
        //: TalkingSocketManager.shared.callerDelegate = nil
        SantaLuciaFirSocketDelegate.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        SantaLuciaFirSocketDelegate.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        SantaLuciaFirSocketDelegate.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func effective(animated: Bool = true) {
        //: self.stopBGM()
        self.doMinimum()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 创建音频通话房间
    //: private func createTalkRoom() {
    private func own() {
        //: self.voiceManager.createRoomAndStartPublish(roomId: self.chatModel!.logId)
        self.voiceManager.message(roomId: self.chatModel!.logId)
    }

    /// 通话重连
    //: private func resumeTalk() {
    private func pressurize() {
        //: if self.chatModel?.resumeTalk == true {
        if self.chatModel?.resumeTalk == true {
            //: createTalkRoom()
            own()
        }
    }
}

// MARK: - 通知

//: extension TalkingVoiceChatViewController {
extension TrailerRecognizerDelegate {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func heckleObserver() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(atMedium(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(molarity(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: k_skipMessage,
                                               //: object: nil)
                                               object: nil)

        //: voiceCallHelper.setDelegate(self, queue: DispatchQueue.main)
        dataShowStr.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func atMedium(notif _: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            effective()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func molarity(note _: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard SantaLuciaFirSocketDelegate.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        SantaLuciaFirSocketDelegate.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        SantaLuciaFirSocketDelegate.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        SantaLuciaFirSocketDelegate.shared.errorDelegate = self
        //: self.voiceManager.restartTalkWhenDisconnectedAndNetAvailable(roomId: self.chatModel?.logId ?? 0)
        self.voiceManager.crucify(roomId: self.chatModel?.logId ?? 0)
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVoiceChatViewController: CXCallObserverDelegate {
extension TrailerRecognizerDelegate: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption".localized)
            self.enablece(showMsg: (String(notiTabKey) + user_nothingData.replacingOccurrences(of: "disc", with: "n")).localized)
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                SantaLuciaFirSocketDelegate.shared.amOrigin(info: [String(bytes: data_exName.map{liteFace(way: $0)}, encoding: .utf8)!: (String(userSkinText) + String(userGrayId)), String(bytes: kAbsTitle.map{$0^44}, encoding: .utf8)!: [String(bytes: noti_writtenTitle.map{$0^34}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                SantaLuciaFirSocketDelegate.shared.amOrigin(info: [String(bytes: data_exName.map{liteFace(way: $0)}, encoding: .utf8)!: (appPinMessage.replacingOccurrences(of: "communication", with: "ca") + "lCall"), String(bytes: kAbsTitle.map{$0^44}, encoding: .utf8)!: [String(bytes: noti_writtenTitle.map{$0^34}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                SantaLuciaFirSocketDelegate.shared.amOrigin(info: [String(bytes: data_exName.map{liteFace(way: $0)}, encoding: .utf8)!: (String(show_vitalCountTitle.suffix(7)) + constTenderPath.replacingOccurrences(of: "vertical", with: "ll")), String(bytes: kAbsTitle.map{$0^44}, encoding: .utf8)!: [String(bytes: kResourceValue.map{distinctiveSum(change: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.detectScreen()
                //: TalkingSocketManager.shared.currInfoVC = nil
                SantaLuciaFirSocketDelegate.shared.currInfoVC = nil
            }
            //: popCurrentViewController()
            effective()

            //: } else if call.hasEnded == true || call.isOnHold == true {
        } else if call.hasEnded == true || call.isOnHold == true { // 电话挂断 || 无人接听挂断
            //: self.voiceManager.enableAudioModule(enable: true)
            self.voiceManager.darkDevice(enable: true)
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVoiceChatViewController {
extension TrailerRecognizerDelegate {
    /// 初始化音频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func identityRefuse() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? PhaseEquityValueConvertible.Call : PhaseEquityValueConvertible.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = ApplicationReactiveCompatible.limitedType(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func actionEnablePhone() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.stage()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func doMinimum() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.belowAppropriate()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVoiceChatViewController {
extension TrailerRecognizerDelegate {
    /// 超时定时器
    //: private func timeoutRules() {
    private func huntingLicenseRules() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? constSystemKey : self.chatModel?.dotTime()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            recommend()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.recommend()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func recommend() {
        //: guard !TalkingSocketManager.shared.isInfo else { return }
        guard !SantaLuciaFirSocketDelegate.shared.isInfo else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.enablece(showMsg: (String(constClipTitle) + String(appFormData)).localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: data_exName.map{liteFace(way: $0)}, encoding: .utf8)!: (appPinMessage.replacingOccurrences(of: "communication", with: "ca") + "lCall"), String(bytes: kAbsTitle.map{$0^44}, encoding: .utf8)!: [String(bytes: noti_writtenTitle.map{$0^34}, encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: data_exName.map{liteFace(way: $0)}, encoding: .utf8)!: (String(show_vitalCountTitle.suffix(7)) + constTenderPath.replacingOccurrences(of: "vertical", with: "ll")), String(bytes: kAbsTitle.map{$0^44}, encoding: .utf8)!: [String(bytes: kResourceValue.map{distinctiveSum(change: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        SantaLuciaFirSocketDelegate.shared.amOrigin(info: socketDict)
        //: popCurrentViewController()
        effective()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVoiceChatViewController {
extension TrailerRecognizerDelegate {
    /// 恢复音频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func brasilBean(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: noti_writtenTitle.map{$0^34}, encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(String(mainMpData) + String(userPerformEyebrowPlatFormat))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.noDraftNext(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.outsideCityAm()
            //: createTalkRoom()
            own()
            //: self.stopBGM()
            self.doMinimum()
        }
    }
}

// MARK: - AssetsObjectProtocol【拨打方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCallerDelegate {
extension TrailerRecognizerDelegate: AssetsObjectProtocol {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func milage(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(user_transitUrl) + String(k_twoData.suffix(4))).localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(String(notiMarketingValue) + String(data_violationUrl))].stringValue == FenceSequence.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(constPanStaffPath.prefix(4)) + "as hun" + String(user_halfValue)).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.enablece(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.detectScreen()
            //: TalkingSocketManager.shared.currInfoVC = nil
            SantaLuciaFirSocketDelegate.shared.currInfoVC = nil
        }

        //: popCurrentViewController()
        effective()
    }

    /// 对方接受了来电，建立音频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func layer(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: noti_writtenTitle.map{$0^34}, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.stopBGM()
        self.doMinimum()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.outsideCityAm()
        //: self.createTalkRoom()
        self.own()
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func earRate(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(String(noti_chooseUrl.suffix(6)) + String(user_healthySumTitle))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(user_transitUrl) + String(k_twoData.suffix(4))).localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(main_coreValue) + mainPromptOccurKey.replacingOccurrences(of: "max", with: "x"))].stringValue == FenceSequence.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(constPanStaffPath.prefix(4)) + "as hun" + String(user_halfValue)).localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if DismissReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue {
            if DismissReactiveCompatible.share.appStatus == AfterArtSumLiteral.normal.rawValue {
                //: typeStr = "Insufficient gold coins, call has ended".localized
                typeStr = String(bytes: dataActualValue.map{flowMini(situation: $0)}, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(const_automaticallyFormat) + String(const_bassChinData.prefix(4)) + "p the " + userPromptOpeneValue.replacingOccurrences(of: "beyond", with: "p")).localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.enablece(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.detectScreen()
            //: TalkingSocketManager.shared.currInfoVC = nil
            SantaLuciaFirSocketDelegate.shared.currInfoVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.routeStatus()
        //: self.popCurrentViewController()
        self.effective()
        //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: app_toKey, object: nil)
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func maleData(msg: String, data: [String: Any]) {
        //: guard DismissReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard DismissReactiveCompatible.share.appStatus == AfterArtSumLiteral.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(showArgumentPath.replacingOccurrences(of: "my", with: "e"))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(appHideName.replacingOccurrences(of: "sole", with: "j") + String(appSeveralId.prefix(6)))] as? String else { return }
            //: let httpsUrl = DismissReactiveCompatible.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = DismissReactiveCompatible.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = EndConfig()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: PushThen.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            PushThen.share.plumeBright(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            AllowanceReactiveCompatible.maximal(title: nil,
                                     //: message: msg,
                                     message: msg,
                                     //: leftBtnTitle: "Cancel".localized,
                                     leftBtnTitle: (String(constRelatedProblemMessage.suffix(5)) + user_combineSomebodyMessage.replacingOccurrences(of: "challenge", with: "l")).localized,
                                     //: rightBtnTitle: "Get gold coins".localized) {
                                     rightBtnTitle: (String(appBetweenText.suffix(6)) + "ld coin" + app_micContent.replacingOccurrences(of: "announcement", with: "s")).localized)
            {
                //: TalkingAlertShow.hideAlert()
                AllowanceReactiveCompatible.minuteOriginBridge()
                //: } rightBlock: {
            } rightBlock: {
                //: PushThen.share.func__jumpToWebRecharge(clickEvent: clickLess1minButton, sufficient: false)
                PushThen.share.twoParams(clickEvent: noti_systemName, sufficient: false)
            }
        }
    }

    /// 女性通话积分激励动效展示
    //: func socket_videoChat_onCallMotivation(data: [String: Any]) {
    func valueData(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: let addBean = json["addBean"].stringValue
        let addBean = json[(String(main_postSessionWhereId.suffix(6)) + show_thenFormat.replacingOccurrences(of: "tell", with: "n"))].stringValue
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: interactionView.showPointAnimaData(point: addBean)
            interactionView.streamRequire(point: addBean)
            //: } else {
        } else {
            //: self.miniView?.showPointAnimaData(point: addBean)
            self.miniView?.refuse(point: addBean)
        }
    }
}

// MARK: - MedalSolarObjectProtocol【接收方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension TrailerRecognizerDelegate: MedalSolarObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func sumRelationData(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(user_transitUrl) + String(k_twoData.suffix(4))).localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(String(mainResultUrl.prefix(7)) + kPairNorGapPath.lowercased())].stringValue == FenceSequence.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(constPanStaffPath.prefix(4)) + "as hun" + String(user_halfValue)).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.enablece(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.detectScreen()
            //: TalkingSocketManager.shared.currInfoVC = nil
            SantaLuciaFirSocketDelegate.shared.currInfoVC = nil
        }
        //: popCurrentViewController()
        effective()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func information(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: noti_writtenTitle.map{$0^34}, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow
        //: self.stopBGM()
        self.doMinimum()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.outsideCityAm()
        //: self.createTalkRoom()
        self.own()
    }
}

// MARK: - EnableceErrorDelegate【异常处理】

//: extension TalkingVoiceChatViewController: TalkingSocketManagerErrorDelegate {
extension TrailerRecognizerDelegate: EnableceErrorDelegate {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func assemblage(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (String(show_vitalCountTitle.suffix(7)) + constTenderPath.replacingOccurrences(of: "vertical", with: "ll")) { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.enablece(showMsg: (String(constClipTitle) + String(appFormData)).localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.detectScreen()
                //: TalkingSocketManager.shared.currInfoVC = nil
                SantaLuciaFirSocketDelegate.shared.currInfoVC = nil
            }
            //: self.popCurrentViewController()
            self.effective()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(notiProgramText) + String(showShallKey.prefix(5)) + "ptCall") && errorNo == 101 { // 接听通话时，余额不足
            //: guard DismissReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard DismissReactiveCompatible.share.appStatus == AfterArtSumLiteral.normal.rawValue else { return }
            //: PushThen.share.func__jumpToWebRecharge(sufficient: false)
            PushThen.share.twoParams(sufficient: false)

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(notiProgramText) + String(showShallKey.prefix(5)) + "ptCall") && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.enablece(showMsg: msg)
            //: self.popCurrentViewController()
            self.effective()
        }
    }
}

// MARK: - SpillViewDelegate【通话按钮操作事件】

//: extension TalkingVoiceChatViewController: Talking1v1VoiceInteractionViewDelegate {
extension TrailerRecognizerDelegate: SpillViewDelegate {
    /// 接听音频通话
    //: func interactionView_shouldAcceptCall() {
    func notice() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        SantaLuciaFirSocketDelegate.shared.amOrigin(info: [String(bytes: data_exName.map{liteFace(way: $0)}, encoding: .utf8)!: (dataLimitHappyGiftValue.replacingOccurrences(of: "bone", with: "a") + String(constCoreStr.prefix(6))), String(bytes: kAbsTitle.map{$0^44}, encoding: .utf8)!: [String(bytes: kResourceValue.map{distinctiveSum(change: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func bowdlerizer() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.enablece(showMsg: (String(k_cordThanKey.suffix(4)) + "s ar" + String(userMakeFormat.prefix(5)) + "cell" + constHeatCloudText.replacingOccurrences(of: "help", with: "e")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            SantaLuciaFirSocketDelegate.shared.amOrigin(info: [String(bytes: data_exName.map{liteFace(way: $0)}, encoding: .utf8)!: (appPinMessage.replacingOccurrences(of: "communication", with: "ca") + "lCall"), String(bytes: kAbsTitle.map{$0^44}, encoding: .utf8)!: [String(bytes: noti_writtenTitle.map{$0^34}, encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.enablece(showMsg: (String(k_cordThanKey.suffix(4)) + "s ar" + String(userMakeFormat.prefix(5)) + "cell" + constHeatCloudText.replacingOccurrences(of: "help", with: "e")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            SantaLuciaFirSocketDelegate.shared.amOrigin(info: [String(bytes: data_exName.map{liteFace(way: $0)}, encoding: .utf8)!: (String(show_vitalCountTitle.suffix(7)) + constTenderPath.replacingOccurrences(of: "vertical", with: "ll")), String(bytes: kAbsTitle.map{$0^44}, encoding: .utf8)!: [String(bytes: kResourceValue.map{distinctiveSum(change: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.enablece(showMsg: (String(app_profileYesterdayTitle.prefix(5)) + String(app_slideValue) + String(const_commonTrailUrl.suffix(4)) + "nded").localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            SantaLuciaFirSocketDelegate.shared.amOrigin(info: [String(bytes: data_exName.map{liteFace(way: $0)}, encoding: .utf8)!: (String(userSkinText) + String(userGrayId)), String(bytes: kAbsTitle.map{$0^44}, encoding: .utf8)!: [String(bytes: noti_writtenTitle.map{$0^34}, encoding: .utf8)!: self.chatModel?.logId]])
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.routeStatus()
        //: self.popCurrentViewController()
        self.effective()
    }

    /// 是否静音
    //: func interactionView_shouldChangeMute(isMute: Bool) {
    func readyDown(isMute: Bool) {
        //: self.voiceManager.setMute(isMute: isMute)
        self.voiceManager.trunk(isMute: isMute)
    }

    /// 是否免提
    //: func interactionView_shouldChangeSpeaker(isHandsfree: Bool) {
    func querier(isHandsfree: Bool) {
        //: self.voiceManager.setHandsfree(isHandsfree: isHandsfree)
        self.voiceManager.assetHandsfree(isHandsfree: isHandsfree)
    }

    /// 展示语音通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func characterWindow() {
        //: TalkingSocketManager.shared.currInfoVC = self
        SantaLuciaFirSocketDelegate.shared.currInfoVC = self
        //: self.miniView = TalkingVoiceMiniView.buildVoiceMiniView()
        self.miniView = RatingMiniView.argument()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.exitSchedule()?.navigationController?.pushViewController(self, animated: true)
        }
        //: let talkingTime = self.interactionView.getCurrentTalkingTime()
        let talkingTime = self.interactionView.allSmallApply()
        //: self.miniView?.updateTalkingTime(talkingTime)
        self.miniView?.equal(talkingTime)
        //: popCurrentViewController()
        effective()
    }

    /// 更新小窗口通话时间
    //: func interactionView_shouldUpdateTime(time: Double) {
    func visualAspectTime(time: Double) {
        //: guard self.miniView != nil else { return }
        guard self.miniView != nil else { return }
        //: self.miniView?.updateTalkingTime(time)
        self.miniView?.equal(time)
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func rangeResolution(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.rewardArr(giftArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingVoiceChatViewController {
extension TrailerRecognizerDelegate {
    /// 添加视图
    //: private func setupSubviews() {
    private func strokeSubviews() {
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func custom() {
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
