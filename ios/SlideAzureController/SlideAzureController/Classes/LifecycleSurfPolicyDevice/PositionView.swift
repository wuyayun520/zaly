
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let user_detectTitle:[UInt8] = [0x64,0x65,0x74,0x6e,0x65,0x6d,0x65,0x6c,0x70,0x6d,0x69,0x20,0x6e,0x65,0x65,0x62,0x20,0x74,0x6f,0x6e,0x20,0x73,0x61,0x68,0x20,0x29,0x3a,0x72,0x65,0x64,0x6f,0x63,0x28,0x74,0x69,0x6e,0x69]

/*: "Cancel" :*/
fileprivate let k_columnName:[Character] = ["C","a","n","c","e","l"]

/*: "Submit" :*/
fileprivate let constAppearanceStr:String = "Submitremark mobile name ask platform"

/*: "Please tell us your problems                   " :*/
fileprivate let notiResentData:[UInt8] = [0x84,0xa0,0x99,0x95,0xa7,0x99,0x54,0xa8,0x99,0xa0,0xa0,0x54,0xa9,0xa7,0x54,0xad,0xa3,0xa9,0xa6,0x54,0xa4,0xa6,0xa3,0x96,0xa0,0x99,0xa1,0xa7,0x54,0x54,0x54,0x54,0x54,0x54,0x54,0x54,0x54,0x54,0x54,0x54,0x54,0x54,0x54,0x54,0x54,0x54,0x54]

fileprivate func kingBoard(extent num: UInt8) -> UInt8 {
    let value = Int(num) - 52
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "You need to choose at least one." :*/
fileprivate let user_prettyStr:[UInt8] = [0xe,0x38,0x22,0x77,0x39,0x32,0x32,0x33,0x77,0x23,0x38,0x77,0x34,0x3f,0x38,0x38,0x24,0x32,0x77,0x36,0x23,0x77,0x3b,0x32,0x36,0x24,0x23,0x77,0x38,0x39,0x32,0x79]

private func quantityryBuild(harassment num: UInt8) -> UInt8 {
    return num ^ 87
}

/*: "Please tell us your problems." :*/
fileprivate let data_personalClingTitle:String = "flow fence request savePlease"
fileprivate let app_stripKey:String = " us ylong inform activity force electron"
fileprivate let app_wealthyId:String = "paspectbl"

/*: "app/userReport" :*/
fileprivate let userReNearData:String = "app/it lat"
fileprivate let appHisFormat:String = "Reportdon road ref distribute"

/*: "reason" :*/
fileprivate let const_fastFutureBehindData:[Character] = ["r","e","a","s","o","n"]

/*: "desc" :*/
fileprivate let constNearbyKey:[UInt8] = [0xe4,0xe5,0xf3,0xe3]

private func requestGray(her num: UInt8) -> UInt8 {
    return num ^ 128
}

/*: "mf/moment/report" :*/
fileprivate let k_sweetMessage:[Character] = ["m","f","/","m","o","m","e","n","t","/","r","e","p","o","r","t"]

/*: "momentId" :*/
fileprivate let main_fashionMsg:[UInt8] = [0x64,0x49,0x74,0x6e,0x65,0x6d,0x6f,0x6d]

/*: "mf/momentReply/report" :*/
fileprivate let showHomeBetterBridgeValue:String = "version sequence thumb basemf/mo"
fileprivate let data_agreeStrengthKey:String = "Reply/scale physics"

/*: "app/videoReport" :*/
fileprivate let dataHaveUrl:String = "map taapp/"
fileprivate let show_hugeId:String = "answer replacement wrap physics libraryReport"

/*: "logId" :*/
fileprivate let user_listenHourName:[UInt8] = [0xba,0xbd,0xb5,0x97,0xb2]

fileprivate func renderNap(publish num: UInt8) -> UInt8 {
    let value = Int(num) - 78
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Submitted successfully" :*/
fileprivate let data_easternFragmentName:[Character] = ["S","u","b","m","i","t","t","e","d"]
fileprivate let showProduceContainUrl:[Character] = [" ","s","u","c","c","e","s","s","f","u","l","l","y"]

/*: "Nudity" :*/
fileprivate let userTrendId:String = "next threshold whenNudity"

/*: "Violence" :*/
fileprivate let dataTitleMpId:[Character] = ["V","i","o","l","e","n","c","e"]

/*: "Harassment" :*/
fileprivate let notiSkinText:[Character] = ["H","a"]
fileprivate let mainBagData:[Character] = ["r","a","s","s","m","e","n","t"]

/*: "Suicide or Self-injury" :*/
fileprivate let user_neckNowTitle:[Character] = ["S","u","i","c","i","d","e"," ","o","r"," ","S","e","l","f","-","i","n","j","u"]
fileprivate let constRowWarningReplacementValue:String = "narrowy"

/*: "False Information" :*/
fileprivate let showExploreSubjectTitle:String = "False interaction remove limited"
fileprivate let appUniteId:String = "maeveryoneon"

/*: "Spam" :*/
fileprivate let constRichFormat:[Character] = ["S","p","a","m"]

/*: "Hate Speech" :*/
fileprivate let app_mineTitle:String = "lovely route sceneHate S"

/*: "Terrorism" :*/
fileprivate let mainAtMessage:String = "processing medal resultTerrorism"

/*: "Sale of Illegal or Regulated Goods" :*/
fileprivate let app_hearIntervalPath:[UInt8] = [0x73,0x64,0x6f,0x6f,0x47,0x20,0x64,0x65,0x74,0x61,0x6c,0x75,0x67,0x65,0x52,0x20,0x72,0x6f,0x20,0x6c,0x61,0x67,0x65,0x6c,0x6c,0x49,0x20,0x66,0x6f,0x20,0x65,0x6c,0x61,0x53]

/*: "Sending disgusting messages" :*/
fileprivate let app_disabledId:String = "return builder overSendi"
fileprivate let dataHungPath:String = "gustbot"
fileprivate let kGoAcquirePath:String = "eactiveages"

/*: "Using photos of nudity or drugs" :*/
fileprivate let app_tarCollectionFormat:[UInt8] = [0x59,0x77,0x6d,0x72,0x6b,0x24,0x74,0x6c,0x73,0x78,0x73,0x77,0x24,0x73,0x6a,0x24,0x72,0x79,0x68,0x6d,0x78,0x7d,0x24,0x73,0x76,0x24,0x68,0x76,0x79,0x6b,0x77]

fileprivate func writeUp(medal num: UInt8) -> UInt8 {
    let value = Int(num) + 252
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Harassing or sending ads" :*/
fileprivate let k_shallSlowPath:[Character] = ["H","a","r","a","s","s","i","n","g"," ","o","r"," ","s","e","n","d","i","n","g"," "]
fileprivate let k_againstUrl:[Character] = ["a","d","s"]

/*: "Defraud or using fake info" :*/
fileprivate let const_behindId:[Character] = ["D","e","f","r","a","u","d"," ","o","r"," ","u","s","i","n","g"," "]
fileprivate let kMigrationData:String = "faame"

/*: "Others" :*/
fileprivate let constPortTitle:String = "Otherfamily instruction flow appearance secret"
fileprivate let main_wrapUrl:String = "foundation"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  PositionView.swift
//  AbroadTalking
//
//  Created by Bolo on 2022/9/14.
//

//: import Then
import Then
//: import UIKit
import UIKit

//: enum TalkingReportAlertType: CaseIterable {
enum RevenueAlertType: CaseIterable {
    //: case reportCommentType
    case reportCommentType // 举报动态
    //: case reportDynamicType
    case reportDynamicType
    //: case reportUserType
    case reportUserType // 举报用户
    //: case reportVideo
    case reportVideo // 举报视频通话
}

// 是否举报成功block
//: typealias ReportViewHideBlock = (_ isReportSucc: Bool) -> Void
typealias ReportViewHideBlock = (_ isReportSucc: Bool) -> Void

//: class TalkingReportAlertView: UIView {
class PositionView: UIView {
    //: var reportType: TalkingReportAlertType?
    var reportType: RevenueAlertType?
    //: var dataArray: Array<TalkingReportModel> = []
    var dataArray: [ReportReactiveCompatible] = []
    //: var selectedModel = TalkingReportModel()
    var selectedModel = ReportReactiveCompatible()
    //: var tempId: String = ""
    var tempId: String = ""
    //: var reportHideBlock: ReportViewHideBlock?
    var reportHideBlock: ReportViewHideBlock?
    //: let disposeBag = DisposeBag()
    let disposeBag = DisposeBag()

    //: public init(frame: CGRect, type: TalkingReportAlertType?, rId: String) {
    public init(frame: CGRect, type: RevenueAlertType?, rId: String) {
        //: tempId = rId
        tempId = rId
        //: reportType = type
        reportType = type
        //: super.init(frame: frame)
        super.init(frame: frame)
        //: setupSubviews()
        status()
        //: setupSubViewsConstraint()
        confinement()
        //: setupReasons()
        beautyReasons()
        //: bindInteraction()
        icon()
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: user_detectTitle.reversed(), encoding: .utf8)!)
    }

    // MARK: - Lazy load

    //: lazy var cancelButton = UIButton().then {
    lazy var cancelButton = UIButton().then {
        //: $0.setTitle("Cancel".localized, for: .normal)
        $0.setTitle((String(k_columnName)).localized, for: .normal)
        //: $0.setTitleColor(UIColor.appThemeColor(), for: .normal)
        $0.setTitleColor(UIColor.attractiveIn(), for: .normal)
        //: $0.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 17)
        $0.titleLabel?.font = UIFont.bigGallery(type: .Medium, fontSize: 17)
        //: $0.backgroundColor = UIColor.white
        $0.backgroundColor = UIColor.white
        //: $0.layer.borderWidth = 1
        $0.layer.borderWidth = 1
        //: $0.layer.borderColor = UIColor.appThemeColor().cgColor
        $0.layer.borderColor = UIColor.attractiveIn().cgColor
        //: $0.layer.cornerRadius = 25
        $0.layer.cornerRadius = 25
        //: $0.clipsToBounds = true
        $0.clipsToBounds = true
        //: $0.addTarget(self, action: #selector(registerCancelAction), for: .touchUpInside)
        $0.addTarget(self, action: #selector(plumb), for: .touchUpInside)
    }

    //: lazy var submitButton = UIButton().then {
    lazy var submitButton = UIButton().then {
        //: $0.setTitle("Submit".localized, for: .normal)
        $0.setTitle((String(constAppearanceStr.prefix(6))).localized, for: .normal)
        //: $0.setTitleColor(UIColor.white, for: .normal)
        $0.setTitleColor(UIColor.white, for: .normal)
        //: $0.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 17)
        $0.titleLabel?.font = UIFont.bigGallery(type: .Medium, fontSize: 17)
        //: $0.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: 126, height: 50)), for: .normal)
        $0.setBackgroundImage(UIImage.beforeCan(colors: UIColor.minimizeOn(), size: CGSize(width: 126, height: 50)), for: .normal)
        //: $0.layer.cornerRadius = 25
        $0.layer.cornerRadius = 25
        //: $0.clipsToBounds = true
        $0.clipsToBounds = true
        //: $0.addTarget(self, action: #selector(registerSubmitAction), for: .touchUpInside)
        $0.addTarget(self, action: #selector(give), for: .touchUpInside)
    }

    //: lazy var tableFooterView = UIView().then {
    lazy var tableFooterView = UIView().then {
        //: $0.frame = CGRect(x: 0, y: 0, width: ScreenWidth-actualWidth(w: 108), height: actualHeight(h: 74))
        $0.frame = CGRect(x: 0, y: 0, width: main_logStr - actualWidth(w: 108), height: actualHeight(h: 74))
        //: $0.backgroundColor = UIColor.RGBA(245, 245, 248, 1)
        $0.backgroundColor = UIColor.searched(245, 245, 248, 1)
        //: $0.layer.masksToBounds = true
        $0.layer.masksToBounds = true
        //: $0.layer.cornerRadius = 5
        $0.layer.cornerRadius = 5
    }

    //: lazy var otherTextView = PlaceholderTextView(placeholder: "Please tell us your problems                   ".localized, placeholderColor: UIColor.appValueDetailColor(), frame: CGRect(x: 10, y: 10, width: ScreenWidth-actualWidth(w: 128), height: actualHeight(h: 54))).then {
    lazy var otherTextView = SettleThen(placeholder: String(bytes: notiResentData.map{kingBoard(extent: $0)}, encoding: .utf8)!.localized, placeholderColor: UIColor.saveInColor(), frame: CGRect(x: 10, y: 10, width: main_logStr - actualWidth(w: 128), height: actualHeight(h: 54))).then {
        //: $0.backgroundColor = .clear
        $0.backgroundColor = .clear
        //: $0.placeholdertextView.backgroundColor = .clear
        $0.placeholdertextView.backgroundColor = .clear
        //: $0.limitWords = 200
        $0.limitWords = 200
        //: $0.isShowCountLabel = false
        $0.isShowCountLabel = false
        //: $0.isReturnHidden = true
        $0.isReturnHidden = true
        //: $0.plaleLabel.font = .pingfangFont(type: .Regular, fontSize: 16)
        $0.plaleLabel.font = .bigGallery(type: .Regular, fontSize: 16)
        //: $0.placeholdertextView.textColor = UIColor.appTitleColor()
        $0.placeholdertextView.textColor = UIColor.dialog()
        //: $0.placeholdertextView.font = .pingfangFont(type: .Regular, fontSize: 16)
        $0.placeholdertextView.font = .bigGallery(type: .Regular, fontSize: 16)
    }

    //: lazy var bgView = UIView().then {
    lazy var bgView = UIView().then {
        //: $0.backgroundColor = UIColor.RGBA(0, 0, 0, 0.6)
        $0.backgroundColor = UIColor.searched(0, 0, 0, 0.6)
    }

    //: lazy var containerView = UIView().then {
    lazy var containerView = UIView().then {
        //: $0.backgroundColor = .white
        $0.backgroundColor = .white
        //: $0.layer.masksToBounds = true
        $0.layer.masksToBounds = true
        //: $0.layer.cornerRadius = 15
        $0.layer.cornerRadius = 15
    }

    //: lazy var tableView = UITableView.init(frame: .zero, style: .plain).then {
    lazy var tableView = UITableView(frame: .zero, style: .plain).then {
        //: $0.backgroundColor = .white
        $0.backgroundColor = .white
        //: $0.separatorStyle = .none
        $0.separatorStyle = .none
        //: $0.showsVerticalScrollIndicator = false
        $0.showsVerticalScrollIndicator = false
        //: $0.delegate = self
        $0.delegate = self
        //: $0.dataSource = self
        $0.dataSource = self
        //: if reportType != .reportCommentType, reportType != .reportVideo {
        if reportType != .reportCommentType, reportType != .reportVideo {
            //: $0.tableFooterView = self.tableFooterView
            $0.tableFooterView = self.tableFooterView
        }
        //: $0.register(TalkingReportViewCell.self, forCellReuseIdentifier: TalkingReportViewCell.className())
        $0.register(EngineeringView.self, forCellReuseIdentifier: EngineeringView.className())
    }
}

// MARK: - Bind && Event

//: extension TalkingReportAlertView {
extension PositionView {
    //: @objc func func__TapGestureRecognizer() {
    @objc func yesterdayLab() {
        //: self.endEditing(true)
        self.endEditing(true)
    }

    //: public func showReportViewIn(view: UIView?) {
    public func priceBlack(view: UIView?) {
        //: if view == nil {
        if view == nil {
            //: let window = DepictionThen.getWindow()
            let window = DepictionThen.fossilization()
            //: window.addSubview(self)
            window.addSubview(self)
            //: return
            return
        }
        //: view?.addSubview(self)
        view?.addSubview(self)
    }

    /// 移除视图
    /// - Parameter succeed: 是否举报成功
    //: private func dismissReportView(succeed: Bool) {
    private func aSomeone(succeed: Bool) {
        //: reportHideBlock?(succeed)
        reportHideBlock?(succeed)
        //: self.removeFromSuperview()
        self.removeFromSuperview()
    }

    /// 取消
    //: @objc func registerCancelAction() {
    @objc func plumb() {
        //: self.dismissReportView(succeed: false)
        self.aSomeone(succeed: false)
    }

    /// 提交举报内容
    //: @objc func registerSubmitAction() {
    @objc func give() {
        //: if selectedModel.selected == false {
        if selectedModel.selected == false {
            //: self.func__showStatusBarErrorMsg(showMsg: "You need to choose at least one.".localized)
            self.enablece(showMsg: String(bytes: user_prettyStr.map{quantityryBuild(harassment: $0)}, encoding: .utf8)!.localized)
            //: return
            return
        }
        //: if reportType == .reportUserType, otherTextView.placeholdertextView.text.isEmptyString {
        if reportType == .reportUserType, otherTextView.placeholdertextView.text.isEmptyString {
            //: self.func__showStatusBarErrorMsg(showMsg: "Please tell us your problems.".localized)
            self.enablece(showMsg: (String(data_personalClingTitle.suffix(6)) + " tell" + String(app_stripKey.prefix(5)) + "our " + app_wealthyId.replacingOccurrences(of: "aspect", with: "ro") + "ems.").localized)
            //: return
            return
        }

        //: var param: [String: String] = [:]
        var param: [String: String] = [:]
        //: var path: String = ""
        var path = ""

        //: switch self.reportType {
        switch self.reportType {
        //: case .reportUserType:
        case .reportUserType:
            //: path = "app/userReport"
            path = (String(userReNearData.prefix(4)) + "user" + String(appHisFormat.prefix(6)))
            //: param = ["id": tempId, "reason": selectedModel.type, "desc": otherTextView.placeholdertextView.text]
            param = ["id": tempId, (String(const_fastFutureBehindData)): selectedModel.type, String(bytes: constNearbyKey.map{requestGray(her: $0)}, encoding: .utf8)!: otherTextView.placeholdertextView.text]

        //: case .reportDynamicType:
        case .reportDynamicType:
            //: path = "mf/moment/report"
            path = (String(k_sweetMessage))
            //: param = ["momentId": tempId, "reason": selectedModel.type]
            param = [String(bytes: main_fashionMsg.reversed(), encoding: .utf8)!: tempId, (String(const_fastFutureBehindData)): selectedModel.type]

        //: case .reportCommentType:
        case .reportCommentType:
            //: path = "mf/momentReply/report"
            path = (String(showHomeBetterBridgeValue.suffix(5)) + "ment" + String(data_agreeStrengthKey.prefix(6)) + "report")
            //: param = ["id": tempId, "reason": selectedModel.type]
            param = ["id": tempId, (String(const_fastFutureBehindData)): selectedModel.type]

        //: case .reportVideo:
        case .reportVideo:
            //: path = "app/videoReport"
            path = (String(dataHaveUrl.suffix(4)) + "video" + String(show_hugeId.suffix(6)))
            //: param = ["logId": tempId, "reason": selectedModel.type]
            param = [String(bytes: user_listenHourName.map{renderNap(publish: $0)}, encoding: .utf8)!: tempId, (String(const_fastFutureBehindData)): selectedModel.type]

        //: default:
        default:
            //: break
            break
        }

        //: self.submitButton.isEnabled = false
        self.submitButton.isEnabled = false
        //: TalkingUserRequestManager.func__reportRequest(reqPath: path, reqParams: param) { succeed, result, errorModel in
        ThresholdLevelRequestManager.soundDay(reqPath: path, reqParams: param) { succeed, _, _ in
            //: self.submitButton.isEnabled = true
            self.submitButton.isEnabled = true
            //: if succeed == true {
            if succeed == true {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Submitted successfully".localized)
                self.showGuidanceName(showMsg: (String(data_easternFragmentName) + String(showProduceContainUrl)).localized)
                //: self.dismissReportView(succeed: true)
                self.aSomeone(succeed: true)
            }
        }
    }
}

// MARK: - Delegate

//: extension TalkingReportAlertView: UITableViewDelegate, UITableViewDataSource {
extension PositionView: UITableViewDelegate, UITableViewDataSource {
    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: return 1
        return 1
    }

    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return dataArray.count
        return dataArray.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt _: IndexPath) -> CGFloat {
        //: return actualHeight(h: 40)
        return actualHeight(h: 40)
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let cell = tableView.dequeueReusableCell(withIdentifier: TalkingReportViewCell.className()) as! TalkingReportViewCell
        let cell = tableView.dequeueReusableCell(withIdentifier: EngineeringView.className()) as! EngineeringView
        //: let model = dataArray[indexPath.item]
        let model = dataArray[indexPath.item]
        //: cell.updateReportCell(model: model)
        cell.modifyHome(model: model)
        //: return cell
        return cell
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
        //: for model in dataArray {
        for model in dataArray {
            //: model.selected = false
            model.selected = false
        }

        //: let itemModel = dataArray[indexPath.item]
        let itemModel = dataArray[indexPath.item]
        //: itemModel.selected = true
        itemModel.selected = true
        //: self.selectedModel = itemModel
        self.selectedModel = itemModel

        //: tableView.reloadData()
        tableView.reloadData()
    }
}

// MARK: - Layout

//: extension TalkingReportAlertView {
extension PositionView {
    /// 添加视图
    //: private func setupSubviews() {
    private func status() {
        //: addSubview(bgView)
        addSubview(bgView)
        //: addSubview(containerView)
        addSubview(containerView)
        //: containerView.addSubview(tableView)
        containerView.addSubview(tableView)
        //: containerView.addSubview(cancelButton)
        containerView.addSubview(cancelButton)
        //: containerView.addSubview(submitButton)
        containerView.addSubview(submitButton)
        //: tableFooterView.addSubview(otherTextView)
        tableFooterView.addSubview(otherTextView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func confinement() {
        //: var height = actualHeight(h: 325)
        var height = actualHeight(h: 325)
        //: if reportType == .reportUserType {
        if reportType == .reportUserType {
            //: height = actualHeight(h: 375)
            height = actualHeight(h: 375)
            //: } else if reportType == .reportVideo {
        } else if reportType == .reportVideo {
            //: height = actualHeight(h: 338)
            height = actualHeight(h: 338)
        }
        //: let topY = (self.frame.height - height) / 2.0
        let topY = (self.frame.height - height) / 2.0

        //: bgView.snp.makeConstraints { make in
        bgView.snp.makeConstraints { make in
            //: make.leading.top.trailing.bottom.equalTo(0)
            make.leading.top.trailing.bottom.equalTo(0)
        }
        //: containerView.snp.makeConstraints { make in
        containerView.snp.makeConstraints { make in
            //: make.top.equalTo(topY)
            make.top.equalTo(topY)
            //: make.leading.equalTo(actualWidth(w: 40))
            make.leading.equalTo(actualWidth(w: 40))
            //: make.trailing.equalTo(actualWidth(w: -40))
            make.trailing.equalTo(actualWidth(w: -40))
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: tableView.snp.makeConstraints { make in
        tableView.snp.makeConstraints { make in
            //: make.leading.equalTo(actualWidth(w: 14))
            make.leading.equalTo(actualWidth(w: 14))
            //: make.trailing.equalTo(actualWidth(w: -14))
            make.trailing.equalTo(actualWidth(w: -14))
            //: make.top.equalTo(actualHeight(h: 36))
            make.top.equalTo(actualHeight(h: 36))
            //: if reportType == .reportVideo {
            if reportType == .reportVideo {
                //: make.bottom.equalTo(actualHeight(h: -62))
                make.bottom.equalTo(actualHeight(h: -62))
                //: } else {
            } else {
                //: make.bottom.equalTo(actualHeight(h: -82))
                make.bottom.equalTo(actualHeight(h: -82))
            }
        }
        //: cancelButton.snp.makeConstraints { make in
        cancelButton.snp.makeConstraints { make in
            //: make.leading.equalTo(actualWidth(w: 14))
            make.leading.equalTo(actualWidth(w: 14))
            //: make.bottom.equalTo(actualHeight(h: -16))
            make.bottom.equalTo(actualHeight(h: -16))
            //: make.size.equalTo(CGSize(width: 126, height: 50))
            make.size.equalTo(CGSize(width: 126, height: 50))
        }
        //: submitButton.snp.makeConstraints { make in
        submitButton.snp.makeConstraints { make in
            //: make.trailing.equalTo(actualWidth(w: -14))
            make.trailing.equalTo(actualWidth(w: -14))
            //: make.bottom.equalTo(actualHeight(h: -16))
            make.bottom.equalTo(actualHeight(h: -16))
            //: make.size.equalTo(CGSize(width: 126, height: 50))
            make.size.equalTo(CGSize(width: 126, height: 50))
        }
    }

    /// 添加数据
    //: private func setupReasons() {
    private func beautyReasons() {
        //: var reasonArray: [String] = []
        var reasonArray: [String] = []
        //: var type = 0
        var type = 0

        //: switch self.reportType {
        switch self.reportType {
        //: case .reportDynamicType, .reportCommentType:
        case .reportDynamicType, .reportCommentType:
            //: type = 6
            type = 6
            //: reasonArray = ["Nudity".localized,
            reasonArray = [(String(userTrendId.suffix(6))).localized,
                           //: "Violence".localized,
                           (String(dataTitleMpId)).localized,
                           //: "Harassment".localized,
                           (String(notiSkinText) + String(mainBagData)).localized,
                           //: "Suicide or Self-injury".localized,
                           (String(user_neckNowTitle) + constRowWarningReplacementValue.replacingOccurrences(of: "narrow", with: "r")).localized,
                           //: "False Information".localized,
                           (String(showExploreSubjectTitle.prefix(6)) + "Infor" + appUniteId.replacingOccurrences(of: "everyone", with: "ti")).localized,
                           //: "Spam".localized,
                           (String(constRichFormat)).localized,
                           //: "Hate Speech".localized,
                           (String(app_mineTitle.suffix(6)) + "peech").localized,
                           //: "Terrorism".localized,
                           (String(mainAtMessage.suffix(9))).localized,
                           //: "Sale of Illegal or Regulated Goods".localized]
                           String(bytes: app_hearIntervalPath.reversed(), encoding: .utf8)!.localized]

        //: case .reportUserType:
        case .reportUserType:
            //: type = 1
            type = 1
            //: reasonArray = ["Sending disgusting messages".localized,
            reasonArray = [(String(app_disabledId.suffix(5)) + "ng dis" + dataHungPath.replacingOccurrences(of: "bot", with: "i") + "ng m" + kGoAcquirePath.replacingOccurrences(of: "active", with: "ss")).localized,
                           //: "Using photos of nudity or drugs".localized,
                           String(bytes: app_tarCollectionFormat.map{writeUp(medal: $0)}, encoding: .utf8)!.localized,
                           //: "Harassing or sending ads".localized,
                           (String(k_shallSlowPath) + String(k_againstUrl)).localized,
                           //: "Defraud or using fake info".localized,
                           (String(const_behindId) + kMigrationData.replacingOccurrences(of: "am", with: "k") + " info").localized,
                           //: "Others".localized]
                           (String(constPortTitle.prefix(5)) + main_wrapUrl.replacingOccurrences(of: "foundation", with: "s")).localized]

        //: case .reportVideo:
        case .reportVideo:
            //: if let reason = DismissReactiveCompatible.share.appConfigMode.videoReport["reason"] {
            if let reason = DismissReactiveCompatible.share.appConfigMode.videoReport[(String(const_fastFutureBehindData))] {
                //: reasonArray = reason as! [String]
                reasonArray = reason as! [String]
            }

        //: default:
        default:
            //: type = 0
            type = 0
            //: reasonArray = []
            reasonArray = []
        }

        //: for i in 0..<reasonArray.count {
        for i in 0 ..< reasonArray.count {
            //: let model = TalkingReportModel()
            let model = ReportReactiveCompatible()
            //: model.reason = reasonArray[i]
            model.reason = reasonArray[i]
            //: if self.reportType == .reportVideo {
            if self.reportType == .reportVideo {
                //: model.type = "\(i)"
                model.type = "\(i)"
                //: } else {
            } else {
                //: model.type = "\(i+type)"
                model.type = "\(i + type)"
            }
            //: dataArray.append(model)
            dataArray.append(model)
        }

        //: tableView.reloadData()
        tableView.reloadData()
    }

    /// 添加事件
    //: private func bindInteraction() {
    private func icon() {
        //: let tapGesture = UITapGestureRecognizer()
        let tapGesture = UITapGestureRecognizer()
        //: bgView.addGestureRecognizer(tapGesture)
        bgView.addGestureRecognizer(tapGesture)
        //: tapGesture.rx.event.bind { [weak self] recognizer in
        tapGesture.rx.event.bind { [weak self] _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.endEditing(true)
            self.endEditing(true)
        }
        //: .disposed(by: disposeBag)
        .disposed(by: disposeBag)
    }
}
