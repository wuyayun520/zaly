
//: Declare String Begin

/*: "bg_message_top" :*/
fileprivate let showRunnerValue:String = "bg_meincrease domestic required withdraw"
fileprivate let const_endFemaleName:String = "e_topwithdraw extension rich"

/*: "777777" :*/
fileprivate let show_midWaitUrl:[Character] = ["7","7","7","7","7","7"]

/*: "Messages" :*/
fileprivate let constPinIceId:String = "Messageno pattern"
fileprivate let k_additionalName:[Character] = ["s"]

/*: "Who like me" :*/
fileprivate let constHelpStr:[Character] = ["W","h","o"," ","l","i","k","e"," "]
fileprivate let showPlanPath:String = "ME"

/*: "Call" :*/
fileprivate let show_hideStr:String = "Callmember manual"

/*: "#FF2348" :*/
fileprivate let k_pageStandContactPath:String = "qualify pair bill character#FF2348"

/*: "Current network unavailable" :*/
fileprivate let noti_downKey:String = "Currenlat early speak line since"
fileprivate let constStoryValue:String = "coordinatorrk"
fileprivate let dataDateEntryMessage:String = "ilacomposition"
fileprivate let appManagerPath:[Character] = ["e"]

/*: "icon_yidu_pre" :*/
fileprivate let main_underId:[Character] = ["i","c","o","n","_","y","i","d","u","_","p","r","e"]

/*: "transform.rotation" :*/
fileprivate let kLensPackUrl:String = "closeran"
fileprivate let k_suitSecretMessage:String = "m.rotpose which"

/*: "transform.scale" :*/
fileprivate let constHourName:String = "migrationransf"
fileprivate let userExhibitFormat:String = "coptionle"

/*: "zoom&shake" :*/
fileprivate let noti_fashionMsg:[Character] = ["z","o","o"]
fileprivate let data_handleUrl:[Character] = ["m","&","s","h","a","k","e"]

/*: "yyyy-MM-dd" :*/
fileprivate let k_backgroundData:String = "resorty"
fileprivate let data_libraryStr:String = "y-MM-ddarrangement third action primary"

/*: "Allow %@ to send you notifications?" :*/
fileprivate let show_actDimensionText:[UInt8] = [0x47,0x6a,0x6a,0x69,0x71,0x26,0x23,0x46,0x26,0x72,0x69,0x26,0x75,0x63,0x68,0x62,0x26,0x7f,0x69,0x73,0x26,0x68,0x69,0x72,0x6f,0x60,0x6f,0x65,0x67,0x72,0x6f,0x69,0x68,0x75,0x39]

private func soundARef(cell num: UInt8) -> UInt8 {
    return num ^ 6
}

/*: "Cancel" :*/
fileprivate let k_contactUpperContent:[Character] = ["C","a","n","c","e","l"]

/*: "Settings" :*/
fileprivate let userAppHoldSafeId:[Character] = ["S","e","t","t","i","n","g","s"]

/*: "badNumber" :*/
fileprivate let notiSessionMsg:String = "badNextra own shade"

/*: "isConnection" :*/
fileprivate let const_themeValue:String = "beautiful herisConn"
fileprivate let data_extentPath:String = "ecsuition"

/*: "networkStatus" :*/
fileprivate let const_lessFirmOnId:String = "nearwork"
fileprivate let notiHostMsg:[Character] = ["S","t","a","t","u","s"]

/*: "unreadMessageNum" :*/
fileprivate let mainFlagTitle:String = "unsune"
fileprivate let mainYourselfPath:String = "ssagnatural"
fileprivate let const_bitPath:String = "Numgather beautiful later once missing"

/*: "Do you want to mark all messages as read?" :*/
fileprivate let dataCommonPicFormat:[UInt8] = [0x6f,0x44,0xb,0x52,0x44,0x5e,0xb,0x5c,0x4a,0x45,0x5f,0xb,0x5f,0x44,0xb,0x46,0x4a,0x59,0x40,0xb,0x4a,0x47,0x47,0xb,0x46,0x4e,0x58,0x58,0x4a,0x4c,0x4e,0x58,0xb,0x4a,0x58,0xb,0x59,0x4e,0x4a,0x4f,0x14]

private func timeThen(year num: UInt8) -> UInt8 {
    return num ^ 43
}

/*: "消息列表一键已读失败：code: :*/
fileprivate let appVariationMessage:String = "消息列表一share"
fileprivate let app_localMessage:[Character] = ["已","读","失","败"]
fileprivate let appMaybeStr:String = "：code:elect brace enjoy trigger"

/*: , desc: :*/
fileprivate let constBackgroundTitle:String = "record am memory, desc:"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  RatingDisableThen.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/1/20.
//

//: import UIKit
import UIKit

//: class TalkingMessageViewController: TalkingBaseViewController {
class RatingDisableThen: WeddingLovelyRecognizerDelegate {
    //: var isNoChangeTop = false
    var isNoChangeTop = false
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var seleteIndex = 0
    private var seleteIndex = 0

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: setIsShowCleanAmimat()
        realise()
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: createUI()
        thingLoad()
        //: bindInteraction()
        bondPhoneQuantityeractionOperate()
        //: func__turnOnSystemNotification()
        proprietorNotification()
    }

    // MARK: - Lazy load

    // 背景
    //: private lazy var colorImageView: UIImageView = {
    private lazy var colorImageView: UIImageView = {
        //: let colorV = UIImageView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: 100+StatusBarNavigationBarHeight))
        let colorV = UIImageView(frame: CGRect(x: 0, y: 0, width: main_logStr, height: 100 + constAgentJumpValue))
        //: colorV.image = UIImage.BundleImageNamed(name: "bg_message_top")
        colorV.image = UIImage.locationName(name: (String(showRunnerValue.prefix(5)) + "ssag" + String(const_endFemaleName.prefix(5))))
        //: return colorV
        return colorV
        //: }()
    }()

    //: private lazy var noticeView: TalkingNoticeTipView = {
    private lazy var noticeView: TransferThen = {
        //: let V = TalkingNoticeTipView()
        let V = TransferThen()
        //: V.isHidden = true
        V.isHidden = true
        //: return V
        return V
        //: }()
    }()

    //: private lazy var slideVC: QYSlideNavigationViewController = {
    private lazy var slideVC: ImpressionNavigationViewController = {
        //: let vc = QYSlideNavigationViewController()
        let vc = ImpressionNavigationViewController()
        //: vc.topDistance = 0
        vc.topDistance = 0
        //: vc.controllerItems = controllerItems
        vc.controllerItems = controllerItems
        //: vc.titleItems = titleItems
        vc.titleItems = titleItems
        //: vc.rightSpace = 60
        vc.rightSpace = 60
        //: vc.titleViewBackgroundColor = .clear
        vc.titleViewBackgroundColor = .clear
        //: vc.titleFont = .pingfangFont(type: .Medium, fontSize: 16)
        vc.titleFont = .bigGallery(type: .Medium, fontSize: 16)
        //: vc.selectTitleFont = .pingfangFont(type: .Medium, fontSize: 18)
        vc.selectTitleFont = .bigGallery(type: .Medium, fontSize: 18)
        //: vc.normalColor = UIColor(hex: "777777")!
        vc.normalColor = UIColor(hex: (String(show_midWaitUrl)))!
        //: vc.selectedColor = UIColor.appTitleColor()
        vc.selectedColor = UIColor.dialog()
        //: vc.hasBttomTabBar = true
        vc.hasBttomTabBar = true
        //: vc.delegate = self
        vc.delegate = self
        //: vc.scrollEnabled = false
        vc.scrollEnabled = false
        //: return vc
        return vc
        //: }()
    }()

    //: private lazy var controllerItems: [TalkingBaseViewController] = {
    private lazy var controllerItems: [WeddingLovelyRecognizerDelegate] = {
        //: var arr = [self.chatListVC, TalkingWhoLikeViewController()]
        var arr = [self.chatListVC, LikeRecognizerDelegate()]
        //: if DismissReactiveCompatible.share.loginUserMode.callTabSwitch == 1 {
        if DismissReactiveCompatible.share.loginUserMode.callTabSwitch == 1 {
            //: arr.insert(TalkingVideoCallVC(), at: arr.count-1)
            arr.insert(RatingViewController(), at: arr.count - 1)
        }
        //: return arr
        return arr

        //: }()
    }()

    //: private lazy var chatListVC: TalkingChatListViewController = {
    private lazy var chatListVC: BibliographyViewDelegate = {
        //: let vc = TalkingChatListViewController()
        let vc = BibliographyViewDelegate()
        //: return vc
        return vc
        //: }()
    }()

    //: private lazy var titleItems: [String] = {
    private lazy var titleItems: [String] = {
        //: var arr = ["Messages".localized, "Who like me".localized]
        var arr = [(String(constPinIceId.prefix(7)) + String(k_additionalName)).localized, (String(constHelpStr) + showPlanPath.lowercased()).localized]
        //: if DismissReactiveCompatible.share.loginUserMode.callTabSwitch == 1 {
        if DismissReactiveCompatible.share.loginUserMode.callTabSwitch == 1 {
            //: arr.insert("Call".localized, at: arr.count-1)
            arr.insert((String(show_hideStr.prefix(4))).localized, at: arr.count - 1)
        }
        //: return arr
        return arr
        //: }()
    }()

    // 断网提醒
    //: private lazy var netTipLabel: UILabel = {
    private lazy var netTipLabel: UILabel = {
        //: let tipView = UILabel.init(frame: CGRect(x: 0, y: StatusBarNavigationBarHeight, width: ScreenWidth, height: 30))
        let tipView = UILabel(frame: CGRect(x: 0, y: constAgentJumpValue, width: main_logStr, height: 30))
        //: tipView.font = UIFont.pingfangRugularFont(fontSize: 14)
        tipView.font = UIFont.bonePlace(fontSize: 14)
        //: tipView.textColor = .white
        tipView.textColor = .white
        //: tipView.backgroundColor = UIColor.init(hex: "#FF2348")?.withAlphaComponent(0.8)
        tipView.backgroundColor = UIColor(hex: (String(k_pageStandContactPath.suffix(7))))?.withAlphaComponent(0.8)
        //: tipView.textAlignment = .center
        tipView.textAlignment = .center
        //: tipView.text = "Current network unavailable".localized
        tipView.text = (String(noti_downKey.prefix(6)) + "t net" + constStoryValue.replacingOccurrences(of: "coordinator", with: "wo") + " unava" + dataDateEntryMessage.replacingOccurrences(of: "composition", with: "bl") + String(appManagerPath)).localized
        //: tipView.isHidden = true
        tipView.isHidden = true
        //: return tipView
        return tipView
        //: }()
    }()

    // message红点角标
    //: private lazy var msgNumLabel: BadgeLab = {
    private lazy var msgNumLabel: TapBadgeLab = {
        //: let numbLabel = BadgeLab()
        let numbLabel = TapBadgeLab()
        //: numbLabel.isHidden = true
        numbLabel.isHidden = true
        //: return numbLabel
        return numbLabel
        //: }()
    }()

    // whoLikeMe红点角标
    //: private lazy var badgLB: BadgeLab = {
    private lazy var badgLB: TapBadgeLab = {
        //: let numbLabel = BadgeLab()
        let numbLabel = TapBadgeLab()
        //: numbLabel.isHidden = true
        numbLabel.isHidden = true
        //: return numbLabel
        return numbLabel
        //: }()
    }()

    // 一键清除
    //: private lazy var cleanBtn: UIButton = {
    private lazy var cleanBtn: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: btn.backgroundColor = .clear
        btn.backgroundColor = .clear
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_yidu_pre"), for: .normal)
        btn.setImage(UIImage.locationName(name: (String(main_underId))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_yidu_pre"), for: .highlighted)
        btn.setImage(UIImage.locationName(name: (String(main_underId))), for: .highlighted)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - WeddingNavigationDelegate

//: extension TalkingMessageViewController: QYSlideNavigationDelegate {
extension RatingDisableThen: WeddingNavigationDelegate {
    //: func selectedViewController(_ viewController: UIViewController, index: Int) {
    func remind(_ viewController: UIViewController, index: Int) {
        //: self.seleteIndex = Int(index)
        self.seleteIndex = Int(index)
        //: if viewController.isKind(of: TalkingChatListViewController.self) {
        if viewController.isKind(of: BibliographyViewDelegate.self) {
            //: self.cleanBtn.isHidden = false
            self.cleanBtn.isHidden = false
            //: setIsShowCleanAmimat()
            realise()

            //: } else if viewController.isKind(of: TalkingWhoLikeViewController.self) {
        } else if viewController.isKind(of: LikeRecognizerDelegate.self) {
            //: self.cleanBtn.isHidden = true
            self.cleanBtn.isHidden = true
            //: uploadRecord.uploadRecordEvent(eventID: ClickWLikeMeTabNoP)
            const_pathMsg.tabKeep(eventID: main_beautyExploreUrl)
        }
    }
}

// MARK: - Event

//: extension TalkingMessageViewController {
extension RatingDisableThen {
    //: func setIsTopAll() {
    func atAvailable() {
        /// 默认选中all
        //: if seleteIndex == 0 {
        if seleteIndex == 0 {
            //: let vc = slideVC.controllerItems.first as? TalkingChatListViewController
            let vc = slideVC.controllerItems.first as? BibliographyViewDelegate
            //: vc?.resetToTopItemView()
            vc?.computeView()
            // 刷新在线状态
            //: vc?.refreshOnlineStatus(isFirst: true)
            vc?.shapeOf(isFirst: true)
        }
    }

    /// 无网络提示
    //: func func__checkNetTipState() {
    func layJudge() {
        //: if DismissReactiveCompatible.share.networkStatus != .Unavailable && AfterThen.shared.isConnection {
        if DismissReactiveCompatible.share.networkStatus != .Unavailable, AfterThen.shared.isConnection {
            //: netTipLabel.isHidden = true
            netTipLabel.isHidden = true
            //: } else {
        } else {
            //: netTipLabel.isHidden = false
            netTipLabel.isHidden = false
        }
    }

    /// message红点
    //: func func__messagebadgLBState(num: Int) {
    func intervaleraction(num: Int) {
        //: if num > 0 {
        if num > 0 {
            //: msgNumLabel.isHidden = false
            msgNumLabel.isHidden = false
            //: if num > 99 {
            if num > 99 {
                //: msgNumLabel.text = "99+"
                msgNumLabel.text = "99+"
                //: } else {
            } else {
                //: msgNumLabel.text = String(num)
                msgNumLabel.text = String(num)
            }
            //: } else {
        } else {
            //: msgNumLabel.isHidden = true
            msgNumLabel.isHidden = true
        }
    }

    /// 一键清除动画
    //: func setIsShowCleanAmimat() {
    func realise() {
        //: let unreadMsgCount = DismissReactiveCompatible.share.unreadMessageNum
        let unreadMsgCount = DismissReactiveCompatible.share.unreadMessageNum
        //: let isClean: Bool = UserDefaults.standard.object(forKey: isShowCleanAnimatCacheKey) as? Bool ?? false
        let isClean: Bool = UserDefaults.standard.object(forKey: user_addressFormat) as? Bool ?? false
        //: if unreadMsgCount >= 10 && !isClean {
        if unreadMsgCount >= 10, !isClean {
            //: self.cleanBtnBtnAnimat()
            self.neglectPartner()
        }
    }

    //: func cleanBtnBtnAnimat() {
    func neglectPartner() {
        //: let degreesToRadians = 180.0 * .pi
        let degreesToRadians = 180.0 * .pi
        //: let angle1 = 0 / degreesToRadians
        let angle1 = 0 / degreesToRadians
        //: let angle2 = 100 / degreesToRadians
        let angle2 = 100 / degreesToRadians
        //: let angle3 = -100 / degreesToRadians
        let angle3 = -100 / degreesToRadians
        //: let angle4 = 100 / degreesToRadians
        let angle4 = 100 / degreesToRadians
        //: let angle5 = -100 / degreesToRadians
        let angle5 = -100 / degreesToRadians
        //: let angle6 = 0 / degreesToRadians
        let angle6 = 0 / degreesToRadians

        //: let du1: NSNumber = 0
        let du1: NSNumber = 0
        //: let du2: NSNumber = 0.13
        let du2: NSNumber = 0.13
        //: let du3: NSNumber = 0.3
        let du3: NSNumber = 0.3
        //: let du4: NSNumber = 0.47
        let du4: NSNumber = 0.47
        //: let du5: NSNumber = 0.8
        let du5: NSNumber = 0.8
        //: let du6: NSNumber = 1.0
        let du6: NSNumber = 1.0

        // 旋转
        //: let roate = CAKeyframeAnimation.init(keyPath: "transform.rotation")
        let roate = CAKeyframeAnimation(keyPath: (kLensPackUrl.replacingOccurrences(of: "close", with: "t") + "sfor" + String(k_suitSecretMessage.prefix(5)) + "ation"))
        //: roate.values = [(angle1), (angle2), (angle3), (angle4), (angle5), (angle6)]
        roate.values = [angle1, angle2, angle3, angle4, angle5, angle6]
        //: roate.keyTimes = [(du1), (du2), (du3), (du4), (du5), (du6)]
        roate.keyTimes = [du1, du2, du3, du4, du5, du6]

        // 缩放
        //: let zoom = CAKeyframeAnimation.init(keyPath: "transform.scale")
        let zoom = CAKeyframeAnimation(keyPath: (constHourName.replacingOccurrences(of: "migration", with: "t") + "orm.s" + userExhibitFormat.replacingOccurrences(of: "option", with: "a")))
        //: zoom.values = [(1.0), (1.2), (1.0)]
        zoom.values = [1.0, 1.2, 1.0]
        //: zoom.keyTimes = [(0), (0.5), (1.0)]
        zoom.keyTimes = [0, 0.5, 1.0]

        //
        //: let groupAnnimation = CAAnimationGroup.init()
        let groupAnnimation = CAAnimationGroup()
        //: groupAnnimation.autoreverses = false
        groupAnnimation.autoreverses = false
        //: groupAnnimation.animations = [roate, zoom]
        groupAnnimation.animations = [roate, zoom]
        //: groupAnnimation.duration = 1.0
        groupAnnimation.duration = 1.0
        //: groupAnnimation.repeatCount = 2
        groupAnnimation.repeatCount = 2
        //: groupAnnimation.isRemovedOnCompletion = false
        groupAnnimation.isRemovedOnCompletion = false
        //: groupAnnimation.fillMode = .forwards
        groupAnnimation.fillMode = .forwards
        //: cleanBtn.layer.add(groupAnnimation, forKey: "zoom&shake")
        cleanBtn.layer.add(groupAnnimation, forKey: (String(noti_fashionMsg) + String(data_handleUrl)))
    }

    /// 检查系统通知是否开启
    //: private func func__turnOnSystemNotification() {
    private func proprietorNotification() {
        //: TalkingPermissionTool.checkPushNotification { isEnable in
        SpecialPermissionTool.thinChange { isEnable in

            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: if isEnable {
                if isEnable {
                    //: self.noticeView.isHidden = true
                    self.noticeView.isHidden = true
                    //: self.func__hideNotificationTipView(hide: true)
                    self.actuarialTable(hide: true)
                    //: }else{
                } else {
                    /// 页面顶部提示
                    //: let today = NSDate.getTimeString(date: Date(), dateFormat: "yyyy-MM-dd")
                    let today = NSDate.switchly(date: Date(), dateFormat: (k_backgroundData.replacingOccurrences(of: "resort", with: "yy") + String(data_libraryStr.prefix(7))))
                    //: if let closeDateStr = Defaults.string(forKey: TalkingMessageTabCloseNotificationsKey), today == closeDateStr {
                    if let closeDateStr = data_partyText.string(forKey: const_viewUrl), today == closeDateStr {
                        //: self.func__hideNotificationTipView(hide: true)
                        self.actuarialTable(hide: true)
                        //: }else{
                    } else {
                        //: self.func__hideNotificationTipView(hide: false)
                        self.actuarialTable(hide: false)
                    }
                    /// 弹框逻辑
                    //: let isShow = Defaults.bool(forKey: TalkingMessageTabTurnOnNotificationsKey)
                    let isShow = data_partyText.bool(forKey: mainDisplayFirstUrl)
                    //: if !isShow {
                    if !isShow {
                        //: Defaults.set(true, forKey: TalkingMessageTabTurnOnNotificationsKey)
                        data_partyText.set(true, forKey: mainDisplayFirstUrl)
                        //: TalkingAlertShow.alert(title: nil,
                        AllowanceReactiveCompatible.maximal(title: nil,
                                                 //: message: "Allow %@ to send you notifications?".localizedArguments(AppName),
                                                 message: String(bytes: show_actDimensionText.map{soundARef(cell: $0)}, encoding: .utf8)!.unitedStates(showLengthControlUrl),
                                                 //: leftBtnTitle: "Cancel".localized,
                                                 leftBtnTitle: (String(k_contactUpperContent)).localized,
                                                 //: rightBtnTitle: "Settings".localized) {
                                                 rightBtnTitle: (String(userAppHoldSafeId)).localized)
                        {
                            //: TalkingAlertShow.hideAlert()
                            AllowanceReactiveCompatible.minuteOriginBridge()
                            //: return
                            //: } rightBlock: {
                        } rightBlock: {
                            //: if let settingsUrl = URL(string: UIApplication.openSettingsURLString) {
                            if let settingsUrl = URL(string: UIApplication.openSettingsURLString) {
                                //: UIApplication.shared.open(settingsUrl)
                                UIApplication.shared.open(settingsUrl)
                            }
                        }
                    }
                }
            }
        }
    }

    //: private func func__hideNotificationTipView(hide: Bool) {
    private func actuarialTable(hide: Bool) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: if hide{
            if hide {
                //: self.noticeView.isHidden = true
                self.noticeView.isHidden = true
                //: self.slideVC.view.snp.updateConstraints { make in
                self.slideVC.view.snp.updateConstraints { make in
                    //: make.top.equalToSuperview().offset(StatusBarHeight)
                    make.top.equalToSuperview().offset(dataPointTitle)
                }
                //: self.slideVC.viewHeight = ScreenHeight - StatusBarHeight - TabBarViewHeight
                self.slideVC.viewHeight = main_nameData - dataPointTitle - main_explainMessage
                //: }else{
            } else {
                //: self.noticeView.isHidden = false
                self.noticeView.isHidden = false
                //: self.slideVC.view.snp.updateConstraints { make in
                self.slideVC.view.snp.updateConstraints { make in
                    //: make.top.equalToSuperview().offset(StatusBarHeight + self.noticeView.viewHeight)
                    make.top.equalToSuperview().offset(dataPointTitle + self.noticeView.viewHeight)
                }
                //: self.slideVC.viewHeight = ScreenHeight - self.noticeView.bottom - TabBarViewHeight
                self.slideVC.viewHeight = main_nameData - self.noticeView.bottom - main_explainMessage
            }
        }
    }
}

// MARK: - 通知

//: extension TalkingMessageViewController {
extension RatingDisableThen {
    /// who like me 红点通知
    //: @objc func showbadgNumberNotif(notification: NSNotification) {
    @objc func showbadgToNotification(notification: NSNotification) {
        //: let userinfo = notification.userInfo as![String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let badNumber: Int  = userinfo["badNumber"] as! Int
        let badNumber: Int = userinfo[(String(notiSessionMsg.prefix(4)) + "umber")] as! Int
        //: badgLB.isHidden = false
        badgLB.isHidden = false
        //: if badNumber > 99 {
        if badNumber > 99 {
            //: badgLB.text = "99+"
            badgLB.text = "99+"
            //: } else if badNumber<=0 {
        } else if badNumber <= 0 {
            //: badgLB.isHidden = true
            badgLB.isHidden = true
            //: badgLB.text = "0"
            badgLB.text = "0"
            //: } else {
        } else {
            //: badgLB.text = String(badNumber)
            badgLB.text = String(badNumber)
        }
    }
}

// MARK: - Layout

//: extension TalkingMessageViewController {
extension RatingDisableThen {
    /// UI
    //: private func createUI() {
    private func thingLoad() {
        //: view.backgroundColor = .white
        view.backgroundColor = .white

        //: view.addSubview(colorImageView)
        view.addSubview(colorImageView)
        //: view.addSubview(noticeView)
        view.addSubview(noticeView)
        //: self.addChild(slideVC)
        self.addChild(slideVC)

        //: self.view.addSubview(slideVC.view)
        self.view.addSubview(slideVC.view)
        //: slideVC.view.backgroundColor = UIColor.clear
        slideVC.view.backgroundColor = UIColor.clear
        //: slideVC.view.snp.makeConstraints { make in
        slideVC.view.snp.makeConstraints { make in
            //: make.top.equalToSuperview().offset(StatusBarHeight)
            make.top.equalToSuperview().offset(dataPointTitle)
            //: make.leading.trailing.equalToSuperview()
            make.leading.trailing.equalToSuperview()
            //: make.bottom.equalToSuperview()
            make.bottom.equalToSuperview()
        }

        //: for btn in slideVC.allBtn {
        for btn in slideVC.allBtn {
            //: let curenbrn: UIButton = btn
            let curenbrn: UIButton = btn
            //: if curenbrn.titleLabel!.text == "Who like me".localized {
            if curenbrn.titleLabel!.text == (String(constHelpStr) + showPlanPath.lowercased()).localized {
                //: (curenbrn as AnyObject).addSubview(badgLB)
                (curenbrn as AnyObject).addSubview(badgLB)
                //: badgLB.snp.makeConstraints { make in
                badgLB.snp.makeConstraints { make in
                    //: make.leading.equalTo(curenbrn.titleLabel!.snp.trailing).offset(-5)
                    make.leading.equalTo(curenbrn.titleLabel!.snp.trailing).offset(-5)
                    //: make.top.equalTo(curenbrn).offset(7)
                    make.top.equalTo(curenbrn).offset(7)
                    //: make.height.equalTo(16)
                    make.height.equalTo(16)
                    //: make.width.greaterThanOrEqualTo(16)
                    make.width.greaterThanOrEqualTo(16)
                }
                //: badgLB.isHidden = true
                badgLB.isHidden = true
                //: } else if (curenbrn.titleLabel!.text == "Messages".localized) {
            } else if curenbrn.titleLabel!.text == (String(constPinIceId.prefix(7)) + String(k_additionalName)).localized {
                //: (curenbrn as AnyObject).addSubview(msgNumLabel)
                (curenbrn as AnyObject).addSubview(msgNumLabel)
                //: msgNumLabel.snp.makeConstraints { make in
                msgNumLabel.snp.makeConstraints { make in
                    //: make.leading.equalTo(curenbrn.titleLabel!.snp.trailing).offset(-5)
                    make.leading.equalTo(curenbrn.titleLabel!.snp.trailing).offset(-5)
                    //: make.top.equalTo(curenbrn).offset(7)
                    make.top.equalTo(curenbrn).offset(7)
                    //: make.height.equalTo(16)
                    make.height.equalTo(16)
                    //: make.width.greaterThanOrEqualTo(16)
                    make.width.greaterThanOrEqualTo(16)
                }
            }
        }

        //: view.addSubview(cleanBtn)
        view.addSubview(cleanBtn)
        //: cleanBtn.snp.makeConstraints { make in
        cleanBtn.snp.makeConstraints { make in
            //: make.top.equalTo(slideVC.view.snp.top)
            make.top.equalTo(slideVC.view.snp.top)
            //: make.trailing.equalTo(-5)
            make.trailing.equalTo(-5)
            //: make.size.equalTo(CGSize(width: 44, height: 44))
            make.size.equalTo(CGSize(width: 44, height: 44))
        }

        //: view.addSubview(netTipLabel)
        view.addSubview(netTipLabel)
        //: netTipLabel.snp.makeConstraints { make in
        netTipLabel.snp.makeConstraints { make in
            //: make.top.equalTo(slideVC.view.snp.top).offset(44)
            make.top.equalTo(slideVC.view.snp.top).offset(44)
            //: make.leading.trailing.equalToSuperview()
            make.leading.trailing.equalToSuperview()
            //: make.height.equalTo(30)
            make.height.equalTo(30)
        }
    }

    /// 绑定事件
    //: private func bindInteraction() {
    private func bondPhoneQuantityeractionOperate() {
        //: AfterThen.shared.rx
        AfterThen.shared.rx
            //: .observeWeakly(Bool.self, "isConnection")
            .observeWeakly(Bool.self, (String(const_themeValue.suffix(6)) + data_extentPath.replacingOccurrences(of: "suit", with: "t")))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.func__checkNetTipState()
                self.layJudge()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: DismissReactiveCompatible.share.rx.observeWeakly(Int.self, "networkStatus")
        DismissReactiveCompatible.share.rx.observeWeakly(Int.self, (const_lessFirmOnId.replacingOccurrences(of: "ear", with: "et") + String(notiHostMsg)))
            //: .subscribe(onNext: { [weak self]  (value) in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.func__checkNetTipState()
                self.layJudge()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        // 展示全部红点
        //: DismissReactiveCompatible.share.rx.observeWeakly(Int.self, "unreadMessageNum")
        DismissReactiveCompatible.share.rx.observeWeakly(Int.self, (mainFlagTitle.replacingOccurrences(of: "sun", with: "r") + "adMe" + mainYourselfPath.replacingOccurrences(of: "natural", with: "e") + String(const_bitPath.prefix(3))))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: let valueInt = value ?? 0
                let valueInt = value ?? 0
                //: self.func__messagebadgLBState(num: valueInt)
                self.intervaleraction(num: valueInt)
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: cleanBtn.rx.controlEvent(.touchUpInside)
        cleanBtn.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: {
            .subscribe(onNext: {
                //: let config = ShowAlertConfig()
                let config = PortraitAlertConfig()
                //: config.alignment = .center
                config.alignment = .center

                //: TalkingAlertShow.customAlert(message: "Do you want to mark all messages as read?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "OK".localized, leftBlock: {
                AllowanceReactiveCompatible.dateConfig(message: String(bytes: dataCommonPicFormat.map{timeThen(year: $0)}, encoding: .utf8)!.localized, leftBtnTitle: (String(k_contactUpperContent)).localized, rightBtnTitle: "OK".localized, leftBlock: {
                    //: TalkingAlertShow.hideAlert()
                    AllowanceReactiveCompatible.minuteOriginBridge()

                    //: }, rightBlock: {
                }, rightBlock: {
                    //: V2TIMManager.sharedInstance().markAllMessage {
                    V2TIMManager.sharedInstance().markAllMessage {
                        //: UserDefaults.standard.set(true, forKey: isShowCleanAnimatCacheKey)
                        UserDefaults.standard.set(true, forKey: user_addressFormat)
                        //: } fail: { code, desc in
                    } fail: { code, desc in
                        //: UploadLogTool.writeLog(msg: "消息列表一键已读失败：code:\(code), desc:\(String(describing: desc))")
                        MateThen.trackBy(msg: (appVariationMessage.replacingOccurrences(of: "share", with: "键") + String(app_localMessage) + String(appMaybeStr.prefix(6))) + "\(code)" + (String(constBackgroundTitle.suffix(7))) + "\(String(describing: desc))")
                    }
                    //: }, config: config)
                }, config: config)

                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        // who like me 红点通知
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(showbadgNumberNotif(notification:)),
                                               selector: #selector(showbadgToNotification(notification:)),
                                               //: name: WHOLIKEME_BADGENUMBER_NOTIF,
                                               name: showToMessage,
                                               //: object: nil)
                                               object: nil)

        //: noticeView.closeBtnBlock = { [weak self] ()  -> Void in
        noticeView.closeBtnBlock = { [weak self] () in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: let today = NSDate.getTimeString(date: Date(), dateFormat: "yyyy-MM-dd")
            let today = NSDate.switchly(date: Date(), dateFormat: (k_backgroundData.replacingOccurrences(of: "resort", with: "yy") + String(data_libraryStr.prefix(7))))
            //: Defaults.set(today, forKey: TalkingMessageTabCloseNotificationsKey)
            data_partyText.set(today, forKey: const_viewUrl)
            //: self.func__hideNotificationTipView(hide: true)
            self.actuarialTable(hide: true)
        }

        //: NotificationCenter.default.rx.notification( UIApplication.willEnterForegroundNotification)
        NotificationCenter.default.rx.notification(UIApplication.willEnterForegroundNotification)
            //: .subscribe(onNext: { [weak self] (notification) in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.func__turnOnSystemNotification()
                self.proprietorNotification()
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }
}
