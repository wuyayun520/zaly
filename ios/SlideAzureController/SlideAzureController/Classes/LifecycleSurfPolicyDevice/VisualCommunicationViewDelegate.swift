
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let mainQuantityFlagData:[UInt8] = [0xcf,0xc8,0xcf,0xd2,0x8e,0xc5,0xc9,0xc2,0xc3,0xd4,0x9c,0x8f,0x86,0xce,0xc7,0xd5,0x86,0xc8,0xc9,0xd2,0x86,0xc4,0xc3,0xc3,0xc8,0x86,0xcf,0xcb,0xd6,0xca,0xc3,0xcb,0xc3,0xc8,0xd2,0xc3,0xc2]

private func brightArrow(others num: UInt8) -> UInt8 {
    return num ^ 166
}

/*: "uid" :*/
fileprivate let user_hideData:[UInt8] = [0xb7,0xab,0xa6]

private func blowWarm(theme num: UInt8) -> UInt8 {
    return num ^ 194
}

/*: "headPic" :*/
fileprivate let main_succeedMessage:String = "headPiccomment sick"

/*: "nickname" :*/
fileprivate let userPointTitle:[Character] = ["n","i","c","k","n","a"]
fileprivate let noti_contrastTitle:[Character] = ["m","e"]

/*: "birthday" :*/
fileprivate let app_trackMyFormat:String = "birthdiscy"

/*: "videoUrl" :*/
fileprivate let mainServiceMsg:String = "firm stall movie absolute domainvideoUrl"

/*: "cover" :*/
fileprivate let kSeedUrl:String = "worldver"

/*: "location" :*/
fileprivate let data_frontHisUrl:[Character] = ["l","o"]
fileprivate let main_cropTitle:String = "cadragion"

/*: "cmd" :*/
fileprivate let notiContextMsg:[UInt8] = [0x86,0x88,0x81]

private func signHim(menu num: UInt8) -> UInt8 {
    return num ^ 229
}

/*: "refuseCall" :*/
fileprivate let notiRefreshlyText:String = "guidance speak siderefuseCa"
fileprivate let dataBasicObserverPath:String = "them"

/*: "data" :*/
fileprivate let mainDisableMerelySpringMsg:[UInt8] = [0xbf,0xba,0xaf,0xba]

private func socialShot(swing num: UInt8) -> UInt8 {
    return num ^ 219
}

/*: "Call interruption" :*/
fileprivate let showRemoveForwardStr:[Character] = ["C","a","l","l"," ","i","n","t","e","r","r","u","p","t","i","o","n"]

/*: "stopCall" :*/
fileprivate let notiOwnerMsg:[Character] = ["s","t","o","p","C","a","l"]
fileprivate let main_supportWhatName:[Character] = ["l"]

/*: "logId" :*/
fileprivate let show_familyContent:[UInt8] = [0x78,0x7b,0x73,0x5d,0x70]

private func betweenSpread(filter num: UInt8) -> UInt8 {
    return num ^ 20
}

/*: "cancelCall" :*/
fileprivate let noti_thinName:[Character] = ["c","a","n","c","e","l","C","a"]
fileprivate let showResMsg:[Character] = ["l","l"]

/*: "No answer" :*/
fileprivate let notiKingdomUrl:String = "No acompare element red heavy magnitude"

/*: "duration" :*/
fileprivate let dataFollowingPath:String = "DURA"

/*: "She has hung up" :*/
fileprivate let showFailMsg:String = "She hminute object margin big"
fileprivate let const_browData:[Character] = [" ","u","p"]

/*: "cancelSex" :*/
fileprivate let main_inputValue:String = "cancelSexret precedent deep advertising"

/*: "He has hung up" :*/
fileprivate let appMainFormat:[Character] = ["H","e"," ","h","a","s"," ","h","u","n","g"," "]
fileprivate let showTotalroTrailStr:String = "viewp"

/*: "endType" :*/
fileprivate let mainPageMessage:[Character] = ["e","n","d","T","y","p","e"]

/*: "stopSex" :*/
fileprivate let dataUsKey:String = "record drown stroke birthday splitstopS"
fileprivate let const_appropriateFormat:[Character] = ["e","x"]

/*: "Insufficient gold coins, call has ended" :*/
fileprivate let dataDateKey:[UInt8] = [0x36,0x11,0xc,0xa,0x19,0x19,0x16,0x1c,0x16,0x1a,0x11,0xb,0x5f,0x18,0x10,0x13,0x1b,0x5f,0x1c,0x10,0x16,0x11,0xc,0x53,0x5f,0x1c,0x1e,0x13,0x13,0x5f,0x17,0x1e,0xc,0x5f,0x1a,0x11,0x1b,0x1a,0x1b]

/*: "The other hung up the phone" :*/
fileprivate let main_replacementTitle:String = "water enhance expression there taThe o"
fileprivate let appValidContent:String = "hung uroute attach yes"
fileprivate let constBirthKey:String = " phoneconversation spread hidden remain"

/*: "type" :*/
fileprivate let appQualityMissingValue:String = "TYPE"

/*: "jumpUrl" :*/
fileprivate let noti_writingTitle:String = "jumpUrlpath priority cur plan"

/*: "Cancel" :*/
fileprivate let noti_makeupRobotName:[Character] = ["C","a","n","c","e","l"]

/*: "Get gold coins" :*/
fileprivate let userExclusiveColdText:[Character] = ["G"]
fileprivate let user_kingdomReadingInsertName:[Character] = ["e","t"," ","g","o","l","d"," ","c","o","i","n","s"]

/*: "addBean" :*/
fileprivate let constUnknownStr:String = "addBeanblind positive para tool animal"

/*: "refuseSex" :*/
fileprivate let dataHangPath:String = "reprose"
fileprivate let notiControlStrikeHeadMsg:String = "Sexpose fashion"

/*: "onAcceptCall" :*/
fileprivate let dataTipAtPath:String = "dominant drawonAccep"
fileprivate let userShoulderKey:String = "tCallmovement resistance now bad transportation"

/*: "acceptCall" :*/
fileprivate let mainBusyClearExceptionMessage:[Character] = ["a","c","c","e","p","t","C","a","l","l"]

/*: "Calls are cancelled" :*/
fileprivate let appRecordingId:String = "Calls bit policy slide asset"
fileprivate let noti_giveText:String = "foundllfoundd"

/*: "Continue matching with other users？" :*/
fileprivate let dataDisappearStr:[UInt8] = [0x9f,0xbc,0xef,0x73,0x72,0x65,0x73,0x75,0x20,0x72,0x65,0x68,0x74,0x6f,0x20,0x68,0x74,0x69,0x77,0x20,0x67,0x6e,0x69,0x68,0x63,0x74,0x61,0x6d,0x20,0x65,0x75,0x6e,0x69,0x74,0x6e,0x6f,0x43]

/*: "Yes" :*/
fileprivate let notiContactTitle:[Character] = ["Y","e","s"]

/*: "The call has ended" :*/
fileprivate let user_distributeName:[Character] = ["T","h","e"," "]
fileprivate let k_theFormat:String = "call hmin beautiful currency"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  VisualCommunicationViewDelegate.swift
//  AbroadTalking
//
//  Created by young on 2022/11/8.
//

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver
//: import UIKit
import UIKit

// 通话监听(必须为全局属性，否则无法接听)
//: let videoCallHelper = CXCallObserver()
let mainMePointUrl = CXCallObserver()
//: class TalkingVideoChatViewController: TalkingBaseViewController {
class VisualCommunicationViewDelegate: WeddingLovelyRecognizerDelegate {
    //: var chatModel: TalkingVideoChatModel?
    var chatModel: IceChatModel? // 视频通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: ApplicationReactiveCompatible? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVideoMiniView?
    private var miniView: SpecialEndMiniView? // 视频通话小窗口
    //: private var isFromMiniView = false
    private var isFromMiniView = false // 是否点击小窗口进入
    //: private var reportView: TalkingVideoFloatScreenView?
    private var reportView: WrittenSymbolThen? // 视频举报弹窗

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        SantaLuciaFirSocketDelegate.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        SantaLuciaFirSocketDelegate.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        SantaLuciaFirSocketDelegate.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: mainQuantityFlagData.map{brightArrow(others: $0)}, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! TicktockViewController
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.lengthDown()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVideoAuth()
            inform()
            //: } else if self.chatModel?.videoStage == .Calling {
        } else if self.chatModel?.videoStage == .Calling { // 直接创建通话中的界面（包括重连）
            // 通话小窗口进入无需重复创建
            //: guard isFromMiniView == false else { return }
            guard isFromMiniView == false else { return }
            //: createCallingRoom(logId: self.chatModel?.logId)
            conversationDisable(logId: self.chatModel?.logId)
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.currInfoVC = nil
            SantaLuciaFirSocketDelegate.shared.currInfoVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.byproduct()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        separateOut()
        //: setupSubViewsConstraint()
        setupFromConstraint()
        //: initNotificationObserver()
        supplyBlind()
        //: req_callGetUserInfo()
        grade()
        //: timeoutRules()
        goShrink()
        //: setUpBGMPlayer()
        glycerolize()
        //: if self.chatModel?.videoStage != .Recieved {
        if self.chatModel?.videoStage != .Recieved {
            //: self.startPreview()
            self.alter()
        }
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if DismissReactiveCompatible.share.appConfigMode.disableShootScreen {
            if DismissReactiveCompatible.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = AeronauticalEngineeringReactiveCompatible(frame: CGRect(x: 0, y: 0, width: main_logStr, height: main_nameData))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.isInfo = false
        SantaLuciaFirSocketDelegate.shared.isInfo = false
        //: TalkingSocketManager.shared.isCalling = false
        SantaLuciaFirSocketDelegate.shared.isCalling = false
        //: self.stopVCAllActivties()
        self.narrowActivties()
    }

    // MARK: - Lazy Load

    /// 视频通话背景
    //: private lazy var bgVideoView: TalkingBGVideoView = {
    private lazy var bgVideoView: ContainerView = {
        //: let v = TalkingBGVideoView()
        let v = ContainerView()
        //: v.showCoverView(show: self.chatModel?.videoStage != .Calling)
        v.tillCity(show: self.chatModel?.videoStage != .Calling)
        //: return v
        return v
        //: }()
    }()

    /// 视频通话操作按钮
    //: private lazy var interactionView: Talking1v1VideoInteractionView = {
    private lazy var interactionView: PrecociousViewDelegate = {
        //: let v = Talking1v1VideoInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = PrecociousViewDelegate(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    // 视频通话小窗口
    //: private lazy var windowView: TalkingVideoWindowView = {
    private lazy var windowView: DevoteThen = {
        //: let v = TalkingVideoWindowView()
        let v = DevoteThen()
        //: var x = ScreenWidth-15-actualWidth(w: 125)
        var x = main_logStr - 15 - actualWidth(w: 125)
        //: if LanguageManager.shared.direction == .rightToLeft {
        if RatingReactiveCompatible.shared.direction == .rightToLeft {
            //: x = 15
            x = 15
        }
        //: v.frame = CGRect(x: x, y: StatusBarHeight+actualHeight(h: 25)+26+35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        v.frame = CGRect(x: x, y: dataPointTitle + actualHeight(h: 25) + 26 + 35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        //: v.isHidden = self.chatModel?.videoStage != .Calling
        v.isHidden = self.chatModel?.videoStage != .Calling
        //: v.block = { [weak self] in
        v.block = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.changeRenderView()
            self.quantityerval()
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: RegularArtCloudDelegate = {
        //: let m = TalkingVideoChatManager()
        let m = RegularArtCloudDelegate()
        //: m.infoModel = self.chatModel
        m.infoModel = self.chatModel
        // 视图传递给manager
        //: m.bgVideoView = self.bgVideoView.renderView
        m.bgVideoView = self.bgVideoView.renderView
        //: m.bgView = self.bgVideoView.bgView
        m.bgView = self.bgVideoView.bgView
        //: m.windowVideoView = self.windowView.renderView
        m.windowVideoView = self.windowView.renderView
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request && 刷新UI

//: extension TalkingVideoChatViewController {
extension VisualCommunicationViewDelegate {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func grade() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        RegularArtCloudDelegate.draftCompletion(uid: pairUid) { [weak self] succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: user_hideData.map{blowWarm(theme: $0)}, encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(main_succeedMessage.prefix(7)))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(String(userPointTitle) + String(noti_contrastTitle))].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(app_trackMyFormat.replacingOccurrences(of: "disc", with: "da"))].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(String(mainServiceMsg.suffix(8)))].stringValue
            //: self.chatModel?.cover = json["cover"].stringValue
            self.chatModel?.cover = json[(kSeedUrl.replacingOccurrences(of: "world", with: "co"))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(String(data_frontHisUrl) + main_cropTitle.replacingOccurrences(of: "drag", with: "t"))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.agentInfo()
            //: self.videoBG_refreshViewWithCameraStatus()
            self.statusRobot()
        }
    }

    /// 根据摄像头是否开启状态，刷新视频通话背景
    //: private func videoBG_refreshViewWithCameraStatus() {
    private func statusRobot() {
        //: var bgHeadPic: String?
        var bgHeadPic: String?
        //: var bgShowName: String?
        var bgShowName: String?
        //: var bgOpenCamera = VideoCameraStatus.Open
        var bgOpenCamera = SubordinateCameraStatus.Open
        //: var windowHeadPic: String?
        var windowHeadPic: String?
        //: var windowOpenCamera = VideoCameraStatus.Open
        var windowOpenCamera = SubordinateCameraStatus.Open

        //: if self.chatModel?.renderPostion == .SelfOnWindow {
        if self.chatModel?.renderPostion == .SelfOnWindow { // 自己在窗口
            //: bgHeadPic = self.chatModel?.pairHeadPic
            bgHeadPic = self.chatModel?.pairHeadPic
            //: bgShowName = self.chatModel?.pairNickName
            bgShowName = self.chatModel?.pairNickName
            //: bgOpenCamera = self.videoManager.config.remoteCameraStatus
            bgOpenCamera = self.videoManager.config.remoteCameraStatus
            //: windowHeadPic = self.chatModel?.iHeadPic
            windowHeadPic = self.chatModel?.iHeadPic
            //: windowOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            windowOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed

            //: } else {
        } else { // 自己在大背景
            //: bgHeadPic = self.chatModel?.iHeadPic
            bgHeadPic = self.chatModel?.iHeadPic
            //: bgShowName = self.chatModel?.iNickName
            bgShowName = self.chatModel?.iNickName
            //: bgOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            bgOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed
            //: windowHeadPic = self.chatModel?.pairHeadPic
            windowHeadPic = self.chatModel?.pairHeadPic
            //: windowOpenCamera = self.videoManager.config.remoteCameraStatus
            windowOpenCamera = self.videoManager.config.remoteCameraStatus
        }

        //: self.bgVideoView.refreshBgView(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        self.bgVideoView.segment(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        //: self.windowView.refreshBgView(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
        self.windowView.counterval(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
    }
}

// MARK: - Event

//: extension TalkingVideoChatViewController {
extension VisualCommunicationViewDelegate {
    /// 权限校验，播放BGM
    //: private func checkVideoAuth() {
    private func inform() {
        //: TalkingPermissionTool.checkCameraAndMicrophone { isOpen in
        SpecialPermissionTool.remainMicrophone { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.enablece(showMsg: appMicUrl)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                SantaLuciaFirSocketDelegate.shared.amOrigin(info: [String(bytes: notiContextMsg.map{signHim(menu: $0)}, encoding: .utf8)!: (String(notiRefreshlyText.suffix(8)) + dataBasicObserverPath.replacingOccurrences(of: "them", with: "ll")), String(bytes: mainDisableMerelySpringMsg.map{socialShot(swing: $0)}, encoding: .utf8)!: [String(bytes: user_hideData.map{blowWarm(theme: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.indicatorMatch()
                //: return
                return
            }

            //: self.startPreview()
            self.alter()
            //: self.stopBGM()
            self.receive()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = ApplicationReactiveCompatible.limitedType(type: .Response)
            //: self.startBGM()
            self.lengthDown()
        }
    }

    /// 预览视频画面
    //: private func startPreview() {
    private func alter() {
        //: self.videoManager.startPreview()
        self.videoManager.precedent()
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func narrowActivties() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.receive()
        //: TalkingVideoDanmuManager.danmu_releaseAllResource()
        SparkDanmuManager.fiscalYearResource()
        //: TalkingSocketManager.shared.callerDelegate = nil
        SantaLuciaFirSocketDelegate.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        SantaLuciaFirSocketDelegate.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        SantaLuciaFirSocketDelegate.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func indicatorMatch(animated: Bool = true) {
        //: self.stopBGM()
        self.receive()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 视频通话双方窗口转换
    //: private func changeRenderView() {
    private func quantityerval() {
        //: if self.chatModel?.renderPostion == .selfOnBg {
        if self.chatModel?.renderPostion == .selfOnBg {
            //: self.chatModel?.renderPostion = .SelfOnWindow
            self.chatModel?.renderPostion = .SelfOnWindow
            //: } else if self.chatModel?.renderPostion == .SelfOnWindow {
        } else if self.chatModel?.renderPostion == .SelfOnWindow {
            //: self.chatModel?.renderPostion = .selfOnBg
            self.chatModel?.renderPostion = .selfOnBg
        }

        //: self.videoManager.changeRenderView()
        self.videoManager.system()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.statusRobot()
    }

    /// 创建视频通话房间
    //: private func createTalkRoom() {
    private func talkAppearance() {
        //: self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        //: self.videoManager.createRoomAndStartPublish()
        self.videoManager.untilCapture()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.statusRobot()
        // 展示投诉说明飘屏（男性）
        //: self.show_videoFloatScreenAnimation()
        self.burn()
    }

    /// 展示投诉说明飘屏（男性）
    //: private func show_videoFloatScreenAnimation() {
    private func burn() {
        //: guard DismissReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue else { return }
        guard DismissReactiveCompatible.share.loginUserMode.sex == FenceSequence.male.rawValue else { return }
        //: guard reportView == nil else { return }
        guard reportView == nil else { return }

        //: reportView = TalkingVideoFloatScreenView()
        reportView = WrittenSymbolThen()
        //: view.addSubview(reportView!)
        view.addSubview(reportView!)
        //: reportView?.show_animation()
        reportView?.controlFactor()
    }
}

// MARK: - 通知

//: extension TalkingVideoChatViewController {
extension VisualCommunicationViewDelegate {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func supplyBlind() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(pauseNo(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 监听 对方摄像头是否可用
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(updateCameraStatus(notification:)),
                                               selector: #selector(yesterdayLaunch(notification:)),
                                               //: name: VIDEO_CHAT_OTHER_CAMERA_AVAILABLE_NOTIFICATION,
                                               name: noti_micSystemName,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(reachability(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: k_skipMessage,
                                               //: object: nil)
                                               object: nil)

        //: videoCallHelper.setDelegate(self, queue: DispatchQueue.main)
        mainMePointUrl.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func pauseNo(notif _: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            indicatorMatch()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func reachability(note _: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard SantaLuciaFirSocketDelegate.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        SantaLuciaFirSocketDelegate.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        SantaLuciaFirSocketDelegate.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        SantaLuciaFirSocketDelegate.shared.errorDelegate = self
        //: self.videoManager.restartTalkWhenDisconnectedAndNetAvailable()
        self.videoManager.boneAvailable()
    }

    /// 根据对方摄像头状态更新UI
    /// - Parameter notification: 通知
    //: @objc private func updateCameraStatus(notification: NSNotification) {
    @objc private func yesterdayLaunch(notification _: NSNotification) {
        //: self.videoBG_refreshViewWithCameraStatus()
        self.statusRobot()
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVideoChatViewController: CXCallObserverDelegate {
extension VisualCommunicationViewDelegate: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption")
            self.enablece(showMsg: (String(showRemoveForwardStr)))
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                SantaLuciaFirSocketDelegate.shared.amOrigin(info: [String(bytes: notiContextMsg.map{signHim(menu: $0)}, encoding: .utf8)!: (String(notiOwnerMsg) + String(main_supportWhatName)), String(bytes: mainDisableMerelySpringMsg.map{socialShot(swing: $0)}, encoding: .utf8)!: [String(bytes: show_familyContent.map{betweenSpread(filter: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                SantaLuciaFirSocketDelegate.shared.amOrigin(info: [String(bytes: notiContextMsg.map{signHim(menu: $0)}, encoding: .utf8)!: (String(noti_thinName) + String(showResMsg)), String(bytes: mainDisableMerelySpringMsg.map{socialShot(swing: $0)}, encoding: .utf8)!: [String(bytes: show_familyContent.map{betweenSpread(filter: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                SantaLuciaFirSocketDelegate.shared.amOrigin(info: [String(bytes: notiContextMsg.map{signHim(menu: $0)}, encoding: .utf8)!: (String(notiRefreshlyText.suffix(8)) + dataBasicObserverPath.replacingOccurrences(of: "them", with: "ll")), String(bytes: mainDisableMerelySpringMsg.map{socialShot(swing: $0)}, encoding: .utf8)!: [String(bytes: user_hideData.map{blowWarm(theme: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.detectScreen()
                //: TalkingSocketManager.shared.currInfoVC = nil
                SantaLuciaFirSocketDelegate.shared.currInfoVC = nil
            }
            //: popCurrentViewController()
            indicatorMatch()
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVideoChatViewController {
extension VisualCommunicationViewDelegate {
    /// 初始化视频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func glycerolize() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? PhaseEquityValueConvertible.Call : PhaseEquityValueConvertible.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = ApplicationReactiveCompatible.limitedType(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func lengthDown() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.stage()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func receive() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.belowAppropriate()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVideoChatViewController {
extension VisualCommunicationViewDelegate {
    /// 超时定时器
    //: private func timeoutRules() {
    private func goShrink() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? constSystemKey : self.chatModel?.dotTime()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            benignity()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.benignity()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func benignity() {
        //: guard !TalkingSocketManager.shared.isInfo else { return }
        guard !SantaLuciaFirSocketDelegate.shared.isInfo else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.enablece(showMsg: (String(notiKingdomUrl.prefix(4)) + "nswer").localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: notiContextMsg.map{signHim(menu: $0)}, encoding: .utf8)!: (String(noti_thinName) + String(showResMsg)), String(bytes: mainDisableMerelySpringMsg.map{socialShot(swing: $0)}, encoding: .utf8)!: [String(bytes: show_familyContent.map{betweenSpread(filter: $0)}, encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: notiContextMsg.map{signHim(menu: $0)}, encoding: .utf8)!: (String(notiRefreshlyText.suffix(8)) + dataBasicObserverPath.replacingOccurrences(of: "them", with: "ll")), String(bytes: mainDisableMerelySpringMsg.map{socialShot(swing: $0)}, encoding: .utf8)!: [String(bytes: user_hideData.map{blowWarm(theme: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        SantaLuciaFirSocketDelegate.shared.amOrigin(info: socketDict)
        //: popCurrentViewController()
        indicatorMatch()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVideoChatViewController {
extension VisualCommunicationViewDelegate {
    /// 恢复视频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func timeEnable(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: show_familyContent.map{betweenSpread(filter: $0)}, encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(dataFollowingPath.lowercased() + "tion")].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.bringCreate(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.validStage()
            //: createTalkRoom()
            talkAppearance()
            //: self.stopBGM()
            self.receive()
        }
    }

    /// 创建通话中房间
    /// - Parameter logId: 通话记录Id
    //: private func createCallingRoom(logId: Int?) {
    private func conversationDisable(logId: Int?) {
        //: guard let logId = logId else { return }
        guard let logId = logId else { return }

        //: self.chatModel?.logId = logId
        self.chatModel?.logId = logId
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.videoManager.changeRenderView()
        self.videoManager.system()
        //: self.bgVideoView.showCoverView(show: false)
        self.bgVideoView.tillCity(show: false)
        //: self.stopBGM()
        self.receive()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.validStage()
        //: self.createTalkRoom()
        self.talkAppearance()
    }
}

// MARK: - AssetsObjectProtocol【拨打方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCallerDelegate {
extension VisualCommunicationViewDelegate: AssetsObjectProtocol {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func milage(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(showFailMsg.prefix(5)) + "as hung" + String(const_browData)).localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(String(main_inputValue.prefix(9)))].stringValue == FenceSequence.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(appMainFormat) + showTotalroTrailStr.replacingOccurrences(of: "view", with: "u")).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.enablece(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.detectScreen()
            //: TalkingSocketManager.shared.currInfoVC = nil
            SantaLuciaFirSocketDelegate.shared.currInfoVC = nil
        }

        //: popCurrentViewController()
        indicatorMatch()
    }

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func layer(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        conversationDisable(logId: json[String(bytes: show_familyContent.map{betweenSpread(filter: $0)}, encoding: .utf8)!].intValue)
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func earRate(data: [String: Any]) {
        //: TalkingAlertShow.hideAlert()
        AllowanceReactiveCompatible.minuteOriginBridge()
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(String(mainPageMessage))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(showFailMsg.prefix(5)) + "as hung" + String(const_browData)).localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(dataUsKey.suffix(5)) + String(const_appropriateFormat))].stringValue == FenceSequence.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(appMainFormat) + showTotalroTrailStr.replacingOccurrences(of: "view", with: "u")).localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if DismissReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue {
            if DismissReactiveCompatible.share.appStatus == AfterArtSumLiteral.normal.rawValue {
                //: typeStr = "Insufficient gold coins, call has ended".localized
                typeStr = String(bytes: dataDateKey.map{$0^127}, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(main_replacementTitle.suffix(5)) + "ther " + String(appValidContent.prefix(6)) + "p the" + String(constBirthKey.prefix(6))).localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.enablece(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.detectScreen()
            //: TalkingSocketManager.shared.currInfoVC = nil
            SantaLuciaFirSocketDelegate.shared.currInfoVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.platformDismiss()
        //: popCurrentViewController()
        indicatorMatch()
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func maleData(msg: String, data: [String: Any]) {
        //: guard DismissReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard DismissReactiveCompatible.share.appStatus == AfterArtSumLiteral.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(appQualityMissingValue.lowercased())] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
//            if self.interactionView.isShowRecharge {
//                return
//            }
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(noti_writingTitle.prefix(7)))] as? String else { return }
            //: let httpsUrl = DismissReactiveCompatible.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = DismissReactiveCompatible.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = EndConfig()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: PushThen.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            PushThen.share.plumeBright(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            AllowanceReactiveCompatible.maximal(title: nil,
                                     //: message: msg,
                                     message: msg,
                                     //: leftBtnTitle: "Cancel".localized,
                                     leftBtnTitle: (String(noti_makeupRobotName)).localized,
                                     //: rightBtnTitle: "Get gold coins".localized) {
                                     rightBtnTitle: (String(userExclusiveColdText) + String(user_kingdomReadingInsertName)).localized)
            {
                //: TalkingAlertShow.hideAlert()
                AllowanceReactiveCompatible.minuteOriginBridge()
                //: } rightBlock: {
            } rightBlock: {
                //: PushThen.share.func__jumpToWebRecharge(clickEvent: clickLess1minButton, sufficient: false)
                PushThen.share.twoParams(clickEvent: noti_systemName, sufficient: false)
            }
        }
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func shArr(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.dealSuccess(giftArr: giftArr)
    }

    /// 女性通话积分激励动效展示
    //: func socket_videoChat_onCallMotivation(data: [String: Any]) {
    func valueData(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: let addBean = json["addBean"].stringValue
        let addBean = json[(String(constUnknownStr.prefix(7)))].stringValue
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: interactionView.showPointAnimaData(point: addBean)
            interactionView.replacement(point: addBean)
            //: } else {
        } else {
            //: self.miniView?.showPointAnimaData(point: addBean)
            self.miniView?.showAbout(point: addBean)
        }
    }
}

// MARK: - MedalSolarObjectProtocol【接收方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension VisualCommunicationViewDelegate: MedalSolarObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func sumRelationData(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(showFailMsg.prefix(5)) + "as hung" + String(const_browData)).localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(dataHangPath.replacingOccurrences(of: "pro", with: "fu") + String(notiControlStrikeHeadMsg.prefix(3)))].stringValue == FenceSequence.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(appMainFormat) + showTotalroTrailStr.replacingOccurrences(of: "view", with: "u")).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.enablece(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.detectScreen()
            //: TalkingSocketManager.shared.currInfoVC = nil
            SantaLuciaFirSocketDelegate.shared.currInfoVC = nil
        }

        //: popCurrentViewController()
        indicatorMatch()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func information(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        conversationDisable(logId: json[String(bytes: show_familyContent.map{betweenSpread(filter: $0)}, encoding: .utf8)!].intValue)
    }
}

// MARK: - EnableceErrorDelegate【异常处理】

//: extension TalkingVideoChatViewController: TalkingSocketManagerErrorDelegate {
extension VisualCommunicationViewDelegate: EnableceErrorDelegate {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func assemblage(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (String(notiRefreshlyText.suffix(8)) + dataBasicObserverPath.replacingOccurrences(of: "them", with: "ll")) { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.enablece(showMsg: (String(notiKingdomUrl.prefix(4)) + "nswer").localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.detectScreen()
                //: TalkingSocketManager.shared.currInfoVC = nil
                SantaLuciaFirSocketDelegate.shared.currInfoVC = nil
            }
            //: popCurrentViewController()
            indicatorMatch()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(dataTipAtPath.suffix(7)) + String(userShoulderKey.prefix(5))) && errorNo == 101 { // 接听通话时，余额不足
            //: guard DismissReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard DismissReactiveCompatible.share.appStatus == AfterArtSumLiteral.normal.rawValue else { return }
            //: PushThen.share.func__jumpToWebRecharge(sufficient: false)
            PushThen.share.twoParams(sufficient: false)

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(dataTipAtPath.suffix(7)) + String(userShoulderKey.prefix(5))) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.enablece(showMsg: msg)
            //: popCurrentViewController()
            indicatorMatch()
        }
    }
}

// MARK: - HiddenObjectProtocol【通话按钮操作事件】

//: extension TalkingVideoChatViewController: Talking1v1VideoInteractionViewDelegate {
extension VisualCommunicationViewDelegate: HiddenObjectProtocol {
    /// 接听视频通话
    //: func interactionView_shouldAcceptCall() {
    func browsing() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        SantaLuciaFirSocketDelegate.shared.amOrigin(info: [String(bytes: notiContextMsg.map{signHim(menu: $0)}, encoding: .utf8)!: (String(mainBusyClearExceptionMessage)), String(bytes: mainDisableMerelySpringMsg.map{socialShot(swing: $0)}, encoding: .utf8)!: [String(bytes: user_hideData.map{blowWarm(theme: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func sumerplay() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.enablece(showMsg: (String(appRecordingId.prefix(6)) + "are canc" + noti_giveText.replacingOccurrences(of: "found", with: "e")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            SantaLuciaFirSocketDelegate.shared.amOrigin(info: [String(bytes: notiContextMsg.map{signHim(menu: $0)}, encoding: .utf8)!: (String(noti_thinName) + String(showResMsg)), String(bytes: mainDisableMerelySpringMsg.map{socialShot(swing: $0)}, encoding: .utf8)!: [String(bytes: show_familyContent.map{betweenSpread(filter: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.enablece(showMsg: (String(appRecordingId.prefix(6)) + "are canc" + noti_giveText.replacingOccurrences(of: "found", with: "e")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            SantaLuciaFirSocketDelegate.shared.amOrigin(info: [String(bytes: notiContextMsg.map{signHim(menu: $0)}, encoding: .utf8)!: (String(notiRefreshlyText.suffix(8)) + dataBasicObserverPath.replacingOccurrences(of: "them", with: "ll")), String(bytes: mainDisableMerelySpringMsg.map{socialShot(swing: $0)}, encoding: .utf8)!: [String(bytes: user_hideData.map{blowWarm(theme: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: if chatModel?.isRandomVCPush ?? false {
            if chatModel?.isRandomVCPush ?? false {
                //: TalkingAlertShow.alert(title: nil, message: "Continue matching with other users？".localized, leftBtnTitle: "No".localized, rightBtnTitle: "Yes".localized, leftBlock: {
                AllowanceReactiveCompatible.maximal(title: nil, message: String(bytes: dataDisappearStr.reversed(), encoding: .utf8)!.localized, leftBtnTitle: "No".localized, rightBtnTitle: (String(notiContactTitle)).localized, leftBlock: {
                    //: self.randowCallIsEndTool(isNewRandow: false)
                    self.sawLogSkip(isNewRandow: false)
                    //: }, rightBlock: {
                }, rightBlock: {
                    //: self.randowCallIsEndTool(isNewRandow: true)
                    self.sawLogSkip(isNewRandow: true)
                    //: })
                })
                //: return
                return
                    //: } else {
            } else {
                //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
                self.enablece(showMsg: (String(user_distributeName) + String(k_theFormat.prefix(6)) + "as ended").localized)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                SantaLuciaFirSocketDelegate.shared.amOrigin(info: [String(bytes: notiContextMsg.map{signHim(menu: $0)}, encoding: .utf8)!: (String(notiOwnerMsg) + String(main_supportWhatName)), String(bytes: mainDisableMerelySpringMsg.map{socialShot(swing: $0)}, encoding: .utf8)!: [String(bytes: show_familyContent.map{betweenSpread(filter: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            }
        }

        //: popCurrentViewController()
        indicatorMatch()
    }

    /// 随机匹配通话挂断时
    //: func randowCallIsEndTool(isNewRandow: Bool) {
    func sawLogSkip(isNewRandow: Bool) {
        //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
        self.enablece(showMsg: (String(user_distributeName) + String(k_theFormat.prefix(6)) + "as ended").localized)
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
        SantaLuciaFirSocketDelegate.shared.amOrigin(info: [String(bytes: notiContextMsg.map{signHim(menu: $0)}, encoding: .utf8)!: (String(notiOwnerMsg) + String(main_supportWhatName)), String(bytes: mainDisableMerelySpringMsg.map{socialShot(swing: $0)}, encoding: .utf8)!: [String(bytes: show_familyContent.map{betweenSpread(filter: $0)}, encoding: .utf8)!: self.chatModel?.logId]])

        //: if !isNewRandow{
        if !isNewRandow {
            //: if let targetVC = navigationController?.viewControllers.first(where: { $0 is RandomMatchVC }) {
            if let targetVC = navigationController?.viewControllers.first(where: { $0 is AdvertizingReactiveCompatible }) {
                //: navigationController?.viewControllers = [targetVC]
                navigationController?.viewControllers = [targetVC]
                //: navigationController?.popToViewController(targetVC, animated: false)
                navigationController?.popToViewController(targetVC, animated: false)
            }
            //: }else {
        } else {
            //: popCurrentViewController()
            indicatorMatch()
            //: NotificationCenter.default.post(name: MATCHVIDEOCALL_END_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: userWorkValue, object: nil)
        }
    }

    /// 旋转摄像头
    //: func interactionView_shouldChangeCamera(isFront: Bool) {
    func crunch(isFront: Bool) {
        //: self.videoManager.useFrontCamera(isFront)
        self.videoManager.stageCompute(isFront)
    }

    /// 开启/关闭摄像头
    //: func interactionView_shouldForbiddenCamera(forbidden: Bool) {
    func camera(forbidden: Bool) {
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved {
            //: self.bgVideoView.showCoverView(show: !forbidden)
            self.bgVideoView.tillCity(show: !forbidden)
        }
        //: self.videoManager.useCamera(!forbidden)
        self.videoManager.offdCannibalise(!forbidden)
        //: self.videoBG_refreshViewWithCameraStatus()
        self.statusRobot()
    }

    /// 展示视频通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func recital() {
        //: TalkingSocketManager.shared.currInfoVC = self
        SantaLuciaFirSocketDelegate.shared.currInfoVC = self
        //: self.miniView = TalkingVideoMiniView.buildVideoMiniView()
        self.miniView = SpecialEndMiniView.op()
        //: self.videoManager.smallRenderView = self.miniView?.renderView
        self.videoManager.smallRenderView = self.miniView?.renderView
        //: self.videoManager.enterSmallRenderMode()
        self.videoManager.inputShape()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.videoManager.exitSmallRenderMode()
            self.videoManager.brattice()
            //: self.isFromMiniView = true
            self.isFromMiniView = true
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.exitSchedule()?.navigationController?.pushViewController(self, animated: true)
        }
        //: popCurrentViewController()
        indicatorMatch()
    }

    /// 投诉成功
    //: func interactionView_reportSucceed() {
    func declaration() {
        //: if reportView?.superview != nil {
        if reportView?.superview != nil {
            //: reportView?.removeFromSuperview()
            reportView?.removeFromSuperview()
        }
    }
}

// MARK: - Layout

//: extension TalkingVideoChatViewController {
extension VisualCommunicationViewDelegate {
    /// 添加视图
    //: private func setupSubviews() {
    private func separateOut() {
        //: self.view.layer.masksToBounds = true
        self.view.layer.masksToBounds = true
        //: self.view.addSubview(bgVideoView)
        self.view.addSubview(bgVideoView)
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
        //: self.interactionView.addWindowView(view: self.windowView)
        self.interactionView.bindExecute(view: self.windowView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func setupFromConstraint() {
        //: bgVideoView.snp.makeConstraints { make in
        bgVideoView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
