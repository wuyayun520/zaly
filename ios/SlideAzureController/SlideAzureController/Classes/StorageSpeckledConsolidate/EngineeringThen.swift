
//: Declare String Begin

/*: "mfBean" :*/
fileprivate let user_compoundTakeId:[UInt8] = [0x71,0x7a,0x5e,0x79,0x7d,0x72]

private func roadPrepare(professional num: UInt8) -> UInt8 {
    return num ^ 28
}

/*: "bannerList" :*/
fileprivate let constIceUrl:[Character] = ["b","a","n","n","e","r","L"]
fileprivate let showEthicalData:[Character] = ["i","s","t"]

/*: "icon_me_chatsettings" :*/
fileprivate let k_thatElectronTitle:String = "empty adjustment extension before mattericon_m"
fileprivate let notiFormationPromptStr:String = "float provide via sume_ch"
fileprivate let user_physicalYearName:String = "heavygs"

/*: "icon_me_automatic" :*/
fileprivate let appPassMsg:String = "ask oficon_me"
fileprivate let k_edgeText:[Character] = ["_","a","u","t","o","m","a","t","i","c"]

/*: "icon_me_settings" :*/
fileprivate let show_authorityStr:[Character] = ["i","c","o","n","_","m","e","_","s"]
fileprivate let dataAnnouncementRetLeftPath:[Character] = ["e","t","t","i","n","g","s"]

/*: "icon_me_tc" :*/
fileprivate let noti_subjectMessage:[Character] = ["i","c","o","n","_","m","e","_","t","c"]

/*: "icon_me_videoSet" :*/
fileprivate let show_dateUrl:String = "icobecome"
fileprivate let mainAcrossText:[Character] = ["_","m","e","_","v","i","d","e","o","S","e","t"]

/*: "icon_me_bs" :*/
fileprivate let const_coldResortTitle:[Character] = ["i","c","o","n","_","m","e","_","b","s"]

/*: "Enter " :*/
fileprivate let userTuneName:String = "mobile stemEnter"
fileprivate let mainComeTitle:String = "meeting"

/*: "Settings" :*/
fileprivate let data_moreUrl:String = "Seresource bind object interaction blank"
fileprivate let mainLabelName:String = "ttiviewgs"

/*: " and open " :*/
fileprivate let user_tellStr:[Character] = [" ","a","n","d"," ","o"]
fileprivate let data_responseMessage:String = "pen result prompt"

/*: "Camera" :*/
fileprivate let appSoulUrl:String = "deadline success bone via roundCamer"
fileprivate let notiRobotConMsg:[Character] = ["a"]

/*: " permission to use this function normally" :*/
fileprivate let showBlackValue:[UInt8] = [0xff,0xaf,0xba,0xad,0xb2,0xb6,0xac,0xac,0xb6,0xb0,0xb1,0xff,0xab,0xb0,0xff,0xaa,0xac,0xba,0xff,0xab,0xb7,0xb6,0xac,0xff,0xb9,0xaa,0xb1,0xbc,0xab,0xb6,0xb0,0xb1,0xff,0xb1,0xb0,0xad,0xb2,0xbe,0xb3,0xb3,0xa6]

private func commonPerson(unknown num: UInt8) -> UInt8 {
    return num ^ 223
}

/*: "Cancel" :*/
fileprivate let userFemalePath:[Character] = ["C","a","n","c","e","l"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  EngineeringThen.swift
//  AbroadTalking
//
//  Created by young on 2023/4/25.
//

//: import UIKit
import UIKit

//: class TalkingMeViewController: TalkingBaseViewController {
class EngineeringThen: WeddingLovelyRecognizerDelegate {
    //: private var tupleData = [(CenterItemType, String)]()
    private var tupleData = [(TapPortionValueConvertible, String)]()
    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.doingTo()
        //: reloadLocalData()
        justInAccountingData()
        //: func__reqBanner()
        someEffect()
        //: setupSubviews()
        dataSetup()
        //: setupSubViewsConstraint()
        unnecessaryPair()

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(pushEdit),
                                               selector: #selector(formatDetect),
                                               //: name: PUSH_MEEDIT_NOTIFICATION,
                                               name: app_infoMessage,
                                               //: object: nil)
                                               object: nil)
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__requestUserInfo),
                                               selector: #selector(createer),
                                               //: name: USER_UPDATE_LOUNGEPLUSINFO_NOTIFICATION,
                                               name: mainWorkFormat,
                                               //: object: nil)
                                               object: nil)
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: func__requestUserInfo()
        createer()
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    }

    // MARK: - Lazy Load

    //: private lazy var tableView: UITableView = {
    private lazy var tableView: UITableView = {
        //: let table = UITableView(frame: .zero, style: .plain)
        let table = UITableView(frame: .zero, style: .plain)
        //: table.backgroundColor = .clear
        table.backgroundColor = .clear
        //: table.separatorStyle = .none
        table.separatorStyle = .none
        //: table.tableHeaderView = UIView()
        table.tableHeaderView = UIView()
        //: table.sectionHeaderHeight = 0
        table.sectionHeaderHeight = 0
        //: table.sectionFooterHeight = 0
        table.sectionFooterHeight = 0
        //: table.dataSource = self
        table.dataSource = self
        //: table.delegate = self
        table.delegate = self
        //: table.contentInset = UIEdgeInsets(top: 0, left: 0, bottom: 12, right: 0)
        table.contentInset = UIEdgeInsets(top: 0, left: 0, bottom: 12, right: 0)
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: table.contentInsetAdjustmentBehavior = .never
            table.contentInsetAdjustmentBehavior = .never
            //: if #available(iOS 15.0, *) {
            if #available(iOS 15.0, *) {
                //: UITableView.appearance().sectionHeaderTopPadding = 0
                UITableView.appearance().sectionHeaderTopPadding = 0
            }
            //: } else {
        } else {
            //: self.automaticallyAdjustsScrollViewInsets = false
            self.automaticallyAdjustsScrollViewInsets = false
        }
        //: table.register(UITableViewCell.self, forCellReuseIdentifier: UITableViewCell.className())
        table.register(UITableViewCell.self, forCellReuseIdentifier: UITableViewCell.className())
        //: table.register(TalkingMeTopCell.self, forCellReuseIdentifier: TalkingMeTopCell.className())
        table.register(CoverViewCell.self, forCellReuseIdentifier: CoverViewCell.className())
        //: table.register(TalkingMeBannerCell.self, forCellReuseIdentifier: TalkingMeBannerCell.className())
        table.register(EquityBannerCell.self, forCellReuseIdentifier: EquityBannerCell.className())
        //: table.register(TalkingMeTwoColumnsCell.self, forCellReuseIdentifier: TalkingMeTwoColumnsCell.className())
        table.register(MotivationView.self, forCellReuseIdentifier: MotivationView.className())
        //: table.register(TalkingMeThreeColumnsCell.self, forCellReuseIdentifier: TalkingMeThreeColumnsCell.className())
        table.register(ExecuteReactiveCompatible.self, forCellReuseIdentifier: ExecuteReactiveCompatible.className())
        //: table.register(TalkingUserCenterCell.self, forCellReuseIdentifier: TalkingUserCenterCell.className())
        table.register(WallLeaseCellView.self, forCellReuseIdentifier: WallLeaseCellView.className())
        //: table.addHeaderRefresh { [weak self] in
        table.contrastComplection { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.headerRefresh()
            self.factPrevious()
        }
        //: return table
        return table
        //: }()
    }()

    //: lazy var bannerListData: [SocialAdBannerModel] = {
    lazy var bannerListData: [PrimaryRedModelType] = //: return Array<PrimaryRedModelType>()
        .init()
    //: }()
}

// MARK: - Request

//: extension TalkingMeViewController {
extension EngineeringThen {
    /// 下拉刷新
    //: private func headerRefresh() {
    private func factPrevious() {
        //: tableView.mj_header?.beginRefreshing()
        tableView.mj_header?.beginRefreshing()
        //: func__requestUserInfo()
        createer()
    }

    /// 刷新个人资料接口
    //: @objc private func func__requestUserInfo() {
    @objc private func createer() {
        //: AppManagerRequest.func__requestUserInfo { succeed, result, errorModel in
        RevenueMovementManagerRequest.account { _, _, _ in
            //: self.reloadLocalData()
            self.justInAccountingData()
            //: self.tableView.endRefresh()
            self.tableView.combineIn()
            //: self.tableView.reloadData()
            self.tableView.reloadData()

            //: if TalkingLiveManager.shared().isLive {
            if ManagerPushListener.nowPanel().isLive {
                //: NotificationCenter.default.post(name: LIVE_USER_POINT_CHANGE, object: nil, userInfo: ["mfBean": 0.0])
                NotificationCenter.default.post(name: k_succeedUrl, object: nil, userInfo: [String(bytes: user_compoundTakeId.map{roadPrepare(professional: $0)}, encoding: .utf8)!: 0.0])
            }
        }
    }

    /// banner
    //: func func__reqBanner() {
    func someEffect() {
        //: SocialRequestManager.init().req_adBanner(position: 2) { [weak self] succeed, result, errorModel in
        WallRangeRequestManager().genderStat(position: 2) { [weak self] _, result, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard let result = result as? Dictionary<String, Any> else {
            guard let result = result as? [String: Any] else {
                //: return
                return
            }
            //: let array = JSON(result["bannerList"] ?? []).arrayValue
            let array = JSON(result[(String(constIceUrl) + String(showEthicalData))] ?? []).arrayValue
            //: self.bannerListData.removeAll()
            self.bannerListData.removeAll()
            //: for dict in array {
            for dict in array {
                //: if let model = SocialAdBannerModel.deserialize(from: dict.dictionaryObject) {
                if let model = PrimaryRedModelType.deserialize(from: dict.dictionaryObject) {
                    //: self.bannerListData.append(model)
                    self.bannerListData.append(model)
                }
            }
            //: self.tableView.reloadData()
            self.tableView.reloadData()
        }
    }

    /// 重载本地数据
    //: private func reloadLocalData() {
    private func justInAccountingData() {
        //: if DismissReactiveCompatible.share.loginUserMode.sex == Gender.female.rawValue && DismissReactiveCompatible.share.appStatus != AppSkinStatus.special.rawValue {
        if DismissReactiveCompatible.share.loginUserMode.sex == FenceSequence.female.rawValue, DismissReactiveCompatible.share.appStatus != AfterArtSumLiteral.special.rawValue { // 女性 && 非审核模式
            //: tupleData = [(.My_Chat_Settings, "icon_me_chatsettings"),
            tupleData = [(.My_Chat_Settings, (String(k_thatElectronTitle.suffix(6)) + String(notiFormationPromptStr.suffix(4)) + "atsett" + user_physicalYearName.replacingOccurrences(of: "heavy", with: "in"))),
                         //: (.My_Automatic, "icon_me_automatic"),
                         (.My_Automatic, (String(appPassMsg.suffix(7)) + String(k_edgeText))),
                         //: (.My_Settings, "icon_me_settings")]
                         (.My_Settings, (String(show_authorityStr) + String(dataAnnouncementRetLeftPath)))]

            //: } else {
        } else {
            //: tupleData = [(.My_Settings, "icon_me_settings")]
            tupleData = [(.My_Settings, (String(show_authorityStr) + String(dataAnnouncementRetLeftPath)))]
        }
        //: if DismissReactiveCompatible.share.appUserConfigMode.showTaskCenter {
        if DismissReactiveCompatible.share.appUserConfigMode.showTaskCenter {
            //: tupleData.insert((.Task_Center, "icon_me_tc"), at: 0)
            tupleData.insert((.Task_Center, (String(noti_subjectMessage))), at: 0)
        }
        //: if DismissReactiveCompatible.share.loginUserMode.sex == Gender.female.rawValue {
        if DismissReactiveCompatible.share.loginUserMode.sex == FenceSequence.female.rawValue {
            //: tupleData.insert((.My_VideoSetting, "icon_me_videoSet"), at: tupleData.count-1)
            tupleData.insert((.My_VideoSetting, (show_dateUrl.replacingOccurrences(of: "become", with: "n") + String(mainAcrossText))), at: tupleData.count - 1)
            //: } else {
        } else {
            //: if SenseTime_Use && tupleData.count > 0 {
            if main_dismissMessage, tupleData.count > 0 {
                //: tupleData.insert((.My_Beautify, "icon_me_bs"), at: tupleData.count-1)
                tupleData.insert((.My_Beautify, (String(const_coldResortTitle))), at: tupleData.count - 1)
            }
        }
    }
}

//: extension TalkingMeViewController {
extension EngineeringThen {
    //: @objc func pushEdit() {
    @objc func formatDetect() {
        //: let editvc = TalkingEditProfilesVC()
        let editvc = CutProfilesVc()
        //: self.navigationController?.pushViewController(editvc, animated: true)
        self.navigationController?.pushViewController(editvc, animated: true)
    }

    func getMessageStr() -> String {
        return (String(userTuneName.suffix(5)) + mainComeTitle.replacingOccurrences(of: "meeting", with: " ")) + "\"" + (String(data_moreUrl.prefix(2)) + mainLabelName.replacingOccurrences(of: "view", with: "n")) + "\"" + (String(user_tellStr) + String(data_responseMessage.prefix(4))) + "\"" + (String(appSoulUrl.suffix(5)) + String(notiRobotConMsg)) + "\"" + String(bytes: showBlackValue.map{commonPerson(unknown: $0)}, encoding: .utf8)!.localized
    }
    
    //: func judgeCameraAuthorization() {
    func putUp() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(false) { (isOpen: Bool) in
        SpecialPermissionTool.exitSmart(false) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: guard TalkingSocketManager.shared.isInfo == false else {
                guard SantaLuciaFirSocketDelegate.shared.isInfo == false else {
                    //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
                    self.enablece(showMsg: kLikeStr)
                    //: return
                    return
                }
                //: let vc = TalkingMyBeautyVC()
                let vc = PrimaryViewController()
                //: self.navigationController?.pushViewController(vc, animated: true)
                self.navigationController?.pushViewController(vc, animated: true)
                //: } else {
            } else {
                //: TalkingAlertShow.alert(title: nil, message: "Enter \"Settings\" and open \"Camera\" permission to use this function normally".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Settings".localized) {
                AllowanceReactiveCompatible.maximal(title: nil, message: self.getMessageStr(), leftBtnTitle: (String(userFemalePath)).localized, rightBtnTitle: (String(data_moreUrl.prefix(2)) + mainLabelName.replacingOccurrences(of: "view", with: "n")).localized) {
                    //: TalkingAlertShow.hideAlert()
                    AllowanceReactiveCompatible.minuteOriginBridge()
                    //: } rightBlock: {
                } rightBlock: {
                    //: TalkingAlertShow.hideAlert()
                    AllowanceReactiveCompatible.minuteOriginBridge()
                    //: let url = URL(string: UIApplication.openSettingsURLString)
                    let url = URL(string: UIApplication.openSettingsURLString)
                    //: if  UIApplication.shared.canOpenURL(url!) {
                    if UIApplication.shared.canOpenURL(url!) {
                        //: if #available(iOS 10, *) {
                        if #available(iOS 10, *) {
                            //: UIApplication.shared.open(url!, options: [:], completionHandler: {(success) in})
                            UIApplication.shared.open(url!, options: [:], completionHandler: { _ in })
                            //: } else {
                        } else {
                            //: UIApplication.shared.openURL(url!)
                            UIApplication.shared.openURL(url!)
                        }
                    }
                }
            }
        }
    }
}

// MARK: - UITableViewDelegate, UITableViewDataSource

//: extension TalkingMeViewController: UITableViewDelegate, UITableViewDataSource {
extension EngineeringThen: UITableViewDelegate, UITableViewDataSource {
    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: return 2
        return 2
    }

    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection section: Int) -> Int {
        //: if section == 0 {
        if section == 0 {
            //: return 4
            return 4
        }
        //: return tupleData.count
        return tupleData.count
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: guard indexPath.section == 0 else {
        guard indexPath.section == 0 else {
            //: let cell = tableView.dequeueReusableCell(withIdentifier: TalkingUserCenterCell.className(), for: indexPath) as! TalkingUserCenterCell
            let cell = tableView.dequeueReusableCell(withIdentifier: WallLeaseCellView.className(), for: indexPath) as! WallLeaseCellView
            //: let data = tupleData[indexPath.row]
            let data = tupleData[indexPath.row]
            //: let isLast = tupleData.count == (indexPath.row+1)
            let isLast = tupleData.count == (indexPath.row + 1)
            //: cell.setViewData(title: data.0.rawValue, icon: data.1, row: indexPath.row, isLast: isLast)
            cell.endReasonLast(title: data.0.rawValue, icon: data.1, row: indexPath.row, isLast: isLast)
            //: if data.0 ==  .Task_Center {
            if data.0 == .Task_Center {
                //: cell.setTaskCenterBtn()
                cell.supersedure()
            }
            //: return cell
            return cell
        }

        //: switch(indexPath.row) {
        switch indexPath.row {
        //: case 0:
        case 0:
            //: let cell = tableView.dequeueReusableCell(withIdentifier: TalkingMeTopCell.className(), for: indexPath) as! TalkingMeTopCell
            let cell = tableView.dequeueReusableCell(withIdentifier: CoverViewCell.className(), for: indexPath) as! CoverViewCell
            //: cell.setViewData()
            cell.tingData()
            //: return cell
            return cell
        //: case 1:
        case 1:
            //: let cell = tableView.dequeueReusableCell(withIdentifier: TalkingMeBannerCell.className(), for: indexPath) as! TalkingMeBannerCell
            let cell = tableView.dequeueReusableCell(withIdentifier: EquityBannerCell.className(), for: indexPath) as! EquityBannerCell
            //: cell.setViewData(bannerData: self.bannerListData)
            cell.instalSet(bannerData: self.bannerListData)
            //: return cell
            return cell
        //: case 2:
        case 2:
            //: let cell = tableView.dequeueReusableCell(withIdentifier: TalkingMeTwoColumnsCell.className(), for: indexPath) as! TalkingMeTwoColumnsCell
            let cell = tableView.dequeueReusableCell(withIdentifier: MotivationView.className(), for: indexPath) as! MotivationView
            //: cell.setViewData()
            cell.nightRank()
            //: return cell
            return cell
        //: case 3:
        case 3:
            //: let cell = tableView.dequeueReusableCell(withIdentifier: TalkingMeThreeColumnsCell.className(), for: indexPath) as! TalkingMeThreeColumnsCell
            let cell = tableView.dequeueReusableCell(withIdentifier: ExecuteReactiveCompatible.className(), for: indexPath) as! ExecuteReactiveCompatible
            //: cell.setViewData()
            cell.displaceOrAccumulation()
            //: return cell
            return cell

        //: default:
        default:
            //: break
            break
        }

        //: let cell = tableView.dequeueReusableCell(withIdentifier: UITableViewCell.className(), for: indexPath)
        let cell = tableView.dequeueReusableCell(withIdentifier: UITableViewCell.className(), for: indexPath)
        //: cell.backgroundColor = .clear
        cell.backgroundColor = .clear
        //: cell.selectionStyle = .none
        cell.selectionStyle = .none
        //: return cell
        return cell
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
        //: if indexPath.section == 0 && indexPath.row == 1 && self.bannerListData.count <= 0 {
        if indexPath.section == 0 && indexPath.row == 1 && self.bannerListData.count <= 0 {
            //: return 0
            return 0
        }
        //: return UITableView.automaticDimension
        return UITableView.automaticDimension
    }

    //: func tableView(_ tableView: UITableView, estimatedHeightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, estimatedHeightForRowAt _: IndexPath) -> CGFloat {
        //: return 80
        return 80
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_: UITableView, didSelectRowAt indexPath: IndexPath) {
        //: guard indexPath.section == 1 else { return }
        guard indexPath.section == 1 else { return }
        //: let data = tupleData[indexPath.row]
        let data = tupleData[indexPath.row]
        //: switch data.0 {
        switch data.0 {
        //: case .My_Chat_Settings:
        case .My_Chat_Settings:
            //: let vc = TalkingFemalePriceSetVC()
            let vc = AdvancedActualViewDelegate()
            //: navigationController?.pushViewController(vc, animated: true)
            navigationController?.pushViewController(vc, animated: true)

        //: case .My_Automatic:
        case .My_Automatic:
            //: let vc = TalkingAutoGreetSettingVC()
            let vc = EagerDataSource()
            //: navigationController?.pushViewController(vc, animated: true)
            navigationController?.pushViewController(vc, animated: true)

        //: case .My_Settings:
        case .My_Settings:
            //: let vc = TalkingSettingsVC()
            let vc = PortrayalViewController()
            //: navigationController?.pushViewController(vc, animated: true)
            navigationController?.pushViewController(vc, animated: true)

        //: case .Task_Center:
        case .Task_Center:
            //: PushThen.share.func__pushToWebVC(webViewType: .TaskCenter)
            PushThen.share.plumage(webViewType: .TaskCenter)

        //: case .My_Beautify:
        case .My_Beautify:
            //: judgeCameraAuthorization()
            putUp()

        //: case .My_VideoSetting:
        case .My_VideoSetting:
            //: let vc = TalkingVideoSettingsVC.init()
            let vc = SettingsViewDelegate()
            //: navigationController?.pushViewController(vc, animated: true)
            navigationController?.pushViewController(vc, animated: true)

        //: default: break
        default: break
        }
    }
}

// MARK: - Layout

//: extension TalkingMeViewController {
extension EngineeringThen {
    /// 添加视图
    //: private func setupSubviews() {
    private func dataSetup() {
        //: self.view.addSubview(tableView)
        self.view.addSubview(tableView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func unnecessaryPair() {
        //: tableView.snp.makeConstraints { make in
        tableView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
