
//: Declare String Begin

/*: "WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION" :*/
fileprivate let show_accuracyPath:[UInt8] = [0x82,0x90,0x97,0x83,0x9c,0x90,0x82,0x8a,0x81,0x9d,0x9c,0x87,0x91,0x8a,0x87,0x90,0x93,0x87,0x90,0x86,0x9d,0x96,0x9a,0x9c,0x9b,0x8a,0x9b,0x9a,0x81,0x9c,0x93,0x9c,0x96,0x94,0x81,0x9c,0x9a,0x9b]

private func uponEh(memory num: UInt8) -> UInt8 {
    return num ^ 213
}

/*: "goodsId" :*/
fileprivate let main_migrationKey:String = "keepods"
fileprivate let user_hisText:String = "information question dragId"

/*: "source" :*/
fileprivate let kCancelHappyMessage:String = "sourspeaker"

/*: "type" :*/
fileprivate let appBrushPath:[Character] = ["t","y","p","e"]

/*: "title" :*/
fileprivate let mainJoinUrl:[Character] = ["t","i","t","l","e"]

/*: "url" :*/
fileprivate let kWindId:String = "dotrl"

/*: "money" :*/
fileprivate let user_heavyTitle:[Character] = ["m","o","n","e","y"]

/*: "Other" :*/
fileprivate let notiVisibleArrowPath:String = "Otherta warn play end push"

/*: "scene" :*/
fileprivate let data_recommendUrl:[Character] = ["s","c","e","n","e"]

/*: "show" :*/
fileprivate let constParkName:String = "shchannelw"

/*: "target" :*/
fileprivate let notiUpperFormat:String = "inner"
fileprivate let appAlongMessage:[Character] = ["a","r","g","e","t"]

/*: "eventName" :*/
fileprivate let main_theStr:[Character] = ["e","v","e","n","t","N","a","m","e"]

/*: "jsonString" :*/
fileprivate let showBraceVidData:String = "JS"
fileprivate let noti_statisticalSeveralName:[Character] = ["o","n","S","t","r","i","n","g"]

/*: "coin" :*/
fileprivate let main_waitName:[Character] = ["c","o","i","n"]

/*: "uid" :*/
fileprivate let dataPanId:[Character] = ["u","i","d"]

/*: "closeWeb" :*/
fileprivate let mainRunnerMessage:String = "cclings"

/*: "未实现的js事件： :*/
fileprivate let user_ofStr:String = "未\u{5b9e}现的j"

/*: "PurchaseClick" :*/
fileprivate let dataColumnKey:String = "Purcharegister inside capital serve choose"
fileprivate let kEliteUrl:String = "crestriction"

/*: "Retry After or Go to " :*/
fileprivate let const_somebodyTitle:[Character] = ["R","e","t","r","y"," ","A","f","t","e","r"," ","o","r"," ","G","o"," ","t","o"," "]

/*: "Feedback" :*/
fileprivate let user_progressTitle:String = "span natural several withoutFeed"

/*: " to contact us" :*/
fileprivate let showDetectProcedureMsg:[Character] = [" ","t","o"," ","c","o","n","t"]
fileprivate let showServePathMessage:String = "express handle speed menu worldact us"

/*: "Apple" :*/
fileprivate let user_nosePath:[Character] = ["A","p","p","l","e"]

/*: " apple支付充值失败： :*/
fileprivate let mainScheduleMsg:[Character] = [""," ","a","p","p"]
fileprivate let dataParentValue:String = "le支付充output language refuse finish need"

/*: "payResultCallback();" :*/
fileprivate let main_seedValue:[Character] = ["p","a","y","R","e","s","u","l","t","C","a","l","l","b","a","c","k"]
fileprivate let app_waterMessage:String = "();"

/*: "USD" :*/
fileprivate let appColumnData:String = "usd"

/*: "amount" :*/
fileprivate let show_beNagFormat:[UInt8] = [0x74,0x6e,0x75,0x6f,0x6d,0x61]

/*: "getSystemNotificationtStatus(true)" :*/
fileprivate let const_qualityStr:[UInt8] = [0x2f,0x2d,0x3c,0x1b,0x31,0x3b,0x3c,0x2d,0x25,0x6,0x27,0x3c,0x21,0x2e,0x21,0x2b,0x29,0x3c,0x21,0x27,0x26,0x3c,0x1b,0x3c,0x29,0x3c,0x3d,0x3b,0x60,0x3c,0x3a,0x3d,0x2d,0x61]

private func programCapacity(income num: UInt8) -> UInt8 {
    return num ^ 72
}

/*: "getSystemNotificationtStatus(false)" :*/
fileprivate let kContrastStr:[UInt8] = [0xbc,0xba,0xc9,0xa8,0xce,0xc8,0xc9,0xba,0xc2,0xa3,0xc4,0xc9,0xbe,0xbb,0xbe,0xb8,0xb6,0xc9,0xbe,0xc4,0xc3,0xc9,0xa8,0xc9,0xb6,0xc9,0xca,0xc8,0x7d,0xbb,0xb6,0xc1,0xc8,0xba,0x7e]

fileprivate func resignDisappear(sweep num: UInt8) -> UInt8 {
    let value = Int(num) - 85
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "mfBean" :*/
fileprivate let show_gradeMessage:[UInt8] = [0x12,0xb,0xe7,0xa,0x6,0x13]

fileprivate func padSignOutline(drown num: UInt8) -> UInt8 {
    let value = Int(num) + 91
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  PrimaryDelegate+JSEvent.swift
//  AbroadTalking
//
//  Created by young on 2022/9/6.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

// 通知三方H5刷新金币
//: public let WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION = NSNotification.Name(rawValue: "WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION")
public let mainNetUrl = NSNotification.Name(rawValue: String(bytes: show_accuracyPath.map{uponEh(memory: $0)}, encoding: .utf8)!)

//: extension TalkingWebViewController {
extension PrimaryDelegate {
    /// 处理JS事件
    /// - Parameters:
    ///   - message: 消息
    //: func HandleScriptMessage(_ message: WKScriptMessage) {
    func anyReward(_ message: WKScriptMessage) {
        //: let type = WebMethodType(rawValue: message.name)
        let type = DismissComparable(rawValue: message.name)
        //: let json = JSON(message.body)
        let json = JSON(message.body)
        //: switch type {
        switch type {
        //: case .MethodType_Exit:
        case .MethodType_Exit:
            //: self.func__closeAction(sender: nil)
            self.fieldSender(sender: nil)

        //: case .MethodType_needLogin:
        case .MethodType_needLogin:
            //: needLogin()
            missAcross()

        //: case .MethodType_apPay:
        case .MethodType_apPay:
            //: applePay(productId: json["goodsId"].stringValue, source: json["source"].intValue, payType: .Pay)
            vow(productId: json[(main_migrationKey.replacingOccurrences(of: "keep", with: "go") + String(user_hisText.suffix(2)))].stringValue, source: json[(kCancelHappyMessage.replacingOccurrences(of: "speaker", with: "ce"))].intValue, payType: .Pay)

        //: case .MethodType_apPay_subscribe:
        case .MethodType_apPay_subscribe:
            //: if json["type"].stringValue == "6" {
            if json[(String(appBrushPath))].stringValue == "6" {
                //: isVideoCallRechargeHalfPage = true
                isVideoCallRechargeHalfPage = true
                //: RECHARGE_CLICK_EVENT = subscribtionRecordButton
                k_buttonMinPath = const_sinceValue
                //: } else if json["type"].stringValue == "7" { // 点击订阅弹窗的订阅按钮
            } else if json[(String(appBrushPath))].stringValue == "7" { // 点击订阅弹窗的订阅按钮
                //: RECHARGE_CLICK_EVENT = subscribtionRecordButton
                k_buttonMinPath = const_sinceValue
            }
            //: applePay_subscribe(type: json["type"].stringValue, productId: json["goodsId"].stringValue, payType: .Subscribe)
            strengthType(type: json[(String(appBrushPath))].stringValue, productId: json[(main_migrationKey.replacingOccurrences(of: "keep", with: "go") + String(user_hisText.suffix(2)))].stringValue, payType: .Subscribe)

        //: case .MethodType_changeMenuButton:
        case .MethodType_changeMenuButton:
            //: changeRightMenuButton(title: json["title"].stringValue, url: json["url"].stringValue)
            spendError(title: json[(String(mainJoinUrl))].stringValue, url: json[(kWindId.replacingOccurrences(of: "dot", with: "u"))].stringValue)

        //: case .MethodType_refreshCoin:
        case .MethodType_refreshCoin:
            //: refreshCoin(price: Double(json["money"].stringValue) ?? 0, payMode: "Other", scene: json["scene"].stringValue)
            marketingCostSegment(price: Double(json[(String(user_heavyTitle))].stringValue) ?? 0, payMode: (String(notiVisibleArrowPath.prefix(5))), scene: json[(String(data_recommendUrl))].stringValue)

        //: case .MethodType_appearsOnGiftBroadcast:
        case .MethodType_appearsOnGiftBroadcast:
            //: appearsOnGiftBroadcast(show: json["show"].boolValue)
            remoteCompareShow(show: json[(constParkName.replacingOccurrences(of: "channel", with: "o"))].boolValue)

        //: case.MethodType_ToUrl:
        case .MethodType_ToUrl:
            //: openTourl(url: json.rawString() ?? "")
            misconduct(url: json.rawString() ?? "")

        //: case .MethodType_customerService:
        case .MethodType_customerService:
            //: PushThen.share.func__pushToPriveteChatVC(chatID: DepictionThen.getCustomerServiceID())
            PushThen.share.resolutionBegin(chatID: DepictionThen.noteFamily())

        //: case.setRightMenuButton:
        case .setRightMenuButton:
            //: break
            break

        //: case .MethodType_taskCenter:
        case .MethodType_taskCenter:
            //: let target = json["target"].intValue
            let target = json[(notiUpperFormat.replacingOccurrences(of: "inner", with: "t") + String(appAlongMessage))].intValue
            //: self.reloadInputViews()
            self.reloadInputViews()
            //: switch target {
            switch target {
            //: case 1:
            case 1: /// 签到领金币页
                //: PushThen.share.func__pushToSubscribePageWebVC()
                PushThen.share.forceBack()

            //: case 2:
            case 2: /// 个人信息编辑页面
                //: let vc = TalkingEditProfilesVC.init()
                let vc = CutProfilesVc()
                //: self.navigationController?.pushViewController(vc, animated: true)
                self.navigationController?.pushViewController(vc, animated: true)

            //: case 3:
            case 3: /// 真人认证页面
                //: verifyBtnClick()
                doingStageClick()
            //: case 4:
            case 4: /// 发布动态页面
                //: tabberSelete(type: TabBarItemType.Moment.rawValue)
                versionNor(type: TypeYaNumberervalLiteral.Moment.rawValue)
                /// 防止动态首页广播还没创建
                //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                    //: NotificationCenter.default.post(name: FREEBTN_UPLOAD_POST_NOTIFICATION,
                    NotificationCenter.default.post(name: mainEnvironmentMessage,
                                                    //: object: nil,
                                                    object: nil,
                                                    //: userInfo: nil)
                                                    userInfo: nil)
                }
            //: case 5:
            case 5: /// 我的页面
                //: tabberSelete(type: TabBarItemType.Account.rawValue)
                versionNor(type: TypeYaNumberervalLiteral.Account.rawValue)
            //: case 6:
            case 6: /// 首页
                //: tabberSelete(type: TabBarItemType.Social.rawValue)
                versionNor(type: TypeYaNumberervalLiteral.Social.rawValue)
            //: case 7:
            case 7: /// 消息列表页
                //: tabberSelete(type: TabBarItemType.Message.rawValue)
                versionNor(type: TypeYaNumberervalLiteral.Message.rawValue)
            //: case 8:
            case 8: /// Greet setting页
                //: let vc = TalkingAutoGreetSettingVC()
                let vc = EagerDataSource()
                //: navigationController?.pushViewController(vc, animated: true)
                navigationController?.pushViewController(vc, animated: true)
            //: case 9:
            case 9: /// 动态列表页
                //: tabberSelete(type: TabBarItemType.Moment.rawValue)
                versionNor(type: TypeYaNumberervalLiteral.Moment.rawValue)
            //: default:
            default:
                //: break
                break
            }
        //: case .MethodType_systemNotification:
        case .MethodType_systemNotification:
            //: let type = json["type"].intValue
            let type = json[(String(appBrushPath))].intValue
            //: changeNotifaStatus(type: type)
            circleOn(type: type)

        //: case .MethodType_refreshPoint:
        case .MethodType_refreshPoint:
            //: refreshPoint()
            pointBy()

        //: case .MethodType_onEvent:
        case .MethodType_onEvent:
            //: reportEvent(eventName: json["eventName"].stringValue, jsonStr: json["jsonString"].stringValue)
            atomicMagnitude54(eventName: json[(String(main_theStr))].stringValue, jsonStr: json[(showBraceVidData.lowercased() + String(noti_statisticalSeveralName))].stringValue)

        //: case .MethodType_refreshCoinAll:
        case .MethodType_refreshCoinAll:
            //: req_refreshCoinAll(coin: json["coin"].stringValue)
            characterTo(coin: json[(String(main_waitName))].stringValue)

        //: case .MethodType_refreshPackage:
        case .MethodType_refreshPackage:
            //: req_refreshPackage()
            installPackage()

        //: case .MethodType_vibrate:
        case .MethodType_vibrate:
            //: phoneVibrate()
            whenVibrate()

        //: case .MethodType_goFreeVideo:
        case .MethodType_goFreeVideo:
            //: AppManagerRequest.func__requestUserInfo { succeed, result, errorModel in
            RevenueMovementManagerRequest.account { _, _, _ in
                //: if DismissReactiveCompatible.share.loginUserMode.jumpType == 1 {
                if DismissReactiveCompatible.share.loginUserMode.jumpType == 1 {
                    //: self.func__closeAction(sender: nil)
                    self.fieldSender(sender: nil)
                    //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                    DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                        //: PushThen.share.func__pushToRandomVideoVC(isBeginRand: false)
                        PushThen.share.popOutClose(isBeginRand: false)
                    }
                }
            }

        //: case .MethodType_goMessageDetail:
        case .MethodType_goMessageDetail:
            //: PushThen.share.func__pushToPriveteChatVC(chatID: json["uid"].stringValue)
            PushThen.share.resolutionBegin(chatID: json[(String(dataPanId))].stringValue)

        //: case .MethodType_goPersonInfo:
        case .MethodType_goPersonInfo:
            //: PushThen.share.func__pushToUserDetailVC(uid: json["uid"].stringValue)
            PushThen.share.sessionSomebody(uid: json[(String(dataPanId))].stringValue)

        //: case .MethodType_showRechargeDialog:
        case .MethodType_showRechargeDialog:
            //: PushThen.share.func__pushToHalfWebVC(webViewType: .RechargeHalfPage)
            PushThen.share.obligatoEvent(webViewType: .RechargeHalfPage)

        //: case .OpenRetainPopup:
        case .OpenRetainPopup:
            //: let view = TalkingRechargeDetentionView.init()
            let view = FederativeRepublicOfBrazilReactiveCompatible()
            //: view.dtype = json["type"].intValue
            view.dtype = json[(String(appBrushPath))].intValue
            //: view.closeWeb = json["closeWeb"].boolValue
            view.closeWeb = json[(mainRunnerMessage.replacingOccurrences(of: "cling", with: "lo") + "eWeb")].boolValue
            //: view.show()
            view.mergeDelete()
            //: view.closWebClock = { [weak self] in
            view.closWebClock = { [weak self] in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: self.func__closeAction(sender: nil)
                self.fieldSender(sender: nil)
            }

        //: case .openBrowserWithUrl:
        case .openBrowserWithUrl:
            //: openToOutUrl(url: json.rawString() ?? "")
            bookseller(url: json.rawString() ?? "")
        //: case .none:
        case .none:
            //: printLog(message: "未实现的js事件：\(message.name)")
            printLog(message: (user_ofStr + "s事件：") + "\(message.name)")
        }
    }

    // MARK: - Event

    /// 苹果订阅
    /// - Parameters:
    ///   - type: 订阅入口；1：首页banner，2：全屏充值页，3：半屏充值页，4：领取金币弹窗
    ///   - productId: 商品Id
    ///   - payType: 支付类型
    //: private func applePay_subscribe(type: String, productId: String, payType: ApplePayType) {
    private func strengthType(type _: String, productId: String, payType: DismissType) {
        //: applePay(productId: productId, payType: payType)
        vow(productId: productId, payType: payType)
    }

    /// 苹果支付/订阅
    /// - Parameters:
    ///   - productId: productId: 商品Id
    ///   - source: 充值来源
    //: private func applePay(productId: String, source: Int = -1, payType: ApplePayType) {
    private func vow(productId: String, source: Int = -1, payType: DismissType) {
        //: if DismissReactiveCompatible.share.loginUid.isEmptyString {
        if DismissReactiveCompatible.share.loginUid.isEmptyString {
            //: return
            return
        }
        //: FirebaseAnalyticsManager.share.logEvent(name: "PurchaseClick")
        LeaseTrailerThen.share.tupeloShowPlay(name: (String(dataColumnKey.prefix(6)) + "seCli" + kEliteUrl.replacingOccurrences(of: "restriction", with: "k")))

        //: ProgressHUD.show()
        InsertProgressHUD.momentShow()
        //: var index = isVideoCallRechargeHalfPage ? 1: 0
        var index = isVideoCallRechargeHalfPage ? 1 : 0
        //: if source != -1 {
        if source != -1 {
            //: index = source
            index = source
        }
        //: AppleIAPManager.shared.iap_startPurchase(productId: productId, payType: payType, source: index) { status, reportMoney, payType in
        TermsRequestDelegate.shared.sourceHandle(productId: productId, payType: payType, source: index) { status, reportMoney, payType in
            //: ProgressHUD.dismiss()
            InsertProgressHUD.detectDismiss()
            //: DispatchQueue.main.async { [self] in
            DispatchQueue.main.async { [self] in
                //: switch status {
                switch status {
                //: case .verityFail:
                case .verityFail:
                    //: ProgressHUD.toast( "Retry After or Go to \"Feedback\" to contact us".localized)
                    InsertProgressHUD.overDeadline((String(const_somebodyTitle)) + "\"" + (String(user_progressTitle.suffix(4)) + "back") + "\"" + (String(showDetectProcedureMsg) + String(showServePathMessage.suffix(6))).localized)
                //: case .veritySucceed:
                case .veritySucceed:
                    //: appleIAPVeritySucceed(price: reportMoney, payType: payType)
                    originalType(price: reportMoney, payType: payType)
                //: case .renewSucceed:
                case .renewSucceed:
                    //: let scene = (payType == .Subscribe) ? "1":"2"
                    let scene = (payType == .Subscribe) ? "1" : "2"
                    //: refreshCoin(price: reportMoney, payMode: "Apple", scene: scene)
                    marketingCostSegment(price: reportMoney, payMode: (String(user_nosePath)), scene: scene)
                //: default:
                default:
                    //: printLog(message: " apple支付充值失败：\(status.rawValue)")
                    printLog(message: (String(mainScheduleMsg) + String(dataParentValue.prefix(5)) + "值\u{5931}败：") + "\(status.rawValue)")
                }
            }
        }
    }

    /// 设置导航右侧按钮
    /// - Parameters:
    ///   - title: 标题
    ///   - url: 地址
    //: private func changeRightMenuButton(title: String, url: String) {
    private func spendError(title: String, url: String) {
        //: let rightBtn = TalkingButton()
        let rightBtn = RangeReactiveCompatible()
        //: rightBtn.titleLabel?.font = .systemFont(ofSize: 14)
        rightBtn.titleLabel?.font = .systemFont(ofSize: 14)
        //: rightBtn.setTitle(title, for: .normal)
        rightBtn.setTitle(title, for: .normal)
        //: rightBtn.setTitleColor(.RGBA(51, 51, 51, 1), for: .normal)
        rightBtn.setTitleColor(.searched(51, 51, 51, 1), for: .normal)
        //: rightBtn.addTarget(self, action: #selector(rightButtonClick), for: .touchUpInside)
        rightBtn.addTarget(self, action: #selector(signRight), for: .touchUpInside)
        //: rightBtn.sizeToFit()
        rightBtn.sizeToFit()
        //: rightBtn.userInfo = ["url": url]
        rightBtn.userInfo = [(kWindId.replacingOccurrences(of: "dot", with: "u")): url]
        //: self.navigationItem.rightBarButtonItem = UIBarButtonItem(customView: rightBtn)
        self.navigationItem.rightBarButtonItem = UIBarButtonItem(customView: rightBtn)
    }

    //: @objc func rightButtonClick(button: TalkingButton) {
    @objc func signRight(button: RangeReactiveCompatible) {
        //: guard let urlString = button.userInfo?["url"] as? String else {
        guard let urlString = button.userInfo?[(kWindId.replacingOccurrences(of: "dot", with: "u"))] as? String else {
            //: return
            return
        }

        //: self.urlStr = urlString
        self.urlStr = urlString
        //: func__startRequest()
        itRequest()
    }

    /// 退出登录
    //: private func needLogin() {
    private func missAcross() {
        //: guard Int(DismissReactiveCompatible.share.loginUid) ?? 0 > 0 else {
        guard Int(DismissReactiveCompatible.share.loginUid) ?? 0 > 0 else {
            //: self.webView.reload()
            self.webView.reload()
            //: return
            return
        }

        //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION,
        NotificationCenter.default.post(name: kOnValue,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: nil)
                                        userInfo: nil)
    }

    /// 苹果 支付/订阅 成功验证通过
    /// - Parameters:
    ///   - price: 充值金额
    ///   - payType: 支付类型
    //: func appleIAPVeritySucceed(price: Double, payType: ApplePayType) {
    func originalType(price: Double, payType: DismissType) {
        //: self.webView.evaluateJavaScript("payResultCallback();") { data, error in
        self.webView.evaluateJavaScript((String(main_seedValue) + app_waterMessage.capitalized)) { _, _ in
        }
        //: let scene = (payType == .Subscribe) ? "1":"2"
        let scene = (payType == .Subscribe) ? "1" : "2"
        //: refreshCoin(price: price, payMode: "Apple", scene: scene)
        marketingCostSegment(price: price, payMode: (String(user_nosePath)), scene: scene)
    }

    /// 支付/订阅 成功刷新个人信息【Apple/Other】
    /// - Parameters:
    ///   - price: 充值金额
    ///   - payMode: 类型
    ///   - scene: 1：订阅，2：内购
    //: private func refreshCoin(price: Double, payMode: String, scene: String) {
    private func marketingCostSegment(price: Double, payMode: String, scene: String) {
        //: NotificationCenter.default.post(name: UPDATE_USER_GETMYINFO_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: data_versionKey, object: nil)
        //: NotificationCenter.default.post(name: UPDATE_USER_GETINFOCOLUMN_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: mainBlockName, object: nil)
        //: NotificationCenter.default.post(name: REFRESH_INDEX_GETCONFIG_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: appProductionId, object: nil)
        //: NotificationCenter.default.post(name: USER_UPDATE_LOUNGEPLUSINFO_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: mainWorkFormat, object: nil)
        // 成功埋点
        //: if scene == "1" {
        if scene == "1" {
            //: TalkingAdjustManager.share.addSubscribeEvent(price: price, currency: "USD")
            ProgMedalReactiveCompatible.share.con(price: price, currency: (appColumnData.uppercased()))
            //: } else {
        } else {
            //: TalkingAdjustManager.share.addPurchasedEvent(price: price, currency: "USD")
            ProgMedalReactiveCompatible.share.narrowFresh(price: price, currency: (appColumnData.uppercased()))
        }
        //: FirebaseAnalyticsManager.share.trackPurchase(payType: payMode, price: price, currency: "USD")
        LeaseTrailerThen.share.successCurrency(payType: payMode, price: price, currency: (appColumnData.uppercased()))

        // 半屏充值成功埋点
        //: if RECHARGE_CLICK_EVENT.count > 0 {
        if k_buttonMinPath.count > 0 {
            //: uploadRecord.uploadRecordEvent(eventID: RECHARGE_CLICK_EVENT, parameterStr: ["amount": price])
            const_pathMsg.launchStr(eventID: k_buttonMinPath, parameterStr: [String(bytes: show_beNagFormat.reversed(), encoding: .utf8)!: price])
        }

        // 支付成功发送通知，调用三方js事件
        //: NotificationCenter.default.post(name: WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: mainNetUrl, object: nil)
    }

    /// 广播设置
    /// - Parameter show: 是否上广播
    //: private func appearsOnGiftBroadcast(show: Bool) {
    private func remoteCompareShow(show _: Bool) {}

    //: private func verifyBtnClick() {
    private func doingStageClick() {
        //: PushThen.share.func__pushUserVerifyController(toast: nil)
        PushThen.share.undercoverOperation(toast: nil)
    }

    /// 底部tabbar切换
    //: private func tabberSelete(type: Int) {
    private func versionNor(type: Int) {
        //: guard type < self.tabBarController?.viewControllers?.count ?? 0 else { return }
        guard type < self.tabBarController?.viewControllers?.count ?? 0 else { return }
        //: let vc = self.tabBarController?.viewControllers?[type]
        let vc = self.tabBarController?.viewControllers?[type]
        //: self.tabBarController?.tabBar.isHidden = false
        self.tabBarController?.tabBar.isHidden = false
        //: self.tabBarController?.selectedIndex = type
        self.tabBarController?.selectedIndex = type
        //: self.tabBarController?.selectedViewController = vc
        self.tabBarController?.selectedViewController = vc
        //: self.navigationController?.popViewController(animated: true)
        self.navigationController?.popViewController(animated: true)
    }

    /// 获取系统通知状态
    //: private func changeNotifaStatus(type: Int) {
    private func circleOn(type: Int) {
        //: if type == 0 {
        if type == 0 {
            //: if let url = URL(string: UIApplication.openSettingsURLString), UIApplication.shared.canOpenURL(url) {
            if let url = URL(string: UIApplication.openSettingsURLString), UIApplication.shared.canOpenURL(url) {
                //: if #available(iOS 10.0, *) {
                if #available(iOS 10.0, *) {
                    //: UIApplication.shared.open(url, options: [:], completionHandler: nil)
                    UIApplication.shared.open(url, options: [:], completionHandler: nil)
                    //: } else {
                } else {
                    //: if UIApplication.shared.canOpenURL(url) {
                    if UIApplication.shared.canOpenURL(url) {
                        //: UIApplication.shared.openURL(url)
                        UIApplication.shared.openURL(url)
                    }
                }
            }
            //: } else if type == 1 {
        } else if type == 1 {
            //: AppManagerRequest.func__requestUserConfig { succeed, result, errorModel in }
            RevenueMovementManagerRequest.societalCompletion { _, _, _ in }
            //: AppManagerRequest.func__requestUserInfo { succeed, result, errorModel in }
            RevenueMovementManagerRequest.account { _, _, _ in }
            //: TalkingPermissionTool.checkPushNotification { isShow in
            SpecialPermissionTool.thinChange { isShow in
                //: DispatchQueue.main.sync {
                DispatchQueue.main.sync {
                    //: let str = isShow ? "getSystemNotificationtStatus(true)" : "getSystemNotificationtStatus(false)"
                    let str = isShow ? String(bytes: const_qualityStr.map{programCapacity(income: $0)}, encoding: .utf8)! : String(bytes: kContrastStr.map{resignDisappear(sweep: $0)}, encoding: .utf8)!
                    //: self.webView.evaluateJavaScript(str) { data, error in
                    self.webView.evaluateJavaScript(str) { _, _ in
                    }
                }
            }
        }
    }

    //: private func refreshPoint() {
    private func pointBy() {
        //: AppManagerRequest.func__requestUserInfo { succeed, result, errorModel in
        RevenueMovementManagerRequest.account { _, _, _ in
            //: if TalkingLiveManager.shared().isLive {
            if ManagerPushListener.nowPanel().isLive {
                //: NotificationCenter.default.post(name: LIVE_USER_POINT_CHANGE, object: nil, userInfo: ["mfBean": 0.0])
                NotificationCenter.default.post(name: k_succeedUrl, object: nil, userInfo: [String(bytes: show_gradeMessage.map{padSignOutline(drown: $0)}, encoding: .utf8)!: 0.0])
            }
        }
    }

    /// 刷新金币
    /// - Parameter coin: 传' ', 刷新接口；传其他，进行相加（含负数））
    //: private func req_refreshCoinAll(coin: String) {
    private func characterTo(coin: String) {
        //: let value: Double = Double(coin) ?? 0
        let value = Double(coin) ?? 0
        //: if value == 0 {
        if value == 0 { // 刷新接口
            //: AppManagerRequest.func__getInfoColumn { succeed, result, errorModel in
            RevenueMovementManagerRequest.day { _, _, _ in
            }
            //: } else {
        } else { // 加减
            //: let newCoin = Double(DismissReactiveCompatible.share.loginUserMode.mf_coin)! + value
            let newCoin = Double(DismissReactiveCompatible.share.loginUserMode.mf_coin)! + value
            //: DismissReactiveCompatible.share.loginUserMode.mf_coin = String(format: "%.2f", newCoin)
            DismissReactiveCompatible.share.loginUserMode.mf_coin = String(format: "%.2f", newCoin)
        }
    }

    /// 刷新礼物背包
    //: private func req_refreshPackage() {
    private func installPackage() {
        //: NotificationCenter.default.post(name: GIFT_REFRESHPACKAGE_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: data_failId, object: nil)
    }

    /// 事件上报
    /// - Parameters:
    ///   - eventName: 事件名称
    ///   - jsonStr: 事件参数
    //: private func reportEvent(eventName: String, jsonStr: String) {
    private func atomicMagnitude54(eventName: String, jsonStr: String) {
        //: uploadRecord.uploadRecordEvent(eventID: eventName, toUid: "", jsonStr: jsonStr)
        const_pathMsg.tabKeep(eventID: eventName, toUid: "", jsonStr: jsonStr)
    }

    /// 手机震动
    //: private func phoneVibrate() {
    private func whenVibrate() {
        //: AudioServicesPlaySystemSound(kSystemSoundID_Vibrate)
        AudioServicesPlaySystemSound(kSystemSoundID_Vibrate)
    }
}
