
//: Declare String Begin

/*: ; build: :*/
fileprivate let const_modifyPath:[Character] = [";"," ","b","u","i","l","d",":"]

/*: ; iOS  :*/
fileprivate let dataPureEarlyUrl:[Character] = [";"," ","i","O","S"]
fileprivate let const_characterName:[Character] = [" "]

/*: "audio" :*/
fileprivate let kEngineeringData:String = "autogethero"

/*: .wav" :*/
fileprivate let mainFarPath:String = "detect expected.wav"

/*: "audio/wav" :*/
fileprivate let k_saleMsg:String = "flow bad of argument topicaudio/"
fileprivate let userForcePath:[Character] = ["w","a","v"]

/*: .jpg" :*/
fileprivate let main_edgeMessage:String = "tissue active bride.jpg"

/*: "image/jpeg" :*/
fileprivate let kExtentPath:String = "image/point source"

/*: "://app." :*/
fileprivate let appHideShadeAdministrativePath:String = "pan empty thanks theme://app."

/*: "data" :*/
fileprivate let notiQuitMaximumMsg:String = "dhelpa"

/*: ":null" :*/
fileprivate let userMeanThreeKey:String = "pause extent forward:null"

/*: "Request failed, Try again later" :*/
fileprivate let mainGoldMessage:[UInt8] = [0xba,0x8d,0x99,0x9d,0x8d,0x9b,0x9c,0xc8,0x8e,0x89,0x81,0x84,0x8d,0x8c,0xc4,0xc8,0xbc,0x9a,0x91,0xc8,0x89,0x8f,0x89,0x81,0x86,0xc8,0x84,0x89,0x9c,0x8d,0x9a]

private func remBridge(house num: UInt8) -> UInt8 {
    return num ^ 232
}

/*: "json error" :*/
fileprivate let mainCoreKey:[Character] = ["j","s","o","n"," ","e","r"]
fileprivate let const_alreadyFormat:[Character] = ["r","o","r"]

/*: "platform=iphone&version= :*/
fileprivate let kSitMessage:String = "platspeakerrm"
fileprivate let k_drawingUrl:[Character] = ["=","i","p","h","o","n","e","&","v","e","r","s","i","o","n","="]

/*: &packageId= :*/
fileprivate let show_normallyStr:String = "ad page there calculate&packa"

/*: &bundleId= :*/
fileprivate let show_listMessage:[Character] = ["&","b","u","n","d","l","e","I","d"]
fileprivate let showYellowUrl:[Character] = ["="]

/*: &lang= :*/
fileprivate let user_systemMsg:String = "exclusive that approval physical part&lang="

/*: "/route/slowindex" :*/
fileprivate let k_oneId:String = "since following progress tick/rou"
fileprivate let constExploreMsg:String = "picture bond may instruction overte/sl"

/*: "/route/index" :*/
fileprivate let dataHungWeekKey:[Character] = ["/","r","o","u","t","e","/","i","n","d"]
fileprivate let const_detailedStr:String = "eunite"

/*: "?path= :*/
fileprivate let data_pointValue:String = "?path=fort energy change"

/*: "&GJNonceStr= :*/
fileprivate let constProcessorTitle:String = "slide union&GJN"
fileprivate let appPoleName:[Character] = ["r","="]

/*: "&params= :*/
fileprivate let show_ownerDealFormat:String = "&paratranslation refer via"
fileprivate let data_latId:String = "clock reason rocket childms="

/*: "token" :*/
fileprivate let notiPairNagAirData:[Character] = ["t","o","k","e","n"]

/*: "请求成功 :*/
fileprivate let noti_advertisingTitle:String = "\u{8bf7}\u{6c42}\u{6210}功"

/*: "AAAAAAAA" :*/
fileprivate let notiActualLopeId:String = "meanmeanmeanmean"

/*: "下载完成 -  :*/
fileprivate let appMarketingId:String = "下\u{8f7d}完\u{6210} - "

/*: "/Documents/" :*/
fileprivate let app_phaseStr:String = "fill unit instruction request/Docu"
fileprivate let userResignUrl:[Character] = ["m","e","n","t","s","/"]

/*: ".mp4" :*/
fileprivate let kStretchStr:[Character] = [".","m","p","4"]

/*: "移动地址: :*/
fileprivate let userStartMessage:[Character] = ["移","动","\u{5730}","址",":"]

/*: "后台任务下载回来" :*/
fileprivate let appDenyTrustValue:String = "\u{540e}台任务下载"
fileprivate let noti_captureMsg:String = "\u{56de}\u{6765}"

/*: "下载成功 : :*/
fileprivate let mainNothingPath:[Character] = ["\u{4e0b}","\u{8f7d}","成","功"," ",":"]

/*: "Net Error" :*/
fileprivate let data_identifyFormat:String = "Net boy lose"

/*: "下载失败 : :*/
fileprivate let data_privacyName:[Character] = ["下","\u{8f7d}","失","\u{8d25}"," ",":"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  KingSessionDelegate.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/1/28.
//

//: import Alamofire
import Alamofire
//: import CoreMedia
import CoreMedia
//: import UIKit
import UIKit

// 默认请求超时时间
//: let REQUEST_TIMEOUT_INTERVAL = 10.0
let app_colorPath = 10.0

//: let userAgent = "\(AppName)/\(AppVersion) (\(AppBundle); build:\(AppBuildNumber); iOS \(UIDevice.current.systemVersion); \(UIDevice.modelName))"
let noti_sampleStr = "\(showLengthControlUrl)/\(main_sampleMaleContent) (\(k_failMsg)" + (String(const_modifyPath)) + "\(showCollectionKey)" + (String(dataPureEarlyUrl) + String(const_characterName)) + "\(UIDevice.current.systemVersion); \(UIDevice.modelName))"

//: typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: TalkingErrorResponse?) -> Void
typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: EngineeringErrorResponse?) -> Void

//: let GJ = TalkingRequestTool.init()
let const_backRateStr = KingSessionDelegate()

//: @objc class TalkingRequestTool: NSObject, URLSessionDownloadDelegate {
@objc class KingSessionDelegate: NSObject, URLSessionDownloadDelegate {
    //: public func startRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func lumbarVertebraContainer(model: ProcessingThen, completion: @escaping FinishBlock) {
        //: if model.paramsContainBinaryObject() {
        if model.robotAcross() {
            //: self.startUploadDataRequest(model: model, completion: completion)
            self.designLocal(model: model, completion: completion)
            //: } else {
        } else {
            //: self.startNormalRequest(model: model, completion: completion)
            self.brightBy(model: model, completion: completion)
        }
    }

    //: public func startUploadDataRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func designLocal(model: ProcessingThen, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.chophouse(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.glomInsideModel(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    pinCompletion(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, EngineeringErrorResponse(errorCode: LateNightHourOnsetTarget.NetError.rawValue, errorMsg: main_textStr))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        enablece(showMsg: main_textStr)
                    }

                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.upload(multipartFormData: { (multipartFormData) in
            AF.upload(multipartFormData: { multipartFormData in
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: if value is Data {
                    if value is Data {
                        //: if key == "audio" {
                        if key == (kEngineeringData.replacingOccurrences(of: "together", with: "di")) {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.getCurrentTimeStamp())+\(PackageID).wav", mimeType: "audio/wav")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.commonExpress())+\(userLengthValue)" + (String(mainFarPath.suffix(4))), mimeType: (String(k_saleMsg.suffix(6)) + String(userForcePath)))
                            //: } else {
                        } else {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(key).jpg", mimeType: "image/jpeg")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(key)" + (String(main_edgeMessage.suffix(4))), mimeType: (String(kExtentPath.prefix(6)) + "jpeg"))
                        }
                        //: } else {
                    } else {
                        //: let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        //: multipartFormData.append(data_, withName: key)
                        multipartFormData.append(data_, withName: key)
                    }
                }
                //: }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
            }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    pinCompletion(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, EngineeringErrorResponse(errorCode: LateNightHourOnsetTarget.NetError.rawValue, errorMsg: main_textStr))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        enablece(showMsg: main_textStr)
                    }

                    //: break
                }
            }
        }
    }

    //: public func startNormalRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func brightBy(model: ProcessingThen, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.chophouse(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.glomInsideModel(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: (Encryption_Request ? nil : model.params), headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: notiMarginPath ? nil : model.params, headers: headers, requestModifier: { $0.timeoutInterval = app_colorPath }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    pinCompletion(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, EngineeringErrorResponse(errorCode: LateNightHourOnsetTarget.NetError.rawValue, errorMsg: main_textStr))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        enablece(showMsg: main_textStr)
                    }
                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = app_colorPath }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    pinCompletion(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, EngineeringErrorResponse(errorCode: LateNightHourOnsetTarget.NetError.rawValue, errorMsg: main_textStr))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        enablece(showMsg: main_textStr)
                    }

                    //: break
                }
            }
        }
    }

    //: func func__requestSucess(model: TalkingRequestModel, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
    func pinCompletion(model: ProcessingThen, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
        //: let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        //: if httpCookies.count > 0 {
        if httpCookies.count > 0 {
            //: TalkingRequestAddrTool.share.func__updateTalkingCookies(cookies: httpCookies)
            ManufacturerThen.share.bringForth(cookies: httpCookies)
        }

        //: var responseJson = String(data: responseData, encoding: .utf8)
        var responseJson = String(data: responseData, encoding: .utf8)

        //: if Encryption_Request, model.requestServer.contains("://app.") {
        if notiMarginPath, model.requestServer.contains((String(appHideShadeAdministrativePath.suffix(7)))) {
            //: responseJson = responseJson?.aes256Decrypt(key: TalkingRequestAddrTool.share.encryKeyStr)
            responseJson = responseJson?.socialKey(key: ManufacturerThen.share.encryKeyStr)
        }

        //: responseJson = responseJson?.replacingOccurrences(of: "\"data\":null", with: "\"data\":{}")
        responseJson = responseJson?.replacingOccurrences(of: "\"" + (notiQuitMaximumMsg.replacingOccurrences(of: "help", with: "at")) + "\"" + (String(userMeanThreeKey.suffix(5))), with: "" + "\"" + (notiQuitMaximumMsg.replacingOccurrences(of: "help", with: "at")) + "\"" + ":{}")
        //: if !Encryption_Request {
        if !notiMarginPath {
            //: responseJson = responseJson?.replacingOccurrences(of: ":null", with: ":\"\"")
            responseJson = responseJson?.replacingOccurrences(of: (String(userMeanThreeKey.suffix(5))), with: ":" + "\"" + "\"")
        }
        //: if let responseModel = JSONDeserializer<TalkingBaseResponse>.deserializeFrom(json: responseJson) {
        if let responseModel = JSONDeserializer<GrantTransformable>.deserializeFrom(json: responseJson) {
            //: if responseModel.errno == RequestResultCode.Normal.rawValue {
            if responseModel.errno == LateNightHourOnsetTarget.Normal.rawValue {
                //: completion(true, responseModel.data, nil)
                completion(true, responseModel.data, nil)
                //: } else {
            } else {
                //: completion(false, responseModel.data, TalkingErrorResponse.init(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                completion(false, responseModel.data, EngineeringErrorResponse(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                //: if model.showErrorStatusBar {
                if model.showErrorStatusBar {
                    //: func__showStatusBarErrorMsg(showMsg: responseModel.msg ?? "Request failed, Try again later".localized)
                    enablece(showMsg: responseModel.msg ?? String(bytes: mainGoldMessage.map{remBridge(house: $0)}, encoding: .utf8)!.localized)
                }
                //: switch responseModel.errno {
                switch responseModel.errno {
                //: case RequestResultCode.NeedReLogin.rawValue:
                case LateNightHourOnsetTarget.NeedReLogin.rawValue:
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: kOnValue, object: nil, userInfo: nil)
                //: default:
                default:
                    //: break
                    break
                }
            }
            //: } else {
        } else {
            //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "json error".localized))
            completion(false, nil, EngineeringErrorResponse(errorCode: LateNightHourOnsetTarget.NetError.rawValue, errorMsg: (String(mainCoreKey) + String(const_alreadyFormat)).localized))
        }
    }

    //: func buildServerUrl(model: TalkingRequestModel) -> String {
    func chophouse(model: ProcessingThen) -> String {
        //: var serverUrl: String = model.requestServer
        var serverUrl: String = model.requestServer

        //: var otherParams = "platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)&bundleId=\(AppBundle)&lang=\(TalkingRequestAddrTool.share.interfaceLang)"
        var otherParams = (kSitMessage.replacingOccurrences(of: "speaker", with: "fo") + String(k_drawingUrl)) + "\(userCountData)" + (String(show_normallyStr.suffix(6)) + "geId=") + "\(userLengthValue)" + (String(show_listMessage) + String(showYellowUrl)) + "\(k_failMsg)" + (String(user_systemMsg.suffix(6))) + "\(ManufacturerThen.share.interfaceLang)"
        //: if Encryption_Request, serverUrl.contains("://app.") {
        if notiMarginPath, serverUrl.contains((String(appHideShadeAdministrativePath.suffix(7)))) {
            //: let allowCharSet: CharacterSet = CharacterSet.init(charactersIn: "?!@#$^&%*+,:;='\"`<>()[]{}/\\| ").inverted
            let allowCharSet = CharacterSet(charactersIn: "?!@#$^&%*+,:;='" + "\"" + "`<>()[]{}/\\| ").inverted
            //: if TalkingRequestAddrTool.share.slowPathArr.contains(model.requestPath) {
            if ManufacturerThen.share.slowPathArr.contains(model.requestPath) {
                //: serverUrl.append("/route/slowindex")
                serverUrl.append((String(k_oneId.suffix(4)) + String(constExploreMsg.suffix(5)) + "owindex"))
                //: } else {
            } else {
                //: serverUrl.append("/route/index") // 如果使用IP需要加上app/
                serverUrl.append((String(dataHungWeekKey) + const_detailedStr.replacingOccurrences(of: "unite", with: "x"))) // 如果使用IP需要加上app/
            }

            //: let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            //: serverUrl.append("?path=\(pathStr)")
            serverUrl.append((String(data_pointValue.prefix(6))) + "\(pathStr)")
            //: serverUrl.append("&GJNonceStr=\(TalkingRequestAddrTool.share.randomStr)")
            serverUrl.append((String(constProcessorTitle.suffix(4)) + "onceSt" + String(appPoleName)) + "\(ManufacturerThen.share.randomStr)")

            //: if model.requestType == .GET {
            if model.requestType == .GET {
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: otherParams.append("&\(key)=\(value)")
                    otherParams.append("&\(key)=\(value)")
                }
            }
            //: if let encryStr = otherParams.aes256Encrypt(key: TalkingRequestAddrTool.share.encryKeyStr) {
            if let encryStr = otherParams.calpacKey(key: ManufacturerThen.share.encryKeyStr) {
                //: let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                //: serverUrl.append("&params=\(paramsStr)")
                serverUrl.append((String(show_ownerDealFormat.prefix(5)) + String(data_latId.suffix(3))) + "\(paramsStr)")
            }
            //: printLog(message: serverUrl)
            printLog(message: serverUrl)
            //: } else {
        } else {
            //: if !model.requestPath.isEmptyString {
            if !model.requestPath.isEmptyString {
                //: serverUrl.append("/\(model.requestPath)")
                serverUrl.append("/\(model.requestPath)")
            }
            //: serverUrl.append("?\(otherParams)")
            serverUrl.append("?\(otherParams)")
        }
        //: return serverUrl
        return serverUrl
    }

    /// 获取请求头参数
    /// - Parameter model: 请求模型
    /// - Returns: 请求头参数
    //: func getRequestHeader(model: TalkingRequestModel) -> HTTPHeaders {
    func glomInsideModel(model: ProcessingThen) -> HTTPHeaders {
        //: var headers = [HTTPHeader.userAgent(userAgent)]
        var headers = [HTTPHeader.userAgent(noti_sampleStr)]
        //: if model.addHeaderToken.isEmpty == false {
        if model.addHeaderToken.isEmpty == false {
            //: let token = HTTPHeader(name: "token", value: model.addHeaderToken)
            let token = HTTPHeader(name: (String(notiPairNagAirData)), value: model.addHeaderToken)
            //: headers.append(token)
            headers.append(token)
        }
        //: return HTTPHeaders(headers)
        return HTTPHeaders(headers)
    }

    // MARK: - 系统后台下载

    //: func urlSessionBackgroundDownLoad(model: TalkingRequestModel) {
    func groupDiscussionLoadDownMTheoryBackground(model: ProcessingThen) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.chophouse(model: model)
        //: URLSession.shared.dataTask(with: URL.init(string: serverUrl)!) {(data, response, error) in
        URLSession.shared.dataTask(with: URL(string: serverUrl)!) { _, response, error in
            //: if error == nil {
            if error == nil {
                //: printLog(message: "请求成功\(String(describing: response))" )
                printLog(message: (noti_advertisingTitle) + "\(String(describing: response))")
            }
        }

//        let configuration1 = URLSessionConfiguration.default    // default模式下系统会创建一个持久化的缓存并在用户的钥匙串中存储证书
//        let configuration2 = URLSessionConfiguration.ephemeral  // 所有内容的生命周期都与session相同，当session无效时，所有内容自动释放。

        // 后台下载
        //: let configuration3 = URLSessionConfiguration.background(withIdentifier: "AAAAAAAA")  // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        let configuration3 = URLSessionConfiguration.background(withIdentifier: (notiActualLopeId.replacingOccurrences(of: "mean", with: "A") + "AAAA")) // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        //: configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        //: configuration3.networkServiceType = .default
        configuration3.networkServiceType = .default // 网络服务的类型
        //: configuration3.allowsCellularAccess = true
        configuration3.allowsCellularAccess = true // 一个布尔值，用于确定是否应通过蜂窝网络进行连接
        //: configuration3.timeoutIntervalForRequest = 15
        configuration3.timeoutIntervalForRequest = 15
        //: configuration3.timeoutIntervalForResource = 15
        configuration3.timeoutIntervalForResource = 15
        //: let session = URLSession.init(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        let session = URLSession(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        //: session.downloadTask(with: URL.init(string: serverUrl)!).resume()
        session.downloadTask(with: URL(string: serverUrl)!).resume()
    }

    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
    func urlSession(_: URLSession, downloadTask _: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
        //: printLog(message: "下载完成 - \(location)")
        printLog(message: (appMarketingId) + "\(location)")
        //: let locationPath = location.path
        let locationPath = location.path
        //: let documnets = NSHomeDirectory() + "/Documents/" + NSDate.getCurrentTimeStamp() + ".mp4"
        let documnets = NSHomeDirectory() + (String(app_phaseStr.suffix(5)) + String(userResignUrl)) + NSDate.commonExpress() + (String(kStretchStr))
        //: printLog(message: "移动地址:\(documnets)")
        printLog(message: (String(userStartMessage)) + "\(documnets)")
        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: do {
        do {
            //: try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            //: } catch {
        } catch {
            // catch 中默认提供error信息, 当序列化不成功是, 返回error
            //: printLog(message: error)
            printLog(message: error)
        }
    }

    // 监听下载进度
    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didWriteData bytesWritten: Int64, totalBytesWritten: Int64, totalBytesExpectedToWrite: Int64) {
    func urlSession(_: URLSession, downloadTask _: URLSessionDownloadTask, didWriteData _: Int64, totalBytesWritten _: Int64, totalBytesExpectedToWrite _: Int64) {}

    //: func urlSessionDidFinishEvents(forBackgroundURLSession session: URLSession) {
    func urlSessionDidFinishEvents(forBackgroundURLSession _: URLSession) {
        //: printLog(message: "后台任务下载回来")
        printLog(message: (appDenyTrustValue + noti_captureMsg))
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard let backgroundHandle = AppDelegateHelper.shared.backgroundSessionCompletionHandler else { return }
            guard let backgroundHandle = PortraitReactiveCompatible.shared.backgroundSessionCompletionHandler else { return }
            //: backgroundHandle()
            backgroundHandle()
        }
    }

    // MARK: - 动画下载

    //: public func startGiftZipRequest(giftUrl: String, path: String, completion: @escaping FinishBlock) {
    public func proclamationObject(giftUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }
        //: let giftUrlStr = giftUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let giftUrlStr = giftUrl.replacingOccurrences(of: userValueTitle.until6decrypt(), with: showReasonContent)
        //: AF.download(giftUrlStr, to: destination).downloadProgress { speed in
        AF.download(giftUrlStr, to: destination).downloadProgress { _ in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: (String(mainNothingPath)) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, EngineeringErrorResponse(errorCode: LateNightHourOnsetTarget.Normal.rawValue, errorMsg: (String(data_identifyFormat.prefix(4)) + "Error").localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, EngineeringErrorResponse(errorCode: LateNightHourOnsetTarget.NetError.rawValue, errorMsg: (String(data_identifyFormat.prefix(4)) + "Error").localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: (String(data_privacyName)) + "\(errMsg)")
            }
        }
    }

    //: public func startVoiceRequest(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
    public func magnitudeCompletion(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }

        //: let voiceUrlStr = voiceUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let voiceUrlStr = voiceUrl.replacingOccurrences(of: userValueTitle.until6decrypt(), with: showReasonContent)
        //: AF.download(voiceUrlStr, to: destination).downloadProgress { speed in
        AF.download(voiceUrlStr, to: destination).downloadProgress { _ in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: (String(mainNothingPath)) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, EngineeringErrorResponse(errorCode: LateNightHourOnsetTarget.Normal.rawValue, errorMsg: (String(data_identifyFormat.prefix(4)) + "Error").localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, EngineeringErrorResponse(errorCode: LateNightHourOnsetTarget.NetError.rawValue, errorMsg: (String(data_identifyFormat.prefix(4)) + "Error").localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: (String(data_privacyName)) + "\(errMsg)")
            }
        }
    }
}
