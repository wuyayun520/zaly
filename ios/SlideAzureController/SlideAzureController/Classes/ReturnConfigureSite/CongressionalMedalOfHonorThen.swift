
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let constCurvePath:[UInt8] = [0xa2,0xa7,0xa2,0xad,0x61,0x9c,0xa8,0x9d,0x9e,0xab,0x73,0x62,0x59,0xa1,0x9a,0xac,0x59,0xa7,0xa8,0xad,0x59,0x9b,0x9e,0x9e,0xa7,0x59,0xa2,0xa6,0xa9,0xa5,0x9e,0xa6,0x9e,0xa7,0xad,0x9e,0x9d]

fileprivate func weddingPrivacy(extent num: UInt8) -> UInt8 {
    let value = Int(num) + 199
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Personal information" :*/
fileprivate let dataElectKey:String = "sum capital gray whetherPers"
fileprivate let mainFlatMsg:String = " infojob bear editor ago missing"

/*: "male" :*/
fileprivate let mainPoseLatePath:[UInt8] = [0xd4,0xd8,0xd5,0xdc]

private func mateBook(trigger num: UInt8) -> UInt8 {
    return num ^ 185
}

/*: "female" :*/
fileprivate let const_hugeName:String = "feoffle"

/*: "newHeadPic" :*/
fileprivate let dataBeamStr:String = "newHeaheight double add public route"

/*: "invite_code" :*/
fileprivate let data_arrangementName:[Character] = ["i","n","v","i","t","e","_","c","o"]
fileprivate let mainBeFormat:[Character] = ["d","e"]

/*: "codeFillType" :*/
fileprivate let dataElementName:[Character] = ["c","o","d"]
fileprivate let show_cityName:String = "eFillok reason thin coat insert"

/*: "RegisterSuccess" :*/
fileprivate let k_carrierMessage:[Character] = ["R","e","g","i","s","t","e"]
fileprivate let showTodayGenerateWordData:String = "rSuccessthan notice now single"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  CongressionalMedalOfHonorThen.swift
//  AbroadTalking
//
//  Created by Charlotte on 2024/5/24.
//

//: import UIKit
import UIKit

//: class TalkingLoginEditPhotoVC: TalkingBaseViewController {
class CongressionalMedalOfHonorThen: WeddingLovelyRecognizerDelegate {
    //: fileprivate let disposeBag = DisposeBag()
    fileprivate let disposeBag = DisposeBag()
    //: var params = [String: Any]()
    var params = [String: Any]()
    //: init() {
    init() {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: constCurvePath.map{weddingPrivacy(extent: $0)}, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: popGesture(isOpen: false)
        rankSlow(isOpen: false)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.title = "Personal information".localized
        self.title = (String(dataElectKey.suffix(4)) + "onal" + String(mainFlatMsg.prefix(5)) + "rmation").localized
        //: self.view.backgroundColor = .white
        self.view.backgroundColor = .white
        //: self.setupSubviews()
        self.judge()
        //: self.setupSubViewsConstraint()
        self.afterRating()
        //: self.bindInteraction()
        self.bot()
        //: addTapGestureRecognizer()
        workIn()

        //: func__checkFinishBtnState()
        popular()
    }

    //: deinit {
    deinit {}

    // MARK: - Lazy Load

    //: lazy var editInfoView: TalkingLoginEditPhotoView = {
    lazy var editInfoView: ActualReactiveCompatible = {
        //: let infoView = TalkingLoginEditPhotoView.init()
        let infoView = ActualReactiveCompatible()
        //: return infoView
        return infoView
        //: }()
    }()
}

// MARK: - Public Event

//: extension TalkingLoginEditPhotoVC {
extension CongressionalMedalOfHonorThen {
    /// 重写返回按钮事件
    //: override func naviPopback() {
    override func popbackResult() {
        //: super.naviPopback()
        super.popbackResult()
        // 埋点
        //: let eventID = "\(click_registration_information2_backBTN)_\(DismissReactiveCompatible.share.userFillInfoMode.sex == Gender.male.rawValue ? "male" : "female")"
        let eventID = "\(showFailureUrl)_\(DismissReactiveCompatible.share.userFillInfoMode.sex == FenceSequence.male.rawValue ? String(bytes: mainPoseLatePath.map{mateBook(trigger: $0)}, encoding: .utf8)! : (const_hugeName.replacingOccurrences(of: "off", with: "ma")))"
        //: uploadRecord.uploadRecordEvent(eventID: eventID)
        const_pathMsg.tabKeep(eventID: eventID)
    }

    /// next事件
    //: func commitControlAction() {
    func contact() {
        // 埋点
        //: let eventID = "\(click_registration_information2_nextBTN)_\(DismissReactiveCompatible.share.userFillInfoMode.sex == Gender.male.rawValue ? "male" : "female")"
        let eventID = "\(notiEventName)_\(DismissReactiveCompatible.share.userFillInfoMode.sex == FenceSequence.male.rawValue ? String(bytes: mainPoseLatePath.map{mateBook(trigger: $0)}, encoding: .utf8)! : (const_hugeName.replacingOccurrences(of: "off", with: "ma")))"
        //: uploadRecord.uploadRecordEvent(eventID: eventID)
        const_pathMsg.tabKeep(eventID: eventID)

        //: if DismissReactiveCompatible.share.userFillInfoMode.headImage != nil {
        if DismissReactiveCompatible.share.userFillInfoMode.headImage != nil {
            //: let image = DismissReactiveCompatible.share.userFillInfoMode.headImage!
            let image = DismissReactiveCompatible.share.userFillInfoMode.headImage!
            //: params["newHeadPic"] = image.jpegData(compressionQuality: 1)
            params[(String(dataBeamStr.prefix(6)) + "dPic")] = image.jpegData(compressionQuality: 1)
        }
        //: if DismissReactiveCompatible.share.userFillInfoMode.inviteCode.count > 0 {
        if DismissReactiveCompatible.share.userFillInfoMode.inviteCode.count > 0 {
            //: params["invite_code"] = DismissReactiveCompatible.share.userFillInfoMode.inviteCode
            params[(String(data_arrangementName) + String(mainBeFormat))] = DismissReactiveCompatible.share.userFillInfoMode.inviteCode

            //: } else {
        } else {
            //: let inviteCode = UserDefaults.standard.string(forKey: SaveInviteCodeKey)
            let inviteCode = UserDefaults.standard.string(forKey: show_marginScaleId)
            //: if inviteCode?.count ?? 0 > 1 {
            if inviteCode?.count ?? 0 > 1 {
                //: params["invite_code"] = inviteCode
                params[(String(data_arrangementName) + String(mainBeFormat))] = inviteCode
                //: params["codeFillType"] = 1
                params[(String(dataElementName) + String(show_cityName.prefix(5)) + "Type")] = 1
            }
        }

        //: if DismissReactiveCompatible.share.userFillInfoMode.sex == "1" {
        if DismissReactiveCompatible.share.userFillInfoMode.sex == "1" {
            //: view.endEditing(true)
            view.endEditing(true)
            //: ProgressHUD.show()
            InsertProgressHUD.momentShow()
            //: TalkingLoginRequestTool.req_updateUserInfo(params: params) { succeed, result, errorModel in
            FirRequestTool.userInfoReloadPushCompletion(params: params) { succeed, result, _ in
                //: ProgressHUD.dismiss()
                InsertProgressHUD.detectDismiss()
                //: if succeed {
                if succeed {
                    // 发送通知
                    //: NotificationCenter.default.post(name: DID_LOGIN_SUCCESS_NOTIFICATION, object: nil, userInfo: result as! Dictionary<String, Any>)
                    NotificationCenter.default.post(name: noti_groupUrl, object: nil, userInfo: result as! [String: Any])
                    // 注册成功埋点
                    //: TalkingAdjustManager.share.addEvent(key: "RegisterSuccess")
                    ProgMedalReactiveCompatible.share.stem(key: (String(k_carrierMessage) + String(showTodayGenerateWordData.prefix(8))))
                    //: FirebaseAnalyticsManager.share.logEvent(name: "RegisterSuccess")
                    LeaseTrailerThen.share.tupeloShowPlay(name: (String(k_carrierMessage) + String(showTodayGenerateWordData.prefix(8))))

                    //: if DismissReactiveCompatible.share.loginUserMode.remindBindEmail == true {
                    if DismissReactiveCompatible.share.loginUserMode.remindBindEmail == true { // 需要绑定邮箱
                        //: DispatchQueue.main.asyncAfter(deadline: .now()+0.5) {
                        DispatchQueue.main.asyncAfter(deadline: .now() + 0.5) {
                            //: PushThen.share.func__pushToLockUserEmailVC(isShowBack: false)
                            PushThen.share.tabAcross(isShowBack: false)
                        }
                    }
                }
            }
            //: } else {
        } else {
            //: let VC = TalkingLoginEditAuthAndVideoVC.init()
            let VC = TimeOfLifeRecognizerDelegate()
            //: VC.params = params
            VC.params = params
            //: self.currentViewController()?.navigationController?.pushViewController(VC, animated: true)
            self.exitSchedule()?.navigationController?.pushViewController(VC, animated: true)
        }
    }

    //: func func__checkFinishBtnState() {
    func popular() {
        //: let headImg = DismissReactiveCompatible.share.userFillInfoMode.headImage ?? nil
        let headImg = DismissReactiveCompatible.share.userFillInfoMode.headImage ?? nil
        //: editInfoView.commitButton.isEnabled = headImg != nil
        editInfoView.commitButton.isEnabled = headImg != nil
    }
}

// MARK: - Delegate

//: extension TalkingLoginEditPhotoVC: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension CongressionalMedalOfHonorThen: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func photosAction() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) {  (isOpen: Bool) in
        SpecialPermissionTool.burst(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(allowImgCrop: true, maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = MedalPickTool.sameMember(allowImgCrop: true, maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: PushThen.share.func__getCurrentActivityVC()?.present(vc, animated: true)
                PushThen.share.doingGoing()?.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 _: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 _: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: DismissReactiveCompatible.share.userFillInfoMode.headImage = photos![0]
                            DismissReactiveCompatible.share.userFillInfoMode.headImage = photos![0]
                            //: self.editInfoView.addHeadButton.setImage(photos![0], for: .normal)
                            self.editInfoView.addHeadButton.setImage(photos![0], for: .normal)
                            //: self.func__checkFinishBtnState()
                            self.popular()
                        }
                }
            }
        }
    }
}

// MARK: - Layout

//: extension TalkingLoginEditPhotoVC {
extension CongressionalMedalOfHonorThen {
    // 添加视图
    //: private func setupSubviews() {
    private func judge() {
        //: view.addSubview(editInfoView)
        view.addSubview(editInfoView)
    }

    // 添加约束
    //: private func setupSubViewsConstraint() {
    private func afterRating() {
        //: editInfoView.snp.makeConstraints { make in
        editInfoView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }

    // 添加事件
    //: private func bindInteraction() {
    private func bot() {
        //: editInfoView.btnBlock = { [weak self] (_ type: EditInfoType) -> Void in
        editInfoView.btnBlock = { [weak self] (_ type: SmallInfoType) in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: switch type {
            switch type {
            //: case .HeadPic:
            case .HeadPic:
                //: func__choicePhotos()
                photosAction()
            //: break
            //: case .NickName, .Birth, .Skip: break
            case .NickName, .Birth, .Skip: break
            //: case .Finish:
            case .Finish:
                //: self.commitControlAction()
                self.contact()
                //: break
            }

            //: return
        }
    }
}
