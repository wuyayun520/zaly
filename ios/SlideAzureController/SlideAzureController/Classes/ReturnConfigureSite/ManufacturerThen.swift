
//: Declare String Begin

/*: "http://" :*/
fileprivate let const_produceMsg:String = "http://ski false actual border failure"

/*: "https://" :*/
fileprivate let user_helpData:String = "httqualify"

/*: "http:// :*/
fileprivate let kBondRelateName:[Character] = ["h","t","t","p",":","/","/"]

/*: "serverSelctIndex =  :*/
fileprivate let noti_argumentPath:[Character] = ["s","e","r","v","e","r","S","e","l","c","t","I","n"]
fileprivate let notiWhetherSuspendData:String = "photo"
fileprivate let const_supplyName:String = "job relation gray zz startex = "

/*: "serverUrlStr =  :*/
fileprivate let dataNameureMsg:[Character] = ["s","e","r","v","e","r"]
fileprivate let dataFirmFormat:String = "UrlStr = when trade conversion additional"

/*: "app. :*/
fileprivate let constWouldMessage:[Character] = ["a","p","p","."]

/*: .com" :*/
fileprivate let app_absoluteFormat:[Character] = [".","c","o","m"]

/*: "mf/chatRoom/sendGift" :*/
fileprivate let const_structureKey:[Character] = ["m","f","/","c","h","a"]
fileprivate let k_offerPremiumManMessage:[Character] = ["t","R","o","o","m","/"]
fileprivate let main_postName:[Character] = ["s","e","n","d","G","i","f","t"]

/*: "recharge/applePayNotify" :*/
fileprivate let k_lackData:[Character] = ["r","e","c","h","a","r","g","e","/","a","p","p","l","e","P"]
fileprivate let user_decreaseKey:String = "tip"
fileprivate let show_behindTitle:[Character] = ["y","N","o","t","i","f","y"]

/*: "user/appleLogin" :*/
fileprivate let showShrinkStr:String = "sequence recordinguser/a"
fileprivate let k_introMsg:String = "oshallin"

/*: "IM/getMyBlackList" :*/
fileprivate let app_inmateValue:String = "tap resistanceIM/get"
fileprivate let app_documentCarrierFormat:String = "staff bouncekList"

/*: "mf/business/sendMsg" :*/
fileprivate let mainPrivacyTrailTitle:[Character] = ["m","f","/","b","u","s","i","n","e","s","s","/","s","e","n","d","M","s","g"]

/*: "mf/business/sendGift" :*/
fileprivate let showWrapDraftMsg:[Character] = ["m","f","/","b","u","s","i"]
fileprivate let user_lackPath:String = "distance motivation exitness/"
fileprivate let mainHoodMsg:String = "plat finite cos going lastsendGift"

/*: "mf/chatRoom/sendMsg" :*/
fileprivate let const_shrinkPath:String = "mf/chatlower remaining sequence"
fileprivate let showCloudData:String = "Room/dress agree kit attach"

/*: "GJ21FB" :*/
fileprivate let const_lovelyContent:[Character] = ["G","J","2","1","F"]
fileprivate let data_oppositeGiveText:[Character] = ["B"]

/*: "完成了检测,请求状态正常" :*/
fileprivate let kCustomKey:String = "完成\u{4e86}检"

/*: "接口请求失败" :*/
fileprivate let appFutureThreeName:[Character] = ["接","口","请","\u{6c42}","\u{5931}","败"]

/*: "完成了检测,需要更换server url" :*/
fileprivate let k_nagName:[Character] = ["完","成","\u{4e86}","检","测",",","需","要","更","换","s","e","r"]
fileprivate let appDistinctiveStr:String = "speaker"
fileprivate let noti_informLengthStr:String = "er urluniform package us anonymous self"

/*: "HTTPProxy" :*/
fileprivate let dataHoneySoccerKey:String = "observer minimize panelHTTPPr"
fileprivate let notiLeadingStr:[Character] = ["o","x","y"]

/*: "__SCOPED__" :*/
fileprivate let k_cordId:String = "particle handle main__SCO"

/*: "tap" :*/
fileprivate let app_stepUrl:String = "TAP"

/*: "tun" :*/
fileprivate let app_mysteryTitle:String = "tview"

/*: "ipsec" :*/
fileprivate let dataSubmitName:String = "speaksec"

/*: "ppp" :*/
fileprivate let const_workThinkMessage:[Character] = ["p","p","p"]

/*: "func__updateTalkingCookies =  :*/
fileprivate let k_boxFormat:[Character] = ["f","u","n","c","_","_","u","p","d","a","t","e","T","a","l","k","i","n","g","C","o","o","k","i","e","s"," ","="," "]

/*: "https:// :*/
fileprivate let data_powMemberKey:String = "https://nothing layer"

/*: "func__deleteSameCookiesWithUrl =  :*/
fileprivate let user_clickAddUrl:[UInt8] = [0x0,0x13,0x8,0x5,0x39,0x39,0x2,0x3,0xa,0x3,0x12,0x3,0x35,0x7,0xb,0x3,0x25,0x9,0x9,0xd,0xf,0x3,0x15,0x31,0xf,0x12,0xe,0x33,0x14,0xa,0x46,0x5b,0x46]

/*: "UID" :*/
fileprivate let userMinStr:[Character] = ["U","I","D"]

/*: "PHPSESSID" :*/
fileprivate let const_recognizeMessage:[Character] = ["P","H","P","S","E","S","S","I","D"]

/*: "func__saveAllCookies =  :*/
fileprivate let dataTailAnyoneContent:[Character] = ["f","u","n","c","_","_","s","a","v","e","A"]
fileprivate let showParentMotiveData:[Character] = ["l","l","C","o","o","k","i","e","s"," ","="," "]

/*: "func__saveAllCookies  error" :*/
fileprivate let constDotKey:String = "func__personal secure design bold always"
fileprivate let appBroadcastTitle:String = "llCoappearance unless bridge"
fileprivate let dataSunId:String = "eloador"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ManufacturerThen.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/23.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

//: enum LangType: String {
enum ImpressionName: String {
    //: case en = "en"
    case en
    //: case ar = "ar"
    case ar
    //: case es = "es"
    case es
    //: case pt = "pt"
    case pt
}

//: public class TalkingRequestAddrTool: NSObject {
public class ManufacturerThen: NSObject {
    //: @objc static public let share = TalkingRequestAddrTool()
    @objc public static let share = ManufacturerThen()
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        //: setUp()
        setPut()
    }

    //: private var timerDispose: Disposable?
    private var timerDispose: Disposable?

    //: var serverSelctIndex = 0
    var serverSelctIndex = 0
    //: var hasFinishCheck   = false
    var hasFinishCheck = false
    //: var hostCheckNum     = 0
    var hostCheckNum = 0

    //: var domainStr: String?
    var domainStr: String? // 用户登录的信息
    //: var _serverUrlStr: String = ""       //
    var _serverUrlStr: String = "" //

    //: @objc public var serverUrlStr: String {
    @objc public var serverUrlStr: String {
        //: get {
        get {
            //: return _serverUrlStr
            return _serverUrlStr
        }
        //: set {
        set {
            //: var newServerUrl: String = ""
            var newServerUrl: String = ""
            //: if newValue.hasPrefix("http://") || newValue.hasPrefix("https://") {
            if newValue.hasPrefix((String(const_produceMsg.prefix(7)))) || newValue.hasPrefix((user_helpData.replacingOccurrences(of: "qualify", with: "p") + "s://")) {
                //: newServerUrl = newValue
                newServerUrl = newValue
                //: } else {
            } else {
                //: if Environment_Formal && !Encryption_Request {
                if kAddId && !notiMarginPath {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(kBondRelateName)) + "\(newValue)"
                    //: } else {
                } else {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(kBondRelateName)) + "\(newValue)"
                }
            }
            //: if NSURL.init(string: newServerUrl) == nil {
            if NSURL(string: newServerUrl) == nil {
                //: return
                return
            }
            //: let oldServerUrl: String = _serverUrlStr
            let oldServerUrl: String = _serverUrlStr

            //: if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
            if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
                //: _serverUrlStr = newServerUrl
                _serverUrlStr = newServerUrl
                //: if oldServerUrl.count > 0 {
                if oldServerUrl.count > 0 {
                    //: let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: oldServerUrl)!)!
                    let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: oldServerUrl)!)!
                    //: func__updateTalkingCookies(cookies: oldCookies)
                    bringForth(cookies: oldCookies)
                }
            }
        }
    }

    //: func setUp() {
    func setPut() {
        //: serverSelctIndex = Defaults.integer(forKey: TalkingAddrServerUrlSelectIndexKey)
        serverSelctIndex = data_partyText.integer(forKey: userBeginUrl)
        //: printLog(message: "serverSelctIndex = \(serverSelctIndex)")
        printLog(message: (String(noti_argumentPath) + notiWhetherSuspendData.replacingOccurrences(of: "photo", with: "d") + String(const_supplyName.suffix(5))) + "\(serverSelctIndex)")
        //: let serverDomain = func__getInitialServerUrl()
        let serverDomain = postShade()
        //: var serverPrefix = "http://"
        var serverPrefix = (String(const_produceMsg.prefix(7)))
        //: if Environment_Formal && !Encryption_Request {
        if kAddId && !notiMarginPath {
            //: serverPrefix = "http://"
            serverPrefix = (String(const_produceMsg.prefix(7)))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
        //: printLog(message: "serverUrlStr = \(serverUrlStr)")
        printLog(message: (String(dataNameureMsg) + String(dataFirmFormat.prefix(9))) + "\(serverUrlStr)")
    }

    //: func func__getInitialServerUrl() -> String {
    func postShade() -> String {
        //: var appServerUrl = ""
        var appServerUrl = ""
        //: appServerUrl = serverUrlArr[serverSelctIndex]
        appServerUrl = serverUrlArr[serverSelctIndex]
        //: return appServerUrl
        return appServerUrl
    }

    //: func func__saveNewTalkingServerUrl(serverStr: String) {
    func guidance(serverStr: String) {
        //: serverUrlStr = serverStr
        serverUrlStr = serverStr
    }

    //: private lazy var serverUrlArr: Array = {
    private lazy var serverUrlArr: Array = //: return ["app.\(ReplaceUrlDomain).com", "app.\(ReplaceUrlDomain).com"]
        [(String(constWouldMessage)) + "\(showReasonContent)" + (String(app_absoluteFormat)), (String(constWouldMessage)) + "\(showReasonContent)" + (String(app_absoluteFormat))]
    //: }()

    //: lazy var slowPathArr: Array = {
    lazy var slowPathArr: Array = //: return ["mf/chatRoom/sendGift",
        [(String(const_structureKey) + String(k_offerPremiumManMessage) + String(main_postName)),
         //: "recharge/applePayNotify",
         (String(k_lackData) + user_decreaseKey.replacingOccurrences(of: "tip", with: "a") + String(show_behindTitle)),
         //: "user/appleLogin"] + slowPathArr1
         (String(showShrinkStr.suffix(6)) + "ppleL" + k_introMsg.replacingOccurrences(of: "shall", with: "g"))] + slowPathArr1
    //: }()

    //: private lazy var slowPathArr1 = {
    private lazy var slowPathArr1 = //: return ["IM/getMyBlackList",
        [(String(app_inmateValue.suffix(6)) + "MyBlac" + String(app_documentCarrierFormat.suffix(5))),
         //: "mf/business/sendMsg",
         (String(mainPrivacyTrailTitle)),
         //: "mf/business/sendGift",
         (String(showWrapDraftMsg) + String(user_lackPath.suffix(5)) + String(mainHoodMsg.suffix(8))),
         //: "mf/chatRoom/sendMsg"]
         (String(const_shrinkPath.prefix(7)) + String(showCloudData.prefix(5)) + "sendMsg")]
    //: }()

    /// 获取接口语言
    //: lazy var interfaceLang: String = {
    lazy var interfaceLang: String = {
        //: let lang = DepictionThen.getSystemLangCode()
        let lang = DepictionThen.outline()
        //: if ["en", "ar", "es", "pt"].contains(lang) {
        if ["en", "ar", "es", "pt"].contains(lang) {
            //: return lang
            return lang
        }
        //: return "en"
        return "en"
        //: }()
    }()

    //: lazy var randomStr: String = {
    lazy var randomStr: String = //: return String.randomString(length: 16)
        .weekBy(length: 16)
    //: }()

    //: lazy var encryKeyStr: String = {
    lazy var encryKeyStr: String = {
        //: let sufStr = randomStr.suffix(6)
        let sufStr = randomStr.suffix(6)
        //: let reversedStr: String = String(sufStr.reversed())
        let reversedStr = String(sufStr.reversed())
        //: return "GJ21FB" + reversedStr
        return (String(const_lovelyContent) + String(data_oppositeGiveText)) + reversedStr
        //: }()
    }()

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

//: extension TalkingRequestAddrTool {
extension ManufacturerThen {
    //: func checkServerUrl() {
    func blindTask() {
        //: if DismissReactiveCompatible.share.networkStatus != .Unavailable {
        if DismissReactiveCompatible.share.networkStatus != .Unavailable {
            //: func_requestHost()
            fileMedium()
            //: } else {
        } else {
            //: func_starCodeTime()
            domesticAnimal()
        }
    }

    //: func func_starCodeTime() {
    func domesticAnimal() {
        //: if self.hasFinishCheck {
        if self.hasFinishCheck {
            //: return
            return
        }
        //: if timerDispose != nil {
        if timerDispose != nil {
            //: timerDispose?.dispose()
            timerDispose?.dispose()
        }
        //: timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
        timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] num in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.func_requestHost()
                self.fileMedium()
                //: })
            })
    }

    //: func func_endCodeTime() {
    func arrowBy() {
        //: timerDispose?.dispose()
        timerDispose?.dispose()
    }

    //: func func_requestHost() {
    func fileMedium() {
        //: if DismissReactiveCompatible.share.networkStatus == .Unavailable {
        if DismissReactiveCompatible.share.networkStatus == .Unavailable {
            //: return
            return
        }
        //: func_endCodeTime()
        arrowBy()
        //: AppManagerRequest.func__initRequestHost { [weak self] succeed, result, errorModel in
        RevenueMovementManagerRequest.pull { [weak self] succeed, _, _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: self.hasFinishCheck = true
                self.hasFinishCheck = true
                //: printLog(message: "完成了检测,请求状态正常")
                printLog(message: (kCustomKey + "\u{6d4b},请\u{6c42}\u{72b6}态\u{6b63}常"))
                //: } else {
            } else {
                //: printLog(message: "接口请求失败")
                printLog(message: (String(appFutureThreeName)))
                //: if self.hasFinishCheck {return}
                if self.hasFinishCheck { return }
                //: self.hostCheckNum += 1
                self.hostCheckNum += 1
                //: if self.hostCheckNum < 3 {
                if self.hostCheckNum < 3 {
                    //: self.func_starCodeTime()
                    self.domesticAnimal()
                    //: } else {
                } else {
                    //: printLog(message: "完成了检测,需要更换server url")
                    printLog(message: (String(k_nagName) + appDistinctiveStr.replacingOccurrences(of: "speaker", with: "v") + String(noti_informLengthStr.prefix(6))))
                    //: self.func__changeHostUrl()
                    self.listUrl()
                }
            }
        }
    }

    //: func func__changeHostUrl() {
    func listUrl() {
        //: hostCheckNum = 0
        hostCheckNum = 0
        //: hasFinishCheck = true
        hasFinishCheck = true
        //: func_endCodeTime()
        arrowBy()

        //: serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        //: Defaults.set(serverSelctIndex, forKey: TalkingAddrServerUrlSelectIndexKey)
        data_partyText.set(serverSelctIndex, forKey: userBeginUrl)
        //: let serverDomain = serverUrlArr[serverSelctIndex]
        let serverDomain = serverUrlArr[serverSelctIndex]
        //: var serverPrefix = "http://"
        var serverPrefix = (String(const_produceMsg.prefix(7)))
        //: if Environment_Formal && !Encryption_Request {
        if kAddId, !notiMarginPath {
            //: serverPrefix = "http://"
            serverPrefix = (String(const_produceMsg.prefix(7)))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
    }
}

// MARK: - 判断用户设备是否开启VPN

//: extension TalkingRequestAddrTool {
public extension ManufacturerThen {
    /// 判断用户设备是否开启代理
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedProxy() -> Bool {
    class func outIndicatorWeekly() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let HTTPProxy = dict["HTTPProxy"] as? String else { return false }
        guard let HTTPProxy = dict[(String(dataHoneySoccerKey.suffix(6)) + String(notiLeadingStr))] as? String else { return false }
        //: if (HTTPProxy.count > 0) {
        if HTTPProxy.count > 0 {
            //: return true
            return true
        }
        //: return false
        return false
    }

    /// 判断用户设备是否开启代理VPN
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedVPN() -> Bool {
    class func signSub() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let scopedDic = dict["__SCOPED__"] as? [String: Any] else { return false }
        guard let scopedDic = dict[(String(k_cordId.suffix(5)) + "PED__")] as? [String: Any] else { return false }
        //: for keyStr in scopedDic.keys {
        for keyStr in scopedDic.keys {
            //: if keyStr.contains("tap") || keyStr.contains("tun") || keyStr.contains("ipsec") || keyStr.contains("ppp"){
            if keyStr.contains((app_stepUrl.lowercased())) || keyStr.contains((app_mysteryTitle.replacingOccurrences(of: "view", with: "un"))) || keyStr.contains((dataSubmitName.replacingOccurrences(of: "speak", with: "ip"))) || keyStr.contains((String(const_workThinkMessage))) {
                //: return true
                return true
            }
        }
        //: return false
        return false
    }
}

//: extension TalkingRequestAddrTool {
extension ManufacturerThen {
    //: func func__updateTalkingCookies(cookies: Array<HTTPCookie>) {
    func bringForth(cookies: [HTTPCookie]) {
        //: printLog(message: "func__updateTalkingCookies = \(cookies)")
        printLog(message: (String(k_boxFormat)) + "\(cookies)")
        //: var tempCookies: Array<HTTPCookie> = cookies
        var tempCookies: [HTTPCookie] = cookies
        //: if cookies.count == 0 {
        if cookies.count == 0 {
            //: tempCookies = func__getCookiesFromOldServerUrl()
            tempCookies = wayRecoupUrl()
        }
        //: if tempCookies.count == 0 {
        if tempCookies.count == 0 {
            //: return
            return
        }
        //: var tempUrlArr: Array<URL> = []
        var tempUrlArr: [URL] = []

        //: if !self.serverUrlStr.isEmptyString {
        if !self.serverUrlStr.isEmptyString {
            //: tempUrlArr.append(URL.init(string: self.serverUrlStr)!)
            tempUrlArr.append(URL(string: self.serverUrlStr)!)
        }

        //: var urlDomain: String = DismissReactiveCompatible.share.appConfigMode.urlH5Domain
        var urlDomain: String = DismissReactiveCompatible.share.appConfigMode.urlH5Domain
        //: urlDomain = urlDomain.replacingOccurrences(of: "http://", with: "https://")
        urlDomain = urlDomain.replacingOccurrences(of: (String(const_produceMsg.prefix(7))), with: (user_helpData.replacingOccurrences(of: "qualify", with: "p") + "s://"))
        //: if urlDomain.hasPrefix("https://") {
        if urlDomain.hasPrefix((user_helpData.replacingOccurrences(of: "qualify", with: "p") + "s://")) {
            //: tempUrlArr.append(URL.init(string: urlDomain)!)
            tempUrlArr.append(URL(string: urlDomain)!)
            //: } else {
        } else {
            //: tempUrlArr.append(URL.init(string: "https://\(urlDomain)")!)
            tempUrlArr.append(URL(string: (String(data_powMemberKey.prefix(8))) + "\(urlDomain)")!)
        }

        //: var addCookieArr: Array<HTTPCookie> = []
        var addCookieArr: [HTTPCookie] = []
        //: for cookies in tempCookies {
        for cookies in tempCookies {
            //: var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            //: for url in tempUrlArr {
            for url in tempUrlArr {
                //: cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                //: cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                //: let newCookie = HTTPCookie.init(properties: cookieInfo)
                let newCookie = HTTPCookie(properties: cookieInfo)
                //: if newCookie != nil {
                if newCookie != nil {
                    //: addCookieArr.append(newCookie!)
                    addCookieArr.append(newCookie!)
                }
            }
        }
        //: for cookies in addCookieArr {
        for cookies in addCookieArr {
            //: HTTPCookieStorage.shared.setCookie(cookies)
            HTTPCookieStorage.shared.setCookie(cookies)
        }

        //: for url in tempUrlArr {
        for url in tempUrlArr {
            //: func__deleteSameCookiesWithUrl(url: url)
            suiteUrl(url: url)
        }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            }
        }

        //: func__saveAllCookies()
        thirdOf()
    }

    //: func func__deleteSameCookiesWithUrl(url: URL) {
    func suiteUrl(url: URL) {
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var saveDic: Dictionary<String, String> = [:]
        var saveDic: [String: String] = [:]
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: if !cookies.name.isEmptyString {
            if !cookies.name.isEmptyString {
                //: let saveKey = cookies.name
                let saveKey = cookies.name
                //: if saveDic.keys.contains(saveKey) {
                if saveDic.keys.contains(saveKey) {
                    //: printLog(message: "func__deleteSameCookiesWithUrl = \(cookies.name)")
                    printLog(message: String(bytes: user_clickAddUrl.map{$0^102}, encoding: .utf8)! + "\(cookies.name)")
                    //: HTTPCookieStorage.shared.deleteCookie(cookies)
                    HTTPCookieStorage.shared.deleteCookie(cookies)
                    //: } else {
                } else {
                    //: saveDic[saveKey] = "1"
                    saveDic[saveKey] = "1"
                }
            }
        }
    }

    //: func func__getCookiesFromOldServerUrl() -> Array<HTTPCookie> {
    func wayRecoupUrl() -> [HTTPCookie] {
        //: var serverPrefix = "http://"
        var serverPrefix = (String(const_produceMsg.prefix(7)))
        //: if Environment_Formal && !Encryption_Request {
        if kAddId, !notiMarginPath {
            //: serverPrefix = "https://"
            serverPrefix = (user_helpData.replacingOccurrences(of: "qualify", with: "p") + "s://")
        }
        //: let fullDomain = serverPrefix.appending("app.\(ReplaceUrlDomain).com")
        let fullDomain = serverPrefix.appending((String(constWouldMessage)) + "\(showReasonContent)" + (String(app_absoluteFormat)))
        //: let oldCookies = HTTPCookieStorage.shared.cookies(for: URL.init(string: fullDomain)!)
        let oldCookies = HTTPCookieStorage.shared.cookies(for: URL(string: fullDomain)!)
        //: return oldCookies!
        return oldCookies!
    }

    //: @objc func cookieChanged(note: Notification) {
    @objc func blindThe(note _: Notification) {
        //: if DismissReactiveCompatible.share.loginUid.isEmptyString {
        if DismissReactiveCompatible.share.loginUid.isEmptyString {
            //: return
            return
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: checkHtmlCookieIfNeedCurrentVersion()
            overleap()
            //: } else {
        } else {
            //: checkHtmlCookieIfNeedEarlyVersion()
            libraryBold()
        }
    }

    //: func checkHtmlCookieIfNeedCurrentVersion() {
    func overleap() {
        //: var htmlDomain: String = DismissReactiveCompatible.share.appConfigMode.urlH5Domain
        var htmlDomain: String = DismissReactiveCompatible.share.appConfigMode.urlH5Domain

        //: if !htmlDomain.isEmptyString {
        if !htmlDomain.isEmptyString {
            //: let url = URL.init(string: htmlDomain)
            let url = URL(string: htmlDomain)
            //: htmlDomain = url?.host ?? ""
            htmlDomain = url?.host ?? ""
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var appCookie: [String: HTTPCookie] = Dictionary()
            //: var h5Cookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var h5Cookie: [String: HTTPCookie] = Dictionary()

            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr
                //: let temphtmlDomainStr = htmlDomain
                let temphtmlDomainStr = htmlDomain
                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (String(userMinStr)) {
                        //: appCookie["UID"] = cookie
                        appCookie[(String(userMinStr))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (String(const_recognizeMessage)) {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(String(const_recognizeMessage))] = cookie
                    }

                    //: } else if temphtmlDomainStr.contains(cookie.domain) {
                } else if temphtmlDomainStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (String(userMinStr)) {
                        //: h5Cookie["UID"] = cookie
                        h5Cookie[(String(userMinStr))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (String(const_recognizeMessage)) {
                        //: h5Cookie["PHPSESSID"] = cookie
                        h5Cookie[(String(const_recognizeMessage))] = cookie
                    }
                }
                //: var cookieEqual = true
                var cookieEqual = true
                //: if appCookie.keys.count != h5Cookie.keys.count {
                if appCookie.keys.count != h5Cookie.keys.count {
                    //: cookieEqual = false
                    cookieEqual = false
                    //: } else {
                } else {
                    //: if appCookie.keys.contains("UID") && h5Cookie.keys.contains("UID") {
                    if appCookie.keys.contains((String(userMinStr))), h5Cookie.keys.contains((String(userMinStr))) {
                        //: let appUid: HTTPCookie = appCookie["UID"]!
                        let appUid: HTTPCookie = appCookie[(String(userMinStr))]!
                        //: let h5Uid: HTTPCookie = h5Cookie["UID"]!
                        let h5Uid: HTTPCookie = h5Cookie[(String(userMinStr))]!

                        //: if h5Uid.value != appUid.value {
                        if h5Uid.value != appUid.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                    //: if appCookie.keys.contains("PHPSESSID") && h5Cookie.keys.contains("PHPSESSID") {
                    if appCookie.keys.contains((String(const_recognizeMessage))), h5Cookie.keys.contains((String(const_recognizeMessage))) {
                        //: let appPHPSESSID: HTTPCookie = appCookie["PHPSESSID"]!
                        let appPHPSESSID: HTTPCookie = appCookie[(String(const_recognizeMessage))]!
                        //: let h5PHPSESSID: HTTPCookie = h5Cookie["PHPSESSID"]!
                        let h5PHPSESSID: HTTPCookie = h5Cookie[(String(const_recognizeMessage))]!
                        //: if appPHPSESSID.value != h5PHPSESSID.value {
                        if appPHPSESSID.value != h5PHPSESSID.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                }
                //: if !cookieEqual {
                if !cookieEqual {
                    //: for (key, _) in appCookie {
                    for (key, _) in appCookie {
                        //: let cookie: HTTPCookie = appCookie[key]!
                        let cookie: HTTPCookie = appCookie[key]!
                        //: var cookiePro: Dictionary<HTTPCookiePropertyKey, Any> = cookie.properties!
                        var cookiePro: [HTTPCookiePropertyKey: Any] = cookie.properties!
                        //: cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        //: cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: cookiePro)!)
                        HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: cookiePro)!)
                    }
                }
            }
        }
    }

    //: func checkHtmlCookieIfNeedEarlyVersion() {
    func libraryBold() {
        //: let htmlDomain: String = DismissReactiveCompatible.share.appConfigMode.urlH5Domain
        let htmlDomain: String = DismissReactiveCompatible.share.appConfigMode.urlH5Domain
        //: let url: URL = URL.init(string: htmlDomain)!
        let url = URL(string: htmlDomain)!
        //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var hasUID = false
        var hasUID = false
        //: var hasPHPSSID = false
        var hasPHPSSID = false
        //: for cookie in cookieArr {
        for cookie in cookieArr {
            //: if cookie.name == "UID" && cookie.value == DismissReactiveCompatible.share.loginUserMode.userID {
            if cookie.name == (String(userMinStr)), cookie.value == DismissReactiveCompatible.share.loginUserMode.userID {
                //: hasUID = true
                hasUID = true
            }
            //: if cookie.name == "PHPSESSID" {
            if cookie.name == (String(const_recognizeMessage)) {
                //: hasPHPSSID = true
                hasPHPSSID = true
            }
        }
        //: if !hasUID || !hasPHPSSID {
        if !hasUID || !hasPHPSSID {
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, Any> =  Dictionary()
            var appCookie: [String: Any] = Dictionary()
            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr

                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (String(userMinStr)) {
                        //: appCookie["UID"] = cookie
                        appCookie[(String(userMinStr))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (String(const_recognizeMessage)) {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(String(const_recognizeMessage))] = cookie
                    }
                }
                //: if !hasUID && appCookie.keys.contains("UID") {
                if !hasUID, appCookie.keys.contains((String(userMinStr))) {
                    //: let uidCookie: HTTPCookie = appCookie["UID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(String(userMinStr))] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
                //: if !hasPHPSSID && appCookie.keys.contains("PHPSESSID") {
                if !hasPHPSSID, appCookie.keys.contains((String(const_recognizeMessage))) {
                    //: let uidCookie: HTTPCookie = appCookie["PHPSESSID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(String(const_recognizeMessage))] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
            }
        }
    }

    //: func func__saveAllCookies() {
    func thirdOf() {
        //: do {
        do {
            //: let cookies: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: printLog(message: "func__saveAllCookies = \(cookies)")
            printLog(message: (String(dataTailAnyoneContent) + String(showParentMotiveData)) + "\(cookies)")
            //: let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            //: Defaults.set(cookieData, forKey: TalkingUserCookiesKey)
            data_partyText.set(cookieData, forKey: mainThirdUrl)
            //: } catch _ {
        } catch _ {
            //: printLog(message: "func__saveAllCookies  error")
            printLog(message: (String(constDotKey.prefix(6)) + "saveA" + String(appBroadcastTitle.prefix(4)) + "okies  " + dataSunId.replacingOccurrences(of: "load", with: "rr")))
        }
    }

    //: func func__setAppCookies() {
    func streamCurrency() {
        //: let data = Defaults.object(forKey: TalkingUserCookiesKey)
        let data = data_partyText.object(forKey: mainThirdUrl)
        //: if data != nil {
        if data != nil {
            //: let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            //: for cookie in data2 {
            for cookie in data2 {
                //: HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
                HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
            }
        }
    }

    //: func func__removeAllCookies() {
    func causalAgent() {
        //: Defaults.removeObject(forKey: TalkingUserCookiesKey)
        data_partyText.removeObject(forKey: mainThirdUrl)
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: printLog(message: "🍪 删除\(cookies.name)")
            printLog(message: "🍪 删除\(cookies.name)")
            //: HTTPCookieStorage.shared.deleteCookie(cookies)
            HTTPCookieStorage.shared.deleteCookie(cookies)
        }
    }
}
