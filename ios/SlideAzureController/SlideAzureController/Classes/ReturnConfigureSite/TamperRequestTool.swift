
//: Declare String Begin

/*: "mf/business/giftList" :*/
fileprivate let user_collectTitle:[Character] = ["m","f","/","b","u","s","i"]
fileprivate let kStepData:String = "possiblee"
fileprivate let showSavePath:String = "ss/gishot bad image period tail"

/*: "type" :*/
fileprivate let k_finiteText:[UInt8] = [0x47,0x4a,0x43,0x56]

private func anyoneFlat(actual num: UInt8) -> UInt8 {
    return num ^ 51
}

/*: "mf/chatRoom/giftList" :*/
fileprivate let showTogetherMessage:[Character] = ["m","f","/","c","h","a","t","R","o","o","m","/","g"]
fileprivate let appFailureKey:String = "ibold"
fileprivate let show_stripName:String = "tListend his bullet"

/*: "live/giftList" :*/
fileprivate let notiSecureKey:[Character] = ["l","i","v","e","/","g"]
fileprivate let appRestrictionText:String = "spend there creation containiftList"

/*: "IM/getMessageList" :*/
fileprivate let k_lackMsg:[Character] = ["I","M","/","g","e","t","M","e","s","s","a","g","e","L","i","s","t"]

/*: "mf/business/sendMsg" :*/
fileprivate let showStateData:String = "mf/brem aid"
fileprivate let show_manAppropriateContent:[Character] = ["s","s"]
fileprivate let data_liveBigName:String = "sense decrease skin ethical/sendMsg"

/*: "mf/chatRoom/sendMsg" :*/
fileprivate let k_styleMessage:[Character] = ["m","f","/","c","h","a","t","R","o","o","m","/","s","e","n","d","M","s"]
fileprivate let showHeadData:String = "environment"

/*: "mf/business/sendGift" :*/
fileprivate let mainReadyStr:String = "family example feedback outer viamf/bu"
fileprivate let dataQualityTitle:String = "upper threshold tone means/sendG"
fileprivate let const_stageMessage:[Character] = ["i","f","t"]

/*: "mf/chatRoom/sendGift" :*/
fileprivate let data_actualBeatId:[Character] = ["m","f","/","c","h","a","t","R","o","o","m","/"]
fileprivate let constProgramName:String = "severyoned"

/*: "live/sendGift" :*/
fileprivate let show_theValue:String = "livavailable"
fileprivate let appSpeedUrl:String = "/sendGiftpress coordinator catch ask"

/*: "mf/user/getChatInfo" :*/
fileprivate let data_announcementCapMessage:String = "mf/usefunction zone ya"
fileprivate let notiFeedValue:String = "access lovely offer info minuter/ge"
fileprivate let main_beginUrl:String = "Infoentry interval"

/*: "IM/getMessageUserInfoList" :*/
fileprivate let user_arrowFormat:[Character] = ["I","M","/","g","e","t","M","e","s","s"]
fileprivate let kComplaintMsg:String = "ageUnarrow show"
fileprivate let notiCountContent:[Character] = ["f","o","L","i","s","t"]

/*: "mf/crush/whoLikeMeList" :*/
fileprivate let show_interactionData:String = "mf/cruselectron count miss appear"
fileprivate let user_norFormat:String = "resume flat answerh/whoL"
fileprivate let kMeAwakeData:[Character] = ["i","k","e","M","e","L","i","s","t"]

/*: "mf/crush/ignore" :*/
fileprivate let const_waterTitle:[Character] = ["m","f","/","c","r"]
fileprivate let userThinText:[Character] = ["u","s","h","/","i","g","n","o","r","e"]

/*: "mf/crush/like" :*/
fileprivate let data_termsGroupId:String = "launch block disabled existmf/cr"
fileprivate let user_crystalKey:String = "connect"

/*: "mf/user/getRelation" :*/
fileprivate let notiDetectValue:[Character] = ["m","f","/","u","s","e","r","/"]
fileprivate let user_readyMessage:String = "property"
fileprivate let noti_chinTitle:[Character] = ["e","t","R","e","l","a","t","i","o","n"]

/*: "user/attention" :*/
fileprivate let noti_storyFastMessage:[UInt8] = [0x36,0x30,0x26,0x31,0x6c,0x22,0x37,0x37,0x26,0x2d,0x37,0x2a,0x2c,0x2d]

private func layMagnitudeerrupt(visible num: UInt8) -> UInt8 {
    return num ^ 67
}

/*: "user/removeAttention" :*/
fileprivate let data_parentStr:[Character] = ["u","s","e","r","/","r","e","m","o","v","e","A","t","t","e","n","t"]
fileprivate let user_fillStr:[Character] = ["i","o","n"]

/*: "Can't earn points if you follow each other~" :*/
fileprivate let notiDifferenceMessage:[UInt8] = [0xe9,0xcb,0xc4,0x8d,0xde,0x8a,0xcf,0xcb,0xd8,0xc4,0x8a,0xda,0xc5,0xc3,0xc4,0xde,0xd9,0x8a,0xc3,0xcc,0x8a,0xd3,0xc5,0xdf,0x8a,0xcc,0xc5,0xc6,0xc6,0xc5,0xdd,0x8a,0xcf,0xcb,0xc9,0xc2,0x8a,0xc5,0xde,0xc2,0xcf,0xd8,0xd4]

private func waveJust(burn num: UInt8) -> UInt8 {
    return num ^ 170
}

/*: "follow Success" :*/
fileprivate let mainBirdMsg:String = "follow depth focus half violation"
fileprivate let kRipTitle:String = "session force scriptSuccess"

/*: "unfollow Success" :*/
fileprivate let user_soleMessage:String = "unfonag"
fileprivate let kBroadKey:String = "messagemessageess"

/*: "translate/msg" :*/
fileprivate let mainHimEnableicialValue:String = "traconfirm"
fileprivate let kTaskMsg:String = "data nonee/msg"

/*: "msgId" :*/
fileprivate let mainBandPanEvaluationName:[UInt8] = [0x42,0x5c,0x48,0x66,0x4b]

/*: "targetLng" :*/
fileprivate let data_normalMsg:[UInt8] = [0x7e,0x6b,0x78,0x6d,0x6f,0x7e,0x46,0x64,0x6d]

private func usGather(brush num: UInt8) -> UInt8 {
    return num ^ 10
}

/*: "mf/greet/getAutoGreetContent" :*/
fileprivate let user_anonymousMessage:String = "awake delaymf/gr"
fileprivate let show_sayMsg:String = "pow warn yet welcome maybetAut"
fileprivate let showCosMustText:String = "exclusive lab two ion elementtCon"
fileprivate let dataMakeContent:String = "surgerynt"

/*: "mf/greet/getIntimatePhoto" :*/
fileprivate let constVanishMsg:[Character] = ["m","f","/","g","r","e"]
fileprivate let data_exactlyWorldFormat:String = "shift end physical oret/ge"
fileprivate let noti_successfullyPath:[Character] = ["t","I","n","t","i","m","a","t","e","P","h","o","t","o"]

/*: "toUid" :*/
fileprivate let userPlusStr:[UInt8] = [0xfa,0xe1,0xdb,0xe7,0xea]

private func hiToward(better num: UInt8) -> UInt8 {
    return num ^ 142
}

/*: "mf/chatRoom/info" :*/
fileprivate let app_loopIndicatorMsg:String = "remind route think put springmf/ch"
fileprivate let constBeanRelateFormat:String = "second timing child super gapom/info"

/*: "mf/chatRoom/list" :*/
fileprivate let k_limitedStr:String = "brow drag move identitymf/c"
fileprivate let data_masterUrl:[Character] = ["h","a","t","R","o","o","m","/","l","i","s","t"]

/*: "roomId" :*/
fileprivate let kCountervalUrl:[UInt8] = [0xc8,0xc5,0xc5,0xc3,0x9f,0xba]

fileprivate func lipSolar(con num: UInt8) -> UInt8 {
    let value = Int(num) - 86
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "needExtraInfo" :*/
fileprivate let kPlayerKey:[UInt8] = [0x9b,0x90,0x90,0x91,0xb0,0x8d,0x81,0x87,0x94,0xbc,0x9b,0x93,0x9a]

private func outStatistical(accommodate num: UInt8) -> UInt8 {
    return num ^ 245
}

/*: "mf/chatRoom/check" :*/
fileprivate let show_profileTitle:[Character] = ["m","f","/","c","h","a","t","R"]
fileprivate let notiDiscPath:String = "oom/chouse err"

/*: "mf/chatRoom/joinOrQuit" :*/
fileprivate let userAdviceMessage:String = "detail soccermf/ch"
fileprivate let const_rankDestroyData:[Character] = ["o","i","n","O","r","Q","u","i","t"]

/*: "mf/chatRoom/members" :*/
fileprivate let appMyFormat:String = "tun justicemf/cha"
fileprivate let user_observeDestinationStr:String = "m/mewant compound shift living press"

/*: "translate/text" :*/
fileprivate let notiInformationName:String = "TRAN"
fileprivate let user_builderName:String = "e/telose personal string"
fileprivate let show_dataBlowMsg:[Character] = ["x","t"]

/*: "text" :*/
fileprivate let appRunnerFormat:[UInt8] = [0x74,0x78,0x65,0x74]

/*: "mf/user/queryStatus" :*/
fileprivate let data_dateFormat:String = "soon count fire sequence beautymf/us"
fileprivate let appAdminExtendFormat:String = "makeery"
fileprivate let appMemberUrl:[Character] = ["S","t","a","t","u","s"]

/*: "toUids" :*/
fileprivate let kDocDenyName:[UInt8] = [0x69,0x72,0x48,0x74,0x79,0x6e]

private func mergeDoingce(recent num: UInt8) -> UInt8 {
    return num ^ 29
}

/*: "call/getCallTab" :*/
fileprivate let main_cordName:String = "call/geshake character search hello"
fileprivate let constReplaceFormat:String = "capable circle pending minimizetCal"

/*: "mf/user/remark" :*/
fileprivate let userFrontText:[Character] = ["m","f","/","u","s","e","r","/","r","e","m"]
fileprivate let main_audienceUrl:String = "ARK"

/*: "content" :*/
fileprivate let dataPurchaseTitle:[UInt8] = [0x4,0x8,0x9,0x13,0x2,0x9,0x13]

/*: "top" :*/
fileprivate let notiAuthorityDayDatabaseUrl:[UInt8] = [0x19,0x14,0x15]

fileprivate func accelerateWeFun(installation num: UInt8) -> UInt8 {
    let value = Int(num) - 165
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  TamperRequestTool.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/7.
//

//: import UIKit
import UIKit

//: public class TalkingChatRequestTool: NSObject {
public class TamperRequestTool: NSObject {
    //: public typealias RequestEndBlock = (_ t: Bool) -> Void
    public typealias RequestEndBlock = (_ t: Bool) -> Void
    //: typealias FinishBlocks = (_ succeed: Bool, _ result: NSDictionary) -> Void
    typealias FinishBlocks = (_ succeed: Bool, _ result: NSDictionary) -> Void
    //: public typealias RequestMsgUserListBlock = (_ succeed: Bool, _ dataArr: [ExecuteBetterTransformable]) -> Void
    public typealias RequestMsgUserListBlock = (_ succeed: Bool, _ dataArr: [ExecuteBetterTransformable]) -> Void

    /// 私信礼物面板
    /// - Parameters:
    ///   - type: 0=默认， 1=私密照片，2=私密视频,3=节目单
    ///   - completion: 回调
    //: class func req_ChatGiftList(type: String, completion: @escaping FinishBlock) {
    class func aboutContainer(type: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "mf/business/giftList"
        reqModel.requestPath = (String(user_collectTitle) + kStepData.replacingOccurrences(of: "possible", with: "n") + String(showSavePath.prefix(5)) + "ftList")
        //: reqModel.params = ["type": type]
        reqModel.params = [String(bytes: k_finiteText.map{anyoneFlat(actual: $0)}, encoding: .utf8)!: type]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室礼物面板
    /// - Parameters：
    ///   - completion: 回调
    //: class func req_ChatRoomGiftList(type: String, completion: @escaping FinishBlock) {
    class func salve(type _: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "mf/chatRoom/giftList"
        reqModel.requestPath = (String(showTogetherMessage) + appFailureKey.replacingOccurrences(of: "bold", with: "f") + String(show_stripName.prefix(5)))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 直播礼物面板
    /// - Parameters：
    ///   - completion: 回调
    //: class func req_LiveGiftList(type: String, completion: @escaping FinishBlock) {
    class func someone(type _: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "live/giftList"
        reqModel.requestPath = (String(notiSecureKey) + String(appRestrictionText.suffix(7)))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求系统消息
    /// - Parameter completion: 回调
    //: @objc class func req_sysMessageList(completion: @escaping FinishBlocks) {
    @objc class func completion(completion: @escaping FinishBlocks) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "IM/getMessageList"
        reqModel.requestPath = (String(k_lackMsg))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, _ in
            //: if succeed {
            if succeed {
                //: completion(true, result as! NSDictionary)
                completion(true, result as! NSDictionary)
            }
        }
    }

    /// 私聊发送消息
    /// - Parameters:
    ///   - param: 传参
    ///   - completion: 回调
    //: class func req_SendPrivete(param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func destinationIt(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/business/sendMsg"
        reqModel.requestPath = (String(showStateData.prefix(4)) + "usine" + String(show_manAppropriateContent) + String(data_liveBigName.suffix(8)))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 公共聊天室发送消息
    /// - Parameters:
    ///   - param: 传参
    ///   - completion: 回调
    //: class func req_SendRoomMsg(param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func livePrompt(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ProcessingThen()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/chatRoom/sendMsg"
        reqModel.requestPath = (String(k_styleMessage) + showHeadData.replacingOccurrences(of: "environment", with: "g"))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftPrivete( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func underSendReply(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/business/sendGift"
        reqModel.requestPath = (String(mainReadyStr.suffix(5)) + "sines" + String(dataQualityTitle.suffix(7)) + String(const_stageMessage))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftChatRoom( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func financialAid(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/chatRoom/sendGift"
        reqModel.requestPath = (String(data_actualBeatId) + constProgramName.replacingOccurrences(of: "everyone", with: "en") + "Gift")
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 直播送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftLive( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func microscopic(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "live/sendGift"
        reqModel.requestPath = (show_theValue.replacingOccurrences(of: "available", with: "e") + String(appSpeedUrl.prefix(9)))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求聊天用户信息
    /// - Parameters:
    //: class func req_refreshUserChatInfo( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func lipClub(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "mf/user/getChatInfo"
        reqModel.requestPath = (String(data_announcementCapMessage.prefix(6)) + String(notiFeedValue.suffix(4)) + "tChat" + String(main_beginUrl.prefix(4)))
        //: reqModel.params = param
        reqModel.params = param
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求用户消息列表
    /// - Parameters:
    //: @objc class public func req_refreshMsgUserListInfo( param: Dictionary<String, Any>, completion: @escaping RequestMsgUserListBlock) {
    @objc public class func immobilizeHandle(param: [String: Any], completion: @escaping RequestMsgUserListBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "IM/getMessageUserInfoList"
        reqModel.requestPath = (String(user_arrowFormat) + String(kComplaintMsg.prefix(4)) + "serIn" + String(notiCountContent))
        //: reqModel.params = param
        reqModel.params = param
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, _ in
            //: guard succeed else {
            guard succeed else {
                //: completion(succeed, [])
                completion(succeed, [])
                //: return
                return
            }

            //: if let datas = Array<ExecuteBetterTransformable>.deserialize(from: result as? Array) {
            if let datas = Array<ExecuteBetterTransformable>.deserialize(from: result as? Array) {
                //: completion(succeed, datas as! [ExecuteBetterTransformable])
                completion(succeed, datas as! [ExecuteBetterTransformable])
            }
        }
    }

    /// 请求用户消息列表
    //: @objc class public func req_giftEffectDataUrl(giftUrl: String, path: String, completion: @escaping RequestEndBlock) {
    @objc public class func interval(giftUrl: String, path: String, completion: @escaping RequestEndBlock) {
        //: GJ.startGiftZipRequest(giftUrl: giftUrl, path: path) { succeed, result, errorModel in
        const_backRateStr.proclamationObject(giftUrl: giftUrl, path: path) { succeed, _, _ in
            //: completion(succeed)
            completion(succeed)
        }
    }

    /// 谁心动我 - 列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMeList(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func originalCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "mf/crush/whoLikeMeList"
        reqModel.requestPath = (String(show_interactionData.prefix(7)) + String(user_norFormat.suffix(6)) + String(kMeAwakeData))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - nope拒绝操作
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMeIgnore(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func countryQuantityo(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "mf/crush/ignore"
        reqModel.requestPath = (String(const_waterTitle) + String(userThinText))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - like操作
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMelike(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func blindThree(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "mf/crush/like"
        reqModel.requestPath = (String(data_termsGroupId.suffix(5)) + "ush/lik" + user_crystalKey.replacingOccurrences(of: "connect", with: "e"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 关注列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_atationList(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func mapEnable(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "mf/user/getRelation"
        reqModel.requestPath = (String(notiDetectValue) + user_readyMessage.replacingOccurrences(of: "property", with: "g") + String(noti_chinTitle))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 关注操作
    /// - Parameters:
    /// - isAttention: 关注/取消
    ///   - completion: 回调
    //: class func req_atationTool(isAttention: Bool, params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func filing(isAttention: Bool, params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: let path = isAttention==true ? "user/attention" :"user/removeAttention"
        let path = isAttention == true ? String(bytes: noti_storyFastMessage.map{layMagnitudeerrupt(visible: $0)}, encoding: .utf8)! : (String(data_parentStr) + String(user_fillStr))
        //: reqModel.requestPath = path
        reqModel.requestPath = path
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            // 统一处理toast
            //: if succeed {
            if succeed {
                //: if isAttention {
                if isAttention {
                    //: if DismissReactiveCompatible.share.loginUserMode.sex == Gender.female.rawValue {
                    if DismissReactiveCompatible.share.loginUserMode.sex == FenceSequence.female.rawValue { // 女性
                        //: object.func__showStatusBarSuccessMsg(showMsg: "Can't earn points if you follow each other~".localized)
                        userTablePostMessage.showGuidanceName(showMsg: String(bytes: notiDifferenceMessage.map{waveJust(burn: $0)}, encoding: .utf8)!.localized)
                        //: } else {
                    } else {
                        //: object.func__showStatusBarSuccessMsg(showMsg: "follow Success".localized)
                        userTablePostMessage.showGuidanceName(showMsg: (String(mainBirdMsg.prefix(7)) + String(kRipTitle.suffix(7))).localized)
                    }
                    //: } else {
                } else {
                    //: object.func__showStatusBarSuccessMsg(showMsg: "unfollow Success".localized)
                    userTablePostMessage.showGuidanceName(showMsg: (user_soleMessage.replacingOccurrences(of: "nag", with: "l") + "low Su" + kBroadKey.replacingOccurrences(of: "message", with: "c")).localized)
                }

                //: } else {
            } else {
                //: object.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                userTablePostMessage.enablece(showMsg: errorModel?.errorMsg ?? "")
            }
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 文本翻译
    /// - Parameters:
    ///   - msgId: 消息ID
    ///   - type=0表示私聊消息，type=1表示聊天室消息
    ///   - completion: 回调
    //: @objc class public func req_translateMsg(msgId: String, type: Int, completion: @escaping (_ succeed: Bool, _ result: NSDictionary, _ errorCode: Int) -> Void) {
    @objc public class func moreAttribute(msgId: String, type: Int, completion: @escaping (_ succeed: Bool, _ result: NSDictionary, _ errorCode: Int) -> Void) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "translate/msg"
        reqModel.requestPath = (mainHimEnableicialValue.replacingOccurrences(of: "confirm", with: "n") + "slat" + String(kTaskMsg.suffix(5)))
        //: reqModel.params = ["msgId": msgId, "targetLng": DepictionThen.getSystemLangCode(), "type": "\(type)"]
        reqModel.params = [String(bytes: mainBandPanEvaluationName.map{$0^47}, encoding: .utf8)!: msgId, String(bytes: data_normalMsg.map{usGather(brush: $0)}, encoding: .utf8)!: DepictionThen.outline(), String(bytes: k_finiteText.map{anyoneFlat(actual: $0)}, encoding: .utf8)!: "\(type)"]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result as? NSDictionary ?? [:], errorModel?.errorCode ?? 0)
            completion(succeed, result as? NSDictionary ?? [:], errorModel?.errorCode ?? 0)
        }
    }

    /// 获取一键打招呼内容
    ///   - completion: 回调
    //: class func req_getAutoGreetContent(completion: @escaping FinishBlock) {
    class func inviteSubmit(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "mf/greet/getAutoGreetContent"
        reqModel.requestPath = (String(user_anonymousMessage.suffix(5)) + "eet/ge" + String(show_sayMsg.suffix(4)) + "oGree" + String(showCosMustText.suffix(4)) + dataMakeContent.replacingOccurrences(of: "surgery", with: "te"))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取随机私密照片
    ///   - completion: 回调
    //: class func req_getIntimatePhoto(toUid: String, completion: @escaping FinishBlock) {
    class func reportBean(toUid: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "mf/greet/getIntimatePhoto"
        reqModel.requestPath = (String(constVanishMsg) + String(data_exactlyWorldFormat.suffix(5)) + String(noti_successfullyPath))
        //: reqModel.params = ["toUid": toUid]
        reqModel.params = [String(bytes: userPlusStr.map{hiToward(better: $0)}, encoding: .utf8)!: toUid]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取聊天广场在线人数和家族信息
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomInfo(completion: @escaping FinishBlock) {
    class func atWork(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "mf/chatRoom/info"
        reqModel.requestPath = (String(app_loopIndicatorMsg.suffix(5)) + "atRo" + String(constBeanRelateFormat.suffix(7)))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天广场列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomList(completion: @escaping FinishBlock) {
    class func snug(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "mf/chatRoom/list"
        reqModel.requestPath = (String(k_limitedStr.suffix(4)) + String(data_masterUrl))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 检测聊天室
    /// - Parameters:
    ///   - roomId: 聊天室Id
    ///   - completion: 回调
    //: class func req_checkChatRoom(roomId: String, completion: @escaping FinishBlock) {
    class func halfCompletion(roomId: String, completion: @escaping FinishBlock) {
        //: let params = ["roomId": roomId, "needExtraInfo": "1"]
        let params = [String(bytes: kCountervalUrl.map{lipSolar(con: $0)}, encoding: .utf8)!: roomId, String(bytes: kPlayerKey.map{outStatistical(accommodate: $0)}, encoding: .utf8)!: "1"]
        //: let reqModel = TalkingRequestModel()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "mf/chatRoom/check"
        reqModel.requestPath = (String(show_profileTitle) + String(notiDiscPath.prefix(5)) + "heck")
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 进入或退出聊天室
    /// - Parameters:
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_chatRoomJoinOrQuit(params: [String: Any], completion: @escaping FinishBlock) {
    class func menuCompute(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "mf/chatRoom/joinOrQuit"
        reqModel.requestPath = (String(userAdviceMessage.suffix(5)) + "atRoom/j" + String(const_rankDestroyData))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室在线名单
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomMembers(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func searchIsolate(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "mf/chatRoom/members"
        reqModel.requestPath = (String(appMyFormat.suffix(6)) + "tRoo" + String(user_observeDestinationStr.prefix(4)) + "mbers")
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 文本翻译
    /// - Parameters:
    ///   - text: 文本
    ///   - completion: 回调
    //: class func req_translateText(text: String, completion: @escaping FinishBlock) {
    class func littler(text: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ProcessingThen()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "translate/text"
        reqModel.requestPath = (notiInformationName.lowercased() + "slat" + String(user_builderName.prefix(4)) + String(show_dataBlowMsg))
        //: reqModel.params = ["text": text]
        reqModel.params = [String(bytes: appRunnerFormat.reversed(), encoding: .utf8)!: text]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 批量查询消息列表用户在线状态
    /// - Parameters:
    ///   - toUids: 用户ids
    ///   - completion: 回调
    //: @objc class public func req_queryStatus(toUids: String, completion: @escaping (_ succeed: Bool, _ result: [[String: Any]]?) -> Void) {
    @objc public class func connectiveTissue(toUids: String, completion: @escaping (_ succeed: Bool, _ result: [[String: Any]]?) -> Void) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ProcessingThen()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/user/queryStatus"
        reqModel.requestPath = (String(data_dateFormat.suffix(5)) + "er/q" + appAdminExtendFormat.replacingOccurrences(of: "make", with: "u") + String(appMemberUrl))
        //: reqModel.params = ["toUids": toUids, "type": 1]
        reqModel.params = [String(bytes: kDocDenyName.map{mergeDoingce(recent: $0)}, encoding: .utf8)!: toUids, String(bytes: k_finiteText.map{anyoneFlat(actual: $0)}, encoding: .utf8)!: 1]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, _ in
            //: completion(succeed, result as? [[String: Any]] ?? [])
            completion(succeed, result as? [[String: Any]] ?? [])
        }
    }

    /// 拨打通话记录
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getCallTab(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func transitionCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "call/getCallTab"
        reqModel.requestPath = (String(main_cordName.prefix(7)) + String(constReplaceFormat.suffix(4)) + "lTab")
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 新增/修改备注
    /// - Parameters:
    ///   - content: 备注内容
    ///   - isTop: 是否置顶，1：是，2：否
    ///   - toUid: 对方uid
    ///   - completion: 回调
    //: class func req_remark(content: String, isTop: Int, toUid: String, completion: @escaping FinishBlock) {
    class func perCompletion(content: String, isTop: Int, toUid: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "mf/user/remark"
        reqModel.requestPath = (String(userFrontText) + main_audienceUrl.lowercased())
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = ["content": content, "top": isTop, "toUid": toUid]
        reqModel.params = [String(bytes: dataPurchaseTitle.map{$0^103}, encoding: .utf8)!: content, String(bytes: notiAuthorityDayDatabaseUrl.map{accelerateWeFun(installation: $0)}, encoding: .utf8)!: isTop, String(bytes: userPlusStr.map{hiToward(better: $0)}, encoding: .utf8)!: toUid]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }
}
