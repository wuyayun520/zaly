
//: Declare String Begin

/*: "^([a-zA-Z0-9_\\.-]+)@([\\da-z\\.-]+)\\.([a-z\\.]{2,6})$" :*/
fileprivate let showDonData:[UInt8] = [0x66,0x10,0x63,0x59,0x15,0x42,0x79,0x15,0x62,0x8,0x15,0x1,0x67,0x64,0x16,0x15,0x65,0x13,0x11,0x78,0x10,0x63,0x64,0x5c,0x59,0x15,0x42,0x64,0x16,0x15,0x65,0x13,0x11,0x64,0x16,0x10,0x63,0x59,0x15,0x42,0x64,0x16,0x65,0x43,0xa,0x14,0xe,0x45,0x11,0x1c]

/*: "^((1[34578][0-9]{9})|((0\\d{2}-\\d{8})|(0\\d{3}-\\d{7,8})|(0\\d{10,11}))$" :*/
fileprivate let app_recommendationData:[UInt8] = [0xb7,0xc1,0xc1,0xd8,0xb2,0xda,0xdd,0xdc,0xde,0xd1,0xb4,0xb2,0xd9,0xc4,0xd0,0xb4,0x92,0xd0,0x94,0xc0,0x95,0xc1,0xc1,0xd9,0xb5,0x8d,0x92,0xdb,0x94,0xc4,0xb5,0x8d,0x92,0xd1,0x94,0xc0,0x95,0xc1,0xd9,0xb5,0x8d,0x92,0xda,0x94,0xc4,0xb5,0x8d,0x92,0xde,0xc5,0xd1,0x94,0xc0,0x95,0xc1,0xd9,0xb5,0x8d,0x92,0xd8,0xd9,0xc5,0xd8,0xd8,0x94,0xc0,0xc0,0xcd]

private func transportLarge(sure num: UInt8) -> UInt8 {
    return num ^ 233
}

/*: "^([\\u4E00-\\u9FA5A-Za-z0-9_]{2,10}$)" :*/
fileprivate let constLabelId:[UInt8] = [0x15,0x63,0x10,0x17,0x3e,0x7f,0xe,0x7b,0x7b,0x66,0x17,0x3e,0x72,0xd,0xa,0x7e,0xa,0x66,0x11,0x2a,0x66,0x31,0x7b,0x66,0x72,0x14,0x16,0x30,0x79,0x67,0x7a,0x7b,0x36,0x6f,0x62]

private func localVanish(address num: UInt8) -> UInt8 {
    return num ^ 75
}

/*: "^[0-8]\\d{5}(?!\\d)$" :*/
fileprivate let k_takeFormat:String = "^[hi-8]"
fileprivate let userComponentValue:String = "?!\\d)$"

/*: "^[a-zA-Z0-9]{6,20}+$" :*/
fileprivate let main_accountGravityPath:String = "^[a-zsomebody if"
fileprivate let main_humanUrl:String = "]{via,"
fileprivate let show_boxStr:[Character] = ["2","0","}","+","$"]

/*: "^(https?:\\/\\/)?([\\da-z\\.-]+)\\.([a-z\\.]{2,6})([\\/\\w \\.-]*)*\\/?$" :*/
fileprivate let user_masterPath:[UInt8] = [0x3,0x75,0x35,0x29,0x29,0x2d,0x2e,0x62,0x67,0x1,0x72,0x1,0x72,0x74,0x62,0x75,0x6,0x1,0x39,0x3c,0x70,0x27,0x1,0x73,0x70,0x0,0x76,0x74,0x1,0x73,0x75,0x6,0x3c,0x70,0x27,0x1,0x73,0x0,0x26,0x6f,0x71,0x6b,0x20,0x74,0x75,0x6,0x1,0x72,0x1,0x2a,0x7d,0x1,0x73,0x70,0x0,0x77,0x74,0x77,0x1,0x72,0x62,0x79]

private func kingCapAt(reject num: UInt8) -> UInt8 {
    return num ^ 93
}

/*: "^(?:(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\\.) {3}(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)$" :*/
fileprivate let notiSearchId:[UInt8] = [0xf3,0x85,0x92,0x97,0x85,0x92,0x97,0x9f,0x98,0xf6,0x9d,0x80,0x98,0xf0,0xd1,0x9f,0xf6,0x9d,0x80,0x99,0xf0,0xf6,0x9d,0x80,0x94,0xf0,0xd1,0xf6,0x9d,0x9c,0xf0,0x92,0xf6,0x9d,0x80,0x94,0xf0,0xf6,0x9d,0x80,0x94,0xf0,0x92,0x84,0xf1,0x83,0x84,0x8d,0xd6,0x9e,0xd0,0x85,0x92,0x97,0x9f,0x98,0xf6,0x9d,0x80,0x98,0xf0,0xd1,0x9f,0xf6,0x9d,0x80,0x99,0xf0,0xf6,0x9d,0x80,0x94,0xf0,0xd1,0xf6,0x9d,0x9c,0xf0,0x92,0xf6,0x9d,0x80,0x94,0xf0,0xf6,0x9d,0x80,0x94,0xf0,0x92,0x84,0x89]

/*: "(^[0-9]{15}$)|([0-9]{17}([0-9]|[xX])$)" :*/
fileprivate let user_parMsg:[UInt8] = [0xe2,0x94,0x91,0xfa,0xe7,0xf3,0x97,0xb1,0xfb,0xff,0xb7,0xee,0xe3,0xb6,0xe2,0x91,0xfa,0xe7,0xf3,0x97,0xb1,0xfb,0xfd,0xb7,0xe2,0x91,0xfa,0xe7,0xf3,0x97,0xb6,0x91,0xb2,0x92,0x97,0xe3,0xee,0xe3]

private func writtenSpecTotalo(build num: UInt8) -> UInt8 {
    return num ^ 202
}

/*: "SELF MATCHES %@" :*/
fileprivate let main_publishData:[Character] = ["S","E","L","F"," ","M"]
fileprivate let mainLocationMessage:[Character] = ["A","T","C","H","E","S"," ","%","@"]

/*: "Feedback" :*/
fileprivate let k_iceKey:[Character] = ["F","e","e","d","b","a","c","k"]

/*: "Enter your feedback…" :*/
fileprivate let noti_accessId:String = "tender par sightEnter "
fileprivate let app_mmMsg:[Character] = ["f","e","e","d","b","a","c","k","…"]

/*: "#999999" :*/
fileprivate let mainShallAllowPath:String = "#beastbeastbeast"

/*: "Your email (Required) " :*/
fileprivate let noti_flagWeightPath:String = "Your eevaluation continuing"
fileprivate let const_skinSureMsg:String = "stretch cell(Req"
fileprivate let data_conversationProcessKey:String = "plain spot catch noted) "

/*: "icon_me_feelback_star" :*/
fileprivate let appWaterShirtName:String = "square some relationicon_"
fileprivate let show_readFormat:String = "elback_appear adjust mask"
fileprivate let app_visualMessage:[Character] = ["s","t","a","r"]

/*: "#CCCCCC" :*/
fileprivate let noti_justKey:String = "#CCCCCClibrary bride rid daily"

/*: "Send" :*/
fileprivate let app_blackName:String = "Sendshared owner nag"

/*: "#D0D0D0" :*/
fileprivate let kWarningContent:[Character] = ["#","D","0","D","0","D","0"]

/*: "Please fill in the content" :*/
fileprivate let userOpValue:String = "Pleassouthwest constraint"
fileprivate let noti_behaviorFormat:[Character] = ["i"]
fileprivate let user_universalTrailSiblingName:String = "cancel hidden direct click maten the "

/*: "Please enter the correct email address" :*/
fileprivate let userVersionStr:[UInt8] = [0x72,0x8e,0x87,0x83,0x95,0x87,0x42,0x87,0x90,0x96,0x87,0x94,0x42,0x96,0x8a,0x87,0x42,0x85,0x91,0x94,0x94,0x87,0x85,0x96,0x42,0x87,0x8f,0x83,0x8b,0x8e,0x42,0x83,0x86,0x86,0x94,0x87,0x95,0x95]

fileprivate func applicationVoice(forehead num: UInt8) -> UInt8 {
    let value = Int(num) - 34
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "content" :*/
fileprivate let show_specialTitle:String = "bindonte"
fileprivate let user_suitName:String = "nstate"

/*: "contactWay" :*/
fileprivate let showWordOurMessage:[Character] = ["c","o","n","t"]
fileprivate let user_boxMuscleFormat:String = "result tool tipactWay"

/*: "platform" :*/
fileprivate let user_beginKey:String = "ppo"
fileprivate let constLabelValue:[Character] = ["a","t","f","o","r","m"]

/*: "iphone" :*/
fileprivate let appFrontName:[Character] = ["i"]
fileprivate let const_placeFormat:String = "capitalhone"

/*: "version" :*/
fileprivate let const_enrollHundredName:[Character] = ["v","e","r","s","i","o","n"]

/*: "type" :*/
fileprivate let show_selectedMessage:String = "tysighte"

/*: "Operation succeeded" :*/
fileprivate let main_yearContent:[Character] = ["O","p","e","r","a","t"]
fileprivate let const_pauseContent:[Character] = ["i","o","n"," ","s","u","c","c","e","e","d","e","d"]

/*: "Problem statements" :*/
fileprivate let user_portraitMsg:[Character] = ["P","r","o","b","l","e","m"," ","s","t","a","t"]
fileprivate let user_renderKey:String = "ememenus"

/*: "Feature advice" :*/
fileprivate let data_iconTitle:[Character] = ["F","e","a","t","u","r","e"," ","a"]
fileprivate let dataSpanPartName:String = "dvbeance"

/*: "Operation questions" :*/
fileprivate let dataLowerHerValue:String = "behavior correct kind putOper"
fileprivate let kDeletePath:String = "family mm relation char numb ques"

/*: "Others" :*/
fileprivate let k_toUrl:[Character] = ["O","t","h","e","r"]
fileprivate let dataBehindText:String = "S"

/*: "#EFEDFF" :*/
fileprivate let notiMeMsg:[Character] = ["#","E","F","E","D"]
fileprivate let const_variationData:String = "margin"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ProgViewController.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/15.
//

//: import UIKit
import UIKit

//: enum Validate {
enum GrantValidate {
    //: case email(_: String)
    case email(_: String) //  邮箱
    //: case phoneNumber(_: String)
    case phoneNumber(_: String) //  手机号
    //: case userName(_: String)
    case userName(_: String) //  用户名
    //: case password(_: String)
    case password(_: String) //  密码
    //: case nickName(_: String)
    case nickName(_: String) //  昵称
    //: case postalCode(_: String)
    case postalCode(_: String) //  邮编
    //: case URL(_: String)
    case URL(_: String) //  URL
    //: case IP(_: String)
    case IP(_: String) //  IP
    //: case isChinese(_: String)
    case isChinese(_: String) //  是否全是中文字符
    //: case idNumber(_: String)
    case idNumber(_: String) //  身份证号

    //: var isRight: Bool {
    var isRight: Bool {
        //: var predicateStr: String!
        var predicateStr: String!
        //: var currentObject: String!
        var currentObject: String!
        //: switch self {
        switch self {
        //: case let .email(str):
        case let .email(str):
            //: predicateStr = "^([a-zA-Z0-9_\\.-]+)@([\\da-z\\.-]+)\\.([a-z\\.]{2,6})$"
            predicateStr = String(bytes: showDonData.map{$0^56}, encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        //: case let .phoneNumber(str):
        case let .phoneNumber(str):
            //: predicateStr = "^((1[34578][0-9]{9})|((0\\d{2}-\\d{8})|(0\\d{3}-\\d{7,8})|(0\\d{10,11}))$"
            predicateStr = String(bytes: app_recommendationData.map{transportLarge(sure: $0)}, encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        //: case let .userName(str):
        case let .userName(str):
            //: predicateStr = "^([\\u4E00-\\u9FA5A-Za-z0-9_]{2,10}$)"
            predicateStr = String(bytes: constLabelId.map{localVanish(address: $0)}, encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        //: case let .postalCode(str):
        case let .postalCode(str):
            //: predicateStr = "^[0-8]\\d{5}(?!\\d)$"
            predicateStr = (k_takeFormat.replacingOccurrences(of: "hi", with: "0") + "\\d{5}(" + userComponentValue)
            //: currentObject = str
            currentObject = str
        //: case let .password(str):
        case let .password(str):
            //: predicateStr = "^[a-zA-Z0-9]{6,20}+$"
            predicateStr = (String(main_accountGravityPath.prefix(5)) + "A-Z0-9" + main_humanUrl.replacingOccurrences(of: "via", with: "6") + String(show_boxStr))
            //: currentObject = str
            currentObject = str
        //: case let .nickName(str):
        case let .nickName(str):
            //: predicateStr = "^([\\u4E00-\\u9FA5A-Za-z0-9_]{2,10}$)"
            predicateStr = String(bytes: constLabelId.map{localVanish(address: $0)}, encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        //: case let .URL(str):
        case let .URL(str):
            //: predicateStr = "^(https?:\\/\\/)?([\\da-z\\.-]+)\\.([a-z\\.]{2,6})([\\/\\w \\.-]*)*\\/?$"
            predicateStr = String(bytes: user_masterPath.map{kingCapAt(reject: $0)}, encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        //: case let .IP(str):
        case let .IP(str):
            //: predicateStr = "^(?:(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\\.) {3}(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)$"
            predicateStr = String(bytes: notiSearchId.map{$0^173}, encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        //: case let . isChinese(str):
        case let .isChinese(str):
            //: predicateStr = "(^[\u{4e00}-\u{9fa5}]$)"
            predicateStr = "(^[\u{4e00}-\u{9fa5}]$)"
            //: currentObject = str
            currentObject = str
        //: case let . idNumber(str):
        case let .idNumber(str):
            //: predicateStr = "(^[0-9]{15}$)|([0-9]{17}([0-9]|[xX])$)"
            predicateStr = String(bytes: user_parMsg.map{writtenSpecTotalo(build: $0)}, encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        }
        //: let predicate =  NSPredicate(format: "SELF MATCHES %@", predicateStr)
        let predicate = NSPredicate(format: (String(main_publishData) + String(mainLocationMessage)), predicateStr)
        //: return predicate.evaluate(with: currentObject)
        return predicate.evaluate(with: currentObject)
    }
}

//: class TalkingFeedbackVC: TalkingBaseViewController {
class ProgViewController: WeddingLovelyRecognizerDelegate {
    //: let limitCount = 300
    let limitCount = 300
    //: let EmialCount = 50
    let EmialCount = 50
    //: var seleteBtn: UIButton?
    var seleteBtn: UIButton?

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()

        //: self.title = "Feedback".localized
        self.title = (String(k_iceKey)).localized
        //: self.view.backgroundColor = .white
        self.view.backgroundColor = .white
        //: designView()
        designLight()
        //: addTapGestureRecognizer()
        workIn()
        //: addKeyboardNotification()
        sh()
    }

    // MARK: - Lazy load

    //: lazy var backView: UIScrollView = {
    lazy var backView: UIScrollView = {
        //: let view = UIScrollView.init()
        let view = UIScrollView()
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: view.showsHorizontalScrollIndicator = false
        view.showsHorizontalScrollIndicator = false
        //: view.showsVerticalScrollIndicator = false
        view.showsVerticalScrollIndicator = false
        //: return view
        return view
        //: }()
    }()

    //: lazy var contView: UIView = {
    lazy var contView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: return view
        return view
        //: }()
    }()

    //: lazy var inputTView: UIView = {
    lazy var inputTView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.clipsToBounds = true
        view.clipsToBounds = true
        //: view.layer.cornerRadius = 8
        view.layer.cornerRadius = 8
        //: view.backgroundColor = .appBgColor()
        view.backgroundColor = .doingTo()
        //: return view
        return view
        //: }()
    }()

    //: lazy var textView: TalkingTextView = {
    lazy var textView: GenerateScrollView = {
        //: let textView = TalkingTextView.init()
        let textView = GenerateScrollView()
        //: textView.placeholder = "Enter your feedback…".localized
        textView.placeholder = (String(noti_accessId.suffix(6)) + "your " + String(app_mmMsg)).localized
        //: textView.placeholderColor = UIColor.init(hex: "#999999")!
        textView.placeholderColor = UIColor(hex: (mainShallAllowPath.replacingOccurrences(of: "beast", with: "99")))!
        //: textView.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        textView.font = UIFont.bigGallery(type: .Regular, fontSize: 15)
        //: textView.textColor = UIColor.appTitleColor()
        textView.textColor = UIColor.dialog()
        //: textView.PlaceholderInsets = UIEdgeInsets(top: 16, left: 16, bottom: 10, right: 16)
        textView.PlaceholderInsets = UIEdgeInsets(top: 16, left: 16, bottom: 10, right: 16)

        //: textView.textContainer.maximumNumberOfLines = 0
        textView.textContainer.maximumNumberOfLines = 0
        //: textView.textContainer.lineBreakMode = .byTruncatingTail
        textView.textContainer.lineBreakMode = .byTruncatingTail
        //: textView.delegate = self
        textView.delegate = self
        //: textView.returnKeyType = UIReturnKeyType.done
        textView.returnKeyType = UIReturnKeyType.done
        //: textView.bounces = false
        textView.bounces = false
        //: textView.backgroundColor = .appBgColor()
        textView.backgroundColor = .doingTo()
        //: numberLB.text =  "0/\(limitCount)"
        numberLB.text = "0/\(limitCount)"
        //: return textView
        return textView
        //: }()
    }()

    //: lazy var numberLB: UILabel = {
    lazy var numberLB: UILabel = {
        //: let LB = UILabel.init()
        let LB = UILabel()
        //: LB.textColor = UIColor.init(hex: "#999999")
        LB.textColor = UIColor(hex: (mainShallAllowPath.replacingOccurrences(of: "beast", with: "99")))
        //: LB.font = UIFont.pingfangFont(type: .Regular, fontSize: 14)
        LB.font = UIFont.bigGallery(type: .Regular, fontSize: 14)
        //: LB.text = "0/\(limitCount)"
        LB.text = "0/\(limitCount)"
        //: LB.textAlignment = .right
        LB.textAlignment = .right
        //: return LB
        return LB
        //: }()
    }()

    //: lazy var emailTF: UITextField = {
    lazy var emailTF: UITextField = {
        //: let tf = UITextField.init()
        let tf = UITextField()
        //: tf.backgroundColor = .appBgColor()
        tf.backgroundColor = .doingTo()
        //: tf.textColor = UIColor.appTitleColor()
        tf.textColor = UIColor.dialog()
        //: tf.font = UIFont.pingfangFont(type: .Regular, fontSize: 16)
        tf.font = UIFont.bigGallery(type: .Regular, fontSize: 16)
        //: tf.delegate = self
        tf.delegate = self
        //: tf.returnKeyType = UIReturnKeyType.done
        tf.returnKeyType = UIReturnKeyType.done
        //: tf.attributedPlaceholder = NSAttributedString.init(string: "Your email (Required) ".localized, attributes: [
        tf.attributedPlaceholder = NSAttributedString(string: (String(noti_flagWeightPath.prefix(6)) + "mail " + String(const_skinSureMsg.suffix(4)) + "uire" + String(data_conversationProcessKey.suffix(3))).localized, attributes: [
            //: NSAttributedString.Key.foregroundColor: UIColor.init(hex: "#999999")!])
            NSAttributedString.Key.foregroundColor: UIColor(hex: (mainShallAllowPath.replacingOccurrences(of: "beast", with: "99")))!])
        //: tf.clipsToBounds = true
        tf.clipsToBounds = true
        //: tf.layer.cornerRadius = 8
        tf.layer.cornerRadius = 8
        //: if LanguageManager.shared.direction == .rightToLeft {
        if RatingReactiveCompatible.shared.direction == .rightToLeft {
            //: tf.textAlignment = .right
            tf.textAlignment = .right
        }
        //: let leftView = UIView()
        let leftView = UIView()
        //: leftView.frame = CGRect(origin: .zero, size: CGSize(width: 30, height: 49))
        leftView.frame = CGRect(origin: .zero, size: CGSize(width: 30, height: 49))

        //: let leftImgView = UIImageView.init()
        let leftImgView = UIImageView()
        //: leftImgView.frame = CGRect(x: 16, y: 20, width: 9, height: 9)
        leftImgView.frame = CGRect(x: 16, y: 20, width: 9, height: 9)
        //: leftImgView.image = UIImage.BundleImageNamed(name: "icon_me_feelback_star")
        leftImgView.image = UIImage.locationName(name: (String(appWaterShirtName.suffix(5)) + "me_fe" + String(show_readFormat.prefix(7)) + String(app_visualMessage)))
        //: leftView.addSubview(leftImgView)
        leftView.addSubview(leftImgView)
        //: tf.leftView = leftView
        tf.leftView = leftView
        //: tf.leftViewMode = .always
        tf.leftViewMode = .always
        //: return tf
        return tf
        //: }()
    }()

    //: lazy var doneBtn: UIButton = {
    lazy var doneBtn: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.setBackgroundColor(color: UIColor.init(hex: "#CCCCCC")!, forState: .disabled)
        btn.openColor(color: UIColor(hex: (String(noti_justKey.prefix(7))))!, forState: .disabled)
        //: btn.setBackgroundColor(color: UIColor.appThemeColor(), forState: .normal)
        btn.openColor(color: UIColor.attractiveIn(), forState: .normal)
        //: btn.setTitle("Send".localized, for: .normal)
        btn.setTitle((String(app_blackName.prefix(4))).localized, for: .normal)
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 17)
        btn.titleLabel?.font = UIFont.bigGallery(type: .Medium, fontSize: 17)
        //: btn.addTarget(self, action: #selector(doneBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(slow), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - Event

//: extension TalkingFeedbackVC {
extension ProgViewController {
    //: @objc func TagBtnClick(sender: UIButton) {
    @objc func commonSender(sender: UIButton) {
        //: seleteBtn?.isSelected = false
        seleteBtn?.isSelected = false
        //: seleteBtn?.layer.borderColor = UIColor.init(hex: "#D0D0D0")?.cgColor
        seleteBtn?.layer.borderColor = UIColor(hex: (String(kWarningContent)))?.cgColor
        //: sender.isSelected = !sender.isSelected
        sender.isSelected = !sender.isSelected
        //: seleteBtn = sender
        seleteBtn = sender
        //: seleteBtn!.layer.borderColor = UIColor.clear.cgColor
        seleteBtn!.layer.borderColor = UIColor.clear.cgColor
    }

    //: @objc func doneBtnClick() {
    @objc func slow() {
        //: if seleteBtn == nil || textView.text.count<=0 || emailTF.text!.count<=0 {
        if seleteBtn == nil || textView.text.count <= 0 || emailTF.text!.count <= 0 {
            //: self.func__showStatusBarErrorMsg(showMsg: "Please fill in the content".localized)
            self.enablece(showMsg: (String(userOpValue.prefix(5)) + "e fill " + String(noti_behaviorFormat) + String(user_universalTrailSiblingName.suffix(6)) + "content").localized)
            //: return
            return
        }

        //: if LanguageManager.shared.direction == .leftToRight {
        if RatingReactiveCompatible.shared.direction == .leftToRight {
            //: if !Validate.email(emailTF.text!).isRight {
            if !GrantValidate.email(emailTF.text!).isRight {
                //: self.func__showStatusBarErrorMsg(showMsg: "Please enter the correct email address".localized)
                self.enablece(showMsg: String(bytes: userVersionStr.map{applicationVoice(forehead: $0)}, encoding: .utf8)!.localized)
                //: return
                return
            }
        }

        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["content"] = textView.text
        dict[(show_specialTitle.replacingOccurrences(of: "bind", with: "c") + user_suitName.replacingOccurrences(of: "state", with: "t"))] = textView.text
        //: dict["contactWay"] = emailTF.text
        dict[(String(showWordOurMessage) + String(user_boxMuscleFormat.suffix(6)))] = emailTF.text
        //: dict["platform"] = "iphone"
        dict[(user_beginKey.replacingOccurrences(of: "po", with: "l") + String(constLabelValue))] = (String(appFrontName) + const_placeFormat.replacingOccurrences(of: "capital", with: "p"))
        //: dict["version"] = AppVersion
        dict[(String(const_enrollHundredName))] = main_sampleMaleContent
        //: dict["type"] = String((seleteBtn?.tag ?? 100)-100)
        dict[(show_selectedMessage.replacingOccurrences(of: "sight", with: "p"))] = String((seleteBtn?.tag ?? 100) - 100)

        //: ProgressHUD.show()
        InsertProgressHUD.momentShow()

        //: TalkingMeRequestTool.req_Feedback(params: dict) { succeed, result, errorModel in
        FlyerThen.halfStaff(params: dict) { succeed, _, _ in
            //: ProgressHUD.dismiss()
            InsertProgressHUD.detectDismiss()
            //: if succeed {
            if succeed {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Operation succeeded".localized)
                self.showGuidanceName(showMsg: (String(main_yearContent) + String(const_pauseContent)).localized)
                //: self.navigationController?.popViewController(animated: true)
                self.navigationController?.popViewController(animated: true)
            }
        }
    }

    //: func addKeyboardNotification() {
    func sh() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(keyboardWillShow(notification:)),
                                               selector: #selector(readModel(notification:)),
                                               //: name: UIResponder.keyboardWillShowNotification,
                                               name: UIResponder.keyboardWillShowNotification,
                                               //: object: nil)
                                               object: nil)
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(keyboardWillHide(notification:)),
                                               selector: #selector(volition(notification:)),
                                               //: name: UIResponder.keyboardWillHideNotification,
                                               name: UIResponder.keyboardWillHideNotification,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func keyboardWillShow(notification: Notification) {
    @objc func readModel(notification: Notification) {
        //: guard emailTF.isFirstResponder else { return }
        guard emailTF.isFirstResponder else { return }
        //: guard let info = notification.userInfo else { return }
        guard let info = notification.userInfo else { return }
        //: let value = info[UIResponder.keyboardFrameEndUserInfoKey] as? NSValue
        let value = info[UIResponder.keyboardFrameEndUserInfoKey] as? NSValue
        //: let frame = value?.cgRectValue ?? .zero
        let frame = value?.cgRectValue ?? .zero
        //: let duration = info[UIResponder.keyboardAnimationDurationUserInfoKey] as? Double ?? 0.25
        let duration = info[UIResponder.keyboardAnimationDurationUserInfoKey] as? Double ?? 0.25
        //: guard (emailTF.bottom + StatusBarNavigationBarHeight) > frame.minY else { return }
        guard (emailTF.bottom + constAgentJumpValue) > frame.minY else { return }

        //: let y = (emailTF.bottom + StatusBarNavigationBarHeight) - frame.minY
        let y = (emailTF.bottom + constAgentJumpValue) - frame.minY
        //: UIView.animate(withDuration: duration) {
        UIView.animate(withDuration: duration) {
            //: self.backView.transform = CGAffineTransform(translationX: 0, y: -y)
            self.backView.transform = CGAffineTransform(translationX: 0, y: -y)
        }
    }

    //: @objc func keyboardWillHide(notification: Notification) {
    @objc func volition(notification: Notification) {
        //: guard let info = notification.userInfo else { return }
        guard let info = notification.userInfo else { return }
        //: let duration = info[UIResponder.keyboardAnimationDurationUserInfoKey] as? Double ?? 0.25
        let duration = info[UIResponder.keyboardAnimationDurationUserInfoKey] as? Double ?? 0.25

        //: UIView.animate(withDuration: duration) {
        UIView.animate(withDuration: duration) {
            //: self.backView.transform = .identity
            self.backView.transform = .identity
        }
    }
}

// MARK: - UITextViewDelegate, UITextFieldDelegate

//: extension TalkingFeedbackVC: UITextViewDelegate, UITextFieldDelegate {
extension ProgViewController: UITextViewDelegate, UITextFieldDelegate {
    //: public func textViewDidChange(_ textView: UITextView) {
    public func textViewDidChange(_ textView: UITextView) {
        //: if textView.text.count > limitCount {
        if textView.text.count > limitCount {
            // 获得已输出字数与正输入字母数
            //: let selectRange = textView.markedTextRange
            let selectRange = textView.markedTextRange

            // 获取高亮部分 － 如果有联想词则解包成功
            //: if let selectRange = selectRange {
            if let selectRange = selectRange {
                //: let position =  textView.position(from: (selectRange.start), offset: 0)
                let position = textView.position(from: selectRange.start, offset: 0)
                //: if (position != nil) {
                if position != nil {
                    //: return
                    return
                }
            }

            //: let textContent = textView.text ?? ""
            let textContent = textView.text ?? ""
            //: let textNum = textContent.count
            let textNum = textContent.count

            // 截取
            //: if textNum > limitCount && limitCount > 0 {
            if textNum > limitCount && limitCount > 0 {
                //: textView.text = string_prefix(index: limitCount, text: textContent)
                textView.text = limited(index: limitCount, text: textContent)
            }
        }
        //: numberLB.text =  "\(textView.text.count)/\(limitCount)"
        numberLB.text = "\(textView.text.count)/\(limitCount)"
    }

    //: public func textViewShouldBeginEditing(_ textView: UITextView) -> Bool {
    public func textViewShouldBeginEditing(_: UITextView) -> Bool {
        //: numberLB.isHidden = false
        numberLB.isHidden = false
        //: return true
        return true
    }

    //: public func textViewDidEndEditing(_ textView: UITextView) {
    public func textViewDidEndEditing(_ textView: UITextView) {
        //: if textView.text.isEmpty {
        if textView.text.isEmpty {
            //: numberLB.isHidden = false
            numberLB.isHidden = false
            //: } else {
        } else {
            //: numberLB.isHidden = true
            numberLB.isHidden = true
        }
    }

    //: public func textView(_ textView: UITextView, shouldChangeTextIn range: NSRange, replacementText text: String) -> Bool {
    public func textView(_ textView: UITextView, shouldChangeTextIn _: NSRange, replacementText text: String) -> Bool {
        //: if textView.returnKeyType == .done {
        if textView.returnKeyType == .done {
            //: if text == "\n" {
            if text == "\n" {
                //: textView.resignFirstResponder()
                textView.resignFirstResponder()
            }
            //: return true
            return true
        }
        //: return true
        return true
    }

    // 字符串的截取 从头截取到指定index
    //: private func string_prefix(index: Int,text:String) -> String {
    private func limited(index: Int, text: String) -> String {
        //: if text.count <= index {
        if text.count <= index {
            //: return text
            return text
            //: } else {
        } else {
            //: let index = text.index(text.startIndex, offsetBy: index)
            let index = text.index(text.startIndex, offsetBy: index)
            //: let str = text.prefix(upTo: index)
            let str = text.prefix(upTo: index)
            //: return String(str)
            return String(str)
        }
    }

    //: func textFieldShouldReturn(_ textField: UITextField) -> Bool {
    func textFieldShouldReturn(_ textField: UITextField) -> Bool {
        //: textField.resignFirstResponder()
        textField.resignFirstResponder()

        //: return true
        return true
    }

    //: func textField(_ textField: UITextField, shouldChangeCharactersIn range: NSRange, replacementString string: String) -> Bool {
    func textField(_ textField: UITextField, shouldChangeCharactersIn _: NSRange, replacementString string: String) -> Bool {
        //: if textField.text!.count+string.count>EmialCount || textField.text!.count+string.count <= 0 {
        if textField.text!.count + string.count > EmialCount || textField.text!.count + string.count <= 0 {
            //: return false
            return false
        }
        //: return true
        return true
    }
}

// MARK: - UI

//: extension TalkingFeedbackVC {
extension ProgViewController {
    //: func designView() {
    func designLight() {
        //: self.view.addSubview(backView)
        self.view.addSubview(backView)
        //: backView.snp.makeConstraints { make in
        backView.snp.makeConstraints { make in
            //: make.edges.equalTo(self.view)
            make.edges.equalTo(self.view)
        }
        //: backView.addSubview(contView)
        backView.addSubview(contView)
        //: contView.snp.makeConstraints { make in
        contView.snp.makeConstraints { make in
            //: make.leading.trailing.top.bottom.equalTo(backView)
            make.leading.trailing.top.bottom.equalTo(backView)
        }

        //: contView.addSubview(inputTView)
        contView.addSubview(inputTView)
        //: inputTView.snp.makeConstraints { make in
        inputTView.snp.makeConstraints { make in
            //: make.leading.equalTo(contView).offset(15)
            make.leading.equalTo(contView).offset(15)
            //: make.trailing.equalTo(contView.snp.trailing).offset(-15)
            make.trailing.equalTo(contView.snp.trailing).offset(-15)
            //: make.top.equalTo(contView).offset(128)
            make.top.equalTo(contView).offset(128)
            //: make.height.equalTo(210)
            make.height.equalTo(210)
        }

        //: inputTView.addSubview(textView)
        inputTView.addSubview(textView)
        //: inputTView.addSubview(numberLB)
        inputTView.addSubview(numberLB)
        //: textView.snp.makeConstraints { make in
        textView.snp.makeConstraints { make in
            //: make.leading.trailing.top.equalTo(inputTView)
            make.leading.trailing.top.equalTo(inputTView)
            //: make.bottom.equalTo(inputTView.snp.bottom).offset(-40)
            make.bottom.equalTo(inputTView.snp.bottom).offset(-40)
        }
        //: numberLB.snp.makeConstraints { make in
        numberLB.snp.makeConstraints { make in
            //: make.trailing.equalTo(inputTView.snp.trailing).offset(-16)
            make.trailing.equalTo(inputTView.snp.trailing).offset(-16)
            //: make.bottom.equalTo(inputTView.snp.bottom).offset(-12)
            make.bottom.equalTo(inputTView.snp.bottom).offset(-12)
            //: make.height.equalTo(20)
            make.height.equalTo(20)
        }
        //: contView.addSubview(emailTF)
        contView.addSubview(emailTF)
        //: emailTF.snp.makeConstraints { make in
        emailTF.snp.makeConstraints { make in
            //: make.leading.equalTo(contView).offset(15)
            make.leading.equalTo(contView).offset(15)
            //: make.trailing.equalTo(contView.snp.trailing).offset(-15)
            make.trailing.equalTo(contView.snp.trailing).offset(-15)
            //: make.top.equalTo(inputTView.snp.bottom).offset(16)
            make.top.equalTo(inputTView.snp.bottom).offset(16)
            //: make.height.equalTo(49)
            make.height.equalTo(49)
        }

        //: contView.addSubview(doneBtn)
        contView.addSubview(doneBtn)
        //: doneBtn.snp.makeConstraints { make in
        doneBtn.snp.makeConstraints { make in
            //: make.leading.equalTo(contView).offset(54)
            make.leading.equalTo(contView).offset(54)
            //: make.trailing.equalTo(contView.snp.trailing).offset(-54)
            make.trailing.equalTo(contView.snp.trailing).offset(-54)
            //: make.top.equalTo(emailTF.snp.bottom).offset(111)
            make.top.equalTo(emailTF.snp.bottom).offset(111)
            //: make.height.equalTo(50)
            make.height.equalTo(50)
        }

        //: let arr = ["Problem statements".localized,
        let arr = [(String(user_portraitMsg) + user_renderKey.replacingOccurrences(of: "menu", with: "nt")).localized,
                   //: "Feature advice".localized,
                   (String(data_iconTitle) + dataSpanPartName.replacingOccurrences(of: "bean", with: "i")).localized,
                   //: "Operation questions".localized,
                   (String(dataLowerHerValue.suffix(4)) + "ation" + String(kDeletePath.suffix(5)) + "tions").localized,
                   //: "Others".localized]
                   (String(k_toUrl) + dataBehindText.lowercased()).localized]

        //: for i in 0..<arr.count {
        for i in 0 ..< arr.count {
            //: let xz = 15
            let xz = 15
            //: let yz = 16
            let yz = 16
            //: let Width = (Int(ScreenWidth)-xz*3)/2
            let Width = (Int(main_logStr) - xz * 3) / 2
            //: let height = 38
            let height = 38
            //: let X = xz+(xz + Width)*(i%2)
            let X = xz + (xz + Width) * (i % 2)
            //: let Y = yz+(yz+height)*(i/2)
            let Y = yz + (yz + height) * (i / 2)

            //: let btn = UIButton.init()
            let btn = UIButton()
            //: btn.frame =  CGRect.init(x: X, y: Y , width: Width, height: height)
            btn.frame = CGRect(x: X, y: Y, width: Width, height: height)
            //: btn.setTitle(arr[i], for: .normal)
            btn.setTitle(arr[i], for: .normal)
            //: btn.setTitleColor(.black, for: .normal)
            btn.setTitleColor(.black, for: .normal)
            //: btn.setTitleColor(.appThemeColor(), for: .selected)
            btn.setTitleColor(.attractiveIn(), for: .selected)

            //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
            btn.titleLabel?.font = UIFont.bigGallery(type: .Regular, fontSize: 15)
            //: if TalkingRequestAddrTool.share.interfaceLang == LangType.es.rawValue || TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue {
            if ManufacturerThen.share.interfaceLang == ImpressionName.es.rawValue || ManufacturerThen.share.interfaceLang == ImpressionName.pt.rawValue {
                //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Regular, fontSize: 13)
                btn.titleLabel?.font = UIFont.bigGallery(type: .Regular, fontSize: 13)
            }
            //: btn.layer.cornerRadius = 19
            btn.layer.cornerRadius = 19
            //: btn.layer.borderWidth = 1
            btn.layer.borderWidth = 1
            //: btn.layer.borderColor = UIColor.init(hex: "#D0D0D0")?.cgColor
            btn.layer.borderColor = UIColor(hex: (String(kWarningContent)))?.cgColor
            //: btn.setBackgroundColor(color: UIColor.init(hex: "#EFEDFF")!, forState: .selected)
            btn.openColor(color: UIColor(hex: (String(notiMeMsg) + const_variationData.replacingOccurrences(of: "margin", with: "FF")))!, forState: .selected)
            //: btn.setBackgroundColor(color: .white, forState: .normal)
            btn.openColor(color: .white, forState: .normal)
            //: btn.tag = 100+i
            btn.tag = 100 + i
            //: btn.addTarget( self, action: #selector(TagBtnClick(sender:)), for: .touchUpInside)
            btn.addTarget(self, action: #selector(commonSender(sender:)), for: .touchUpInside)
            //: contView.addSubview(btn)
            contView.addSubview(btn)
        }

        //: contView.snp.remakeConstraints { make in
        contView.snp.remakeConstraints { make in
            //: make.bottom.equalTo(doneBtn.snp.bottom).offset(50)
            make.bottom.equalTo(doneBtn.snp.bottom).offset(50)
            //: make.width.equalTo(backView)
            make.width.equalTo(backView)
        }
    }
}
