
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let notiManagerStreetData:[UInt8] = [0x1e,0x19,0x1e,0x3,0x5f,0x14,0x18,0x13,0x12,0x5,0x4d,0x5e,0x57,0x1f,0x16,0x4,0x57,0x19,0x18,0x3,0x57,0x15,0x12,0x12,0x19,0x57,0x1e,0x1a,0x7,0x1b,0x12,0x1a,0x12,0x19,0x3,0x12,0x13]

private func nearbyLater(reach num: UInt8) -> UInt8 {
    return num ^ 119
}

/*: "More attractive photo more calls" :*/
fileprivate let userProductUrl:[UInt8] = [0x76,0x54,0x49,0x5e,0x1b,0x5a,0x4f,0x4f,0x49,0x5a,0x58,0x4f,0x52,0x4d,0x5e,0x1b,0x4b,0x53,0x54,0x4f,0x54,0x1b,0x56,0x54,0x49,0x5e,0x1b,0x58,0x5a,0x57,0x57,0x48]

/*: "Upload any photo you like" :*/
fileprivate let showEveryoneFormat:String = "lens classUplo"
fileprivate let user_impressionPlusMessage:String = "category privacy sampleny ph"
fileprivate let mainProcessingSquareTransmissionMessage:String = "selected ret mechanical kingou like"

/*: "F4F4F4" :*/
fileprivate let userStandFormat:[Character] = ["F","4","F","4","F","4"]

/*: "btn_add_head_cover_nor" :*/
fileprivate let constAnimaFormat:String = "btn_the both attach brush"
fileprivate let app_formatUrl:String = "ad_covcorrupt format want"
fileprivate let noti_huntingValue:[Character] = ["e","r","_","n","o","r"]

/*: "Please upload a clear and beautiful photo of yourself" :*/
fileprivate let show_justMsg:[UInt8] = [0x87,0xa3,0x9c,0x98,0xaa,0x9c,0x57,0xac,0xa7,0xa3,0xa6,0x98,0x9b,0x57,0x98,0x57,0x9a,0xa3,0x9c,0x98,0xa9,0x57,0x98,0xa5,0x9b,0x57,0x99,0x9c,0x98,0xac,0xab,0xa0,0x9d,0xac,0xa3,0x57,0xa7,0x9f,0xa6,0xab,0xa6,0x57,0xa6,0x9d,0x57,0xb0,0xa6,0xac,0xa9,0xaa,0x9c,0xa3,0x9d]

fileprivate func trapCondition(failure num: UInt8) -> UInt8 {
    let value = Int(num) - 55
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "No violence, pornography, or politics allowed" :*/
fileprivate let app_libraryBorderErrValue:[UInt8] = [0x97,0xb6,0xf9,0xaf,0xb0,0xb6,0xb5,0xbc,0xb7,0xba,0xbc,0xf5,0xf9,0xa9,0xb6,0xab,0xb7,0xb6,0xbe,0xab,0xb8,0xa9,0xb1,0xa0,0xf5,0xf9,0xb6,0xab,0xf9,0xa9,0xb6,0xb5,0xb0,0xad,0xb0,0xba,0xaa,0xf9,0xb8,0xb5,0xb5,0xb6,0xae,0xbc,0xbd]

private func rescueCorner(agent num: UInt8) -> UInt8 {
    return num ^ 217
}

/*: "Next" :*/
fileprivate let const_plusUrl:[Character] = ["N","e","x","t"]

/*: "edit_head_Image_male_ :*/
fileprivate let show_natureMessage:[Character] = ["e","d","i","t","_","h","e","a","d","_","I","m","a","g","e","_","m","a","l","e"]
fileprivate let data_partContent:String = "_"

/*: "edit_head_Image_ :*/
fileprivate let noti_weightPath:String = "edit_hetarget water"
fileprivate let k_windPartyKey:String = "ticket recognize allow feedback stopad_I"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ActualReactiveCompatible.swift
//  AbroadTalking
//
//  Created by Charlotte on 2024/5/24.
//

//: import UIKit
import UIKit

//: class TalkingLoginEditPhotoView: UIView {
class ActualReactiveCompatible: UIView {
    //: typealias InfoActionBlock = (_ type: EditInfoType) -> Void
    typealias InfoActionBlock = (_ type: SmallInfoType) -> Void

    //: let disposeBag = DisposeBag()
    let disposeBag = DisposeBag()
    //: var btnBlock: InfoActionBlock?
    var btnBlock: InfoActionBlock?

    //: private var nameCount: Int = 20
    private var nameCount: Int = 20

    //: override init(frame: CGRect) {
    override init(frame: CGRect) {
        //: super.init(frame: frame)
        super.init(frame: frame)
        //: self.backgroundColor = UIColor.white
        self.backgroundColor = UIColor.white

        //: self.setupSubviews()
        self.thin()
        //: self.setupSubViewsConstraint()
        self.minSearch()
        //: self.bindInteraction()
        self.parcel()
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: notiManagerStreetData.map{nearbyLater(reach: $0)}, encoding: .utf8)!)
    }

    //: lazy var contentView: UIView = {
    lazy var contentView: UIView = {
        //: let contentView = UIView.init()
        let contentView = UIView()
        //: return contentView
        return contentView
        //: }()
    }()

    //: lazy var tipsLab: UILabel = {
    lazy var tipsLab: UILabel = {
        //: let label = UILabel()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Semibold, fontSize: 17)
        label.font = .bigGallery(type: .Semibold, fontSize: 17)
        //: label.textColor = .appTitleColor()
        label.textColor = .dialog()
        //: label.textAlignment = .center
        label.textAlignment = .center
        //: if DismissReactiveCompatible.share.userFillInfoMode.sex == Gender.female.rawValue {
        if DismissReactiveCompatible.share.userFillInfoMode.sex == FenceSequence.female.rawValue {
            //: label.text = "More attractive photo more calls".localized
            label.text = String(bytes: userProductUrl.map{$0^59}, encoding: .utf8)!.localized
            //: } else {
        } else {
            //: label.text = "Upload any photo you like".localized
            label.text = (String(showEveryoneFormat.suffix(4)) + "ad a" + String(user_impressionPlusMessage.suffix(5)) + "oto y" + String(mainProcessingSquareTransmissionMessage.suffix(7))).localized
        }
        //: return label
        return label
        //: }()
    }()

    //: lazy var addHeadButton: UIButton = {
    lazy var addHeadButton: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: btn.backgroundColor = UIColor(hex: "F4F4F4")
        btn.backgroundColor = UIColor(hex: (String(userStandFormat)))
        //: btn.layer.cornerRadius = 5
        btn.layer.cornerRadius = 5
        //: btn.imageView?.contentMode = .scaleAspectFill
        btn.imageView?.contentMode = .scaleAspectFill
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true

        //: let headImg = DismissReactiveCompatible.share.userFillInfoMode.headImage ?? nil
        let headImg = DismissReactiveCompatible.share.userFillInfoMode.headImage ?? nil
        //: if headImg != nil {
        if headImg != nil {
            //: btn.setImage(headImg, for: .normal)
            btn.setImage(headImg, for: .normal)
            //: } else {
        } else {
            //: btn.setImage(UIImage.BundleImageNamed(name: "btn_add_head_cover_nor"), for: .normal)
            btn.setImage(UIImage.locationName(name: (String(constAnimaFormat.prefix(4)) + "add_he" + String(app_formatUrl.prefix(6)) + String(noti_huntingValue))), for: .normal)
        }
        //: return btn
        return btn
        //: }()
    }()

    //: lazy var msgLab: UILabel = {
    lazy var msgLab: UILabel = {
        //: let label = UILabel()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Regular, fontSize: 15)
        label.font = .bigGallery(type: .Regular, fontSize: 15)
        //: label.textColor = .appValueColor()
        label.textColor = .ownLight()
        //: if DismissReactiveCompatible.share.userFillInfoMode.sex == Gender.female.rawValue {
        if DismissReactiveCompatible.share.userFillInfoMode.sex == FenceSequence.female.rawValue {
            //: label.text = "Please upload a clear and beautiful photo of yourself".localized
            label.text = String(bytes: show_justMsg.map{trapCondition(failure: $0)}, encoding: .utf8)!.localized
            //: } else {
        } else {
            //: label.text = "No violence, pornography, or politics allowed".localized
            label.text = String(bytes: app_libraryBorderErrValue.map{rescueCorner(agent: $0)}, encoding: .utf8)!.localized
        }
        //: label.numberOfLines = 0
        label.numberOfLines = 0
        //: return label
        return label
        //: }()
    }()

    //: lazy var commitButton: UIButton = {
    lazy var commitButton: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: btn.isEnabled = false
        btn.isEnabled = false
        //: btn.setTitleColor(UIColor.white, for: .normal)
        btn.setTitleColor(UIColor.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 17)
        btn.titleLabel?.font = UIFont.bigGallery(type: .Medium, fontSize: 17)
        //: btn.setTitle("Next".localized, for: .normal)
        btn.setTitle((String(const_plusUrl)).localized, for: .normal)
        //: btn.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: ScreenWidth-60, height: 50)), for: .normal)
        btn.setBackgroundImage(UIImage.beforeCan(colors: UIColor.minimizeOn(), size: CGSize(width: main_logStr - 60, height: 50)), for: .normal)
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - Layout

//: extension TalkingLoginEditPhotoView {
extension ActualReactiveCompatible {
    // 添加视图
    //: private func setupSubviews() {
    private func thin() {
        //: addSubview(contentView)
        addSubview(contentView)
        //: contentView.addSubview(tipsLab)
        contentView.addSubview(tipsLab)
        //: contentView.addSubview(addHeadButton)
        contentView.addSubview(addHeadButton)
        //: contentView.addSubview(commitButton)
        contentView.addSubview(commitButton)
        //: contentView.addSubview(msgLab)
        contentView.addSubview(msgLab)
    }

    // 添加约束
    //: private func setupSubViewsConstraint() {
    private func minSearch() {
        //: contentView.snp.makeConstraints { make in
        contentView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: tipsLab.snp.makeConstraints { make in
        tipsLab.snp.makeConstraints { make in
            //: make.leading.equalTo(24)
            make.leading.equalTo(24)
            //: make.height.equalTo(28)
            make.height.equalTo(28)
            //: make.top.equalTo(34)
            make.top.equalTo(34)
        }

        //: addHeadButton.snp.makeConstraints { make in
        addHeadButton.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.width.height.equalTo(159)
            make.width.height.equalTo(159)
            //: make.top.equalTo(tipsLab.snp.bottom).offset(34)
            make.top.equalTo(tipsLab.snp.bottom).offset(34)
        }

        //: let imgWidth: CGFloat = (ScreenWidth - 76) / 3.0
        let imgWidth: CGFloat = (main_logStr - 76) / 3.0
        //: let imgHeight: CGFloat = imgWidth
        let imgHeight: CGFloat = imgWidth

        //: for i in 0 ..< 3 {
        for i in 0 ..< 3 {
            //: let imageView = UIImageView.init()
            let imageView = UIImageView()
            //: if DismissReactiveCompatible.share.userFillInfoMode.sex == "1" {
            if DismissReactiveCompatible.share.userFillInfoMode.sex == "1" {
                //: imageView.image = UIImage.BundleImageNamed(name: "edit_head_Image_male_\(i)")
                imageView.image = UIImage.locationName(name: (String(show_natureMessage) + data_partContent.capitalized) + "\(i)")
                //: } else {
            } else {
                //: imageView.image = UIImage.BundleImageNamed(name: "edit_head_Image_\(i)")
                imageView.image = UIImage.locationName(name: (String(noti_weightPath.prefix(7)) + String(k_windPartyKey.suffix(4)) + "mage_") + "\(i)")
            }
            //: contentView.addSubview(imageView)
            contentView.addSubview(imageView)
            //: imageView.snp.makeConstraints { make in
            imageView.snp.makeConstraints { make in
                //: make.top.equalTo(addHeadButton.snp.bottom).offset(30)
                make.top.equalTo(addHeadButton.snp.bottom).offset(30)
                //: make.leading.equalTo(30 + (imgWidth+8)*CGFloat(i))
                make.leading.equalTo(30 + (imgWidth + 8) * CGFloat(i))
                //: make.width.equalTo(imgWidth)
                make.width.equalTo(imgWidth)
                //: make.height.equalTo(imgHeight)
                make.height.equalTo(imgHeight)
            }
        }
        //: msgLab.snp.makeConstraints { make in
        msgLab.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.top.equalTo(addHeadButton.snp.bottom).offset(146)
            make.top.equalTo(addHeadButton.snp.bottom).offset(146)
        }

        //: commitButton.snp.makeConstraints { make in
        commitButton.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.height.equalTo(50)
            make.height.equalTo(50)
            //: make.top.equalTo(msgLab.snp.bottom).offset(30)
            make.top.equalTo(msgLab.snp.bottom).offset(30)
        }
    }

    // 添加事件
    //: private func bindInteraction() {
    private func parcel() {
        //: commitButton.rx.tap
        commitButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: if self.btnBlock != nil {
                if self.btnBlock != nil {
                    //: self.btnBlock!(.Finish)
                    self.btnBlock!(.Finish)
                }
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: addHeadButton.rx.tap
        addHeadButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: if self.btnBlock != nil {
                if self.btnBlock != nil {
                    //: self.btnBlock!(.HeadPic)
                    self.btnBlock!(.HeadPic)
                }
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
    }
}
