
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let userAspectContent:[UInt8] = [0x65,0x62,0x65,0x78,0x24,0x6f,0x63,0x68,0x69,0x7e,0x36,0x25,0x2c,0x64,0x6d,0x7f,0x2c,0x62,0x63,0x78,0x2c,0x6e,0x69,0x69,0x62,0x2c,0x65,0x61,0x7c,0x60,0x69,0x61,0x69,0x62,0x78,0x69,0x68]

private func pressEqual(shared num: UInt8) -> UInt8 {
    return num ^ 12
}

/*: "Personal information" :*/
fileprivate let show_handKey:[Character] = ["P","e","r","s","o","n","a","l"," ","i","n","f","o","r","m","a","t","i","o"]
fileprivate let data_missingFormat:String = "them"

/*: "FenceSequence" :*/
fileprivate let appObserverMsg:String = "after signal easeGender"

/*: "Once gender is set, it Cannot be changed." :*/
fileprivate let constVehicleName:[UInt8] = [0x6d,0x4c,0x41,0x47,0x2,0x45,0x47,0x4c,0x46,0x47,0x50,0x2,0x4b,0x51,0x2,0x51,0x47,0x56,0xe,0x2,0x4b,0x56,0x2,0x61,0x43,0x4c,0x4c,0x4d,0x56,0x2,0x40,0x47,0x2,0x41,0x4a,0x43,0x4c,0x45,0x47,0x46,0xc]

private func writingPartySupposed(which num: UInt8) -> UInt8 {
    return num ^ 34
}

/*: "#2ABBFF" :*/
fileprivate let show_tissueMessage:String = "#2"
fileprivate let show_gatherUrl:[Character] = ["A","B","B","F","F"]

/*: "btn_male_nv_nor" :*/
fileprivate let userAppealValue:[Character] = ["b","t","n","_","m","a","l","e"]
fileprivate let data_precedentKey:String = "_nv_nortrigger produce age surgery"

/*: "btn_male_nv_sel" :*/
fileprivate let show_singleAgencyMsg:String = "he gift during calculation sequencebtn_"
fileprivate let user_findData:[Character] = ["m","a","l","e","_","n","v","_","s","e","l"]

/*: "Male" :*/
fileprivate let main_databaseName:String = "parent usedMale"

/*: "#FF5372" :*/
fileprivate let constAmValue:[Character] = ["#","F","F","5","3","7","2"]

/*: "btn_female_nv_nor" :*/
fileprivate let constExtraUrl:[Character] = ["b","t","n","_","f","e","m","a","l","e","_","n","v","_","n"]
fileprivate let user_liteMsg:String = "ospeaker"

/*: "btn_female_nv_sel" :*/
fileprivate let noti_assignPath:String = "btn_fefavorite own worker"
fileprivate let k_atValue:[Character] = ["_","s","e","l"]

/*: "Female" :*/
fileprivate let app_orientationContent:String = "Femalepart anima"

/*: "Next" :*/
fileprivate let k_strengthStr:[Character] = ["N","e","x","t"]

/*: "sex" :*/
fileprivate let main_decideFormat:[UInt8] = [0x6d,0x7b,0x66]

private func currentLive(path num: UInt8) -> UInt8 {
    return num ^ 30
}

/*: "RegisterSuccess" :*/
fileprivate let dataMediaMsg:String = "anonymous distribute maximum gather injuryRegis"
fileprivate let showSeekTitle:String = "utransitess"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  FenceThen.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/30.
//

//: import UIKit
import UIKit

//: class TalkingLoginGenderVC: TalkingBaseViewController {
class FenceThen: WeddingLovelyRecognizerDelegate {
    //: let disposeBag = DisposeBag()
    let disposeBag = DisposeBag()

    //: init() {
    init() {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: userAspectContent.map{pressEqual(shared: $0)}, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: popGesture(isOpen: false)
        rankSlow(isOpen: false)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.title = "Personal information".localized
        self.title = (String(show_handKey) + data_missingFormat.replacingOccurrences(of: "them", with: "n")).localized
        //: DismissReactiveCompatible.share.userFillInfoMode.sex = "1"
        DismissReactiveCompatible.share.userFillInfoMode.sex = "1"
        //: self.setupSubviews()
        self.calibrate()
        //: self.setupSubViewsConstraint()
        self.too()
        //: self.bindInteraction()
        self.totalerchange()
        //: func__checkNextBtnState()
        activity()
    }

    //: deinit {
    deinit {}

    //: private lazy var logoTitleLabel: UILabel = {
    private lazy var logoTitleLabel: UILabel = {
        //: let label = UILabel()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Semibold, fontSize: 17)
        label.font = .bigGallery(type: .Semibold, fontSize: 17)
        //: label.textColor = .appTitleColor()
        label.textColor = .dialog()
        //: label.textAlignment = .center
        label.textAlignment = .center
        //: label.text = "Gender".localized
        label.text = (String(appObserverMsg.suffix(6))).localized
        //: return label
        return label
        //: }()
    }()

    //: private lazy var genderTipLabel: UILabel = {
    private lazy var genderTipLabel: UILabel = {
        //: let label = UILabel()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Regular, fontSize: 15)
        label.font = .bigGallery(type: .Regular, fontSize: 15)
        //: label.textColor = .appValueColor()
        label.textColor = .ownLight()
        //: label.numberOfLines = 0
        label.numberOfLines = 0
        //: label.text = "Once gender is set, it Cannot be changed.".localized
        label.text = String(bytes: constVehicleName.map{writingPartySupposed(which: $0)}, encoding: .utf8)!.localized
        //: return label
        return label
        //: }()
    }()

    //: private lazy var maleButton: TalkingButton = {
    private lazy var maleButton: RangeReactiveCompatible = {
        //: let btn = TalkingButton(type: .custom)
        let btn = RangeReactiveCompatible(type: .custom)
        //: btn.isSelected = false
        btn.isSelected = false
        //: btn.spaceBetweenTitleAndImage = 20
        btn.spaceBetweenTitleAndImage = 20
        //: btn.setBackgroundColor(color: UIColor.init(red: 244/255.0, green: 244/255.0, blue: 244/255.0, alpha: 1.0), forState: .normal)
        btn.openColor(color: UIColor(red: 244 / 255.0, green: 244 / 255.0, blue: 244 / 255.0, alpha: 1.0), forState: .normal)
        //: btn.setBackgroundColor(color: UIColor.init(hex: "#2ABBFF")!, forState: .selected)
        btn.openColor(color: UIColor(hex: (show_tissueMessage.capitalized + String(show_gatherUrl)))!, forState: .selected)
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_male_nv_nor"), for: .normal)
        btn.setImage(UIImage.locationName(name: (String(userAppealValue) + String(data_precedentKey.prefix(7)))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_male_nv_sel"), for: .selected)
        btn.setImage(UIImage.locationName(name: (String(show_singleAgencyMsg.suffix(4)) + String(user_findData))), for: .selected)
        //: btn.setTitle("Male".localized, for: .normal)
        btn.setTitle((String(main_databaseName.suffix(4))).localized, for: .normal)
        //: btn.setTitle("Male".localized, for: .selected)
        btn.setTitle((String(main_databaseName.suffix(4))).localized, for: .selected)
        //: btn.setTitleColor(.black, for: .normal)
        btn.setTitleColor(.black, for: .normal)
        //: btn.setTitleColor(.white, for: .selected)
        btn.setTitleColor(.white, for: .selected)
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.titleLabel?.font = UIFont.pingfangRugularFont(fontSize: 17)
        btn.titleLabel?.font = UIFont.bonePlace(fontSize: 17)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var femaleButton: TalkingButton = {
    private lazy var femaleButton: RangeReactiveCompatible = {
        //: let btn = TalkingButton(type: .custom)
        let btn = RangeReactiveCompatible(type: .custom)
        //: btn.isSelected = false
        btn.isSelected = false
        //: btn.spaceBetweenTitleAndImage = 20
        btn.spaceBetweenTitleAndImage = 20
        //: btn.setBackgroundColor(color: UIColor.init(red: 244/255.0, green: 244/255.0, blue: 244/255.0, alpha: 1.0), forState: .normal)
        btn.openColor(color: UIColor(red: 244 / 255.0, green: 244 / 255.0, blue: 244 / 255.0, alpha: 1.0), forState: .normal)
        //: btn.setBackgroundColor(color: UIColor.init(hex: "#FF5372")!, forState: .selected)
        btn.openColor(color: UIColor(hex: (String(constAmValue)))!, forState: .selected)
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_female_nv_nor"), for: .normal)
        btn.setImage(UIImage.locationName(name: (String(constExtraUrl) + user_liteMsg.replacingOccurrences(of: "speaker", with: "r"))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_female_nv_sel"), for: .selected)
        btn.setImage(UIImage.locationName(name: (String(noti_assignPath.prefix(6)) + "male_nv" + String(k_atValue))), for: .selected)
        //: btn.setTitle("Female".localized, for: .normal)
        btn.setTitle((String(app_orientationContent.prefix(6))).localized, for: .normal)
        //: btn.setTitle("Female".localized, for: .selected)
        btn.setTitle((String(app_orientationContent.prefix(6))).localized, for: .selected)
        //: btn.setTitleColor(.black, for: .normal)
        btn.setTitleColor(.black, for: .normal)
        //: btn.setTitleColor(.white, for: .selected)
        btn.setTitleColor(.white, for: .selected)
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.titleLabel?.font = UIFont.pingfangRugularFont(fontSize: 17)
        btn.titleLabel?.font = UIFont.bonePlace(fontSize: 17)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var commitButton: UIButton = {
    private lazy var commitButton: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: btn.isEnabled = false
        btn.isEnabled = false
        //: btn.setTitleColor(UIColor.white, for: .normal)
        btn.setTitleColor(UIColor.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 17)
        btn.titleLabel?.font = UIFont.bigGallery(type: .Medium, fontSize: 17)
        //: btn.setTitle("Next".localized, for: .normal)
        btn.setTitle((String(k_strengthStr)).localized, for: .normal)
        //: btn.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: ScreenWidth-60, height: 50)), for: .normal)
        btn.setBackgroundImage(UIImage.beforeCan(colors: UIColor.minimizeOn(), size: CGSize(width: main_logStr - 60, height: 50)), for: .normal)
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - Public Event

//: extension TalkingLoginGenderVC {
extension FenceThen {
    //: private func func__checkNextBtnState() {
    private func activity() {
        //: if DismissReactiveCompatible.share.userFillInfoMode.sex == "1" {
        if DismissReactiveCompatible.share.userFillInfoMode.sex == "1" {
            //: self.commitButton.isEnabled = true
            self.commitButton.isEnabled = true
            //: self.maleButton.isSelected = true
            self.maleButton.isSelected = true
            //: self.femaleButton.isSelected = false
            self.femaleButton.isSelected = false
            //: } else if DismissReactiveCompatible.share.userFillInfoMode.sex == "2"{
        } else if DismissReactiveCompatible.share.userFillInfoMode.sex == "2" {
            //: self.commitButton.isEnabled = true
            self.commitButton.isEnabled = true
            //: self.maleButton.isSelected = false
            self.maleButton.isSelected = false
            //: self.femaleButton.isSelected = true
            self.femaleButton.isSelected = true
            //: } else {
        } else {
            //: self.commitButton.isEnabled = false
            self.commitButton.isEnabled = false
            //: self.maleButton.isSelected = false
            self.maleButton.isSelected = false
            //: self.femaleButton.isSelected = false
            self.femaleButton.isSelected = false
        }
    }

    //: private func func__pushToNextProgress() {
    private func tarReport() {
        //        // 1.3 判断用户设备SIM卡归属地是否来自被剔除的国家或地区
        //        // 1.4 判断用户设备是否开启VPN
//        if DismissReactiveCompatible.share.appConfigMode.vpnSimCardCheck {
//
//            guard  !ManufacturerThen.isUsedProxy() && !ManufacturerThen.isUsedVPN() else {
//                self.func__showStatusBarErrorMsg(showMsg: "Network error. Check it and try again.".localized)
//                self.navigationController?.popToRootViewController(animated: true)
//                NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
//                RevenueMovementManagerRequest.func__reportDeviceID()
//                return
//            }
//        }
        /// 清除后面已填写过资料
//        let sex = DismissReactiveCompatible.share.userFillInfoMode.sex
//        DismissReactiveCompatible.share.userFillInfoMode = FootingInfoModel.init()
//        DismissReactiveCompatible.share.userFillInfoMode.sex = sex
//        let VC = ArcanumViewController()
//        self.navigationController?.pushViewController(VC, animated: true)

        //: view.endEditing(true)
        view.endEditing(true)
        //: ProgressHUD.show()
        InsertProgressHUD.momentShow()
        //: TalkingLoginRequestTool.req_updateUserInfo(params: ["sex":DismissReactiveCompatible.share.userFillInfoMode.sex]) { succeed, result, errorModel in
        FirRequestTool.userInfoReloadPushCompletion(params: [String(bytes: main_decideFormat.map{currentLive(path: $0)}, encoding: .utf8)!: DismissReactiveCompatible.share.userFillInfoMode.sex]) { succeed, result, _ in
            //: ProgressHUD.dismiss()
            InsertProgressHUD.detectDismiss()
            //: if succeed {
            if succeed {
                // 发送通知
                //: NotificationCenter.default.post(name: DID_LOGIN_SUCCESS_NOTIFICATION, object: nil, userInfo: result as! Dictionary<String, Any>)
                NotificationCenter.default.post(name: noti_groupUrl, object: nil, userInfo: result as! [String: Any])
                // 注册成功埋点
                //: TalkingAdjustManager.share.addEvent(key: "RegisterSuccess")
                ProgMedalReactiveCompatible.share.stem(key: (String(dataMediaMsg.suffix(5)) + "terS" + showSeekTitle.replacingOccurrences(of: "transit", with: "cc")))
                //: FirebaseAnalyticsManager.share.logEvent(name: "RegisterSuccess")
                LeaseTrailerThen.share.tupeloShowPlay(name: (String(dataMediaMsg.suffix(5)) + "terS" + showSeekTitle.replacingOccurrences(of: "transit", with: "cc")))
            }
        }
    }
}

// MARK: - Layout

//: extension TalkingLoginGenderVC {
extension FenceThen {
    // 添加视图
    //: private func setupSubviews() {
    private func calibrate() {
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: view.addSubview(logoTitleLabel)
        view.addSubview(logoTitleLabel)
        //: view.addSubview(genderTipLabel)
        view.addSubview(genderTipLabel)
        //: view.addSubview(maleButton)
        view.addSubview(maleButton)
        //: view.addSubview(femaleButton)
        view.addSubview(femaleButton)
        //: view.addSubview(commitButton)
        view.addSubview(commitButton)
    }

    // 添加约束
    //: private func setupSubViewsConstraint() {
    private func too() {
        //: logoTitleLabel.snp.makeConstraints { make in
        logoTitleLabel.snp.makeConstraints { make in
            //: make.top.equalTo(34)
            make.top.equalTo(34)
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
        }
        //: genderTipLabel.snp.makeConstraints { make in
        genderTipLabel.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.width.equalTo(ScreenWidth-60)
            make.width.equalTo(main_logStr - 60)
            //: make.top.equalTo(logoTitleLabel.snp.bottom).offset(6)
            make.top.equalTo(logoTitleLabel.snp.bottom).offset(6)
        }
        //: maleButton.snp.makeConstraints { make in
        maleButton.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.width.equalTo(actualWidth(w: 140))
            make.width.equalTo(actualWidth(w: 140))
            //: make.height.equalTo(actualWidth(w: 50))
            make.height.equalTo(actualWidth(w: 50))
            //: make.top.equalTo(genderTipLabel.snp.bottom).offset(20)
            make.top.equalTo(genderTipLabel.snp.bottom).offset(20)
        }
        //: femaleButton.snp.makeConstraints { make in
        femaleButton.snp.makeConstraints { make in
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.width.equalTo(actualWidth(w: 140))
            make.width.equalTo(actualWidth(w: 140))
            //: make.height.equalTo(actualWidth(w: 50))
            make.height.equalTo(actualWidth(w: 50))
            //: make.top.equalTo(genderTipLabel.snp.bottom).offset(20)
            make.top.equalTo(genderTipLabel.snp.bottom).offset(20)
        }
        //: commitButton.snp.makeConstraints { make in
        commitButton.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.width.equalTo(ScreenWidth-60)
            make.width.equalTo(main_logStr - 60)
            //: make.height.equalTo(50)
            make.height.equalTo(50)
            //: make.top.equalTo(maleButton.snp.bottom).offset(40)
            make.top.equalTo(maleButton.snp.bottom).offset(40)
        }
    }

    // 添加事件
    //: private func bindInteraction() {
    private func totalerchange() {
        //: commitButton.rx.tap
        commitButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.func__pushToNextProgress()
                self.tarReport()

                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: femaleButton.rx.tap
        femaleButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: DismissReactiveCompatible.share.userFillInfoMode.sex = "2"
                DismissReactiveCompatible.share.userFillInfoMode.sex = "2"
                //: self.func__checkNextBtnState()
                self.activity()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: maleButton.rx.tap
        maleButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: DismissReactiveCompatible.share.userFillInfoMode.sex = "1"
                DismissReactiveCompatible.share.userFillInfoMode.sex = "1"
                //: self.func__checkNextBtnState()
                self.activity()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
    }
}
