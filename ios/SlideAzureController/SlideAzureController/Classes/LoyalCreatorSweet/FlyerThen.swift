
//: Declare String Begin

/*: "user/updateInfo" :*/
fileprivate let showBySomeoneName:[Character] = ["u","s","e","r","/","u","p"]
fileprivate let const_littleFormat:String = "ddisct"
fileprivate let app_remainingContent:[Character] = ["e","I","n","f","o"]

/*: "gallery/getGalleryByUid" :*/
fileprivate let constPanelKey:String = "gallecoat"
fileprivate let data_chinPath:[Character] = ["l","e","r","y","B","y","U","i","d"]

/*: "gallery/upload" :*/
fileprivate let show_shadeFormat:String = "gtenderler"
fileprivate let mainBridgePhoneUrl:String = "vertical beat acquire exist overy/upload"

/*: "gallery/deletePic" :*/
fileprivate let app_videoContent:String = "galleshot"
fileprivate let user_litePairContent:String = "letePicshadow forehead"

/*: "user/tagDelete" :*/
fileprivate let const_sampleName:String = "USER"
fileprivate let const_bottomMsg:String = "Deleabsolute giving owner"
fileprivate let mainSunTitle:[Character] = ["t","e"]

/*: "mf/user/tagEdit" :*/
fileprivate let showFunFormat:String = "mf/user/net opening media priority electron"
fileprivate let dataResultStr:[Character] = ["t","a","g","E","d","i","t"]

/*: "app/feedback" :*/
fileprivate let constIconId:String = "app/fbase post job"

/*: "IM/getMyBlackList" :*/
fileprivate let mainFoodText:[Character] = ["I","M","/","g","e","t","M"]
fileprivate let userPutStr:String = "guard tick mistyBla"

/*: "mf/user/getSetting" :*/
fileprivate let k_clickText:[Character] = ["m","f","/","u","s","e","r"]
fileprivate let const_squareData:String = "texture you element on create/ge"
fileprivate let appAnonymousPath:String = "now rounding outlet possibletSetting"

/*: "mf/user/setConfig" :*/
fileprivate let app_numberUrl:String = "about forth shadow off slidemf/user/"
fileprivate let constPingPath:String = "setCowhole gender contrast"

/*: "mf/user/settingStatus" :*/
fileprivate let const_surgeryEvaluationKey:[Character] = ["m","f","/","u","s","e","r","/","s","e","t","t","i"]
fileprivate let user_calendarMsg:String = "arc electronic gift submit arrowngS"
fileprivate let noti_descriptionSubFormat:String = "cutetus"

/*: "type" :*/
fileprivate let main_veryUnableData:[UInt8] = [0x99,0x94,0x9d,0x88]

/*: "user/lockAccount" :*/
fileprivate let showDryKey:String = "user/linstallation square acquire certain"
fileprivate let k_aliveImageValue:String = "OUNT"

/*: "mf/user/editInfo" :*/
fileprivate let dataBeginTitle:String = "mf/uswrite athletic pattern draw highlight"
fileprivate let constBeyondText:String = "ditInfoopposition sufficient lovely earn"

/*: "mf/userSign/list" :*/
fileprivate let userNeverId:[Character] = ["m","f","/","u","s","e"]
fileprivate let kActiveBeatId:[Character] = ["r","S","i","g"]
fileprivate let mainRelatedTitle:String = "n/listdecision about bean reply all"

/*: "mf/userSign/signIn" :*/
fileprivate let notiSlowContent:String = "long bettermf/use"
fileprivate let data_aidKey:String = "gnInplat until clear"

/*: "mf/dating/saveVideo" :*/
fileprivate let notiListenerUrl:String = "mf/dares point dominant related"
fileprivate let main_dynamicsTitle:String = "/saveVicell indicator"
fileprivate let k_reserveStandardName:String = "dmap"

/*: "mf/dating/getConfig" :*/
fileprivate let appPanelName:[Character] = ["m","f","/","d","a","t","i","n","g","/","g","e","t","C"]
fileprivate let k_capitalMsg:String = "onfollowingig"

/*: "mf/dating/deleteVideo" :*/
fileprivate let notiPickPath:String = "shape location certainmf/dat"
fileprivate let app_resultContent:String = "ELETE"
fileprivate let k_whenCoreName:[Character] = ["V","i","d","e","o"]

/*: "mf/greet/updateAudioRemark" :*/
fileprivate let main_natureText:[Character] = ["m","f","/","g","r","e","e","t","/"]
fileprivate let kConnectPath:[Character] = ["u","p","d","a","t","e","A","u","d","i","o","R","e","m","a","r","k"]

/*: "mf/greet/list" :*/
fileprivate let mainGoServiceMsg:String = "mf/gjoin need closed mp"
fileprivate let notiActualHappyFormat:String = "LIST"

/*: "mf/greet/add" :*/
fileprivate let dataLoadValue:String = "when offer lab agency slowmf/g"
fileprivate let main_airUrl:[Character] = ["a","d","d"]

/*: "mf/greet/del" :*/
fileprivate let dataFilterTitle:String = "concentration each topmf/g"
fileprivate let notiPremiumTitle:String = "eyebrow class/del"

/*: "mf/greet/getSettings" :*/
fileprivate let k_pictureGreetContinueId:[Character] = ["m","f","/","g","r","e","e","t","/","g","e","t","S","e","t","t","i","n","g","s"]

/*: "mf/greet/extraSetting" :*/
fileprivate let userLaboratoryItsPath:[Character] = ["m","f","/","g","r","e"]
fileprivate let appSubjectValue:[Character] = ["e","t","/","e","x","t","r","a","S","e","t","t","i","n","g"]

/*: "game/list" :*/
fileprivate let userLopeMsg:String = "game/lidetail worker hunting behavior ask"
fileprivate let noti_rateContent:String = "girlt"

/*: "category" :*/
fileprivate let user_outletMsg:[UInt8] = [0x6e,0x6c,0x7f,0x70,0x72,0x7a,0x7d,0x84]

fileprivate func sitGroup(busy num: UInt8) -> UInt8 {
    let value = Int(num) - 11
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  FlyerThen.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/9.
//

//: import UIKit
import UIKit

//: class TalkingMeRequestTool: NSObject {
class FlyerThen: NSObject {
    /// 资料完善
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_UpdateUserInfo(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func parent(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "user/updateInfo"
        reqModel.requestPath = (String(showBySomeoneName) + const_littleFormat.replacingOccurrences(of: "disc", with: "a") + String(app_remainingContent))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取用户相册
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_GalleryByUid(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func by(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "gallery/getGalleryByUid"
        reqModel.requestPath = (constPanelKey.replacingOccurrences(of: "coat", with: "r") + "y/getGal" + String(data_chinPath))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 上传图片
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_UploadPic(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func screenThrough(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "gallery/upload"
        reqModel.requestPath = (show_shadeFormat.replacingOccurrences(of: "tender", with: "al") + String(mainBridgePhoneUrl.suffix(8)))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除图片
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_DeletePic(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func model(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "gallery/deletePic"
        reqModel.requestPath = (app_videoContent.replacingOccurrences(of: "shot", with: "r") + "y/de" + String(user_litePairContent.prefix(7)))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除单个用户标签
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_DeleteTag(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func mediumFull(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "user/tagDelete"
        reqModel.requestPath = (const_sampleName.lowercased() + "/tag" + String(const_bottomMsg.prefix(4)) + String(mainSunTitle))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 编辑用户标签
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_EditTag(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func combine(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "mf/user/tagEdit"
        reqModel.requestPath = (String(showFunFormat.prefix(8)) + String(dataResultStr))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 意见反馈
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_Feedback(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func halfStaff(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "app/feedback"
        reqModel.requestPath = (String(constIconId.prefix(5)) + "eedback")
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取黑名单列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_BlackList(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func changeCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "IM/getMyBlackList"
        reqModel.requestPath = (String(mainFoodText) + String(userPutStr.suffix(4)) + "ckList")
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取设置信息接口
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_GetSetting(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func scenario(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "mf/user/getSetting"
        reqModel.requestPath = (String(k_clickText) + String(const_squareData.suffix(3)) + String(appAnonymousPath.suffix(8)))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 个人中心设置
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_SettingChange(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func displayMutual(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "mf/user/setConfig"
        reqModel.requestPath = (String(app_numberUrl.suffix(8)) + String(constPingPath.prefix(5)) + "nfig")
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 女性busy状态上报接口
    /// - Parameters:
    ///   - type: 0 :不活跃， 1:活跃
    ///   - completion: 回调
    //: class func req_settingStatus(type: Int, completion: @escaping FinishBlock) {
    class func lineUpOff(type: Int, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "mf/user/settingStatus"
        reqModel.requestPath = (String(const_surgeryEvaluationKey) + String(user_calendarMsg.suffix(3)) + noti_descriptionSubFormat.replacingOccurrences(of: "cute", with: "ta"))
        //: reqModel.params = ["type": type]
        reqModel.params = [String(bytes: main_veryUnableData.map{$0^237}, encoding: .utf8)!: type]
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 注销账号
    /// - Parameters:
    /// - completion: 回调
    //: class func req_LockAccount(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func age(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "user/lockAccount"
        reqModel.requestPath = (String(showDryKey.prefix(6)) + "ockAcc" + k_aliveImageValue.lowercased())
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 个人中心修改私信价格、开启语音授权
    /// - Parameters:
    /// - completion: 回调
    //: class func req_EditInfo(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func orientation(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "mf/user/editInfo"
        reqModel.requestPath = (String(dataBeginTitle.prefix(5)) + "er/e" + String(constBeyondText.prefix(7)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取签到日历列表
    /// - Parameter completion: 回调
    //: class func req_getUserSignList(completion: @escaping FinishBlock) {
    class func half(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "mf/userSign/list"
        reqModel.requestPath = (String(userNeverId) + String(kActiveBeatId) + String(mainRelatedTitle.prefix(6)))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 用户签到
    /// - Parameter completion: 回调
    //: class func req_userSignIn(completion: @escaping FinishBlock) {
    class func acceptSign(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "mf/userSign/signIn"
        reqModel.requestPath = (String(notiSlowContent.suffix(6)) + "rSign/si" + String(data_aidKey.prefix(4)))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 用户资料视频地址上传
    /// - Parameters:
    ///   - completion: 回调
    //: class func uploadVideoUserEdit(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func numbererrupt(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "mf/dating/saveVideo"
        reqModel.requestPath = (String(notiListenerUrl.prefix(5)) + "ting" + String(main_dynamicsTitle.prefix(7)) + k_reserveStandardName.replacingOccurrences(of: "map", with: "eo"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取当前用户上传的视频
    /// - Parameters:
    ///   - completion: 回调
    //: class func getUploadVideoInfo(completion: @escaping FinishBlock) {
    class func ownerCompletion(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "mf/dating/getConfig"
        reqModel.requestPath = (String(appPanelName) + k_capitalMsg.replacingOccurrences(of: "following", with: "f"))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除当前用户上传的视频
    /// - Parameters:
    ///   - completion: 回调
    //: class func deleteUserVideo(completion: @escaping FinishBlock) {
    class func prepare(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "mf/dating/deleteVideo"
        reqModel.requestPath = (String(notiPickPath.suffix(6)) + "ing/d" + app_resultContent.lowercased() + String(k_whenCoreName))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 更新语音备注
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_UpdateAudioRemark(completion: @escaping FinishBlock) {
    class func completionReply(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "mf/greet/updateAudioRemark"
        reqModel.requestPath = (String(main_natureText) + String(kConnectPath))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 自动打招呼-查看设置列表
    /// - Parameters: type: 1=语音， 2=文本
    ///   - completion: 回调
    //: class func req_GetGreetList(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func generateCounterrupt(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "mf/greet/list"
        reqModel.requestPath = (String(mainGoServiceMsg.prefix(4)) + "reet/" + notiActualHappyFormat.lowercased())
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 自动打招呼-添加自定义文案/语音/图片
    /// - Parameters: type: 1：语音；2：文案；3：图片; 4:私密照片
    ///   - completion: 回调
    //: class func req_UploadGreetAdd(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func subtitle(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "mf/greet/add"
        reqModel.requestPath = (String(dataLoadValue.suffix(4)) + "reet/" + String(main_airUrl))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 自动打招呼-设置音频备注
    ///   - completion: 回调
    //: class func req_UpdateAudioRemark(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func guidanceRes(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "mf/greet/updateAudioRemark"
        reqModel.requestPath = (String(main_natureText) + String(kConnectPath))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 自动打招呼-删除文案/音频
    ///   - completion: 回调
    //: class func req_DeleteAudio(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func conGo(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "mf/greet/del"
        reqModel.requestPath = (String(dataFilterTitle.suffix(4)) + "reet" + String(notiPremiumTitle.suffix(4)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 招呼设置-查看设置
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_GetGreetSetting(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func mark(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "mf/greet/getSettings"
        reqModel.requestPath = (String(k_pictureGreetContinueId))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 图片打招呼-额外配置礼物信息
    /// - Parameters: unlockGiftId:解锁礼物id, isBurn:设置的阅后即焚状态
    ///   - completion: 回调
    //: class func req_UploadGreetExtraSetting(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func re(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "mf/greet/extraSetting"
        reqModel.requestPath = (String(userLaboratoryItsPath) + String(appSubjectValue))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取游戏列表
    /// - Parameters:
    ///   - category: 1=我的页面，2=私聊页面
    ///   - completion: 回调
    //: class func req_getGameList(category: Int, completion: @escaping FinishBlock) {
    class func red(category: Int, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "game/list"
        reqModel.requestPath = (String(userLopeMsg.prefix(7)) + noti_rateContent.replacingOccurrences(of: "girl", with: "s"))
        //: reqModel.params = ["category": category]
        reqModel.params = [String(bytes: user_outletMsg.map{sitGroup(busy: $0)}, encoding: .utf8)!: category]
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }
}
