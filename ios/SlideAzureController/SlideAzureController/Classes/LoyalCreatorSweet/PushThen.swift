
//: Declare String Begin

/*: "fee" :*/
fileprivate let user_ticketTitle:String = "triggere"

/*: "VIPFee" :*/
fileprivate let noti_greetValue:String = "VIPFeeupper style train"

/*: "chattingNum" :*/
fileprivate let app_eyeId:String = "mapatti"
fileprivate let const_resumeUrl:[Character] = ["n","g","N","u","m"]

/*: "You're already in her live room" :*/
fileprivate let app_averageMsg:[UInt8] = [0x6d,0x6f,0x6f,0x72,0x20,0x65,0x76,0x69,0x6c,0x20,0x72,0x65,0x68,0x20,0x6e,0x69,0x20,0x79,0x64,0x61,0x65,0x72,0x6c,0x61,0x20,0x65,0x72,0x27,0x75,0x6f,0x59]

/*: "toUid" :*/
fileprivate let data_countryCheckName:[UInt8] = [0x6a,0x71,0x4b,0x77,0x7a]

private func resentArrowVehicle(reject num: UInt8) -> UInt8 {
    return num ^ 30
}

/*: "momentId" :*/
fileprivate let const_packData:[UInt8] = [0x7b,0x79,0x7b,0x73,0x78,0x62,0x5f,0x72]

private func weightStart(his num: UInt8) -> UInt8 {
    return num ^ 22
}

/*: "extra" :*/
fileprivate let k_clipComponentValue:[UInt8] = [0xdb,0xc6,0xca,0xcc,0xdf]

private func termsFile(deadline num: UInt8) -> UInt8 {
    return num ^ 190
}

/*: "uid" :*/
fileprivate let appHolderKey:[UInt8] = [0xd,0x11,0x1c]

private func pushUp(bold num: UInt8) -> UInt8 {
    return num ^ 120
}

/*: "onlineStatus" :*/
fileprivate let notiSmoothUrl:[UInt8] = [0xa7,0xa6,0xa4,0xa1,0xa6,0xad,0x9b,0xbc,0xa9,0xbc,0xbd,0xbb]

private func libraryClick(until num: UInt8) -> UInt8 {
    return num ^ 200
}

/*: "isNewUser" :*/
fileprivate let const_appealPath:[UInt8] = [0x64,0x7e,0x43,0x68,0x7a,0x58,0x7e,0x68,0x7f]

private func larvaConvert(ply num: UInt8) -> UInt8 {
    return num ^ 13
}

/*: "userStatus" :*/
fileprivate let app_fairStr:[UInt8] = [0xf,0x9,0x1f,0x8,0x29,0xe,0x1b,0xe,0xf,0x9]

/*: "Account is restricted！" :*/
fileprivate let data_sText:[Character] = ["A","c","c","o","u","n","t"," ","i","s"," ","r","e","s","t","r","i"]
fileprivate let data_decreaseInfoRefText:String = "others yourcted！"

/*: "Failed to enter the chat room. Please try again later" :*/
fileprivate let showExhibitData:[UInt8] = [0x1e,0x39,0x31,0x34,0x3d,0x3c,0x78,0x2c,0x37,0x78,0x3d,0x36,0x2c,0x3d,0x2a,0x78,0x2c,0x30,0x3d,0x78,0x3b,0x30,0x39,0x2c,0x78,0x2a,0x37,0x37,0x35,0x76,0x78,0x8,0x34,0x3d,0x39,0x2b,0x3d,0x78,0x2c,0x2a,0x21,0x78,0x39,0x3f,0x39,0x31,0x36,0x78,0x34,0x39,0x2c,0x3d,0x2a]

private func southSide(door num: UInt8) -> UInt8 {
    return num ^ 88
}

/*: "url" :*/
fileprivate let const_scienceMethodDiscussionMsg:[Character] = ["u","r","l"]

/*: "userDetail" :*/
fileprivate let kIncomeMessage:String = "which"
fileprivate let dataKindValue:[Character] = ["s","e","r","D","e","t","a","i","l"]

/*: "&type=6" :*/
fileprivate let data_sayPresentName:[Character] = ["&","t","y","p","e","=","6"]

/*: "&type=7" :*/
fileprivate let app_spaceKey:String = "bit official manual&type=7"

/*: "amount" :*/
fileprivate let userMotivationMsg:[UInt8] = [0xd3,0xdf,0xdd,0xc7,0xdc,0xc6]

private func discItem(street num: UInt8) -> UInt8 {
    return num ^ 178
}

/*: "https://" :*/
fileprivate let mainMechanicalUrl:String = "hbugbugp"

/*: "http://" :*/
fileprivate let mainComputeTitle:String = "http:/ethical rate"
fileprivate let mainRewardName:String = "/"

/*: "transparency" :*/
fileprivate let mainOverStr:String = "trahalfp"
fileprivate let userCropKey:[Character] = ["c","y"]

/*: "version= :*/
fileprivate let const_monthMessage:String = "compute"
fileprivate let app_exposureKey:[Character] = ["e","r","s","i","o","n","="]

/*: &packageId= :*/
fileprivate let const_voiceValue:String = "available together license hang mortal&package"
fileprivate let k_soccerContent:[Character] = ["I","d","="]

/*: &bundleId= :*/
fileprivate let userThenUrl:String = "shirt injury gender edit&bund"
fileprivate let app_staffPath:String = "remain"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  PushThen.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/31.
//

//: import UIKit
import UIKit

//: public class PushThen: NSObject {
public class PushThen: NSObject {
    //: @objc static public let share = PushThen()
    @objc public static let share = PushThen()
    //: private override init() {}
    override private init() {}
}

//: extension PushThen {
extension PushThen {
    // MARK: - 随机视频入口

    //: func func__pushToRandomVideoVC(isBeginRand: Bool) {
    func popOutClose(isBeginRand: Bool) {
        //: uploadRecord.uploadRecordEvent(eventID: ClickInRandomvideo)
        const_pathMsg.tabKeep(eventID: dataLiveMsg)
        //: guard TalkingSocketManager.shared.isInfo == false else {
        guard SantaLuciaFirSocketDelegate.shared.isInfo == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.enablece(showMsg: kLikeStr)
            //: return
            return
        }
        //: TalkingPermissionTool.checkCameraAndMicrophone { isOpen in
        SpecialPermissionTool.remainMicrophone { isOpen in
            //: guard isOpen == true else { return }
            guard isOpen == true else { return }
            //: TalkingQuickVideoManager.req_videoMatchCheck() { succeed, result, errorModel in
            OfVideoManager.aAdvanced { succeed, result, _ in
                //: guard succeed else { return }
                guard succeed else { return }
                //: let json = JSON(result ?? [String: Any]())
                let json = JSON(result ?? [String: Any]())
                //: let vc = TalkingRandomVideoViewController()
                let vc = LeaseFourErrorDelegate()
                //: vc.fee = json["fee"].intValue
                vc.fee = json[(user_ticketTitle.replacingOccurrences(of: "trigger", with: "fe"))].intValue
                //: vc.vipFee = json["VIPFee"].intValue
                vc.vipFee = json[(String(noti_greetValue.prefix(6)))].intValue
                //: vc.chattingNum = json["chattingNum"].intValue
                vc.chattingNum = json[(app_eyeId.replacingOccurrences(of: "map", with: "ch") + String(const_resumeUrl))].intValue
                //: vc.isBeginRand = isBeginRand
                vc.isBeginRand = isBeginRand
                //: vc.isFirstRandow = isBeginRand
                vc.isFirstRandow = isBeginRand
                //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
                self.exitSchedule()?.navigationController?.pushViewController(vc, animated: true)
            }
        }
    }

    // MARK: - 完善资料

    //: func func__pushToFillUserInfoVC() {
    func duringNudgeAlbum() {
        //: DismissReactiveCompatible.share.userFillInfoMode = UserFillInfoModel.init()
        DismissReactiveCompatible.share.userFillInfoMode = FootingInfoModel()
        //: let VC = TalkingLoginGenderVC()
        let VC = FenceThen()
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = doingGoing()
        //: if currentVC != nil {
        if currentVC != nil {
            //: currentVC?.navigationController?.pushViewController(VC, animated: true)
            currentVC?.navigationController?.pushViewController(VC, animated: true)
        }
    }

    // MARK: - 绑定邮箱

    /// - Parameters:
    ///   - type: 类型
    ///   - isShowBack: 是否显示返回按钮
    //: func func__pushToLockUserEmailVC(isShowBack: Bool = true) {
    func tabAcross(isShowBack: Bool = true) {
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = doingGoing()
        //: if currentVC != nil {
        if currentVC != nil {
            //: if currentVC!.isKind(of: TalkingLoginBindEmailVC.self) ||
            if currentVC!.isKind(of: ArtTitleureEmailVc.self) ||
                //: currentVC!.isKind(of: TalkingLoginMainViewController.self) {
                currentVC!.isKind(of: TillViewController.self)
            {
                //: return
                return
            }
            //: let vc = TalkingLoginBindEmailVC()
            let vc = ArtTitleureEmailVc()
            //: vc.isBack = isShowBack
            vc.isBack = isShowBack
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)
        }
    }

    // MARK: - 跳转个人资料

    //: func func__pushToUserDetailVC(uid: String?) {
    func sessionSomebody(uid: String?) {
        //: guard let currentVC = self.currentViewController() else { return }
        guard let currentVC = self.exitSchedule() else { return }
        //: if let liveVC = currentVC as? TalkingLiveBeautifyViewController, TalkingLiveManager.shared().isLive {
        if let liveVC = currentVC as? WritingViewDelegate, ManagerPushListener.nowPanel().isLive {
            // 从主播页跳转个人资料时，切小屏模式
            //: liveVC.topView.miniButtonClick()
            liveVC.topView.coatButton()

            //: } else if let profileVC = currentVC as? TalkingUserDetailViewController {
        } else if let profileVC = currentVC as? CoverViewDelegate {
            // 同一用户的资料页已经在顶部，只刷新
            //: if profileVC.uid == uid {
            if profileVC.uid == uid {
                //: profileVC.requestAllData()
                profileVC.someShould()
                //: return
                return
            }
        }

        //: let vc = TalkingUserDetailViewController(tempUid: uid)
        let vc = CoverViewDelegate(tempUid: uid)
        //: getNavigationController()?.pushViewController(vc, animated: true)
        controller()?.pushViewController(vc, animated: true)
    }

    // MARK: - 观众侧进入直播间

    /// 观众侧跳转到直播间
    /// - Parameters:
    ///   - uid: 主播id
    ///   - enterType: 入口
    //: func func_audiencePushToLiveRoomVC(uid: String, enterType: LiveEnterType) {
    func alongMax(uid: String, enterType: SightEnterType) {
        //: guard TalkingPermissionTool.isLiveOrPartyActive() == false else { return }
        guard SpecialPermissionTool.once() == false else { return }

        // 同一直播间只能进一次
        //: guard String(TalkingLiveManager.shared().liveRoomModel.streamerInfo.uid) != uid else {
        guard String(ManagerPushListener.nowPanel().liveRoomModel.streamerInfo.uid) != uid else {
            //: self.func__showStatusBarErrorMsg(showMsg: "You're already in her live room".localized)
            self.enablece(showMsg: String(bytes: app_averageMsg.reversed(), encoding: .utf8)!.localized)
            //: return
            return
        }
        // 校验接口，通过后进入直播间
        //: TalkingAudienceManager().req_enterLiveRoom(streamerUid: uid) { succeed, result, errorModel in
        BarrierReactiveCompatible().appearance(streamerUid: uid) { succeed, result, _ in
            //: guard succeed else { return }
            guard succeed else { return }
            //: guard let livemodel = TalkingLiveRoomModel.deserialize(from: result as? Dictionary) else { return }
            guard let livemodel = DwellHiddenMeasurable.deserialize(from: result as? Dictionary) else { return }
            // 退出之前的直播间
            //: let currentVC = self.func__getCurrentActivityVC()
            let currentVC = self.doingGoing()
            //: if let vcArr = currentVC?.navigationController?.viewControllers {
            if let vcArr = currentVC?.navigationController?.viewControllers {
                //: for index in 0..<(vcArr.count) {
                for index in 0 ..< (vcArr.count) {
                    //: let vc = vcArr[index]
                    let vc = vcArr[index]
                    //: if vc.isKind(of: TalkingLivePullStreamsViewController.self) == true {
                    if vc.isKind(of: RelativeViewController.self) == true {
                        //: (vc as! TalkingLivePullStreamsViewController).popCurrentViewController()
                        (vc as! RelativeViewController).creation()
                    }
                }
            }

            // 进入新直播间
            //: TalkingLiveManager.shared().liveRoomModel = livemodel
            ManagerPushListener.nowPanel().liveRoomModel = livemodel
            //: let liveVc = TalkingLivePullStreamsViewController()
            let liveVc = RelativeViewController()
            //: liveVc.type = enterType
            liveVc.type = enterType
            //: currentVC?.navigationController?.pushViewController(liveVc, animated: true)
            currentVC?.navigationController?.pushViewController(liveVc, animated: true)
            // 埋点
            //: switch enterType {
            switch enterType {
            //: case .match:
            case .match:
                //: uploadRecord.uploadRecordEvent(eventID: ClickMatchLiveWindowNoP, toUid: uid)
                const_pathMsg.tabKeep(eventID: notiTitleKey, toUid: uid)
            //: case .userDetail:
            case .userDetail:
                //: uploadRecord.uploadRecordEvent(eventID: ClickProfileLiveWindowNoP, toUid: uid)
                const_pathMsg.tabKeep(eventID: dataLevelKey, toUid: uid)
            //: case .unknown: break
            case .unknown: break
            }
        }
    }

    // MARK: - 跳转聊天

    /// 跳转到消息列表页
    /// - Parameter isHalf: 是否半屏展示
    //: func func__pushToChatListVC(isHalfView: Bool = false) {
    func futurismView(isHalfView: Bool = false) {
        //: let vc = TalkingChatListViewController(isHalfView: isHalfView)
        let vc = BibliographyViewDelegate(isHalfView: isHalfView)
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = doingGoing()
        //: if isHalfView == false {
        if isHalfView == false {
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)
            //: } else {
        } else {
            //: currentVC?.addChild(vc)
            currentVC?.addChild(vc)
            //: currentVC?.view.addSubview(vc.view)
            currentVC?.view.addSubview(vc.view)
        }
    }

    /// 跳转到消息聊天页
    /// - Parameters:
    ///   - chatID: 对方Id
    ///   - isFrom: 私聊页入口
    /// - Returns: 消息聊天页

    //: func func__pushToPriveteChatVC(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, completion: ((_ vc: TalkingPrivateChatController) -> Void)? = nil ) {
    func resolutionBegin(chatID: String, isFrom: EnablestageOthersParaFromEnum = .Normal, completion: ((_ vc: SmallControllerDelegate) -> Void)? = nil) {
        //: guard chatID.count > 0 else { return }
        guard chatID.count > 0 else { return }

        //: if chatID != DepictionThen.getXiaoMiID() {
        if chatID != DepictionThen.xiao() { // 系统消息
            //: ProgressHUD.show()
            InsertProgressHUD.momentShow()
            //: let param = ["toUid": chatID, "momentId": "0", "extra": "1"]
            let param = [String(bytes: data_countryCheckName.map{resentArrowVehicle(reject: $0)}, encoding: .utf8)!: chatID, String(bytes: const_packData.map{weightStart(his: $0)}, encoding: .utf8)!: "0", String(bytes: k_clipComponentValue.map{termsFile(deadline: $0)}, encoding: .utf8)!: "1"]
            //: TalkingChatRequestTool.req_refreshUserChatInfo(param: param) { [weak self] succeed, result, errorModel in
            TamperRequestTool.lipClub(param: param) { [weak self] succeed, result, _ in
                //: ProgressHUD.dismiss()
                InsertProgressHUD.detectDismiss()
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: guard succeed else { return }
                guard succeed else { return }

                //: let userWrap = ExecuteBetterTransformable.func__transformedChatinfo(userDic: result as! Dictionary<String, Any>)
                let userWrap = ExecuteBetterTransformable.statusLabel(userDic: result as! [String: Any])
                //: DBUserInfoManager.cache_updateMsgUserInfoAndPostNotif(with: userWrap)
                CutInfoManager.angleWith(with: userWrap)
                // 更新消息列表用户在线状态
                //: let userInfo = ["uid": chatID,
                let userInfo = [String(bytes: appHolderKey.map{pushUp(bold: $0)}, encoding: .utf8)!: chatID,
                                //: "onlineStatus": userWrap.onlineStatus,
                                String(bytes: notiSmoothUrl.map{libraryClick(until: $0)}, encoding: .utf8)!: userWrap.onlineStatus,
                                //: "isNewUser":userWrap.isNewUser,
                                String(bytes: const_appealPath.map{larvaConvert(ply: $0)}, encoding: .utf8)!: userWrap.isNewUser,
                                //: "userStatus": userWrap.userStatus]
                                String(bytes: app_fairStr.map{$0^122}, encoding: .utf8)!: userWrap.userStatus]
                //: NotificationCenter.default.post(name: MSGLIST_UPDATE_ONLINESTATUS_NOTIFICATION,
                NotificationCenter.default.post(name: show_voiceMsg,
                                                //: object: nil,
                                                object: nil,
                                                //: userInfo: userInfo)
                                                userInfo: userInfo)
                //: if userWrap.userStatus == 3 {
                if userWrap.userStatus == 3 { /// 被封号
                    //: self.func__showStatusBarErrorMsg(showMsg: "Account is restricted！".localized)
                    self.enablece(showMsg: (String(data_sText) + String(data_decreaseInfoRefText.suffix(5))).localized)
                    //: return
                    return
                }
                //: DispatchQueue.main.async {
                DispatchQueue.main.async {
                    //: let chatVC = self.func__canPushToPriveteChatVC(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
                    let chatVC = self.chat(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
                    //: completion?(chatVC)
                    completion?(chatVC)
                }
            }
            //: }else{
        } else {
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: let chatVC = self.func__canPushToPriveteChatVC(chatID: chatID, isFrom: isFrom, userWrap: ExecuteBetterTransformable())
                let chatVC = self.chat(chatID: chatID, isFrom: isFrom, userWrap: ExecuteBetterTransformable())
                //: completion?(chatVC)
                completion?(chatVC)
            }
        }
    }

    //: func func__canPushToPriveteChatVC(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, userWrap: ExecuteBetterTransformable ) -> TalkingPrivateChatController {
    func chat(chatID: String, isFrom: EnablestageOthersParaFromEnum = .Normal, userWrap: ExecuteBetterTransformable) -> SmallControllerDelegate {
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = doingGoing()
        //: if isFrom != .LiveRoom, let vcArr = currentVC?.navigationController?.viewControllers {
        if isFrom != .LiveRoom, let vcArr = currentVC?.navigationController?.viewControllers {
            //: for index in 0..<(vcArr.count) {
            for index in 0 ..< (vcArr.count) {
                //: if vcArr[index] .isKind(of: TalkingPrivateChatController.self) == true {
                if vcArr[index].isKind(of: SmallControllerDelegate.self) == true {
                    //: let vc = vcArr[index] as! TalkingPrivateChatController
                    let vc = vcArr[index] as! SmallControllerDelegate
                    //: if vc.msgView.targetID == chatID {
                    if vc.msgView.targetID == chatID {
                        //: currentVC?.navigationController?.popToViewController(vc, animated: true)
                        currentVC?.navigationController?.popToViewController(vc, animated: true)
                        //: return vc
                        return vc
                    }
                }
            }
        }

        //: let vc = TalkingPrivateChatController(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
        let vc = SmallControllerDelegate(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
        //: if isFrom == .LiveRoom {
        if isFrom == .LiveRoom {
            //: currentVC?.addChild(vc)
            currentVC?.addChild(vc)
            //: currentVC?.view.addSubview(vc.view)
            currentVC?.view.addSubview(vc.view)
            //: } else {
        } else {
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)
        }
        //: return vc
        return vc
    }

    // MARK: - 跳转群聊

    //: func func__pushToGroupChat(groupId: String) {
    func miscalculation(groupId: String) {
        //: guard groupId.count > 0 else { return }
        guard groupId.count > 0 else { return }
        //: ProgressHUD.show()
        InsertProgressHUD.momentShow()
        //: TalkingChatRequestTool.req_checkChatRoom(roomId: groupId) { succeed, result, errorModel in
        TamperRequestTool.halfCompletion(roomId: groupId) { succeed, result, errorModel in
            //: ProgressHUD.dismiss()
            InsertProgressHUD.detectDismiss()
            //: guard succeed == true else {
            guard succeed == true else {
                //: if errorModel?.errorCode == 106 {
                if errorModel?.errorCode == 106 {
                    //: } else {
                } else {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                    self.enablece(showMsg: errorModel?.errorMsg ?? "")
                    //: if errorModel?.errorCode == 103 {
                    if errorModel?.errorCode == 103 { // 不在家族里
                    }
                }

                //: return
                return
            }

            //: guard let data = result as? [String: Any] else {
            guard let data = result as? [String: Any] else {
                //: return
                return
            }

            //: if DepictionThen.isGroupChat(groupId) {
            if DepictionThen.doingPrepare(groupId) {
                //: } else {
            } else { // 公共聊天室
                //: V2TIMManager.sharedInstance().joinGroup(groupId, msg: nil) {
                V2TIMManager.sharedInstance().joinGroup(groupId, msg: nil) {
                    //: guard self.func__checkIsGroupChatVC(roomId: groupId) == false else { return }
                    guard self.birthdayInmate(roomId: groupId) == false else { return }
                    //: let vc = TalkingGroupChatController(groupType: GroupType_Meeting, infoData: data)
                    let vc = SmallChatController(groupType: GroupType_Meeting, infoData: data)
                    //: self.func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
                    self.doingGoing()?.navigationController?.pushViewController(vc, animated: true)

                    //: } fail: { code, desc in
                } fail: { code, _ in
                    //: if code == 10013 {
                    if code == 10013 { // 程序异常退出，未退出聊天室
                        //: guard self.func__checkIsGroupChatVC(roomId: groupId) == false else { return }
                        guard self.birthdayInmate(roomId: groupId) == false else { return }
                        //: let vc = TalkingGroupChatController(groupType: GroupType_Meeting, infoData: data)
                        let vc = SmallChatController(groupType: GroupType_Meeting, infoData: data)
                        //: self.func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
                        self.doingGoing()?.navigationController?.pushViewController(vc, animated: true)
                        //: } else {
                    } else {
                        //: self.func__showStatusBarErrorMsg(showMsg: "Failed to enter the chat room. Please try again later".localized)
                        self.enablece(showMsg: String(bytes: showExhibitData.map{southSide(door: $0)}, encoding: .utf8)!.localized)
                    }
                }
            }
        }
    }

    /// 检测堆栈中是否有群聊控制器，有则跳转到群聊页
    /// - Parameter roomId: 房间Id
    /// - Returns: 检测结果（true: 有; false: 无）
    //: private func func__checkIsGroupChatVC(roomId: String) -> Bool {
    private func birthdayInmate(roomId: String) -> Bool {
        //: let currentVC = self.func__getCurrentActivityVC()
        let currentVC = self.doingGoing()
        //: if let vcArr = currentVC?.navigationController?.viewControllers {
        if let vcArr = currentVC?.navigationController?.viewControllers {
            //: for index in 0..<(vcArr.count) {
            for index in 0 ..< (vcArr.count) {
                //: if vcArr[index].isKind(of: TalkingGroupChatController.self) == true {
                if vcArr[index].isKind(of: SmallChatController.self) == true {
                    //: let vc = vcArr[index] as! TalkingGroupChatController
                    let vc = vcArr[index] as! SmallChatController
                    //: if vc.infoModel.roomId == roomId {
                    if vc.infoModel.roomId == roomId {
                        //: currentVC?.navigationController?.popToViewController(vc, animated: true)
                        currentVC?.navigationController?.popToViewController(vc, animated: true)
                        //: return true
                        return true
                    }
                }
            }
        }

        //: return false
        return false
    }

    // MARK: - 真人认证

    //: func func__pushUserVerifyController(toast: String?) {
    func undercoverOperation(toast _: String?) {
        //: let tpAuth = TPUserAuth(rawValue: DismissReactiveCompatible.share.loginUserMode.isTPAuth)
        let tpAuth = MovementBasicType(rawValue: DismissReactiveCompatible.share.loginUserMode.isTPAuth)
        //: switch(tpAuth) {
        switch tpAuth {
        //: case .unknown, .isRefused:
        case .unknown, .isRefused:
            //: let vc = TalkingStatementEquityVC()
            let vc = TransferViewController()
//            vc.toastStr = toast ?? ""
            //: let currentVC = func__getCurrentActivityVC()
            let currentVC = doingGoing()
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)

        //: case .isOnGoing:
        case .isOnGoing:
            //: let vc = TalkingEditProfilesVC()
            let vc = CutProfilesVc()
            //: let currentVC = func__getCurrentActivityVC()
            let currentVC = doingGoing()
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)

        //: default:
        default:
            //: break
            break
        }
    }

    // MARK: - 任意用户的动态列表

    //: func func__pushUserPostListWithUid(uid: String) {
    func homeError(uid _: String) {}

    // MARK: - 反馈feebook

    //: func func__pushFeedbackVC() {
    func countoBeauty() {
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = doingGoing()
        //: let vc = TalkingFeedbackVC.init()
        let vc = ProgViewController()
        //: currentVC?.navigationController?.pushViewController(vc, animated: true)
        currentVC?.navigationController?.pushViewController(vc, animated: true)
    }

    // MARK: - 消息跳转

    //: func func__actionPushForMessage(jumpModel: TalkingMessageJumpModel) {
    func orientationAction(jumpModel: ArmModelType) {
        //: if jumpModel.jumpKey == "url" {
        if jumpModel.jumpKey == (String(const_scienceMethodDiscussionMsg)) {
            //: func__pushToWebVC(urlStr: jumpModel.url)
            plumeBright(urlStr: jumpModel.url)
            //: } else if jumpModel.jumpKey == "userDetail"{
        } else if jumpModel.jumpKey == (kIncomeMessage.replacingOccurrences(of: "which", with: "u") + String(dataKindValue)) {}
    }

    // MARK: - 会员订阅网页

    //: @objc public func func__pushToSubscribePageWebVC() {
    @objc public func forceBack() {
        //: PushThen.share.func__pushToWebVC(webViewType: .SubscribePage)
        PushThen.share.plumage(webViewType: .SubscribePage)
    }

    // MARK: - 设置密码页面

    /// 跳转设置密码页面
    /// - Parameters:
    ///   - type: 手机号/邮箱
    ///   - content: 展示内容
    ///   - isNewPwd: 是否新设置密码（修改需要验证码）
    //: func func__pushToSetNewPasswordVC(type: LoginType, content: String, isNewPwd: Bool) {
    func toValid(type: ContainerCustomReflectable, content: String, isNewPwd: Bool) {
        //: let vc = TalkingSetNewPasswordVC()
        let vc = SparkRecentViewController()
        //: vc.type = type
        vc.type = type
        //: vc.content = content
        vc.content = content
        //: vc.isNewPwd = isNewPwd
        vc.isNewPwd = isNewPwd
        //: func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
        doingGoing()?.navigationController?.pushViewController(vc, animated: true)
    }
}

// MARK: - 跳转H5

//: extension PushThen {
extension PushThen {
    // 跳转全屏H5入口
    //: func func__pushToWebVC(webViewType: WebUrlType) {
    func plumage(webViewType: RelativeWeddingQuickLookable) {
        //: func__pushToWebVC(webViewType: webViewType, webConfig: TalkingWebConfig())
        flowScene(webViewType: webViewType, webConfig: EndConfig())
    }

    /// 系统主动拨打弹窗，余额不足跳转充值/订阅页
    //: func func__jumpVideoCallToWebRecharge(popupCallIndex: Int, appendParams: String = "&type=6") {
    func manner(popupCallIndex: Int, appendParams: String = (String(data_sayPresentName))) {
        //: let view = TalkingInsufficientBalanceView.init()
        let view = InmateAimReactiveCompatible()
        //: view.show()
        view.favourite()
        //: view.openBtnClock = { [weak self] in
        view.openBtnClock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if popupCallIndex == 1 {
            if popupCallIndex == 1 { // 半屏充值页
                //: self.func__pushToHalfWebVC(webViewType: .RechargeHalfPage)
                self.obligatoEvent(webViewType: .RechargeHalfPage)
                //: } else if popupCallIndex == 2 {
            } else if popupCallIndex == 2 { // 会员订阅弹窗
                //: func__pushToSubscribeAlert(appendParams: appendParams)
                enablelyKey(appendParams: appendParams)
            }
        }
    }

    /// 余额不足跳转充值/订阅页
    /// - Parameters:
    ///   - clickEvent: 埋点事件
    ///   - sufficient: 余额是否充足（默认true）
    //: func func__jumpToWebRecharge(clickEvent: String = "", sufficient: Bool = true, appendParams: String = "&type=7") {
    func twoParams(clickEvent: String = "", sufficient: Bool = true, appendParams: String = (String(app_spaceKey.suffix(7)))) {
        //: let payWinType = DismissReactiveCompatible.share.appUserConfigMode.payWinType
        let payWinType = DismissReactiveCompatible.share.appUserConfigMode.payWinType
        // 样式：半屏充值页 || (已订阅 && 余额不足)

        //: let view = TalkingInsufficientBalanceView.init()
        let view = InmateAimReactiveCompatible()
        //: view.show()
        view.favourite()
        //: view.openBtnClock = { [weak self] in
        view.openBtnClock = { [weak self] in
            //: guard let self = self else {
            guard let self = self else {
                //: return
                return
            }
            //: if payWinType == 1 || (DismissReactiveCompatible.share.loginUserMode.loungePlus && sufficient == false) {
            if payWinType == 1 || (DismissReactiveCompatible.share.loginUserMode.loungePlus && sufficient == false) { // 半屏充值页
                //: self.func__pushToHalfWebVC(webViewType: .RechargeHalfPage, clickEvent: clickEvent)
                self.obligatoEvent(webViewType: .RechargeHalfPage, clickEvent: clickEvent)
                //: } else if payWinType == 2 {
            } else if payWinType == 2 { // 会员订阅弹窗
                //: func__pushToSubscribeAlert(appendParams: appendParams)
                enablelyKey(appendParams: appendParams)
            }
        }
    }

    /// 跳转会员订阅弹窗
    /// - Parameter appendParams: url拼接参数（假视频流程拼接："&type=6；默认流程："&type=7"）
    //: func func__pushToSubscribeAlert(appendParams: String = "&type=7") {
    func enablelyKey(appendParams: String = (String(app_spaceKey.suffix(7)))) {
        //: var urlStr = TalkingWebManager.getFullUrl(urlType: .SubscribeAlert)
        var urlStr = InjuryArtManager.collect(urlType: .SubscribeAlert)
        //: urlStr.append(appendParams)
        urlStr.append(appendParams)
        //: PushThen.share.func__pushToWebVC(urlStr: urlStr)
        PushThen.share.plumeBright(urlStr: urlStr)
        // 曝光用户数埋点
        //: uploadRecord.uploadRecordEvent(eventID: subscribtionRecordButton, parameterStr: ["amount": 0])
        const_pathMsg.launchStr(eventID: const_sinceValue, parameterStr: [String(bytes: userMotivationMsg.map{discItem(street: $0)}, encoding: .utf8)!: 0])
    }

    /// 跳转半屏H5入口
    /// - Parameters:
    ///   - webViewType: 网页URL
    ///   - clickEvent: 埋点事件
    //: func func__pushToHalfWebVC(webViewType: WebUrlType, clickEvent: String = "") {
    func obligatoEvent(webViewType: RelativeWeddingQuickLookable, clickEvent: String = "") {
        //: var config = TalkingWebConfig.init()
        var config = EndConfig()
        //: config.showLoadingView = false
        config.showLoadingView = false
        //: config.isHalfView = true
        config.isHalfView = true
        //: func__pushToWebVC(webViewType: webViewType, webConfig: config)
        flowScene(webViewType: webViewType, webConfig: config)
        // 充值埋点
        //: if webViewType == .RechargeHalfPage {
        if webViewType == .RechargeHalfPage {
            //: RECHARGE_CLICK_EVENT = clickEvent
            k_buttonMinPath = clickEvent
            //: if clickEvent.count > 0 {
            if clickEvent.count > 0 {
                //: uploadRecord.uploadRecordEvent(eventID: clickEvent, parameterStr: ["amount": 0])
                const_pathMsg.launchStr(eventID: clickEvent, parameterStr: [String(bytes: userMotivationMsg.map{discItem(street: $0)}, encoding: .utf8)!: 0])
            }
        }
    }

    /// 跳转到充值优惠弹窗
    //: func func_pushToDiscountRechargeView() {
    func cherryRed() {
        //: guard !DismissReactiveCompatible.share.appUserConfigMode.discountUrl.isEmpty else { return }
        guard !DismissReactiveCompatible.share.appUserConfigMode.discountUrl.isEmpty else { return }
        //: let httpsUrl = DismissReactiveCompatible.share.appConfigMode.urlH5Domain + DismissReactiveCompatible.share.appUserConfigMode.discountUrl
        let httpsUrl = DismissReactiveCompatible.share.appConfigMode.urlH5Domain + DismissReactiveCompatible.share.appUserConfigMode.discountUrl
        //: let httpUrl = httpsUrl.replacingOccurrences(of: "https://", with: "http://")
        let httpUrl = httpsUrl.replacingOccurrences(of: (mainMechanicalUrl.replacingOccurrences(of: "bug", with: "t") + "s://"), with: (String(mainComputeTitle.prefix(6)) + mainRewardName.capitalized))
        //: PushThen.share.func__pushToWebVC(urlStr: httpUrl)
        PushThen.share.plumeBright(urlStr: httpUrl)
    }

    //: func func__pushToWebVC(webViewType: WebUrlType, webConfig: TalkingWebConfig) {
    func flowScene(webViewType: RelativeWeddingQuickLookable, webConfig: EndConfig) {
        //: let urlStr = TalkingWebManager.getFullUrl(urlType: webViewType)
        let urlStr = InjuryArtManager.collect(urlType: webViewType)
        //: func__pushToWebView(urlStr: urlStr, webViewType: webViewType, webConfig: webConfig)
        clubOff(urlStr: urlStr, webViewType: webViewType, webConfig: webConfig)
    }

    //: func func__pushToWebVC(urlStr: String?, webConfig: TalkingWebConfig? = nil) {
    func plumeBright(urlStr: String?, webConfig: EndConfig? = nil) {
        //: let config = (webConfig == nil) ? TalkingWebConfig():webConfig
        let config = (webConfig == nil) ? EndConfig() : webConfig
        //: func__pushToWebView(urlStr: urlStr, webViewType: nil, webConfig: config!)
        clubOff(urlStr: urlStr, webViewType: nil, webConfig: config!)
    }

    //: func func__pushToWebView(urlStr: String?, webViewType: WebUrlType?, webConfig: TalkingWebConfig) {
    func clubOff(urlStr: String?, webViewType: RelativeWeddingQuickLookable?, webConfig: EndConfig) {
        //: guard urlStr != nil && urlStr != "" else { return }
        guard urlStr != nil, urlStr != "" else { return }
        //: var config = webConfig
        var config = webConfig

        // 有值且大于0，半屏按照比例展示
        //: if config.widthHeight ?? 0 > 0 {
        if config.widthHeight ?? 0 > 0 {
            //: config.isHalfView = true
            config.isHalfView = true
        }
        // 解析url中带的参数
        //: let urlParams = JSON(urlStr!.urlParameters ?? [:])
        let urlParams = JSON(urlStr!.urlParameters ?? [:])
        //: if urlParams["transparency"].stringValue == "1" { // 透明背景
        if urlParams[(mainOverStr.replacingOccurrences(of: "half", with: "ns") + "aren" + String(userCropKey))].stringValue == "1" { // 透明背景
            //: config.clearBgColor = true
            config.clearBgColor = true
        }

        // url拼接参数
        //: let allowCharSet: CharacterSet = CharacterSet.init(charactersIn: "`%^{}\"[]|\\<>").inverted
        let allowCharSet = CharacterSet(charactersIn: "`%^{}" + "\"" + "[]|\\<>").inverted
        //: var urlStr = urlStr!.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
        var urlStr = urlStr!.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
        //: let otherParams = "version=\(AppNetVersion)&packageId=\(PackageID)&bundleId=\(AppBundle)"
        let otherParams = (const_monthMessage.replacingOccurrences(of: "compute", with: "v") + String(app_exposureKey)) + "\(userCountData)" + (String(const_voiceValue.suffix(8)) + String(k_soccerContent)) + "\(userLengthValue)" + (String(userThenUrl.suffix(5)) + "leId" + app_staffPath.replacingOccurrences(of: "remain", with: "=")) + "\(k_failMsg)"
        //: if urlStr.contains("?") {
        if urlStr.contains("?") {
            //: urlStr = "\(urlStr)&\(otherParams)"
            urlStr = "\(urlStr)&\(otherParams)"
            //: } else {
        } else {
            //: urlStr = "\(urlStr)?\(otherParams)"
            urlStr = "\(urlStr)?\(otherParams)"
        }

        //: let vc = TalkingWebViewController.init(urlString: urlStr, webViewType: webViewType, webConfig: config)
        let vc = PrimaryDelegate(urlString: urlStr, webViewType: webViewType, webConfig: config)

        // 展示网页
        //: if let currentVC = func__getCurrentActivityVC() {
        if let currentVC = doingGoing() {
            // 如果当前视图是模态上来的，跳转到下级时只能使用模态效果跳转
            //: var currentVCIsPresent = false
            var currentVCIsPresent = false
            //: if currentVC.isKind(of: TalkingWebViewController.self) &&
            if currentVC.isKind(of: PrimaryDelegate.self) &&
                //: (currentVC as! TalkingWebViewController).isModal == true {
                (currentVC as! PrimaryDelegate).isModal == true
            {
                //: currentVCIsPresent = true
                currentVCIsPresent = true
            }

            //: if config.isHalfView {
            if config.isHalfView { // 半屏使用present
                //: if config.widthHeight == nil {
                if config.widthHeight == nil {
                    //: vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight*2/3)
                    vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: main_logStr, height: main_nameData * 2 / 3)
                    //: vc.tranConfig.configCorner(corners: [.topLeft, .topRight], radius: 18)
                    vc.tranConfig.capacityEye(corners: [.topLeft, .topRight], radius: 18)
                    //: } else {
                } else {
                    //: vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenWidth/config.widthHeight!)
                    vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: main_logStr, height: main_logStr / config.widthHeight!)
                }
                //: vc.isModal = true
                vc.isModal = true
                //: currentVC.present(vc, animated: true)
                currentVC.present(vc, animated: true)

                //: } else if config.clearBgColor || currentVCIsPresent {
            } else if config.clearBgColor || currentVCIsPresent { // 透明背景只能用present
                //: vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight)
                vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: main_logStr, height: main_nameData)
                //: vc.isModal = true
                vc.isModal = true
                //: currentVC.present(vc, animated: true)
                currentVC.present(vc, animated: true)

                //: } else {
            } else {
                //: vc.isModal = false
                vc.isModal = false
                //: currentVC.navigationController?.pushViewController(vc, animated: true)
                currentVC.navigationController?.pushViewController(vc, animated: true)
            }
        }
    }
}

// MARK: - 获取当前视图、导航

//: extension PushThen {
public extension PushThen {
    //: @objc public func func__getCurrentActivityVC() -> UIViewController? {
    @objc func doingGoing() -> UIViewController? {
        //: return currentViewController()
        return exitSchedule()
    }
}

//: struct TalkingMessageJumpModel: HandyJSON {
struct ArmModelType: HandyJSON {
    //: var jumpKey: String?
    var jumpKey: String? // 评论的id
    //: var jumpUid: String?
    var jumpUid: String?
    //: var url: String?
    var url: String?
}
