
//: Declare String Begin

/*: "cmd" :*/
fileprivate let noti_nextName:[UInt8] = [0x57,0x59,0x50]

private func springRed(pad num: UInt8) -> UInt8 {
    return num ^ 52
}

/*: "cancelCall" :*/
fileprivate let data_resignName:[Character] = ["c","a"]
fileprivate let const_arrangementText:String = "ncelCallboard smart break inner well"

/*: "data" :*/
fileprivate let noti_liteWorkerContainerTitle:[UInt8] = [0x3b,0x3e,0x2b,0x3e]

private func barrierMirror(search num: UInt8) -> UInt8 {
    return num ^ 95
}

/*: "logId" :*/
fileprivate let main_mortalOppositionValue:[Character] = ["l","o","g","I","d"]

/*: "#322870" :*/
fileprivate let show_automaticName:[Character] = ["#","3","2","2","8","7"]
fileprivate let user_militaryFormat:String = "expected"

/*: "Tap to start video chatting" :*/
fileprivate let k_replaceId:[Character] = ["T","a","p"," ","t","o"," "]
fileprivate let showTrainPath:String = "stnumbr"
fileprivate let main_snapHeartQuantityryData:String = "plan search topicdeo c"

/*: "icon_close_nor" :*/
fileprivate let showGivenWordMaybeValue:String = "bold control trend translate mateicon_c"

/*: "icon_views_number" :*/
fileprivate let kYaMessage:[Character] = ["i","c","o","n","_","v","i","e","w","s","_","n","u","m","b"]
fileprivate let constLimitedRoundMessage:String = "fun"

/*: "%@ chatting" :*/
fileprivate let k_cameraFormat:String = "production domain label lock already%@ c"

/*: "icon_lounge" :*/
fileprivate let const_yellowFormat:[Character] = ["i","c"]
fileprivate let showRecordMessage:String = "on_loinstall to adjustment good"

/*: "icon_rank_coin" :*/
fileprivate let data_houseId:[Character] = ["i","c","o"]
fileprivate let mainTowardKey:String = "n_ranbroadcast structure finish mobile"

/*: "vip_btn_top" :*/
fileprivate let k_inputValue:[Character] = ["v","i","p","_"]
fileprivate let noti_jointMsg:String = "broad host smooth statbtn_top"

/*: "btn_nearby_settings_nor" :*/
fileprivate let constMonthFormat:[Character] = ["b","t","n","_","n","e"]
fileprivate let show_panelUrl:[Character] = ["a","r","b","y","_","s","e","t","t","i","n","g","s","_","n","o","r"]

/*: "%@ Coins/min" :*/
fileprivate let app_forthCharacterPath:String = "visitor clothing proposal except%@ C"
fileprivate let show_chanceEnvironmentPath:[Character] = ["i","n"]

/*: "img_match_connecting" :*/
fileprivate let main_kitHerValue:[Character] = ["i","m","g","_","m","a","t","c","h"]
fileprivate let showResentTextureMessage:[Character] = ["_","c","o","n","n","e"]
fileprivate let show_heatId:String = "marginting"

/*: "#9F80FF" :*/
fileprivate let dataCapitalLovelyUrl:[Character] = ["#","9","F","8","0"]
fileprivate let appAnyoneContent:String = "successfully"

/*: "get json error" :*/
fileprivate let dataForceValue:String = "highlight letter alongside ethical errorget "
fileprivate let userSupportImpulsePath:String = "infoinfooinfo"

/*: "matchId" :*/
fileprivate let data_competeKey:[Character] = ["m","a","t","c","h","I","d"]

/*: "errno" :*/
fileprivate let show_lateTitle:[UInt8] = [0x6f,0x6e,0x72,0x72,0x65]

/*: "VIPFee" :*/
fileprivate let app_presentName:String = "limited blindVIPFee"

/*: "Matching now" :*/
fileprivate let constDisplayValue:String = "case content precedent import soulMatch"

/*: "userTouch" :*/
fileprivate let k_literalUrl:String = "uwarmer"
fileprivate let mainPageTitle:[Character] = ["h"]

/*: "mf/freeCall/getVideoInfo" :*/
fileprivate let main_viaText:[Character] = ["m","f","/","f","r","e","e"]
fileprivate let const_corruptText:String = "part handleCall"
fileprivate let show_searchBlowPath:String = "Videobasic through"

/*: "fromType" :*/
fileprivate let appBroadClingId:[UInt8] = [0xea,0xfe,0xe3,0xe1,0xd8,0xf5,0xfc,0xe9]

private func wellRug(priority num: UInt8) -> UInt8 {
    return num ^ 140
}

/*: "You've been barred from receiving calls" :*/
fileprivate let const_packageUrl:[UInt8] = [0xd2,0xe4,0xfe,0xac,0xfd,0xee,0xab,0xe9,0xee,0xee,0xe5,0xab,0xe9,0xea,0xf9,0xf9,0xee,0xef,0xab,0xed,0xf9,0xe4,0xe6,0xab,0xf9,0xee,0xe8,0xee,0xe2,0xfd,0xe2,0xe5,0xec,0xab,0xe8,0xea,0xe7,0xe7,0xf8]

/*: "source" :*/
fileprivate let user_tipActiveTitle:[UInt8] = [0x96,0x8a,0x90,0x97,0x86,0x80]

private func beatAbout(closed num: UInt8) -> UInt8 {
    return num ^ 229
}

/*: "type" :*/
fileprivate let k_includeMessage:[UInt8] = [0x85,0x8a,0x81,0x76]

fileprivate func somebodyRelation(overwhelm num: UInt8) -> UInt8 {
    let value = Int(num) + 239
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "uid" :*/
fileprivate let app_guidanceUrl:[UInt8] = [0xa3,0xbf,0xb2]

private func circleNative(once num: UInt8) -> UInt8 {
    return num ^ 214
}

/*: "fromFreeCall" :*/
fileprivate let show_submitCoordinatorStr:String = "fromFassist deny task"
fileprivate let data_transformWeddingStr:[Character] = ["r","e","e","C","a","l","l"]

/*: "requestCall" :*/
fileprivate let userLiteralData:String = "pieques"
fileprivate let data_shoulderValue:[Character] = ["t","C","a","l","l"]

/*: "onVideoMatch" :*/
fileprivate let constPortUsedCityData:[Character] = ["o","n","V","i","d","e","o","M","a","t","c","h"]

/*: "[Automatic video] Receive video call incoming" :*/
fileprivate let const_willUrl:[UInt8] = [0x7d,0x67,0x53,0x52,0x49,0x4b,0x47,0x52,0x4f,0x45,0x6,0x50,0x4f,0x42,0x43,0x49,0x7b,0x6,0x74,0x43,0x45,0x43,0x4f,0x50,0x43,0x6,0x50,0x4f,0x42,0x43,0x49,0x6,0x45,0x47,0x4a,0x4a,0x6,0x4f,0x48,0x45,0x49,0x4b,0x4f,0x48,0x41]

private func slideBehind(nearby num: UInt8) -> UInt8 {
    return num ^ 38
}

/*: "isRandomVCPush" :*/
fileprivate let const_whiteTitle:String = "physics club itemisRa"
fileprivate let noti_sumUrl:[Character] = ["n","d","o","m","V","C","P","u","s","h"]

/*: "refuseCall" :*/
fileprivate let main_bookId:String = "refpar"
fileprivate let const_poseName:[Character] = ["l"]

/*: "onCancelCall" :*/
fileprivate let kTranslateValue:String = "speak shirtonCanc"
fileprivate let main_beforeTitle:String = "name military thatelCall"

/*: "retryAfter" :*/
fileprivate let noti_bandMessage:String = "retrhowever"
fileprivate let userHeartEdgeMessage:String = "Aftereven enter huge par"

/*: "onRequestCall" :*/
fileprivate let main_variationMessage:[Character] = ["o","n","R","e","q","u","e","s","t","C","a","l","l"]

/*: "failType" :*/
fileprivate let notiCompositionWithinKey:[Character] = ["f","a"]
fileprivate let kMinuteLayerMessage:String = "remark price slim kindilType"

/*: "callWaitingTime" :*/
fileprivate let constOperationFormat:String = "callWaby random suspend schedule"
fileprivate let main_decisionText:String = "completetcompleten"
fileprivate let kOffPremiumPath:String = "trail incomegTime"

/*: "toUid" :*/
fileprivate let k_sceneUrl:String = "steep givetoUid"

/*: "Free 1 min" :*/
fileprivate let dataDisplayPath:String = "formation gatherFree "

/*: "%@/min" :*/
fileprivate let appFadeStr:String = "orbit orbit demand%@/min"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  LeaseFourErrorDelegate.swift
//  AbroadTalking
//
//  Created by young on 2023/8/22.
//

//: import UIKit
import UIKit

//: class TalkingRandomVideoViewController: TalkingBaseViewController {
class LeaseFourErrorDelegate: WeddingLovelyRecognizerDelegate {
    //: var fee = 0
    var fee = 0 // 原价
    //: var vipFee = 0
    var vipFee = 0 // VIP价格
    //: var chattingNum = 0
    var chattingNum = 0 // 当前匹配人数
    //: private var matchId: Int = 0
    private var matchId: Int = 0 // 速配批次Id
    //: private var waitingTimer: Timer?
    private var waitingTimer: Timer? // 呼叫等待定时（等待15s）
    //: private var retryAfterTimer: Timer?
    private var retryAfterTimer: Timer? // 匹配失败重试定时器
    //: private var logId = 0
    private var logId = 0 // 通话id
    //: private var isShowFreeVideo = false
    private var isShowFreeVideo = false // 是否已展示免费视频挽留弹窗
    //: var beginTime: TimeInterval?
    var beginTime: TimeInterval?
    //: private var dispatchWorkItem: DispatchWorkItem?
    private var dispatchWorkItem: DispatchWorkItem?
    //: private var closeBtnWorkItem: DispatchWorkItem?
    private var closeBtnWorkItem: DispatchWorkItem?
    //: var isFirstRandow = true
    var isFirstRandow = true
    //: var isBeginRand = false
    var isBeginRand = false

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
            //: if !self.isFirstRandow {
            if !self.isFirstRandow {
                //: self.refreshVideoCallPrice()
                self.discourse()
            }
        }
        //: self.resetCamera()
        self.lensHood()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true

        //: setupSubviews()
        commonage()
        //: setupSubViewsConstraint()
        statusOf()
        //: bindInteraction()
        includeThat()
        //: videoManager.startPreview(isMatch: true)
        videoManager.precedent(isMatch: true)
        //: setPlayer(isRandon: false)
        norPlayerSet(isRandon: false)
        //: if isBeginRand {
        if isBeginRand {
            //: start_randomVideo()
            intervalerrogation()
        }
    }

    //: deinit {
    deinit {
        //: if waitingTimer != nil {
        if waitingTimer != nil {
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": logId]])
            SantaLuciaFirSocketDelegate.shared.amOrigin(info: [String(bytes: noti_nextName.map{springRed(pad: $0)}, encoding: .utf8)!: (String(data_resignName) + String(const_arrangementText.prefix(8))), String(bytes: noti_liteWorkerContainerTitle.map{barrierMirror(search: $0)}, encoding: .utf8)!: [(String(main_mortalOppositionValue)): logId]])
        }
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: destroyWaitingTimer()
        aboveBy()
        //: if TalkingSocketManager.shared.isInfo == false {
        if SantaLuciaFirSocketDelegate.shared.isInfo == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            SantaLuciaFirSocketDelegate.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            SantaLuciaFirSocketDelegate.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            SantaLuciaFirSocketDelegate.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            SantaLuciaFirSocketDelegate.shared.errorDelegate = nil
        }
    }

    // MARK: - Lazy Load

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: RegularArtCloudDelegate = {
        //: let m = TalkingVideoChatManager()
        let m = RegularArtCloudDelegate()
        //: let info = TalkingVideoChatModel()
        let info = IceChatModel()
        //: info.videoStage = .Calling
        info.videoStage = .Calling
        //: info.renderPostion = .selfOnBg
        info.renderPostion = .selfOnBg
        //: m.infoModel = info
        m.infoModel = info
        // 视图传递给manager
        //: m.bgVideoView = self.renderView
        m.bgVideoView = self.renderView
        //: return m
        return m
        //: }()
    }()

    // 视频画面渲染视图
    //: private lazy var renderView: UIView = {
    private lazy var renderView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .appTitleColor()
        v.backgroundColor = .dialog()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var gradientBtn: UIButton = {
    private lazy var gradientBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(start_randomVideo), for: .touchUpInside)
        btn.addTarget(self, action: #selector(intervalerrogation), for: .touchUpInside)
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        // 渐变
        //: let layer = CAGradientLayer()
        let layer = CAGradientLayer()
        //: layer.frame = CGRect(x: 0, y: 0, width: ScreenWidth+1, height: ScreenHeight+1)
        layer.frame = CGRect(x: 0, y: 0, width: main_logStr + 1, height: main_nameData + 1)
        //: layer.startPoint = CGPoint(x: 0.5, y: 0)
        layer.startPoint = CGPoint(x: 0.5, y: 0)
        //: layer.endPoint = CGPoint(x: 0.5, y: 1)
        layer.endPoint = CGPoint(x: 0.5, y: 1)
        //: layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: "#322870")!.cgColor]
        layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: (String(show_automaticName) + user_militaryFormat.replacingOccurrences(of: "expected", with: "0")))!.cgColor]
        //: layer.locations = [0, 1]
        layer.locations = [0, 1]
        //: btn.layer.addSublayer(layer)
        btn.layer.addSublayer(layer)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var svgaPlayer: SVGAPlayer = {
    private lazy var svgaPlayer: SVGAPlayer = {
        //: let player = SVGAPlayer.init()
        let player = SVGAPlayer()
        //: player.loops = 0
        player.loops = 0
        //: player.clearsAfterStop = false
        player.clearsAfterStop = false
        //: player.isUserInteractionEnabled = false
        player.isUserInteractionEnabled = false
        //: player.contentMode = .scaleAspectFill
        player.contentMode = .scaleAspectFill

        //: return player
        return player
        //: }()
    }()

    //: private lazy var tipsLab: UILabel = {
    private lazy var tipsLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.boldSystemFont(ofSize: 20)
        lab.font = UIFont.boldSystemFont(ofSize: 20)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Tap to start video chatting".localized
        lab.text = (String(k_replaceId) + showTrainPath.replacingOccurrences(of: "numb", with: "a") + "t vi" + String(main_snapHeartQuantityryData.suffix(5)) + "hatting").localized
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_close_nor"), for: .normal)
        btn.setImage(UIImage.locationName(name: (String(showGivenWordMaybeValue.suffix(6)) + "lose_nor")), for: .normal)
        //: btn.addTarget(self, action: #selector(closeButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(starAwake), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var chattingNumBtn: TalkingButton = {
    private lazy var chattingNumBtn: RangeReactiveCompatible = {
        //: let btn = TalkingButton()
        let btn = RangeReactiveCompatible()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_views_number"), for: .normal)
        btn.setImage(UIImage.locationName(name: (String(kYaMessage) + constLimitedRoundMessage.replacingOccurrences(of: "fun", with: "er"))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_views_number"), for: .disabled)
        btn.setImage(UIImage.locationName(name: (String(kYaMessage) + constLimitedRoundMessage.replacingOccurrences(of: "fun", with: "er"))), for: .disabled)
        //: btn.spaceBetweenTitleAndImage = 5
        btn.spaceBetweenTitleAndImage = 5
        //: btn.isEnabled = false
        btn.isEnabled = false
        //: btn.setTitleColor(UIColor.white, for: .normal)
        btn.setTitleColor(UIColor.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        btn.titleLabel?.font = UIFont.bigGallery(type: .Regular, fontSize: 15)
        //: btn.setTitle("%@ chatting".localizedArguments(chattingNum), for: .normal)
        btn.setTitle((String(k_cameraFormat.suffix(4)) + "hatting").unitedStates(chattingNum), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var priceStackView: UIStackView = {
    private lazy var priceStackView: UIStackView = {
        //: let v = UIStackView()
        let v = UIStackView()
        //: v.axis = .horizontal
        v.axis = .horizontal
        //: v.alignment = .center
        v.alignment = .center
        //: v.spacing = 4
        v.spacing = 4
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: return v
        return v
        //: }()
    }()

    //: private lazy var vipIcon: UIImageView = {
    private lazy var vipIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_lounge")
        img.image = UIImage.locationName(name: (String(const_yellowFormat) + String(showRecordMessage.prefix(5)) + "unge"))
        //: img.contentMode = .scaleAspectFill
        img.contentMode = .scaleAspectFill
        //: return img
        return img
        //: }()
    }()

    //: private lazy var goldCoinIcon: UIImageView = {
    private lazy var goldCoinIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_rank_coin")
        img.image = UIImage.locationName(name: (String(data_houseId) + String(mainTowardKey.prefix(5)) + "k_coin"))
        //: return img
        return img
        //: }()
    }()

    //: private lazy var priceLab: UILabel = {
    private lazy var priceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        lab.font = UIFont.bigGallery(type: .Regular, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var vipTopImgView: UIImageView = {
    private lazy var vipTopImgView: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.isHidden = true
        imgV.isHidden = true
        //: imgV.image  = UIImage.BundleImageNamed(name: "vip_btn_top")
        imgV.image = UIImage.locationName(name: (String(k_inputValue) + String(noti_jointMsg.suffix(7))))
        //: return imgV
        return imgV
        //: }()
    }()

    // 会员折扣价
    //: private lazy var vipPriceBtn: TalkingButton = {
    private lazy var vipPriceBtn: RangeReactiveCompatible = {
        //: let btn = TalkingButton()
        let btn = RangeReactiveCompatible()
        //: btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        //: btn.layer.cornerRadius = actualWidth(w: 27)
        btn.layer.cornerRadius = actualWidth(w: 27)
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(maleVipPriceButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(vidOnDirect), for: .touchUpInside)
        //: btn.isHidden = true
        btn.isHidden = true
        //: btn.setBackgroundImage(UIImage.BundleImageNamed(name: "btn_nearby_settings_nor"), for: .normal)
        btn.setBackgroundImage(UIImage.locationName(name: (String(constMonthFormat) + String(show_panelUrl))), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var vipPriceImg: UIImageView = {
    private lazy var vipPriceImg: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.image  = UIImage.BundleImageNamed(name: "icon_rank_coin")
        imgV.image = UIImage.locationName(name: (String(data_houseId) + String(mainTowardKey.prefix(5)) + "k_coin"))
        //: return imgV
        return imgV
        //: }()
    }()

    //: private lazy var vipPriceLab: UILabel = {
    private lazy var vipPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .userVipColor()
        lab.textColor = .thanMortal()
        //: lab.text = "%@ Coins/min".localizedArguments(vipFee)
        lab.text = (String(app_forthCharacterPath.suffix(4)) + "oins/m" + String(show_chanceEnvironmentPath)).unitedStates(vipFee)
        //: lab.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        lab.font = UIFont.bigGallery(type: .Medium, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var norPriceLab: UILabel = {
    private lazy var norPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: return lab
        return lab
        //: }()
    }()

    // 免费视频挽留弹窗
    //: private lazy var freeVideoAlert: TalkingLiveAlertView = {
    private lazy var freeVideoAlert: ProviderAlertView = //: return ProviderAlertView(frame: .zero, type: .freeVideo)
        .init(frame: .zero, type: .freeVideo)
    //: }()

    //: private lazy var connectingImg: UIImageView = {
    private lazy var connectingImg: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.image  = UIImage.BundleImageNamed(name: "img_match_connecting")
        imgV.image = UIImage.locationName(name: (String(main_kitHerValue) + String(showResentTextureMessage) + show_heatId.replacingOccurrences(of: "margin", with: "c")))
        //: let view = UIActivityIndicatorView.init()
        let view = UIActivityIndicatorView()
        //: view.color = UIColor(hex: "#9F80FF")
        view.color = UIColor(hex: (String(dataCapitalLovelyUrl) + appAnyoneContent.replacingOccurrences(of: "successfully", with: "FF")))
        //: imgV.addSubview(view)
        imgV.addSubview(view)
        //: view.snp.makeConstraints { make in
        view.snp.makeConstraints { make in
            //: make.bottom.equalToSuperview().offset(-12)
            make.bottom.equalToSuperview().offset(-12)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(20)
            make.size.equalTo(20)
        }
        //: view.startAnimating()
        view.startAnimating()
        //: imgV.isHidden = true
        imgV.isHidden = true
        //: return imgV
        return imgV
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingRandomVideoViewController {
extension LeaseFourErrorDelegate {
    //: private func setPlayer(isRandon: Bool) {
    private func norPlayerSet(isRandon: Bool) {
        //: let url = SVGAEffectTool.default.getZipEffectPath(type: isRandon ? .Random_video_bg : .Random_video_bg_nor )
        let url = BlackEffectTool.default.recordingRevenue(type: isRandon ? .Random_video_bg : .Random_video_bg_nor)
        //: do {
        do {
            //: let data = try Data(contentsOf: url)
            let data = try Data(contentsOf: url)
            //: let parser = SVGAParser.init()
            let parser = SVGAParser()
            //: parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
            parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: svgaPlayer.videoItem = videoItem
                svgaPlayer.videoItem = videoItem
                //: svgaPlayer.startAnimation()
                svgaPlayer.startAnimation()
            }
            //: } catch {
        } catch {
            //: printLog(message: "get json error")
            printLog(message: (String(dataForceValue.suffix(4)) + "json e" + userSupportImpulsePath.replacingOccurrences(of: "info", with: "r")))
        }
    }

    /// 开始随机视频（男）
    /// - Parameters:
    ///   - cmd: 事件名
    ///   - errno: cmd的错误码
    //: private func req_randomVideoToMatch(cmd: String, errno: String) {
    private func queryOutScript(cmd: String, errno: String) {
        //: beginTime = Date().timeIntervalSince1970
        beginTime = Date().timeIntervalSince1970
        //: let params = ["matchId": self.matchId,
        let params = [(String(data_competeKey)): self.matchId,
                      //: "cmd": cmd,
                      String(bytes: noti_nextName.map{springRed(pad: $0)}, encoding: .utf8)!: cmd,
                      //: "errno": errno] as [String: Any]
                      String(bytes: show_lateTitle.reversed(), encoding: .utf8)!: errno] as [String: Any]
        //: TalkingQuickVideoManager.req_randomVideoToMatch(params: params) { succeed, result, errorModel in
        OfVideoManager.flushCompletion(params: params) { succeed, result, _ in
            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.readArray()
                //: return
                return
            }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(data_competeKey))].intValue
        }
    }

    /// 更新VIP价格
    //: @objc private func req_updateVIPFee() {
    @objc private func failFee() {
        //: TalkingQuickVideoManager.req_videoMatchCheck() { succeed, result, errorModel in
        OfVideoManager.aAdvanced { succeed, result, _ in
            //: guard succeed else { return }
            guard succeed else { return }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.vipFee = json["VIPFee"].intValue
            self.vipFee = json[(String(app_presentName.suffix(6)))].intValue
            //: self.refreshVideoCallPrice()
            self.discourse()
        }
    }
}

// MARK: - Event

//: extension TalkingRandomVideoViewController {
extension LeaseFourErrorDelegate {
    /// 男性侧vip价格按钮点击事件
    //: @objc private func maleVipPriceButtonClick() {
    @objc private func vidOnDirect() {
        //: PushThen.share.func__pushToSubscribeAlert()
        PushThen.share.enablelyKey()
    }

    /// 关闭按钮点击事件
    //: @objc func closeButtonClick() {
    @objc func starAwake() {
        //: dispatchWorkItem?.cancel()
        dispatchWorkItem?.cancel()
        //: showCloseBtn()
        oralStage()
        //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCancelNoP)
        const_pathMsg.tabKeep(eventID: kSumKey)

        //: if DismissReactiveCompatible.share.appConfigMode.displayMatchV4PopUp, DismissReactiveCompatible.share.loginUserMode.freeCallTimes > 0,
        if DismissReactiveCompatible.share.appConfigMode.displayMatchV4PopUp, DismissReactiveCompatible.share.loginUserMode.freeCallTimes > 0,
           //: isShowFreeVideo == false {
           isShowFreeVideo == false
        {
            //: self.freeVideoAlert.show()
            self.freeVideoAlert.stripPhone()
            //: isShowFreeVideo = true
            isShowFreeVideo = true
            //: return
            return
        }
        //: currentViewController()?.navigationController?.popViewController(animated: true)
        exitSchedule()?.navigationController?.popViewController(animated: true)
    }

    /// 开始随机视频
    //: @objc private func start_randomVideo() {
    @objc private func intervalerrogation() {
        //: guard TalkingSocketManager.shared.isInfo == false else {
        guard SantaLuciaFirSocketDelegate.shared.isInfo == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.enablece(showMsg: kLikeStr)
            //: return
            return
        }
        //: guard gradientBtn.isEnabled == true else { return }
        guard gradientBtn.isEnabled == true else { return }
        //: afterShowClose()
        rowPrepare()
        //: self.matchId = 0
        self.matchId = 0 // 重置匹配池
        //: gradientBtn.isEnabled = false
        gradientBtn.isEnabled = false
        //: tipsLab.text = "Matching now".localized
        tipsLab.text = (String(constDisplayValue.suffix(5)) + "ing now").localized
        //: setPlayer(isRandon: true)
        norPlayerSet(isRandon: true)
        //: priceStackView.isHidden = true
        priceStackView.isHidden = true
        //: vipPriceBtn.isHidden = true
        vipPriceBtn.isHidden = true
        //: vipTopImgView.isHidden = true
        vipTopImgView.isHidden = true
        //: if DismissReactiveCompatible.share.loginUserMode.randomVideoType == 2 && DismissReactiveCompatible.share.loginUserMode.robotVideoNum > 0 {
        if DismissReactiveCompatible.share.loginUserMode.randomVideoType == 2, DismissReactiveCompatible.share.loginUserMode.robotVideoNum > 0 {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRobotRandomButton)
            const_pathMsg.tabKeep(eventID: app_clickName)
            //: req_getVideoInfo()
            haveLabInfo()
            //: } else {
        } else {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCallNoP)
            const_pathMsg.tabKeep(eventID: dataInfoUrl)

            //: TalkingSocketManager.shared.videoMatchDelegate = self
            SantaLuciaFirSocketDelegate.shared.videoMatchDelegate = self
            //: TalkingSocketManager.shared.callerDelegate = self
            SantaLuciaFirSocketDelegate.shared.callerDelegate = self
            //: TalkingSocketManager.shared.calledPartyDelegate = self
            SantaLuciaFirSocketDelegate.shared.calledPartyDelegate = self
            //: TalkingSocketManager.shared.errorDelegate = self
            SantaLuciaFirSocketDelegate.shared.errorDelegate = self
            //: restart_randomVideo()
            restartLikeErrno()
        }
    }

    /// 重新开始视频速配
    /// - Parameters:
    ///   - cmd: 事件名（默认用户主动点击）
    ///   - errno: cmd的错误码
    //: private func restart_randomVideo(cmd: String = "userTouch", errno: String = "0") {
    private func restartLikeErrno(cmd: String = (k_literalUrl.replacingOccurrences(of: "warm", with: "s") + "Touc" + String(mainPageTitle)), errno: String = "0") {
        // 停止定时器
        //: destroyWaitingTimer()
        aboveBy()
        //: req_randomVideoToMatch(cmd: cmd, errno: errno)
        queryOutScript(cmd: cmd, errno: errno)
    }

    /// 停止随机视频
    //: private func stop_randomVideo() {
    private func readArray() {
        // 停止定时器
        //: destroyWaitingTimer()
        aboveBy()
        //: showCloseBtn()
        oralStage()

        //: gradientBtn.isEnabled = true
        gradientBtn.isEnabled = true
        //: tipsLab.text = "Tap to start video chatting".localized
        tipsLab.text = (String(k_replaceId) + showTrainPath.replacingOccurrences(of: "numb", with: "a") + "t vi" + String(main_snapHeartQuantityryData.suffix(5)) + "hatting").localized
        //: setPlayer(isRandon: false)
        norPlayerSet(isRandon: false)
        //: self.isFirstRandow = false
        self.isFirstRandow = false
        //: if TalkingSocketManager.shared.isInfo == false {
        if SantaLuciaFirSocketDelegate.shared.isInfo == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            SantaLuciaFirSocketDelegate.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            SantaLuciaFirSocketDelegate.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            SantaLuciaFirSocketDelegate.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            SantaLuciaFirSocketDelegate.shared.errorDelegate = nil
        }
        //: refreshVideoCallPrice()
        discourse()
    }

    /// 销毁定时器
    //: private func destroyWaitingTimer() {
    private func aboveBy() {
        //: if self.waitingTimer != nil {
        if self.waitingTimer != nil {
            //: self.waitingTimer?.invalidate()
            self.waitingTimer?.invalidate()
            //: self.waitingTimer = nil
            self.waitingTimer = nil
        }

        //: if self.retryAfterTimer != nil {
        if self.retryAfterTimer != nil {
            //: self.retryAfterTimer?.invalidate()
            self.retryAfterTimer?.invalidate()
            //: self.retryAfterTimer = nil
            self.retryAfterTimer = nil
        }
    }

    /// 获取用户主动拨打视频信息
    //: @objc private func req_getVideoInfo() {
    @objc private func haveLabInfo() {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "mf/freeCall/getVideoInfo"
        reqModel.requestPath = (String(main_viaText) + String(const_corruptText.suffix(4)) + "/get" + String(show_searchBlowPath.prefix(5)) + "Info")
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["fromType": "1"]
        reqModel.params = [String(bytes: appBroadClingId.map{wellRug(priority: $0)}, encoding: .utf8)!: "1"]
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in

            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.readArray()
                //: if errorModel?.errorCode ?? 1 > 0 {
                if errorModel?.errorCode ?? 1 > 0 {
                    //: DismissReactiveCompatible.share.loginUserMode.randomVideoType = 1
                    DismissReactiveCompatible.share.loginUserMode.randomVideoType = 1
                    //: self.start_randomVideo()
                    self.intervalerrogation()
                    //: return
                    return
                }
                //: self.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                self.enablece(showMsg: errorModel?.errorMsg ?? "")
                //: return
                return
            }
            //: guard let result = result as? Dictionary<String, Any> else {
            guard let result = result as? [String: Any] else {
                //: return
                return
            }
            //: self.connectingImg.isHidden = false
            self.connectingImg.isHidden = false
            //: if let model = TalkingVideoInitivCallModel.deserialize(from: result as? Dictionary) {
            if let model = RoughnessModel.deserialize(from: result as? Dictionary) {
                /// 预加载
                //: let  player = TalkingVideoPlayerManager.init()
                let player = PackagingPlayListener()
                //: player.setMute(bEnable: true)
                player.scheduleColumn(bEnable: true)
                //: player.playerWithUrlAndVideoView(url: model.callVideoUrl, view: UIView.init())
                player.loseTrack(url: model.callVideoUrl, view: UIView())

                //: TalkingVideoInitivCallManager.shared.videoCallModel = model
                GenerateVideoManager.shared.videoCallModel = model

                //: TalkingPermissionTool.checkCameraAndMicrophone { [weak self] isOpen in
                SpecialPermissionTool.remainMicrophone { [weak self] isOpen in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: guard isOpen == true else { return }
                    guard isOpen == true else { return }

                    //: self.putThrough()
                    self.ladleChannel()
                }
            }
        }
    }

    /// 接通电话
    //: private func putThrough() {
    private func ladleChannel() {
        //: uploadRecord.uploadRecordEvent(eventID: clickVideoAcceptButton)
        const_pathMsg.tabKeep(eventID: kColorId)
        /// 视频通话被禁用
        //: guard TalkingVideoInitivCallManager.shared.videoCallModel.isBan == 0 else {
        guard GenerateVideoManager.shared.videoCallModel.isBan == 0 else {
            //: self.func__showStatusBarErrorMsg(showMsg: "You've been barred from receiving calls".localized)
            self.enablece(showMsg: String(bytes: const_packageUrl.map{$0^139}, encoding: .utf8)!.localized)
            //: self.connectingImg.isHidden = true
            self.connectingImg.isHidden = true
            //: return
            return
        }
        //: self.dispatchWorkItem = DispatchWorkItem {
        self.dispatchWorkItem = DispatchWorkItem {
            //: self.connectingImg.isHidden = true
            self.connectingImg.isHidden = true
            //: TalkingVideoInitivCallManager.shared.req_userOperation(type: 1) { succeed, result, errorModel in
            GenerateVideoManager.shared.requireCompletion(type: 1) { succeed, _, _ in
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: guard succeed == true else {
                guard succeed == true else {
                    //: self.stop_randomVideo()
                    self.readArray()
                    //: return
                    return
                }
                //: let vc = TalkingVideoInitivCallVC()
                let vc = NameureRatingViewController()
                //: vc.isRandomVCPush = true
                vc.isRandomVCPush = true
                //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
                self.exitSchedule()?.navigationController?.pushViewController(vc, animated: true)
                //: self.stop_randomVideo()
                self.readArray()
            }
        }
        // 添加延时任务
        //: DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
        DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
    }

    /// 匹配时x秒显示关闭按钮
    //: private func afterShowClose() {
    private func rowPrepare() {
        //: if DismissReactiveCompatible.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
        if DismissReactiveCompatible.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
            //: self.closeBtn.isHidden = true
            self.closeBtn.isHidden = true
            //: closeBtnWorkItem = DispatchWorkItem { [weak self] in
            closeBtnWorkItem = DispatchWorkItem { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.closeBtn.isHidden = false
                self.closeBtn.isHidden = false
            }
            //: DispatchQueue.main.asyncAfter(deadline: .now() + Double(DismissReactiveCompatible.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
            DispatchQueue.main.asyncAfter(deadline: .now() + Double(DismissReactiveCompatible.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
        }
    }

    //: private func showCloseBtn() {
    private func oralStage() {
        //: closeBtnWorkItem?.cancel()
        closeBtnWorkItem?.cancel()
        //: self.closeBtn.isHidden = false
        self.closeBtn.isHidden = false
    }

    //: private func resetCamera() {
    private func lensHood() {
        //: guard TalkingSocketManager.shared.isInfo == false else {
        guard SantaLuciaFirSocketDelegate.shared.isInfo == false else {
            //: return
            return
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
            //: self.videoManager.infoModel?.videoStage = .Waiting
            self.videoManager.infoModel?.videoStage = .Waiting
            //: self.videoManager.config.openCamera = true
            self.videoManager.config.openCamera = true
            //: self.videoManager.isOpenTRTCPreview = false
            self.videoManager.isOpenTRTCPreview = false
            //: self.videoManager.startPreview()
            self.videoManager.precedent()
        }
    }

    /// 匹配通话结束时
    //: @objc private func closeTool() {
    @objc private func controlVersion() {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2) {
            //: self.start_randomVideo()
            self.intervalerrogation()
        }
    }
}

// MARK: - ManagingDirectorObjectProtocol

//: extension TalkingRandomVideoViewController: SocketManagerVideoMatchDelegate {
extension LeaseFourErrorDelegate: ManagingDirectorObjectProtocol {
    /// 视频速配成功
    //: func socket_match_onVideoMatch(data: [String: Any]) {
    func attributeSuspend(data: [String: Any]) {
        // 拨打视频通话
        //: guard let model = TalkingMatchResultModel.deserialize(from: data) else { return }
        guard let model = ConcentrationScientificTheoryMeasurable.deserialize(from: data) else { return }
        //: self.matchId = model.matchId
        self.matchId = model.matchId
        //: var callData: [String: Any] = ["matchId": model.matchId,
        var callData: [String: Any] = [(String(data_competeKey)): model.matchId,
                                       //: "source": model.source,
                                       String(bytes: user_tipActiveTitle.map{beatAbout(closed: $0)}, encoding: .utf8)!: model.source,
                                       //: "type": model.type,
                                       String(bytes: k_includeMessage.map{somebodyRelation(overwhelm: $0)}, encoding: .utf8)!: model.type,
                                       //: "uid": model.uid]
                                       String(bytes: app_guidanceUrl.map{circleNative(once: $0)}, encoding: .utf8)!: model.uid]
        //: let index = EnableFreeCallType.randow
        let index = PrimaryBasicType.randow
        //: if DismissReactiveCompatible.share.loginUserMode.freeCallTimes > 0 && DismissReactiveCompatible.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
        if DismissReactiveCompatible.share.loginUserMode.freeCallTimes > 0 && DismissReactiveCompatible.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
            //: callData.updateValue(1, forKey: "fromFreeCall")
            callData.updateValue(1, forKey: (String(show_submitCoordinatorStr.prefix(5)) + String(data_transformWeddingStr)))
            //: TalkingSocketManager.shared.isFreeCall = true
            SantaLuciaFirSocketDelegate.shared.isFreeCall = true
        }
        //: let info: [String: Any] = ["cmd": "requestCall", "data": callData]
        let info: [String: Any] = [String(bytes: noti_nextName.map{springRed(pad: $0)}, encoding: .utf8)!: (userLiteralData.replacingOccurrences(of: "pi", with: "r") + String(data_shoulderValue)), String(bytes: noti_liteWorkerContainerTitle.map{barrierMirror(search: $0)}, encoding: .utf8)!: callData]
        //: TalkingSocketManager.shared.sendMessage(info: info)
        SantaLuciaFirSocketDelegate.shared.amOrigin(info: info)
        // 15s未接听主动挂断
        //: waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] timer in
        waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.logId]])
            SantaLuciaFirSocketDelegate.shared.amOrigin(info: [String(bytes: noti_nextName.map{springRed(pad: $0)}, encoding: .utf8)!: (String(data_resignName) + String(const_arrangementText.prefix(8))), String(bytes: noti_liteWorkerContainerTitle.map{barrierMirror(search: $0)}, encoding: .utf8)!: [(String(main_mortalOppositionValue)): self.logId]])
            //: self.restart_randomVideo(cmd: "onVideoMatch")
            self.restartLikeErrno(cmd: (String(constPortUsedCityData)))
            //: }, repeats: false)
        }, repeats: false)
        //: connectingImg.isHidden = false
        connectingImg.isHidden = false
    }

    //: func socket_match_onRequestCall(data: [String: Any]) {
    func aggregation(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.logId = json["logId"].intValue
        self.logId = json[(String(main_mortalOppositionValue))].intValue
    }
}

// MARK: - AssetsObjectProtocol【视频拨打方处理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCallerDelegate {
extension LeaseFourErrorDelegate: AssetsObjectProtocol {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {}
    func milage(data _: [String: Any]) {}

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {}
    func earRate(data _: [String: Any]) {}

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {}
    func maleData(msg _: String, data _: [String: Any]) {}

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func layer(data: [String: Any]) {
        // 应用在后台，先提醒用户
        //: if UIApplication.shared.applicationState == .background {
        if UIApplication.shared.applicationState == .background {
            //: AppDelegateHelper.pushLocalNotification(body: "[Automatic video] Receive video call incoming".localized)
            PortraitReactiveCompatible.join(body: String(bytes: const_willUrl.map{slideBehind(nearby: $0)}, encoding: .utf8)!.localized)
        }
        //: let model = TalkingMatchResultModel.deserialize(from: data)
        let model = ConcentrationScientificTheoryMeasurable.deserialize(from: data)
        //: if let uid = model?.uid {
        if let uid = model?.uid {
            //: uploadSuccessTime(toUid: String(uid))
            liveUid(toUid: String(uid))
        }
        //: connectingImg.isHidden = true
        connectingImg.isHidden = true
        //: stop_randomVideo()
        readArray()
        // 开启视频通话时先销毁当前预览画面
        //: videoManager.shouldReleaseAllResource()
        videoManager.isConversion()
        //: var newData = data
        var newData = data
        //: newData["isRandomVCPush"] = true
        newData[(String(const_whiteTitle.suffix(4)) + String(noti_sumUrl))] = true
        //: DismissReactiveCompatible.share.start1v1TalkCall(info: newData, situation: .Calling)
        DismissReactiveCompatible.share.syllabus(info: newData, situation: .Calling)
    }
}

// MARK: - MedalSolarObjectProtocol【接收到回执代理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCalledPartyDelegate {
extension LeaseFourErrorDelegate: MedalSolarObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func sumRelationData(data _: [String: Any]) {
        //: restart_randomVideo(cmd: "refuseCall")
        restartLikeErrno(cmd: (main_bookId.replacingOccurrences(of: "par", with: "us") + "eCal" + String(const_poseName)))
        //: connectingImg.isHidden = true
        connectingImg.isHidden = true
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {}
    func information(data _: [String: Any]) {}
}

// MARK: - EnableceErrorDelegate

//: extension TalkingRandomVideoViewController: TalkingSocketManagerErrorDelegate {
extension LeaseFourErrorDelegate: EnableceErrorDelegate {
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func assemblage(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
        //: connectingImg.isHidden = true
        connectingImg.isHidden = true
        //: if cmd == "onCancelCall" {  // 取消通话异常
        if cmd == (String(kTranslateValue.suffix(6)) + String(main_beforeTitle.suffix(6))) { // 取消通话异常
            //: stop_randomVideo()
            readArray()
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 404 { // 匹配池用完
        if cmd == (String(constPortUsedCityData)), errorNo == 404 { // 匹配池用完
            //: stop_randomVideo()
            readArray()
            //: let json = JSON(data)
            let json = JSON(data)
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(data_competeKey))].intValue
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.enablece(showMsg: msg)
            //: uploadFailTime(type: 2)
            former(type: 2)
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 449 { // 匹配池用完，延时重试
        if cmd == (String(constPortUsedCityData)), errorNo == 449 { // 匹配池用完，延时重试
            //: let json = JSON(data)
            let json = JSON(data)
            //: retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json["retryAfter"].intValue), block: { [weak self] timer in
            retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json[(noti_bandMessage.replacingOccurrences(of: "however", with: "y") + String(userHeartEdgeMessage.prefix(5)))].intValue), block: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.matchId = json["matchId"].intValue
                self.matchId = json[(String(data_competeKey))].intValue
                //: self.restart_randomVideo(cmd: cmd, errno: String(errorNo))
                self.restartLikeErrno(cmd: cmd, errno: String(errorNo))
                //: }, repeats: false)
            }, repeats: false)
            //: return
            return
        }
        //: if cmd == "onRequestCall", errorNo == VideoChatErrorCode.MoneyLack.rawValue { // 余额不足
        if cmd == (String(main_variationMessage)), errorNo == BuildTitleConvertible.MoneyLack.rawValue { // 余额不足
            //: stop_randomVideo()
            readArray()
            //: guard DismissReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard DismissReactiveCompatible.share.appStatus == AfterArtSumLiteral.normal.rawValue else { return }
            //: PushThen.share.func__jumpToWebRecharge(clickEvent: clickRandomvideoButton, sufficient: false)
            PushThen.share.twoParams(clickEvent: showFrameFormat, sufficient: false)
            //: uploadFailTime(type: 1)
            former(type: 1)
            //: return
            return
        }

        //: restart_randomVideo(cmd: cmd, errno: String(errorNo))
        restartLikeErrno(cmd: cmd, errno: String(errorNo))
    }
}

// MARK: - 埋点

//: extension TalkingRandomVideoViewController {
extension LeaseFourErrorDelegate {
    //: func uploadFailTime(type: Int) {
    func former(type: Int) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["failType"] = type
        parm[(String(notiCompositionWithinKey) + String(kMinuteLayerMessage.suffix(6)))] = type
        //: parm["callWaitingTime"] = time
        parm[(String(constOperationFormat.prefix(6)) + main_decisionText.replacingOccurrences(of: "complete", with: "i") + String(kOffPremiumPath.suffix(5)))] = time
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideoFailwaitTime, parameterStr: parm)
        const_pathMsg.launchStr(eventID: notiWarnTitle, parameterStr: parm)
    }

    //: func uploadSuccessTime(toUid: String) {
    func liveUid(toUid: String) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["callWaitingTime"] = time
        parm[(String(constOperationFormat.prefix(6)) + main_decisionText.replacingOccurrences(of: "complete", with: "i") + String(kOffPremiumPath.suffix(5)))] = time
        //: parm["toUid"] = toUid
        parm[(String(k_sceneUrl.suffix(5)))] = toUid
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideowaitTime, parameterStr: parm)
        const_pathMsg.launchStr(eventID: appNetFormat, parameterStr: parm)
    }
}

// MARK: - Layout

//: extension TalkingRandomVideoViewController {
extension LeaseFourErrorDelegate {
    /// 刷新视频通话价格
    //: @objc private func refreshVideoCallPrice() {
    @objc private func discourse() {
        //: guard DismissReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else {
        guard DismissReactiveCompatible.share.appStatus == AfterArtSumLiteral.normal.rawValue else {
            //: priceStackView.isHidden = true
            priceStackView.isHidden = true
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true
            //: return
            return
        }

        //: vipIcon.isHidden = !DismissReactiveCompatible.share.loginUserMode.loungePlus
        vipIcon.isHidden = !DismissReactiveCompatible.share.loginUserMode.loungePlus
        //: priceStackView.isHidden = false
        priceStackView.isHidden = false
        //: let indexType = EnableFreeCallType.randow
        let indexType = PrimaryBasicType.randow
        //: if DismissReactiveCompatible.share.loginUserMode.freeCallTimes > 0 && DismissReactiveCompatible.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue {
        if DismissReactiveCompatible.share.loginUserMode.freeCallTimes > 0, DismissReactiveCompatible.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue { // 有免费次数
            //: if DismissReactiveCompatible.share.appUserConfigMode.callBtnStyle == 1 {
            if DismissReactiveCompatible.share.appUserConfigMode.callBtnStyle == 1 {
                //: priceLab.text = "Free 1 min".localized
                priceLab.text = (String(dataDisplayPath.suffix(5)) + "1 min").localized
                //: } else {
            } else {
                //: priceStackView.isHidden = true
                priceStackView.isHidden = true
            }
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true

            //: } else {
        } else { // 无免费次数, 展示价格
            //: let price = DismissReactiveCompatible.share.loginUserMode.loungePlus ? vipFee:fee
            let price = DismissReactiveCompatible.share.loginUserMode.loungePlus ? vipFee : fee
            //: priceLab.text = "%@/min".localizedArguments(price)
            priceLab.text = (String(appFadeStr.suffix(6))).unitedStates(price)

            //: let attr = NSMutableAttributedString(string: "%@/min".localizedArguments(price), attributes: [.strikethroughStyle : NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.pingfangMediumFont(fontSize: 10)])
            let attr = NSMutableAttributedString(string: (String(appFadeStr.suffix(6))).unitedStates(price), attributes: [.strikethroughStyle: NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.pingfang(fontSize: 10)])
            //: norPriceLab.attributedText = attr
            norPriceLab.attributedText = attr
            //: vipPriceBtn.isHidden = DismissReactiveCompatible.share.loginUserMode.loungePlus
            vipPriceBtn.isHidden = DismissReactiveCompatible.share.loginUserMode.loungePlus
            //: vipTopImgView.isHidden = DismissReactiveCompatible.share.loginUserMode.loungePlus
            vipTopImgView.isHidden = DismissReactiveCompatible.share.loginUserMode.loungePlus

            //: priceStackView.isHidden = !DismissReactiveCompatible.share.loginUserMode.loungePlus
            priceStackView.isHidden = !DismissReactiveCompatible.share.loginUserMode.loungePlus
        }
    }

    /// 添加视图
    //: private func setupSubviews() {
    private func commonage() {
        //: view.addSubview(renderView)
        view.addSubview(renderView)
        //: view.addSubview(gradientBtn)
        view.addSubview(gradientBtn)
        //: view.addSubview(svgaPlayer)
        view.addSubview(svgaPlayer)
        //: view.addSubview(tipsLab)
        view.addSubview(tipsLab)
        //: view.addSubview(chattingNumBtn)
        view.addSubview(chattingNumBtn)
        //: view.addSubview(priceStackView)
        view.addSubview(priceStackView)
        //: priceStackView.addArrangedSubview(vipIcon)
        priceStackView.addArrangedSubview(vipIcon)
        //: priceStackView.addArrangedSubview(goldCoinIcon)
        priceStackView.addArrangedSubview(goldCoinIcon)
        //: priceStackView.addArrangedSubview(priceLab)
        priceStackView.addArrangedSubview(priceLab)
        //: view.addSubview(vipPriceBtn)
        view.addSubview(vipPriceBtn)
        //: view.addSubview(vipTopImgView)
        view.addSubview(vipTopImgView)
        //: vipPriceBtn.addSubview(vipPriceImg)
        vipPriceBtn.addSubview(vipPriceImg)
        //: vipPriceBtn.addSubview(vipPriceLab)
        vipPriceBtn.addSubview(vipPriceLab)
        //: vipPriceBtn.addSubview(norPriceLab)
        vipPriceBtn.addSubview(norPriceLab)

        //: view.addSubview(closeBtn)
        view.addSubview(closeBtn)
        //: view.addSubview(connectingImg)
        view.addSubview(connectingImg)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func statusOf() {
        //: renderView.snp.makeConstraints { make in
        renderView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: gradientBtn.snp.makeConstraints { make in
        gradientBtn.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: svgaPlayer.snp.makeConstraints { make in
        svgaPlayer.snp.makeConstraints { make in
            //: make.leading.equalTo(-8)
            make.leading.equalTo(-8)
            //: make.trailing.equalTo(8)
            make.trailing.equalTo(8)
            //: make.centerY.equalToSuperview()
            make.centerY.equalToSuperview()
            //: make.height.equalTo(actualWidth(w: 395))
            make.height.equalTo(actualWidth(w: 395))
        }
        //: tipsLab.snp.makeConstraints { make in
        tipsLab.snp.makeConstraints { make in
            //: make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            //: make.width.centerX.equalToSuperview()
            make.width.centerX.equalToSuperview()
        }
        //: chattingNumBtn.snp.makeConstraints { make in
        chattingNumBtn.snp.makeConstraints { make in
            //: make.top.equalTo(tipsLab.snp.bottom).offset(35)
            make.top.equalTo(tipsLab.snp.bottom).offset(35)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: priceStackView.snp.makeConstraints { make in
        priceStackView.snp.makeConstraints { make in
            //: make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.height.equalTo(31)
            make.height.equalTo(31)
            //: make.trailing.lessThanOrEqualToSuperview()
            make.trailing.lessThanOrEqualToSuperview()
        }
        //: vipIcon.snp.makeConstraints { make in
        vipIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
        }
        //: goldCoinIcon.snp.makeConstraints { make in
        goldCoinIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(actualWidth(w: 15))
            make.width.height.equalTo(actualWidth(w: 15))
        }
        //: priceLab.snp.makeConstraints { make in
        priceLab.snp.makeConstraints { make in
            //: make.height.equalToSuperview()
            make.height.equalToSuperview()
        }

        //: vipPriceBtn.snp.makeConstraints { make in
        vipPriceBtn.snp.makeConstraints { make in
            //: make.top.equalTo(priceStackView.snp.bottom).offset(16)
            make.top.equalTo(priceStackView.snp.bottom).offset(16)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
            make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
        }
        //: vipTopImgView.snp.makeConstraints { make in
        vipTopImgView.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            //: make.leading.equalTo(vipPriceBtn.snp.leading)
            make.leading.equalTo(vipPriceBtn.snp.leading)
            //: make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
            make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
        }

        //: vipPriceImg.snp.makeConstraints { make in
        vipPriceImg.snp.makeConstraints { make in
            //: make.top.equalToSuperview().offset(actualWidth(w: 12))
            make.top.equalToSuperview().offset(actualWidth(w: 12))
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
            //: make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
            make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
        }

        //: vipPriceLab.snp.makeConstraints { make in
        vipPriceLab.snp.makeConstraints { make in
            //: make.centerY.equalTo(vipPriceImg)
            make.centerY.equalTo(vipPriceImg)
            //: make.centerX.equalToSuperview().offset(8)
            make.centerX.equalToSuperview().offset(8)
        }
        //: norPriceLab.snp.makeConstraints { make in
        norPriceLab.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: closeBtn.snp.makeConstraints { make in
        closeBtn.snp.makeConstraints { make in
            //: make.top.equalTo(StatusBarHeight+6)
            make.top.equalTo(dataPointTitle + 6)
            //: make.trailing.equalTo(-10)
            make.trailing.equalTo(-10)
            //: make.width.height.equalTo(actualWidth(w: 44))
            make.width.height.equalTo(actualWidth(w: 44))
        }

        //: connectingImg.snp.makeConstraints { make in
        connectingImg.snp.makeConstraints { make in
            //: make.center.equalToSuperview()
            make.center.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 262), height: actualWidth(w: 102)))
            make.size.equalTo(CGSize(width: actualWidth(w: 262), height: actualWidth(w: 102)))
        }
    }

    /// 绑定事件
    //: private func bindInteraction() {
    private func includeThat() {
        // index/getconfig接口回调刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(refreshVideoCallPrice),
                                               selector: #selector(discourse),
                                               //: name: UPDATE_INDEX_GETCONFIG_NOTIFICATION,
                                               name: mainReplaceTitle,
                                               //: object: nil)
                                               object: nil)
        // 个人信息更新成功刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(req_updateVIPFee),
                                               selector: #selector(failFee),
                                               //: name: USER_GETMYINFO_SUCCEED_NOTIFICATION,
                                               name: dataConversationMsg,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(closeTool), name: MATCHVIDEOCALL_END_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(controlVersion), name: userWorkValue, object: nil)
    }
}
