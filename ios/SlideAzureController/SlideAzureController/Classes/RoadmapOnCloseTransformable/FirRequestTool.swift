
//: Declare String Begin

/*: "setPassword" :*/
fileprivate let main_withdrawMsg:String = "harvest jump see usersetPas"
fileprivate let k_harassmentPath:[Character] = ["d"]

/*: "user/appleLogin" :*/
fileprivate let showBeamTitle:[Character] = ["u"]
fileprivate let dataCosLieKey:String = "glass scale concentration artser/"
fileprivate let k_justiceTitle:String = "Logineveryone cloud"

/*: "user/googleLogin" :*/
fileprivate let app_versionLocalId:String = "leave mark global naturaluser/go"
fileprivate let appSkipWayKey:String = "at"
fileprivate let dataBeyondKey:String = "gleLoginroad skirt trend invite user"

/*: "user/smsLogin" :*/
fileprivate let constShapeMessage:String = "scope here customer data corneruser/s"
fileprivate let dataStaffPrivacyExhibitUrl:[Character] = ["g","i","n"]

/*: "user/emailVcodeLogin" :*/
fileprivate let notiAgainWithinData:[Character] = ["u","s","e","r","/","e","m","a","i","l","V","c","o","d","e","L","o","g","i","n"]

/*: "user/pwdLogin" :*/
fileprivate let app_resignName:String = "coatser"
fileprivate let noti_verticalValue:String = "complygin"

/*: "user/emailPwdLogin" :*/
fileprivate let mainRegardSafetyStr:[Character] = ["u","s"]
fileprivate let app_closedTitle:String = "er/empow vital assistant double back"
fileprivate let show_failTitle:String = "wdLoginconnection explain water"

/*: "mobile" :*/
fileprivate let dataOfInvolvementMessage:[UInt8] = [0xf6,0xf8,0xeb,0xf2,0xf5,0xee]

fileprivate func lieMin(better num: UInt8) -> UInt8 {
    let value = Int(num) - 137
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "areaCode" :*/
fileprivate let data_harvestKey:[UInt8] = [0xf2,0x3,0xf6,0xf2,0xd4,0x0,0xf5,0xf6]

fileprivate func ownShine(brush num: UInt8) -> UInt8 {
    let value = Int(num) + 111
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "user/sendLoginSms" :*/
fileprivate let noti_failUrl:String = "user/both distribute variety long"
fileprivate let show_meStr:String = "active clearlynSms"

/*: "RSA加密失败" :*/
fileprivate let kPermissionId:String = "RSA加密失败appear device motivation regard situation"

/*: "password" :*/
fileprivate let kRequireFormat:[UInt8] = [0x3,0xf4,0x6,0x6,0xa,0x2,0x5,0xf7]

fileprivate func reCreation(successfully num: UInt8) -> UInt8 {
    let value = Int(num) + 109
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "vcode" :*/
fileprivate let showEqualStr:[UInt8] = [0x5,0x10,0x1c,0x17,0x16]

private func lockRegular(burn num: UInt8) -> UInt8 {
    return num ^ 115
}

/*: "user/modifyPassword" :*/
fileprivate let dataSumTitle:String = "mystery explain pow ago frameuser/m"
fileprivate let showVolumeUrl:String = "odformationf"

/*: "user/sendModifyPwdSms" :*/
fileprivate let noti_streetEarnPath:String = "uconcentrationr"
fileprivate let constCookiePackPath:String = "glass both blank written dominantModi"
fileprivate let const_lightPath:[Character] = ["f","y","P","w","d","S","m","s"]

/*: "user/sendModifyPwdEmail" :*/
fileprivate let k_itPath:String = "adjustment skirtuser/s"
fileprivate let showStrikeName:String = "slip add beautifulendM"
fileprivate let const_timeValue:String = "PwdEnone combine particle"

/*: "mf/user/getMyInfo" :*/
fileprivate let const_toneUrl:[Character] = ["m","f","/","u","s","e","r","/","g","e","t","M","y","I","n","f","o"]

/*: "invite/bindInviter" :*/
fileprivate let app_passageValue:String = "ichannelvi"
fileprivate let notiCutKey:String = "ndIndaily lip bright manage"
fileprivate let k_portraitStr:String = "vitcleanr"

/*: "invite/isBind" :*/
fileprivate let userBeastKey:String = "instrike"
fileprivate let noti_kitStr:String = "pole formation still storageBind"

/*: "mf/user/CheckInviteCode" :*/
fileprivate let dataSheUrl:String = "reduce file s bearmf/u"
fileprivate let k_includeEnrollUrl:String = "eckIbear exception"
fileprivate let userHolderFormat:String = "eCodemode analyze middle background wall"

/*: "mf/user/updateInfo" :*/
fileprivate let userPureBusyData:String = "mf/user/discussion clean more topic condition"
fileprivate let mainWithdrawPath:String = "uinviteate"

/*: "email" :*/
fileprivate let notiDocMysteryFormat:[UInt8] = [0x9b,0x93,0x9f,0x97,0x92]

/*: "user/getEmailBindVCode" :*/
fileprivate let k_dealName:[Character] = ["u","s","e","r","/","g","e","t","E","m","a","i","l","B","i","n","d"]
fileprivate let show_trunkThatKey:String = "listener side printVCode"

/*: "user/sendLoginEmail" :*/
fileprivate let show_leagueName:String = "usehear"
fileprivate let constBucketKey:String = "oginEmaildistribution personality"

/*: "user/emailBind" :*/
fileprivate let k_mustName:String = "user/come hang sweep area income"
fileprivate let show_audienceFormat:String = "Bindkingdom equity orange con"

/*: "user/logout" :*/
fileprivate let notiSaveerText:String = "user/losalt thumb moment ignore"
fileprivate let notiAwakePath:String = "goquickt"

/*: "app/countryList" :*/
fileprivate let mainRemoveMessage:String = "app/cowithout sufficient success calendar"
fileprivate let app_cookieMessage:String = "compete view networkList"

/*: "user/getMobileBindVCode" :*/
fileprivate let appGrayKey:String = "usautomatically"
fileprivate let mainOurPath:String = "bilrefuse"
fileprivate let show_expectedContent:[Character] = ["V","C","o","d","e"]

/*: "user/mobileBind" :*/
fileprivate let main_resignTitle:[Character] = ["u","s","e","r","/","m","o","b","i","l","e","B","i","n"]
fileprivate let showReaderBeautyMsg:String = "stack"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  FirRequestTool.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/29.
//

//: import UIKit
import UIKit

//: class TalkingLoginRequestTool: NSObject {
class FirRequestTool: NSObject {
    /// 第三方登录请求
    /// - Parameters:
    ///   - type: 登录类型
    ///   - content: 未加密手机号/邮箱
    ///   - params: 参数
    ///   - completion: 回调

    //: class func req_thirdLogin(type: LoginType, content: String = "", params: [String: String], completion: @escaping FinishBlock) {
    class func replaceCompletion(type: ContainerCustomReflectable, content: String = "", params: [String: String], completion: @escaping FinishBlock) {
        //: let reqModel = self.req_thirdLoginModel(type: type, params: params)
        let reqModel = self.angstromUnitParams(type: type, params: params)
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
                //: return
                return
            }
            // 跳转设置密码页
            //: let json = JSON(result ?? [:])
            let json = JSON(result ?? [:])
            //: if json["setPassword"].boolValue == true {
            if json[(String(main_withdrawMsg.suffix(6)) + "swor" + String(k_harassmentPath))].boolValue == true {
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
                //: PushThen.share.func__pushToSetNewPasswordVC(type: type, content: content, isNewPwd: true)
                PushThen.share.toValid(type: type, content: content, isNewPwd: true)
                //: return
                return
            }

            // 获取用户信息
            //: TalkingLoginRequestTool.req_LoginUserInfoData(type, completion: completion)
            FirRequestTool.listener(type, completion: completion)
        }
    }

    /// 第三方登录请求模型
    /// - Parameters:
    ///   - type: 登录类型
    ///   - params: 参数
    /// - Returns: 请求模型
    //: private class func req_thirdLoginModel(type: LoginType, params: [String: String]) -> TalkingRequestModel {
    private class func angstromUnitParams(type: ContainerCustomReflectable, params: [String: String]) -> ProcessingThen {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: switch type {
        switch type {
        //: case .AppleLogin:
        case .AppleLogin:
            //: reqModel.requestPath = "user/appleLogin"
            reqModel.requestPath = (String(showBeamTitle) + String(dataCosLieKey.suffix(4)) + "apple" + String(k_justiceTitle.prefix(5)))
        //: case .GoogleLogin:
        case .GoogleLogin:
            //: reqModel.requestPath = "user/googleLogin"
            reqModel.requestPath = (String(app_versionLocalId.suffix(7)) + appSkipWayKey.replacingOccurrences(of: "at", with: "o") + String(dataBeyondKey.prefix(8)))
        //: case .PhoneLogin:
        case .PhoneLogin:
            //: reqModel.requestPath = "user/smsLogin"
            reqModel.requestPath = (String(constShapeMessage.suffix(6)) + "msLo" + String(dataStaffPrivacyExhibitUrl))
        //: case .EmailLogin:
        case .EmailLogin:
            //: reqModel.requestPath = "user/emailVcodeLogin"
            reqModel.requestPath = (String(notiAgainWithinData))
        //: case .PwdPhoneLogin:
        case .PwdPhoneLogin:
            //: reqModel.requestPath = "user/pwdLogin"
            reqModel.requestPath = (app_resignName.replacingOccurrences(of: "coat", with: "u") + "/pwdL" + noti_verticalValue.replacingOccurrences(of: "comply", with: "o"))
        //: case .PwdEmailLogin:
        case .PwdEmailLogin:
            //: reqModel.requestPath = "user/emailPwdLogin"
            reqModel.requestPath = (String(mainRegardSafetyStr) + String(app_closedTitle.prefix(5)) + "ailP" + String(show_failTitle.prefix(7)))
        }
        //: reqModel.params = params
        reqModel.params = params

        //: return reqModel
        return reqModel
    }

    /// 获取验证码
    /// - Parameters:
    ///   - type: 登录类型
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_getPhoneCode( phoneStr: String, areaStr: String, completion: @escaping FinishBlock) {
    class func focusProfile(phoneStr: String, areaStr: String, completion: @escaping FinishBlock) {
        //: var reslutStr = ""
        var reslutStr = ""
        //: var reslutCodeStr = ""
        var reslutCodeStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: main_saveName)
            //: let clear = try ClearMessage(string: phoneStr, using: .utf8)
            let clear = try ClearMessage(string: phoneStr, using: .utf8)
            //: reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            //: reslutCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let params = ["mobile": reslutStr, "areaCode": reslutCodeStr]
            let params = [String(bytes: dataOfInvolvementMessage.map{lieMin(better: $0)}, encoding: .utf8)!: reslutStr, String(bytes: data_harvestKey.map{ownShine(brush: $0)}, encoding: .utf8)!: reslutCodeStr]
            //: let reqModel = TalkingRequestModel.init()
            let reqModel = ProcessingThen()
            //: reqModel.requestType = .POST
            reqModel.requestType = .POST
            //: reqModel.requestPath = "user/sendLoginSms"
            reqModel.requestPath = (String(noti_failUrl.prefix(5)) + "sendLogi" + String(show_meStr.suffix(4)))
            //: reqModel.params = params
            reqModel.params = params
            //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
            const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
            }

            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (String(kPermissionId.prefix(7))))
        }
    }

    /// 密码登录
    /// - Parameters:
    ///   - phoneStr: 手机号登录
    ///   - areaStr: 区号
    ///   - pwd: 密码
    ///   - completion: 回调
    //: class func req_passwordLogin(phoneStr: String, areaStr: String, pwd: String, completion: @escaping FinishBlock) {
    class func follow(phoneStr: String, areaStr: String, pwd: String, completion: @escaping FinishBlock) {
        //: var resultPhoneStr = ""
        var resultPhoneStr = ""
        //: var resultCodeStr = ""
        var resultCodeStr = ""
        //: var resultPwdStr = ""
        var resultPwdStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: main_saveName)
            //: let clear = try ClearMessage(string: phoneStr, using: .utf8)
            let clear = try ClearMessage(string: phoneStr, using: .utf8)
            //: resultPhoneStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            resultPhoneStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            //: resultCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            resultCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let pwdClear = try ClearMessage(string: "\(pwd)", using: .utf8)
            let pwdClear = try ClearMessage(string: "\(pwd)", using: .utf8)
            //: resultPwdStr = try pwdClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            resultPwdStr = try pwdClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let params = ["mobile": resultPhoneStr, "areaCode": resultCodeStr, "password": resultPwdStr]
            let params = [String(bytes: dataOfInvolvementMessage.map{lieMin(better: $0)}, encoding: .utf8)!: resultPhoneStr, String(bytes: data_harvestKey.map{ownShine(brush: $0)}, encoding: .utf8)!: resultCodeStr, String(bytes: kRequireFormat.map{reCreation(successfully: $0)}, encoding: .utf8)!: resultPwdStr]
            //: TalkingLoginRequestTool.req_thirdLogin(type: .PwdPhoneLogin, params: params, completion: completion)
            FirRequestTool.replaceCompletion(type: .PwdPhoneLogin, params: params, completion: completion)
            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (String(kPermissionId.prefix(7))))
        }
    }

    /// 修改密码
    /// - Parameters:
    ///   - pwd: 密码
    ///   - vcode: 验证码
    ///   - completion: 回调
    //: class func req_modifyPassword(pwd: String, vcode: String?, completion: @escaping FinishBlock) {
    class func funVcodeCorruptPassword(pwd: String, vcode: String?, completion: @escaping FinishBlock) {
        //: var resultPwdStr = ""
        var resultPwdStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: main_saveName)
            //: let pwdClear = try ClearMessage(string: "\(pwd)", using: .utf8)
            let pwdClear = try ClearMessage(string: "\(pwd)", using: .utf8)
            //: resultPwdStr = try pwdClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            resultPwdStr = try pwdClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            //: var params = ["password": resultPwdStr]
            var params = [String(bytes: kRequireFormat.map{reCreation(successfully: $0)}, encoding: .utf8)!: resultPwdStr]

            //: if vcode?.count ?? 0 > 0 {
            if vcode?.count ?? 0 > 0 {
                //: params["vcode"] = vcode
                params[String(bytes: showEqualStr.map{lockRegular(burn: $0)}, encoding: .utf8)!] = vcode
            }

            //: let reqModel = TalkingRequestModel()
            let reqModel = ProcessingThen()
            //: reqModel.requestType = .POST
            reqModel.requestType = .POST
            //: reqModel.requestPath = "user/modifyPassword"
            reqModel.requestPath = (String(dataSumTitle.suffix(6)) + showVolumeUrl.replacingOccurrences(of: "formation", with: "i") + "yPassword")
            //: reqModel.params = params
            reqModel.params = params
            //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
            const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
            }

            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (String(kPermissionId.prefix(7))))
        }
    }

    /// 获取修改【手机号】密码验证码
    /// - Parameter completion: 回调
    //: class func req_sendModifyPwdSms(completion: @escaping FinishBlock) {
    class func positive(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ProcessingThen()
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.requestPath = "user/sendModifyPwdSms"
        reqModel.requestPath = (noti_streetEarnPath.replacingOccurrences(of: "concentration", with: "se") + "/send" + String(constCookiePackPath.suffix(4)) + String(const_lightPath))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取修改【邮箱】密码验证码
    /// - Parameter completion: 回调
    //: class func req_sendModifyPwdEmail(completion: @escaping FinishBlock) {
    class func email(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ProcessingThen()
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.requestPath = "user/sendModifyPwdEmail"
        reqModel.requestPath = (String(k_itPath.suffix(6)) + String(showStrikeName.suffix(4)) + "odify" + String(const_timeValue.prefix(4)) + "mail")
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取用户信息接口
    /// - Parameters:
    ///   - type: 登录类型
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_LoginUserInfoData(_ type: LoginType, completion: FinishBlock? = nil) {
    class func listener(_ type: ContainerCustomReflectable, completion: FinishBlock? = nil) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "mf/user/getMyInfo"
        reqModel.requestPath = (String(const_toneUrl))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: completion?(succeed, result, errorModel)
                completion?(succeed, result, errorModel)
                //: return
                return
            }
            //: Defaults.set(result, forKey: TalkingLoginUserInfoCacheKey)
            data_partyText.set(result, forKey: appErrTitle)
            //: if let userModel = JSONDeserializer<LoginUserModel>.deserializeFrom(dict: result as? Dictionary<String, Any>, designatedPath: nil) {
            if let userModel = JSONDeserializer<InmateRegularReactiveCompatible>.deserializeFrom(dict: result as? [String: Any], designatedPath: nil) {
                //: DismissReactiveCompatible.share.loginUserMode = userModel
                DismissReactiveCompatible.share.loginUserMode = userModel

                // 记录上次登录类型
                //: Defaults.set(type.rawValue, forKey: TalkingLastLoginTypeCacheKey)
                data_partyText.set(type.rawValue, forKey: userOffdAgentValue)
                //: if DismissReactiveCompatible.share.loginUserMode.updateInfo == false {
                if DismissReactiveCompatible.share.loginUserMode.updateInfo == false { // 需要完善资料
                    //: completion?(false, nil, errorModel)
                    completion?(false, nil, errorModel)
                    //: PushThen.share.func__pushToFillUserInfoVC()
                    PushThen.share.duringNudgeAlbum()
                    //: return
                    return
                }

                //: completion?(succeed, result, errorModel)
                completion?(succeed, result, errorModel)
                //: NotificationCenter.default.post(name: DID_LOGIN_SUCCESS_NOTIFICATION, object: nil, userInfo: result as! Dictionary<String, Any>)
                NotificationCenter.default.post(name: noti_groupUrl, object: nil, userInfo: result as! [String: Any])

                //: if DismissReactiveCompatible.share.loginUserMode.remindBindEmail == true {
                if DismissReactiveCompatible.share.loginUserMode.remindBindEmail == true { // 需要绑定邮箱
                    //: DispatchQueue.main.asyncAfter(deadline: .now()+0.5) {
                    DispatchQueue.main.asyncAfter(deadline: .now() + 0.5) {
                        //: PushThen.share.func__pushToLockUserEmailVC(isShowBack: false)
                        PushThen.share.tabAcross(isShowBack: false)
                    }
                }
            }
        }
    }

    /// 绑定邀请邀请码
    /// - Parameters:
    ///   - params: 邀请码
    ///   - completion: 回调
    //: class func req_inviteBindInviter(params: [String: String], completion: @escaping FinishBlock) {
    class func sum(params: [String: String], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "invite/bindInviter"
        reqModel.requestPath = (app_passageValue.replacingOccurrences(of: "channel", with: "n") + "te/bi" + String(notiCutKey.prefix(4)) + k_portraitStr.replacingOccurrences(of: "clean", with: "e"))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 是否已经绑定过邀请码
    /// - Parameters:
    ///   - params: 邀请码
    ///   - completion: 回调
    //: class func req_isBindInviter(completion: @escaping FinishBlock) {
    class func alongContain(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "invite/isBind"
        reqModel.requestPath = (userBeastKey.replacingOccurrences(of: "strike", with: "vi") + "te/is" + String(noti_kitStr.suffix(4)))

        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 检测邀请码
    /// - Parameters:
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_CheckCodeData(params: [String: String], completion: @escaping FinishBlock) {
    class func titlePage(params: [String: String], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "mf/user/CheckInviteCode"
        reqModel.requestPath = (String(dataSheUrl.suffix(4)) + "ser/Ch" + String(k_includeEnrollUrl.prefix(4)) + "nvit" + String(userHolderFormat.prefix(5)))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 更新用户信息
    /// - Parameters:
    ///   - type: 登录类型
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_updateUserInfo(params: [String: Any], completion: @escaping FinishBlock) {
    class func userInfoReloadPushCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "mf/user/updateInfo"
        reqModel.requestPath = (String(userPureBusyData.prefix(8)) + mainWithdrawPath.replacingOccurrences(of: "invite", with: "pd") + "Info")
        //: var temparams = params
        var temparams = params
        //: temparams += UIDevice.deviceInfoDic
        temparams += UIDevice.deviceInfoDic
        //: if let poStr = temparams["p0"] {
        if let poStr = temparams["p0"] {
            //: let token = NSDate.getCurrentTimeStamp()
            let token = NSDate.commonExpress()
            //: reqModel.addHeaderToken = token
            reqModel.addHeaderToken = token
            //: let key = token.appending(TokenSaltStr)
            let key = token.appending(appSkipName)
            //: temparams["p0"] = (poStr as? String)?.encrypt(withKey: key)
            temparams["p0"] = (poStr as? String)?.model(key)
        }
        //: reqModel.params = temparams
        reqModel.params = temparams
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// Email Code
    /// - Parameters:
    ///   - emailStr: Email
    ///   - emailType: Login or Bind
    //: class func req_getBindEmailCode( emailStr: String, isBind: Bool, completion: @escaping FinishBlock) {
    class func bit(emailStr: String, isBind: Bool, completion: @escaping FinishBlock) {
        //: var reslutStr = ""
        var reslutStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: main_saveName)
            //: let clear = try ClearMessage(string: emailStr, using: .utf8)
            let clear = try ClearMessage(string: emailStr, using: .utf8)
            //: reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            //: let params = ["email": reslutStr]
            let params = [String(bytes: notiDocMysteryFormat.map{$0^254}, encoding: .utf8)!: reslutStr]
            //: let reqModel = TalkingRequestModel.init()
            let reqModel = ProcessingThen()
            //: reqModel.requestType = .POST
            reqModel.requestType = .POST
            //: if isBind {
            if isBind {
                //: reqModel.requestPath = "user/getEmailBindVCode"
                reqModel.requestPath = (String(k_dealName) + String(show_trunkThatKey.suffix(5)))
                //: } else {
            } else {
                //: reqModel.requestPath = "user/sendLoginEmail"
                reqModel.requestPath = (show_leagueName.replacingOccurrences(of: "hear", with: "r") + "/sendL" + String(constBucketKey.prefix(9)))
            }
            //: reqModel.params = params
            reqModel.params = params
            //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
            const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
            }

            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (String(kPermissionId.prefix(7))))
        }
    }

    /// 绑定邮箱验证码
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_bindEmailCode( emailCode: String, completion: @escaping FinishBlock) {
    class func bind(emailCode: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "user/emailBind"
        reqModel.requestPath = (String(k_mustName.prefix(5)) + "email" + String(show_audienceFormat.prefix(4)))
        //: reqModel.params = ["vcode": emailCode]
        reqModel.params = [String(bytes: showEqualStr.map{lockRegular(burn: $0)}, encoding: .utf8)!: emailCode]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    //: class func req_passwordEmailLogin(emailStr: String, pwd: String, completion: @escaping FinishBlock) {
    class func adminCompletion(emailStr: String, pwd: String, completion: @escaping FinishBlock) {
        //: var resultEmailStr = ""
        var resultEmailStr = ""
        //: var resultPwdStr = ""
        var resultPwdStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: main_saveName)
            //: let clear = try ClearMessage(string: emailStr, using: .utf8)
            let clear = try ClearMessage(string: emailStr, using: .utf8)
            //: resultEmailStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            resultEmailStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let pwdClear = try ClearMessage(string: "\(pwd)", using: .utf8)
            let pwdClear = try ClearMessage(string: "\(pwd)", using: .utf8)
            //: resultPwdStr = try pwdClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            resultPwdStr = try pwdClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let params = ["email": resultEmailStr, "password": resultPwdStr]
            let params = [String(bytes: notiDocMysteryFormat.map{$0^254}, encoding: .utf8)!: resultEmailStr, String(bytes: kRequireFormat.map{reCreation(successfully: $0)}, encoding: .utf8)!: resultPwdStr]
            //: TalkingLoginRequestTool.req_thirdLogin(type: .PwdEmailLogin, params: params, completion: completion)
            FirRequestTool.replaceCompletion(type: .PwdEmailLogin, params: params, completion: completion)
            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (String(kPermissionId.prefix(7))))
        }
    }

    //: typealias RequestLoginOutBlock = (_ t: Bool) -> Void
    typealias RequestLoginOutBlock = (_ t: Bool) -> Void

    //: @objc class func req_loginOut(completion: @escaping RequestLoginOutBlock) {
    @objc class func allowFor(completion: @escaping RequestLoginOutBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "user/logout"
        reqModel.requestPath = (String(notiSaveerText.prefix(7)) + notiAwakePath.replacingOccurrences(of: "quick", with: "u"))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, _, _ in
            //: completion(succeed)
            completion(succeed)
        }
    }

    //: @objc class func func__requestReloginUserInfo(completion: @escaping RequestLoginOutBlock) {
    @objc class func state(completion: @escaping RequestLoginOutBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "mf/user/getMyInfo"
        reqModel.requestPath = (String(const_toneUrl))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, _ in
            //: if succeed {
            if succeed {
                //: Defaults.set(result, forKey: TalkingLoginUserInfoCacheKey)
                data_partyText.set(result, forKey: appErrTitle)
                //: if let userModel = JSONDeserializer<LoginUserModel>.deserializeFrom(dict: result as? Dictionary<String, Any>, designatedPath: nil) {
                if let userModel = JSONDeserializer<InmateRegularReactiveCompatible>.deserializeFrom(dict: result as? [String: Any], designatedPath: nil) {
                    //: DismissReactiveCompatible.share.loginUserMode = userModel
                    DismissReactiveCompatible.share.loginUserMode = userModel
                }
            }
            //: completion(succeed)
            completion(succeed)
        }
    }

    /// 获取允许国家列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_countryList( completion: @escaping FinishBlock) {
    class func effect(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ProcessingThen()
        //: reqModel.requestPath = "app/countryList"
        reqModel.requestPath = (String(mainRemoveMessage.prefix(6)) + "untry" + String(app_cookieMessage.suffix(4)))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 绑定手机号获取验证码
    //: class func req_bindPhoneGetCode( phoneStr: String, areaStr: String, completion: @escaping FinishBlock) {
    class func who(phoneStr: String, areaStr: String, completion: @escaping FinishBlock) {
        //: var reslutStr = ""
        var reslutStr = ""
        //: var reslutCodeStr = ""
        var reslutCodeStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: main_saveName)
            //: let clear = try ClearMessage(string: phoneStr, using: .utf8)
            let clear = try ClearMessage(string: phoneStr, using: .utf8)
            //: reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            //: reslutCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let params = ["mobile": reslutStr, "areaCode": reslutCodeStr]
            let params = [String(bytes: dataOfInvolvementMessage.map{lieMin(better: $0)}, encoding: .utf8)!: reslutStr, String(bytes: data_harvestKey.map{ownShine(brush: $0)}, encoding: .utf8)!: reslutCodeStr]
            //: let reqModel = TalkingRequestModel.init()
            let reqModel = ProcessingThen()
            //: reqModel.requestType = .POST
            reqModel.requestType = .POST
            //: reqModel.requestPath = "user/getMobileBindVCode"
            reqModel.requestPath = (appGrayKey.replacingOccurrences(of: "automatically", with: "er") + "/getMo" + mainOurPath.replacingOccurrences(of: "refuse", with: "e") + "Bind" + String(show_expectedContent))
            //: reqModel.params = params
            reqModel.params = params
            //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
            const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
            }

            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (String(kPermissionId.prefix(7))))
        }
    }

    /// 绑定手机号
    //: class func req_MobileBind(phoneStr: String, areaStr: String, vcode: String, completion: @escaping FinishBlock) {
    class func micromillimetreAccess(phoneStr: String, areaStr: String, vcode: String, completion: @escaping FinishBlock) {
        //: var reslutStr = ""
        var reslutStr = ""
        //: var reslutCodeStr = ""
        var reslutCodeStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: main_saveName)
            //: let clear = try ClearMessage(string: phoneStr, using: .utf8)
            let clear = try ClearMessage(string: phoneStr, using: .utf8)
            //: reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            //: reslutCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let params = ["mobile": reslutStr, "areaCode": reslutCodeStr, "vcode": vcode]
            let params = [String(bytes: dataOfInvolvementMessage.map{lieMin(better: $0)}, encoding: .utf8)!: reslutStr, String(bytes: data_harvestKey.map{ownShine(brush: $0)}, encoding: .utf8)!: reslutCodeStr, String(bytes: showEqualStr.map{lockRegular(burn: $0)}, encoding: .utf8)!: vcode]
            //: let reqModel = TalkingRequestModel.init()
            let reqModel = ProcessingThen()
            //: reqModel.requestType = .POST
            reqModel.requestType = .POST
            //: reqModel.requestPath = "user/mobileBind"
            reqModel.requestPath = (String(main_resignTitle) + showReaderBeautyMsg.replacingOccurrences(of: "stack", with: "d"))
            //: reqModel.params = params
            reqModel.params = params
            //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
            const_backRateStr.lumbarVertebraContainer(model: reqModel) { succeed, result, errorModel in
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
            }

            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (String(kPermissionId.prefix(7))))
        }
    }
}
