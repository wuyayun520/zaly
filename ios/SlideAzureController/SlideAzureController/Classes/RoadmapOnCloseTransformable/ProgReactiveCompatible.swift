
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let userGiveFormat:[UInt8] = [0x64,0x65,0x74,0x6e,0x65,0x6d,0x65,0x6c,0x70,0x6d,0x69,0x20,0x6e,0x65,0x65,0x62,0x20,0x74,0x6f,0x6e,0x20,0x73,0x61,0x68,0x20,0x29,0x3a,0x72,0x65,0x64,0x6f,0x63,0x28,0x74,0x69,0x6e,0x69]

/*: "Voice greeting" :*/
fileprivate let app_spareValue:[Character] = ["V","o","i","c","e"," ","g","r","e","e","t","i","n"]
fileprivate let user_personMessage:String = "G"

/*: "Add" :*/
fileprivate let userUsText:String = "Addski case i added database"

/*: "type" :*/
fileprivate let main_whatName:String = "trustpe"

/*: "addStatus" :*/
fileprivate let k_orbitTitle:String = "that"
fileprivate let app_recordCertainUrl:String = "player pair lease memoryddStatus"

/*: "list" :*/
fileprivate let noti_meetStr:String = "LIST"

/*: "audio" :*/
fileprivate let noti_addFormat:String = "begindio"

/*: "length" :*/
fileprivate let user_bigFormat:[Character] = ["l","e","n","g","t","h"]

/*: "url" :*/
fileprivate let notiWhiteId:String = "urnumber"

/*: "Successfully added, please wait for approval" :*/
fileprivate let kCapMsg:[UInt8] = [0x4d,0x6b,0x7d,0x7d,0x7b,0x6d,0x6d,0x78,0x6b,0x72,0x72,0x67,0x3e,0x7f,0x7a,0x7a,0x7b,0x7a,0x32,0x3e,0x6e,0x72,0x7b,0x7f,0x6d,0x7b,0x3e,0x69,0x7f,0x77,0x6a,0x3e,0x78,0x71,0x6c,0x3e,0x7f,0x6e,0x6e,0x6c,0x71,0x68,0x7f,0x72]

/*: "Are you sure to delete this message？" :*/
fileprivate let dataPopularMessage:[UInt8] = [0xdd,0xee,0xf9,0xbc,0xe5,0xf3,0xe9,0xbc,0xef,0xe9,0xee,0xf9,0xbc,0xe8,0xf3,0xbc,0xf8,0xf9,0xf0,0xf9,0xe8,0xf9,0xbc,0xe8,0xf4,0xf5,0xef,0xbc,0xf1,0xf9,0xef,0xef,0xfd,0xfb,0xf9,0x73,0x20,0x3]

/*: "Cancel" :*/
fileprivate let app_padData:String = "Canceldisk on vehicle forth"

/*: "Delete" :*/
fileprivate let showMutualUrl:String = "Deletematch mutual demand dismiss"

/*: "Please wait for the audit result" :*/
fileprivate let const_thatUrl:[UInt8] = [0xed,0xd1,0xd8,0xdc,0xce,0xd8,0x9d,0xca,0xdc,0xd4,0xc9,0x9d,0xdb,0xd2,0xcf,0x9d,0xc9,0xd5,0xd8,0x9d,0xdc,0xc8,0xd9,0xd4,0xc9,0x9d,0xcf,0xd8,0xce,0xc8,0xd1,0xc9]

private func barrierYellow(worker num: UInt8) -> UInt8 {
    return num ^ 189
}

/*: "The upper limit has been reached" :*/
fileprivate let userLevelId:[UInt8] = [0x45,0x59,0x56,0x11,0x66,0x61,0x61,0x56,0x63,0x11,0x5d,0x5a,0x5e,0x5a,0x65,0x11,0x59,0x52,0x64,0x11,0x53,0x56,0x56,0x5f,0x11,0x63,0x56,0x52,0x54,0x59,0x56,0x55]

fileprivate func positionEffect(core num: UInt8) -> UInt8 {
    let value = Int(num) - 241
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "filePath" :*/
fileprivate let data_ticketTitle:String = "above cloud involvement passfileP"
fileprivate let const_focusArrayTitle:String = "atmember"

/*: "time" :*/
fileprivate let k_boldFormat:String = "tidatabase"

/*: "#FF506D" :*/
fileprivate let notiDepthBetweenText:String = "nut transmission report#FF506D"

/*: "btn_message_delete_nor" :*/
fileprivate let main_oppositeData:[Character] = ["b","t","n","_","m","e","s","s","a","g","e","_","d","e"]
fileprivate let notiSpareText:String = "lfeede"

/*: "Currently mute" :*/
fileprivate let show_clearlyKey:String = "Curreabsolute pure"
fileprivate let main_wholeFirePath:String = "create object harvest story mute"

/*: "Edit Title" :*/
fileprivate let main_bitMessage:[Character] = ["E","d"]
fileprivate let kPhysicalName:String = "it Titlecommit succeed mean view"

/*: "remark" :*/
fileprivate let mainForwardContent:String = "REMAR"
fileprivate let app_programMessage:[Character] = ["k"]

/*: "Click the button below to add\ncontent" :*/
fileprivate let app_ownRipLivingTitle:[UInt8] = [0x44,0x6b,0x6e,0x64,0x6c,0x27,0x73,0x6f,0x62,0x27,0x65,0x72,0x73,0x73,0x68,0x69,0x27,0x65,0x62,0x6b,0x68,0x70,0x27,0x73,0x68,0x27,0x66,0x63,0x63,0xd,0x64,0x68,0x69,0x73,0x62,0x69,0x73]

private func resettlementCount(variety num: UInt8) -> UInt8 {
    return num ^ 7
}

/*: "Add multiple voice greetings, and the system will send voice messages by turns;\nClick the bubble to play the voice, slide to the voice area to delete it, and the voice title can be edited." :*/
fileprivate let dataShouldText:[UInt8] = [0x2e,0x64,0x65,0x74,0x69,0x64,0x65,0x20,0x65,0x62,0x20,0x6e,0x61,0x63,0x20,0x65,0x6c,0x74,0x69,0x74,0x20,0x65,0x63,0x69,0x6f,0x76,0x20,0x65,0x68,0x74,0x20,0x64,0x6e,0x61,0x20,0x2c,0x74,0x69,0x20,0x65,0x74,0x65,0x6c,0x65,0x64,0x20,0x6f,0x74,0x20,0x61,0x65,0x72,0x61,0x20,0x65,0x63,0x69,0x6f,0x76,0x20,0x65,0x68,0x74,0x20,0x6f,0x74,0x20,0x65,0x64,0x69,0x6c,0x73,0x20,0x2c,0x65,0x63,0x69,0x6f,0x76,0x20,0x65,0x68,0x74,0x20,0x79,0x61,0x6c,0x70,0x20,0x6f,0x74,0x20,0x65,0x6c,0x62,0x62,0x75,0x62,0x20,0x65,0x68,0x74,0x20,0x6b,0x63,0x69,0x6c,0x43,0xa,0x3b,0x73,0x6e,0x72,0x75,0x74,0x20,0x79,0x62,0x20,0x73,0x65,0x67,0x61,0x73,0x73,0x65,0x6d,0x20,0x65,0x63,0x69,0x6f,0x76,0x20,0x64,0x6e,0x65,0x73,0x20,0x6c,0x6c,0x69,0x77,0x20,0x6d,0x65,0x74,0x73,0x79,0x73,0x20,0x65,0x68,0x74,0x20,0x64,0x6e,0x61,0x20,0x2c,0x73,0x67,0x6e,0x69,0x74,0x65,0x65,0x72,0x67,0x20,0x65,0x63,0x69,0x6f,0x76,0x20,0x65,0x6c,0x70,0x69,0x74,0x6c,0x75,0x6d,0x20,0x64,0x64,0x41]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ProgReactiveCompatible.swift
//  AbroadTalking
//
//  Created by Charlotte on 2023/2/9.
//

//: import UIKit
import UIKit

//: class TalkingGreetAudioVC: TalkingBaseViewController {
class ProgReactiveCompatible: WeddingLovelyRecognizerDelegate {
    //: var addStatus = -1
    var addStatus = -1 // 1：可以添加；2：已有正在审核中得音频，不能添加；3：已有20条审核通过的音频，不能添加；4：已有20条审核通过的文字，不能添加；5：已有20条审核中或审核通过的图片，不能添加；
    //: var recordView: TalkingGreetRecordAudioView?
    var recordView: MedalTickToolDelegate?
    //: var isPlaying = false
    var isPlaying = false
    //: var playIndex: IndexPath?
    var playIndex: IndexPath?
    //: var player: TalkingAudioPlayTool?
    var player: TermsPlayerDelegate?
    //: var isMute = false
    var isMute = false
    //: var currenCell: TalkingGreetAudioCell?
    var currenCell: GrantViewCell?
    //: init() {
    init() {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: userGiveFormat.reversed(), encoding: .utf8)!)
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.title = "Voice greeting".localized
        self.title = (String(app_spareValue) + user_personMessage.lowercased()).localized
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.doingTo()
        //: designView()
        work()
        //: getAudioListData()
        audio()
        //: setMutedDetection()
        start()
        //: NotificationCenter.default.addObserver(self, selector: #selector(cancelPlayerAudio(notification:)), name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(theme(notification:)), name: show_logData, object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(interruptionChange(notification:)), name: AVAudioSession.interruptionNotification, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(overAccept(notification:)), name: AVAudioSession.interruptionNotification, object: nil)

        // 添加接收到音视频通话通知
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(stopPlay),
                                               selector: #selector(richPerson),
                                               //: name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION,
                                               name: show_logData,
                                               //: object: nil)
                                               object: nil)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: if isPlaying {
        if isPlaying {
            //: self.player?.stopPlay()
            self.player?.indianFile()
        }
    }

    // MARK: - Lazy load

    //: private lazy var dataSourceArr: [TalkingGreetModel] = {
    private lazy var dataSourceArr: [ConcealedModelType] = {
        //: var array = [TalkingGreetModel]()
        var array = [ConcealedModelType]()
        //: return array
        return array
        //: }()
    }()

    //: lazy var MainTable: UITableView = {
    lazy var MainTable: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight-88), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: main_logStr, height: main_nameData - 88), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: return table
        return table
        //: }()
    }()

    //: lazy var emptyView: EmptyView = {
    lazy var emptyView: EmptyView = {
        //: let view = EmptyView()
        let view = EmptyView()
        //: return view
        return view
        //: }()
    }()

    //: lazy var addButton: UIButton = {
    lazy var addButton: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.setTitle("Add".localized, for: .normal)
        btn.setTitle((String(userUsText.prefix(3))).localized, for: .normal)
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        btn.titleLabel?.font = UIFont.bigGallery(type: .Medium, fontSize: 18)
        //: btn.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: 267, height: 50)), for: .normal)
        btn.setBackgroundImage(UIImage.beforeCan(colors: UIColor.minimizeOn(), size: CGSize(width: 267, height: 50)), for: .normal)
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(addButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(worldWideClick), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingGreetAudioVC {
extension ProgReactiveCompatible {
    //: func getAudioListData() {
    func audio() {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["type"] = 1
        dict[(main_whatName.replacingOccurrences(of: "trust", with: "ty"))] = 1
        //: ProgressHUD.show()
        InsertProgressHUD.momentShow()
        //: TalkingMeRequestTool.req_GetGreetList(params: dict) {[weak self] succeed, result, errorModel in
        FlyerThen.generateCounterrupt(params: dict) { [weak self] succeed, result, _ in

            //: ProgressHUD.dismiss()
            InsertProgressHUD.detectDismiss()
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if (self.MainTable.backgroundView == nil) {
            if self.MainTable.backgroundView == nil {
                //: self.setupEmptyDataView()
                self.about()
            }
            //: if succeed {
            if succeed {
                //: guard let dict = result as? Dictionary<String, Any> else {
                guard let dict = result as? [String: Any] else {
                    //: return
                    return
                }
                //: self.addStatus = dict["addStatus"] as! Int
                self.addStatus = dict[(k_orbitTitle.replacingOccurrences(of: "that", with: "a") + String(app_recordCertainUrl.suffix(8)))] as! Int
                //: let array: Array =  dict["list"] as! Array<Any>
                let array: Array = dict[(noti_meetStr.lowercased())] as! [Any]
                //: var dataArr: [TalkingGreetModel] = []
                var dataArr: [ConcealedModelType] = []
                //: for i in 0..<array.count {
                for i in 0 ..< array.count {
                    //: if let model = JSONDeserializer<TalkingGreetModel>.deserializeFrom(dict: array[i] as? Dictionary<String, Any>, designatedPath: nil) {
                    if let model = JSONDeserializer<ConcealedModelType>.deserializeFrom(dict: array[i] as? [String: Any], designatedPath: nil) {
                        //: dataArr.append(model)
                        dataArr.append(model)
                    }
                }
                //: self.player = nil
                self.player = nil
                //: self.dataSourceArr = dataArr
                self.dataSourceArr = dataArr
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }

    //: func requestUploadAudioFileData(data: NSData, time: String) {
    func merge(data: NSData, time: String) {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["type"] = 1
        dict[(main_whatName.replacingOccurrences(of: "trust", with: "ty"))] = 1
        //: dict["audio"] = data
        dict[(noti_addFormat.replacingOccurrences(of: "begin", with: "au"))] = data
        //: dict["length"] = Int(time)
        dict[(String(user_bigFormat))] = Int(time)

        //: ProgressHUD.show()
        InsertProgressHUD.momentShow()
        //: TalkingMeRequestTool.req_UploadGreetAdd(params: dict) {[weak self] succeed, result, errorModel in
        FlyerThen.subtitle(params: dict) { [weak self] _, result, _ in

            //: ProgressHUD.dismiss()
            InsertProgressHUD.detectDismiss()
            //: guard let self = self else {return}
            guard let self = self else { return }

            //: guard let dict = result as? Dictionary<String, Any> else {
            guard let dict = result as? [String: Any] else {
                //: return
                return
            }
            //: let url: String = dict["url"] as! String
            let url: String = dict[(notiWhiteId.replacingOccurrences(of: "number", with: "l"))] as! String
            //: if !url.isEmptyString {
            if !url.isEmptyString {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Successfully added, please wait for approval".localized)
                self.showGuidanceName(showMsg: String(bytes: kCapMsg.map{$0^30}, encoding: .utf8)!.localized)
                //: self.getAudioListData()
                self.audio()
                //: self.recordView?.dismiss()
                self.recordView?.loadDismiss()
                //: self.recordView = nil
                self.recordView = nil
            }
        }
    }

    //: func shoWdeleteAlert(index: Int) {
    func bag(index: Int) {
        //: let config = ShowAlertConfig()
        let config = PortraitAlertConfig()
        //: config.textFont = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        config.textFont = UIFont.bigGallery(type: .Medium, fontSize: 18)
        //: config.textlineBreakMode = .byCharWrapping
        config.textlineBreakMode = .byCharWrapping

        //: TalkingAlertShow.customAlert(message: "Are you sure to delete this message？".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Delete".localized, leftBlock: {
        AllowanceReactiveCompatible.dateConfig(message: String(bytes: dataPopularMessage.map{$0^156}, encoding: .utf8)!.localized, leftBtnTitle: (String(app_padData.prefix(6))).localized, rightBtnTitle: (String(showMutualUrl.prefix(6))).localized, leftBlock: {
            //: TalkingAlertShow.hideAlert()
            AllowanceReactiveCompatible.minuteOriginBridge()
            //: }, rightBlock: { [weak self] in
        }, rightBlock: { [weak self] in

            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingAlertShow.hideAlert()
            AllowanceReactiveCompatible.minuteOriginBridge()
            //: self.stopPlay()
            self.richPerson()

            //: let model: TalkingGreetModel = self.dataSourceArr[index]
            let model: ConcealedModelType = self.dataSourceArr[index]
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["id"] = model.greetId
            dict["id"] = model.greetId
            //: TalkingMeRequestTool.req_DeleteAudio(params: dict) { succeed, result, errorModel in
            FlyerThen.conGo(params: dict) { succeed, _, _ in
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: self.getAudioListData()
                self.audio()
            }
            //: }, config: config)
        }, config: config)
    }
}

// MARK: - Event

//: extension TalkingGreetAudioVC {
extension ProgReactiveCompatible {
    //: @objc func cancelPlayerAudio(notification: NSNotification) -> Void {
    @objc func theme(notification _: NSNotification) {
        //: stopPlay()
        richPerson()
    }

    /// 播放过程中中断
    //: @objc func interruptionChange(notification: NSNotification) -> Void {
    @objc func overAccept(notification: NSNotification) {
        //: let dic = notification.userInfo as![String: AnyObject]
        let dic = notification.userInfo as! [String: AnyObject]

        //: let changeReason = dic[AVAudioSessionInterruptionTypeKey]
        let changeReason = dic[AVAudioSessionInterruptionTypeKey]
        //: if changeReason?.intValue ?? 0 == AVAudioSession.InterruptionType.began.rawValue {
        if changeReason?.intValue ?? 0 == AVAudioSession.InterruptionType.began.rawValue {
            //: stopPlay()
            richPerson()
        }
    }

    //: @objc func addButtonClick() {
    @objc func worldWideClick() {
        //: guard TalkingSocketManager.shared.isInfo == false else {
        guard SantaLuciaFirSocketDelegate.shared.isInfo == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.enablece(showMsg: kLikeStr)
            //: return
            return
        }

        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        SpecialPermissionTool.visualAspect(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.enablece(showMsg: appMicUrl)
                //: return
                return
            }
            //: if self.addStatus == 2 {
            if self.addStatus == 2 {
                //: self.func__showStatusBarErrorMsg(showMsg: "Please wait for the audit result".localized)
                self.enablece(showMsg: String(bytes: const_thatUrl.map{barrierYellow(worker: $0)}, encoding: .utf8)!.localized)
                //: return
                return
                    //: }else if (self.addStatus == 3) {
            } else if self.addStatus == 3 {
                //: self.func__showStatusBarErrorMsg(showMsg: "The upper limit has been reached".localized)
                self.enablece(showMsg: String(bytes: userLevelId.map{positionEffect(core: $0)}, encoding: .utf8)!.localized)
                //: return
                return
            }
            //: self.stopPlay()
            self.richPerson()
            //: self.recordView = TalkingGreetRecordAudioView.init(frame: self.view.frame)
            self.recordView = MedalTickToolDelegate(frame: self.view.frame)
            //: self.recordView?.show()
            self.recordView?.rootage()
            //: self.recordView?.audioFileUpload = {[weak self] dict in
            self.recordView?.audioFileUpload = { [weak self] dict in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: let data = NSData.init(contentsOfFile: dict["filePath"] as! String)
                let data = NSData(contentsOfFile: dict[(String(data_ticketTitle.suffix(5)) + const_focusArrayTitle.replacingOccurrences(of: "member", with: "h"))] as! String)
                //: guard data != nil else {
                guard data != nil else {
                    //: return
                    return
                }
                //: self.requestUploadAudioFileData(data: data!, time: dict["time"] as! String)
                self.merge(data: data!, time: dict[(k_boldFormat.replacingOccurrences(of: "database", with: "me"))] as! String)
            }
        }
    }
}

//: extension TalkingGreetAudioVC {
extension ProgReactiveCompatible {
    //: func setMutedDetection() {
    func start() {
        //: RBDMuteSwitch.shared.detectMuteSwitch()
        IceThen.shared.taproot()
        //: RBDMuteSwitch.shared.isMutedBlock = { [weak self] mute in
        IceThen.shared.isMutedBlock = { [weak self] mute in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.isMute = mute
            self.isMute = mute
        }
    }
}

// MARK: - UITableViewDelegate, UITableViewDataSource

//: extension TalkingGreetAudioVC: UITableViewDelegate, UITableViewDataSource {
extension ProgReactiveCompatible: UITableViewDelegate, UITableViewDataSource {
    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return 1
        return 1
    }

    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        //: MainTable.bounces = dataSourceArr.count > 0
        MainTable.bounces = dataSourceArr.count > 0
        //: return dataSourceArr.count
        return dataSourceArr.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt _: IndexPath) -> CGFloat {
        //: return 101
        return 101
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let identifier = TalkingGreetAudioCell.className()
        let identifier = GrantViewCell.className()
        //: let cell: TalkingGreetAudioCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! TalkingGreetAudioCell
        let cell: GrantViewCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! GrantViewCell

        //: cell.setGreetAudioCell(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count-1)
        cell.result(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count - 1)

        //: if isPlaying && playIndex?.row == indexPath.section {
        if isPlaying && playIndex?.row == indexPath.section {
            //: cell.syncPlayState(isPlaying: true)
            cell.eye(isPlaying: true)
            //: }else {
        } else {
            //: cell.syncPlayState(isPlaying: false)
            cell.eye(isPlaying: false)
        }

        //: cell.audioPlayActionBlock = { [weak self] in
        cell.audioPlayActionBlock = { [weak self] in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: RBDMuteSwitch.shared.detectMuteSwitch()
            IceThen.shared.taproot()
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                //: self.playAudio(index: indexPath)
                self.rate(index: indexPath)
            }
        }
        //: cell.editActionBlock = { [weak self] in
        cell.editActionBlock = { [weak self] in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.stopPlay()
            self.richPerson()
            //: self.editTitle(index: indexPath.section)
            self.telegraphicSignal(index: indexPath.section)
        }
        //: return cell
        return cell
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_: UITableView, didSelectRowAt _: IndexPath) {}

    // ios11以前
    //: func tableView(_ tableView: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
    func tableView(_: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
        //: let deleteBtn = UITableViewRowAction(style: .normal, title: "Delete".localized) { action, indexP in
        let deleteBtn = UITableViewRowAction(style: .normal, title: (String(showMutualUrl.prefix(6))).localized) { _, _ in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.bag(index: indexPath.section)
        }
        //: deleteBtn.backgroundColor = UIColor(hex: "#FF506D")
        deleteBtn.backgroundColor = UIColor(hex: (String(notiDepthBetweenText.suffix(7))))

        //: return [deleteBtn]
        return [deleteBtn]
    }

    //: @available(iOS 11.0, *)
    @available(iOS 11.0, *)
    //: func tableView(_ tableView: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
    func tableView(_: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
        //: let deleteBtn = UIContextualAction(style: .normal, title: nil) { action, view, completionHandler in
        let deleteBtn = UIContextualAction(style: .normal, title: nil) { _, _, completionHandler in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.bag(index: indexPath.section)
            //: completionHandler(false)
            completionHandler(false)
        }

        //: if let deleteBtnTrashcan =  UIImage.BundleImageNamed(name: "btn_message_delete_nor").cgImage {
        if let deleteBtnTrashcan = UIImage.locationName(name: (String(main_oppositeData) + notiSpareText.replacingOccurrences(of: "feed", with: "et") + "_nor")).cgImage {
            //: deleteBtn.image = ImageWithoutRender(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
            deleteBtn.image = IceWithoutRender(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
        }
        //: deleteBtn.backgroundColor = .white
        deleteBtn.backgroundColor = .white
        //: let actions = [deleteBtn]
        let actions = [deleteBtn]
        //: let config = UISwipeActionsConfiguration(actions: actions)
        let config = UISwipeActionsConfiguration(actions: actions)
        //: config.performsFirstActionWithFullSwipe = false
        config.performsFirstActionWithFullSwipe = false
        //: return config
        return config
    }

    //: func playAudio(index: IndexPath) {
    func rate(index: IndexPath) {
        //: guard !TalkingSocketManager.shared.isInfo else {
        guard !SantaLuciaFirSocketDelegate.shared.isInfo else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.enablece(showMsg: kLikeStr)
            //: return
            return
        }
        //: if isPlaying && playIndex == index {
        if isPlaying && playIndex == index {
            //: isPlaying = false
            isPlaying = false
            //: MainTable.reloadData()
            MainTable.reloadData()
            //: self.player?.stopPlay()
            self.player?.indianFile()
            //: return
            return
        }
        //: if isPlaying {
        if isPlaying {
            //: currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? TalkingGreetAudioCell
            currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? GrantViewCell
            //: if currenCell != nil {
            if currenCell != nil {
                //: currenCell?.syncPlayState(isPlaying: false)
                currenCell?.eye(isPlaying: false)
            }
        }

        //: playIndex = index
        playIndex = index
        //: let model = self.dataSourceArr[index.section]
        let model = self.dataSourceArr[index.section]

        //: if isMute || AVAudioSession.sharedInstance().outputVolume <= 0 {
        if isMute || AVAudioSession.sharedInstance().outputVolume <= 0 {
            //: self.func__showStatusBarErrorMsg(showMsg: "Currently mute".localized)
            self.enablece(showMsg: (String(show_clearlyKey.prefix(5)) + "ntly" + String(main_wholeFirePath.suffix(5))).localized)
            //: return
            return
        }
        //: if self.player == nil {
        if self.player == nil {
            //: self.player = TalkingAudioPlayTool.shared
            self.player = TermsPlayerDelegate.shared
            //: self.player?.delegate = self
            self.player?.delegate = self
        }
        //: self.player?.stopPlay()
        self.player?.indianFile()
        //: self.player?.play(urlStr: model.content)
        self.player?.next(urlStr: model.content)
        //: isPlaying = true
        isPlaying = true
        //: currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? TalkingGreetAudioCell
        currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? GrantViewCell
        //: if currenCell != nil {
        if currenCell != nil {
            //: currenCell?.syncPlayState(isPlaying: true)
            currenCell?.eye(isPlaying: true)
        }
    }

    //: func editTitle(index: Int) {
    func telegraphicSignal(index: Int) {
        //: let view = TalkingEditAlertView.init(title: "Edit Title".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "OK".localized, config: nil)
        let view = GrantReactiveCompatible(title: (String(main_bitMessage) + String(kPhysicalName.prefix(8))).localized, leftBtnTitle: (String(app_padData.prefix(6))).localized, rightBtnTitle: "OK".localized, config: nil)
        //: view.show()
        view.lip()

        //: view.rightBlock = { [weak self] text in
        view.rightBlock = { [weak self] text in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: ProgressHUD.show()
            InsertProgressHUD.momentShow()

            //: var model: TalkingGreetModel = self.dataSourceArr[index]
            var model: ConcealedModelType = self.dataSourceArr[index]
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["id"] = model.greetId
            dict["id"] = model.greetId
            //: dict["remark"] = text
            dict[(mainForwardContent.lowercased() + String(app_programMessage))] = text

            //: TalkingMeRequestTool.req_UpdateAudioRemark(params: dict) { succeed, result, errorModel in
            FlyerThen.guidanceRes(params: dict) { succeed, _, _ in
                //: ProgressHUD.dismiss()
                InsertProgressHUD.detectDismiss()
                //: if succeed {
                if succeed {
                    //: model.remark = text
                    model.remark = text
                    //: self.dataSourceArr[index] = model
                    self.dataSourceArr[index] = model
                    //: self.MainTable.reloadData()
                    self.MainTable.reloadData()
                }
            }
        }
    }

    //: @objc func stopPlay() {
    @objc func richPerson() {
        //: if isPlaying {
        if isPlaying {
            //: isPlaying = false
            isPlaying = false
            //: self.player?.stopPlay()
            self.player?.indianFile()
            //: if currenCell != nil {
            if currenCell != nil {
                //: currenCell?.syncPlayState(isPlaying: false)
                currenCell?.eye(isPlaying: false)
            }
        }
    }
}

//: extension TalkingGreetAudioVC: TalkingAudioPlayToolDelegate {
extension ProgReactiveCompatible: NameureToolDelegate {
    //: func playerStatusChange(status: AudioPlayerStatus) {
    func fertilizationAge(status: DecideWidthMagnitude) {
        //: switch status {
        switch status {
        //: case .PlayerFinish:
        case .PlayerFinish:
            //: stopPlay()
            richPerson()
        //: break
        //: case .PlayerInterruption:
        case .PlayerInterruption:
            //: stopPlay()
            richPerson()
        //: break
        //: default:
        default:
            //: break
            break
        }
    }
}

// MARK: - Layout

//: extension TalkingGreetAudioVC {
extension ProgReactiveCompatible {
    //: private func setupEmptyDataView() {
    private func about() {
        //: var style = EmptyStyle()
        var style = RedEmptyStyle()
        //: style.TipsTitle = "Click the button below to add\ncontent".localized
        style.TipsTitle = String(bytes: app_ownRipLivingTitle.map{resettlementCount(variety: $0)}, encoding: .utf8)!.localized
        //: emptyView = EmptyView.init(frame: self.view.frame, style: style)
        emptyView = EmptyView(frame: self.view.frame, style: style)
        //: emptyView.isHidden = true
        emptyView.isHidden = true
        //: emptyView.isScrollEnabled = false
        emptyView.isScrollEnabled = false
        //: emptyView.backgroundColor = .appBgColor()
        emptyView.backgroundColor = .doingTo()
        //: MainTable.backgroundView = emptyView
        MainTable.backgroundView = emptyView

        //: emptyView.emptyBlock = { [weak self] in
        emptyView.emptyBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.MainTable.mj_header?.beginRefreshing()
            self.MainTable.mj_header?.beginRefreshing()
        }
    }

    //: private func designView() {
    private func work() {
        //: self.view.addSubview(addButton)
        self.view.addSubview(addButton)
        //: addButton.snp.makeConstraints { make in
        addButton.snp.makeConstraints { make in
            //: make.bottom.equalToSuperview().offset(-34)
            make.bottom.equalToSuperview().offset(-34)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 267, height: 50))
            make.size.equalTo(CGSize(width: 267, height: 50))
        }
        //: self.view.addSubview(MainTable)
        self.view.addSubview(MainTable)
        //: MainTable.register(TalkingGreetAudioCell.self, forCellReuseIdentifier: TalkingGreetAudioCell.className())
        MainTable.register(GrantViewCell.self, forCellReuseIdentifier: GrantViewCell.className())
        //: MainTable.separatorStyle = .none
        MainTable.separatorStyle = .none
        //: MainTable.dataSource = self
        MainTable.dataSource = self
        //: MainTable.delegate = self
        MainTable.delegate = self
        //: MainTable.snp.makeConstraints { make in
        MainTable.snp.makeConstraints { make in
            //: make.edges.equalTo(self.view)
            make.edges.equalTo(self.view)
            //: make.bottom.equalTo(addButton.snp.top).offset(-16)
            make.bottom.equalTo(addButton.snp.top).offset(-16)
        }
        //: MainTable.tableFooterView = UIView.init()
        MainTable.tableFooterView = UIView()

        //: let headerView = UIView.init(frame: CGRect.init(x: 0, y: 0, width: ScreenWidth, height: 145))
        let headerView = UIView(frame: CGRect(x: 0, y: 0, width: main_logStr, height: 145))
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.textColor = UIColor.appValueDetailColor()
        label.textColor = UIColor.saveInColor()
        //: label.font = UIFont.pingfangRugularFont(fontSize: 15)
        label.font = UIFont.bonePlace(fontSize: 15)
        //: label.numberOfLines = 0
        label.numberOfLines = 0
        //: let string = "Add multiple voice greetings, and the system will send voice messages by turns;\nClick the bubble to play the voice, slide to the voice area to delete it, and the voice title can be edited.".localized
        let string = String(bytes: dataShouldText.reversed(), encoding: .utf8)!.localized
        //: let attri = NSMutableAttributedString.init(string: string)
        let attri = NSMutableAttributedString(string: string)
        //: let style = NSMutableParagraphStyle.init()
        let style = NSMutableParagraphStyle()
        //: style.lineSpacing = 2
        style.lineSpacing = 2
        //: attri.yy_paragraphStyle = style
        attri.yy_paragraphStyle = style
        //: label.attributedText = attri
        label.attributedText = attri
        //: headerView.addSubview(label)
        headerView.addSubview(label)
        //: label.snp.makeConstraints { make in
        label.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.bottom.equalTo(-16)
            make.bottom.equalTo(-16)
            //: make.top.equalTo(12)
            make.top.equalTo(12)
        }
        //: MainTable.tableHeaderView = headerView
        MainTable.tableHeaderView = headerView
    }
}
